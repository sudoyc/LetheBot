# Long-Running Goal State

- `updated_at`: 2026-08-01 03:20 CST +0800
- `evidence_snapshot_at`: 2026-08-01 03:20 CST +0800 for the completed
  `SEC-INGRESS-01A/01B`, `SEC-BODY-01`, `PRIV-LOG-01`, `WORK-FAIR-01A`,
  `PI-CONC-01A/01B1/01B2`, `PI-OBS-01A/01B/01C/01D`,
  `MEM-MAINT-01A/01B1/01B2/01C1/01C2/01C3`,
  `TOOL-PROD-01A/01B1/01B2/01C1/01D1`,
  `ARCH-APP-01A1/01A2A/01A2B/01B1/01B2A/01B2B/01C/01D`, and
  `GOV-UX-`
  deterministic slices through `GOV-UX-01B27D` and `GOV-UX-01C9B`, including
  the `GOV-UX-01C5E` prerequisite; live behavior evidence remains dated
  2026-07-13/14 and is historical only
- `retention_local_status`: `COMPLETE`; configured
  retention now has D11 defaults, strict background payload validation,
  stage-aware SELECT-only planning, exact `IMMEDIATE` apply, aggregate audit,
  system-scoped preview-confirm, an independent Operations browser panel, and
  passing focused, full-suite, build, preflight, release, and diff gates.
- `LOCAL_CODE_TEST_DOCS`: `COMPLETE`
- `SYNTHETIC_ACCEPTANCE`: `COMPLETE`
- `external_boundary`: `BLOCKED_EXTERNAL`; live Provider/QQ, deployment or
  restart, private DB/raw chat, stopped-service restore/retention execution,
  live soak, and production accessibility sign-off remain unauthorized and
  unexecuted.
- `goal_status`: `BLOCKED_EXTERNAL`
- `program`: `LONG_TERM_P0_P9`
- `program_phase`: `P8_LOCAL_GOVERNANCE_EXPERIENCE`
- `program_phase_status`: `LOCAL_COMPLETE_EXTERNAL_BLOCKED`
- `active_slice`: `GOV-UX-01C9B`
- `active_slice_status`: `DETERMINISTIC_VERIFIED`
- `group_reliability_status`: `DETERMINISTIC_READY_LIVE_UNPROVED`
- `branch`: `main`
- `head`: `cfebb77`
- `worktree`: `main...origin/main` at `cfebb77`; 75 tracked paths are modified
  and 75 paths are untracked under `--untracked-files=all`. The untracked paths
  are
  `docs/governance-ui.md`,
  `migrations/007_pi_turn_model_invocations.sql`,
  `migrations/008_memory_maintenance_proposals.sql`,
  `src/application/background-runtime-service.ts`,
  `src/application/conversation-turn-service.ts`,
  `src/application/turn-application-service.ts`,
  `src/governance/operations-coordinator.ts`,
  `src/governance/query-service.ts`,
  `src/http/application-http-server.ts`,
  `src/http/governance-browser-assets.ts`,
  `src/http/governance-browser-assets/activity.js`,
  `src/http/governance-browser-assets/administration.js`,
  `src/http/governance-browser-assets/app.css`,
  `src/http/governance-browser-assets/app.html`,
  `src/http/governance-browser-assets/app.js`,
  `src/http/governance-browser-assets/display-profile.js`,
  `src/http/governance-browser-assets/explain.js`,
  `src/http/governance-browser-assets/group-summary.js`,
  `src/http/governance-browser-assets/memory-application.js`,
  `src/http/governance-browser-assets/memory-maintenance-transitions.js`,
  `src/http/governance-browser-assets/memory-presentation.js`,
  `src/http/governance-browser-assets/memory-record-mutations.js`,
  `src/http/governance-browser-assets/memory.js`,
  `src/http/governance-browser-assets/privacy.js`,
  `src/http/governance-http-server.ts`,
  `src/http/governance-preview-handle-registry.ts`,
  `src/http/governance-resource-handle-registry.ts`,
  `src/http/governance-scope-handle-registry.ts`,
  `src/ingestion/event-admission-recovery.ts`,
  `src/ingestion/event-ingress-claim.ts`,
  `src/memory/maintenance-candidate-snapshot.ts`,
  `src/memory/maintenance-proposal.ts`,
  `src/operations/doctor.ts`,
  `src/operations/governance-restore-handoff-consumer.ts`,
  `src/operations/governance-restore-handoff.ts`,
  `src/operations/governance-restore-handoff-reader.ts`,
  `src/pi/turn-admission-controller.ts`,
  `src/scripts/copy-governance-browser-assets.ts`,
  `src/storage/memory-maintenance-proposal-repository.ts`,
  `src/tools/builtins/runtime-status.ts`,
  `src/tools/builtins/runtime-tools.ts`,
  `src/tools/builtins/web-fetch-text.ts`,
  `src/tools/builtins/workspace-list.ts`,
  `src/tools/builtins/workspace-read-text.ts`,
  `tests/integration/application-http-characterization.test.ts`,
  `tests/integration/governance-http-wiring.test.ts`,
  `tests/integration/governance-http.test.ts`,
  `tests/integration/pi-concurrency.test.ts`,
  `tests/unit/application/background-runtime-service.test.ts`,
  `tests/unit/application/conversation-turn-service.test.ts`,
  `tests/unit/application/turn-application-service.test.ts`,
  `tests/unit/governance/operations-coordinator.test.ts`,
  `tests/unit/governance/query-service.test.ts`,
  `tests/unit/http/application-http-server.test.ts`,
  `tests/unit/http/governance-preview-handle-registry.test.ts`,
  `tests/unit/http/governance-resource-handle-registry.test.ts`,
  `tests/unit/http/governance-scope-handle-registry.test.ts`,
  `tests/unit/ingestion/event-admission-recovery.test.ts`,
  `tests/unit/ingestion/event-ingress-claim.test.ts`,
  `tests/unit/operations/doctor.test.ts`,
  `tests/unit/operations/governance-restore-handoff-consumer.test.ts`,
  `tests/unit/operations/governance-restore-handoff.test.ts`,
  `tests/unit/operations/governance-restore-handoff-reader.test.ts`,
  `tests/unit/pi/turn-admission-controller.test.ts`,
  `tests/unit/storage/memory-maintenance-proposal-repository.test.ts`,
  `tests/unit/storage/schema-v7-migration.test.ts`,
  `tests/unit/storage/schema-v8-migration.test.ts`,
  `tests/unit/tools/runtime-status.test.ts`,
  `tests/unit/tools/runtime-tools.test.ts`,
  `tests/unit/tools/web-fetch-text.test.ts`,
  `tests/unit/tools/workspace-list.test.ts`,
  `tests/unit/tools/workspace-read-text.test.ts`,
  `tests/unit/workers/memory-conflict.test.ts`,
  `tests/unit/workers/memory-consolidation.test.ts`, and
  `tests/unit/workers/memory-decay.test.ts`. This supersedes the earlier
  inventory and includes the preserved P1-P3 program work.
- `ownership`: all 150 paths currently reported by
  `git status --short --untracked-files=all` are
  recognized current-program work. Every other path remains protected; no
  staging, commit, push, worktree cleanup, reset, or revert is authorized.
- `local_release_gate`: `PASS` on 2026-07-31 in a data-excluded isolated copy.
  The exact `pnpm release:check` chain passed `pnpm typecheck`, `pnpm lint`,
  `pnpm build`, `pnpm release:preflight`, `pnpm test:run`, and
  `git diff --check`; preflight validated four required files plus the
  pnpm/lockfile and schema contract. Vitest reported 137 passed and one skipped
  test file, with 2,869 passed and 10 skipped tests (2,879 total). A separate
  `git diff --check` against the current working tree was also clean. This is
  local synthetic evidence only and does not establish live deployment,
  Provider/QQ behavior, stopped-service restore execution, soak, or production
  accessibility.
- `authorization`: `LOCAL_CODE_TEST_DOCS=AUTHORIZED` and
  `SYNTHETIC_TMP_EVIDENCE=AUTHORIZED`; `COMMITS=NOT_AUTHORIZED`,
  `PUSH=NOT_AUTHORIZED`, `LIVE_PROVIDER=NOT_AUTHORIZED`,
  `LIVE_QQ=NOT_AUTHORIZED`, `LIVE_DEPLOYMENT_OR_RESTART=NOT_AUTHORIZED`,
  `PRIVATE_DB_OR_RAW_CHAT_READ=NOT_AUTHORIZED`, and
  `DESTRUCTIVE_CLEANUP_OR_REVERT=NOT_AUTHORIZED`. Secrets, raw chat text,
  private platform identifiers, live DB rows, and filled live evidence remain
  excluded from repository and intended evidence; the completed C8H section
  records one accidental over-broad command-output path-scope incident.

## Completed Slice: `GOV-UX-01B16C`

- `requirement`: `GOV-UX-01B16C` HTTP-disabled fixed exact-scope Memory-record
  page.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: B16A/B16B now discover record-backed exact scopes and issue
  purpose-isolated future Memory-read handles, but there is no fixed page safe
  for that authority. The existing B12C `listMemory` is a CLI-oriented filtered
  read that defaults to active records, accepts raw selectors and caller limits,
  returns hydrated `MemoryRecord[]`, and exposes raw record, owner, source, and
  evaluator identifiers plus unrestricted title/content. It therefore cannot be
  used as the future HTTP projection.
- `selection_reason`: the completed record catalog/route pair makes its
  purpose-specific handle target the next dependency. A separate query-service
  page is the smallest reversible slice: it proves exact-scope selection and a
  safe DTO before any Memory data route or resource-handle design is introduced.
- `implementation_contract`: add only
  `GovernanceQueryService.listMemoryRecordsForScope(scope)`. A tool scope returns
  a fixed empty page. Global/system predicates require the exact scope and null
  canonical-user, group, and conversation owner columns; user and group
  predicates require their exact owner; a private conversation requires the
  exact conversation and `group_id IS NULL`; a group conversation requires the
  exact conversation and group. Apply that predicate before a fixed 101-row
  probe over every lifecycle state, then return at most 100 entries ordered by
  importance, update time, creation time, and internal-ID tie-breaker. Each
  entry contains only a purpose-bound 16-character record reference, scope kind,
  visibility/sensitivity/authority/kind/state classifications, confidence,
  importance, source/revision counts, dates, and fixed-size redacted title and
  content preview with explicit hidden/redacted/truncated signals. Secret and
  prohibited record text uses one fixed hidden marker while its non-content
  metadata remains discoverable. Raw record/owner/subject/source/evaluator IDs,
  source context, raw scope fields, unrestricted text, selectors, filters, and
  caller limits remain absent. Do not call or change B12C `listMemory`.
- `allowed_paths`: `src/governance/query-service.ts`,
  `tests/unit/governance/query-service.test.ts`, `docs/governance-ui.md`, and this
  checkpoint only.
- `protected_paths`: B12B-B12D CLI/query DTOs and behavior; B16A/B16B catalog,
  issuer, route, and handle semantics; HTTP/session/CSRF/scope/resource/preview
  dispatch; maintenance and Privacy reads/mutations; CLI/QQ/browser code;
  repositories/schema/migrations/dependencies/lockfile; Provider, private/live
  data, deployment, restart, container, persistence format, and every prior
  slice.
- `assertions`: each supported scope returns only its exact records across all
  six lifecycle states, and lookalike/cross-scope/malformed-owner rows remain
  absent. Private and group conversations with the same conversation ID remain
  isolated. Tool scope is empty. Scope filtering precedes the 100/101 bound;
  ordering is stable under tied public sort values. The exact fixed DTO omits all
  raw identifiers and unrestricted/source fields, recursively redacts secret and
  platform-ID text, bounds title to 160 Unicode code points and content preview
  to 512, exposes correct truncation/redaction/hiding signals and aggregate
  source/revision counts, and fixed-hides secret/prohibited text. Repeated reads
  are byte-stable and write-free; integrity/FKs stay clean; B12C behavior,
  catalog discovery, routes, and mutations remain unchanged; `/memory/records`
  remains absent.
- `focused_commands`: one fail-first B16C query-service regression; the final
  focused regression; full query-service unit coverage and governance HTTP
  wiring adjacency; the canonical P8 command; `pnpm typecheck`; scoped ESLint;
  `pnpm release:check`; `git diff --check`; migration hashes; inventory/privacy
  hygiene; and exact three-path private reverse/forward patch proof.
- `schema_dependency_live_boundary`: read-only query-service code, synthetic
  tests, and its canonical contract only. No persistence write, HTTP route,
  schema, migration, dependency, lockfile, governance mutation, Provider, QQ,
  private DB, deployment, restart, container, or browser operation is permitted.
- `rollback_boundary`: the exact B16B baseline for the three allowed
  product/test paths is privately captured byte-for-byte and mode-for-mode under
  mode-`0700` `/tmp/lethebot-gov-ux-01b16c-roundtrip-NdMmvW`; after implementation
  one exact patch must independently prove reverse-first and forward-first
  restoration without touching the workspace.
- `fail_first_evidence`: the focused Vitest run collected 34 query-service
  tests, skipped 33 unrelated cases, and failed only because
  `service.listMemoryRecordsForScope is not a function`. The synthetic fixture
  had already established all exact-scope, lifecycle, bound, privacy, count,
  ordering, no-write, integrity, and FK assertions; no production source or
  canonical behavior document had been edited.
- `final_evidence`: the focused final regression passed 1 / 1 with 33 skipped,
  the full query-service file passed 34 / 34, query/service/HTTP/scope-registry
  adjacency passed 4 files / 71 tests, and the canonical P8 gate passed 6 files
  / 284 tests. Typecheck, scoped ESLint, and `pnpm release:check` passed; release
  recorded 132 / 1 skipped files and 2700 / 10 skipped tests. The standalone
  page applies exact global/user/group/private-conversation/group-conversation/
  system predicates before the 101-row probe across every lifecycle state,
  returns a maximum 100 entries in stable importance/update/create/ID order,
  uses only 16-character record references, bounds fully redacted text to
  160/512 Unicode code points, fixed-hides secret/prohibited text, exposes only
  fixed classifications/counts/dates, and performs zero writes. Raw owner,
  record, subject, source, evaluator, scope, session, platform, and secret values
  stay absent; B12C, catalogs, routes, and mutations remain unchanged, including
  absent `/memory/records`. The exact three-path patch is
  `/tmp/lethebot-gov-ux-01b16c-roundtrip-NdMmvW/gov-ux-01b16c.patch`
  (`sha256:84260a29924ad87fb64d6ad08a05b16c2daaa12325922f463af3732e03d9e1be`,
  mode `0600`, size `28092`); its proof root and all trees are mode `0700`,
  independent reverse-first and forward-first cycles restored baseline and
  candidate bytes/modes, and the candidate snapshot matches the workspace.
  Inventory remains 68 tracked plus 49 untracked paths with zero staged;
  `git diff --check` is clean, the patch is ASCII-only, sensitive untracked
  filenames are absent, and migrations 007/008 retain SHA-256
  `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`/
  `95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`.
- `exact_next_action`: completed; expose only the B16C page through a separate
  exact-scope read-only `/memory/records` route that accepts only the B16B
  Memory-read handle and adds no detail or mutation behavior.

## Completed Slice: `GOV-UX-01B16D`

- `requirement`: `GOV-UX-01B16D` exact-scope read-only Memory-record route.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: B16B issues current-session, purpose-bound exact-scope handles
  and B16C owns the fixed identifier-free Memory-record page, but
  `GET /governance/api/v1/memory/records` remains absent, so the scoped read
  workflow cannot reach that page.
- `selection_reason`: discovery and its safe page are independently proved.
  Registering one existing static authorized route is the smallest dependency-
  ordered slice and adds no query, detail, resource handle, mutation, repository,
  or generic HTTP behavior.
- `implementation_contract`: register only read-only
  `GET /governance/api/v1/memory/records` in the existing exact-scope route class
  with purpose `governance.memory.records.read`. Let the shared boundary
  authenticate, reject queries, require and resolve exactly one opaque handle
  against the current session and purpose, and pass only the resolved scope to
  the callback. Call only B16C `listMemoryRecordsForScope(scope)` and return its
  fixed page. Do not add record detail, resource handles, mutations, raw
  selectors, filters, caller limits, or wider maintenance/Privacy authority.
- `allowed_paths`: `src/index.ts`,
  `tests/integration/governance-http-wiring.test.ts`, `docs/architecture.md`,
  `docs/deployment.md`, `docs/governance-ui.md`, `docs/security-privacy.md`, and
  this checkpoint only.
- `protected_paths`: B12C/B16A/B16B/B16C query-service, catalog, DTO, bounds,
  redaction, and handle semantics; `GovernanceHttpServer`;
  `GovernanceScopeHandleRegistry`; generic session/CSRF/scope/resource/preview
  dispatch; maintenance/Privacy/Activity routes and mutations; CLI/QQ/browser
  code; config/repositories/schema/migrations/dependencies/lockfile; Provider,
  private/live data, deployment, restart, container, persistence format, and
  every prior slice.
- `assertions`: unauthenticated access is `401`; missing or malformed scope and
  a query are `400`; unknown, cross-session, or cross-purpose authority is
  `404`; all rejections make zero B16C calls. A B16B handle returns `200` and
  passes only its resolved exact scope to B16C. The response preserves the fixed
  100/101 bound, stable order, ISO dates, record references, classifications,
  counts, bounded redaction, restricted-text hiding, and truncation signals
  while omitting raw record/owner/subject/source/evaluator/session/platform/
  secret values and unrestricted fields. Repeated reads are byte-stable and
  write-free; integrity/FKs stay clean; discovery, maintenance, Privacy, and
  Activity behavior remain exact; no Memory detail/mutation route appears; and
  the listener remains default-unreachable.
- `focused_commands`: fail-first then final single-route integration coverage;
  full governance wiring plus query/service/HTTP/scope-registry adjacency; the
  canonical P8 command; `pnpm typecheck`; scoped ESLint; `pnpm release:check`;
  `git diff --check`; migration hashes; inventory/privacy hygiene; and exact
  six-path private reverse/forward patch proof.
- `schema_dependency_live_boundary`: read-only route wiring, synthetic tests,
  and canonical docs only. No query-service behavior, persistence write, Memory
  detail or mutation, schema, migration, dependency, lockfile, Provider, QQ,
  private DB, deployment, restart, container, or browser operation is permitted.
- `rollback_boundary`: the exact B16C baseline for all six product/test paths is
  privately captured byte-for-byte and mode-for-mode under mode-`0700`
  `/tmp/lethebot-gov-ux-01b16d-roundtrip-sFHkDD`; after implementation one exact
  patch must independently prove reverse-first and forward-first restoration
  without touching the workspace.
- `fail_first_evidence`: the focused integration run collected 20 tests,
  skipped 19 unrelated cases, and failed because unauthenticated
  `GET /governance/api/v1/memory/records` returned `404` instead of the
  required `401`. The B16C query method and scope-handle resolver remained
  uncalled, proving the route is absent before production or canonical route
  documentation changes.
- `final_evidence`: the focused final regression passed 1 / 1 with 19 skipped,
  full governance HTTP wiring passed 20 / 20, query/service/HTTP/scope-registry
  adjacency passed 4 files / 72 tests, and the canonical six-file P8 gate
  passed 284 / 284. Typecheck, scoped ESLint, and `pnpm release:check` passed;
  release recorded 132 / 1 skipped files and 2701 / 10 skipped tests. The route
  now accepts only a current-session B16B Memory-read handle, passes only the
  resolved exact scope to B16C, and returns its fixed bounded page unchanged.
  Authentication/query/scope failures occur before B16C; maintenance and
  Privacy handles fail closed; repeated reads are byte-stable and write-free;
  record detail remains `404`; and integrity/FKs stay clean. The exact six-path
  patch is
  `/tmp/lethebot-gov-ux-01b16d-roundtrip-sFHkDD/gov-ux-01b16d.patch`
  (`sha256:1a0a98d458e17da83e5eb72f671fb39a1f16fb9038fcba6366abc565f5d28bae`,
  mode `0600`, size `21882`); its proof root and all trees are mode `0700`,
  independent reverse-first and forward-first cycles restored baseline and
  candidate bytes/modes, and the candidate snapshot matches the workspace.
  Inventory remains 68 tracked plus 49 untracked paths with zero staged;
  `git diff --check` is clean, added lines are ASCII while the patch has one
  pre-existing non-ASCII context line, sensitive untracked filenames are
  absent, and migrations 007/008 retain SHA-256
  `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`/
  `95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`.
- `exact_next_action`: completed; the required two-slice P8 reassessment selects
  `GOV-UX-01B17A`, a fixed HTTP-disabled exact-scope Memory record/source/
  revision/audit detail projection before any resource-handle or detail route.

## Completed Slice: `GOV-UX-01B17A`

- `requirement`: `GOV-UX-01B17A` HTTP-disabled exact-scope Memory provenance
  detail.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: B16D makes the bounded Memory list reachable, but the required
  record/source/revision provenance detail remains unavailable. B12B
  `showMemory(memoryId)` accepts a raw selector, returns raw or merely redacted
  record/owner/source/revision/evaluator identifiers and unrestricted snapshot
  structures, and does not bound sources or revisions, so it is not a safe HTTP
  projection.
- `selection_reason`: after the B16C/B16D page-and-route pair, operator task 3
  is the earliest incomplete read workflow. Proving the fixed detail DTO and
  exact-scope query before issuing any resource handle preserves the established
  projection-before-route sequence and is smaller than combining query,
  registry, and HTTP behavior.
- `implementation_contract`: add only
  `GovernanceQueryService.getMemoryRecordDetailForScope({ scope, memoryId })`.
  The internal memory ID is future trusted resource-registry input, never a
  browser selector. Tool scope and missing or cross-scope records return null.
  Apply the same exact global/user/group/private-conversation/group-conversation/
  system predicate as B16C before reading detail. Reuse the fixed B16C record
  projection. Read sources, revisions, and matching memory audit rows through
  separate 33-row probes, return at most 32 of each in stable source-time/ID,
  revision-number/ID, and audit-time/ID order, and expose explicit truncation.
  Source entries contain only a purpose-bound source reference, fixed source/
  resolution/extractor classifications, and timestamp. Revision entries contain
  only a purpose-bound revision reference, revision number, fixed change/actor
  classifications, optional valid lifecycle transition, bounded redacted reason
  with signals, evaluator-link boolean, and timestamp. Audit entries contain
  only a purpose-bound audit reference, bounded redacted classifications and
  summary with signals, evaluator-link boolean, timestamp, and fixed hidden-
  details signal. Secret/prohibited record text, revision reasons, and audit
  summaries use the fixed restricted marker. Raw record/owner/subject/source/
  revision/audit/evaluator/link identifiers, source context, structured state
  snapshots, audit details, selectors, caller limits, and unrestricted text are
  absent. Do not call or change B12B `showMemory`, issue handles, or register a
  route.
- `allowed_paths`: `src/governance/query-service.ts`,
  `tests/unit/governance/query-service.test.ts`, `docs/governance-ui.md`, and
  this checkpoint only.
- `protected_paths`: B12B/B12C/B16A-D methods, DTOs, bounds, routes, catalogs,
  and handle semantics; HTTP/session/CSRF/scope/resource/preview dispatch;
  maintenance/Privacy/Activity behavior; CLI/QQ/browser code; repositories/
  schema/migrations/dependencies/lockfile; governance mutations; Provider,
  private/live data, deployment, restart, container, persistence format, and
  every prior slice.
- `assertions`: every supported exact scope returns only the requested record
  when both ID and complete owner tuple match; lookalike and cross-scope records,
  private/group conversation collisions, tool scope, and missing IDs return
  null before provenance reads. The record matches B16C exactly. Each provenance
  collection proves scope-before-detail selection, stable 32/33 bounds and tie
  ordering, fixed references/classifications/dates, restricted hiding, bounded
  recursive redaction, and omission of raw IDs, link columns, snapshots,
  details, platform identifiers, secrets, and unrestricted text. Repeated reads
  are byte-stable and write-free; integrity/FKs stay clean; B12B/B16C behavior,
  routes, handles, and mutations remain unchanged; no detail route appears.
- `focused_commands`: one fail-first B17A query-service regression; final
  focused regression; full query-service coverage and Memory route adjacency;
  the canonical P8 command; `pnpm typecheck`; scoped ESLint;
  `pnpm release:check`; `git diff --check`; migration hashes; inventory/privacy
  hygiene; and exact three-path private reverse/forward patch proof.
- `schema_dependency_live_boundary`: read-only query-service code, synthetic
  tests, and its canonical contract only. No persistence write, issuer, HTTP
  route, schema, migration, dependency, lockfile, governance mutation, Provider,
  QQ, private DB, deployment, restart, container, or browser operation is
  permitted.
- `rollback_boundary`: the exact B16D baseline for the three allowed
  product/test paths is privately captured byte-for-byte and mode-for-mode under
  mode-`0700` `/tmp/lethebot-gov-ux-01b17a-roundtrip-P2prSJ`; after
  implementation one exact patch must independently prove reverse-first and
  forward-first restoration without touching the workspace.
- `fail_first_evidence`: the focused Vitest run collected 35 query-service
  tests, skipped 34 unrelated cases, and failed only because
  `service.getMemoryRecordDetailForScope is not a function`. The synthetic
  fixture had already established exact-scope/null behavior, all three 32/33
  bounds, stable ordering, safe normal and restricted projections, legacy-read
  isolation, zero writes, integrity, and FKs; no production source or canonical
  behavior document had been edited.
- `final_evidence`: the focused final regression passed 1 / 1 with 34 skipped,
  the full query-service file passed 35 / 35, query-service/Memory-route
  adjacency passed 2 files / 55 tests, and the canonical six-file P8 gate
  passed 285 / 285. Typecheck, scoped ESLint, and `pnpm release:check` passed;
  release recorded 132 / 1 skipped files and 2702 / 10 skipped tests. Exact
  record-and-scope selection now precedes every provenance read; a match reuses
  the B16C record projection with exact windowed source/revision totals and
  independently returns at most 32 stable source, revision, and audit entries
  from 33-row probes. Purpose-bound references, fixed classifications, bounded
  redaction, restricted-text hiding, and explicit truncation are preserved while
  raw owner/record/source/revision/audit/evaluator/link identifiers, snapshots,
  audit details, and unrestricted text remain absent. B12B/B16C are unchanged,
  no handle or route was added, repeated reads are stable and write-free, and
  integrity/FKs stay clean. The exact three-path patch is
  `/tmp/lethebot-gov-ux-01b17a-roundtrip-P2prSJ/gov-ux-01b17a.patch`
  (`sha256:25413c97ff80c7089825869fb0089f3b5a9f9502ddbc6257eaac53a8d08cdf48`,
  mode `0600`, size `34212`); its proof root and all trees are mode `0700`,
  independent reverse-first and forward-first cycles restored baseline and
  candidate bytes/modes, and the candidate snapshot matches the workspace.
  Inventory remains 68 tracked plus 49 untracked paths with zero staged;
  `git diff --check` is clean, the patch is ASCII-only, sensitive untracked
  filenames are absent, and migrations 007/008 retain SHA-256
  `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`/
  `95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`.
- `exact_next_action`: completed; add an HTTP-disabled bounded exact-scope
  Memory-record resource-handle page before changing the B16D list route or
  registering a detail route.

## Completed Slice: `GOV-UX-01B17B`

- `requirement`: `GOV-UX-01B17B` HTTP-disabled bounded exact-scope Memory-record
  resource-handle page.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: B17A accepts a trusted internal memory ID, but B16C/B16D expose
  only a non-authoritative display reference. A future detail route therefore
  has no bounded, session-bindable resource authority, and recovering an
  internal ID from `recordRef` would break its one-way display-only contract.
- `selection_reason`: the fixed detail projection is independently proved. A
  separate query-service handle page is the smallest next dependency: it can
  prove that only records selected by the existing exact-scope 100/101 page are
  offered to a trusted issuer before any session wiring or detail route exists.
- `implementation_contract`: add only
  `GovernanceQueryService.listMemoryRecordResourceHandlePage(scope, issueHandle)`.
  Reuse the exact B16C scope predicate, all-state 101-row probe, stable ordering,
  record projector, redaction, counts, and truncation. Tool scope returns a fixed
  empty page and makes no issuer call. For each of at most 100 selected rows,
  pass only its normalized exact scope and internal memory ID to the trusted
  issuer, then add only its opaque `handle` and numeric `handleExpiresAt` to the
  unchanged B16C record DTO. Ignore issuer extras. The future binding uses the
  existing Memory-read purpose and a distinct `memory_record` resource kind,
  but B17B changes no registry, session ownership, HTTP route, or B16D response.
- `allowed_paths`: `src/governance/query-service.ts`,
  `tests/unit/governance/query-service.test.ts`, `docs/governance-ui.md`, and
  this checkpoint only.
- `protected_paths`: B12B/B12C/B16A-D/B17A methods, DTO semantics, bounds,
  routes, catalogs, and handle behavior; resource/scope/preview registries;
  HTTP/session/CSRF dispatch; maintenance/Privacy/Activity behavior; CLI/QQ/
  browser code; repositories/schema/migrations/dependencies/lockfile;
  governance mutations; Provider, private/live data, deployment, restart,
  container, persistence format, and every prior slice.
- `assertions`: the issuer sees exactly the internal IDs and normalized scopes
  for the bounded B16C-selected rows in stable order, never an omitted,
  cross-scope, malformed-owner, conversation-collision, or 101st row. Tool and
  empty scopes issue nothing. Stripping `handle` and `handleExpiresAt` yields the
  exact B16C page; issuer extras, raw IDs/scope/session values, secrets, and
  unrestricted fields stay absent. A real resource registry proves stable live
  binding plus cross-session, cross-purpose, cross-kind, and cross-scope denial.
  Repeated reads are write-free with clean integrity/FKs; B17A, B16D, routes,
  registries, and mutations remain unchanged, and no detail route appears.
- `focused_commands`: one fail-first B17B query-service regression; final
  focused regression; full query-service and resource-registry adjacency;
  Memory HTTP-route adjacency; the canonical P8 command; `pnpm typecheck`;
  scoped ESLint; `pnpm release:check`; `git diff --check`; migration hashes;
  inventory/privacy hygiene; and exact three-path private reverse/forward patch
  proof.
- `schema_dependency_live_boundary`: query-service code, synthetic tests, and
  canonical docs only. No persistence write, HTTP/session/registry behavior,
  route, schema, migration, dependency, lockfile, governance mutation, Provider,
  QQ, private DB, deployment, restart, container, or browser operation is
  permitted.
- `rollback_boundary`: the exact B17A candidate for the three allowed
  product/test paths is privately captured byte-for-byte and mode-for-mode under
  mode-`0700` `/tmp/lethebot-gov-ux-01b17b-roundtrip-cYkLx2`; after
  implementation one exact patch must independently prove reverse-first and
  forward-first restoration without touching the workspace.
- `fail_first_evidence`: the focused Vitest run collected 36 query-service
  tests, skipped 35 unrelated cases, and failed only because
  `service.listMemoryRecordResourceHandlePage is not a function`. The synthetic
  fixture had already established exact-scope 100/101 selection, B16C DTO
  parity, supported-scope normalization, private/group conversation isolation,
  issuer-input bounds, real registry isolation, stable reissuance, no-write
  behavior, integrity, and FKs; no production source or canonical behavior
  document had been edited.
- `final_evidence`: the focused final regression passed 1 / 1 with 35 skipped,
  query-service/resource-registry adjacency passed 2 files / 39 tests,
  query-service/Memory-route adjacency passed 2 files / 56 tests, and the
  canonical six-file P8 gate passed 286 / 286. Typecheck, scoped ESLint, and
  `pnpm release:check` passed; release recorded 132 / 1 skipped files and 2703 /
  10 skipped tests. B16C and B17B now use one exact-scope bounded row selector;
  the new page gives the trusted issuer only each selected internal memory ID
  and normalized exact scope, then adds only its opaque handle and numeric
  expiry to the unchanged B16C DTO. The real registry proves stable reissuance
  plus cross-session, cross-purpose, cross-kind, and cross-scope denial. Tool,
  empty, malformed-owner, cross-scope, conversation-collision, and 101st rows
  issue no authority; issuer extras and raw identifiers remain absent. B17A,
  B16D, application/session/registry behavior, and routes remain unchanged;
  repeated reads perform zero writes and integrity/FKs stay clean. The exact
  three-path patch is
  `/tmp/lethebot-gov-ux-01b17b-roundtrip-cYkLx2/gov-ux-01b17b.patch`
  (`sha256:bdbbcc51609aa90b9e54c75ad2e5c5c9f2f5c9d6f6edc9abf7e7bbc1b40c7fe8`,
  mode `0600`, size `21587`); its proof root and all trees are mode `0700`,
  independent reverse-first and forward-first cycles restored baseline and
  candidate bytes/modes, and the candidate snapshot matches the workspace.
  Inventory remains 68 tracked plus 49 untracked paths with zero staged;
  `git diff --check` is clean, the patch is ASCII-only, sensitive untracked
  filenames are absent, and migrations 007/008 retain SHA-256
  `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`/
  `95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`.
- `exact_next_action`: completed; wire the existing `/memory/records` route to
  B17B through current-session `governance.memory.records.read` resource
  bindings of kind `memory_record`, while keeping the record-detail route
  absent for a separate slice.

## Completed Slice: `GOV-UX-01B17C`

- `requirement`: `GOV-UX-01B17C` current-session Memory-record resource-handle
  list wiring.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: B17B proves the bounded resource-handle projection, but the
  existing exact-scope `GET /governance/api/v1/memory/records` route still calls
  B16C and returns no authoritative record handle. B17A therefore remains
  intentionally unreachable without inventing a raw or reversible selector.
- `selection_reason`: the page, session callback metadata, resource registry,
  and Memory-read purpose already exist. Replacing one route delegation with
  the B17B page and an injected registry issuer is the smallest dependency-
  ordered slice; registering the detail route remains a separate authorization
  and dispatch change.
- `implementation_contract`: add only the `memory_record` resource-kind
  constant and change the existing `/memory/records` handler to call
  `GovernanceQueryService.listMemoryRecordResourceHandlePage(scope, issuer)`.
  The issuer binds each B17B-selected internal memory ID and normalized exact
  scope to the callback's current session digest and expiry, the existing
  `governance.memory.records.read` purpose, and resource kind `memory_record`.
  Return the B17B page unchanged. Do not register a resource route, resolve a
  record handle, call B17A, or change the HTTP server or registry.
- `allowed_paths`: `src/index.ts`,
  `tests/integration/governance-http-wiring.test.ts`, `docs/architecture.md`,
  `docs/deployment.md`, `docs/governance-ui.md`, `docs/security-privacy.md`, and
  this checkpoint only.
- `protected_paths`: B12B/B12C/B16A-D/B17A/B17B query methods, DTOs, predicates,
  bounds, redaction, and catalog semantics; scope/resource/preview registries;
  `GovernanceHttpServer` session/CSRF/scope/resource dispatch; Memory detail and
  mutation routes; maintenance/Privacy/Activity behavior; CLI/QQ/browser code;
  config/repositories/schema/migrations/dependencies/lockfile; Provider,
  private/live data, deployment, restart, container, persistence format, and
  every prior slice.
- `assertions`: unauthenticated, query-bearing, missing/malformed/unknown,
  cross-session, and cross-purpose scope requests fail before B17B or resource
  issuance. A valid B16B handle passes only its resolved exact scope to B17B;
  each returned record handle is bound to the current session digest/expiry,
  existing Memory-read purpose, `memory_record` kind, exact normalized scope,
  and internal record ID. The response preserves B16C fields, bounds, stable
  order, redaction, restricted hiding, dates, and truncation while adding only
  opaque handle/expiry fields. Repeated same-session reads reuse stable handles;
  raw IDs, session values, secrets, and unrestricted fields remain absent. The
  legacy B16C route delegate and B17A are uncalled, record detail remains `404`,
  reads perform zero writes, integrity/FKs stay clean, and the listener remains
  default-unreachable.
- `focused_commands`: one fail-first then final B17C route regression; full
  governance HTTP wiring; query-service/resource-registry/HTTP/scope-registry
  adjacency; the canonical P8 command; `pnpm typecheck`; scoped ESLint;
  `pnpm release:check`; `git diff --check`; migration hashes; inventory/privacy
  hygiene; and exact six-path private reverse/forward patch proof.
- `schema_dependency_live_boundary`: application composition wiring, synthetic
  tests, and canonical route docs only. No query-service, HTTP-server, registry,
  persistence write, Memory detail/mutation route, schema, migration,
  dependency, lockfile, Provider, QQ, private DB, deployment, restart,
  container, or browser operation is permitted.
- `rollback_boundary`: the exact B17B candidate for all six product/test paths
  is privately captured byte-for-byte and mode-for-mode under mode-`0700`
  `/tmp/lethebot-gov-ux-01b17c-roundtrip-fUgVQE`; after implementation one exact
  patch must independently prove reverse-first and forward-first restoration
  without touching the workspace.
- `fail_first_evidence`: the focused integration run collected 20 tests,
  skipped 19 unrelated cases, and failed only because
  `listMemoryRecordResourceHandlePage` was called zero rather than one time.
  The authenticated exact-scope request still returned `200` through the
  legacy B16C route delegation, while every negative boundary assertion before
  that point passed; no production source or canonical behavior document had
  been edited.
- `final_evidence`: the focused final regression passed 1 / 1 with 19 skipped,
  full governance HTTP wiring passed 20 / 20, query/resource/scope/HTTP
  adjacency passed 4 files / 57 tests, and the canonical six-file P8 gate passed
  286 / 286. Typecheck, scoped ESLint, and `pnpm release:check` passed; release
  recorded 132 / 1 skipped files and 2703 / 10 skipped tests. The existing
  exact-scope route now calls only B17B and binds every selected internal memory
  ID and normalized exact scope to the callback's current session digest and
  expiry, existing Memory-read purpose, and `memory_record` kind. Its response
  preserves the bounded B16C projection while adding only stable opaque handles
  and numeric expiries. Authentication, query, malformed/unknown/cross-session/
  cross-purpose scope rejection occurs before B17B or issuance; the legacy B16C
  route delegate and B17A remain uncalled, a returned handle's detail path stays
  `404` without resource resolution, repeated reads are byte-stable and write-
  free, and integrity/FKs stay clean. The exact six-path patch is
  `/tmp/lethebot-gov-ux-01b17c-roundtrip-fUgVQE/gov-ux-01b17c.patch`
  (`sha256:78326da798758b2f3133e7572a2ae110e311066f9b719c710a10237ea67d8e40`,
  mode `0600`, size `17968`); its proof root and all trees are mode `0700`,
  independent reverse-first and forward-first cycles restored baseline and
  candidate bytes/modes, and the candidate snapshot matches the workspace.
  Inventory remains 68 tracked plus 49 untracked paths with zero staged;
  `git diff --check` is clean, the patch is ASCII-only, sensitive untracked
  filenames are absent, and migrations 007/008 retain SHA-256
  `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`/
  `95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`.
- `exact_next_action`: completed; expose B17A through one read-only
  `/memory/records/:resourceHandle` route that accepts only B17C's current-
  session Memory-read `memory_record` authority and adds no mutation behavior.

## Completed Slice: `GOV-UX-01B17D`

- `requirement`: `GOV-UX-01B17D` read-only exact-scope Memory provenance-detail
  route.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: B17C returns current-session, purpose/kind/scope-bound record
  handles, and B17A owns the fixed bounded provenance detail, but no HTTP route
  resolves that authority. A returned handle therefore still reaches `404` and
  the required record/source/revision detail workflow remains incomplete.
- `selection_reason`: projection, authority issuance, shared resource
  resolution, and session revocation are independently proved. Registering one
  read-only resource route and delegating its resolved inputs to B17A is the
  smallest remaining Memory-read slice and introduces no new selector,
  registry, query, mutation, or HTTP primitive.
- `implementation_contract`: register only read-only
  `GET /governance/api/v1/memory/records/:resourceHandle` with the existing
  `governance.memory.records.read` purpose and resource kind `memory_record`.
  Let the shared boundary authenticate, reject queries, require and resolve the
  exact-scope handle, and resolve the resource handle against the current
  session, purpose, kind, and scope. Pass only the resolved exact scope and
  internal resource ID to `getMemoryRecordDetailForScope`; return its fixed
  detail as `200` or fixed `404` when absent. Add no mutation route.
- `allowed_paths`: `src/index.ts`,
  `tests/integration/governance-http-wiring.test.ts`, `docs/architecture.md`,
  `docs/deployment.md`, `docs/governance-ui.md`, `docs/security-privacy.md`, and
  this checkpoint only.
- `protected_paths`: B12B/B12C/B16A-D/B17A-C query methods, DTOs, predicates,
  bounds, redaction, catalogs, and list-route response; scope/resource/preview
  registries; `GovernanceHttpServer` session/CSRF/scope/resource dispatch;
  Memory mutations; maintenance/Privacy/Activity behavior; CLI/QQ/browser code;
  config/repositories/schema/migrations/dependencies/lockfile; Provider,
  private/live data, deployment, restart, container, persistence format, and
  every prior slice.
- `assertions`: unauthenticated, query-bearing, missing/malformed/unknown, cross-
  session, cross-purpose, cross-kind, and cross-scope authority fails before
  B17A. A valid B17C handle resolves only under the current session, Memory-read
  purpose, `memory_record` kind, and exact scope, and the handler passes only
  that scope plus internal memory ID to B17A. The response preserves the B17A
  record/source/revision/audit bounds, stable order, references,
  classifications, dates, redaction, restricted hiding, and truncation signals
  while omitting raw record/owner/source/revision/audit/evaluator/session/
  platform/secret values and unrestricted fields. A resolved missing record is
  `404`; repeated reads are byte-stable and write-free; integrity/FKs stay
  clean; B17C list behavior is unchanged; no Memory mutation route appears; and
  the listener remains default-unreachable.
- `focused_commands`: one fail-first then final B17D detail-route regression;
  full governance HTTP wiring; query-service/resource-registry/HTTP/scope-
  registry adjacency; the canonical P8 command; `pnpm typecheck`; scoped
  ESLint; `pnpm release:check`; `git diff --check`; migration hashes;
  inventory/privacy hygiene; and exact six-path private reverse/forward patch
  proof.
- `schema_dependency_live_boundary`: application composition wiring, synthetic
  tests, and canonical route docs only. No query-service, HTTP-server, registry,
  persistence write, Memory mutation, schema, migration, dependency, lockfile,
  Provider, QQ, private DB, deployment, restart, container, or browser
  operation is permitted.
- `rollback_boundary`: the exact B17C candidate for all six product/test paths
  is privately captured byte-for-byte and mode-for-mode under mode-`0700`
  `/tmp/lethebot-gov-ux-01b17d-roundtrip-0yx2wE`; after implementation one exact
  patch must independently prove reverse-first and forward-first restoration
  without touching the workspace.
- `fail_first_evidence`: after correcting a test-only parse error before any
  behavioral result, the focused integration run collected 20 tests, skipped
  19 unrelated cases, and failed only because unauthenticated
  `GET /governance/api/v1/memory/records/<resource-handle>` returned `404`
  instead of the required `401`. No resource resolution or B17A call occurred,
  proving the dynamic route is absent before production or canonical-doc edits.
- `final_evidence`: the focused B17D regression passed 1 / 1 with 19 skipped;
  full governance HTTP wiring passed 20 / 20; four-file query/resource/scope/
  HTTP adjacency passed 57 / 57; and the canonical P8 gate passed 286 / 286.
  `pnpm typecheck`, scoped ESLint, and `pnpm release:check` passed, with 132 / 1
  skipped files and 2703 / 10 skipped tests. The route now accepts only B17C's
  current-session Memory-read `memory_record` authority, passes only the
  resolved exact scope and internal resource ID to B17A, returns its fixed
  bounded detail or fixed `404`, and remains stable, write-free, and FK-clean.
  Strict reverse-first and forward-first application restored the exact B17C
  baseline and B17D candidate across all six paths byte-for-byte and mode-for-
  mode at `/tmp/lethebot-gov-ux-01b17d-roundtrip-0yx2wE/gov-ux-01b17d.patch`
  (`sha256:73f63879fb3ef527dc0192f7a14f2e0ad55090fc1a79be4ed678c511706daf8d`,
  mode `0600`, size `20158`) with proof-root/tree mode `0700`; candidate/
  workspace equality, the 68 tracked plus 49 untracked and zero-staged
  inventory, clean diff, ASCII-only patch, sensitive-filename hygiene, and
  unchanged migration hashes all passed.
- `exact_next_action`: completed; the P8 reassessment selects
  `GOV-UX-01B18A`, an HTTP-disabled bounded Explain conversation-scope handle
  catalog, before any Explain data route. Capture a fresh baseline and prove
  exact-scope selection, fixed identifier-free projection, trusted issuer
  inputs, purpose isolation, bounds, stable ordering, and zero writes first.

## Completed Slice: `GOV-UX-01B18A`

- `requirement`: `GOV-UX-01B18A` HTTP-disabled bounded Explain conversation-
  scope handle catalog.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: B13A-B13C own the shared stored-context, linked action/tool,
  and explicit/latest turn-resolution Explain reads, but there is no bounded
  way for an HTTP session to discover exact conversations that have durable
  Explain evidence. A future Explain route therefore has no identifier-free,
  purpose-isolated authority source.
- `selection_reason`: `context_traces` is the narrow durable authority because
  it stores the exact private/group conversation tuple and replayable Explain
  evidence. Bare `agent_turns` lacks conversation type and group ID, so treating
  it as discovery authority would collapse private/group scopes or advertise
  turns that have no stored trace. A query-service-only catalog is the smallest
  reversible dependency before any Explain data route or registry wiring.
- `implementation_contract`: add only
  `GovernanceQueryService.listExplainConversationScopeHandles(issueHandle)` and
  its fixed types. Read distinct, valid conversation tuples from
  `context_traces` joined to the referenced `agent_turns`; require the trace and
  turn conversation IDs to match, require private rows to have null group ID,
  and require group rows to have a valid group ID. Validate conversation and
  group identifiers as text, 1-256 characters, clean under the complete
  supported trim set, and free of NUL/C0/DEL controls. Normalize and deduplicate
  before a 101-row probe, order by the latest trace time descending and then the
  deterministic conversation tuple, and return at most 100 entries. Pass only
  the exact conversation scope to a trusted issuer. Return only fingerprint,
  fixed conversation scope kind/type/label, opaque handle, numeric expiry, and
  explicit truncation; use the `explain-conversation-scope` fingerprint domain.
  Do not call ContextBuilder, rebuild a trace, or change B13A-B13C behavior.
- `allowed_paths`: `src/governance/query-service.ts`,
  `tests/unit/governance/query-service.test.ts`, `docs/governance-ui.md`, and
  this checkpoint only.
- `protected_paths`: B13A-B13C stored/action/tool/turn-resolution methods and
  DTOs; CLI rebuild behavior; scope/resource/preview registries and all HTTP
  application/server routing; Memory, maintenance, Privacy, and Activity reads
  or mutations; QQ/browser/config/repositories/schema/migrations/dependencies/
  lockfile; Provider, private/live data, deployment, restart, container,
  persistence format, and every prior slice.
- `assertions`: valid private and group trace-backed conversations are emitted
  as separate exact scopes, including the same conversation ID in both types;
  duplicates retain the most recent trace ordering and deduplicate before the
  100/101 bound. Padded, blank, control-bearing, non-text, and overlong IDs;
  missing/invalid group IDs; private rows with group IDs; trace/turn
  conversation mismatches; and turns without a stored trace are omitted. The
  trusted issuer receives only the exact normalized scope and issuer extras are
  ignored. Fingerprints and handles are stable across repeated reads, handles
  resolve only for the issuing session and future
  `governance.explain.turns.read` purpose, output contains no raw identifiers,
  secrets, session values, or unexpected fields, reads make zero writes, and
  integrity/FKs remain clean. No Explain route is registered.
- `focused_commands`: one fail-first then final B18A query-service regression;
  full query-service coverage; query/scope-registry/HTTP/wiring non-exposure
  adjacency; the canonical P8 command; `pnpm typecheck`; scoped ESLint;
  `pnpm release:check`; `git diff --check`; migration hashes; inventory/privacy
  hygiene; and exact three-path private reverse/forward patch proof.
- `schema_dependency_live_boundary`: read-only query-service code, synthetic
  tests, and its canonical contract only. No persistence write, route,
  registry implementation, ContextBuilder call, schema, migration, dependency,
  lockfile, CLI, QQ, Provider, private DB, deployment, restart, container, or
  browser operation is permitted.
- `rollback_boundary`: the exact B17D candidate for the three allowed
  product/test paths is privately captured byte-for-byte and mode-for-mode under
  mode-`0700` `/tmp/lethebot-gov-ux-01b18a-roundtrip-xJMtlR`; after
  implementation one exact patch must independently prove reverse-first and
  forward-first restoration without touching the workspace.
- `fail_first_evidence`: the focused Vitest run collected 37 query-service
  tests, skipped 36 unrelated cases, and failed only because
  `service.listExplainConversationScopeHandles is not a function`. All valid,
  malformed, deduplication, bound, issuer, privacy, no-write, integrity, and FK
  fixtures were established before production source or canonical behavior
  documentation changed.
- `final_evidence`: the focused final regression passed 1 / 1 with 36 skipped,
  the full query-service file passed 37 / 37, query/scope-registry/HTTP/wiring
  adjacency passed 4 files / 75 tests, and the canonical P8 gate passed 6 files
  / 287 tests. Typecheck, scoped ESLint, and `pnpm release:check` passed; release
  recorded 132 / 1 skipped files and 2704 / 10 skipped tests. The catalog uses
  only valid, turn-consistent stored traces, preserves private/group collisions
  as separate exact scopes, validates the complete trim/control/type/length
  boundary, deduplicates by exact tuple before the 101-row probe, orders by
  latest trace then tuple, and returns at most 100 fixed identifier-free entries.
  The trusted issuer receives only each exact scope; repeated handles are stable
  and resolve only for the issuing session and
  `governance.explain.turns.read`. Reads make zero writes, integrity/FKs remain
  clean, B13A-B13C and CLI behavior are unchanged, and no Explain route exists.
  Strict reverse-first and forward-first application restored the exact B17D
  baseline and B18A candidate across all three paths byte-for-byte and mode-for-
  mode at
  `/tmp/lethebot-gov-ux-01b18a-roundtrip-xJMtlR/gov-ux-01b18a.patch`
  (`sha256:e707f06158ff26f88fbd157fc25c0f05fcaa4311429413bc21ab009c480efe3c`,
  mode `0600`, size `25311`) with proof-root/tree mode `0700`; candidate/
  workspace equality, the 68 tracked plus 49 untracked and zero-staged
  inventory, clean diff, ASCII-only patch, sensitive-filename hygiene, unchanged
  dependency versions, and unchanged migration hashes all passed.
- `exact_next_action`: completed; expose only this catalog through a separate
  authenticated read-only `/explain/scopes` route that binds its exact scopes to
  the current session and `governance.explain.turns.read`, while keeping all
  Explain data routes and B13A-B13C behavior unchanged.

## Completed Slice: `GOV-UX-01B18B`

- `requirement`: `GOV-UX-01B18B` authenticated read-only Explain conversation-
  scope discovery route.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: B18A owns the bounded identifier-free catalog and trusted
  exact-scope issuer boundary, but `/governance/api/v1/explain/scopes` is not
  registered. A local-admin session therefore cannot obtain an Explain-purpose
  handle without bypassing the HTTP boundary.
- `selection_reason`: catalog selection, validation, ordering, privacy, and
  issuance inputs are independently proved. Reusing the existing authenticated-
  unscoped route class and scope-handle registry is the smallest dependency-
  ordered slice and adds no Explain data projection or new HTTP primitive.
- `implementation_contract`: register only read-only
  `GET /governance/api/v1/explain/scopes` with discovery purpose
  `governance.explain.turns.scopes`. Let the shared boundary authenticate and
  reject queries or any scope header before dispatch. Call only B18A
  `listExplainConversationScopeHandles`, and bind each issuer input to the
  callback's current session digest/expiry, exact conversation scope, and
  `governance.explain.turns.read`. Return the unchanged B18A catalog. Do not add
  `/explain/turns`, a resource route, trace rebuild, or mutation.
- `allowed_paths`: `src/index.ts`,
  `tests/integration/governance-http-wiring.test.ts`, `docs/architecture.md`,
  `docs/deployment.md`, `docs/governance-ui.md`, `docs/security-privacy.md`, and
  this checkpoint only.
- `protected_paths`: B13A-B13C and B18A query methods, DTOs, SQL, validation,
  ordering, bounds, fingerprint, and issuer semantics; `GovernanceHttpServer`;
  `GovernanceScopeHandleRegistry`; generic session/CSRF/scope/resource/preview
  dispatch; all Explain data routes; Memory, maintenance, Privacy, Activity,
  CLI, QQ, browser, repositories, schema, migrations, dependencies, lockfile,
  Provider, private/live data, deployment, restart, container, persistence
  format, and every prior slice.
- `assertions`: unauthenticated access is `401`; a query or any scope header is
  `400`; every rejection makes zero B18A or issuer calls. An authenticated read
  calls only B18A and passes a trusted issuer that binds every exact private/
  group scope to the current session, expiry, and Explain-read purpose. The
  response preserves B18A's order, bound, labels, fingerprints, handles, expiry,
  and truncation while omitting raw conversation/group/session/platform/secret
  values. Repeated discovery is byte-stable and write-free; handles resolve
  only for the issuing session and Explain-read purpose and fail for Memory,
  Privacy, and maintenance purposes; existing catalogs remain isolated;
  `/explain/turns` remains `404`; integrity/FKs stay clean; and the listener
  remains default-unreachable.
- `focused_commands`: one fail-first then final B18B integration regression;
  full governance HTTP wiring; query/scope-registry/HTTP/wiring adjacency; the
  canonical P8 command; `pnpm typecheck`; scoped ESLint; `pnpm release:check`;
  `git diff --check`; migration hashes; inventory/privacy hygiene; and exact
  six-path private reverse/forward patch proof.
- `schema_dependency_live_boundary`: application composition wiring, synthetic
  tests, and canonical route docs only. No query-service, HTTP-server, registry,
  persistence write, Explain data route, schema, migration, dependency,
  lockfile, governance mutation, CLI, QQ, Provider, private DB, deployment,
  restart, container, or browser operation is permitted.
- `rollback_boundary`: the exact B18A candidate for all six product/test paths
  is privately captured byte-for-byte and mode-for-mode under mode-`0700`
  `/tmp/lethebot-gov-ux-01b18b-roundtrip-IvDn58`; after implementation one exact
  patch must independently prove reverse-first and forward-first restoration
  without touching the workspace.
- `fail_first_evidence`: the focused integration run collected 21 governance
  wiring tests, skipped 20 unrelated cases, and failed only because
  unauthenticated `GET /governance/api/v1/explain/scopes` returned `404` instead
  of the required `401`. Neither B18A nor scope-handle issuance ran, and no
  production source or canonical route document had changed.
- `final_evidence`: the focused final regression passed 1 / 1 with 20 skipped;
  full governance HTTP wiring passed 21 / 21; four-file query/scope-registry/
  HTTP/wiring adjacency passed 76 / 76; and the canonical P8 gate passed 6
  files / 287 tests. `pnpm typecheck`, scoped ESLint, and
  `pnpm release:check` passed, with 132 / 1 skipped files and 2705 / 10 skipped
  tests. The authenticated-unscoped boundary rejects unauthenticated access,
  queries, and scope headers before B18A or issuance. Successful reads call
  only B18A, bind every exact private/group conversation scope to the current
  session digest/expiry and `governance.explain.turns.read`, preserve the fixed
  identifier-free catalog byte-for-byte, and remain write-free. Cross-session,
  Memory, Privacy, and maintenance resolution fails; existing catalogs remain
  isolated; `/explain/turns` remains `404`; and integrity/FKs stay clean.
  Strict reverse-first and forward-first application restored the exact B18A
  baseline and B18B candidate across all six paths byte-for-byte and mode-for-
  mode at
  `/tmp/lethebot-gov-ux-01b18b-roundtrip-IvDn58/gov-ux-01b18b.patch`
  (`sha256:32d7c03c37b66a6322241f07db37bfbb2dfe75f70e525942cd2d7b575ae01d80`,
  mode `0600`, size `21780`) with proof-root/tree mode `0700`; candidate/
  workspace equality, the 68 tracked plus 49 untracked and zero-staged
  inventory, clean diff, ASCII-only patch, sensitive-filename hygiene,
  unchanged dependency versions, and unchanged migration hashes all passed.
- `exact_next_action`: completed; reassessment after the B18A/B18B discovery
  pair selects `GOV-UX-01B18C`, an HTTP-disabled identifier-free exact-
  conversation Explain turn-summary page. Do not expose the raw-ID B13A-B13C
  DTOs or add `/explain/turns` until that separate projection is proved.

## Completed Slice: `GOV-UX-01B18C`

- `requirement`: `GOV-UX-01B18C` HTTP-disabled bounded exact-conversation
  Explain turn-summary page.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: B18B issues exact current-session Explain-read conversation
  authority, but B13A-B13C return CLI-oriented raw turn, context, memory,
  message, action, tool, conversation, group, and sender identifiers. Exposing
  those DTOs would violate the identifier-free HTTP boundary, while no separate
  bounded turn selector currently exists.
- `selection_reason`: after the required B18A/B18B pair reassessment, a fixed
  query-service projection is the smallest dependency before any Explain data
  route. It can select only trace-backed turns inside one exact conversation,
  provide a stable bounded selector, and prove privacy independently of scope
  resolution, resource issuance, full context detail, or browser behavior.
- `implementation_contract`: add only
  `GovernanceQueryService.listExplainTurnsForScope(scope)` and fixed DTOs. For
  a valid exact private/group conversation scope, select distinct
  `agent_turns` that have at least one structurally matching stored
  `context_trace`; the trace and turn conversation IDs must agree, private
  traces must have null group ID, and group traces must match the exact group
  ID. Order by turn start descending and deterministic turn ID, probe 101 rows,
  and return at most 100 entries with explicit truncation. Each entry contains
  only a domain-separated 16-character fingerprint, fixed `Turn` label,
  `traceSource: stored`, the finite turn status, start time, and optional
  completion time. Omit every raw identifier, provider/model/response/token
  value, context body, action/tool payload, and unrestricted text. Do not call
  ContextBuilder, B13A-B13C, issue a handle, or register a route.
- `allowed_paths`: `src/governance/query-service.ts`,
  `tests/unit/governance/query-service.test.ts`, `docs/governance-ui.md`, and
  this checkpoint only.
- `protected_paths`: B13A-B13C and B18A/B18B methods, DTOs, SQL, validation,
  ordering, issuance, CLI behavior, and routes; scope/resource/preview
  registries and all HTTP application/server wiring; Explain detail and trace
  rebuilds; Memory, maintenance, Privacy, Activity, and mutation behavior; QQ,
  browser, config, repositories, schema, migrations, dependencies, lockfile,
  Provider, private/live data, deployment, restart, container, persistence
  format, and every prior slice.
- `assertions`: private/group collisions remain separate; only matching trace-
  backed turns inside the exact scope appear; duplicate traces do not duplicate
  turns; mismatched conversation/group rows and turns without stored traces are
  absent. Non-conversation and malformed scopes return an empty page. Ordering,
  the 100/101 bound, status/date projection, fingerprints, field allowlist,
  identifier/secret omission, repeated-read stability, zero writes, clean
  integrity/FKs, and continued HTTP non-exposure all pass.
- `focused_commands`: one fail-first then final B18C query-service regression;
  full query-service coverage; query/scope-registry/HTTP/wiring non-exposure
  adjacency; canonical P8; typecheck; scoped ESLint; release; diff, migration,
  inventory/privacy, dependency, and exact three-path rollback proof.
- `schema_dependency_live_boundary`: read-only query-service code, synthetic
  tests, and canonical documentation only. No persistence write, route,
  registry, CLI, mutation, ContextBuilder, schema, migration, dependency,
  lockfile, QQ, Provider, private DB, deployment, restart, container, or browser
  operation is permitted.
- `rollback_boundary`: the exact B18B candidate for all three product/test paths
  is freshly captured byte-for-byte and mode-for-mode under mode-`0700`
  `/tmp/lethebot-gov-ux-01b18c-roundtrip-DZ0QR5`; after implementation one exact
  patch must independently prove reverse-first and forward-first restoration
  without touching the workspace.
- `fail_first_evidence`: the focused Vitest run collected 38 query-service
  tests, skipped 37 unrelated cases, and failed only because
  `service.listExplainTurnsForScope is not a function`. Exact private/group
  selection, invalid-row and malformed-scope rejection, duplicate-trace
  deduplication, deterministic ordering, fixed fields, privacy, 100/101 bounds,
  zero writes, integrity, and FK assertions were fixed before production source
  changed.
- `final_evidence`: the focused B18C regression passed 1 / 1 with 37 skipped;
  full query-service coverage passed 38 / 38; four-file query/scope-registry/
  HTTP/wiring adjacency passed 77 / 77; and the canonical P8 gate passed 6
  files / 288 tests. The added Unicode/runtime-type edge regression first
  failed against the initial implementation and then passed. `pnpm typecheck`,
  scoped and full ESLint, and `pnpm release:check` passed, with 132 / 1 skipped
  files and 2706 / 10 skipped tests. The page accepts only structurally valid
  exact private/group conversation scopes, selects only distinct matching
  stored-trace-backed turns before the fixed 101-row probe, keeps private/group
  collisions isolated, rejects invalid IDs/statuses/dates, and returns at most
  100 fixed identifier-free entries in stable start/ID order. It invokes no
  B13A-B13C method, ContextBuilder, registry, issuer, CLI, or route; Explain data
  remains HTTP-disabled and reads make zero writes with clean integrity/FKs.
  Strict reverse-first and forward-first application restored the exact B18B
  baseline and B18C candidate across all three paths byte-for-byte and mode-for-
  mode at
  `/tmp/lethebot-gov-ux-01b18c-roundtrip-DZ0QR5/gov-ux-01b18c.patch`
  (`sha256:5b3d9eb8914c17239210b4215e07cada911863ddedad30a1b651e2fe169dbdf7`,
  mode `0600`, size `21254`) with proof-root/tree mode `0700`; candidate/
  workspace equality, the 68 tracked plus 49 untracked and zero-staged
  inventory, clean diff, ASCII-only patch, sensitive-filename hygiene,
  unchanged dependency versions, and unchanged migration hashes all passed.
- `exact_next_action`: completed; expose only the B18C page through a separate
  exact-scope read-only `/explain/turns` route that accepts only the B18B
  Explain-read handle and adds no detail, resource, rebuild, or mutation
  behavior.

## Completed Slice: `GOV-UX-01B18D`

- `requirement`: `GOV-UX-01B18D` exact-scope read-only Explain turn-summary
  route.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: B18B issues current-session, purpose-bound exact-conversation
  handles and B18C owns the fixed identifier-free turn page, but authenticated
  `GET /governance/api/v1/explain/turns` remains absent. The scoped Explain
  workflow therefore cannot reach the independently proved page.
- `selection_reason`: discovery, authority, and the safe page are independently
  proved. Registering one existing static authorized route is the smallest
  dependency-ordered slice and adds no query, resource handle, detail, rebuild,
  mutation, repository, or generic HTTP behavior.
- `implementation_contract`: register only read-only
  `GET /governance/api/v1/explain/turns` in the existing exact-scope route class
  with purpose `governance.explain.turns.read`. Let the shared boundary
  authenticate, reject queries, require and resolve exactly one opaque handle
  against the current session and purpose, and pass only the resolved scope to
  the callback. Call only B18C `listExplainTurnsForScope(scope)` and return its
  fixed page. Do not expose B13A-B13C, call ContextBuilder, add an Explain
  resource/detail route, rebuild a trace, or add a mutation.
- `allowed_paths`: `src/index.ts`,
  `tests/integration/governance-http-wiring.test.ts`, `docs/architecture.md`,
  `docs/deployment.md`, `docs/governance-ui.md`, `docs/security-privacy.md`, and
  this checkpoint only.
- `protected_paths`: B13A-B13C and B18A-B18C query-service methods, DTOs, SQL,
  validation, ordering, bounds, fingerprints, catalog, and issuance semantics;
  `GovernanceHttpServer`; `GovernanceScopeHandleRegistry`; generic session,
  CSRF, scope, resource, and preview dispatch; Memory, maintenance, Privacy,
  Activity, CLI, QQ, browser, repositories, schema, migrations, dependencies,
  lockfile, Provider, private/live data, deployment, restart, container,
  persistence format, and every prior slice.
- `assertions`: unauthenticated access is `401`; missing or malformed scope and
  a query are `400`; unknown, cross-session, or cross-purpose authority is
  `404`; all rejections make zero B18C calls. A B18B handle returns `200` and
  passes only its resolved exact private/group conversation scope to B18C. The
  response preserves B18C's fixed 100/101 bound, stable order, fingerprints,
  labels, trace marker, finite statuses, and ISO dates while omitting raw turn,
  trace, context, conversation, group, sender, memory, message, action, tool,
  Provider, model, response, token, session, platform, and secret values.
  Repeated reads are byte-stable and write-free; integrity/FKs stay clean;
  discovery, Memory, maintenance, Privacy, and Activity behavior remains exact;
  no Explain detail/resource/rebuild/mutation route appears; and the listener
  remains default-unreachable.
- `focused_commands`: one fail-first then final B18D integration regression;
  full governance HTTP wiring; query/scope-registry/HTTP/wiring adjacency; the
  canonical P8 command; typecheck; scoped ESLint; release; diff, migration,
  inventory/privacy, dependency, and exact six-path rollback proof.
- `schema_dependency_live_boundary`: application composition wiring, synthetic
  tests, and canonical route documentation only. No query-service, HTTP-server,
  registry, persistence write, Explain detail/resource/rebuild/mutation,
  schema, migration, dependency, lockfile, CLI, QQ, Provider, private DB,
  deployment, restart, container, or browser operation is permitted.
- `rollback_boundary`: the exact B18C candidate for all six product/test paths
  is privately captured byte-for-byte and mode-for-mode under mode-`0700`
  `/tmp/lethebot-gov-ux-01b18d-roundtrip-THqEDE`; after implementation one exact
  patch must independently prove reverse-first and forward-first restoration
  without touching the workspace.
- `fail_first_evidence`: the focused integration run collected 22 governance
  wiring tests, skipped 21 unrelated cases, and failed only because
  unauthenticated `GET /governance/api/v1/explain/turns` returned `404` instead
  of the required `401`. B18C and scope resolution remained uncalled, and no
  production source or canonical route document had changed.
- `final_evidence`: the focused B18D regression passed 1 / 1 with 21 skipped;
  full governance HTTP wiring passed 22 / 22; four-file query/scope-registry/
  HTTP/wiring adjacency passed 78 / 78; and the canonical P8 gate passed 6
  files / 288 tests. `pnpm typecheck`, scoped ESLint, and
  `pnpm release:check` passed, with 132 / 1 skipped files and 2707 / 10 skipped
  tests. The shared boundary now rejects unauthenticated, query-bearing,
  missing/malformed, unknown, cross-session, and cross-purpose requests before
  B18C. A valid B18B handle passes only its resolved exact private/group
  conversation scope to B18C and returns the fixed bounded page with ISO dates;
  B13A-B13C remain uncalled, detail/resource/rebuild/mutation routes remain
  absent, repeated reads are byte-stable and write-free, and integrity/FKs stay
  clean. Strict reverse-first and forward-first application restored the exact
  B18C baseline and B18D candidate across all six paths byte-for-byte and mode-
  for-mode at
  `/tmp/lethebot-gov-ux-01b18d-roundtrip-THqEDE/gov-ux-01b18d.patch`
  (`sha256:4edbd188a136bb2cbf9fcb3d9decb6cecec08594e2edf9c8e12c50fba52860ae`,
  mode `0600`, size `22888`) with proof-root/tree mode `0700`; candidate/
  workspace equality, the 68 tracked plus 49 untracked and zero-staged
  inventory, clean diff, ASCII-only patch, sensitive-filename hygiene,
  unchanged dependency versions, and unchanged migration hashes all passed.
- `exact_next_action`: completed; the B18C/B18D page-and-route reassessment
  selects `GOV-UX-01B19A`, an HTTP-disabled bounded Explain-turn resource-
  handle page, before changing the live list response or defining a detail
  route.

## Completed Slice: `GOV-UX-01B19A`

- `requirement`: `GOV-UX-01B19A` HTTP-disabled bounded exact-conversation
  Explain-turn resource-handle page.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: B18D exposes only B18C's one-way display fingerprint. A future
  turn detail therefore has no bounded, session-bindable resource authority,
  and recovering an internal turn ID from that fingerprint would violate its
  display-only contract.
- `selection_reason`: after the B18C/B18D page-and-route pair, exact row
  selection and scope authority are independently proved. A separate query-
  service handle page is the smallest dependency: it can prove which bounded
  turns may be offered to a trusted resource issuer before changing the live
  list response, defining a detail DTO, or registering a detail route.
- `implementation_contract`: add only
  `GovernanceQueryService.listExplainTurnResourceHandlePage(scope, issueHandle)`
  and fixed handle-page types. Reuse B18C's exact scope validation, structural
  stored-trace predicate, private/group isolation, finite status/date filters,
  101-row probe, stable order, turn projector, and truncation. For each of at
  most 100 selected rows, pass only its normalized exact conversation scope and
  internal turn ID to the trusted issuer, then add only its opaque `handle` and
  numeric `handleExpiresAt` to the unchanged B18C DTO. Ignore issuer extras.
  The future binding uses existing purpose `governance.explain.turns.read` and
  distinct resource kind `explain_turn`, but B19A changes no registry, session,
  application wiring, B18D response, detail projection, or route.
- `allowed_paths`: `src/governance/query-service.ts`,
  `tests/unit/governance/query-service.test.ts`, `docs/governance-ui.md`, and
  this checkpoint only.
- `protected_paths`: B13A-B13C and B18A-B18D methods, DTO semantics, route,
  catalog, scope issuance, bounds, privacy, and ordering; scope/resource/
  preview registries and all HTTP application/server wiring; Explain detail and
  trace rebuilds; Memory, maintenance, Privacy, Activity, and mutations; CLI,
  QQ, browser, repositories, schema, migrations, dependencies, lockfile,
  Provider, private/live data, deployment, restart, container, persistence
  format, and every prior slice.
- `assertions`: the issuer sees exactly the internal turn IDs and normalized
  scopes for B18C-selected rows in stable order, never an omitted, cross-scope,
  structurally mismatched, invalid, duplicate, or 101st row. Empty and malformed
  scopes issue nothing. Stripping `handle` and `handleExpiresAt` yields the
  exact B18C page; issuer extras, raw IDs/scope/session values, secrets, and
  unrestricted fields stay absent. A real resource registry proves stable live
  binding plus cross-session, cross-purpose, cross-kind, and cross-scope denial.
  Repeated reads are write-free with clean integrity/FKs; B13A-B13C, B18D,
  application/session/registry behavior, routes, and mutations remain unchanged,
  and no Explain detail route appears.
- `focused_commands`: one fail-first B19A query-service regression; final
  focused regression; full query-service and resource-registry adjacency;
  Explain HTTP-route non-change adjacency; the canonical P8 command; typecheck;
  scoped ESLint; release; diff, migration, inventory/privacy, dependency, and
  exact three-path rollback proof.
- `schema_dependency_live_boundary`: query-service code, synthetic tests, and
  canonical docs only. No persistence write, registry/session/HTTP behavior,
  route, detail projection, ContextBuilder, schema, migration, dependency,
  lockfile, governance mutation, CLI, QQ, Provider, private DB, deployment,
  restart, container, or browser operation is permitted.
- `rollback_boundary`: the exact B18D candidate for all three product/test paths
  is privately captured byte-for-byte and mode-for-mode under mode-`0700`
  `/tmp/lethebot-gov-ux-01b19a-roundtrip-6F09cj`; after implementation one exact
  patch must independently prove reverse-first and forward-first restoration
  without touching the workspace.
- `fail_first_evidence`: the focused Vitest run collected 39 query-service
  tests, skipped 38 unrelated cases, and failed only because
  `service.listExplainTurnResourceHandlePage is not a function`. The synthetic
  fixture had already established B18C parity, exact private/group issuer
  inputs, duplicate and 100/101 bounds, malformed/empty non-issuance, real
  registry isolation, privacy, zero writes, integrity, and FKs; no production
  source or canonical behavior document had changed.
- `final_evidence`: the focused final regression passed 1 / 1 with 38 skipped,
  the full query-service file passed 39 / 39, query/resource-registry adjacency
  passed 42 / 42, query/resource/B18D-route adjacency passed 64 / 64, and the
  canonical P8 gate passed 289 / 289. Typecheck, scoped ESLint, and
  `pnpm release:check` passed; release recorded 132 / 1 skipped files and 2708 /
  10 skipped tests. B18C and B19A now share one exact bounded selector; B19A
  offers only normalized exact scope plus selected internal turn ID to the
  trusted issuer and returns only the unchanged B18C DTO plus opaque handle and
  numeric expiry. Bounds, stable order, duplicate suppression, malformed-scope
  non-issuance, private/group isolation, registry session/purpose/kind/scope
  isolation, privacy, zero writes, integrity, and FKs passed while application,
  session, registry, route, detail, and mutation behavior stayed unchanged.
  Strict reverse-first and forward-first application restored the exact B18D
  baseline and B19A candidate across all three paths byte-for-byte and mode-for-
  mode at
  `/tmp/lethebot-gov-ux-01b19a-roundtrip-6F09cj/gov-ux-01b19a.patch`
  (`sha256:0e61b596a422a84afef837c69a94c206c73f495af36c514ced98183ec9a2fc90`,
  mode `0600`, size `17485`) with proof-root/tree mode `0700`; candidate/
  workspace equality, the 68 tracked plus 49 untracked and zero-staged
  inventory, clean diff, ASCII-only patch, no reject/original artifacts,
  sensitive-filename hygiene, unchanged dependency versions, and unchanged
  migration hashes all passed.
- `exact_next_action`: completed; expose B19A through the existing B18D list
  route by binding each selected turn to the current session, exact scope,
  existing Explain-read purpose, and distinct `explain_turn` resource kind;
  keep Explain detail and mutation routes absent.

## Completed Slice: `GOV-UX-01B19B`

- `requirement`: `GOV-UX-01B19B` current-session Explain-turn resource-handle
  list wiring.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: B19A proves the bounded handle-bearing page and B18D proves
  the exact-scope read route, but the live route still delegates to B18C and
  therefore returns no authority that a future turn-detail route can resolve.
- `selection_reason`: changing only the existing list-route delegation is the
  smallest dependency-ordered slice after B19A. It can bind selected turns to
  the already-proved session and scope boundary without defining detail data,
  registering another route, or changing registry semantics.
- `implementation_contract`: keep the existing read-only
  `GET /governance/api/v1/explain/turns` registration and purpose
  `governance.explain.turns.read`. Change only its handler to call
  `GovernanceQueryService.listExplainTurnResourceHandlePage(scope, issuer)`.
  The issuer must bind each offered internal turn ID and normalized scope to
  the callback's current session digest and absolute expiry, the same purpose,
  and resource kind `explain_turn` through the existing
  `GovernanceResourceHandleRegistry`. Return B19A's fixed page unchanged. Add no
  detail projection, route, resource resolution, rebuild, or mutation.
- `allowed_paths`: `src/index.ts`,
  `tests/integration/governance-http-wiring.test.ts`, `docs/architecture.md`,
  `docs/deployment.md`, `docs/governance-ui.md`, `docs/security-privacy.md`, and
  this checkpoint only.
- `protected_paths`: B13A-B13C, B18A-B18D, and B19A query selection, projection,
  bounds, order, scope, catalog, and issuer semantics; `GovernanceHttpServer`
  and all scope/resource/preview registry implementations; session, CSRF, and
  generic dispatch behavior; Explain detail/rebuild/mutation behavior; Memory,
  maintenance, Privacy, and Activity routes; CLI, QQ, browser, repositories,
  schema, migrations, dependencies, lockfile, Provider, private/live data,
  deployment, restart, container, persistence format, and every prior slice.
- `assertions`: invalid authentication, query, missing/malformed/unknown,
  cross-session, and cross-purpose scope authority fails before B19A or resource
  issuance. A valid private/group scope calls only B19A and binds each selected
  turn to the callback session digest/expiry, exact normalized scope, existing
  purpose, `explain_turn` kind, and internal turn ID. The response preserves
  B18C/B19A bounds, order, classifications, dates, redaction, and truncation and
  adds only stable opaque handle/expiry. Matching registry resolution succeeds;
  cross-session, cross-purpose, cross-kind, and cross-scope resolution fails.
  B18C and B13A-B13C remain uncalled, detail and mutation paths stay `404`
  without resource resolution, repeated reads are byte-stable and write-free,
  and integrity/FKs remain clean.
- `focused_commands`: one fail-first B19B wiring regression; final focused
  regression; full governance wiring and query/scope/resource/HTTP adjacency;
  the canonical P8 command; typecheck; scoped ESLint; release; diff, migration,
  inventory/privacy, dependency, and exact six-path rollback proof.
- `schema_dependency_live_boundary`: application wiring, synthetic integration
  coverage, and canonical route/security docs only. No query-service,
  HTTP-server, registry, persistence write, Explain detail/rebuild/mutation,
  schema, migration, dependency, lockfile, CLI, QQ, Provider, private DB,
  deployment, restart, container, or browser operation is permitted.
- `rollback_boundary`: the exact B19A candidate for all six product/test paths
  is privately captured byte-for-byte and mode-for-mode under mode-`0700`
  `/tmp/lethebot-gov-ux-01b19b-roundtrip-N0eBVC`; after implementation one exact
  patch must independently prove reverse-first and forward-first restoration
  without touching the workspace.
- `fail_first_evidence`: the focused integration run collected 22 governance
  wiring tests, skipped 21 unrelated cases, and failed only because the
  existing successful Explain-turn route made zero
  `listExplainTurnResourceHandlePage` calls. Authentication, exact scope
  resolution, and the route remained live, while production source and
  canonical behavior docs were still identical to the captured B19A baseline.
- `final_evidence`: the focused final regression passed 1 / 1 with 21 skipped;
  full governance HTTP wiring passed 22 / 22; query-service, scope/resource-
  registry, HTTP-server, and application-wiring adjacency passed 82 / 82; the
  expanded P8 set plus application wiring passed 311 / 311; and the exact
  canonical six-file P8 gate passed 289 / 289. `pnpm typecheck`, scoped ESLint,
  and `pnpm release:check` passed; release recorded 132 / 1 skipped files and
  2708 / 10 skipped tests. The existing `/explain/turns` handler now delegates
  only to B19A and binds each selected internal turn ID plus normalized exact
  scope to the callback session digest/expiry, existing Explain-read purpose,
  and `explain_turn` kind. Matching registry resolution succeeds while cross-
  session, cross-purpose, cross-kind, and cross-scope resolution fails. Invalid
  authentication, query, and scope authority fail before B19A or issuance;
  B18C is not called directly; the handle-bearing private/group responses are
  stable and write-free; detail and mutation paths remain `404` without
  resource resolution; and integrity/FKs stay clean. Strict reverse-first and
  forward-first application restored the exact B19A baseline and B19B candidate
  across all six paths byte-for-byte and mode-for-mode at
  `/tmp/lethebot-gov-ux-01b19b-roundtrip-N0eBVC/gov-ux-01b19b.patch`
  (`sha256:4184883a2a1b6d2de8175d9739324b690aa001ec68893f484b819503d03c742f`,
  mode `0600`, size `22237`) with proof-root/tree mode `0700`; candidate/
  workspace equality, the 68 tracked plus 49 untracked and zero-staged
  inventory, clean diff, ASCII-only patch, no reject/original artifacts,
  sensitive-filename hygiene, unchanged dependency versions, and unchanged
  migration hashes all passed. No B13A-B13C/B18A-D/B19A query behavior,
  HTTP-server/registry/session semantics, Explain detail/rebuild/mutation,
  schema/migration/dependency/lockfile, CLI/QQ/browser/live/private behavior, or
  prior slice changed.
- `exact_next_action`: completed; reassessment after the B19A/B19B page-and-
  wiring pair selects `GOV-UX-01B19C`, an HTTP-disabled exact-conversation
  Explain-turn detail projection. Prove exact scope and internal turn identity
  before bounded stored context/action/tool detail reads, return only a fixed
  identifier-free redacted DTO, and keep application/resource resolution,
  ContextBuilder rebuild, routes, and mutations absent until a later slice.

## Completed Slice: `GOV-UX-01B19C`

- `requirement`: `GOV-UX-01B19C` HTTP-disabled bounded exact-conversation
  Explain-turn detail projection.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: B19B now returns current-session, purpose-, kind-, and exact-
  scope-bound turn handles, but there is no identifier-free detail DTO safe for
  the eventual resource route. B13A-B13C remain CLI-oriented and expose raw
  turn, context, conversation, group, sender, memory, message, action,
  execution, and tool identifiers plus arrays and free-form fields that are not
  bounded for HTTP display.
- `selection_reason`: the B19A/B19B page-and-wiring pair proves which turns may
  receive authority. Defining and testing the exact-scope detail projection in
  the query service is the smallest dependency before any resource resolution
  or route can use that authority.
- `implementation_contract`: add only
  `GovernanceQueryService.getExplainTurnDetailForScope({ scope, turnId })` and
  fixed detail types. Normalize a valid private/group conversation scope and a
  clean 1-256-code-point internal turn ID, then select the turn and newest
  structurally matching stored trace in one exact-scope query. A miss must
  return `null` before any secondary context/action/tool query. Return the B18C
  turn projection plus: fixed context counts and validated token-budget
  numbers; at most 32 bounded redacted filter and injected-identity labels;
  the linked-preferred/latest action decision with finite decision metadata,
  counts, at most 32 normalized action types, and at most 32 fixed execution
  summaries; and at most 32 fixed tool summaries in existing ascending order.
  Every collection uses a 33-row truncation probe. Execution effects use only
  fixed classifications and presence booleans; tool/error labels are redacted
  and code-point bounded with explicit signals. Raw identifiers, scope values,
  context/action/tool payloads, memory/message lists, free-form decision
  reasons/suppressors, downgrade/error messages, Provider/model/response/token-
  usage fields, unrestricted text, and issuer/session data remain absent. Do
  not call or change B13A-B13C, rebuild through ContextBuilder, issue or resolve
  a handle, or register a route.
- `allowed_paths`: `src/governance/query-service.ts`,
  `tests/unit/governance/query-service.test.ts`, `docs/governance-ui.md`, and
  this checkpoint only.
- `protected_paths`: B13A-B13C and B18A-B19B methods, DTOs, selection, ordering,
  issuance, CLI behavior, and routes; `ContextTraceRepository`; all HTTP,
  session, scope/resource/preview registry, application, and browser code;
  Memory, maintenance, Privacy, Activity, mutation, repository, ContextBuilder,
  Pi, tool execution, schema, migration, dependency, lockfile, QQ, Provider,
  private/live data, deployment, restart, container, persistence format, and
  every prior slice.
- `assertions`: malformed scope/turn IDs, missing turns, turns without a
  matching trace, private/group collisions, other groups, and mismatched turn/
  trace conversations return `null` before secondary reads. A valid detail uses
  only the newest exact matching trace, preserves the B18C turn summary, linked-
  decision preference and ascending action/tool order, and proves independent
  32/33 bounds for context labels, action types, executions, and tools. Invalid
  JSON and invalid token-budget fields degrade to fixed empty/omitted evidence.
  Field allowlists, opaque-reference/identifier omission, secret/platform
  redaction, code-point truncation, stable repeated reads, zero writes, clean
  integrity/FKs, continued HTTP non-exposure, and B13/B18/B19A-B behavior all
  pass.
- `focused_commands`: one fail-first then final B19C query-service regression;
  full query-service coverage; query/resource-registry/HTTP/wiring non-exposure
  adjacency; the canonical P8 command; typecheck; scoped ESLint; release; diff,
  migration, inventory/privacy, dependency, and exact three-path rollback proof.
- `schema_dependency_live_boundary`: query-service code, synthetic tests, and
  canonical behavior documentation only. No persistence write, repository,
  ContextBuilder, registry/session/application/HTTP/browser behavior, route,
  schema, migration, dependency, lockfile, mutation, CLI, QQ, Provider, private
  DB, deployment, restart, container, or live operation is permitted.
- `rollback_boundary`: the exact B19B candidate for all three product/test paths
  is freshly captured byte-for-byte and mode-for-mode under mode-`0700`
  `/tmp/lethebot-gov-ux-01b19c-roundtrip-HVqhDL`; after implementation one exact
  patch must independently prove reverse-first and forward-first restoration
  without touching the workspace.
- `fail_first_evidence`: the focused Vitest run collected 40 query-service
  tests, skipped 39 unrelated cases, and failed only because
  `service.getExplainTurnDetailForScope is not a function`. Exact-scope early
  rejection, newest-trace selection, fixed field allowlists, independent 32/33
  bounds, redaction/truncation, malformed-evidence degradation, legacy-read
  isolation, stable zero-write reads, integrity, and FK assertions were fixed
  before production source or canonical behavior documentation changed.
- `final_evidence`: the focused final regression passed 1 / 1 with 39 skipped;
  the full query-service file passed 40 / 40; five-file query/resource/scope/
  HTTP/wiring adjacency passed 83 / 83; and the canonical six-file P8 gate
  passed 290 / 290. `pnpm typecheck`, scoped ESLint, and
  `pnpm release:check` passed; release recorded 132 / 1 skipped files and
  2709 / 10 skipped tests. `getExplainTurnDetailForScope` now proves the exact
  turn and conversation authority through its linked context-pack trace before
  any secondary read, chooses the newest matching trace, and returns only fixed
  identifier-free context, decision, execution, and tool evidence. Independent
  32/33 probes, 96-code-point bounded redaction, validated token budgets,
  linked-decision preference, fixed action/effect classifications, presence
  booleans, and explicit redaction/truncation signals all pass. The synthetic
  secret fixture was corrected from a generic token assignment to an API-key
  assignment so it matches the asserted classification; production redaction
  behavior and assertions were not weakened. Invalid and cross-scope requests
  return `null` before label/action/tool reads; repeated reads are stable and
  write-free; integrity/FKs remain clean; B13A-B13C, ContextBuilder, registries,
  issuers, routes, rebuilds, mutations, schema, migrations, dependencies,
  lockfile, CLI, QQ, browser, live, and private behavior remain unchanged. The
  exact three-path patch is
  `/tmp/lethebot-gov-ux-01b19c-roundtrip-HVqhDL/gov-ux-01b19c.patch`
  (`sha256:67f9b8df924fd93a5ba321990d338a5f3bb4dd4038dbf65ec3088b022eb351fc`,
  mode `0600`, size `47454`); its proof root and trees are mode `0700`, strict
  reverse-first and forward-first cycles restored baseline and candidate bytes
  and modes, and the candidate snapshot matches the workspace. The patch has
  exactly three ASCII-only paths and no reject/original artifacts; the 68
  tracked plus 49 untracked and zero-staged inventory, sensitive-filename
  hygiene, and Pi dependency `^0.80.2` all pass. Migrations 007/008 retain
  SHA-256
  `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`/
  `95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`.
- `exact_next_action`: implement `GOV-UX-01B19D` as one read-only
  `/explain/turns/:resourceHandle` route. Resolve only exact current-session
  scope and `explain_turn` resource authority, pass only that scope and internal
  turn ID to B19C, and return fixed `404` for a resolved missing detail. Keep
  ContextBuilder rebuild and every mutation absent.

## Completed Slice: `GOV-UX-01B19D`

- `requirement`: `GOV-UX-01B19D` exact-scope read-only Explain-turn resource-
  detail route.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: B19B issues current-session, Explain-read-purpose,
  `explain_turn`-kind, exact-scope resource handles and B19C owns the fixed
  identifier-free detail DTO, but
  `GET /governance/api/v1/explain/turns/:resourceHandle` remains absent. The
  bounded Explain workflow therefore cannot resolve its already-issued
  authority to the proved detail projection.
- `selection_reason`: handle issuance and the safe detail projection are now
  independently proved. Registering one existing generic scoped resource route
  is the smallest dependency-ordered slice and requires no HTTP-server,
  registry, query-service, persistence, or ContextBuilder change.
- `implementation_contract`: add only the path constant
  `` `${GOVERNANCE_EXPLAIN_TURNS_PATH}/:resourceHandle` ``, register an
  authenticated scoped read-only `GET` with purpose
  `governance.explain.turns.read` and resource kind `explain_turn`, and add one
  application handler branch. After the shared boundary authenticates, rejects
  queries, resolves the exact current-session scope handle, and resolves the
  resource handle against the same session, purpose, kind, and scope, call only
  `GovernanceQueryService.getExplainTurnDetailForScope({ scope, turnId:
  resource.resourceId })`. Return the unchanged B19C detail with `200`, or the
  fixed `{ error: 'not_found' }` body with `404` when a valid resolved resource
  no longer has detail. Add no ContextBuilder rebuild, issuer, selector,
  mutation, or alternate authority path.
- `allowed_paths`: `src/index.ts`,
  `tests/integration/governance-http-wiring.test.ts`, `docs/architecture.md`,
  `docs/deployment.md`, `docs/governance-ui.md`, `docs/security-privacy.md`, and
  this checkpoint only.
- `protected_paths`: B13A-B13C, B18A-B19C query-service methods, DTOs,
  selection, bounds, redaction, ordering, and issuer behavior;
  `GovernanceHttpServer`; scope/resource/preview registry implementations;
  generic authentication, session, CSRF, scope, resource, preview, and route
  dispatch; Memory, maintenance, Privacy, Activity, and all mutation behavior;
  ContextBuilder, CLI, QQ, browser, repositories, schema, migrations,
  dependencies, lockfile, Provider, private/live data, deployment execution,
  restart, container, persistence format, and every prior slice.
- `assertions`: unauthenticated detail returns fixed `401` before scope or
  resource resolution and before B19C. Missing/malformed scope or resource
  handles and any query return `400` before B19C. Unknown, cross-session,
  cross-purpose, cross-kind, and cross-scope authority returns fixed `404`
  before B19C. A valid resolved handle for a missing turn returns fixed `404`
  after exactly one B19C call with only the resolved scope and internal turn ID.
  Valid private and group handles return the unchanged minimal B19C DTO with
  `200`; the list stays byte-stable; raw identifiers, scope/session/issuer data,
  secrets, and unrestricted fields remain absent. POST and all other mutation
  paths stay `404`; B13A-B13C remain uncalled; repeated reads perform zero
  writes; integrity is `ok` and foreign-key checks are empty.
- `focused_commands`: first and final single-test runs of
  `tests/integration/governance-http-wiring.test.ts`; the full wiring file; the
  five-file query/scope-registry/resource-registry/HTTP/wiring adjacency; the
  canonical P8 command from `docs/long-term-development-constraints.md`;
  `pnpm typecheck`; scoped ESLint over the two code/test paths;
  `pnpm release:check`; `git diff --check`; unchanged migration/dependency
  checks; worktree/privacy hygiene; and exact six-path private reverse-first and
  forward-first patch proof.
- `schema_dependency_live_boundary`: application route wiring, synthetic
  integration coverage, and canonical route/security docs only. No query-
  service, HTTP-server, registry, ContextBuilder, persistence write, schema,
  migration, dependency, lockfile, governance mutation, CLI, QQ, Provider,
  private DB, deployment, restart, container, browser, or live operation is
  permitted.
- `fresh_baseline`: `pnpm release:check` passed at 2026-07-27 09:45 CST +0800
  with typecheck, lint, build, preflight, and diff hygiene green; 132 test files
  passed with one skipped and 2709 tests passed with ten skipped. Branch/HEAD
  remains `main@cfebb77`; inventory remains 68 tracked modified, 49 untracked,
  and zero staged paths.
- `rollback_boundary`: the exact B19C candidate for all six product/test paths
  is freshly captured byte-for-byte and mode-for-mode under mode-`0700`
  `/tmp/lethebot-gov-ux-01b19d-roundtrip-Q3Lrnd`; every baseline file is mode
  `0644` and matches the workspace. After implementation, create one exact
  six-path patch and independently prove reverse-first and forward-first
  restoration without touching the workspace or the prior B19C artifact.
- `exact_fail_first_action`: extend the existing
  `serves only the exact scoped bounded Explain turn page with resource handles`
  integration fixture with the detail spy and boundary matrix before editing
  production or canonical behavior docs. Run that single test and require its
  first unauthenticated detail assertion to fail only because the absent route
  returns `404` instead of `401` while scope/resource/B19C reads remain zero.
- `fail_first_evidence`: the focused Vitest run collected 22 governance wiring
  tests, skipped 21 unrelated cases, and failed only at the first new detail
  assertion because unauthenticated
  `GET /governance/api/v1/explain/turns/:resourceHandle` returned `404` instead
  of required `401`. The existing list, scope/resource issuance fixture, B19C
  method, and production/canonical behavior paths were otherwise unchanged;
  route absence prevented scope/resource resolution and B19C detail reads.
- `final_evidence`: the focused final regression passed 1 / 1 with 21 skipped;
  full governance HTTP wiring passed 22 / 22; query-service, scope/resource-
  registry, HTTP-server, and application-wiring adjacency passed 83 / 83; and
  the canonical six-file P8 gate passed 290 / 290. `pnpm typecheck`, scoped
  ESLint, and `pnpm release:check` passed; release recorded 132 / 1 skipped
  files and 2709 / 10 skipped tests. The generic boundary now rejects
  unauthenticated, query-bearing, missing/malformed, unknown, cross-session,
  cross-purpose, cross-kind, and cross-scope detail requests before B19C. A
  matching private/group binding passes only the resolved exact scope and
  registry-owned internal turn ID to B19C and returns its fixed detail
  unchanged; a resolved missing detail maps to fixed `404`. The list remains
  byte-stable, B13A-B13C remain uncalled, POST remains `404`, repeated reads are
  write-free, and integrity/FKs stay clean. No HTTP-server/registry/query-
  service/ContextBuilder/schema/migration/dependency/CLI/QQ/live/private
  behavior changed. The exact six-path patch is
  `/tmp/lethebot-gov-ux-01b19d-roundtrip-Q3Lrnd/gov-ux-01b19d.patch`
  (`sha256:33402f03d218171b759e6022fb12b8ead4fb028bba6d1cd5cb680478d4923e13`,
  mode `0600`, size `26051`). Its successful `reverse-first-v2` and
  `forward-first-v2` trees are mode `0700`; both independently restored exact
  baseline/candidate bytes and `0644` file modes, and the candidate matches the
  workspace. An initial private proof copy intentionally failed the mode check
  because `umask 077` made `git apply` rewrite files as `0600`; it never touched
  the workspace and is superseded by the exact v2 proof. The patch is ASCII-
  only with exactly six paths; inventory remains 68 tracked plus 49 untracked
  and zero staged, no reject/original or sensitive untracked artifact appeared,
  Pi dependencies remain `0.80.2`, and migrations 007/008 retain SHA-256
  `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`/
  `95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`.
- `exact_next_action`: completed; the required B19C/B19D projection-and-route
  reassessment selects `GOV-UX-01B20A`, an HTTP-disabled exact-scope Memory-
  record forget preview. Prove a clean internal record ID, exact scope,
  non-deleted current lifecycle state, and latest revision before returning one
  fixed identifier-free digest-bound preview; add no handle/route/write or
  governance-service behavior until later slices.

## Completed Slice: `GOV-UX-01B20A`

- `requirement`: `GOV-UX-01B20A` HTTP-disabled exact-scope Memory-record forget
  preview.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: Memory records are now discoverable and inspectable through
  purpose- and exact-scope-bound read handles, while the only existing forget
  owners are the CLI/QQ paths through `GovernanceService`. There is no fixed,
  identifier-free preview that can prove the selected record, current lifecycle
  revision, deletion effects, immediate retrieval exclusion, and separately
  confirmed restore boundary before a future HTTP mutation receives authority.
- `selection_reason`: the B18/B19 Explain read workflow is complete through
  detail. User-controlled deletion is the earliest remaining required P8
  destructive workflow, and proving its read-only projection is the smallest
  dependency before adding any preview handle, route, or governed confirmation.
- `implementation_contract`: add only
  `GovernanceQueryService.getMemoryRecordForgetPreviewForScope({ scope,
  memoryId })`, `MEMORY_RECORD_FORGET_ACTION`, its fixed projection type, and a
  domain-separated SHA-256 preview digest. Reuse
  `memoryRecordExactScopePredicate`; accept only a trimmed, control-free 1-256-
  code-point internal record ID; select only that exact record plus its latest
  revision number; and return `null` for a tool scope, missing/cross-scope or
  malformed ID, deleted or invalid lifecycle state, absent/non-positive/non-
  integral/unsafe revision number, or an unsafe next revision. A valid preview
  contains only action `memory.record.forget`, a forget-purpose-bound 16-
  character `recordRef`, `scopeKind`, current non-deleted lifecycle state and
  revision, expected `deleted` state and next revision, the fixed durable
  effects `memory_record_state_transition`, `memory_revision_append`, and
  `audit_event_append`, fixed immediate `deleted_record_excluded` retrieval
  consequence, a supported rollback boundary requiring separate restore
  confirmation, and the digest. Raw record/owner/scope values, text, source,
  subject, evaluator, revision/audit IDs, session/issuer data, unrestricted
  fields, selectors, and mutation inputs remain absent. Do not call or change
  B12/B16/B17 reads, `GovernanceService`, or any repository mutation.
- `allowed_paths`: `src/governance/query-service.ts`,
  `tests/unit/governance/query-service.test.ts`, `docs/governance-ui.md`, and
  this checkpoint only.
- `protected_paths`: all B12/B16/B17 Memory list/detail/catalog/issuer methods,
  DTOs, bounds, redaction, ordering, CLI behavior, and existing HTTP routes;
  `GovernanceService.forgetMemoryAsLocalAdmin`, QQ forget, and the shared atomic
  forget implementation; all HTTP application/server, authentication, session,
  CSRF, scope/resource/preview registry, routing, and browser code; maintenance,
  Explain, Privacy, Activity, ContextBuilder, Pi, tools, workers, repositories,
  schema, migrations, dependencies, lockfile, Provider, QQ execution, private/
  live data, deployment, restart, container, persistence format, and every
  prior slice.
- `assertions`: global, user, group, private-conversation, group-conversation,
  and system authority returns only the exact matching non-deleted record and
  latest valid revision; private/group conversation collisions and malformed
  owner rows remain isolated, and tool, missing, cross-scope, malformed-ID,
  deleted, missing/malformed/unsafe-revision cases return `null`. Every
  non-deleted lifecycle state is previewable. The exact fixed allowlist uses a
  purpose-isolated record reference, correct next revision, fixed effect and
  rollback classifications, and a domain-separated reproducible digest.
  Serialization omits every raw identifier, owner/scope value, text, source,
  evaluator, session, and unrestricted field. Repeated previews are byte-stable
  and write-free; integrity/FKs stay clean; B12/B16/B17 and
  `GovernanceService` remain uncalled/unchanged; no preview handle, route,
  confirmation, audit, state/retrieval mutation, or `/memory/records` POST
  appears.
- `focused_commands`: one fail-first then final single-test run of
  `tests/unit/governance/query-service.test.ts` for `previews an exact-scope
  memory-record forget without mutation`; the full query-service file;
  query/service/preview-registry/HTTP-wiring adjacency; the canonical P8 command
  from `docs/long-term-development-constraints.md`; `pnpm typecheck`; scoped
  ESLint over the two TypeScript paths; `pnpm release:check`; `git diff
  --check`; unchanged migration/dependency checks; inventory/privacy hygiene;
  and an exact three-path private reverse-first and forward-first patch proof.
- `schema_dependency_live_boundary`: query-service code, synthetic tests, and
  canonical behavior documentation only. No persistence write, service,
  repository, registry/session/application/HTTP/browser behavior, route,
  preview handle, confirmation, schema, migration, dependency, lockfile, CLI,
  QQ, Provider, private DB, deployment, restart, container, or live operation
  is permitted.
- `rollback_boundary`: the exact B19D candidate for the three product/test
  paths is captured byte-for-byte and mode-for-mode under mode-`0700`
  `/tmp/lethebot-gov-ux-01b20a-roundtrip-P1s49t/baseline`; every file is mode
  `0644`. After implementation, create one exact three-path patch and
  independently prove reverse-first and forward-first restoration without
  touching the workspace or any prior rollback artifact.
- `exact_fail_first_action`: append the single comprehensive query-service
  regression before editing production source or canonical behavior docs, run
  only that named test, and require the sole failure to be the missing
  `getMemoryRecordForgetPreviewForScope` method after its fixture has exercised
  all fixed scope, revision, privacy, no-write, integrity, and FK assertions.
- `fail_first_evidence`: the focused Vitest run collected 41 query-service
  tests, skipped 40 unrelated cases, and failed only because
  `service.getMemoryRecordForgetPreviewForScope is not a function`. The
  synthetic exact-scope, lifecycle/revision, allowlist/digest, privacy,
  legacy-read isolation, no-write, integrity, and FK assertions were fixed
  before production source or canonical behavior documentation changed.
- `final_evidence`: the focused final regression passed 1 / 1 with 40 skipped;
  the full query-service file passed 41 / 41; query/service/preview-registry/
  HTTP-wiring adjacency passed 74 / 74; and the canonical six-file P8 gate
  passed 291 / 291. `pnpm typecheck`, scoped ESLint, and
  `pnpm release:check` passed; release recorded 132 / 1 skipped files and
  2710 / 10 skipped tests. The single exact-scope query now accepts only clean
  1-256-code-point IDs, rejects tool/missing/cross-scope/deleted/revisionless/
  fractional/non-positive/unsafe revision cases, and previews every non-deleted
  lifecycle state with the correct safe next revision. Its fixed allowlist uses
  a forget-purpose-isolated 16-character record reference, fixed deletion/
  revision/audit and immediate retrieval-exclusion classifications, a separate
  restore-confirmation boundary, and a reproducible domain-separated digest.
  Synthetic repeated reads are stable and write-free; raw owner, scope, record,
  subject, text, source, evaluator, session, and unrestricted fields remain
  absent; B12/B16/B17 reads and `GovernanceService` remain uncalled/unchanged;
  integrity/FKs stay clean; and no route, handle, confirmation, audit, state, or
  retrieval mutation was added. The exact three-path patch is
  `/tmp/lethebot-gov-ux-01b20a-roundtrip-P1s49t/gov-ux-01b20a.patch`
  (`sha256:800b8b02f92585a611de9875fe8e0c86442f8e6331b5633c8f341b47e0666e9f`,
  mode `0600`, size `20101`). Its mode-`0700` `reverse-first-v2` and
  `forward-first-v2` trees independently restored exact baseline/candidate bytes
  and mode-`0644` files, and the candidate snapshot matches the workspace. The
  patch has exactly three ASCII-only paths with no reject/original artifacts;
  inventory remains 68 tracked plus 49 untracked and zero staged, diff/privacy
  hygiene is clean, Pi dependencies remain `0.80.2`, and migrations 007/008
  retain SHA-256
  `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`/
  `95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`.
- `exact_next_action`: implement `GOV-UX-01B20B` as a preview-only authenticated
  `POST /governance/api/v1/memory/records/:resourceHandle`. Require exact
  current-session scope and `memory_record` resource authority, CSRF, and exact
  body `{ action: 'forget' }`; call only B20A, bind its current state/revision/
  digest into the existing single-use preview registry, and return the fixed
  preview plus opaque expiry. Add no confirmation or durable mutation.

## Active Slice: `GOV-UX-01B20B`

- `requirement`: `GOV-UX-01B20B` exact-scope Memory-record forget preview
  route and current-session preview-handle issuance.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: B17C/B17D already bind displayed Memory records to the exact
  current HTTP session, Memory-read purpose, `memory_record` kind, and exact
  scope, and B20A now owns a fixed write-free forget preview. No route connects
  those two proved boundaries or binds B20A's current state, revision, and
  digest into the existing single-use preview registry, so a future confirmation
  cannot receive stale-safe action-specific authority.
- `selection_reason`: the generic mutation boundary and preview registry already
  own authentication, Origin/CSRF, body limits, session lifecycle, capacity,
  expiry, and single-use binding. Reusing them for one preview-only POST is the
  smallest dependency after B20A and requires no new registry, query, service,
  repository, or persistence behavior.
- `implementation_contract`: import `MEMORY_RECORD_FORGET_ACTION`, register only
  mutation-marked `POST /governance/api/v1/memory/records/:resourceHandle` with
  existing purpose `governance.memory.records.read` and resource kind
  `memory_record`, and add one application handler branch. After the shared
  boundary authenticates, rejects queries, enforces exact Origin/CSRF and body
  bounds, resolves the scope handle, and resolves the resource handle against
  the same current session/purpose/kind/scope, accept only exact JSON body
  `{ action: 'forget' }`. Call only B20A
  `getMemoryRecordForgetPreviewForScope({ scope, memoryId:
  resource.resourceId })`; map `null` to fixed `404`. For a valid preview, call
  the existing preview registry once with current session/expiry, local-admin
  actor, action `memory.record.forget`, resource kind/ID, exact scope, current
  lifecycle state/revision, and preview digest; return `201` with the unchanged
  B20A projection plus only `previewHandle` and `previewExpiresAt`. Add no
  confirm path, preview consumption, service call, audit, state/revision write,
  or retrieval change.
- `allowed_paths`: `src/index.ts`,
  `tests/integration/governance-http-wiring.test.ts`, `docs/architecture.md`,
  `docs/deployment.md`, `docs/governance-ui.md`, `docs/security-privacy.md`, and
  this checkpoint only.
- `protected_paths`: B12/B16/B17/B20A query methods, DTOs, exact-scope logic,
  bounds, redaction, references, digest, and issuer semantics;
  `GovernanceHttpServer`; scope/resource/preview registry implementations and
  generic authentication/session/Origin/CSRF/body/route dispatch;
  `GovernanceService.forgetMemoryAsLocalAdmin`, QQ forget, repository state/
  revision/audit/FTS/retrieval behavior; maintenance, Explain, Privacy,
  Activity, ContextBuilder, Pi, CLI, QQ, browser, schema, migrations,
  dependencies, lockfile, Provider, private/live data, deployment execution,
  restart, container, persistence format, and every prior slice.
- `assertions`: unauthenticated POST is fixed `401`; query-bearing, missing or
  malformed scope/resource authority, wrong Origin, missing/wrong/cross-session
  CSRF, malformed/empty/array/non-JSON/extra-key/wrong-action body, oversize,
  timeout, and cross-session/purpose/kind/scope handles fail with the existing
  fixed boundary status before B20A and preview issuance. A resolved missing or
  stale/deleted record calls B20A once and returns fixed `404` without issuance.
  Valid private and group records call B20A only with the resolved exact scope
  and registry-owned internal ID; issue exactly one action/state/revision/digest-
  bound current-session preview; and return the fixed privacy-safe DTO plus a
  43-character opaque handle and bounded expiry. Repeated previews retain the
  same digest but receive fresh handles. B12/B16/B17, preview consumption,
  `GovernanceService`, CLI/QQ, confirmation, and other routes remain unchanged;
  repeated preview requests write no DB rows, integrity/FKs stay clean, and
  POST `/confirm` remains `404`.
- `focused_commands`: fail-first then final single Memory-record wiring test;
  full governance HTTP wiring; query/service/HTTP/scope/resource/preview-
  registry adjacency; the canonical P8 command; `pnpm typecheck`; scoped ESLint
  over the two TypeScript paths; `pnpm release:check`; `git diff --check`;
  unchanged migration/dependency checks; inventory/privacy hygiene; and an
  exact six-path private reverse-first and forward-first patch proof.
- `schema_dependency_live_boundary`: application route wiring, synthetic
  integration coverage, and canonical route/security docs only. No query-
  service, HTTP-server, registry, service/repository mutation, persistence
  write, confirm path, schema, migration, dependency, lockfile, CLI, QQ,
  Provider, private DB, deployment, restart, container, browser, or live
  operation is permitted.
- `rollback_boundary`: the exact B20A candidate for all six product/test paths
  is captured byte-for-byte and mode-for-mode under mode-`0700`
  `/tmp/lethebot-gov-ux-01b20b-roundtrip-WP3ZtU/baseline`; every file is mode
  `0644`. After implementation, create one exact six-path patch and
  independently prove reverse-first and forward-first restoration without
  touching the workspace or any prior rollback artifact.
- `exact_fail_first_action`: extend only the existing
  `serves only the exact scoped bounded Memory-record page and provenance
  detail` integration fixture with B20A/preview-registry spies and the preview
  boundary matrix before editing production or canonical route docs. Run only
  that test and require the first unauthenticated preview assertion to fail
  because the absent POST route returns `404` instead of `401`, with zero B20A,
  preview-issue, preview-consume, or governance-service calls.
- `fail_first_evidence`: the focused Vitest run collected 22 governance wiring
  tests, skipped 21 unrelated cases, and failed only at the first new preview
  assertion because unauthenticated
  `POST /governance/api/v1/memory/records/:resourceHandle` returned `404`
  instead of the required `401`. The existing Memory scope/resource workflow
  supplied real opaque authority; B20A, preview issue/consume, and the shared
  local-admin forget service remained uncalled, proving route absence before
  production or canonical route documentation changed.
- `final_evidence`: the focused final regression passed 1 / 1 with 21 skipped;
  full governance HTTP wiring passed 22 / 22; query/service/HTTP/scope/resource/
  preview-registry adjacency passed 7 files / 95 tests; and the canonical P8
  gate passed 6 files / 291 tests. `pnpm typecheck`, scoped ESLint, and
  `pnpm release:check` passed; release recorded 132 / 1 skipped files and 2710 /
  10 skipped tests. The mutation-marked preview POST now accepts only current-
  session Memory-read scope/resource authority and exact `{ "action":
  "forget" }`, calls B20A only with the resolved exact scope and registry-owned
  record ID, and issues a fresh handle bound to session, actor, action, resource,
  scope, current state/revision, and digest. Missing/revisionless records return
  fixed `404`; repeated previews retain the digest but receive distinct handles;
  serialized output omits raw identifiers, text, source, evaluator, and session
  data. Preview consumption, `GovernanceService`, confirmation, audit, record,
  revision, retrieval, CLI, and QQ behavior remain unchanged; zero-write,
  integrity, FK, and absent-`/confirm` assertions pass.
- `rollback_evidence`: the exact six-path patch is
  `/tmp/lethebot-gov-ux-01b20b-roundtrip-WP3ZtU/gov-ux-01b20b.patch`
  (`sha256:a26a7c7c60da5ef5e6aec5993bfc59820f2c946ff8038fc6ac19986cad771f17`,
  mode `0600`, size `22937`). Under `umask 022`, independent reverse-first and
  forward-first trees restored the exact B20A baseline and B20B candidate
  byte-for-byte and mode-for-mode; all roots/directories are mode `0700`, all
  snapshot files are mode `0644`, and the candidate equals the workspace. The
  patch has exactly six ASCII-only paths with no reject/original artifacts;
  inventory remains 68 tracked plus 49 untracked and zero staged, diff/privacy
  hygiene is clean, Pi dependencies remain `0.80.2`, and migrations 007/008
  retain SHA-256
  `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`/
  `95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`.
- `exact_next_action`: completed; the B20A/B20B reassessment selects B20C to
  enforce exact scope, state, and latest revision inside the same immediate
  governance transaction as deletion before any confirmation route is added.

## Active Slice: `GOV-UX-01B20C`

- `requirement`: `GOV-UX-01B20C` HTTP-disabled atomic expected-snapshot
  Memory-record forget operation for local governance confirmation.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: B20A and B20B bind an exact-scope preview and its current
  state/revision/digest, but `GovernanceService.forgetMemoryAsLocalAdmin`
  currently accepts only a record ID. It performs deletion transactionally but
  cannot atomically reject a stale preview or a record whose exact scope changed
  between the confirmation read and the write.
- `selection_reason`: adding the confirmation route now would require a
  query-then-write freshness check. The shared governance owner already wraps
  record selection and `MemoryRepository.updateStateSync` in one immediate
  transaction, so extending that single operation is the smallest way to retain
  CLI/QQ parity and make later preview consumption safe against stale state,
  revision, and scope.
- `implementation_contract`: preserve the existing string overload and exact
  CLI/QQ results of `forgetMemoryAsLocalAdmin`. Add a typed expected-snapshot
  overload taking only internal record ID, normalized exact non-tool scope,
  non-deleted expected lifecycle state, positive safe expected revision, and a
  clean bounded reason code. Inside one immediate transaction, select the record
  plus its latest revision, conceal missing/deleted/cross-scope records as
  `not_found`, return `stale` for invalid or mismatched state/revision evidence,
  and call the existing shared repository state transition only on an exact
  match. A success appends exactly one delete revision and one redacted audit,
  returns the resulting revision number, and makes existing retrieval exclude
  the record immediately. Keep all scope/revision comparison and mutation in
  the governance owner; add no route, preview consumption, or query-service
  confirmation projection.
- `allowed_paths`: `src/governance/service.ts`,
  `tests/unit/governance/service.test.ts`, `docs/governance-ui.md`, and this
  checkpoint only.
- `protected_paths`: the existing string-call behavior and output used by CLI;
  QQ command authority, responses, and shared forget results; B12/B16/B17/B20A
  queries and projections; B20B route and every HTTP/session/scope/resource/
  preview-registry behavior; repository state-transition implementation,
  retrieval SQL, schemas, migrations, dependencies, lockfile, ContextBuilder,
  Pi, tools, workers, browser, Provider, private/live data, deployment, restart,
  container, persistence format, and every prior slice.
- `assertions`: the legacy local-admin string call and QQ forget remain byte-
  compatible. Expected-snapshot calls cover global, user, group, private/group
  conversation, and system matches; exact scope comparison rejects owner and
  conversation-type/group collisions. Missing, already-deleted, and cross-scope
  records return `not_found`; malformed/non-positive/fractional/unsafe expected
  revisions, invalid states/reason codes, missing/malformed latest revisions,
  state drift, and revision drift return `stale`. Every failure is write-free.
  An exact call transitions one non-deleted record to deleted at expected
  revision plus one, appends only the existing delete revision/audit evidence
  with fixed local-admin authority and bounded reason metadata, excludes the
  record from ordinary retrieval immediately, and leaves other records intact.
  Retry, concurrent drift simulation, transaction rollback on revision/audit
  insertion failure, total changes, integrity, and foreign keys are proved. No
  HTTP route or preview handle is consumed.
- `focused_commands`: fail-first then final named governance-service regression;
  full governance-service and query-service adjacency; CLI/QQ forget coverage;
  HTTP wiring non-use; canonical P8 command; `pnpm typecheck`; scoped ESLint;
  `pnpm release:check`; diff, migration/dependency, inventory/privacy hygiene;
  and exact three-path private reverse-first and forward-first patch proof.
- `schema_dependency_live_boundary`: shared governance service, synthetic tests,
  and canonical behavior documentation only. No repository implementation,
  query projection, registry/session/HTTP/browser behavior, route, preview
  consumption, schema, migration, dependency, lockfile, Provider, QQ execution,
  private DB, deployment, restart, container, or live operation is permitted.
- `rollback_boundary`: the exact B20B candidate for all three product/test paths
  is captured byte-for-byte and mode-for-mode under mode-`0700`
  `/tmp/lethebot-gov-ux-01b20c-roundtrip-A6yQ8f/baseline`; every file is mode
  `0644`. After implementation, create one exact three-path patch and prove
  reverse-first and forward-first restoration without touching the workspace or
  any prior rollback artifact.
- `exact_fail_first_action`: add one comprehensive named service regression for
  the expected-snapshot overload before editing production source or canonical
  behavior docs. Establish all scope/state/revision/failure/rollback/retrieval/
  integrity assertions first; the focused run must fail only because the current
  string-only method cannot interpret or enforce that input.
- `fail_first_evidence`: after correcting one synthetic fixture foreign-key
  setup error without production edits, the structurally valid named run failed
  only because the old string-only method received the expected-snapshot object;
  SQLite reported `RangeError: Too few parameter values were provided`. This
  established the missing overload before service or canonical behavior edits.
- `final_evidence`: the final named regression passed 1 / 1 with 7 skipped, the
  full governance service passed 8 / 8, the broadened service/query/QQ/CLI/HTTP
  adjacency passed 6 files / 185 tests, and the two deletion-relevant CLI
  process cases passed with 127 unrelated cases skipped. The canonical P8 gate
  passed 6 files / 292 tests. `pnpm typecheck`, scoped ESLint, and
  `pnpm release:check` passed; release recorded 132 / 1 skipped files and 2711 /
  10 skipped tests. Exact successes span global, user, group, private/group
  conversation, and system scopes plus every non-deleted lifecycle state. Valid
  stale revisions, state drift, malformed or missing revisions, scope
  collisions, missing/deleted records, retry, and rollback-on-audit-failure are
  write-free or transactional as required. Successful deletion appends one
  revision and one redacted local-admin audit and is immediately absent from
  ordinary retrieval; the legacy string call, CLI/QQ behavior, routes, and
  preview consumption remain unchanged.
- `rollback_evidence`: the exact three-path patch is
  `/tmp/lethebot-gov-ux-01b20c-roundtrip-A6yQ8f/gov-ux-01b20c.patch`
  (`sha256:f73bd83f44ccc867ae19056a8cd3aab29122e645a3733cd53008f9259f0ea312`,
  mode `0600`, size `24843`). Independent reverse-first and forward-first trees
  restored the exact B20B baseline and B20C candidate byte-for-byte and mode-
  for-mode without touching the workspace; each tree contains exactly three
  files, all roots/directories are mode `0700`, all snapshot files are mode
  `0644`, and the candidate equals the workspace. The patch is ASCII-only;
  inventory remains 68 tracked plus 49 untracked and zero staged, diff/privacy
  hygiene is clean, and migrations 007/008 retain SHA-256
  `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`/
  `95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`.
- `exact_next_action`: completed; B20A's digest, B20B's single-use authority,
  and B20C's atomic expected-snapshot mutation now permit B20D to add one exact
  Memory-record forget confirmation route without a query-then-write race.

## Completed Slice: `GOV-UX-01B20D`

- `requirement`: `GOV-UX-01B20D` exact Memory-record forget confirmation through
  the existing preview authority and shared governed operation.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: B20B issues a current-session, action/resource/scope/state/
  revision/digest-bound preview handle and B20C can atomically reject a stale
  expected snapshot, but no confirmation route consumes the handle or invokes
  the shared operation. The preview remains write-free and durable deletion is
  still unavailable to the local HTTP workflow.
- `selection_reason`: all security, freshness, and mutation owners now exist.
  Wiring them in the application composition root is the smallest reversible
  step and avoids a second mutation implementation, caller-supplied record ID,
  or non-atomic query-then-write check.
- `implementation_contract`: register only mutation-marked
  `POST /governance/api/v1/memory/records/:resourceHandle/confirm` with the
  existing Memory-read purpose and `memory_record` kind. After the shared HTTP
  boundary authenticates, rejects queries, validates Origin/CSRF and body
  bounds, and resolves the exact current-session scope/resource authority,
  accept only exact JSON `{ confirm: true, previewHandle: HANDLE }`. Consume the
  preview once using the same session, local-admin actor, forget action,
  resource kind/ID, and exact scope. Map unknown/denied authority to fixed
  `404` and already-consumed authority to fixed `409`. Recompute only B20A for
  the registry-owned record and require exact state, revision, and digest parity
  with the consumed binding before mutation; mismatch or absence is fixed
  `409`. Call only B20C with the internal ID, exact scope, recomputed non-deleted
  state, bound revision, and fixed reason code
  `governance_http_forget_confirmed`. Map `not_found` to fixed `404`, `stale` to
  fixed `409`, and success to a bounded identifier-free `200` result containing
  action/outcome, B20A record reference and scope kind, deleted state/resulting
  revision, fixed revision/audit/retrieval effect classifications, and the
  separate-restore rollback boundary. Add no restore route, raw identifier,
  caller reason, query-service/repository/registry change, or direct SQL.
- `allowed_paths`: `src/index.ts`,
  `tests/integration/governance-http-wiring.test.ts`, `docs/architecture.md`,
  `docs/deployment.md`, `docs/governance-ui.md`, `docs/security-privacy.md`, and
  this checkpoint only.
- `protected_paths`: B12/B16/B17/B20A query methods and DTOs; B20C service and
  its CLI/QQ string behavior; `GovernanceHttpServer`; scope/resource/preview
  registries and session lifecycle; memory repository, schema, migrations,
  dependencies, lockfile, ContextBuilder, Pi, tools, workers, browser, Provider,
  private/live data, deployment execution, restart, container, persistence
  format, and every prior slice.
- `assertions`: unauthenticated, query-bearing, wrong-Origin, missing/wrong/
  cross-session CSRF, missing/malformed/cross-purpose/cross-session scope or
  resource authority, malformed/empty/array/non-JSON/extra-key/wrong-confirm/
  malformed-handle bodies, oversize, and timeout paths fail at the existing
  fixed boundary before preview consumption, B20A, or service mutation. Unknown
  or wrong-action preview authority is `404`; reuse is `409`. A consumed handle
  with missing/deleted, state-drifted, revision-drifted, or digest-mismatched
  current B20A evidence is `409` and service-free. Exact authority calls B20A
  once and B20C once with no caller-controlled ID/scope/state/revision/reason,
  writes one delete revision and one redacted audit, excludes an active record
  from retrieval immediately, returns only the fixed result, and leaves other
  records intact. A repeated preview from the old snapshot conflicts; integrity
  and foreign keys remain clean. Existing preview/list/detail, maintenance
  confirmations, CLI/QQ, and default-off listener behavior remain unchanged.
- `focused_commands`: fail-first then final extension of the existing Memory-
  record wiring test; full governance HTTP wiring; query/service/HTTP/scope/
  resource/preview-registry adjacency; canonical P8; `pnpm typecheck`; scoped
  ESLint; `pnpm release:check`; diff, migration/dependency, inventory/privacy
  hygiene; and exact six-path private reverse-first and forward-first proof.
- `schema_dependency_live_boundary`: application route wiring, synthetic
  integration coverage, and canonical route/security docs only. No query-
  service, generic HTTP/registry/session, governance-service/repository, schema,
  migration, dependency, lockfile, CLI, QQ, Provider, private DB, deployment,
  restart, container, browser, or live operation change is permitted.
- `rollback_boundary`: the exact B20C candidate for all six product/test paths
  is captured byte-for-byte and mode-for-mode under mode-`0700`
  `/tmp/lethebot-gov-ux-01b20d-roundtrip-7gDUE1/baseline`; every file is mode
  `0644`. After implementation, create one exact six-path patch and independently
  prove reverse-first and forward-first restoration without touching the
  workspace or prior rollback artifacts.
- `exact_fail_first_action`: extend only
  `serves only the exact scoped bounded Memory-record page and provenance detail`
  with confirmation spies and the exact confirmation boundary matrix before
  editing production or route/security docs. Run only that case and require its
  first unauthenticated confirmation assertion to fail because the absent route
  returns `404` instead of `401`, with zero preview consumption, B20A
  recomputation, governance-service call, or durable write.
- `fail_first_evidence`: one initial synthetic-fixture assertion incorrectly
  used the FTS search path and was corrected to ordinary
  `MemoryRepository.retrieve` before production edits. The structurally valid
  focused run then collected 22 tests, skipped 21 unrelated cases, and failed
  only because the absent unauthenticated confirmation route returned `404`
  instead of `401`; preview consumption, B20A recomputation, B20C invocation,
  and durable writes remained zero.
- `final_evidence`: the focused confirmation regression passed 1 / 1 with 21
  skipped, full governance HTTP wiring passed 22 / 22, seven-file query/
  service/HTTP/scope/resource/preview-registry adjacency passed 96 / 96, and the
  canonical P8 gate passed 6 files / 292 tests. `pnpm typecheck`, scoped ESLint,
  and `pnpm release:check` passed; release recorded 132 / 1 skipped files and
  2711 / 10 skipped tests. The new confirmation route accepts only exact
  `{ "confirm": true, "previewHandle": HANDLE }` under the existing auth,
  Origin/CSRF, body, session, scope, and resource boundaries; consumes exact
  action/resource/scope authority once; recomputes B20A state, revision, and
  digest; and calls only B20C's atomic expected-snapshot overload with fixed
  reason code `governance_http_forget_confirmed`. Unknown or not-found authority
  is fixed `404`, stale or reused authority is fixed `409`, and success returns
  only bounded identifier-free deletion, revision, audit, retrieval, and
  separate-restore evidence. Existing preview/list/detail, maintenance,
  CLI/QQ, listener, schema, dependency, and repository behavior remains
  unchanged; restore stays absent.
- `rollback_evidence`: the exact six-path patch is
  `/tmp/lethebot-gov-ux-01b20d-roundtrip-7gDUE1/gov-ux-01b20d.patch`
  (`sha256:d62d94309f09e6a7df07f546643606b86b6d5fb67a312915634d01ed218e15fa`,
  mode `0600`, size `31347`). Independent reverse-first and forward-first trees
  restored the exact B20C baseline and B20D candidate byte-for-byte and mode-
  for-mode without touching the workspace; all roots/directories are mode
  `0700`, all snapshot files are mode `0644`, and the candidate equals the
  workspace. The patch has exactly six paths; all added lines are ASCII while
  one pre-existing Chinese deployment heading appears as context. Inventory
  remains 68 tracked plus 49 untracked and zero staged, diff/privacy hygiene is
  clean, and migrations 007/008 retain SHA-256
  `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`/
  `95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`.
- `exact_next_action`: completed; the restore workflow reassessment selects
  B21A below. Establish one shared expected-snapshot mutation owner and route
  the existing CLI aliases through its legacy call before designing any restore
  preview or HTTP route.

## Completed Slice: `GOV-UX-01B21A`

- `requirement`: `GOV-UX-01B21A` HTTP-disabled shared atomic expected-snapshot
  Memory-record restore operation with existing CLI parity.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: B20D exposes a separately confirmed restore boundary after
  exact Memory deletion, but restore still belongs directly to
  `GovernanceCLI.enableMemory`, which reads and writes through
  `MemoryRepository`. There is no shared operation for a future HTTP preview to
  confirm with exact scope, lifecycle state, and latest-revision freshness.
- `selection_reason`: adding a restore preview or route now would either bypass
  the P8 shared-governance rule or introduce a query-then-write race. The
  repository already owns the allowed `disabled`/`rejected`/`deleted -> active`
  lifecycle transition, revision, audit, and retrieval behavior. Wrapping that
  transition in the existing governance owner and delegating the two CLI aliases
  is the smallest dependency-ordered slice.
- `implementation_contract`: add
  `GovernanceService.restoreMemoryAsLocalAdmin` with a legacy string call and a
  typed expected-snapshot overload. The string call accepts only a restorable
  record, preserves the exact existing CLI actor, reason, audit summary,
  success/error messages, and durable effect, and returns only `restored` or
  `not_found`. The expected overload accepts only a clean internal record ID,
  normalized exact non-tool scope, expected `disabled`, `rejected`, or `deleted`
  state, positive safe latest revision, and clean bounded reason code. Inside
  one immediate transaction, select the record plus latest revision, conceal
  missing, cross-scope, and non-restorable records as `not_found`, return
  `stale` for invalid or mismatched state/revision evidence, and call only the
  existing repository state transition on an exact match. Success appends one
  restore revision and one redacted audit, returns the resulting revision, and
  makes a normal active record immediately retrievable. Route
  `GovernanceCLI.enableMemory` and its `restoreMemory` alias through the string
  call without changing command grammar or output. Pass the smoke script's
  already-owned synthetic database through the existing CLI options so it uses
  the same production path. Add no preview, handle, route, query projection, or
  direct SQL outside the governance owner.
- `allowed_paths`: `src/governance/service.ts`, `src/cli/governance.ts`,
  `scripts/smoke-test.ts`,
  `tests/unit/governance/service.test.ts`,
  `tests/unit/cli/governance.test.ts`, `docs/governance-ui.md`, and this
  checkpoint only.
- `protected_paths`: B20A-D query, preview, route, and forget semantics;
  `GovernanceHttpServer`; scope/resource/preview registries and session
  lifecycle; Memory repository implementation and transition state machine;
  QQ commands; other CLI mutations and command grammar/output; ContextBuilder,
  Pi, tools, workers, browser, schema, migrations, dependencies, lockfile,
  Provider, private/live data, deployment, restart, container, persistence
  format, and every prior slice.
- `assertions`: legacy `enableMemory` and `restoreMemory` restore disabled,
  rejected, and deleted records with byte-compatible results, actor/reason/audit
  evidence, redaction, revision history, retrieval effects, and missing/active/
  proposed/superseded failures, while delegating once to the shared service.
  Expected-snapshot calls cover global, user, group, private/group conversation,
  and system matches plus all three restorable states; exact scope comparison
  rejects owner and conversation-type/group collisions. Missing, cross-scope,
  proposed/active/superseded records return `not_found`; malformed/non-positive/
  fractional/unsafe revisions, invalid state/reason inputs, missing/malformed
  latest revisions, state drift, and revision drift return `stale`. Every
  failure is write-free. Exact success writes one active restore revision and
  one redacted local-admin audit, becomes immediately retrieval-visible, and
  leaves other records intact. Retry, simulated drift, rollback on a child-write
  failure, total changes, integrity, and foreign keys are
  proved. B20D and all HTTP routes remain unchanged.
- `focused_commands`: existing CLI restore characterization; one fail-first then
  final named governance-service regression; full governance-service and CLI
  unit coverage; selected spawned `enable-memory`/`restore-memory` lifecycle and
  redaction coverage; deterministic smoke coverage; governance HTTP wiring non-
  use; the canonical P8 command; `pnpm typecheck`; scoped ESLint;
  `pnpm release:check`; diff, migration/dependency, inventory/privacy hygiene;
  and exact six-path private reverse-
  first and forward-first patch proof.
- `schema_dependency_live_boundary`: shared governance/CLI code, synthetic
  tests, and canonical behavior documentation only. No query projection,
  generic HTTP/registry/session or repository implementation, route, preview
  authority, schema, migration, dependency, lockfile, QQ execution, Provider,
  private DB, deployment, restart, container, browser, or live operation is
  permitted.
- `rollback_boundary`: the exact B20D candidate for all six product/test paths
  is captured byte-for-byte and mode-for-mode under mode-`0700`
  `/tmp/lethebot-gov-ux-01b21a-roundtrip-2cFS5U/baseline`; every file is mode
  `0644`. After implementation, create one exact six-path patch and
  independently prove reverse-first and forward-first restoration without
  touching the workspace or prior rollback artifacts. An earlier private root
  from the first setup attempt contains no copied file and remains inert.
- `exact_fail_first_action`: first run the existing two CLI restore unit cases as
  passing characterization. Then add one comprehensive named service regression
  before editing production or canonical behavior docs; require the focused run
  to fail only because `restoreMemoryAsLocalAdmin` is absent after all scope,
  state, revision, write, rollback, retrieval, integrity, and FK assertions are
  established.
- `fail_first_evidence`: both existing CLI restore characterization cases first
  passed with 50 unrelated cases skipped. The new named service regression then
  collected 9 tests, skipped 8 unrelated cases, and failed only because
  `service.restoreMemoryAsLocalAdmin is not a function`; its complete synthetic
  scope/state/revision/no-write/retrieval/rollback/integrity/FK fixture had
  already run to the first missing-operation call before production or
  canonical behavior documentation changed.
- `release_recovery_evidence`: the first full release attempt passed typecheck,
  full lint, build, and release preflight, then exposed one deterministic smoke
  failure because that script was the only production caller constructing
  `GovernanceCLI` without its already-owned database. It reported 2711 passing
  tests, 1 failing smoke test, and 10 skipped tests. The script was added to the
  rollback boundary and its one construction call now supplies `{ db }`; no
  repository fallback or second restore mutation path was introduced. All 3
  smoke tests then passed.
- `final_evidence`: the named service regression passed 1 / 1 with 8 skipped;
  full governance-service/CLI unit coverage passed 2 files / 61 tests; all 22
  governance HTTP wiring cases passed with no restore route; and 4 selected
  spawned lifecycle/redaction cases passed with 125 unrelated cases skipped.
  The canonical P8 gate passed 6 files / 293 tests. `pnpm typecheck`, scoped
  ESLint, and the final `pnpm release:check` passed; release recorded 132 / 1
  skipped files and 2712 / 10 skipped tests. The shared operation accepts only
  the three repository-restorable states, compares exact scope/state/latest
  revision inside one immediate transaction, returns fixed not-found/stale
  outcomes without writes, appends one restore revision and redacted audit on
  success, and restores ordinary retrieval immediately across global, user,
  group, private/group conversation, and system scopes. CLI enable/restore now
  delegates once while retaining its exact actor, reason, audit summary,
  messages, redaction, lifecycle, and process behavior; B20A-D, QQ, HTTP,
  repository, schema, dependency, and live behavior remain unchanged.
- `rollback_evidence`: the exact six-path patch is
  `/tmp/lethebot-gov-ux-01b21a-roundtrip-2cFS5U/gov-ux-01b21a.patch`
  (`sha256:44b0173e7dd20f26839775e891a53ab7edaee98d3809f9401d25e9932286011f`,
  mode `0600`, size `26327`). Independent reverse-first and forward-first trees
  restored the exact B20D baseline and B21A candidate byte-for-byte and mode-
  for-mode without touching the workspace; all roots/directories are mode
  `0700`, every snapshot file is mode `0644`, and the candidate equals the
  workspace. The patch has exactly six paths and zero non-ASCII added lines;
  one pre-existing non-ASCII context line remains. Inventory is now 69 tracked
  plus 49 untracked and zero staged, VCS-visible sensitive-filename and diff
  hygiene are clean, Pi dependencies remain `0.80.2`, and migrations 007/008
  retain SHA-256
  `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`/
  `95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`.
- `exact_next_action`: completed; perform the required full P8 critical-path
  reassessment after the B20D/B21A confirmation-and-shared-restore pair before
  selecting any restore preview, route, Privacy mutation, Operations workflow,
  or browser work.

## Completed Slice: `GOV-UX-01B21B`

- `requirement`: `GOV-UX-01B21B` HTTP-disabled exact-scope Memory-record
  restore preview.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: B20D returns a separately confirmed restore boundary after
  exact deletion, and B21A now owns one stale-safe shared restore mutation, but
  no fixed read-only projection proves the selected record's restorable state,
  latest revision, retrieval effect, or digest-bound expected snapshot before
  future HTTP authority can be issued.
- `selection_reason`: the required post-B20D/B21A P8 reassessment found restore
  preview to be the smallest dependency-ready slice. Privacy writes still run
  directly through their CLI repository path and first need shared typed
  governance ownership; Operations needs a coordinator and stopped-service
  restore handoff; browser work depends on completed APIs. B21B can reuse the
  proved exact-scope predicate and B21A state contract without adding a route,
  handle, write, repository, or new abstraction.
- `implementation_contract`: add only
  `GovernanceQueryService.getMemoryRecordRestorePreviewForScope({ scope,
  memoryId })`, `MEMORY_RECORD_RESTORE_ACTION`, its fixed projection type, and
  a domain-separated SHA-256 preview digest. Reuse
  `memoryRecordExactScopePredicate`; accept only a trimmed, control-free 1-256-
  code-point internal record ID; select only that exact record plus its latest
  revision number; and return `null` for a tool scope, missing/cross-scope or
  malformed ID, a non-restorable lifecycle state, absent/non-positive/non-
  integral/unsafe revision number, or an unsafe next revision. A valid preview
  accepts only `disabled`, `rejected`, or `deleted` and contains only action
  `memory.record.restore`, a restore-purpose-bound 16-character `recordRef`,
  `scopeKind`, current lifecycle state and revision, expected `active` state and
  next revision, the fixed durable effects `memory_record_state_transition`,
  `memory_revision_append`, and `audit_event_append`, fixed
  `restored_records_included` retrieval consequence, a supported rollback
  boundary requiring separate forget confirmation, and the digest. Raw record,
  owner, and scope values, text, source, subject, evaluator, revision/audit IDs,
  session/issuer data, unrestricted fields, selectors, and mutation inputs
  remain absent. Do not call or change B12/B16/B17/B20 reads,
  `GovernanceService`, or any repository mutation.
- `allowed_paths`: `src/governance/query-service.ts`,
  `tests/unit/governance/query-service.test.ts`, `docs/governance-ui.md`, and
  this checkpoint only.
- `protected_paths`: B12/B16/B17 Memory list/detail/catalog/issuer methods,
  DTOs, bounds, redaction, ordering, and HTTP routes; B20A-D forget preview,
  authority, confirmation, and shared mutation semantics; B21A restore service
  and CLI delegation; all generic HTTP application/server, authentication,
  session, CSRF, scope/resource/preview registry, routing, and browser code;
  maintenance, Explain, Privacy, Activity, ContextBuilder, Pi, tools, workers,
  repositories, schema, migrations, dependencies, lockfile, Provider, QQ
  execution, private/live data, deployment, restart, container, persistence
  format, and every prior slice.
- `assertions`: global, user, group, private-conversation, group-conversation,
  and system authority returns only the exact matching restorable record and
  latest valid revision; private/group conversation collisions and malformed
  owner rows remain isolated, and tool, missing, cross-scope, malformed-ID,
  proposed/active/superseded, missing/malformed/unsafe-revision cases return
  `null`. All three restorable lifecycle states are previewable. The exact fixed
  allowlist uses a purpose-isolated record reference, correct next revision,
  fixed effect/retrieval/rollback classifications, and a domain-separated
  reproducible digest. Serialization omits every raw identifier, owner/scope
  value, text, source, evaluator, session, and unrestricted field. Repeated
  previews are byte-stable and write-free; integrity/FKs stay clean;
  B12/B16/B17/B20 and `GovernanceService` remain uncalled/unchanged; no preview
  handle, route, confirmation, audit, state/retrieval mutation, or restore POST
  appears.
- `focused_commands`: one fail-first then final single-test run of
  `tests/unit/governance/query-service.test.ts` for `previews an exact-scope
  memory-record restore without mutation`; the full query-service file;
  query/service/preview-registry/HTTP-wiring adjacency; the canonical P8 command
  from `docs/long-term-development-constraints.md`; `pnpm typecheck`; scoped
  ESLint over the two TypeScript paths; `pnpm release:check`; `git diff
  --check`; unchanged migration/dependency checks; VCS-visible inventory and
  privacy hygiene; and an exact three-path private reverse-first and forward-
  first patch proof.
- `schema_dependency_live_boundary`: query-service code, synthetic tests, and
  canonical behavior documentation only. No persistence write, service,
  repository, registry/session/application/HTTP/browser behavior, route,
  preview handle, confirmation, schema, migration, dependency, lockfile, CLI,
  QQ, Provider, private DB, deployment, restart, container, or live operation
  is permitted.
- `rollback_boundary`: the exact B21A candidate for the three product/test
  paths is captured byte-for-byte and mode-for-mode under mode-`0700`
  `/tmp/lethebot-gov-ux-01b21b-roundtrip-emWfD8/baseline`; every directory is
  mode `0700` and every file is mode `0644`. The source/test/canonical-doc
  snapshots have SHA-256
  `3e7c2d5ac65b8078fe148b757ff0a2d4b6a512e7b91606ed572f97198615e691`,
  `033a0331f3c813269eefd563a03c8b5c6cc5aebf15f6d37ef38356bcc5dd0e22`,
  and `af1dea1066089b28d879d5994637b6a38d4e9a83f34507d25fb7f9698f2a39c8`.
  After implementation, create one exact three-path patch and independently
  prove reverse-first and forward-first restoration without touching the
  workspace or any prior rollback artifact.
- `exact_fail_first_action`: append the single comprehensive query-service
  regression before editing production source or canonical behavior docs, run
  only that named test, and require the sole failure to be the missing
  `getMemoryRecordRestorePreviewForScope` method after its fixture has exercised
  all fixed scope, state, revision, privacy, no-write, integrity, and FK
  assertions.
- `fail_first_evidence`: the focused Vitest run collected 42 query-service
  tests, skipped 41 unrelated cases, and failed only because
  `service.getMemoryRecordRestorePreviewForScope is not a function`. The full
  synthetic exact-scope, lifecycle/revision, allowlist/digest, privacy,
  legacy-read isolation, no-write, integrity, and FK fixture was fixed before
  production source or canonical behavior documentation changed.
- `final_evidence`: the focused final regression passed 1 / 1 with 41 skipped;
  the full query-service file passed 42 / 42; query/service/preview-registry/
  HTTP-wiring adjacency passed 77 / 77; and the canonical six-file P8 gate
  passed 294 / 294. `pnpm typecheck`, scoped ESLint, and
  `pnpm release:check` passed; release recorded 132 / 1 skipped files and
  2713 / 10 skipped tests. The query accepts only clean 1-256-code-point IDs,
  exact non-tool scope, a current `disabled`, `rejected`, or `deleted` state,
  and a positive safe integral latest revision with a safe successor. It
  returns a stable fixed restore-purpose projection with expected active
  revision, durable effects, retrieval inclusion, separately confirmed forget
  boundary, and domain-separated digest while omitting raw IDs, owners, scope
  values, text, provenance, evaluator/session data, and unrestricted fields.
  All failure and repeated-read cases are write-free and FK-clean; B12/B16/B17,
  B20A-D, B21A, CLI/QQ, generic HTTP/registry/session, repository, schema,
  dependency, and live behavior remain unchanged.
- `rollback_evidence`: the exact three-path patch is
  `/tmp/lethebot-gov-ux-01b21b-roundtrip-emWfD8/gov-ux-01b21b.patch`
  (`sha256:ec7d681c4676aba092995eeb22fef5097fd4fbec106f079d13aa24526f365743`,
  mode `0600`, size `22089`). Independent reverse-first and forward-first
  cycles restored the exact B21A baseline and B21B candidate byte-for-byte and
  mode-for-mode without touching the workspace; all roots/directories are mode
  `0700`, every snapshot/proof file is mode `0644`, and the candidate equals the
  workspace. An initial check-only invocation rejected an absolute
  `--directory` prefix before creating or applying a proof tree; the corrected
  `git -C` cycles passed. The patch has exactly three ASCII-only paths;
  inventory remains 69 tracked plus 49 untracked and zero staged, VCS-visible
  sensitive-filename and diff hygiene are clean, installed Pi dependencies
  remain `0.80.2`, and migrations 007/008 retain SHA-256
  `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`/
  `95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`.
- `exact_next_action`: implement `GOV-UX-01B21C` as a preview-only
  authenticated restore action through the existing Memory-record POST.
  Require current-session exact scope and `memory_record` resource authority,
  Origin/CSRF, and exact body `{ action: 'restore' }`; call only B21B, bind its
  current state/revision/digest into the existing single-use preview registry,
  and return the fixed preview plus opaque expiry. Add no confirmation, preview
  consumption, service call, audit, durable mutation, or retrieval change.

## Completed Slice: `GOV-UX-01B21C`

- `requirement`: `GOV-UX-01B21C` exact-scope Memory-record restore preview
  route and current-session preview-handle issuance.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: B21B owns a fixed write-free restore preview and B21A owns the
  eventual stale-safe mutation, but the existing Memory-record mutation POST
  accepts only `forget`. No authenticated path binds B21B's current restorable
  state, latest revision, and digest into current-session single-use authority.
- `selection_reason`: B17C already issues exact-session, purpose-, kind-, and
  scope-bound `memory_record` handles, while the generic mutation boundary and
  preview registry already own Origin/CSRF, body bounds, session lifecycle,
  expiry, capacity, and action-specific binding. Extending only the existing
  POST action dispatch is the smallest dependency-ordered step after B21B and
  requires no route, registry, query, service, repository, or persistence
  abstraction.
- `implementation_contract`: import `MEMORY_RECORD_RESTORE_ACTION` and extend
  only the already-registered mutation-marked
  `POST /governance/api/v1/memory/records/:resourceHandle`. After the shared
  boundary authenticates, rejects queries, enforces exact Origin/CSRF and body
  bounds, resolves the scope handle, and resolves the resource handle against
  the same current session, Memory-read purpose, `memory_record` kind, and exact
  scope, accept exact JSON body `{ action: 'restore' }` in addition to the
  unchanged exact forget body. For restore only, call B21B
  `getMemoryRecordRestorePreviewForScope({ scope, memoryId:
  resource.resourceId })`; map `null` to fixed `404`. For a valid preview, call
  the existing preview registry once with current session/expiry, local-admin
  actor, action `memory.record.restore`, resource kind/ID, exact scope, current
  lifecycle state/revision, and preview digest; return `201` with the unchanged
  B21B projection plus only `previewHandle` and `previewExpiresAt`. Keep B20B
  forget dispatch byte-compatible. Add no confirm path, preview consumption,
  service call, audit, state/revision write, or retrieval change.
- `allowed_paths`: `src/index.ts`,
  `tests/integration/governance-http-wiring.test.ts`, `docs/architecture.md`,
  `docs/deployment.md`, `docs/governance-ui.md`,
  `docs/security-privacy.md`, and this checkpoint only.
- `protected_paths`: B12/B16/B17/B20/B21B query projections, DTOs, exact-scope
  predicates, bounds, redaction, references, and digest; B20B forget preview
  dispatch and B20D confirmation semantics; B21A restore service and CLI
  delegation; `GovernanceHttpServer` and all scope/resource/preview registry,
  authentication, session, CSRF, generic routing, error, expiry, and capacity
  behavior; every other application route and handler; maintenance, Explain,
  Privacy, Activity, ContextBuilder, Pi, tools, workers, repositories, schema,
  migrations, dependencies, lockfile, QQ, Provider, browser, private/live data,
  deployment execution, restart, container, and every prior slice.
- `assertions`: unauthenticated, missing/wrong Origin or CSRF, query-bearing,
  missing/malformed/cross-session/cross-purpose scope, and malformed/unknown/
  cross-session resource requests fail before B21B or preview issuance. Only
  exact one-key `{ action: 'restore' }` reaches B21B; unknown actions, extra
  fields, invalid JSON, and the existing malformed forget bodies remain fixed
  `400`. Missing, cross-scope, non-restorable, revisionless, or unsafe records
  map to fixed `404` without issuance. Valid `disabled`, `rejected`, and
  `deleted` records return the unchanged fixed B21B projection plus a fresh
  opaque handle/expiry, and the registry receives only the exact session,
  actor, action, resource, scope, current state/revision, and digest. Handles
  remain session/action/resource/scope isolated; sending one to the existing
  forget-only confirm route is concealed and invokes neither forget nor
  restore. Responses omit raw record/owner/scope values, text, provenance,
  evaluator/session data, and unrestricted fields. Reads are write-free and
  FK-clean; no restore confirmation, service call, audit, lifecycle, revision,
  or retrieval effect occurs; B20B forget behavior remains unchanged.
- `focused_commands`: one fail-first then final named governance HTTP wiring
  regression for `previews exact Memory-record restores through current-session
  authority without mutation`; the full wiring file; query/service/preview-
  registry/application-HTTP adjacency; the canonical P8 command from
  `docs/long-term-development-constraints.md`; `pnpm typecheck`; scoped ESLint
  over the two TypeScript paths; `pnpm release:check`; diff, migration/
  dependency, VCS-visible inventory/privacy hygiene; and one exact six-path
  private reverse-first and forward-first patch proof.
- `schema_dependency_live_boundary`: application route wiring, synthetic tests,
  and canonical behavior documentation only. No query/service/repository/
  registry implementation, confirmation or durable mutation, schema,
  migration, dependency, lockfile, CLI/QQ, Provider, private DB, deployment,
  restart, container, browser, or live operation is permitted.
- `rollback_boundary`: the exact B21B candidate for all six product/test paths
  is captured byte-for-byte and mode-for-mode under mode-`0700`
  `/tmp/lethebot-gov-ux-01b21c-roundtrip-6xJVC7/baseline`; every file is mode
  `0644`. The source, test, architecture, deployment, governance, and security
  snapshots have SHA-256
  `5dd511ec65182a8f5f90c5c49b98b1e76e03e264297300a6deca518300586efc`,
  `4a4bdd8a9e3503b2fc10ad3ead7cbe443d28ca798aa832001cd2516d6d7094bf`,
  `9d357ca15208dade32c765b8087df4a1fc699cfed7deeeffdaa458e59bbcc669`,
  `e1aeb8a67a40a1b38ebcdb938921513c0c6fdf08fdba502f93d47e58a5be7593`,
  `aeb2efce04f972d070996fef2db74d071c37ca49cc412ac408213a2aa57ca1b4`,
  and `ccb6cffc6cc92dadb0785300cbedcca382177ef8f5c0b37580a395d7574a2483`.
  After implementation, create one exact six-path patch and independently prove
  reverse-first and forward-first restoration without touching the workspace or
  prior rollback artifacts.
- `exact_fail_first_action`: add only the named integration regression before
  production or canonical behavior docs, run that one case, and require the
  current forget-only handler to return `400` instead of the expected `201`
  after the shared authority boundary has passed, while B21B and preview
  issuance remain uncalled and all earlier negative-boundary assertions pass.
- `fail_first_evidence`: the first fixture attempt was invalid because deleting
  its synthetic record before its linked revision violated the revision foreign
  key and never reached HTTP dispatch. After deleting that synthetic revision
  first, the valid focused run collected 23 wiring tests, skipped 22 unrelated
  cases, passed every shared-boundary assertion, and failed only because the
  existing forget-only handler returned `400` instead of the expected `201` for
  exact `{ "action": "restore" }`. B21B and preview issuance remained uncalled.
  The first source branch then compared the HTTP body token to the domain action
  `memory.record.restore` and reproduced the same `400`; changing only that
  comparison to the public token `restore` completed the route dispatch.
- `final_evidence`: the focused final regression passed 1 / 1 with 22 skipped;
  full governance HTTP wiring passed 23 / 23; query/service/preview-registry/
  application-HTTP adjacency passed 5 files / 82 tests; and the canonical P8
  gate passed 6 files / 294 tests. `pnpm typecheck`, scoped ESLint, and
  `pnpm release:check` passed; release recorded 132 / 1 skipped files and
  2714 / 10 skipped tests. Exact current-session Memory scope/resource
  authority now accepts only the additional one-key restore token, delegates
  only to B21B, and issues fresh session/action/resource/scope/state/revision/
  digest-bound restore authority. Missing and non-restorable records stay fixed
  `404`; malformed and cross-boundary requests fail before delegation; the
  existing forget preview/confirmation remains unchanged; restore confirmation,
  preview consumption, service mutation, audit, revision, and retrieval effects
  remain absent. Repeated reads are write-free and integrity/FKs are clean.
- `rollback_evidence`: the exact six-path patch is
  `/tmp/lethebot-gov-ux-01b21c-roundtrip-6xJVC7/gov-ux-01b21c.patch`
  (`sha256:d0b36694807e6e76bcc1152711f23319bd50cf70adce44d96428aa11478bb4f8`,
  mode `0600`, size `29114`). Independent reverse-first and forward-first
  cycles restored the exact B21B baseline and B21C candidate byte-for-byte and
  mode-for-mode without touching the workspace; all roots/directories are mode
  `0700`, every snapshot/proof file is mode `0644`, and the candidate equals the
  workspace. One strict-shell placeholder stopped before patch generation, and
  two proof-report attempts completed both patch cycles before a zsh reserved
  variable shadowed `PATH`; the corrected complete proof passed. The patch has
  exactly six ASCII-only paths; inventory remains 69 tracked plus 49 untracked
  and zero staged, VCS-visible sensitive-filename and diff hygiene are clean,
  installed Pi dependencies remain `0.80.2`, and migrations 007/008 retain
  SHA-256
  `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`/
  `95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`.
- `exact_next_action`: completed; the required post-B21B/B21C P8 reassessment
  selects B21D restore confirmation. B21C and B21A make it dependency-complete;
  Privacy mutation still needs shared governance ownership, Operations still
  needs a stopped-service coordinator, and browser work depends on those APIs.

## Completed Slice: `GOV-UX-01B21D`

- `requirement`: `GOV-UX-01B21D` exact Memory-record restore confirmation
  through single-use current-session authority and the shared stale-safe
  restore operation.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: B21C now issues a restore preview handle bound to the exact
  session, action, resource, scope, current state, revision, and digest, and
  B21A owns the atomic expected-snapshot restore mutation. The existing Memory
  confirmation route consumes only forget authority, so the proved restore
  preview cannot yet be explicitly confirmed.
- `selection_reason`: the post-B21B/B21C critical-path reassessment compared the
  remaining workflow families. Restore confirmation is the smallest complete
  dependency chain and closes the record lifecycle without a new abstraction.
  Privacy writes still bypass a shared typed governance mutation owner;
  Operations backup/restore needs an operations coordinator and stopped-service
  handoff; and the browser should consume completed APIs rather than define
  mutation semantics. B21D therefore precedes those larger slices.
- `implementation_contract`: extend only the existing mutation-marked
  `POST /governance/api/v1/memory/records/:resourceHandle/confirm`. Preserve
  exact two-key `{ confirm: true, previewHandle }` as the forget confirmation.
  Accept restore only as exact three-key `{ confirm: true, previewHandle,
  action: 'restore' }`. After the shared authentication, query, Origin/CSRF,
  body, scope, and current-session resource checks, consume the preview once
  using domain action `memory.record.restore`, the exact resource kind/ID and
  scope. Map denied/unknown authority to fixed `404` and reused authority to
  fixed `409`. Recompute only B21B, require its state/revision/digest to equal
  the consumed binding, and invoke only B21A
  `restoreMemoryAsLocalAdmin({ memoryId, scope, expectedState,
  expectedRevisionNumber, reasonCode: 'governance_http_restore_confirmed' })`.
  Map service `not_found` to `404`, `stale` to `409`, and success to a fixed
  identifier-free result containing action/outcome/reference/scope, current
  active state and returned revision, copied durable/retrieval evidence,
  `restore` revision plus `memory.restore` audit evidence, and the preview's
  separate-forget rollback boundary. Add no query, registry, service,
  repository, schema, CLI, QQ, or browser behavior.
- `allowed_paths`: `src/index.ts`,
  `tests/integration/governance-http-wiring.test.ts`, `docs/architecture.md`,
  `docs/deployment.md`, `docs/governance-ui.md`,
  `docs/security-privacy.md`, and this checkpoint only.
- `protected_paths`: B12/B16/B17/B20/B21B query projections and digests; B20B
  forget preview dispatch and B20D exact confirmation grammar/status/result;
  B21A service validation, transaction, legacy CLI aliases, revision/audit, and
  retrieval semantics; all scope/resource/preview registry and generic HTTP
  authentication/session/CSRF/body/error/expiry/capacity behavior; every other
  route; maintenance, Explain, Privacy, Activity, ContextBuilder, Pi, tools,
  workers, repositories, schema, migrations, dependencies, lockfile, QQ,
  Provider, browser, private/live data, deployment execution, restart,
  container, and every prior slice.
- `assertions`: unauthenticated, query-bearing, wrong Origin/CSRF, malformed
  body/handle, and invalid scope/resource authority fail through the existing
  fixed boundary before restore preview recomputation or service mutation.
  Only the exact three-key restore confirmation selects restore; the existing
  exact two-key forget confirmation remains byte-compatible. Wrong session,
  action, resource, kind, or scope authority is concealed as `404`; reused
  authority is `409`; no denied handle is consumed. A consumed handle whose
  record disappeared, became non-restorable, or changed state/revision/digest
  returns `409` before B21A. B21A receives only the resolved internal ID, exact
  scope, preview state, consumed revision, and fixed reason. Its `not_found` and
  `stale` outcomes map to `404` and `409`; a successful disabled/rejected/
  deleted restore returns fixed redacted evidence, creates exactly one next
  `restore` revision and `memory.restore` audit, makes the record immediately
  active-retrievable, and leaves sources/history intact. Retry is `409` with no
  second effect. Responses omit raw record/owner/scope values, content,
  provenance, evaluator/session data, and unrestricted fields; integrity/FKs
  stay clean; forget preview/confirmation and CLI aliases retain parity.
- `focused_commands`: extend and rename the B21C Memory restore wiring case,
  run it once fail-first and once final; run the full wiring file; query/service/
  preview-registry/application-HTTP adjacency; the canonical P8 command;
  `pnpm typecheck`; scoped ESLint over the two TypeScript paths;
  `pnpm release:check`; diff, migration/dependency, VCS-visible inventory and
  privacy hygiene; and one exact six-path private reverse-first and
  forward-first patch proof.
- `schema_dependency_live_boundary`: application confirmation wiring,
  synthetic integration coverage, and canonical route/security docs only. No
  query/service/repository/registry implementation, schema, migration,
  dependency, lockfile, CLI/QQ, Provider, private DB, deployment, restart,
  container, browser, or live operation is permitted.
- `rollback_boundary`: the exact B21C candidate for all six product/test paths
  is captured byte-for-byte and mode-for-mode under mode-`0700`
  `/tmp/lethebot-gov-ux-01b21d-roundtrip-iwnyMj/baseline`; every file is mode
  `0644`. The source, test, architecture, deployment, governance, and security
  snapshots have SHA-256
  `ce0f30b9be5a2ebaa632ceb7ced1ec0e0b90f3742e2a26850e2461ed84c02b2c`,
  `f3811fb6278443445273448a151d372267bcaf696e3375fa355dd90a6acbef80`,
  `786365e0e02bce53b03d13e1456b9d2b990429b550571363c04f575fe33e156a`,
  `4a644fbbad7926edfa5eb39f542853fb14f5f24d2ed483fc03b92ff26efe7281`,
  `ee084b08f43724de98303456159dc926842b401c0d7121efaea07956225ec7e4`,
  and `5f6d777089b1638018801a5f168441e63176a4bf25b3035477b39bc86c7bbb71`.
  After implementation, create one exact six-path patch and independently prove
  reverse-first and forward-first restoration without touching the workspace or
  any prior rollback artifact.
- `exact_fail_first_action`: extend and rename only the existing B21C restore
  integration case with the confirmation boundary, stale/single-use/service-
  outcome, durable-effect, retrieval, privacy, integrity, and FK matrix before
  editing production or canonical behavior docs. Run only that named case and
  require the valid exact restore confirmation to fail because the current
  forget-only confirmation parser returns `400` instead of expected `200`,
  while all B21C preview and preceding confirmation-boundary assertions pass.
- `fail_first_evidence`: the valid focused integration run collected 23 wiring
  tests, skipped 22 unrelated cases, passed the B21C preview plus every shared
  authentication, Origin/CSRF, query/body, session, scope, resource, and action
  boundary assertion, and failed only because exact restore confirmation
  returned the forget-only parser's `400` instead of expected `200`.
- `final_evidence`: the focused final regression passed 1 / 1 with 22 skipped;
  full governance HTTP wiring passed 23 / 23; query/service/preview-registry/
  application-HTTP adjacency passed 5 files / 82 tests; and the canonical P8
  gate passed 6 files / 294 tests. `pnpm typecheck`, scoped ESLint, and
  `pnpm release:check` passed; release recorded 132 / 1 skipped files and
  2714 / 10 skipped tests. The existing confirmation path now distinguishes
  only exact two-key forget and exact three-key restore bodies, consumes only
  matching current-session action/resource/scope authority once, recomputes the
  B21B state/revision/digest, and calls only B21A with the exact expected
  snapshot and fixed reason. Unknown/service-not-found remains `404`, stale or
  reused authority remains `409`, and successful disabled/rejected/deleted
  restores return only fixed identifier-free revision/audit/retrieval evidence.
  Success appends exactly one `restore` revision and `memory.restore` audit,
  immediately restores active retrieval, preserves sources/history, and leaves
  the B20 forget branch byte-compatible. Responses remain private; integrity
  and foreign keys are clean.
- `rollback_evidence`: the exact six-path patch is
  `/tmp/lethebot-gov-ux-01b21d-roundtrip-iwnyMj/gov-ux-01b21d.patch`
  (`sha256:1cba0ea522f9b5af0cbbc982a4d40b2b3070225db2fd730c7e545be045889ab5`,
  mode `0600`, size `29417`). Independent reverse-first and forward-first
  cycles restored the exact B21C baseline and B21D candidate byte-for-byte and
  mode-for-mode without touching the workspace or prior artifacts; all roots
  and directories are mode `0700`, snapshot/proof files are mode `0644`, and
  the candidate equals the workspace. The patch has exactly six paths and only
  ASCII added lines. Inventory remains 69 tracked plus 49 untracked with zero
  staged, VCS-visible sensitive-filename and diff hygiene are clean, installed
  Pi dependencies remain `0.80.2`, and migrations 007/008 retain SHA-256
  `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`/
  `95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`.
- `exact_next_action`: completed; the post-B21D P8 reassessment selects B22A.
  Exact-user Privacy reads are already dependency-complete, but their two CLI
  writes still instantiate the repository directly. Operations still needs a
  coordinator and stopped-service restore handoff, while browser work should
  consume complete shared APIs rather than define mutation semantics.

## Completed Slice: `GOV-UX-01B22A`

- `requirement`: `GOV-UX-01B22A` HTTP-disabled shared local-admin Privacy
  preference mutation owner with existing CLI parity.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: B15A-D provide purpose-isolated exact-user Privacy discovery
  and a fixed read page, but `GovernanceCLI.setPrivacyOptOut` and
  `clearPrivacyOptOut` each instantiate `PrivacyPreferenceRepository` and write
  directly. A future HTTP preview/confirm path would otherwise create a second
  mutation owner and violate the P8 UI/CLI shared-governance contract.
- `selection_reason`: centralizing the two already-proved writes is the
  smallest dependency-ordered Privacy step after the completed Memory lifecycle.
  It changes no durable semantics and establishes one typed owner before any
  preview, authority, or route is designed. Operations backup/restore requires
  a broader coordinator and stopped-service boundary; browser implementation
  still depends on the missing Privacy and Operations APIs.
- `implementation_contract`: inject `PrivacyPreferenceRepository` into
  `GovernanceService` and add one typed
  `setPrivacyPreferenceAsLocalAdmin({ canonicalUserId, preferenceType, state,
  reason, now? })` operation. Delegate exactly once to the existing repository
  `setPreference`, preserving local-admin actor user `admin`, actor class
  `admin`, invocation context `admin_cli`, caller reason, optional deterministic
  clock, repository transaction, redaction, upsert, audit, and enforcement
  semantics. Return only fixed `updated` success after the repository completes;
  preserve repository errors. Route only the existing CLI set/clear methods
  through this operation, retaining their default reasons, command grammar,
  success/error messages, list reads, and durable rows. Remove only the CLI's
  now-unused repository import. Add no expected-snapshot behavior, preview,
  confirmation, HTTP route, scope/resource authority, or new policy.
- `allowed_paths`: `src/governance/service.ts`, `src/cli/governance.ts`,
  `tests/unit/governance/service.test.ts`,
  `tests/unit/cli/governance.test.ts`, `docs/governance-ui.md`, and this
  checkpoint only.
- `protected_paths`: `PrivacyPreferenceRepository` implementation, schema,
  redaction, transaction, audit, and enforcement consumers; B15A-D query DTO,
  scope handles, routes, and read behavior; generic HTTP/session/CSRF/scope/
  resource/preview registries; Memory and maintenance governance; QQ command
  grammar; other CLI commands; Operations, retention, identity and summary
  mutations; ContextBuilder, Pi, tools, workers, browser, migrations,
  dependencies, lockfile, Provider, private/live data, deployment, restart,
  container, persistence format, and every prior slice.
- `assertions`: the existing CLI set/list/clear case passes before behavior
  edits. The new service operation writes both `proactive_dm` and
  `memory_association`, both `opted_out` and `opted_in`, with exact target user,
  type, state, caller reason, actor, and optional time; its repository row and
  one redacted `privacy.preference_set` audit remain atomic and immediately
  visible to existing enforcement reads. Secret/platform-like reason text is
  redacted exactly once by the repository. Invalid/missing users and forced
  audit failure preserve existing error/rollback behavior. CLI set and clear
  each call only the shared service once with their exact existing defaults or
  supplied reason, return byte-compatible success/error results, and no longer
  instantiate the repository. Privacy HTTP remains read-only, repeated reads
  remain stable, integrity/FKs stay clean, and all prior routes are unchanged.
- `focused_commands`: first run the existing CLI privacy lifecycle case as
  passing characterization; add and run one fail-first then final named
  governance-service regression; run full governance-service/CLI/privacy-
  repository unit adjacency; selected spawned CLI privacy lifecycle and
  redaction cases; governance HTTP wiring non-use; the canonical P8 command;
  `pnpm typecheck`; scoped ESLint; `pnpm release:check`; diff, migration/
  dependency, VCS-visible inventory/privacy hygiene; and one exact five-path
  private reverse-first and forward-first patch proof.
- `schema_dependency_live_boundary`: shared governance/CLI mutation ownership,
  synthetic tests, and canonical behavior documentation only. No repository,
  query, HTTP/registry/session, preview/confirm, schema, migration, dependency,
  lockfile, QQ, Operations, Provider, private DB, deployment, restart,
  container, browser, or live operation is permitted.
- `rollback_boundary`: the exact B21D candidate for the five product/test paths
  is captured byte-for-byte and mode-for-mode under mode-`0700`
  `/tmp/lethebot-gov-ux-01b22a-roundtrip-B1SanC/baseline`; every file is mode
  `0644`. The service, CLI, service-test, CLI-test, and governance-doc snapshots
  have SHA-256
  `3766e142e3981de3630440ae82488c5c0ff34b3e5313463c92985515cd3c73ae`,
  `3039fa6637a4c7d52002f75ec6bc1de5cfbcba4cd1c484ba2b1cb98aac44774e`,
  `fa3fbe53b2a1dfaed8b05756c2c94c482139623e5603df29ebf6c0f054b92553`,
  `d64fcc3e2238fc4e7f24fdbefdf69b3e26b0fa1f5f238fcf2e9320674e11bf51`,
  and `7a137e1f7a0ef060d4acdfd407188c1300c95fcbeb4ef162c8a1f978a6151a8c`.
  After implementation, create one exact five-path patch and independently
  prove reverse-first and forward-first restoration without touching the
  workspace or prior rollback artifacts.
- `exact_fail_first_action`: completed as specified: the existing CLI lifecycle
  characterization passed before production edits, then the comprehensive
  service regression was added and run alone.
- `fail_first_evidence`: the CLI characterization passed 1 / 1 with 51 skipped;
  the new service run collected 10 cases, skipped nine unrelated cases, and
  failed only because `setPrivacyPreferenceAsLocalAdmin` was absent.
- `final_evidence`: the focused service/CLI pair passed 2 / 2 with 60 skipped;
  governance-service/CLI/privacy-repository adjacency passed 69 / 69; three
  selected spawned CLI Privacy cases passed with 126 skipped; governance HTTP
  non-use wiring passed 23 / 23; and the canonical P8 gate passed 6 files / 295
  tests. `pnpm typecheck`, scoped and full ESLint, and `pnpm release:check`
  passed; release recorded 132 / 1 skipped files and 2715 / 10 skipped tests.
  The shared operation delegates once with exact local-admin actor, context,
  reason, state, type, target user, and optional clock while preserving the
  repository transaction, redaction, audit, enforcement, and errors. Both CLI
  writes delegate once with their byte-compatible defaults and results. Privacy
  HTTP stays read-only, integrity and foreign keys are clean, Pi dependencies
  remain `0.80.2`, and migrations 007/008 are unchanged.
- `rollback_evidence`: the exact five-path patch is
  `/tmp/lethebot-gov-ux-01b22a-roundtrip-B1SanC/gov-ux-01b22a.patch`
  (`sha256:0ab4df940cca7cc9f0f91ea498a8a7ac36dd7889e5c6d3dfd823048b5111a63f`,
  mode `0600`, size `14769`). Independent reverse-first and forward-first
  cycles restored the exact B21D baseline and B22A candidate byte-for-byte and
  mode-for-mode without touching the workspace or prior artifacts; the
  candidate equals the workspace. The patch contains exactly the five allowed
  paths and only ASCII additions. Inventory remains 69 tracked plus 49
  untracked with zero staged, diff and VCS-visible sensitive-filename hygiene
  are clean, and migrations 007/008 retain SHA-256
  `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`/
  `95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`.
- `exact_next_action`: completed; the post-B22A reassessment selects B22B.
  Exact-user Privacy reads and shared mutation ownership are now available, so
  a read-only state-change preview is the smallest dependency-complete step.
  Operations still needs a coordinator and stopped-service restore handoff;
  browser work should consume completed mutation APIs.

## Completed Slice: `GOV-UX-01B22B`

- `requirement`: `GOV-UX-01B22B` HTTP-disabled exact-user Privacy preference-
  change preview projection.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: B15A-D expose an exact-user read page and B22A owns the
  existing Privacy writes, but there is no fixed preview that can bind one
  requested preference state change to the current effective state without
  exposing an owner identifier or mutating durable state.
- `selection_reason`: a query-only preview is the smallest dependency-ordered
  step after B22A and establishes the state/effect/rollback contract before
  authority issuance or expected-snapshot mutation. Operations requires a
  broader coordinator, and browser work remains downstream of complete APIs.
- `implementation_contract`: add only
  `GovernanceQueryService.getPrivacyPreferenceChangePreviewForScope({ scope,
  preferenceType, targetState })`. Accept only a valid exact user represented
  by `canonical_users`, either fixed supported preference type, and either
  fixed target state. Read only that exact preference. Treat an absent row as
  effective `opted_in` with implicit-default version evidence; represent a
  stored row with its bounded update timestamp. Return `null` for non-user,
  malformed, missing-user, invalid persisted evidence, invalid type/state, and
  effective no-op inputs. Otherwise return only fixed action
  `privacy.preference.change`, preference type, identifier-free current and
  target state/version evidence, fixed preference-upsert/audit/immediate-
  enforcement effects, semantic rollback to the current state through a
  separate confirmation boundary, and a SHA-256 digest over the exact payload
  under domain `lethebot-governance:privacy-preference-change-preview:v1`.
  Register no route, issue no authority, and perform no write.
- `allowed_paths`: `src/governance/query-service.ts`,
  `tests/unit/governance/query-service.test.ts`, `docs/governance-ui.md`, and
  this checkpoint only.
- `protected_paths`: B15A-D catalog/page/route and purpose semantics; B22A
  service/repository/CLI mutation behavior; HTTP application/server, session,
  CSRF, scope/resource/preview registries, and every existing route; Memory,
  Explain, Activity, maintenance, Operations, retention, identity, and summary
  behavior; schema, migrations, dependencies, lockfile, QQ, browser, Provider,
  private/live data, deployment, restart, container, persistence format, and
  every prior slice.
- `assertions`: both preference types and both state transitions project exact
  fixed evidence. A missing row previews only the effective `opted_in` to
  `opted_out` transition; a stored row preserves only validated presence and
  update-time version evidence. Repeated previews are byte-stable and digest-
  stable. Non-user, malformed, missing-user, same-state, unsupported type/state,
  malformed persisted state, and unsafe timestamp inputs return `null` without
  widening reads. Raw canonical/updater IDs, reasons, actor context, secrets,
  platform-like values, and repository rows remain absent. No existing query
  helper, mutation owner, issuer, registry, or HTTP route is called; total
  changes, integrity, and foreign keys remain unchanged.
- `focused_commands`: add and run one named fail-first query-service regression;
  run the final named case; full query-service tests; query/privacy-repository/
  service/CLI and Privacy HTTP adjacency; governance HTTP wiring non-exposure;
  the canonical P8 command; `pnpm typecheck`; scoped and full ESLint;
  `pnpm release:check`; diff, migration/dependency, inventory/privacy hygiene;
  and one exact three-path private reverse-first and forward-first patch proof.
- `schema_dependency_live_boundary`: query projection, synthetic unit coverage,
  and canonical behavior documentation only. No mutation, expected-snapshot
  service behavior, authority/registry/route, schema, migration, dependency,
  lockfile, QQ, Operations, Provider, private DB, deployment, restart,
  container, browser, or live operation is permitted.
- `rollback_boundary`: the exact B22A candidate for the three product/test paths
  is captured byte-for-byte and mode-for-mode under mode-`0700`
  `/tmp/lethebot-gov-ux-01b22b-roundtrip-KoxfT4/baseline`; every file is mode
  `0644`. The query service, query-service test, and governance-doc snapshots
  have SHA-256
  `973568b4d3696e29e1cb0dc813318a06fc4bd0c6881edc1c2ab3474fe0391e5f`,
  `ede60d00cdb37cbf6c37546a54b6b29de0e3bb1dc8ebe72d69cc8406843a4885`,
  and `d5f6f1e326759e193afb9005680682c97fbfd1c12a148a7527d2be21ce3f4e97`.
  After implementation, create one exact three-path patch and independently
  prove reverse-first and forward-first restoration without touching the
  workspace or prior rollback artifacts.
- `exact_fail_first_action`: completed as specified: the comprehensive
  query-service regression was added before production or canonical behavior-
  doc edits and run alone.
- `fail_first_evidence`: the focused Vitest run collected 43 query-service
  cases, skipped 42 unrelated cases, and failed only because
  `getPrivacyPreferenceChangePreviewForScope` was absent.
- `final_evidence`: the focused final regression passed 1 / 1 with 42 skipped,
  the full query-service file passed 43 / 43, five-file Privacy adjacency passed
  127 / 127, governance HTTP wiring passed 23 / 23, and the canonical six-file
  P8 gate passed 296 / 296. `pnpm typecheck`, scoped and full ESLint, and
  `pnpm release:check` passed; release recorded 132 / 1 skipped files and 2716 /
  10 skipped tests. One exact-user left-join read now treats an absent preference
  as effective `opted_in` with `implicit_default` evidence, exposes only a
  validated stored state and update timestamp, rejects invalid or effective
  no-op inputs, and returns fixed upsert/audit/immediate-enforcement plus
  semantic rollback evidence under the domain-separated digest. Repeated reads
  are stable and write-free; no route, authority, service mutation, durable
  write, or enforcement change was added. The initial final focused run exposed
  only a synthetic invalid-CHECK fixture cleanup issue; correcting that test
  cleanup required no production change. Integrity and foreign keys are clean,
  Pi dependencies remain `0.80.2`, and migrations 007/008 are unchanged.
- `rollback_evidence`: the exact three-path patch is
  `/tmp/lethebot-gov-ux-01b22b-roundtrip-KoxfT4/gov-ux-01b22b.patch`
  (`sha256:d30005770256726e99237b00017dfe63dc681ce2ee58115b7a1a5fe75a474c54`,
  mode `0600`, size `20187`). Independent reverse-first and forward-first
  cycles restored the exact B22A baseline and B22B candidate byte-for-byte and
  mode-for-mode without touching the workspace or prior artifacts; the
  candidate equals the workspace. The patch contains exactly the three allowed
  paths and only ASCII additions. Two transient proof-script issues were
  corrected before the valid proof: shell `PATH` shadowing and mode-`077` umask
  recreation of applied files. Inventory remains 69 tracked plus 49 untracked
  with zero staged, diff and VCS-visible sensitive/reject/original-artifact
  hygiene are clean, and migrations 007/008 retain SHA-256
  `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`/
  `95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`.
- `exact_next_action`: completed; expose B22B through the existing exact-user
  Privacy scope and single-use preview registry without adding confirmation or
  mutation behavior.

## Completed Slice: `GOV-UX-01B22C`

- `requirement`: `GOV-UX-01B22C` exact-user Privacy preference-change preview
  route and current-session authority issuance.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: B15A-D already provide exact-user Privacy discovery and the
  bounded preference page, and B22B now owns a fixed write-free change preview,
  but the HTTP boundary cannot request that preview or bind it to one exact
  session, user scope, preference type, current state/version, and digest.
- `selection_reason`: exposing the already-proved projection through existing
  scope and preview owners is the smallest dependency-ordered step. It keeps
  confirmation and expected-snapshot mutation separate, while Operations still
  requires a broader coordinator and browser work should consume complete APIs.
- `implementation_contract`: register only mutation-marked
  `POST /governance/api/v1/privacy/preferences` in the existing exact-scope
  route class with purpose `governance.privacy.preferences.read`. Let the shared
  boundary authenticate, reject queries, enforce exact Origin/CSRF, parse only
  the exact body `{ action: 'change', preferenceType, targetState }`, and resolve
  exactly one current-session Privacy scope handle. Call only B22B
  `getPrivacyPreferenceChangePreviewForScope` with the resolved scope and body
  values; map `null` to the fixed concealed `404`. For a projection, issue one
  preview binding with action `privacy.preference.change`, resource kind
  `privacy_preference`, resource ID equal to the preference type, exact scope,
  exact current effective state, digest, and positive registry revision
  `Math.max(1, current.updatedAt ?? 1)`. Return `201` with the unchanged B22B
  projection plus only the opaque preview handle and expiry. Add no confirmation,
  preview consumption, service call, audit, preference write, or enforcement
  change.
- `allowed_paths`: `src/index.ts`,
  `tests/integration/governance-http-wiring.test.ts`, `docs/architecture.md`,
  `docs/deployment.md`, `docs/governance-ui.md`, `docs/security-privacy.md`, and
  this checkpoint only.
- `protected_paths`: B15A-D Privacy catalog/page/query/issuer and GET behavior;
  B22A service/repository/CLI mutation semantics; B22B query implementation,
  DTO, default/stored-state evidence, and digest; `GovernanceHttpServer`, scope
  and preview registry implementations, generic session/CSRF/exact-scope
  dispatch, Memory/maintenance/Explain/Activity routes and mutations;
  Operations, retention, identity, summary, CLI/QQ/browser code; config,
  repositories, schema, migrations, dependencies, lockfile, Provider,
  private/live data, deployment, restart, container, persistence format, and
  every prior slice.
- `assertions`: unauthenticated POST is `401`; authenticated requests with a
  query, invalid Origin/CSRF, malformed/non-exact body, missing/malformed scope,
  and unknown, cross-session, or cross-purpose scope authority fail before B22B
  or preview issuance. A resolved missing/no-op/invalid preference is concealed
  as `404` without authority. Each valid type/state transition calls B22B once
  with only the resolved exact user scope and exact body values, returns its
  projection unchanged with a fresh opaque expiry, and binds precisely the
  action/resource/scope/current-state/positive-version/digest tuple. The
  existing GET and Privacy discovery remain byte-compatible; Memory,
  maintenance, Explain, and Activity handles stay isolated; repeated previews
  create no preference, audit, or other durable row; integrity/FKs stay clean;
  confirmation/consumption/service mutation remain absent; and the listener
  remains default-unreachable.
- `focused_commands`: extend and run only the named Privacy integration case
  fail-first, then final; run full governance HTTP wiring; query-service,
  governance HTTP, scope-registry, and preview-registry adjacency; the canonical
  six-file P8 command; `pnpm typecheck`; scoped and full ESLint;
  `pnpm release:check`; diff, migration/dependency, inventory/privacy hygiene;
  and one exact six-path private reverse-first and forward-first patch proof.
- `schema_dependency_live_boundary`: route wiring, synthetic integration
  coverage, and canonical route/security documentation only. No query/service/
  repository behavior, preview-registry implementation, confirmation or
  consumption, durable mutation, schema, migration, dependency, lockfile, QQ,
  Operations, Provider, private DB, deployment, restart, container, browser, or
  live operation is permitted.
- `rollback_boundary`: the exact B22B candidate for all six product/test paths
  is captured byte-for-byte and mode-for-mode under mode-`0700`
  `/tmp/lethebot-gov-ux-01b22c-roundtrip-Ayuzej/baseline`; every file is mode
  `0644`. The index, governance HTTP wiring test, architecture, deployment,
  governance UI, and security/privacy snapshots have SHA-256
  `d9e85dc14f3cab0d49457c27232505eace9cb962323f91791f2cc4287f1437eb`,
  `3caf991b28ea25864abd51489ab6264460625068af1dafeaa9c6125e1fc99b41`,
  `4133006b382bc08bf1613f44ab2bbc2bbb51cd30000b41c869ca695a911f1049`,
  `d798f813e47934918be03a52b9f539946c2faed87ecf596f096dcda1d8288896`,
  `6e97a7a627b6d30c9f43a2258b63ec026e3a968bd5b7f65a41c38c5d5e3ec689`,
  and `02b44d9ac9a709577bd178f45b5be241c0445438107d29c251501ff64f60adbf`.
  After implementation, create one exact six-path patch and independently prove
  reverse-first and forward-first restoration without touching the workspace or
  prior rollback artifacts.
- `exact_fail_first_action`: completed as specified: only
  `serves only the exact scoped owner-free Privacy preference page` was extended
  before production or canonical route-doc edits and run alone.
- `fail_first_evidence`: the focused Vitest run collected 23 governance wiring
  cases, skipped 22 unrelated cases, and failed only because unauthenticated
  `POST /governance/api/v1/privacy/preferences` returned the absent-route `404`
  instead of the required pre-handler `401`. The B22B query and preview registry
  remained unreachable before the failed assertion.
- `final_evidence`: the focused final regression passed 1 / 1 with 22 skipped,
  full governance HTTP wiring passed 23 / 23, four-file query/HTTP/scope/preview
  adjacency passed 65 / 65, and the canonical six-file P8 gate passed 296 / 296.
  Typecheck, scoped and full ESLint, and `pnpm release:check` passed; release
  recorded 132 / 1 skipped files and 2716 / 10 skipped tests. One initial full-
  wiring attempt hit a pre-start `ConfigValidationError` consistent with two
  released temporary ports selecting the same value; the unchanged immediate
  rerun passed all 23 cases. The route accepts only the current-session Privacy
  scope handle and exact body, calls B22B once, returns its fixed projection plus
  opaque authority, and binds the exact action/resource/scope/current-state/
  positive-version/digest tuple. Invalid boundaries, missing/no-op projections,
  and cross-session or cross-purpose authority fail before mutation; no
  confirmation, consumption, service call, preference/audit write, or enforcement
  change occurs. Integrity and foreign keys remain clean, Pi dependencies remain
  `0.80.2`, and migrations 007/008 are unchanged. A current 13:48 CST resume
  check reran the named focused case successfully and reproduced every candidate
  hash below.
- `rollback_evidence`: the exact six-path patch is
  `/tmp/lethebot-gov-ux-01b22c-roundtrip-Ayuzej/gov-ux-01b22c.patch`
  (`sha256:508570dade8b189ce386de7591738df394f2c41998176938b748cfeec3859021`,
  mode `0600`, size `27402`). Independent reverse-first and forward-first cycles
  restored the exact B22B baseline and B22C candidate byte-for-byte and mode-for-
  mode; baseline, candidate, and proof directories are mode `0700`, and files
  are mode `0644`. The candidate equals the workspace across exactly the six
  allowed paths, whose SHA-256 values are
  `b54b4f109647e6920776a696205c9c1be73bb6cb7dfe80cfb86ae0289b0b7567`,
  `455d5cd54054f8d176186976c3f1b3533dc3df4f7bc4b2dc3b5450200685c435`,
  `c138d99068ecf400f61b970ed88e3a722a98d0ab0d406d0e5b3071da3a65116b`,
  `a583f065f92d4e6f9ef8de9d6ee20d2fb7c01a8eca9567b8db7dd6055f7e4cec`,
  `5e0b6b1d21408c6a6568c0125a7b98f018041e1099ccbc940a2853bd4e8ba760`,
  and `f4fb50bf1f1b7f5a55e628d2d1bd32f64480818be4c90c04c9ed753ccb4559a2`
  in the listed allowed-path order. Inventory remains 69 tracked plus 49
  untracked with zero staged, `git diff --check`, ASCII-addition, sensitive-
  filename, and reject/original-artifact hygiene are clean, and migrations
  007/008 retain SHA-256
  `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`/
  `95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`.
- `exact_next_action`: completed; the post-B22C dependency reassessment selects
  B22D. Add an HTTP-disabled atomic expected-snapshot overload to the shared
  Privacy mutation before any confirmation route consumes B22C authority.

## Completed Slice: `GOV-UX-01B22D`

- `requirement`: `GOV-UX-01B22D` HTTP-disabled atomic expected-snapshot Privacy
  preference mutation.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: B22C can bind a preference change to one effective state,
  implicit-or-stored version, and digest, but B22A's shared mutation accepts no
  expected snapshot. A future confirmation handler could therefore recheck a
  preview and still lose a race before the repository upsert and audit.
- `selection_reason`: the repository already owns the preference upsert and
  `privacy.preference_set` audit transaction. Extending that owner with one
  atomic compare-and-set path is the smallest dependency-ordered step and keeps
  confirmation, preview consumption, and HTTP response design separate.
- `implementation_contract`: preserve the existing B22A service call and legacy
  repository `setPreference`/`setOptOut`/`clearOptOut` behavior byte-for-byte.
  Add one discriminated expected-snapshot overload to the shared local-admin
  service and repository. The expected snapshot contains the exact preference
  type, target state, current effective state, and either
  `{ source: 'implicit_default', updatedAt: null }` or
  `{ source: 'stored_preference', updatedAt: TIMESTAMP }`. The service validates
  a clean canonical user ID, fixed type/states, non-no-op target, exact version,
  bounded reason code, and optional safe clock; invalid evidence returns fixed
  `stale` without delegation. It supplies only the existing local-admin actor and
  fixed reason-code metadata to the repository. In one `BEGIN IMMEDIATE`
  transaction, the repository verifies that the canonical user exists, reads
  only the exact preference, distinguishes row absence from every stored row,
  compares stored state and timestamp exactly, then performs the existing
  redacted upsert and audit. Missing users return `not_found`; a version/state/
  source mismatch or no-op returns `stale`. A stored transition advances its
  timestamp beyond the prior version even under clock rollback, so state/version
  ABA cannot recreate an older snapshot; an unadvanceable timestamp is stale.
  Success returns fixed `updated` plus the durable update timestamp and remains
  immediately visible to enforcement. Add no preview/query/registry change,
  confirmation, HTTP route, new audit type, schema, or caller-controlled reason.
- `allowed_paths`: `src/storage/privacy-preference-repository.ts`,
  `src/governance/service.ts`,
  `tests/unit/storage/privacy-preference-repository.test.ts`,
  `tests/unit/governance/service.test.ts`, `docs/governance-ui.md`, and this
  checkpoint only.
- `protected_paths`: existing B22A CLI call shape/result/reason/actor/error
  behavior and legacy repository writes; B15A-D and B22B/C query DTO, scope,
  preview, digest, route, and registry semantics; `src/index.ts` and generic
  HTTP/session/CSRF/scope/resource/preview dispatch; every other governance
  mutation; CLI/QQ/browser code; enforcement consumers; schema, migrations,
  dependencies, lockfile, Provider, private/live data, deployment, restart,
  container, persistence format, and every prior slice.
- `assertions`: both preference types and both state directions succeed for
  exact stored snapshots; implicit default succeeds only for absent `opted_in`,
  while a stored `opted_in` row with the same effective state and timestamp-like
  registry value is not absence. Missing users are concealed as `not_found`.
  Malformed identifiers, types, states, version shapes/timestamps, reason codes,
  clocks, no-ops, wrong source, row appearance/disappearance, stale state, and
  stale timestamp return `stale` with zero writes. Competing calls permit only
  the exact current snapshot; timestamps advance monotonically under equal or
  rolled-back clocks. Success writes one preference row and one redacted audit
  atomically, preserves `created_at`, returns the exact new `updated_at`, and is
  immediately observed by both enforcement checks. A forced audit failure rolls
  back the preference write. The legacy B22A/CLI path retains exact delegation,
  timestamps, errors, rows, audits, and result. Repeated rejected calls are
  stable; integrity/FKs remain clean; B22B/C remain write-free; no confirmation
  route or preview consumption appears.
- `focused_commands`: add and run one named fail-first service/repository
  regression, then final; run full privacy-repository and governance-service
  files plus CLI Privacy adjacency; B22B query and B22C wiring non-use adjacency;
  the canonical six-file P8 command; `pnpm typecheck`; scoped and full ESLint;
  `pnpm release:check`; diff, migration/dependency, inventory/privacy hygiene;
  and one exact five-path private reverse-first and forward-first patch proof.
- `schema_dependency_live_boundary`: expected-snapshot repository/service code,
  synthetic tests, and canonical behavior documentation only. No CLI, query,
  registry/session, route/confirmation, schema, migration, dependency, lockfile,
  QQ, Operations, Provider, private DB, deployment, restart, container, browser,
  or live operation is permitted.
- `rollback_boundary`: the exact B22C candidate for all five product/test paths
  is captured byte-for-byte and mode-for-mode under mode-`0700`
  `/tmp/lethebot-gov-ux-01b22d-roundtrip-6hkyfi/baseline`; all directories are
  mode `0700` and files are mode `0644`. The repository, service, repository-
  test, service-test, and governance-doc snapshots have SHA-256
  `f5cc0547cc8606791987740fd9013493af124b8978996cd1bc12e3ca4e2d78d4`,
  `4614ff3ff233e20876c8318d9f35c4f47253838d16d8c8036940c3c18b1c6319`,
  `a4cc5767fc87860c942e09589e4ab2745c182ddbfada88738144712227aa814f`,
  `aac191393df2f6b2995e7af5c95b3af1727fd74f299472c2663a8af828060847`,
  and `5e0b6b1d21408c6a6568c0125a7b98f018041e1099ccbc940a2853bd4e8ba760`.
  After implementation, create one exact five-path patch and independently prove
  reverse-first and forward-first restoration without touching the workspace or
  prior rollback artifacts.
- `exact_fail_first_action`: completed as specified: only the comprehensive
  expected-snapshot regressions were added to the two allowed test files and
  run before production or canonical behavior-document edits.
- `fail_first_evidence`: the focused Vitest run collected 19 cases across the
  repository and service files, skipped 17 unrelated cases, and failed only at
  the first result assertion in each new regression: the legacy repository call
  returned `undefined`, and the legacy shared service returned only fixed
  `updated`, because neither expected-snapshot overload exists. The synthetic
  fixtures and assertions were present before production or canonical behavior-
  document edits.
- `final_evidence`: the two focused final regressions passed 2 / 2 with 17
  unrelated cases skipped; the five-file repository/service/CLI/query/wiring
  adjacency passed 137 / 137; and the canonical six-file P8 gate passed 297 /
  297. `pnpm typecheck`, scoped and full ESLint, and `pnpm release:check`
  passed; release recorded 132 / 1 skipped files and 2718 / 10 skipped tests.
  Exact implicit-default and stored-state comparisons now occur in the same
  immediate transaction as the upsert and audit. Missing users, malformed or
  drifted evidence, effective no-ops, row appearance/disappearance, and stale
  timestamps are write-free; stored timestamps advance under equal or rolled-
  back clocks; audit failure rolls back the preference; and success is
  immediately visible to both enforcement reads. The legacy repository,
  service, and CLI paths retain their prior call/result/timestamp/error/audit
  behavior. B22B/C remain write-free, no route consumes preview authority, Pi
  dependencies remain `0.80.2`, and migrations 007/008 are unchanged.
- `rollback_evidence`: the exact five-path patch is
  `/tmp/lethebot-gov-ux-01b22d-roundtrip-6hkyfi/gov-ux-01b22d.patch`
  (`sha256:cf082fb79621620219bdf768a7e23a87bd50280ea211635ea1c8fece21807c8f`,
  mode `0600`, size `33889`). Independent reverse-first and forward-first
  cycles restored the exact B22C baseline and B22D candidate byte-for-byte and
  mode-for-mode; the proof root and all trees are mode `0700`, every file is
  mode `0644`, and the candidate equals the workspace. The repository, service,
  repository-test, service-test, and governance-doc candidate SHA-256 values are
  `182dfa6ff3d8d9a8dcb84984078e7a31d454cfe13d10f1f6d6d997f258d4d943`,
  `aedcc14c30faa05fae8e1f0c722beca76c317109513c072a2f0df698792c5a12`,
  `d523478c6723e7cceaeb1802cbe4d70207f22f99ac18ecd3c04762f396df4bb0`,
  `824079a218a063e658fbba49ffb94f56017bad9e6d290cf44ef816de0196eedc`,
  and `9ba5f18447d5726cc7d9b4475aa31c4b5d3c1b4a46f62a3cfca93624d26822ca`.
  The patch contains exactly the five allowed paths and only ASCII additions.
  Inventory is 71 tracked plus 49 untracked with zero staged; diff, sensitive-
  filename, and reject/original-artifact hygiene are clean; migrations 007/008
  retain SHA-256
  `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`/
  `95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`.
- `exact_next_action`: completed; expose B22D only through a separate scoped
  confirmation route that consumes B22C authority, recomputes B22B, and keeps
  all existing Privacy GET, preview, CLI, and generic registry behavior exact.

## Completed Slice: `GOV-UX-01B22E`

- `requirement`: `GOV-UX-01B22E` exact-user Privacy preference-change
  confirmation through current-session single-use authority.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: B22C issues exact session/type/scope/current-state/version/
  digest authority, and B22D owns the atomic expected-snapshot mutation, but no
  route consumes that authority. Operators can preview a Privacy transition but
  cannot confirm it through the local governance boundary.
- `selection_reason`: projection, authority issuance, and the shared atomic
  mutation are independently proved. One separate confirmation route is the
  smallest dependency-complete Privacy slice and introduces no new registry,
  query, repository, schema, or policy behavior.
- `implementation_contract`: register only mutation-marked
  `POST /governance/api/v1/privacy/preferences/confirm` under the existing
  `governance.privacy.preferences.read` exact-scope purpose. Let the shared
  boundary authenticate, reject queries, enforce exact Origin/CSRF, parse only
  exact `{ confirm: true, previewHandle: HANDLE, preferenceType: TYPE,
  targetState: STATE }`, and resolve one current-session Privacy scope handle.
  Consume only matching `privacy.preference.change` / `privacy_preference` /
  preference-type / exact-scope authority once. Recompute B22B with the exact
  resolved scope, type, and target, then compare its current effective state,
  positive stored-timestamp-or-default registry version, and digest to the
  consumed binding. Invoke only B22D with the recomputed exact implicit-or-
  stored version and fixed reason code
  `governance_http_privacy_preference_change_confirmed`. Unknown or mismatched
  authority is concealed as `404`; reused, drifted, no-op, or atomic stale
  evidence is `409`; a B22D missing user is `404`. Success is `200` with only
  action, fixed `updated` outcome, preference type, new stored state/version,
  copied durable/enforcement effects, `privacy.preference_set` plus update-time
  evidence, and the preview's semantic rollback boundary. Add no caller reason,
  owner/updater identifier, registry/query/service/repository change, new audit
  type, rollback route, schema, or dependency.
- `allowed_paths`: `src/index.ts`,
  `tests/integration/governance-http-wiring.test.ts`, `docs/architecture.md`,
  `docs/deployment.md`, `docs/governance-ui.md`, `docs/security-privacy.md`, and
  this checkpoint only.
- `protected_paths`: B15A-D and B22B query DTO/read/digest behavior; B22A/B22D
  service/repository/CLI call shapes, validation, transactions, audits, and
  enforcement semantics; scope/preview registry implementations; generic HTTP
  session/CSRF/scope/resource dispatch; existing Privacy GET/preview and all
  Memory/maintenance/Explain/Activity routes; Operations, retention, identity,
  summary, QQ/browser code; config, schema, migrations, dependencies, lockfile,
  Provider, private/live data, deployment, restart, container, persistence
  format, and every prior slice.
- `assertions`: unauthenticated confirmation is `401`; a query, invalid Origin/
  CSRF, malformed or non-exact body, missing/malformed scope, and unknown,
  cross-session, or cross-purpose scope authority fail before consumption,
  B22B, or B22D. Wrong type, target, action, resource kind, resource ID, scope,
  expired/revoked handle, and foreign session are concealed before B22B/B22D;
  reuse conflicts without a second write. Valid stored and implicit-default
  transitions consume once, recompute once, pass only the exact user/type/
  target/current-state/current-version/fixed reason to B22D, write one preference
  plus one redacted audit atomically, immediately change enforcement, and return
  the fixed identifier-free response with the exact durable timestamp. Row,
  state, source, timestamp, or digest drift conflicts after consumption and
  before any stale write; a race after recomputation is rejected atomically by
  B22D. Existing GET and preview responses, preview issuance, CLI behavior, and
  other route/handle purposes remain exact. Raw canonical/updater/platform/
  session values, reasons, secrets, and unrestricted rows stay absent; integrity
  and FKs remain clean; the listener remains default-unreachable.
- `focused_commands`: extend only the named Privacy integration case and run it
  fail-first, then final; run full governance HTTP wiring; query/service/privacy-
  repository/HTTP/scope/preview adjacency; the canonical six-file P8 command;
  `pnpm typecheck`; scoped and full ESLint; `pnpm release:check`; diff,
  migration/dependency, inventory/privacy hygiene; and one exact six-path
  private reverse-first and forward-first patch proof.
- `schema_dependency_live_boundary`: route wiring, synthetic integration
  coverage, and canonical route/security documentation only. No query/service/
  repository/registry implementation, CLI/QQ/browser, Operations, schema,
  migration, dependency, lockfile, Provider, private DB, deployment, restart,
  container, persistence-format, or live operation is permitted.
- `rollback_boundary`: the exact B22D candidate for all six product/test paths
  is captured byte-for-byte and mode-for-mode under mode-`0700`
  `/tmp/lethebot-gov-ux-01b22e-roundtrip-zTvHr0/baseline`; all directories are
  mode `0700` and files are mode `0644`. The index, governance HTTP wiring test,
  architecture, deployment, governance UI, and security/privacy snapshots have
  SHA-256
  `b54b4f109647e6920776a696205c9c1be73bb6cb7dfe80cfb86ae0289b0b7567`,
  `455d5cd54054f8d176186976c3f1b3533dc3df4f7bc4b2dc3b5450200685c435`,
  `c138d99068ecf400f61b970ed88e3a722a98d0ab0d406d0e5b3071da3a65116b`,
  `a583f065f92d4e6f9ef8de9d6ee20d2fb7c01a8eca9567b8db7dd6055f7e4cec`,
  `9ba5f18447d5726cc7d9b4475aa31c4b5d3c1b4a46f62a3cfca93624d26822ca`,
  and `f4fb50bf1f1b7f5a55e628d2d1bd32f64480818be4c90c04c9ed753ccb4559a2`.
  After implementation, create one exact six-path patch and independently prove
  reverse-first and forward-first restoration without touching the workspace or
  prior rollback artifacts.
- `exact_fail_first_action`: extend only
  `serves only the exact scoped owner-free Privacy preference page` with the
  confirmation contract, then run that named case before production or
  canonical route-document edits. The first assertion must prove the absent
  route returns `404` instead of the required pre-handler `401` and that preview
  consumption, B22B recomputation, B22D, and durable writes remain unreachable.
- `fail_first_evidence`: the named Privacy integration case collected 23 tests,
  skipped 22 unrelated cases, and failed only because the absent confirmation
  route returned `404` instead of the required pre-handler `401`. Preview
  consumption, B22B recomputation, B22D, and durable writes remained
  unreachable before production or canonical route-document edits.
- `final_evidence`: the named final case passed 1 / 1 with 22 skipped, full
  governance HTTP wiring passed 23 / 23, and query/service/privacy-repository/
  HTTP/scope/preview adjacency passed 8 files / 159 tests. The canonical P8
  command passed 6 files / 297 tests; typecheck, scoped ESLint, full ESLint, and
  `pnpm release:check` passed. Release recorded 132 / 1 skipped files and 2718 /
  10 skipped tests. Authentication, query, Origin/CSRF, exact body and scope,
  cross-session/purpose, unknown/mismatched authority, wrong-target digest,
  replay, pre-service drift, and post-recomputation race paths are deterministic
  and write-free. Stored and implicit-default transitions consume once, invoke
  only B22D with the recomputed expected snapshot and fixed reason, immediately
  change enforcement, return fixed identifier-free state/version/effect/audit/
  rollback evidence, and leave integrity and foreign keys clean. The exact
  six-path patch is
  `/tmp/lethebot-gov-ux-01b22e-roundtrip-zTvHr0/gov-ux-01b22e.patch`
  (`sha256:2917ae518fb3c07e3173dae29fa12c67d46ab3580fd85231ba399ade2669fad1`,
  mode `0600`, size `36223`). Its proof root and all trees are mode `0700`, all
  non-patch files are mode `0644`, independent reverse-first and forward-first
  cycles restored baseline and candidate bytes/modes, and the candidate matches
  the workspace. Inventory remains 71 tracked plus 49 untracked paths with zero
  staged; `git diff --check` is clean, patch additions are ASCII-only, sensitive
  untracked filenames are absent, Pi packages remain `0.80.2`, and migrations
  007/008 retain SHA-256
  `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`/
  `95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`.
- `exact_next_action`: completed; reassess after the five-slice Privacy chain
  and move to the Operations dependency path before any browser code.

## Completed Slice: `GOV-UX-01B23A`

- `requirement`: `GOV-UX-01B23A` behavior-preserving shared Operations doctor
  owner extraction.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: `ops:doctor` already owns useful read-only integrity, foreign-
  key, schema, aggregate-count, configuration, and retention evidence, but that
  logic is private inside the CLI script and its DTO includes a database path.
  A future governance Operations coordinator therefore cannot reuse the same
  owner without importing a CLI entrypoint or duplicating SQLite inspection.
- `selection_reason`: B22E completes the dependency-ready Privacy transition.
  Reassessment after five consecutive Privacy slices moves to the remaining P8
  Operations workflow. Extracting the existing doctor owner without adding an
  HTTP route or mutation is the smallest reversible prerequisite for a bounded
  path-free coordinator, private backup creation, and stopped-service restore
  handoff.
- `implementation_contract`: move only the existing doctor constants, typed
  result, database/schema/count/configuration inspection, and helper functions
  from `src/scripts/ops-maintenance.ts` into a top-level-imported
  `src/operations/doctor.ts`. Export one typed `runOperationsDoctor(db, dbPath,
  config)` function. Keep the CLI command, arguments, default config/path
  selection, readonly open/close, JSON shape/order, timestamps, exit status,
  redaction, error behavior, and zero-write semantics exact by delegating to the
  extracted owner. Add no path-free HTTP projection yet; no coordinator, route,
  session/scope/preview authority, backup, restore, retention mutation, or audit
  behavior changes in this slice.
- `allowed_paths`: `src/operations/doctor.ts`,
  `src/scripts/ops-maintenance.ts`,
  `tests/integration/ops-maintenance-cli.test.ts`,
  `tests/unit/operations/doctor.test.ts`, `docs/operations.md`,
  `docs/governance-ui.md`, and this checkpoint only.
- `protected_paths`: SQLite backup/restore/retention/metrics implementations;
  application-release and worker-soak behavior; config/schema/migrations/
  dependencies/lockfile; governance service/query/HTTP/registry/session/scope/
  preview code; every existing route and Privacy/Memory/maintenance/Explain/
  Activity behavior; browser/QQ/Provider/private/live data, deployment, restart,
  container, persistence format, and every prior slice.
- `assertions`: the shared owner returns the exact existing doctor JSON field
  shape and order for healthy and missing-schema synthetic databases, including
  current ISO time, raw internal path for CLI only, readonly/open state,
  integrity/FK/schema/count/config/retention evidence, and the same overall
  classification. The CLI delegates exactly once, remains read-only, preserves
  stdout/stderr and `0`/`1` exit status, does not expose secrets beyond existing
  deterministic redaction, and creates or chmods no missing database. The new
  module has no import-time effects; no governance listener, route, coordinator,
  HTTP-safe DTO, backup, restore, retention, audit, schema, dependency, or live
  behavior appears.
- `focused_commands`: extend the existing doctor integration case with a
  passing pre-refactor characterization, then run it; add focused owner unit
  coverage and extract; rerun the named CLI cases, full Operations doctor unit/
  integration adjacency, the canonical P8 command, typecheck, scoped/full
  ESLint, `pnpm release:check`, diff/migration/dependency/inventory/privacy
  hygiene, and an exact allowed-path private reverse-first/forward-first patch
  proof.
- `schema_dependency_live_boundary`: behavior-preserving Operations source,
  synthetic tests, and canonical ownership documentation only. No persistence
  write, HTTP/browser/QQ behavior, schema, migration, dependency, lockfile,
  Provider, private DB, deployment, restart, container, or live operation is
  permitted.
- `rollback_boundary`: the exact pre-B23A bytes and modes for
  `src/scripts/ops-maintenance.ts`,
  `tests/integration/ops-maintenance-cli.test.ts`, `docs/operations.md`, and
  `docs/governance-ui.md` are privately captured under mode-`0700`
  `/tmp/lethebot-gov-ux-01b23a-roundtrip-QGgGvK/baseline`; every directory is
  mode `0700`, every file is mode `0644`, and their SHA-256 values are
  `6926a1cae5bc914fa2634d6bab5083ef362a44a4876285b562cdd1a1d64aa474`,
  `462a87146d474f52d2810c4cb9dd3a18d1244f225a4079976d1e9a068b480c79`,
  `7497fe859b724aae18c32c32813e852e93c43f246ebf005f0ba234f0c56beb82`,
  and `f8c281328cf02f29586a47f3964baafca13470a5d90a4ef659349906d9d6c76b`.
  `src/operations/doctor.ts` and `tests/unit/operations/doctor.test.ts` are
  explicitly absent. After implementation, create one exact allowed-path patch
  and independently prove reverse-first and forward-first restoration without
  touching the workspace or prior proof roots.
- `exact_fail_first_action`: completed as specified: the existing healthy
  `ops:doctor` integration case was first strengthened with exact ordered-shape,
  ISO-time, retention, and zero-write assertions and passed before the owner
  unit regression or production extraction was added.
- `fail_first_evidence`: the pre-refactor named CLI characterization passed 1 / 1
  with 23 unrelated cases skipped, proving the existing output contract before
  production edits.
- `final_evidence`: the owner plus full Operations CLI coverage passed 2 files /
  26 tests, broader Operations adjacency passed 3 files / 46 tests, and the
  canonical P8 gate passed 6 files / 297 tests. `pnpm typecheck`, scoped and
  full ESLint, and `git diff --check` passed. The first release run reached one
  environmental `EADDRINUSE` failure because unrelated `mihomo` PID 923 held
  random test port `127.0.0.1:19090`; the unchanged rerun passed 133 / 1 skipped
  files and 2720 / 10 skipped tests. The top-level owner now performs the exact
  existing integrity/FK/schema/count/configuration inspection, the CLI and
  rehearsal delegates preserve field order, redaction, exit status, and
  read-only behavior, and no coordinator, route, backup/restore execution,
  retention mutation, audit, schema, dependency, or live behavior was added.
- `rollback_evidence`: the exact six-path patch is
  `/tmp/lethebot-gov-ux-01b23a-roundtrip-QGgGvK/gov-ux-01b23a.patch`
  (`sha256:906b5696c9d2d66b5e98b9eac8683c35af1f0dc6a04ebc303df11be004184a06`,
  mode `0600`, size `23205`). Candidate/workspace equality and independent
  reverse-first and forward-first restoration passed in
  `reverse-proof3.L5qNXf` and `forward-proof3.gpIxIv`; all proof directories are
  mode `0700`, non-patch files are mode `0644`, and empty newly created parent
  branches are removed after reverse application. The patch contains exactly
  the six product/test/doc paths and only ASCII additions. Inventory remains 72
  tracked plus 51 untracked paths with zero staged; Pi packages remain `0.80.2`,
  and migrations 007/008 retain SHA-256
  `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`/
  `95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`.
- `exact_next_action`: completed; add an HTTP-disabled governance Operations
  coordinator that converts only the shared doctor result into a fixed bounded
  path-free status DTO before any Operations route or action is introduced.

## Completed Slice: `GOV-UX-01B23B`

- `requirement`: `GOV-UX-01B23B` HTTP-disabled path-free governance Operations
  status coordinator.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: B23A provides one typed read-only doctor owner, but its local
  CLI result intentionally contains the database path, raw integrity diagnostic,
  and missing schema names. Passing that result through a future governance
  route would violate the fixed path-free HTTP projection boundary, and no
  shared coordinator currently states which backup and restore workflows are
  available without executing them.
- `selection_reason`: a pure status coordinator is the smallest dependency-
  ordered step after the owner extraction. It can establish the complete safe
  Operations projection and stopped-service handoff before session authority,
  routes, backup creation, restore planning, retention mutation, or browser work.
- `implementation_contract`: add one top-level-imported
  `GovernanceOperationsCoordinator` that accepts the already-open database,
  internal path, and typed doctor configuration, calls only
  `runOperationsDoctor`, and rebuilds its result field-by-field into one fixed
  DTO. Preserve the ISO generation time, overall classification, open/read-only
  booleans, integrity/FK/schema classifications and counts, the fixed aggregate
  table counts, finite OneBot/configuration presence booleans, and validated
  retention-day values. Omit the database path, raw integrity result, missing-
  table names, URLs, tokens, identifiers, filesystem values, row payloads,
  unrestricted keys, and dynamic diagnostics. State only that private verified
  backup creation is available and that restore execution is available through
  boundary `stopped_service_only`; execute neither workflow. Register no HTTP
  route and add no authority, preview, confirmation, audit, mutation, or file
  operation.
- `allowed_paths`: `src/governance/operations-coordinator.ts`,
  `tests/unit/governance/operations-coordinator.test.ts`, `docs/operations.md`,
  `docs/governance-ui.md`, and this checkpoint only.
- `protected_paths`: B23A doctor/CLI/test behavior; SQLite backup/restore/
  retention/metrics and application-release implementations; governance query,
  service, HTTP, registry, session, scope, resource, and preview code; every
  route and Privacy/Memory/maintenance/Explain/Activity behavior; config,
  schema, migrations, dependencies, lockfile, browser/QQ/Provider/private/live
  data, deployment, restart, container, persistence format, and every prior
  slice.
- `assertions`: the coordinator calls the B23A owner exactly once and returns
  exact ordered keys for healthy and attention-required databases. Integrity,
  FK, and schema states derive only from safe booleans/counts; missing-table
  names and raw diagnostics never cross the projection. Counts have only the
  fixed doctor categories. Configuration contains only the finite transport,
  presence booleans, and retention days. Backup is availability metadata only;
  restore reports exact `stopped_service_only`. Recursive inspection finds no
  path, URL, token, platform identifier, malicious injected extra property, raw
  schema name, row payload, or diagnostic text. Repeated inspection is
  read-only, integrity/FKs remain clean, and no route, registry call, audit,
  maintenance execution, durable write, schema, or dependency change appears.
- `focused_commands`: capture the exact four-path private baseline; add and run
  one fail-first coordinator unit regression; implement and rerun that case;
  run full coordinator/doctor/Operations CLI adjacency, the canonical P8
  command, typecheck, scoped/full ESLint, `pnpm release:check`, diff/migration/
  dependency/inventory/privacy hygiene, and one exact four-path private reverse-
  first and forward-first patch proof.
- `schema_dependency_live_boundary`: a read-only coordinator, synthetic unit
  coverage, and canonical ownership documentation only. No HTTP/browser/QQ
  behavior, backup/restore execution, retention mutation, audit, persistence
  write, schema, migration, dependency, lockfile, Provider, private DB,
  deployment, restart, container, or live operation is permitted.
- `rollback_boundary`: the exact B23A candidate bytes and modes for
  `docs/operations.md` and `docs/governance-ui.md` are captured under mode-`0700`
  `/tmp/lethebot-gov-ux-01b23b-roundtrip-3riEPK/baseline`; both files are mode
  `0644` with SHA-256
  `979d9d9934f2eba3789e55663d3a47c339a6df1e000172b60b47acd8198339e4` and
  `1f87cbb08032e40bab3dec461e78cd096e89be560650cc86882592ae5b57c9e6`.
  `src/governance/operations-coordinator.ts` and
  `tests/unit/governance/operations-coordinator.test.ts` are explicitly absent.
  After implementation, create one exact four-path patch and independently
  prove reverse-first and forward-first restoration without touching the
  workspace or prior proof roots.
- `exact_fail_first_action`: completed as specified: the named coordinator
  regression was run before production and canonical behavior edits and failed
  at collection only because the coordinator module was absent; B23A behavior
  remained unchanged.
- `final_evidence`: the coordinator unit passed 3 / 3; the exact Operations
  adjacency (coordinator, doctor, SQLite maintenance, and CLI) passed 4 files /
  49 tests; and the canonical P8 command passed 7 files / 300 tests. The fresh
  `pnpm release:check` passed typecheck, lint, build, preflight, diff hygiene,
  134 / 1 skipped files, and 2723 / 10 skipped tests. The coordinator calls
  `runOperationsDoctor` once and reconstructs only the fixed path-free DTO;
  healthy and attention-required projections omit paths, diagnostics, schema
  names, URLs, credentials, identifiers, payloads, and injected keys. Repeated
  inspection leaves `total_changes()` and foreign-key checks unchanged. The
  inventory is 72 modified tracked paths plus 53 untracked paths with zero
  staged paths; sensitive untracked filenames are absent; Pi dependencies remain
  `0.80.2`; and migrations 007/008 retain SHA-256
  `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b` /
  `95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`.
- `rollback_evidence`: the exact four-path patch is
  `/tmp/lethebot-gov-ux-01b23b-roundtrip-hFTA78/gov-ux-01b23b.patch`
  (`sha256:a6796df830b9e016a0d40db0f85a490610d8b4e5d01d12382262ceeb79270168`,
  mode `0600`, size `20945`). Its private root and proof trees are mode `0700`,
  non-patch files are mode `0644`, and the patch additions are ASCII-only. The
  candidate snapshot matches all four workspace paths; independent reverse-first
  and forward-first application restored baseline and candidate bytes and modes,
  including removal and recreation of the two new parent branches. No route,
  backup/restore execution, retention mutation, audit, schema, dependency,
  browser, QQ, Provider, private-data, deployment, restart, or prior-slice
  behavior changed.
- `exact_next_action`: completed; reassessment selects `GOV-UX-01B23C`, an
  HTTP-disabled verified-backup status/creation projection over the existing
  SQLite maintenance owner. Keep Operations routes, restore execution,
  retention mutation, audit, and browser behavior absent until that slice is
  separately reviewed.

## Completed Slice: `GOV-UX-01B23C`

- `requirement`: `GOV-UX-01B23C` HTTP-disabled verified-backup status/creation
  projection.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: B23B reports that backup creation is available, but it does
  not expose fixed artifact integrity/size evidence or a bounded handoff for a
  future Operations view.
- `selection_reason`: this is the next dependency-ready Operations step over
  the existing SQLite backup and integrity owners. It can prove private,
  verified artifact metadata before any route, stopped-service restore action,
  retention mutation, or browser work is introduced.
- `implementation_contract`: add only
  `GovernanceOperationsCoordinator.createVerifiedBackup({ backupPath })`. Treat
  `backupPath` as trusted internal configuration, call only
  `backupSqliteDatabase({ sourcePath: dbPath, backupPath })`, and rebuild its
  result into fixed `status`, `artifact`, `pages`, and `restore` objects. A
  valid successful result is `completed`, has `verified` integrity, bounded
  positive byte size, bounded consistent total/remaining pages, and an
  available `stopped_service_only` restore handoff. Owner rejection becomes one
  fixed diagnostic-free `attention_required` result with zero numeric evidence.
  Malformed numeric evidence is bounded, marks status `attention_required`, and
  leaves restore unavailable. Omit both paths, raw integrity/error diagnostics,
  database rows, credentials, identifiers, and injected fields. Keep private
  staging, publication, validation, cleanup, and no-clobber logic in the
  existing owner; register no route and add no restore, retention, or audit
  mutation.
- `allowed_paths`: `src/governance/operations-coordinator.ts`,
  `tests/unit/governance/operations-coordinator.test.ts`, `docs/operations.md`,
  `docs/governance-ui.md`, and this checkpoint only.
- `protected_paths`: the B23A doctor/CLI owner and behavior; the existing
  SQLite backup/restore/retention implementations and their tests; governance
  query/service/HTTP/session/scope/resource/preview code; every route and
  Privacy/Memory/maintenance/Explain/Activity behavior; configuration, schema,
  migrations, dependencies, lockfiles, browser/QQ/Provider code, private or
  live data, deployment, restart, container, persistence format, and every
  prior slice.
- `assertions`: `createVerifiedBackup({ backupPath })` delegates exactly once
  to `backupSqliteDatabase({ sourcePath: dbPath, backupPath })` and returns a
  fixed ordered DTO containing only completion status, verified/attention
  integrity classification, bounded total/remaining page evidence, bounded
  artifact size, and the existing `stopped_service_only` restore handoff.
  The projection never returns source/destination paths, raw integrity text,
  filesystem metadata, rows, credentials, identifiers, or injected properties.
  Page evidence is non-negative and internally bounded; a successful owner
  result remains verified. Private staging, `0600` publication, integrity
  validation, and no-clobber behavior remain solely in the existing owner. No
  route, restore execution, retention mutation, audit, schema, dependency, or
  unrelated filesystem operation is added.
- `focused_commands`: verify the captured baseline; add and run one named
  fail-first backup projection regression before production edits; implement
  the adapter and rerun it; then run coordinator/SQLite-maintenance adjacency,
  canonical P8, typecheck, scoped/full ESLint, `pnpm release:check`, diff and
  privacy/inventory hygiene, and an exact four-path reverse-first/
  forward-first patch proof.
- `schema_dependency_live_boundary`: one typed coordinator adapter, synthetic
  tests, and canonical ownership documentation only. No HTTP/browser/QQ
  behavior, restore execution, retention mutation, audit, persistence schema,
  migration, dependency, lockfile, Provider, private DB, deployment, restart,
  container, or live operation is permitted.
- `rollback_boundary`: the exact B23B candidate bytes and modes for all four
  allowed product/test/document paths are captured under mode-`0700`
  `/tmp/lethebot-gov-ux-01b23c-roundtrip-ECYT5K/baseline`. The directories are
  `0700`, the four files are `0644`, and their SHA-256 values are
  `a9bcb40e71d8b8a4e3a010523acafa87837735b5b7a33a8f5539194a96da40fd`,
  `5a675b2a5264f39937dd39a0f6d94a9f22dbff01485563842d0d8662eeed1f34`,
  `3a610388ccd098e9c7ff5eae44fec5b882ec5e20fd20df6064041708e79fbd77`, and
  `786739f3e3338a7e77d6c0e7f086599c6b77eaaa34bda7ea56dbfbd6dbb9e669` in
  the same path order. The coordinator/test paths are present in the
  candidate; after implementation one exact four-path patch must independently
  prove reverse-first and forward-first restoration without touching the
  workspace or prior proof roots.
- `exact_fail_first_action`: completed as specified: the named coordinator
  regression was added after baseline capture and before production/canonical
  behavior edits, then run alone.
- `fail_first_evidence`: Vitest collected four coordinator tests, skipped the
  three B23B cases, and failed only because
  `coordinator.createVerifiedBackup is not a function`. The fixture reached the
  intended missing adapter with no production or canonical behavior edit.
- `implementation_evidence`: the named regression now passes, and full
  coordinator coverage passes 6 / 6. It proves one real owner call, a migrated
  source, private `0600` publication, bounded path-free success evidence, zero
  source-handle writes, fixed diagnostic-free owner-failure output, and
  malformed/injected result isolation. Scoped ESLint and `pnpm typecheck` pass.
- `final_evidence`: coordinator coverage passed 6 / 6; exact coordinator/
  doctor/SQLite-maintenance/Operations-CLI adjacency passed 4 files / 52 tests;
  and the canonical P8 command passed 7 files / 303 tests. `pnpm typecheck`,
  scoped and full ESLint, build, release preflight, and `git diff --check`
  passed. Fresh `pnpm release:check` passed 134 / 1 skipped files and 2726 / 10
  skipped tests. A real migrated source produces one private mode-`0600`
  verified backup and only the fixed path-free success DTO. Owner rejection
  produces the fixed diagnostic-free attention DTO; malformed numeric and
  injected metadata is bounded and cannot enable restore. The coordinator
  never duplicates staging, integrity, cleanup, or no-clobber logic. Inventory
  remains 72 modified tracked paths plus 53 untracked files with zero staged;
  sensitive untracked filenames are absent; Pi dependencies remain `0.80.2`;
  and migrations 007/008 retain SHA-256
  `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b` /
  `95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`.
- `rollback_evidence`: the exact four-path patch is
  `/tmp/lethebot-gov-ux-01b23c-roundtrip-ECYT5K/gov-ux-01b23c.patch`
  (`sha256:23183c23906bd6b9463af98425276134a423ed43b5625d018159403b1f1dac09`,
  mode `0600`, size `15043`). It contains exactly `docs/governance-ui.md`,
  `docs/operations.md`, `src/governance/operations-coordinator.ts`, and
  `tests/unit/governance/operations-coordinator.test.ts`; its additions are
  ASCII-only. Candidate/workspace equality and independent reverse-first and
  forward-first application passed. Baseline, candidate, and both proof trees
  have mode-`0700` directories and mode-`0644` files. Candidate SHA-256 values
  in source/test/Operations-doc/governance-doc order are
  `e5a987088290d851da55c06f652c110b77f0092e6d4187f433782a295665a47b`,
  `f8ba2280746f591a372edf2d2f3f73e1dc6ed2d55fe201d0454ca0b75082ddbc`,
  `922e5ff1d9176602b1eed873ad2ebfd5d91e91820eb0d67dbf6ff8626666281b`, and
  `ad57cc7d65bab8d64acea8d10b123696c24ecc343da874343c56d838b82a1344`.
  No route, restore execution, retention mutation, audit, schema, dependency,
  browser, QQ, Provider, private-data, deployment, restart, or prior-slice
  behavior changed.
- `exact_next_action`: completed; reassessment selects `GOV-UX-01B23D`, the
  authenticated read-only Operations overview route over B23B `inspect()`.
  Expose no backup creation, restore, retention, or audit mutation in that
  slice.

## Completed Slice: `GOV-UX-01B23D`

- `requirement`: `GOV-UX-01B23D` authenticated read-only Operations overview.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: B23B owns the fixed path-free doctor projection and B23C owns
  HTTP-disabled verified backup creation, but no Operations read route exists.
  A browser therefore cannot inspect safe health/configuration/workflow status,
  while exposing backup creation now would combine read authority with a file
  mutation and skip the established route-before-action review boundary.
- `selection_reason`: exposing the already-proved read DTO is the smallest
  dependency-ready step. It establishes authenticated Operations visibility
  independently before backup preview/confirmation, retention changes, restore
  planning, or browser work.
- `implementation_contract`: construct one shared
  `GovernanceOperationsCoordinator` from the already-open database, internal DB
  path, and reviewed Operations-doctor configuration. Register only
  authenticated-unscoped `GET /governance/api/v1/operations` with purpose
  `governance.operations.status.read`; reject query strings and scope headers
  through the existing boundary before calling `inspect()`. Return the B23B DTO
  unchanged. Do not call B23C, issue handles, accept paths or selectors, or add
  any Operations mutation.
- `allowed_paths`: `src/index.ts`,
  `tests/integration/governance-http-wiring.test.ts`, `docs/operations.md`,
  `docs/governance-ui.md`, and this checkpoint only.
- `protected_paths`: B23A doctor and local CLI behavior; B23B/B23C coordinator
  projections and tests; SQLite backup/restore/retention implementations;
  `GovernanceHttpServer` and generic session/CSRF/scope/resource/preview
  behavior; existing Overview, Activity, Explain, Memory, Privacy, and
  maintenance routes; every mutation; config, repositories, schema, migrations,
  dependencies, lockfile, browser/QQ/Provider code, private/live data,
  deployment, restart, container, persistence format, and every prior slice.
- `assertions`: the disabled listener remains closed. An absent session returns
  fixed `401`; a query string or `X-LetheBot-Scope` returns fixed `400`; all
  three paths make zero coordinator calls. An authenticated exact request calls
  only `inspect()` once and returns the exact B23B ordered DTO with valid ISO
  time, fixed classifications/count categories/configuration booleans/retention
  values, backup availability, and `stopped_service_only` restore metadata.
  Source/database paths, raw integrity/schema diagnostics, URLs, tokens,
  identifiers, payloads, and unrestricted keys stay absent. Repeated reads are
  write-free with clean integrity/FKs. B23C backup creation, backup/restore/
  retention/audit writes, handles, registry calls, and all existing routes stay
  unused and unchanged.
- `focused_commands`: capture the exact four-path private baseline; add and run
  one fail-first Operations route regression; implement and rerun it; run full
  governance wiring plus coordinator/doctor/HTTP adjacency, the canonical P8
  command, typecheck, scoped/full ESLint, `pnpm release:check`, diff/migration/
  dependency/inventory/privacy hygiene, and one exact four-path private
  reverse-first and forward-first patch proof.
- `schema_dependency_live_boundary`: route construction/wiring, synthetic tests,
  and canonical route documentation only. No coordinator/doctor behavior,
  persistence write, backup creation, restore, retention, audit, schema,
  migration, dependency, lockfile, Provider, QQ, private DB, deployment,
  restart, container, or browser operation is permitted.
- `rollback_boundary`: the exact B23C candidate bytes and modes for the four
  product/test/document paths are captured under mode-`0700`
  `/tmp/lethebot-gov-ux-01b23d-roundtrip-z5tiaV/baseline`. All directories are
  `0700`, all files are `0644`, and SHA-256 values in source/test/Operations-
  doc/governance-doc order are
  `dbc493d5c81e032d527a22999d080dd8c00893d733a0dfcc90b37db597dfc781`,
  `03261fc1e3bd93668a31f3d44b0b14b5b35be8cca2b2af8af8f7a7a4bfa7206a`,
  `922e5ff1d9176602b1eed873ad2ebfd5d91e91820eb0d67dbf6ff8626666281b`, and
  `ad57cc7d65bab8d64acea8d10b123696c24ecc343da874343c56d838b82a1344`.
  After implementation, create one exact four-path patch and independently
  prove reverse-first and forward-first restoration without touching the
  workspace or prior proof roots.
- `exact_fail_first_action`: completed as specified: the named route regression
  was added after baseline capture and before production/canonical route edits,
  then run alone.
- `fail_first_evidence`: Vitest collected 24 integration tests, skipped 23
  unrelated cases, and failed only because the absent
  `GET /governance/api/v1/operations` returned `404` instead of the required
  unauthenticated `401`. Neither `inspect()` nor `createVerifiedBackup()` ran,
  proving the route was absent before production wiring.
- `implementation_evidence`: the named route regression now passes alone with
  23 unrelated cases skipped. The application constructs one coordinator with
  the explicit doctor configuration, registers one authenticated-unscoped
  read-only route, and dispatches only to `inspect()`. Missing authentication,
  query, and scope-header requests stop before coordinator access; successful
  repeated reads leave `total_changes()` unchanged with clean integrity and
  foreign keys. `createVerifiedBackup()` remains uncalled. Typecheck, scoped
  ESLint, and `git diff --check` pass.
- `final_evidence`: the exact HTTP/coordinator/doctor adjacency passed 4 files /
  47 tests, and the canonical P8 command passed 7 files / 303 tests. Fresh
  `pnpm release:check` passed typecheck, full lint, build, release preflight,
  diff hygiene, 134 / 1 skipped files, and 2727 / 10 skipped tests. The route
  returns the B23B ordered DTO unchanged, omits internal paths and diagnostics,
  keeps `/overview` separate, performs no backup or other mutation, and leaves
  all existing routes unchanged. Inventory remains 72 modified tracked paths
  plus 53 untracked files with zero staged paths; sensitive untracked filenames
  are absent; installed Pi dependencies remain `0.80.2`; and migrations 007/008
  retain SHA-256
  `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b` /
  `95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`.
- `rollback_evidence`: the exact four-path patch is
  `/tmp/lethebot-gov-ux-01b23d-roundtrip-z5tiaV/gov-ux-01b23d.patch`
  (`sha256:255bded918749e549207426e419e81f4b25082ebc1f5a428ce78a87c16e03207`,
  mode `0600`, size `12628`). It contains exactly `docs/governance-ui.md`,
  `docs/operations.md`, `src/index.ts`, and
  `tests/integration/governance-http-wiring.test.ts`; additions are ASCII-only.
  Candidate/workspace equality and independent reverse-first and forward-first
  application passed. An initial proof-only attempt stopped because `umask 077`
  caused Git-rewritten files to become `0600`; that attempt is preserved under
  the private root, and the corrected cycles proved the required mode-`0700`
  directories and mode-`0644` files. Candidate SHA-256 values in source/test/
  Operations-doc/governance-doc order are
  `e65ee65fc14d6f7fbe432626f3fed1bbda13f6a58e638ed1c0c2ac26a6f34751`,
  `92b6054a15b899b2358e2f1bbcfa2e5ced49390798e3a27c9aec89b3aaad5caa`,
  `08e2f28a9a6ccf46dae2ae3a099e90bf5db3560643a01d67072aeefe33c06ea2`, and
  `e8c5a6125a9246e425035e7a8015f06470031625dc3c0981b4e1135a0209cc1a`.
  No coordinator/doctor behavior, backup/restore/retention/audit write, schema,
  dependency, browser, QQ, Provider, private-data, deployment, restart, or
  prior-slice behavior changed.
- `exact_next_action`: completed; reassessment selects `GOV-UX-01B23E`, an
  HTTP-disabled fixed verified-backup preview. Define the confirmation evidence
  before issuing authority or choosing a server-owned destination, and keep
  B23C plus every Operations mutation route unused.

## Completed Slice: `GOV-UX-01B23E`

- `requirement`: `GOV-UX-01B23E` HTTP-disabled verified-backup preview.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: B23C can create a verified backup from a trusted internal
  destination and B23D exposes read-only Operations status, but directly wiring
  B23C would skip the required preview/confirmation lifecycle and would leave
  destination ownership ambiguous.
- `selection_reason`: a pure fixed preview is the smallest dependency-ready
  step. It defines exactly what the operator will confirm before session
  authority, server-owned destination allocation, filesystem mutation, or a
  backup route is introduced.
- `implementation_contract`: add only
  `GovernanceOperationsCoordinator.previewVerifiedBackup()`. Return a fixed,
  path-free DTO with action `create_verified_backup`, current state `available`,
  contract version `1`, explicit database-no-write/private-artifact/integrity-
  verification/no-overwrite effects, the `stopped_service_only` restore
  boundary, unavailable in-process rollback with a fixed artifact-removal-not-
  exposed reason, and a domain-separated SHA-256 preview digest over those
  semantic fields. The method accepts no client input, path, selector, or
  handle; it does not choose or generate a destination and never calls
  `inspect()`, `createVerifiedBackup()`, or a filesystem/SQLite owner. Register
  no route and issue no authority. A later slice must own a private server-side
  destination before confirmation can call B23C.
- `allowed_paths`: `src/governance/operations-coordinator.ts`,
  `tests/unit/governance/operations-coordinator.test.ts`, `docs/operations.md`,
  `docs/governance-ui.md`, and this checkpoint only.
- `protected_paths`: B23A doctor/CLI behavior; B23B status projection; B23C
  backup creation and its owner; B23D route; SQLite backup/restore/retention
  implementations; HTTP/session/scope/resource/preview registries; every
  existing route and mutation; configuration, repositories, schema, migrations,
  dependencies, lockfile, browser/QQ/Provider code, private/live data,
  deployment, restart, container, persistence format, and every prior slice.
- `assertions`: repeated preview calls return the exact ordered fixed DTO and
  stable 64-character lowercase hexadecimal digest. The digest is independently
  recomputable from the fixed domain and semantic fields and changes if a
  covered field changes. Raw/internal paths, timestamps, database counts,
  diagnostics, credentials, identifiers, payloads, unrestricted keys, and
  injected properties remain absent. No doctor, backup, restore, retention,
  audit, registry, filesystem, or database operation occurs; integrity/FKs and
  `total_changes()` remain unchanged.
- `focused_commands`: capture the exact four-path private baseline; add and run
  one named fail-first coordinator preview regression; implement and rerun it;
  run full coordinator plus Operations adjacency, canonical P8, typecheck,
  scoped/full ESLint, `pnpm release:check`, diff/migration/dependency/inventory/
  privacy hygiene, and an exact four-path private reverse-first and
  forward-first patch proof.
- `schema_dependency_live_boundary`: one pure coordinator projection, synthetic
  tests, and canonical ownership documentation only. No destination selection,
  authority, route, file creation, backup execution, restore, retention, audit,
  schema, migration, dependency, lockfile, Provider, QQ, private DB,
  deployment, restart, container, browser, or live operation is permitted.
- `rollback_boundary`: the exact B23D candidate bytes and modes for all four
  allowed product/test/document paths are captured under mode-`0700`
  `/tmp/lethebot-gov-ux-01b23e-roundtrip-CkTBSL/baseline`. All directories are
  `0700`, all files are `0644`, and SHA-256 values in coordinator/test/
  Operations-doc/governance-doc order are
  `e5a987088290d851da55c06f652c110b77f0092e6d4187f433782a295665a47b`,
  `f8ba2280746f591a372edf2d2f3f73e1dc6ed2d55fe201d0454ca0b75082ddbc`,
  `08e2f28a9a6ccf46dae2ae3a099e90bf5db3560643a01d67072aeefe33c06ea2`, and
  `e8c5a6125a9246e425035e7a8015f06470031625dc3c0981b4e1135a0209cc1a`.
  After implementation, build one exact four-path patch and prove independent
  reverse-first and forward-first restoration without touching the workspace or
  any prior proof root.
- `exact_fail_first_action`: completed as specified after baseline capture and
  before any production or canonical-document edit.
- `fail_first_evidence`: the prior six-test coordinator baseline passed. The
  named preview regression then collected seven tests, skipped the six existing
  cases, and failed only because
  `coordinator.previewVerifiedBackup is not a function`. Its `finally` checks
  produced no secondary failure, preserving zero doctor/backup-owner calls,
  unchanged private directory contents and `total_changes()`, and clean foreign
  keys.
- `final_evidence`: the named final regression passed 1 / 1 with six unrelated
  cases skipped, full coordinator coverage passed 7 / 7, exact Operations
  adjacency passed 4 files / 53 tests, and the canonical P8 gate passed 7 files
  / 304 tests. `pnpm typecheck`, scoped ESLint, and fresh
  `pnpm release:check` passed; release recorded 134 / 1 skipped files and 2728 /
  10 skipped tests. The pure method returns only the exact fixed semantic DTO
  and independently recomputable domain-separated digest, while its no-input
  calls make zero doctor, backup-owner, filesystem, destination, registry,
  route, or database operations. The exact four-path patch is
  `/tmp/lethebot-gov-ux-01b23e-roundtrip-CkTBSL/gov-ux-01b23e.patch`
  (`sha256:6907b7ad9d4386818d74673eb1a785d5ec8b22aee38bc1dd0c18fc9c3b243ba7`,
  mode `0600`, size `11799`). Independent candidate-to-baseline-to-candidate
  and baseline-to-candidate-to-baseline cycles restored all bytes and modes;
  proof roots/trees are `0700`, product files are `0644`, and the candidate
  snapshot equals the workspace. Candidate SHA-256 values in coordinator/test/
  Operations-doc/governance-doc order are
  `71f8201b823614eb1a73b43ad29c9f51cac16599ada8e6a43473b471e202cb2c`,
  `88a4e46898f7e7bdc2f49c2c5b7c7839e0a1356ad45b538f55f8a812d0eb66a0`,
  `f9c20d7cc7e295941fd41bb1f2cf3e172d3408db7c012873c428f68f61482d13`,
  and `96be6bbea12f12df2c64082b01c069a5b072e0490268c09a333954e5404cbb48`.
  Inventory remains 72 modified tracked plus 53 untracked paths with zero
  staged; `git diff --check`, ASCII-only patch/additions, VCS-visible sensitive-
  artifact hygiene, installed Pi dependencies `0.80.2`, and unchanged migration
  hashes passed. No destination, authority, route, file creation, backup,
  restore, retention, audit, schema, dependency, browser, live, or prior-slice
  behavior changed.
- `exact_next_action`: completed; the required Operations reassessment selects
  `GOV-UX-01B23F`, an authenticated-unscoped preview route using the existing
  session/CSRF and preview-authority boundary while keeping B23C unwired.

## Completed Slice: `GOV-UX-01B23F`

- `requirement`: `GOV-UX-01B23F` verified-backup preview route and authority.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: B23E defines the fixed confirmation evidence, but it remains
  HTTP-disabled. An operator session therefore cannot request that preview or
  receive single-use authority for a later confirmation. Directly exposing
  B23C would still skip confirmation and leave destination ownership ambiguous.
- `selection_reason`: exposing only the already-proved preview is the smallest
  dependency-ordered Operations slice. The existing authenticated-unscoped
  mutation boundary and preview registry can bind the action without choosing
  a destination, touching SQLite/filesystem state, or introducing confirmation.
- `implementation_contract`: register only authenticated-unscoped mutation
  `POST /governance/api/v1/operations` with purpose
  `governance.operations.backup.preview`. Accept only the exact JSON object
  `{ action: 'create_verified_backup' }`; reuse the existing authentication,
  same-origin, CSRF, JSON body, query, and scope-header rejection boundary. Call
  only `GovernanceOperationsCoordinator.previewVerifiedBackup()`, then issue an
  existing preview handle bound to the current session/local-admin actor, its
  fixed action, system scope, resource kind `operations_verified_backup`,
  resource ID `verified_backup`, current state `available`, contract version
  `1`, and preview digest. Return `201` with the unchanged B23E projection plus
  only `previewHandle` and `previewExpiresAt`. Register no confirm route; do not
  consume authority, inspect Operations state, select/allocate/return a path,
  call B23C, create a file, or write SQLite/audit state.
- `allowed_paths`: `src/index.ts`,
  `tests/integration/governance-http-wiring.test.ts`, `docs/operations.md`,
  `docs/governance-ui.md`, and this checkpoint only.
- `protected_paths`: B23A doctor/CLI behavior; B23B status DTO; B23C backup
  adapter/owner; B23D GET route; B23E preview semantics/digest and its unit
  coverage; `GovernanceHttpServer`; all scope/resource/preview registry
  implementations; SQLite backup/restore/retention; every existing route and
  mutation; configuration, repositories, schema, migrations, dependencies,
  lockfile, browser/QQ/Provider code, private/live data, deployment, restart,
  container, persistence format, and every prior slice.
- `assertions`: unauthenticated requests return `401`; missing/wrong Origin or
  CSRF returns `403`; missing/wrong content type, malformed/oversized JSON,
  queries, scope headers, and every non-exact body return fixed `400` before B23E
  or authority issuance. A valid request calls B23E exactly once, issues one
  current-session preview handle with the exact fixed binding, and returns its
  unchanged projection plus a 43-character opaque handle and bounded expiry.
  Cross-session and cross-action authority remain unusable by the unchanged
  registry. Repetition yields the same semantic digest but fresh authority.
  GET status behavior is unchanged; no inspect, backup, restore, retention,
  audit, destination, filesystem, or database operation occurs; `total_changes()`,
  integrity, FKs, and the private database directory remain unchanged.
- `focused_commands`: run one named fail-first route regression, then its final
  form; run full governance HTTP wiring plus coordinator/doctor/Operations
  adjacency; the canonical P8 command; `pnpm typecheck`; scoped/full ESLint;
  `pnpm release:check`; diff/migration/dependency/inventory/privacy hygiene; and
  an exact four-path private reverse-first and forward-first patch proof.
- `schema_dependency_live_boundary`: route wiring, synthetic tests, existing
  in-memory preview authority, and canonical Operations documentation only. No
  destination allocation or disclosure, confirmation, authority consumption,
  file creation, backup execution, database/audit write, restore, retention,
  schema, migration, dependency, lockfile, Provider, QQ, private DB,
  deployment, restart, container, browser, or live operation is permitted.
- `rollback_boundary`: the exact B23E candidate bytes and modes for all four
  allowed product/test/document paths are captured under mode-`0700`
  `/tmp/lethebot-gov-ux-01b23f-roundtrip-qhFMgB/baseline`. All files are `0644`.
  SHA-256 values in index/wiring-test/Operations-doc/governance-doc order are
  `e65ee65fc14d6f7fbe432626f3fed1bbda13f6a58e638ed1c0c2ac26a6f34751`,
  `92b6054a15b899b2358e2f1bbcfa2e5ced49390798e3a27c9aec89b3aaad5caa`,
  `f9c20d7cc7e295941fd41bb1f2cf3e172d3408db7c012873c428f68f61482d13`,
  and `96be6bbea12f12df2c64082b01c069a5b072e0490268c09a333954e5404cbb48`.
  After implementation, one exact patch must independently prove reverse-first
  and forward-first restoration without touching the workspace or B23E proof.
- `exact_fail_first_action`: completed as specified after baseline capture and
  before any production or canonical-document edit.
- `fail_first_evidence`: the named run collected 25 governance-wiring cases,
  skipped 24 unrelated cases, and failed only because unauthenticated
  `POST /governance/api/v1/operations` returned `404` instead of the required
  `401`. Before that expected assertion, the regression proved zero B23E,
  B23C, or preview-handle issuance calls, unchanged synthetic database-directory
  contents and `total_changes()`, and clean application shutdown.
- `final_evidence`: the named final regression passed 1 / 1 with 24 unrelated
  cases skipped; full HTTP wiring plus coordinator/doctor/SQLite/Operations-CLI
  adjacency passed 5 files / 78 tests; and the exact canonical P8 gate passed 7
  files / 304 tests. `pnpm typecheck`, scoped/full ESLint, and the final fresh
  `pnpm release:check` passed; release recorded 134 / 1 skipped files and 2729 /
  10 skipped tests. The POST route accepts only the exact action after the
  existing session/Origin/CSRF/JSON/query/scope boundary, calls B23E once, and
  issues only fixed system-operation authority; repeated requests preserve the
  digest and produce fresh handles. GET status remains exact, confirmation is
  absent, and inspect/backup/consume/filesystem/database/audit effects remain
  zero. The exact four-path patch is
  `/tmp/lethebot-gov-ux-01b23f-roundtrip-qhFMgB/gov-ux-01b23f.patch`
  (`sha256:13520a7dca7c2f37e68482980d2a821d9c8337455bda95ee984fe7fa89cd5443`,
  mode `0600`, size `17706`). Independent candidate-to-baseline-to-candidate
  and baseline-to-candidate-to-baseline cycles restored all bytes and modes;
  proof roots/trees are `0700`, product files are `0644`, and the candidate
  snapshot equals the workspace. Candidate SHA-256 values in index/wiring-test/
  Operations-doc/governance-doc order are
  `7d42e5e62ce398e27d005fc461df75e443bff3ddeb6824e0476c1a96ae47b068`,
  `a1ece09f920ce116583a7a0f3eb53f7e47cb79d8c342d35b6a88da2839d19f81`,
  `517a321218f556e6c934a1c83b2a3bfc0a57cee6c8edf5e39906bc269477a622`,
  and `47282a1f3028b2afe2a77eea4cd4f0b870d9dd4500502b3a8ee10c741c232d14`.
  Inventory remains 72 modified tracked plus 53 untracked paths with zero
  staged; `git diff --check`, ASCII-only patch/additions, exact four-path/mode
  scope, VCS-visible sensitive-artifact hygiene, Pi dependencies `0.80.2`, and
  unchanged migration hashes passed. B23A-E, generic HTTP/session/registry,
  backup/restore/retention owners, configuration, schema, dependencies, browser,
  live behavior, and every prior slice remain unchanged.
- `exact_next_action`: completed; the post-preview reassessment selects
  `GOV-UX-01B23G`, an HTTP-disabled no-input managed-backup adapter that derives
  a private server-owned destination and opaque reference before any
  confirmation route may invoke B23C.

## Completed Slice: `GOV-UX-01B23G`

- `requirement`: `GOV-UX-01B23G` HTTP-disabled server-owned verified-backup
  execution.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: B23F can issue confirmation authority, but B23C still requires
  a trusted internal path and there is no server-owned destination/reference
  policy. A confirmation route therefore cannot safely invoke B23C without
  accepting a client path or losing durable restore handoff identity.
- `selection_reason`: a no-input coordinator adapter is the smallest missing
  dependency. It can establish one private local directory, opaque durable
  reference, and fixed failure behavior over B23C before authority consumption,
  confirmation routing, or stopped-service restore lookup is introduced.
- `implementation_contract`: add only
  `GovernanceOperationsCoordinator.createServerOwnedVerifiedBackup()`. Resolve
  the actual database file and use fixed hidden directory
  `.lethebot-governance-backups` directly beside it. Create that directory as
  exactly `0700`; if it already exists, accept only a real non-symlink directory
  owned by the current process user where UID checks exist and with exactly
  `0700` permissions. Never repair or traverse an unsafe entry. Generate one
  random 256-bit base64url backup reference, derive only server-side
  `<reference>.db`, and call B23C exactly once with that path. Return the B23C
  fixed status plus `backupRef` only on completed verified publication; return
  `backupRef: null` on any directory, path-resolution, collision, owner, or
  attention result. Never return/log the directory, database path, filename,
  diagnostics, or error. Do not retry, overwrite, scan/list artifacts, write an
  audit/SQLite row, issue/consume authority, or add a route.
- `allowed_paths`: `src/governance/operations-coordinator.ts`,
  `tests/unit/governance/operations-coordinator.test.ts`, `docs/operations.md`,
  `docs/governance-ui.md`, and this checkpoint only.
- `protected_paths`: B23A doctor/CLI behavior; B23B status; B23C trusted-path
  adapter and SQLite owner; B23D GET; B23E preview; B23F POST/authority; every
  HTTP/session/scope/resource/preview implementation; restore/retention owners;
  configuration, repositories, schema, migrations, dependencies, lockfile,
  browser/QQ/Provider code, private/live data, deployment, restart, container,
  persistence format, and every prior slice.
- `assertions`: under permissive umask, one call creates only the fixed sibling
  directory at `0700` and one verified artifact at `0600`; the returned opaque
  43-character reference maps only internally to that artifact and no path or
  injected owner field appears. B23C receives exactly that internal path once.
  A second call creates a distinct reference/artifact without overwrite.
  Existing safe empty/nonempty directories are reused without mode change;
  symlink, file, permissive-mode, wrong-owner where testable, resolution,
  collision and thrown-owner results return the fixed path-free failure, while
  other attention results preserve B23C's bounded evidence; all have a null
  reference and no retry or unrelated-file mutation. Source
  DB bytes, rows, `total_changes()`, integrity, and FKs remain unchanged; B23F
  still never calls B23G/B23C and no confirmation route appears.
- `focused_commands`: run the prior seven-test coordinator baseline; add one
  named fail-first managed-backup regression and run it before source/docs;
  implement and rerun it; run full coordinator plus Operations adjacency,
  governance HTTP non-use, canonical P8, typecheck, scoped/full ESLint,
  `pnpm release:check`, diff/migration/dependency/inventory/privacy hygiene, and
  an exact four-path private reverse-first and forward-first patch proof.
- `schema_dependency_live_boundary`: one HTTP-disabled coordinator filesystem
  policy/adapter, synthetic temp files, tests, and canonical documentation only.
  No client destination, route, confirmation, authority consumption, audit or
  database write, restore, retention, schema, migration, dependency, lockfile,
  Provider, QQ, private DB, deployment, restart, container, browser, or live
  operation is permitted.
- `rollback_boundary`: the exact B23F candidate bytes and modes for all four
  allowed product/test/document paths are captured under mode-`0700`
  `/tmp/lethebot-gov-ux-01b23g-roundtrip-gZwFFM/baseline`; all files are `0644`.
  SHA-256 values in coordinator/test/Operations-doc/governance-doc order are
  `71f8201b823614eb1a73b43ad29c9f51cac16599ada8e6a43473b471e202cb2c`,
  `88a4e46898f7e7bdc2f49c2c5b7c7839e0a1356ad45b538f55f8a812d0eb66a0`,
  `517a321218f556e6c934a1c83b2a3bfc0a57cee6c8edf5e39906bc269477a622`,
  and `47282a1f3028b2afe2a77eea4cd4f0b870d9dd4500502b3a8ee10c741c232d14`.
  After implementation, one exact patch must independently prove reverse-first
  and forward-first restoration without touching the workspace or prior proof.
- `exact_fail_first_action`: completed as specified after the seven-test
  coordinator baseline passed and before any production or canonical-document
  edit.
- `fail_first_evidence`: the named run collected eight coordinator cases,
  skipped seven unrelated cases, and failed only because
  `createServerOwnedVerifiedBackup` was `undefined` instead of a function. Its
  preceding assertions proved zero B23C/owner calls, unchanged synthetic
  database-directory contents and `total_changes()`, and clean fixture cleanup.
- `final_evidence`: the named final regression passed 1 / 1 with seven
  unrelated cases skipped; combined governance HTTP/Operations adjacency
  passed 5 files / 79 tests; and the exact canonical P8 gate passed 7 files /
  305 tests. `pnpm typecheck`, scoped/full ESLint, and the final fresh
  `pnpm release:check` passed; release recorded 134 / 1 skipped files and 2730 /
  10 skipped tests. The no-input adapter creates or validates only the fixed
  sibling private directory, generates one 256-bit opaque reference, delegates
  once to B23C, and returns that reference only for completed evidence. Its
  synthetic tests prove `0700` directory and `0600` artifact modes, safe reuse,
  distinct repeated artifacts, fixed path-free handling of unsafe entries,
  owner failures, collision, and bounded attention evidence, no retries or
  unrelated-file mutation, and unchanged database bytes/rows/change count,
  integrity, and FKs. The exact four-path patch is
  `/tmp/lethebot-gov-ux-01b23g-roundtrip-gZwFFM/gov-ux-01b23g.patch`
  (`sha256:85133246acf2eef0b579fd19b1a8df257de1d6cf775cbc8d7e70642e53be83cf`,
  mode `0600`, size `18537`). Independent candidate-to-baseline-to-candidate
  and baseline-to-candidate-to-baseline cycles restored all bytes and modes;
  proof roots/trees are `0700`, product files are `0644`, and the candidate
  snapshot equals the workspace. Candidate SHA-256 values in coordinator/test/
  Operations-doc/governance-doc order are
  `62ede79f7a082499fc58ea30b7275b410a9b7ce9770584f68e60e395edbb0fa0`,
  `41d2567ce6c822716503240b0bb76444f6aedb9adb3ae502658580ad6da0f34b`,
  `35b9ace69c8b44f19975aa3cfd73674d5907330833e89ae2e3d9891f31dc1b7a`,
  and `696144b0d976bfa9c61d215b1ea88d94efd15850ee30da1f4e6e21468a58156e`.
  Inventory remains 72 modified tracked plus 53 untracked paths with zero
  staged; `git diff --check`, ASCII-only patch/additions, exact four-path/mode
  scope, VCS-visible sensitive-artifact hygiene, Pi dependencies `0.80.2`, and
  unchanged migration hashes passed. B23A-F, HTTP/session/registry behavior,
  restore/retention owners, configuration, schema, dependencies, browser, live
  behavior, and every prior slice remain unchanged.
- `exact_next_action`: completed; the post-adapter reassessment selects
  `GOV-UX-01B23H`, an authenticated-unscoped confirmation route that consumes
  B23F authority, revalidates B23E, and invokes only B23G.

## Completed Slice: `GOV-UX-01B23H`

- `requirement`: `GOV-UX-01B23H` verified-backup confirmation route.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: B23F issues exact current-session confirmation authority and
  B23G owns a safe no-input destination/reference policy, but the authority is
  never consumed and `/governance/api/v1/operations/confirm` remains absent.
- `selection_reason`: wiring only those two proved boundaries is the smallest
  dependency-ordered operation. It can create one private verified artifact
  without a client selector while leaving restore lookup/execution, artifact
  deletion, audit expansion, retention, and browser behavior separate.
- `implementation_contract`: register only authenticated-unscoped mutation
  `POST /governance/api/v1/operations/confirm` with purpose
  `governance.operations.backup.confirm`. Accept exactly
  `{ confirm: true, previewHandle }` after the shared authentication,
  same-origin, CSRF, JSON, query, and scope-header boundary. Consume the handle
  once against the current session/local-admin actor, fixed B23F action,
  `operations_verified_backup` resource, `verified_backup` ID, and system scope.
  Recompute B23E and require exact state, contract-version, and digest equality
  before calling B23G exactly once with no input. Return B23G's fixed result
  unchanged with HTTP `200`, including `backupRef: null` for attention results.
  Do not call B23C directly, accept/return a path, retry, inspect/list/delete an
  artifact, write SQLite/audit state, or add restore behavior.
- `allowed_paths`: `src/index.ts`,
  `tests/integration/governance-http-wiring.test.ts`, `docs/operations.md`,
  `docs/governance-ui.md`, and this checkpoint only.
- `protected_paths`: B23A-G owners, projections, and coordinator unit coverage;
  B23D GET and B23F preview behavior; `GovernanceHttpServer`; every
  session/scope/resource/preview registry implementation; SQLite backup/restore/
  retention owners; all other routes and mutations; configuration,
  repositories, schema, migrations, dependencies, lockfile, browser/QQ/Provider
  code, private/live data, deployment, restart, container, persistence format,
  and every prior slice.
- `assertions`: unauthenticated requests return `401`; missing/wrong Origin or
  CSRF returns `403`; missing/wrong content type, malformed/oversized JSON,
  queries, scope headers, and every non-exact body return fixed `400` before
  consumption or effects. Unknown/cross-session/cross-binding authority returns
  `404`; already-consumed authority returns `409`. Valid authority is consumed
  once, B23E is recomputed once, and any state/version/digest mismatch returns
  `409` before B23G. A valid current confirmation calls B23G once with no input,
  returns only its fixed evidence, creates one `0600` artifact under the private
  `0700` directory, and leaves database rows, `total_changes()`, integrity, FKs,
  and audit count unchanged. Repetition cannot retry the effect; an attention
  result consumes authority and returns fixed null-reference evidence. GET and
  preview remain unchanged; no destination, filename, diagnostic, restore,
  retention, deletion, or rollback route is exposed.
- `focused_commands`: capture the exact B23G baseline, run one named fail-first
  confirmation regression before source/docs, implement and rerun it, then run
  full HTTP wiring plus coordinator/doctor/SQLite/Operations adjacency, the
  canonical P8 command, typecheck, scoped/full ESLint, `pnpm release:check`,
  hygiene checks, and exact four-path reverse-first/forward-first proof.
- `schema_dependency_live_boundary`: one static route, synthetic filesystem
  effects/tests, existing in-memory authority, and canonical Operations
  documentation only. No client destination, direct owner call, database/audit
  write, restore, retention, artifact deletion, schema, migration, dependency,
  lockfile, Provider, QQ, private DB, deployment, restart, container, browser,
  or live operation is permitted.
- `rollback_boundary`: the exact B23G candidate bytes and modes for all four
  allowed product/test/document paths are captured under mode-`0700`
  `/tmp/lethebot-gov-ux-01b23h-roundtrip-eBk9oo/baseline`; all files are `0644`.
  SHA-256 values in index/wiring-test/Operations-doc/governance-doc order are
  `7d42e5e62ce398e27d005fc461df75e443bff3ddeb6824e0476c1a96ae47b068`,
  `a1ece09f920ce116583a7a0f3eb53f7e47cb79d8c342d35b6a88da2839d19f81`,
  `35b9ace69c8b44f19975aa3cfd73674d5907330833e89ae2e3d9891f31dc1b7a`,
  and `696144b0d976bfa9c61d215b1ea88d94efd15850ee30da1f4e6e21468a58156e`.
  After implementation, one exact patch must independently prove reverse-first
  and forward-first restoration without touching the workspace or B23G proof.
- `exact_fail_first_action`: completed after the untouched 25-test HTTP-wiring
  baseline passed and before any production or canonical-document edit.
- `fail_first_evidence`: the named run collected 26 HTTP-wiring cases, skipped
  25 unrelated cases, and failed only because unauthenticated
  `POST /governance/api/v1/operations/confirm` returned `404` instead of the
  required `401`. Before that expected assertion, the regression proved zero
  preview consumption, managed/direct backup calls, file effects, and database
  changes, plus clean application shutdown.
- `final_evidence`: the named confirmation regression passed 1 / 1 with 25
  unrelated cases skipped; full HTTP wiring passed 26 / 26; the five-file
  coordinator/doctor/SQLite/HTTP adjacency gate passed 80 / 80; and the exact
  canonical P8 gate passed 7 files / 305 tests. `pnpm typecheck`, scoped and
  full ESLint, and the final fresh `pnpm release:check` passed; release recorded
  134 / 1 skipped files and 2731 / 10 skipped tests. The route consumes exact
  B23F authority once, recomputes B23E before effects, calls only B23G once with
  no input, returns completed or attention evidence unchanged, and exposes no
  path or diagnostic. Synthetic evidence proves one private `0600` artifact,
  consumed/stale/cross-boundary rejection, no retry after completed or attention
  results, unchanged B23D/B23F behavior, unchanged database rows/change count,
  clean integrity/FKs, and no audit write. The exact four-path patch is
  `/tmp/lethebot-gov-ux-01b23h-roundtrip-eBk9oo/gov-ux-01b23h.patch`
  (`sha256:7f00270cb2d9757ecbedfc5abb57da195a46b05f60150a7e5bc473300b429534`,
  mode `0600`, size `24700`). Independent candidate-to-baseline-to-candidate
  and baseline-to-candidate-to-baseline cycles restored all bytes and modes;
  proof roots/trees are `0700`, product files are `0644`, and the candidate
  snapshot equals the workspace. Candidate SHA-256 values in index/wiring-test/
  Operations-doc/governance-doc order are
  `bf48d7b474c2bd5bbd8bb450ab662a116b7c9393ceb16ea6145954bcca988b2e`,
  `b3135b0622004ca23686187651c193e41ea6a625f853c0e3f406757191ab6cb9`,
  `774ee8cb1a578b32497e07c4163a3166967eae1559f14bf1fe48a593eb61872d`,
  and `ef7fb0a043787e2653b60474ef5b36d8b57742249eb5f2f71a6fdeb32efcfa80`.
  Inventory remains 72 modified tracked plus 53 untracked paths with zero
  staged; `git diff --check`, ASCII-only patch/additions, exact four-path/mode
  scope, VCS-visible sensitive-artifact hygiene, Pi dependencies `0.80.2`, and
  unchanged migration hashes passed. B23A-G, generic HTTP/session/registry,
  restore/retention owners, configuration, schema, dependencies, browser, live
  behavior, and every prior slice remain unchanged.
- `exact_next_action`: completed; the post-confirmation reassessment selects
  `GOV-UX-01B23I`, an HTTP-disabled exact-reference restore-handoff preview.
  This is narrower than an artifact catalog or restore execution and preserves
  the stopped-service boundary.

## Completed Slice: `GOV-UX-01B23I`

- `requirement`: `GOV-UX-01B23I` exact-reference managed-backup restore-handoff
  preview.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: B23H returns a persistent opaque backup reference after one
  verified artifact is created, but no shared owner can safely re-resolve and
  revalidate that exact reference for a later restore handoff. Accepting a path,
  scanning every artifact, or invoking in-process restore would cross three
  separate trust and lifecycle boundaries.
- `selection_reason`: an HTTP-disabled exact-reference read is the smallest
  dependency-ordered next step. It proves safe reference resolution and fixed
  restore-readiness evidence before any catalog, resource handle, route,
  authority, handoff mutation, or stopped-service execution is designed.
- `implementation_contract`: add only
  `GovernanceOperationsCoordinator.previewServerOwnedBackupRestore(backupRef)`.
  Accept an exact 43-character base64url reference, resolve the real database
  location, and inspect only `<fixed-private-directory>/<reference>.db` without
  creating or repairing anything. Require the existing directory to be a real
  current-user-owned `0700` directory where UID checks exist, and the exact
  artifact to be a real current-user-owned `0600` regular file with resolved
  containment and no symlink. Add a shared SQLite snapshot-integrity owner that
  copies supplied bytes, accepts only valid rollback/WAL header versions,
  normalizes those two bytes only in the copy, and verifies the copy in memory
  without opening the artifact path or creating sidecars. Return only a fixed
  `prepare_restore_handoff` preview with verified artifact, bounded size,
  `stopped_service_only` execution boundary, no in-process effect, contract
  version, and a domain-separated digest bound internally to the exact reference
  and artifact bytes. Invalid, missing, unsafe, empty, raced, or corrupt
  artifacts return no preview without revealing which check failed. Do not list
  artifacts, reflect the reference, return a path/filename/diagnostic, issue
  authority, add a route, write a file/database/audit row, delete an artifact,
  or execute restore.
- `allowed_paths`: `src/governance/operations-coordinator.ts`,
  `tests/unit/governance/operations-coordinator.test.ts`,
  `src/operations/sqlite-maintenance.ts`,
  `tests/unit/operations/sqlite-maintenance.test.ts`, `docs/operations.md`,
  `docs/governance-ui.md`, and this checkpoint only.
- `protected_paths`: B23A-H behavior and HTTP wiring; every session/scope/
  resource/preview registry implementation; SQLite backup/restore/retention
  behavior; all routes and mutations; configuration, repositories, schema,
  migrations, dependencies, lockfile, browser/QQ/Provider code, private/live
  data, deployment, restart, container, persistence format, and every prior
  slice.
- `assertions`: a B23H-created artifact produces one stable path-free preview
  without writes; repeated reads preserve bytes, modes, directory contents,
  database rows, `total_changes()`, integrity, FKs, and audit count. Malformed
  references, missing directory/file, symlink/file/permissive/wrong-owner
  directory, symlink/directory/permissive/wrong-owner/empty/corrupt artifact,
  containment or stable-file mismatch, and owner exceptions return no preview
  and no diagnostic or repair. Integrity verification creates no WAL/SHM or
  other sidecar, does not modify the input bytes, and does not weaken the
  existing path-based backup/restore verifier. No sibling is inspected, no
  route appears, and B23H confirmation remains unchanged.
- `focused_commands`: capture the exact B23H four-path coordinator baseline;
  run the prior eight-test coordinator suite; add and run only one named
  fail-first exact-reference preview regression before source/docs; then run
  full coordinator plus SQLite-maintenance and Operations/HTTP adjacency,
  canonical P8, typecheck, scoped/full ESLint, `pnpm release:check`, hygiene
  checks, and an exact six-path private reverse-first/forward-first proof.
- `schema_dependency_live_boundary`: one HTTP-disabled read-only coordinator
  projection and one buffer-only SQLite integrity helper over synthetic local
  artifacts, tests, and canonical Operations documentation only. Existing
  path-based backup/restore verification must remain unchanged. No catalog,
  route, authority, handoff file, database/audit write, deletion, restore
  execution, retention, schema, migration, dependency, lockfile, Provider, QQ,
  private DB, deployment, restart, container, browser, or live operation is
  permitted.
- `rollback_boundary`: the exact B23H candidate bytes and modes for all six
  allowed product/test/document paths are captured under mode-`0700`
  `/tmp/lethebot-gov-ux-01b23i-roundtrip-eSt6Xt/baseline`; all files are `0644`.
  SHA-256 values in coordinator/coordinator-test/SQLite-owner/SQLite-test/
  Operations-doc/governance-doc order are
  `62ede79f7a082499fc58ea30b7275b410a9b7ce9770584f68e60e395edbb0fa0`,
  `41d2567ce6c822716503240b0bb76444f6aedb9adb3ae502658580ad6da0f34b`,
  `982ff6001e059ec6f030bd90be2470005f66d7158e487f1398c64cb64ec14db2`,
  `723601f8b93b27fc0804a3b20889d52bda7822f42915c30ecd1bacb3b0299f42`,
  `774ee8cb1a578b32497e07c4163a3166967eae1559f14bf1fe48a593eb61872d`,
  and `ef7fb0a043787e2653b60474ef5b36d8b57742249eb5f2f71a6fdeb32efcfa80`.
  After implementation, one exact patch must independently prove reverse-first
  and forward-first restoration without touching the workspace or B23H proof.
- `baseline_evidence`: the untouched coordinator suite passed 8 / 8 before the
  B23I regression or any production/canonical-document edit; the baseline tree
  matches the workspace byte-for-byte and mode-for-mode.
- `exact_fail_first_action`: completed as specified after the eight-test
  coordinator baseline and before any production or canonical-document edit.
- `fail_first_evidence`: the named run collected nine coordinator cases,
  skipped eight unrelated cases, and failed only because
  `previewServerOwnedBackupRestore` was `undefined` instead of a function. Its
  preceding assertions proved two valid server-owned artifacts, zero preview
  integrity calls, unchanged directory contents and `total_changes()`, and no
  doctor, restore, or retention execution before the expected assertion.
- `implementation_refinement`: the first method run reached the final
  directory-invariance assertion and exposed that the existing path-based
  integrity helper creates WAL/SHM sidecars when it opens a WAL-header backup.
  Deleting those sidecars after inspection would violate the no-mutation
  boundary. A synthetic buffer experiment proved that validating a copied
  snapshot in memory after copied-header normalization returns `ok` with no
  directory change, so the allowed/rollback scope now includes only that shared
  helper and its focused test; existing path-based behavior remains protected.
- `final_evidence`: the named B23I regression passed 1 / 1 with eight unrelated
  coordinator cases skipped, and the named buffer-integrity regression passed
  1 / 1 with 20 unrelated SQLite-maintenance cases skipped. Full Operations/HTTP
  adjacency passed 5 files / 73 tests, and the canonical P8 gate passed 7 files
  / 306 tests. Typecheck, scoped and full ESLint, and the fresh
  `pnpm release:check` passed; release recorded 134 / 1 skipped files and 2733 /
  10 skipped tests. Exact-reference resolution accepts only the fixed private
  owned `0700` directory and contained owned single-link non-symlink `0600`
  artifact, reads through a no-follow descriptor with stable device/inode/size,
  verifies copied bytes in memory without sidecars, and returns only fixed
  stopped-service handoff semantics plus a reference/bytes/semantics-bound
  digest. Invalid or unsafe inputs return null without repair or diagnostic;
  repeated reads preserve all artifact, directory, database, audit, integrity,
  and FK evidence; B23A-H and the existing path-based verifier remain unchanged.
  The exact six-path patch is
  `/tmp/lethebot-gov-ux-01b23i-roundtrip-eSt6Xt/gov-ux-01b23i.patch`
  (`sha256:6f4374be1e54f5e898f18879cd0a4ee9ae956ebfdb903fd51309ed545ead3f57`,
  mode `0600`, size `30027`). Independent reverse-first and forward-first cycles
  restored baseline and candidate bytes/modes; all proof trees are `0700`, all
  six product snapshots are `0644`, and the candidate snapshot matches the
  workspace. Candidate SHA-256 values in coordinator/coordinator-test/SQLite-
  owner/SQLite-test/Operations-doc/governance-doc order are
  `802b2d6dccad47d19d8f406bd9e41db598d255260714c6ac741d0fe67404a709`,
  `1755370f3ac233b0ea4b2daeff96d5816804a1fd5990a449637a02aba59497a1`,
  `42bd5fd098eb1955905e283cb4fcb44983a5023bbe3cc3f21d6243ceb4fc57d1`,
  `0dd4c8b12cf34e1a798ee3d003c57cd7066f787a9351734c11ebdc868602edd5`,
  `c02a5756e252be95557792a8743dabad41a7d4c3e7f5cc435669dd46f026ea1b`,
  and `a8ce6c9633d5581c96070a06cc05c91d6b6a08af2a9a2d0985a1ae4b4d85ea9e`.
  Inventory remains 72 modified tracked plus 53 untracked paths with zero
  staged; `git diff --check`, ASCII-only patch/candidates, exact six-path scope,
  sensitive untracked filename hygiene, Pi dependencies `0.80.2`, and unchanged
  migration hashes all passed.
- `exact_next_action`: completed; the post-B23I reassessment selects
  `GOV-UX-01B23J`, one authenticated-unscoped restore-preview route that accepts
  only the opaque B23H reference, calls only B23I, and issues exact session-bound
  single-use authority over the fixed preview. Confirmation, handoff mutation,
  restore execution, artifact mutation, catalog/list behavior, and path output
  remain absent.

## Completed Slice: `GOV-UX-01B23J`

- `requirement`: `GOV-UX-01B23J` authenticated restore-handoff preview and
  session-bound authority.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: B23I safely re-resolves one exact opaque backup reference and
  returns fixed path-free restore-readiness evidence, but HTTP cannot request
  that preview or bind it to a current admin session. A confirmation or stopped-
  service executor before that authority exists would skip the established
  preview/confirm boundary.
- `selection_reason`: expose the already-proved B23I owner through one isolated
  preview route before designing handoff confirmation. This is smaller than a
  catalog, artifact resource, handoff file, restore command, or in-process
  effect and follows the existing B23E/B23F dependency order.
- `implementation_contract`: register only authenticated-unscoped mutation
  `POST /governance/api/v1/operations/restore` with purpose
  `governance.operations.restore.preview`. After the existing session, same-
  origin, CSRF, JSON, query, and scope-header boundary, accept exactly
  `{ action: "prepare_restore_handoff", backupRef }` where `backupRef` is an
  exact 43-character base64url value. Call only
  `previewServerOwnedBackupRestore(backupRef)` once. A null preview returns fixed
  `404`; success issues one current-session preview handle bound to local admin,
  action `prepare_restore_handoff`, resource kind `operations_backup_restore`,
  the exact reference as internal resource ID, system scope, current state,
  contract version, and preview digest, then returns the unchanged B23I preview
  plus handle and expiry with `201`. Do not reflect the reference, add a confirm
  route, consume authority, create/delete/modify an artifact, write SQLite/audit
  state, execute restore/retention, or expose a path, filename, or diagnostic.
- `allowed_paths`: `src/index.ts`,
  `tests/integration/governance-http-wiring.test.ts`, `docs/operations.md`,
  `docs/governance-ui.md`, and this checkpoint only.
- `protected_paths`: B23A-I owners, DTOs, digests, backup creation/confirmation,
  and private filesystem behavior; generic HTTP/session/CSRF/scope/resource/
  preview registries; all existing routes; SQLite backup/restore/retention;
  configuration, repositories, schema, migrations, dependencies, lockfile,
  browser/CLI/QQ/Provider code, private/live data, deployment, restart,
  container, persistence format, and every prior slice.
- `assertions`: unauthenticated, wrong/missing Origin or CSRF, non-JSON,
  malformed JSON/body/reference, query, and any scope header fail before B23I or
  authority issuance. A missing/unsafe exact reference returns fixed `404`
  without authority. A B23H-created reference returns the exact fixed B23I DTO,
  a 43-character opaque handle, and bounded expiry; repeated previews retain the
  same digest but receive distinct handles. Issuer input is exactly bound to the
  current session, local admin, action, kind, internal reference, system scope,
  state, version, and digest. Responses omit the reference, paths, private
  values, and diagnostics. Backup confirmation remains exact; restore-preview
  authority is rejected by the backup confirmation route; no restore confirm
  route appears. Reads preserve artifact bytes/modes/directory contents,
  database rows, `total_changes()`, audit count, integrity, and FKs; no restore,
  retention, deletion, or handoff effect occurs; default-disabled behavior and
  shutdown remain unchanged.
- `focused_commands`: capture the exact B23I four-path baseline; run the prior
  26-test HTTP-wiring suite; add and run one named fail-first restore-preview
  route regression before source or canonical-document edits; then run full
  wiring plus coordinator/SQLite/HTTP adjacency, canonical P8, typecheck,
  scoped/full ESLint, `pnpm release:check`, hygiene checks, and exact four-path
  private reverse-first/forward-first proof.
- `schema_dependency_live_boundary`: one static authenticated-unscoped preview
  route, existing in-memory authority, synthetic filesystem tests, and canonical
  Operations documentation only. No confirm route, authority consumption,
  handoff mutation, restore/retention execution, artifact mutation/deletion,
  database/audit write, schema, migration, dependency, lockfile, Provider, QQ,
  private DB, deployment, restart, container, browser, or live operation is
  permitted.
- `rollback_boundary`: the exact B23I candidate bytes and modes for all four
  allowed product/test/document paths are captured under mode-`0700`
  `/tmp/lethebot-gov-ux-01b23j-roundtrip-DDmJeU/baseline`; all files are `0644`.
  SHA-256 values in index/wiring-test/Operations-doc/governance-doc order are
  `bf48d7b474c2bd5bbd8bb450ab662a116b7c9393ceb16ea6145954bcca988b2e`,
  `b3135b0622004ca23686187651c193e41ea6a625f853c0e3f406757191ab6cb9`,
  `c02a5756e252be95557792a8743dabad41a7d4c3e7f5cc435669dd46f026ea1b`,
  and `a8ce6c9633d5581c96070a06cc05c91d6b6a08af2a9a2d0985a1ae4b4d85ea9e`.
  After implementation one exact patch must independently prove reverse-first
  and forward-first restoration without touching the workspace or B23I proof.
- `baseline_evidence`: the untouched governance HTTP-wiring suite passed 26 /
  26, and the baseline tree matches the workspace byte-for-byte and mode-for-
  mode before the B23J regression or any production/canonical-document edit.
- `exact_fail_first_action`: completed after the untouched 26-test wiring
  baseline and before any production or canonical-document edit.
- `fail_first_evidence`: the named run collected 27 wiring cases, skipped 26
  unrelated cases, and failed only because unauthenticated
  `POST /governance/api/v1/operations/restore` returned `404` instead of the
  required `401`. Before that expected assertion, the regression proved zero
  B23I calls, preview-authority issuance, managed-backup creation,
  restore/retention calls, file changes, and database changes, plus clean
  application shutdown.
- `final_evidence`: the named B23J regression passed 1 / 1 with 26 unrelated
  wiring cases skipped; full wiring passed 27 / 27, five-file Operations/HTTP
  adjacency passed 74 / 74, and the canonical P8 gate passed 7 files / 306
  tests. Typecheck, scoped and full ESLint, and the fresh
  `pnpm release:check` passed; release recorded 134 / 1 skipped files and 2734 /
  10 skipped tests. The exact route accepts only the authenticated-unscoped
  body and calls B23I once, fixed `404` returns no authority, and a verified
  reference receives distinct current-session handles bound to the exact
  action/resource/reference/system/state/version/digest evidence without any
  confirmation, file, database, audit, restore, retention, or deletion effect.
  The exact four-path patch is
  `/tmp/lethebot-gov-ux-01b23j-roundtrip-DDmJeU/gov-ux-01b23j.patch`
  (`sha256:a6319132eeecc37fb2a8eed7158791f86734e98be780aec0b9acc91a6cec8bc6`,
  mode `0600`, size `23971`). Independent reverse-first and forward-first
  cycles restored baseline and candidate bytes/modes; all proof trees are
  `0700`, all four product snapshots are `0644`, and the candidate snapshot
  equals the workspace. Candidate SHA-256 values in index/wiring-test/
  Operations-doc/governance-doc order are
  `34074fa808a33327b3f00ca056aa2d5ea709fee599e16a695fd7a88a369e0b70`,
  `9a9c3446dc2433941ee83882840f2729a179e5e5f7f803eff7b4dd15e557396d`,
  `2aaf5f6768b3be01cc73d08ec073827b49b10d91c3dc0bffb0853212e8c2922c`,
  and `d075f65c3533e63817a91731639ca0965a8a09c5b2b2bb1b60cc978b4e8a8262`.
  Inventory remained 72 modified tracked plus 53 untracked paths with zero
  staged; patch/addition ASCII, sensitive-filename hygiene, exact path/mode
  scope, migration hashes, Pi `0.80.2`, and `git diff --check` passed.
- `exact_next_action`: completed; the post-B23J reassessment selected
  `GOV-UX-01B23K`, one authenticated-unscoped confirmation route that consumes
  only exact B23J current-session authority and recomputes B23I without creating
  a handoff file or executing restore.

## Completed Slice: `GOV-UX-01B23K`

- `requirement`: `GOV-UX-01B23K` exact restore-handoff confirmation without
  in-process restore execution.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: B23J issues exact current-session authority for one verified
  managed backup, but no route consumes that authority or detects artifact drift
  before the operator crosses the stopped-service boundary.
- `selection_reason`: consume and revalidate B23J authority before designing any
  durable handoff or external executor. This preserves the established
  preview/confirm order while keeping database replacement outside the running
  process.
- `implementation_contract`: register only authenticated-unscoped mutation
  `POST /governance/api/v1/operations/restore/confirm` with purpose
  `governance.operations.restore.confirm`. After the existing session,
  same-origin, CSRF, JSON, query, and scope-header boundary, accept exactly
  `{ confirm: true, previewHandle, backupRef }`, with both opaque values exact
  43-character base64url strings. Consume current-session authority bound to
  local admin, action `prepare_restore_handoff`, resource kind
  `operations_backup_restore`, the exact reference, and system scope. Unknown or
  mismatched authority returns fixed `404`; consumed authority returns fixed
  `409`. Recompute B23I once and require exact current state, contract version,
  and digest equality; null or drift returns fixed `409`. Current confirmation
  returns the unchanged path-free B23I projection with `200`. Do not write a
  handoff, artifact, database, or audit row; do not stop/restart a service or
  execute restore/retention.
- `allowed_paths`: `src/index.ts`,
  `tests/integration/governance-http-wiring.test.ts`, `docs/operations.md`,
  `docs/governance-ui.md`, and this checkpoint only.
- `protected_paths`: B23A-J owners, DTOs, digests, backup creation/confirmation,
  private filesystem behavior, and restore preview; generic HTTP/session/CSRF/
  scope/resource/preview registries; SQLite backup/restore/retention; all other
  routes; configuration, repositories, schema, migrations, dependencies,
  lockfile, browser/CLI/QQ/Provider code, private/live data, deployment,
  supervisor, restart, container, persistence format, and every prior slice.
- `assertions`: unauthenticated, wrong/missing Origin or CSRF, non-JSON,
  malformed JSON/body/reference/handle, query, and any scope header fail before
  authority consumption or B23I. Unknown, cross-session, wrong-reference,
  wrong-action, and wrong-resource authority return `404`; current authority is
  consumed once. Reuse returns `409`. A null preview or state/version/digest
  drift returns `409` and remains consumed. Success calls B23I once, returns its
  exact projection without the reference, and performs no managed-backup,
  artifact, handoff, database, audit, restore, retention, service, or supervisor
  effect. Integrity/FKs, files, rows, `total_changes()`, and prior routes remain
  unchanged.
- `focused_commands`: capture the exact B23J four-path baseline; run the prior
  27-test HTTP-wiring suite; add and run one named fail-first restore-confirm
  regression before production or canonical-document edits; then run named and
  full wiring, five-file Operations/HTTP adjacency, the canonical P8 command,
  typecheck, scoped/full ESLint, `pnpm release:check`, hygiene checks, and an
  exact four-path private reverse-first/forward-first proof.
- `schema_dependency_live_boundary`: one static authenticated-unscoped
  confirmation route over existing in-memory authority and the existing B23I
  read, synthetic local fixtures, and canonical Operations documentation only.
  No handoff file, database/audit write, artifact mutation/deletion, restore,
  retention, service stop/restart, supervisor integration, schema, migration,
  dependency, lockfile, Provider, QQ, private DB, deployment, container,
  browser, or live operation is permitted.
- `rollback_boundary`: the exact B23J candidate bytes and modes for all four
  allowed product/test/document paths are captured under mode-`0700`
  `/tmp/lethebot-gov-ux-01b23k-roundtrip-iCKPCv/baseline`; all files are `0644`.
  SHA-256 values in index/wiring-test/Operations-doc/governance-doc order are
  `34074fa808a33327b3f00ca056aa2d5ea709fee599e16a695fd7a88a369e0b70`,
  `9a9c3446dc2433941ee83882840f2729a179e5e5f7f803eff7b4dd15e557396d`,
  `2aaf5f6768b3be01cc73d08ec073827b49b10d91c3dc0bffb0853212e8c2922c`,
  and `d075f65c3533e63817a91731639ca0965a8a09c5b2b2bb1b60cc978b4e8a8262`.
  One exact patch independently proves reverse-first and forward-first
  restoration without touching the workspace or B23J proof.
- `baseline_evidence`: the untouched governance HTTP-wiring suite passed 27 /
  27, and the baseline tree matched the workspace byte-for-byte and mode-for-
  mode before the B23K regression or any production/canonical-document edit.
- `exact_fail_first_action`: completed after the untouched 27-test wiring
  baseline and before any production or canonical-document edit.
- `fail_first_evidence`: the named run collected 28 wiring cases, skipped 27
  unrelated cases, and failed only because unauthenticated
  `POST /governance/api/v1/operations/restore/confirm` returned `404` instead of
  required `401`. Its preceding assertions proved zero authority consumption,
  B23I calls, managed-backup creation, restore/retention calls, file changes,
  and database changes.
- `final_evidence`: the named B23K regression passed 1 / 1 with 27 unrelated
  cases skipped; full wiring passed 28 / 28, five-file Operations/HTTP adjacency
  passed 75 / 75, and the exact canonical P8 gate passed 7 files / 306 tests.
  Typecheck, scoped and full ESLint, build, release preflight, and the fresh
  `pnpm release:check` passed; release recorded 134 / 1 skipped files and 2735 /
  10 skipped tests. Exact current-session authority is consumed once, B23I is
  recomputed once, drift/reuse is conflict-safe, and success returns only the
  unchanged path-free plan with no handoff or restore effect. The exact
  four-path patch is
  `/tmp/lethebot-gov-ux-01b23k-roundtrip-iCKPCv/gov-ux-01b23k.patch`
  (`sha256:95eb20e31cef0087a6d2022d2820a349ecde7aef251c9f58e054980621827998`,
  mode `0600`, size `21224`). Independent candidate-to-baseline-to-candidate and
  baseline-to-candidate-to-baseline cycles restored all bytes and modes; proof
  roots/trees are `0700`, product files are `0644`, and the candidate snapshot
  equals the workspace. Candidate SHA-256 values in index/wiring-test/
  Operations-doc/governance-doc order are
  `bf00721d3cbd2b17ed062a79c011a29d0574fb6ce53fe8d83d812e03388550dc`,
  `9be7f9e97c761a5e00a199e9460e96155d8ecc9873951d0b0ba052877640ce9d`,
  `efdf1c41a8eb08573ca1f4dea27cc426faa28470c0e920866aa7550c0587ab98`,
  and `e6c01761e14386944d3fe55f7ad97799f85bb3573d595a6ae4e5b4d780a6e826`.
  Inventory remains 72 modified tracked plus 53 untracked paths with zero
  staged; `git diff --check`, ASCII-only patch/additions, exact four-path/mode
  scope, sensitive-filename hygiene, Pi dependencies `0.80.2`, and unchanged
  migration hashes passed.
- `exact_next_action`: completed; the post-B23K reassessment selects
  `GOV-UX-01B23L`, an HTTP-disabled private durable handoff-envelope owner. The
  route, existing restore command, service lifecycle, and restore execution
  remain unchanged until that filesystem boundary is independently proved.

## Completed Slice: `GOV-UX-01B23L`

- `requirement`: `GOV-UX-01B23L` private durable restore-handoff envelope owner.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: B23K proves current-session confirmation and artifact drift
  detection, but the authority and result live only in the running process.
  After the service stops, the existing path-based maintenance command has no
  server-owned durable evidence tying one confirmed opaque backup reference and
  preview digest to the configured database. Passing client paths or treating
  the expired in-memory handle as stopped-service authority would break the
  established boundary.
- `selection_reason`: prove a narrow HTTP-disabled filesystem owner before
  changing B23K or any CLI/supervisor path. A fixed private pending envelope is
  the smallest bridge that survives process shutdown without executing restore,
  exposing a path, or coupling HTTP to service lifecycle control.
- `implementation_contract`: add only a typed Operations owner in
  `src/operations/governance-restore-handoff.ts`. It accepts trusted internal
  database location plus exact validated backup reference, preview digest,
  contract version, and time; it accepts no client path, session handle, or
  credential. Resolve only a fixed sibling
  `.lethebot-governance-restore-handoff` directory, creating it as `0700` or
  accepting it only as a real current-user-owned non-symlink `0700` directory
  where UID checks exist. Publish at most one fixed `pending.json` envelope as
  owned single-link `0600` data with an explicit version/state, random 256-bit
  handoff ID, exact internal backup reference and preview digest, and bounded
  creation/expiry timestamps. Use operation-owned private staging, fsync, and
  no-clobber publication; an existing, unsafe, malformed, or raced pending entry
  returns one fixed path-free conflict/attention result without repair,
  overwrite, deletion, or diagnostic. Return only fixed pending-state,
  handoff-ID, expiry, execution-boundary, and no-restore-effect evidence. Do not
  register a route, modify B23K, read or restore the target database, inspect a
  catalog, consume/remove a handoff, write SQLite/audit state, or stop/restart a
  service.
- `allowed_paths`: `src/operations/governance-restore-handoff.ts`,
  `tests/unit/operations/governance-restore-handoff.test.ts`,
  `docs/operations.md`, `docs/governance-ui.md`, and this checkpoint only.
- `protected_paths`: B23A-K behavior and every existing route/coordinator/
  registry; managed backup artifacts and directory; SQLite backup/restore/
  retention owners and CLI grammar/output; application/configuration lifecycle;
  repositories, schema, migrations, dependencies, lockfile, browser/QQ/Provider
  code, private/live data, deployment, supervisor, restart, container,
  persistence format, and every prior slice.
- `assertions`: valid synthetic input creates exactly one strict-schema pending
  envelope and returns a fixed path-free receipt. Repeated/raced creation never
  overwrites the first envelope. Invalid reference/digest/version/time,
  symlink/file/permissive/wrong-owner directory, symlink/directory/permissive/
  wrong-owner/multi-link/malformed pending entry, and owner exceptions yield
  fixed evidence without leaked values or repair. Publication leaves no staging
  file, survives reopen byte-for-byte, preserves `0700`/`0600`, and changes no
  database, backup artifact, audit row, route, registry, service, or supervisor
  state. No catalog or restore execution appears.
- `focused_commands`: capture the exact B23K canonical-document baseline and
  absence of the two new owner/test paths; run current SQLite Operations tests;
  add and run one named fail-first owner regression before implementation or
  canonical-document edits; then run the full new owner suite, SQLite/
  coordinator/Operations HTTP adjacency, canonical P8, typecheck, scoped/full
  ESLint, `pnpm release:check`, hygiene checks, and an exact four-path private
  reverse-first/forward-first proof.
- `schema_dependency_live_boundary`: one HTTP-disabled local filesystem owner,
  synthetic directories/files, unit tests, and canonical Operations
  documentation only. No route, authority consumption, database/audit write,
  artifact mutation/deletion, restore/retention, service stop/restart,
  supervisor integration, schema, migration, dependency, lockfile, Provider,
  QQ, private DB, deployment, container, browser, or live operation is
  permitted.
- `rollback_boundary`: before any B23L regression or product/document edit,
  capture the exact B23K `docs/operations.md` and `docs/governance-ui.md` bytes/
  modes plus the verified absence of both new owner/test paths under a private
  mode-`0700` proof root. The final patch must independently prove deletion and
  recreation of the new paths plus baseline/candidate restoration of both docs
  without touching the workspace or B23K proof.
- `exact_fail_first_action`: completed. The untouched SQLite Operations owner
  baseline passed 21 / 21; the named owner test then failed only because the
  static `governance-restore-handoff` module/export was absent, before any
  production or canonical-document edit.
- `fail_first_evidence`: the named Vitest collection produced zero tests and one
  missing-module resolution failure for the required owner import; no unrelated
  assertion or file effect occurred.
- `final_evidence`: the owner suite passed 1 file / 24 tests; the six-file
  Operations/HTTP adjacency passed 99 / 99; the canonical P8 suite passed 7
  files / 306 tests; `pnpm typecheck`, full ESLint, and `git diff --check`
  passed. Fresh `pnpm release:check` passed 135 / 1 skipped files and 2759 / 10
  skipped tests, including build and release preflight. The owner validates
  exact references/digests/version/time before mutation, creates or validates
  only the fixed private sibling directory, publishes one strict version-1
  pending envelope with a random 256-bit ID and 15-minute bounded expiry, and
  returns fixed path-free pending/attention evidence. Exclusive private
  staging, file/directory fsync, hard-link no-clobber publication, inode/link/
  mode/owner checks, unchanged sentinels, unsafe-entry preservation, repeat
  no-clobber, restrictive-umask, byte-reopen, and no-staging assertions passed;
  no route, registry, B23K, database, backup artifact, SQLite/audit, restore,
  retention, service, supervisor, schema, migration, dependency, or live effect
  was added.
- `rollback_evidence`: the exact B23K document baseline retained
  `docs/operations.md` SHA-256
  `efdf1c41a8eb08573ca1f4dea27cc426faa28470c0e920866aa7550c0587ab98` and
  `docs/governance-ui.md` SHA-256
  `e6c01761e14386944d3fe55f7ad97799f85bb3573d595a6ae4e5b4d780a6e826`; both
  new owner/test paths were absent. Candidate SHA-256 values in owner/test/
  Operations-doc/governance-doc order are
  `a76f1b965d11b8f20c673bc77510473e47c9746a04bc384368c5f87e6041a46d`,
  `87566186f45819509ab6a4e846944b477fd63290b3c212042007f1dcc157bc32`,
  `b77736c49b1abbc016bb2ebb7b0208188d67eca1605530949157308d05e4501c`, and
  `e9b5b7f3048a0c01526b4ae52693e1a762991b64f47252381aac3316f9e8c196`.
  Independent reverse-first and forward-first deletion/recreation cycles proved
  all four candidate/baseline byte and mode restorations without touching the
  workspace or B23K proof. The private root and every proof tree are `0700`,
  product snapshots are `0644`, and the patch
  `/tmp/lethebot-gov-ux-01b23l-roundtrip-5d453f/gov-ux-01b23l.patch` has SHA-256
  `a478009cf7c1847c3829a4e8b21817f333c055c51ed751dbcf9d6d46e49b8358`, mode
  `0600`, and size `31502` bytes.
- `inventory_evidence`: the worktree remains 72 modified tracked paths plus 55
  untracked files under `--untracked-files=all`, with zero staged paths; the
  only sensitive-name inventory match remains the expected tracked
  `.env.example`. Added files and docs are ASCII-only, migrations 007/008 retain
  SHA-256 `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b` /
  `95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`, and the
  installed Pi dependency remains `0.80.2`.
- `exact_next_action`: reassess the dependency path and begin `GOV-UX-01B23M`,
  a read-only HTTP-disabled reopen/validation owner over the fixed pending
  envelope. Do not consume/remove it, wire B23K/CLI/supervisor behavior, or
  execute restore first.

## Completed Slice: `GOV-UX-01B23M`

- `requirement`: `GOV-UX-01B23M` durable stopped-service handoff reopen/validation owner.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: B23L durably publishes one private envelope, but there was no
  independent owner that could reopen it after process restart and prove its exact
  schema, ownership, expiry, database-sibling binding, reference, digest, and
  restore-contract semantics without exposing a path or consuming the entry.
- `selection_reason`: validate the durable boundary before adding any consumer,
  deletion, service lifecycle, or restore execution. A read-only reopen owner was
  the smallest next step and keeps the pending envelope inspectable without
  granting stopped-service authority.
- `implementation_contract`: add only a typed HTTP-disabled owner and unit test
  that accept trusted internal database location plus optional current time. It
  resolves only the fixed sibling directory and fixed `pending.json`, requires a
  real current-user-owned non-symlink `0700` directory and owned single-link
  `0600` regular file, opens with no-follow semantics, validates the exact
  version-1 envelope and bounded timestamps, and returns fixed path-free pending
  or attention evidence including the handoff ID/expiry only on valid state. It
  must not write, repair, overwrite, delete, consume, extend, or execute the
  handoff, and must not read the database or backup artifact.
- `allowed_paths`: `src/operations/governance-restore-handoff-reader.ts`,
  `tests/unit/operations/governance-restore-handoff-reader.test.ts`, the two
  canonical Operations documents, and this checkpoint only.
- `protected_paths`: B23L publication semantics, B23A-K routes/coordinator/
  authority, backup artifacts, SQLite/retention/CLI/supervisor/service lifecycle,
  schema/migrations/dependencies, browser/QQ/Provider/private/live data,
  deployment, and every prior slice.
- `assertions`: valid reopen survives a fresh process and returns the same
  path-free identity/expiry evidence; expired, malformed, wrong-version,
  wrong-binding, symlink/file/permissive/wrong-owner directory or pending entry,
  multi-link, changed bytes, and owner exceptions return fixed evidence without
  repair or deletion; all database/backup/SQLite/audit/route/service state stays
  unchanged.
- `focused_commands`: the B23L baseline and named fail-first reopen regression;
  owner and B23L suites; SQLite/coordinator/Operations HTTP adjacency; canonical
  P8; typecheck, scoped/full ESLint, release gate, hygiene, and exact private
  reverse/forward proof.
- `schema_dependency_live_boundary`: HTTP-disabled local filesystem read only;
  no route, mutation, schema, dependency, database, artifact, service,
  supervisor, browser, live, or restore operation.
- `rollback_boundary`: private mode-`0700` baseline with B23L candidate bytes,
  absent new owner/test paths, and exact document hashes; deletion/recreation and
  both document restoration orders were proved without touching the workspace or
  prior proof.
- `exact_fail_first_action`: completed; the named reopen collection failed only
  on the absent reader module after the untouched B23L/SQLite baseline remained
  green.
- `fail_first_evidence`: zero tests were collected and module resolution failed
  for the required reader import; no production or canonical-document effect
  occurred.
- `final_evidence`: the reader suite passed 1 file / 23 tests and the combined
  B23L+B23M owners passed 47 / 47; seven-file Operations/HTTP adjacency passed
  122 / 122 and canonical P8 passed 7 files / 306 tests. Typecheck, scoped/full
  ESLint, `git diff --check`, and fresh `pnpm release:check` passed; release
  recorded 136 / 1 skipped files and 2782 / 10 skipped tests. The reader is
  read-only, reopens a fresh process, validates canonical path/ownership/mode/
  link/no-follow identity, the bounded exact envelope and live 15-minute window,
  and returns only fixed pending or attention evidence without exposing the
  internal reference/digest or changing any state.
- `rollback_evidence`: the B23L document baselines were Operations
  `b77736c49b1abbc016bb2ebb7b0208188d67eca1605530949157308d05e4501c` and
  Governance `e9b5b7f3048a0c01526b4ae52693e1a762991b64f47252381aac3316f9e8c196`.
  Candidate hashes in reader/test/Operations/governance order were
  `3e06f7a34baaafa10d4c20dfe80a08575f7a0d8347eca439d794451312abc74d`,
  `59a2d8b4aea6c2cf9cb156cfd40572685be430534e87e3c5ac8c93584b38c06a`,
  `3e15a3f5b9b8761fc36e52888e88972ba4f523dddb908bb62c31c0cef6d10ab4`, and
  `25d4dd05fcb673978fffc3da872cf85729bd62e1ceb53312ff286ce9655c53aa`.
  Independent reverse-first and forward-first proof restored all bytes/modes;
  the patch is `/tmp/lethebot-gov-ux-01b23m-roundtrip-cf720d/gov-ux-01b23m.patch`
  (SHA-256 `5b488dd7adcf3180dd641e9bfdaaac894b7bc77cf22d4910f667eb4bec94f842`,
  mode `0600`, size `32417`), with proof root/trees `0700`.
- `inventory_evidence`: 72 modified tracked paths plus 57 untracked files under
  `--untracked-files=all`, zero staged; the added files/docs are ASCII-only,
  sensitive-name hygiene is clean apart from the expected tracked
  `.env.example`, Pi dependency `0.80.2` is unchanged, and migration 007/008
  hashes remain `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b` /
  `95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`.
- `exact_next_action`: completed; reassessment selects `GOV-UX-01B23N`, wiring
  exact current-session restore confirmation to the B23L durable publisher while
  retaining the zero-restore/service-lifecycle boundary.

## Completed Slice: `GOV-UX-01B23N`

- `requirement`: `GOV-UX-01B23N` exact restore-confirmation to durable handoff
  publication wiring.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: B23K consumes and revalidates the current-session restore
  authority but still returns only the in-process B23I preview. B23L now owns
  private no-clobber publication and B23M independently reopens it, yet the
  confirmed route does not call that owner, so no stopped-service handoff is
  created.
- `selection_reason`: the publisher and reader are independently proved; wiring
  the existing confirmation callback is the smallest dependency-ordered slice.
  It preserves B23K authority/drift checks and adds no new route, lifecycle
  control, consumer, deletion, or restore execution.
- `implementation_contract`: after exact B23K authority consumption and one
  successful B23I recomputation/digest comparison, call only
  `prepareGovernanceRestoreHandoff` with the trusted configured database path,
  the exact internal `backupRef`, the current preview digest, contract version
  `1`, and the current governance clock. Return that owner receipt unchanged
  with HTTP `200` for both `pending` and fixed `attention_required` outcomes.
  Do not expose the reference, digest, database path, handoff path, or
  diagnostic; do not call the reader, database, backup artifact, SQLite/audit
  owner, restore/retention operation, or service/supervisor control.
- `allowed_paths`: `src/index.ts`,
  `tests/integration/governance-http-wiring.test.ts`, `docs/operations.md`,
  `docs/governance-ui.md`, and this checkpoint only.
- `protected_paths`: B23L/M owners and tests, B23A-K authority/preview/coordinator
  behavior, backup artifacts, SQLite/retention/CLI/supervisor/service lifecycle,
  schema/migrations/dependencies, browser/QQ/Provider/private/live data,
  deployment, and every prior slice.
- `assertions`: unauthenticated, malformed, unknown, cross-session, consumed,
  stale, and drifted confirmations call neither publisher nor reader and leave
  files, database/audit rows, artifacts, and lifecycle unchanged. A valid
  confirmation calls B23L exactly once after B23I equality, creates exactly one
  pending envelope bound to the configured database sibling, and returns only its
  fixed path-free receipt. Repeated confirmation cannot overwrite or republish;
  existing/raced publisher state remains attention evidence; B23K preview and
  all other routes stay byte/behavior stable; B23M can reopen the resulting
  envelope after a fresh process.
- `focused_commands`: baseline captured before edits at
  `/tmp/lethebot-gov-ux-01b23n-roundtrip-4pszh2` with candidate hashes
  `bf00721d3cbd2b17ed062a79c011a29d0574fb6ce53fe8d83d812e03388550dc`,
  `9be7f9e97c761a5e00a199e9460e96155d8ecc9873951d0b0ba052877640ce9d`,
  `3e15a3f5b9b8761fc36e52888e88972ba4f523dddb908bb62c31c0cef6d10ab4`, and
  `25d4dd05fcb673978fffc3da872cf85729bd62e1ceb53312ff286ce9655c53aa` in
  source/test/Operations/governance order. Add the named integration regression
  and run it fail-first before source/document edits; then run full wiring,
  Operations/HTTP adjacency, canonical P8, typecheck/lint, release gate,
  hygiene, and an exact private reverse/forward proof.
- `schema_dependency_live_boundary`: one existing HTTP callback and synthetic
  filesystem integration test only; no schema, dependency, database content,
  artifact bytes, route registry, CLI/QQ/browser, service, supervisor, live, or
  restore operation.
- `rollback_boundary`: preserve the four baseline paths and prove deletion/
  recreation plus reverse-first and forward-first restoration byte-for-byte and
  mode-for-mode without touching B23L/M proof artifacts or the workspace.
- `baseline_evidence`: the untouched governance HTTP-wiring baseline passed
  28 / 28 before the B23N regression; the four-path baseline tree matched the
  workspace byte-for-byte and mode-for-mode before source or canonical-document
  edits.
- `exact_fail_first_action`: completed before the B23N source or canonical-
  document edit.
- `fail_first_evidence`: the named integration regression reached the existing
  B23K confirmation branch and failed only on the pre-wiring B23I projection
  shape. Before that assertion it proved the publisher and reader were not
  called and that no handoff, database, audit, artifact, restore, retention, or
  lifecycle effect occurred.
- `final_evidence`: the focused B23N regression passed 1 / 1; full governance
  HTTP wiring passed 28 / 28; seven-file Operations/HTTP adjacency passed
  122 / 122; and the canonical P8 gate passed 7 files / 306 tests. Typecheck,
  scoped and full ESLint, `git diff --check`, and the fresh `pnpm release:check`
  passed; release recorded 136 / 1 skipped files and 2782 / 10 skipped tests.
  A current B23K confirmation calls B23L exactly once after B23I equality,
  returns its unchanged pending or attention receipt with `200`, and leaves
  repeated, stale, consumed, unsafe, and drifted cases without publication or
  retry. The resulting pending envelope reopens through B23M after a fresh
  process; no route, reader call, SQLite/audit write, artifact mutation,
  restore, retention, or service/supervisor effect was added.
- `rollback_evidence`: the exact four-path patch is
  `/tmp/lethebot-gov-ux-01b23n-roundtrip-BvWmvu/gov-ux-01b23n.patch`
  (SHA-256
  `6f21f382bcd3849194939ecb395e56e982aa1d434171020c7d31d3f17602f289`, mode
  `0600`, size `14049`). Independent reverse-first and forward-first cycles
  restored all four paths byte-for-byte and mode-for-mode; the proof root and
  every tree are `0700`, product snapshots are `0644`, and candidate/workspace
  equality passed. Candidate SHA-256 values in source/test/Operations/
  governance order are
  `50195648760be79be23a7a439d3ae9e6322a16fd8908641d130ecb0fbb1ae26a`,
  `d44c0f70b874b45af1edc38bd90dafd589cec295cf900754692fff050e4db6f8`,
  `8f2c15c65b6b8783bc8fb424e52224ce679f93be0c0941a57c34ba42157294ba`, and
  `3c46fdde491835cb8c90f3fc615f41e46cbe1fde29fdf403b1eb47c153652841`.
- `inventory_evidence`: the worktree remains 72 modified tracked paths plus
  57 untracked files under `--untracked-files=all`, with zero staged; added
  files and docs are ASCII-only, sensitive-name hygiene remains clean apart
  from the expected tracked `.env.example`, Pi dependency `0.80.2` and the
  recorded migration 007/008 hashes are unchanged.
- `exact_next_action`: completed; dependency reassessment selects
  `GOV-UX-01B23O`, the HTTP-disabled stopped-service handoff consumer/executor.
  Capture and verify its baseline before adding the fail-first regression or
  changing any source/canonical document.

## Completed Slice: `GOV-UX-01B23O`

- `requirement`: `GOV-UX-01B23O` stopped-service durable handoff consumer and
  executor.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: B23N publishes one private pending envelope and B23M can
  reopen it, but no independent owner consumes that envelope under a proven
  stopped-service boundary or delegates the exact verified backup to the
  existing atomic SQLite restore operation. B23M intentionally omits the
  internal reference and digest from its public receipt, so the consumer must
  not infer them or parse a second weaker schema.
- `selection_reason`: consuming the already-published envelope is the next
  dependency-ordered boundary. It must be proved as an HTTP-disabled owner
  before any CLI, supervisor, service lifecycle, retention, or browser surface
  is connected.
- `implementation_contract`: add one typed Operations owner that accepts only
  the trusted configured database location, an injected current time, and a
  typed stop-proof supplied by a future service adapter. Extract one shared
  internal B23M validation/claim primitive that returns the strict envelope
  only to trusted Operations code while leaving the public redacted reader DTO
  byte-stable. Require a live strict pending envelope, then re-resolve its exact
  internal backup reference through the B23I integrity owner and require exact
  digest/version/database-sibling equality plus absent target WAL/SHM sidecars.
  Claim the envelope with an operation-owned crash-recoverable state transition,
  then call the existing `restoreSqliteDatabase` only after the stop-proof is
  true; publish a fixed path-free completed or attention/recovery receipt and
  retain enough durable state for one bounded retry after an interrupted
  publication.
  A successful execution consumes the envelope exactly once; repeated, stale,
  drifted, unsafe, running-service, sidecar, integrity, foreign-key, or
  publication failures do not run a second restore and do not delete the source
  backup. No client path, session handle, credential, diagnostic, route, HTTP
  authority, audit row, in-process restore, or supervisor command is accepted.
- `allowed_paths`: a new
  `src/operations/governance-restore-handoff-consumer.ts` owner and its unit
  test, the smallest shared B23M validation/claim or SQLite-maintenance helper
  change required for the state transition, their focused tests, `docs/operations.md`,
  `docs/governance-ui.md`, and this checkpoint only.
- `protected_paths`: B23N HTTP wiring and all B23A-M routes, authority,
  publisher, reader, preview, and artifact semantics unless a narrowly typed
  state-transition helper is required; CLI grammar/output, supervisor and
  service lifecycle implementations, retention, schema/migrations,
  dependencies/lockfile, browser/QQ/Provider code, private/live data,
  deployment, restart, and every prior slice.
- `assertions`: no pending/expired/malformed/changed/unsafe envelope reaches
  restore; a running-service or missing stop-proof exits before any database or
  artifact call; digest, version, sibling, sidecar, integrity, and foreign-key
  drift leaves the pending evidence intact; a crash at claim, restore, or
  publication has one deterministic recovery result; one successful execution
  changes only the configured database through the existing atomic owner and
  records a fixed consumed receipt; repeated execution is a no-op conflict;
  the public B23M receipt stays redacted and byte-stable; backup bytes/modes,
  unrelated files, audit rows, and B23N HTTP behavior stay unchanged.
- `focused_commands`: baseline captured at
  `/tmp/lethebot-gov-ux-01b23o-baseline-Eo7lV1` with the eight existing owner/
  test/document hashes in publisher/reader/SQLite/owner-tests/reader-tests/
  SQLite-test/Operations/governance order; verify both future consumer paths
  are absent, then add and run one named fail-first consumer regression before
  source or canonical-document edits. Follow with the B23L/B23M/SQLite owner
  suites, synthetic stopped-service integration, Operations/HTTP adjacency,
  canonical P8, typecheck/lint, release, hygiene, and an exact private
  reverse-first/forward-first proof.
- `schema_dependency_live_boundary`: one HTTP-disabled local consumer over
  synthetic private envelopes and disposable SQLite files. No route, HTTP
  authority, CLI, supervisor, live service, deployment, schema, migration,
  dependency, browser, Provider, QQ, or real database operation is permitted.
- `rollback_boundary`: the mode-`0700` baseline root above contains only the
  eight existing files, all snapshot files are `0644`, and both future consumer
  paths were absent. Preserve that root and independently prove deletion/
  recreation plus reverse-first and forward-first restoration without touching
  the B23N proof or workspace.
- `baseline_evidence`: the baseline hashes are
  `a76f1b965d11b8f20c673bc77510473e47c9746a04bc384368c5f87e6041a46d`,
  `3e06f7a34baaafa10d4c20dfe80a08575f7a0d8347eca439d794451312abc74d`,
  `42bd5fd098eb1955905e283cb4fcb44983a5023bbe3cc3f21d6243ceb4fc57d1`,
  `87566186f45819509ab6a4e846944b477fd63290b3c212042007f1dcc157bc32`,
  `59a2d8b4aea6c2cf9cb156cfd40572685be430534e87e3c5ac8c93584b38c06a`,
  `0dd4c8b12cf34e1a798ee3d003c57cd7066f787a9351734c11ebdc868602edd5`,
  `8f2c15c65b6b8783bc8fb424e52224ce679f93be0c0941a57c34ba42157294ba`, and
  `3c46fdde491835cb8c90f3fc615f41e46cbe1fde29fdf403b1eb47c153652841`.
  The root and every tree are `0700`, every snapshot is `0644`, and absent
  consumer paths were verified before any B23O edit.
- `fail_first_evidence`: the named stopped-proof consumer regression collected
  zero tests and failed only because
  `src/operations/governance-restore-handoff-consumer.ts` was absent. The test
  fixture published a real B23L pending envelope, but no production source or
  canonical document had been changed and no restore owner was reached.
- `changed_paths`: added
  `src/operations/governance-restore-handoff-consumer.ts` and its unit test;
  changed only the B23M reader's strict internal parser export, the SQLite
  maintenance foreign-key counter export, Operations/governance documentation,
  and this checkpoint. The public B23M DTO and all B23N HTTP wiring stayed
  unchanged.
- `final_evidence`: the B23O consumer passed 17 / 17; B23L publisher passed
  24 / 24; B23M reader passed 23 / 23; SQLite maintenance passed 21 / 21; and
  their combined owner gate passed 4 files / 85 tests. Seven-file Operations/
  HTTP adjacency passed 124 / 124, the unchanged B23N governance wiring passed
  28 / 28, and the canonical P8 gate passed 7 files / 306 tests. Typecheck,
  full ESLint, build, release preflight, and `git diff --check` passed. The fresh
  `pnpm release:check` exited zero with 137 / 1 skipped files and 2799 / 10
  skipped tests.
- `behavior_evidence`: stop proof is rejected before filesystem, resolver,
  backup, or restore access; stale/malformed/foreign/unsafe pending state,
  digest/version/effect drift, WAL/SHM sidecars, corrupt/symlink/FK-invalid
  backups, and changed artifacts remain unclaimed. A migrated SQLite restore
  preserves the verified backup bytes and mode, integrity and foreign keys,
  unrelated files, and backup audit rows while removing target-only rows.
  Claim, attempt, and completion crash windows recover deterministically; the
  initial call plus one retry is the fixed restore-call bound, post-effect
  recovery does not repeat restore, and completed handoffs remain fixed
  `already_consumed` no-ops.
- `rollback_evidence`: the exact ten-path boundary patch is
  `/tmp/lethebot-gov-ux-01b23o-roundtrip-itUJqG/gov-ux-01b23o.patch`
  (SHA-256
  `501d9fbcacb22f2128d486d6a7e7d09d5efbcf014d6ca9d900e08d6a4a69581a`,
  mode `0600`, size `63949`). Independent reverse-first and forward-first
  cycles deleted/recreated both new paths and restored all bytes and `0644`
  product modes; the proof root and every tree are `0700`, the candidate tree
  equals the workspace, and the preserved baseline tree digest remained
  `50307155815c2baf5325823f7ebe8995b7f3e7a41df1f8cf30e5893d3f4fb847`.
  Candidate SHA-256 values in publisher/reader/consumer/SQLite/publisher-test/
  reader-test/consumer-test/SQLite-test/Operations/governance order are
  `a76f1b965d11b8f20c673bc77510473e47c9746a04bc384368c5f87e6041a46d`,
  `f01c7d6d99488fe6bb2de952733fc2136887893234ac4c0fa1f6be02f84d10a0`,
  `c51cc39d676d9f2edfe3fff8150a8ba96b6593a65c847890f237cbe7c68c8498`,
  `2198d7da1a3f6ff39503d97da719e2a64c5459831373d4b0bf71cbea7e889e73`,
  `87566186f45819509ab6a4e846944b477fd63290b3c212042007f1dcc157bc32`,
  `59a2d8b4aea6c2cf9cb156cfd40572685be430534e87e3c5ac8c93584b38c06a`,
  `e735c79fac20c286d3c229f820c560c199c92c12515402f6f9bca90ae3fc4b5b`,
  `0dd4c8b12cf34e1a798ee3d003c57cd7066f787a9351734c11ebdc868602edd5`,
  `90b61117c8b9645059ba65481b46c625f640534e16e3b75f28bc4e389c06c416`,
  and `b2eda1ba1eac32182e97b08194860da3a1c952423c9c71933a54cdfde84c8232`.
- `inventory_evidence`: the worktree remains 72 modified tracked paths plus 59
  untracked files under `--untracked-files=all`, with zero staged. B23O source,
  tests, and canonical-document additions are ASCII-only; Git-visible
  sensitive-name hygiene remains clean apart from the expected tracked
  `.env.example`. Pi dependencies remain `@earendil-works/pi-agent-core` and
  `@earendil-works/pi-ai` `0.80.2`; migration 007/008 hashes remain
  `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b` /
  `95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`.
- `exclusions`: no schema, migration, dependency, lockfile, CLI, HTTP route,
  supervisor, service lifecycle, retention, deployment, restart, live
  Provider/QQ, private database, or raw-chat behavior was added or exercised.
- `exact_next_action`: completed; dependency reassessment selects
  `GOV-UX-01B24A`, the behavior-preserving shared exact-group summary-policy
  status projection, before any summary-policy HTTP authority, mutation, or
  browser work.

## Completed Slice: `GOV-UX-01B24A`

- `requirement`: `GOV-UX-01B24A` shared exact-group summary-policy status
  projection and CLI delegation.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: the summary-policy repository and `GovernanceService` already
  own exact-group status and mutation for CLI/QQ, but the CLI status branch
  reads the raw policy directly and P8 has no fixed identifier-free shared read
  DTO. A future browser route therefore cannot inspect effective default-off
  state through the same bounded projection without exposing a raw group ID or
  adding query logic in HTTP.
- `selection_reason`: after the completed B23A-O Operations sequence, the P0-P9
  critical-path reassessment shifts to the earliest incomplete P8 operator task:
  exact-group summary-policy inspection. A read-only shared projection and
  behavior-preserving CLI delegation are smaller and earlier than scope-handle
  authority, preview/confirmation, retention, identity, or browser work.
- `implementation_contract`: add one typed
  `GovernanceQueryService.getGroupSummaryPolicyForScope(scope)` read. Accept
  only an exact group scope, query only its policy, and return a fixed DTO with
  effective enabled/disabled state, whether durable policy evidence exists,
  nullable finite generation/eligibility and created/updated dates, and no raw
  group, actor, audit, job, conversation, or platform identifier. A valid
  absent policy projects effective disabled state; non-group or malformed scope
  returns no result without querying. Delegate only CLI `memory-summary status`
  to this projection while preserving its exact output and leaving enable/
  disable on `GovernanceService` unchanged. Add no catalog, handle, route,
  preview, confirmation, mutation, audit, schema, dependency, or browser code.
- `allowed_paths`: `src/governance/query-service.ts`, its unit test,
  `src/cli/main.ts`, the existing CLI integration test, `docs/governance-ui.md`,
  and this checkpoint only.
- `protected_paths`: summary-policy repository and worker enforcement,
  `GovernanceService` mutation/QQ semantics, all HTTP/session/handle registries,
  every B23 owner and artifact, other CLI commands/output, schema/migrations,
  dependencies/lockfile, browser, Provider/QQ/live/private data, deployment,
  and every prior slice.
- `assertions`: exact stored enabled/disabled and absent effective-disabled
  states project without identifiers; non-group, malformed, and unsafe evidence
  returns no projection and performs no broad read; CLI status output remains
  byte-stable; enable/disable, job cancellation, generation, audit, QQ, HTTP,
  and retrieval behavior remain unchanged; no write occurs.
- `focused_commands`: capture and verify the five-path source/test/CLI/
  integration-test/governance-document baseline, add one named unit regression
  and run it fail-first before product or canonical-document edits, then run the
  full query-service and CLI suites, canonical P8 gate, typecheck/lint, release,
  hygiene, and an exact private reverse-first/forward-first proof.
- `schema_dependency_live_boundary`: read-only synthetic migrated SQLite and
  CLI subprocess fixtures only. No schema, migration, dependency, HTTP,
  browser, Provider, QQ, private/live data, deployment, or runtime change.
- `rollback_boundary`: preserve exact pre-B24A bytes and modes for the five
  product/test/document paths, then prove reverse-first and forward-first
  restoration without touching the B23O baseline/proof or workspace.
- `baseline_evidence`: the private baseline is
  `/tmp/lethebot-gov-ux-01b24a-baseline-d2gA2m`; root and trees are `0700`, all
  five snapshots are `0644`, and baseline/workspace bytes and modes matched.
  Hashes in query-source/query-test/CLI-source/CLI-integration-test/governance-
  document order are
  `744522222402a9a9a5066fba19f091a02cfb7f5c4215e263478800ea756019cd`,
  `92d351636461060d68e6a3f2f547159f3d81a65e5096249560073a8d7f385378`,
  `2022c5f174eb5c3da4c08c927410391b6c9391c75dc5778c1945e5178c359b55`,
  `6b9915f9a85b43c376485ce977564b4093baee2e3b89d441c3ee20ee7fb15613`,
  and `b2eda1ba1eac32182e97b08194860da3a1c952423c9c71933a54cdfde84c8232`.
  The untouched query-service suite passed 43 / 43 and the existing CLI
  `memory-summary` lifecycle regression passed 1 / 1 before the new test.
- `fail_first_evidence`: the named exact-group summary-policy projection test
  failed 1 / 1 only because
  `GovernanceQueryService.getGroupSummaryPolicyForScope` did not exist; the
  other 43 query-service tests were skipped. No product source or canonical
  document had changed, no status query reached SQLite, and no write occurred.
- `changed_paths`: changed only the shared query service, its unit test, CLI
  main status delegation, the canonical governance document, and this
  checkpoint. The CLI integration test was included in the verified rollback
  boundary but remained byte-identical.
- `final_evidence`: the named projection and existing CLI lifecycle regressions
  passed 2 / 2; the full query-service and spawned CLI owner gate passed 2 files
  / 173 tests; and the canonical P8 gate passed 7 files / 307 tests. Typecheck,
  scoped and full ESLint, build, release preflight, and `git diff --check`
  passed. The fresh `pnpm release:check` exited zero with 137 / 1 skipped files
  and 2800 / 10 skipped tests.
- `behavior_evidence`: stored enabled/disabled policies and absent effective
  default-off state return fixed identifier-free projections. Non-group,
  missing/non-string, blank, padded, control-bearing, overlong, and malformed
  durable evidence returns null without a broad read or write. CLI status keeps
  its exact state/changed/canceled-jobs output, while enable/disable, generation,
  pending-job cancellation, audit, QQ, HTTP, and worker enforcement remain on
  their prior owners. SQLite integrity and foreign keys stayed clean.
- `rollback_evidence`: the exact five-path patch is
  `/tmp/lethebot-gov-ux-01b24a-roundtrip-rhAFxF/gov-ux-01b24a.patch`
  (SHA-256
  `5ebe5dbd885af3e76df0ddbab65fa7656b6cc3ee0f59bd237e9f0c5c0fde29cd`,
  mode `0600`, size `11908`). Independent reverse-first and forward-first
  cycles restored every path byte-for-byte and mode-for-mode; root and trees
  are `0700`, snapshots are `0644`, candidate/workspace equality passed, and
  the preserved baseline tree digest remained
  `6fd3501e13c00feb87661f89bf520eb707caf598b8f440bbd74d0329a0c83042`.
  Candidate hashes in query-source/query-test/CLI-source/CLI-integration-test/
  governance-document order are
  `5e650e5cfb9eec00d29a484dc6521ce6093c91f28f38f73e952e7b582d1a7501`,
  `9165e06b369684d8b4feb51e39a12a90cf164e635d5288de9b8b4c6b445bd345`,
  `87eac2a141a2d9174c426fed1a9f6870174b95be5b571429fae0074c7b45da90`,
  `6b9915f9a85b43c376485ce977564b4093baee2e3b89d441c3ee20ee7fb15613`,
  and `1d186f8654b0337ccd29079822bb6c2e15f16aed05a43c826340a9798ca86e3b`.
- `inventory_evidence`: the worktree remains 72 modified tracked plus 59
  untracked files under `--untracked-files=all`, with zero staged. B24A
  additions are ASCII-only and Git-visible sensitive-name hygiene is clean. Pi
  dependencies remain `0.80.2`; migration 007/008 hashes remain
  `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b` /
  `95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`.
- `exclusions`: no HTTP/session/handle, mutation, repository, worker, schema,
  migration, dependency, browser, Provider/QQ/live/private data, deployment, or
  runtime behavior changed or ran.
- `exact_next_action`: completed; dependency reassessment selects
  `GOV-UX-01B24B`, the HTTP-disabled bounded exact-group summary-policy scope
  catalog, before any route, status authority, preview, confirmation, or write.

## Completed Slice: `GOV-UX-01B24B`

- `requirement`: `GOV-UX-01B24B` purpose-isolated exact-group summary-policy
  scope-handle catalog.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: B24A can project one trusted exact group, including an absent
  effective default-off policy, but no bounded owner discovers selectable groups
  or passes exact scopes to the existing handle issuer. A caller-selected raw
  group ID must not become the future HTTP authority boundary.
- `selection_reason`: a default-off-aware, identifier-free catalog is the next
  dependency before exposing B24A. Keeping catalog generation HTTP-disabled
  proves source selection, validation, ordering, bounds, and issuer isolation
  separately from session routing.
- `implementation_contract`: add only
  `GovernanceQueryService.listGroupSummaryPolicyScopeHandles(issueHandle)`.
  Discover distinct valid canonical `qq-group-<5..12 digits>` groups from the
  union of canonical group chat rows and durable summary-policy rows, so both
  observed default-off and policy-only groups are selectable. Require group chat
  `conversation_id = group_id`, order by newest durable evidence then group ID,
  probe 101 and return at most 100. Pass only `{ kind: 'group', groupId }` to the
  trusted purpose-specific issuer. Return only a domain-separated fingerprint,
  fixed `group` kind/label, opaque handle, numeric expiry, and truncation; copy
  no issuer extras, raw group/platform/name, policy state, row, or diagnostic.
  Add no route, registry wiring, status query, preview, confirmation, mutation,
  audit, schema, dependency, CLI, or browser change.
- `allowed_paths`: `src/governance/query-service.ts`, its unit test,
  `docs/governance-ui.md`, and this checkpoint only.
- `protected_paths`: B24A status/CLI behavior, summary-policy repository/service/
  workers, HTTP/session/scope registries and routes, other query projections, all
  B23 owners/artifacts, schema/migrations, dependencies/lockfile, browser,
  Provider/QQ/live/private data, deployment, and every prior slice.
- `assertions`: canonical chat-only default-off and policy-only groups are
  deduplicated and issued once in stable evidence order; malformed, private,
  mismatched-conversation, non-QQ, padded/control/overlong, and duplicate rows
  are excluded before issuing; the 101st valid group sets truncation without
  issuance; repeated reads are stable and write-free; results contain no raw
  group/name/platform/policy/issuer extras; B24A, CLI, HTTP, and mutations stay
  unchanged.
- `focused_commands`: capture and verify the three-path source/test/document
  baseline, run the query-service suite and canonical P8 baseline, then add one
  named fail-first catalog regression before product/document edits. Finish
  with full query-service and canonical P8 gates, typecheck/lint/release, hygiene,
  and exact private reverse-first/forward-first proof.
- `schema_dependency_live_boundary`: read-only synthetic migrated SQLite only;
  no schema, migration, dependency, HTTP, CLI, browser, Provider, QQ, live/
  private data, deployment, or runtime change.
- `rollback_boundary`: preserve exact pre-B24B bytes/modes for the three
  source/test/document paths and prove reverse-first/forward-first restoration
  without touching B24A or B23O proof artifacts or the workspace.
- `baseline_evidence`: the private baseline is
  `/tmp/lethebot-gov-ux-01b24b-baseline-VH4KZS`; its root and trees are mode
  `0700`, snapshots are mode `0644`, and source/test/document SHA-256 values are
  `5e650e5cfb9eec00d29a484dc6521ce6093c91f28f38f73e952e7b582d1a7501`,
  `9165e06b369684d8b4feb51e39a12a90cf164e635d5288de9b8b4c6b445bd345`,
  and `1d186f8654b0337ccd29079822bb6c2e15f16aed05a43c826340a9798ca86e3b`.
  Product source and the canonical document still matched those snapshots when
  the regression was introduced.
- `fail_first_evidence`: the named catalog regression collected 45 query-service
  tests, skipped 44 unrelated cases, and failed 1 / 1 only because
  `service.listGroupSummaryPolicyScopeHandles is not a function`. The fixture
  had already established chat-only default-off, policy-only, overlap, evidence
  ordering, malformed/private/mismatched/non-QQ exclusion, issuer-extra
  omission, session/purpose isolation, exact 100 and overflow 101 bounds,
  repeated stability, zero-write, integrity, and foreign-key assertions. Source
  and the canonical behavior document remained byte-identical to baseline.
- `changed_paths`: changed only the shared query service, its unit test, the
  canonical governance document, and this checkpoint.
- `final_evidence`: the named regression passed 1 / 1 with 44 unrelated cases
  skipped; the full query-service file passed 45 / 45; and the canonical P8 gate
  passed 7 files / 308 tests. Typecheck, scoped and full ESLint, build, release
  preflight, and `git diff --check` passed. The fresh `pnpm release:check` exited
  zero with 137 / 1 skipped files and 2801 / 10 skipped tests.
- `behavior_evidence`: distinct valid canonical chat and structurally valid
  policy evidence is
  merged before ordering and the fixed 101-row probe. Chat-only effective
  default-off, policy-only, overlap, duplicates, tied timestamps, private rows,
  mismatched conversation/group rows, non-text data, malformed policy state/
  generation/date evidence, non-QQ, zero-prefixed, padded, control-bearing,
  non-digit, short, and overlong scopes all exercised the expected inclusion or
  exclusion. Only 100 exact group scopes reach the issuer; the
  101st sets truncation. Fixed DTOs omit raw group, platform, name, policy,
  session, secret, and issuer-extra data; real registry resolution is restricted
  to the issuing session and read purpose. Repeated calls are stable and
  write-free, B24A still returns default-off for the chat-only scope, and SQLite
  integrity and foreign keys remain clean.
- `rollback_evidence`: the exact three-path patch is
  `/tmp/lethebot-gov-ux-01b24b-roundtrip-imGqdG/gov-ux-01b24b.patch`
  (SHA-256
  `f5f7e26495c5e6f381da88eb8e86a55b9a8327bc02ec8bcf2222fa3f3de7422a`,
  mode `0600`, size `18973`). Its root and all trees are mode `0700`; snapshots
  are mode `0644`. Independent reverse-first and forward-first cycles restored
  exact baseline and candidate bytes/modes, the candidate snapshot matches the
  workspace, and the preserved baseline tree digest remains
  `4874e4b581ffa1a61f634dfe68b7dd8b0fb9d2881a9830e9cbfd0df299f29fd4`.
  Candidate source/test/document hashes are
  `afc21100551de29400a298aedf0cf65f2e5556a500167d5d433dc0dfb5a3ee67`,
  `b8812f96d0fc56e7fc94fecafb888161d529f88058aa91bff9bb5f51bb9f1996`,
  and `04918322efd124215675ff116c8354abdf768b64ee2c1147c147820b6c4dba4b`.
- `inventory_evidence`: the worktree remains 72 modified tracked plus 59
  untracked paths, with zero staged. The exact patch is ASCII-only; Git-visible
  sensitive-artifact filename hygiene is clean. Pi dependencies remain `0.80.2`;
  migration 007/008 hashes remain
  `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b` /
  `95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`.
- `exclusions`: no HTTP/session registry wiring, status route, mutation,
  repository, worker, schema, migration, dependency, CLI, browser, Provider/QQ,
  live/private data, deployment, restart, or runtime behavior changed or ran.
- `exact_next_action`: completed; expose only this catalog through a separate
  authenticated-unscoped read-only route that binds its handles to the future
  exact-group summary-policy status purpose.

## Completed Slice: `GOV-UX-01B24C`

- `requirement`: `GOV-UX-01B24C` authenticated exact-group summary-policy scope
  discovery route.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: B24B owns the bounded identifier-free catalog and exact scope
  callback, but it is HTTP-disabled, so an authenticated governance session has
  no way to obtain status-read authority without supplying a raw group ID.
- `selection_reason`: route exposure is the direct dependency after the proved
  catalog and before the B24A status projection. Reusing the existing
  authenticated-unscoped route class and scope registry is the smallest
  reversible change; it introduces neither status reads nor mutations.
- `implementation_contract`: register only read-only
  `GET /governance/api/v1/group-summary/scopes` with discovery purpose
  `governance.group_summary_policy.scopes`. Reuse the shared authenticated-
  unscoped boundary so authentication, query rejection, scope-header rejection,
  fixed failures, session lifetime, and listener lifecycle remain unchanged.
  Call only B24B and bind each issuer input to the current session digest and
  expiry, purpose `governance.group_summary_policy.status.read`, and the exact
  group scope. Return B24B unchanged. Add no status route, query-service change,
  body, CSRF, resource/preview handle, mutation, schema, dependency, CLI, or
  browser behavior.
- `allowed_paths`: `src/index.ts`,
  `tests/integration/governance-http-wiring.test.ts`, `docs/architecture.md`,
  `docs/deployment.md`, `docs/governance-ui.md`, `docs/security-privacy.md`, and
  this checkpoint only.
- `protected_paths`: B24A/B24B query-service DTO, validation, order, bounds,
  fingerprints, and CLI behavior; `GovernanceHttpServer`; generic session/CSRF/
  scope registry behavior; every existing route and purpose; summary-policy
  repository/service/workers and all mutations; schema/migrations, dependencies/
  lockfile, browser, Provider/QQ/live/private data, deployment, and every prior
  slice.
- `assertions`: unauthenticated access is `401`; a query or scope header is
  `400`; all rejections precede B24B and issuance. A current session returns the
  unchanged fixed catalog, calls B24B once, and binds every exact scope only to
  that session, expiry, and status-read purpose. Cross-session and cross-purpose
  resolution fails; repeated discovery is byte-stable and write-free; raw group,
  platform, name, policy, session, secret, and issuer extras remain absent. All
  legacy catalogs/routes and B24A/CLI/mutations remain unchanged; the status
  route remains absent and the listener remains default-unreachable.
- `focused_commands`: capture a private six-path baseline; add one named
  fail-first integration regression and run it before source/docs edits; rerun
  the focused regression, full governance HTTP wiring plus query/service/HTTP/
  scope-registry adjacency, canonical P8 gate, typecheck/lint/release, hygiene,
  and exact six-path reverse/forward proof.
- `schema_dependency_live_boundary`: route wiring, synthetic migrated SQLite,
  integration tests, and canonical route docs only. No query-service,
  persistence write, status read, mutation, schema, migration, dependency, CLI,
  browser, Provider, QQ, private/live data, deployment, restart, or container
  operation is permitted.
- `rollback_boundary`: preserve exact pre-B24C bytes and modes for all six
  product/test/document paths and prove independent reverse-first and forward-
  first restoration without touching the B24B/B24A/B23O artifacts or workspace.
- `baseline_evidence`: the private baseline is
  `/tmp/lethebot-gov-ux-01b24c-baseline-V9Rk3W`; its root and trees are mode
  `0700`, snapshots are mode `0644`, and source/test/architecture/deployment/
  governance/security SHA-256 values are
  `50195648760be79be23a7a439d3ae9e6322a16fd8908641d130ecb0fbb1ae26a`,
  `d44c0f70b874b45af1edc38bd90dafd589cec295cf900754692fff050e4db6f8`,
  `0cd92f60a805a5c6d8692992485251196245f93e0c3143130a28ef35591ce197`,
  `52e89e5a848ae057cf14354223d159d2dbf7b5218e313bdb859c1189ac6064d0`,
  `04918322efd124215675ff116c8354abdf768b64ee2c1147c147820b6c4dba4b`,
  and `b3b16c8a2c0556c35b6ef4f13ab79f69c2946ad59ad748051b1c8c6185123ceb`.
- `fail_first_evidence`: the named discovery-route regression collected 29
  governance-wiring tests, skipped 28 unrelated cases, and failed 1 / 1 because
  unauthenticated `GET /governance/api/v1/group-summary/scopes` returned `404`
  instead of the registered-route `401`. B24B, the scope issuer, B24A, and the
  policy mutation owner remained uncalled; `src/index.ts` and every canonical
  route document still matched baseline.
- `changed_paths`: changed only `src/index.ts`, the governance HTTP wiring
  integration test, the four canonical architecture/deployment/governance/
  security documents, and this checkpoint. B24A/B24B, the generic HTTP and
  scope-registry owners, CLI/QQ behavior, and summary-policy mutation remained
  unchanged.
- `final_evidence`: the named final regression passed 1 / 1 with 28 unrelated
  cases skipped; full governance HTTP wiring passed 29 / 29; five-file query/
  service/HTTP/scope-registry adjacency passed 103 / 103; and the canonical P8
  gate passed 7 files / 308 tests. Typecheck, full ESLint, and
  `git diff --check` passed. The corrected fresh `pnpm release:check` exited
  zero with 137 / 1 skipped files and 2802 / 10 skipped tests. Two preceding
  release wrappers used `umask 077`, which converted the restore-handoff test's
  intended permissive `0755` fixture to private `0700`; the exact affected test
  passed at the normal process umask, and the unchanged full release gate then
  passed at normal `022`. The successful mode-`0600` log is
  `/tmp/lethebot-gov-ux-01b24c-release.log`; both failed wrapper-attempt logs
  remain separately preserved.
- `behavior_evidence`: unauthenticated discovery returns `401`, while a query or
  scope header returns `400` before B24B or issuance. A current session invokes
  only B24B and issues each exact group scope with that session digest/expiry
  and `governance.group_summary_policy.status.read`. Returned catalog entries
  retain only the fixed fingerprint/kind/label/opaque-handle/expiry fields; raw
  group, platform, name, policy, session, secret, and issuer extras remain
  absent. Cross-session and cross-purpose resolution fails, repeated discovery
  is byte-stable and write-free, B24A and the policy mutation owner remain
  uncalled, the status and mutation routes remain `404`, and SQLite integrity
  and foreign keys stay clean.
- `rollback_evidence`: the exact six-path patch is
  `/tmp/lethebot-gov-ux-01b24c-roundtrip-K9gu3b/gov-ux-01b24c.patch`
  (SHA-256
  `21d498b124544d2ec3a7b1a6f8c2da235e6bc8fea99cc86a5a10562eca21c357`,
  mode `0600`, size `20994`). Its root and all snapshot trees are mode `0700`;
  product snapshots are mode `0644`. Independent candidate-to-baseline-to-
  candidate and baseline-to-candidate-to-baseline cycles restored every path
  byte-for-byte and mode-for-mode, and the candidate snapshot matches the
  workspace. The preserved baseline tree digest is
  `42cff3ca80f7b6a7426e3f22cfbba321294cf7da7483747c758610996b876930`;
  candidate source/test/architecture/deployment/governance/security hashes are
  `3d286f80d2ba76d0aa2c99804cb8658db23c6817b0d48af193f655ee73de8bcb`,
  `2f908f771100684619bfc4c8e97e0aaa92d32ed203104bf1186e017096f3480f`,
  `f92b729eba079b9b6c037457c24332373387a49bc825323270abbb58f471e682`,
  `a294929fb4fa61fc795f9cb52983f745e34a730a9a454806b252ae0615a39ea4`,
  `3da90522919fc9cf487567d3c4b8279b17e783ecca32b0927d11f5de6e13b48b`,
  and `5b5d11b6bef090d6c90151eb132c42731db51b3fbc7894fdf49ca0ff791415ae`.
- `inventory_evidence`: the worktree remains 72 modified tracked plus 59
  untracked paths with zero staged. The patch contains exactly the six allowed
  product/test paths, is ASCII-only, and has no NUL bytes. Git-visible generated
  secret/token/credential/database/log/temp/reject/original artifact filenames
  are absent; the three broad `sqlite` substring matches are canonical source,
  test, and schema-document names. Pi dependencies remain `0.80.2`; migration
  007/008 hashes remain
  `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b` /
  `95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`.
- `exclusions`: no B24A/B24B query or CLI behavior, generic HTTP/session/scope-
  registry semantics, summary-policy write, repository, worker, schema,
  migration, dependency, browser, Provider/QQ, private/live data, deployment,
  restart, or container behavior changed or ran.
- `exact_next_action`: completed; dependency reassessment selects
  `GOV-UX-01B24D`, the exact-scope read-only B24A status route. Capture its
  six-path baseline, add one named fail-first route regression before source or
  canonical-document edits, and keep every summary-policy mutation absent.

## Completed Slice: `GOV-UX-01B24D`

- `requirement`: `GOV-UX-01B24D` exact-scope read-only group-summary-policy
  status route.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: B24C now issues current-session, purpose-bound exact-group
  handles and B24A owns the fixed identifier-free effective-policy projection,
  but `GET /governance/api/v1/group-summary/policy` remains absent. The complete
  read workflow therefore cannot resolve one selected group without exposing a
  raw group ID or duplicating policy query logic in HTTP.
- `selection_reason`: discovery and its safe status projection are independently
  proved. Registering one existing static exact-scope read route is the smallest
  dependency-ordered continuation and adds no preview, confirmation, mutation,
  repository, or generic HTTP behavior.
- `implementation_contract`: register only read-only
  `GET /governance/api/v1/group-summary/policy` in the existing exact-scope route
  class with purpose `governance.group_summary_policy.status.read`. Let the
  shared boundary authenticate, reject queries, require and resolve exactly one
  opaque handle against the current session and purpose, and pass only the
  resolved scope to the callback. Call only B24A
  `getGroupSummaryPolicyForScope(scope)` and return its fixed projection; map a
  defensive null result to the fixed `404` response. Accept no raw selector or
  body. Register no POST, preview, confirmation, mutation, audit, or resource
  handle.
- `allowed_paths`: `src/index.ts`,
  `tests/integration/governance-http-wiring.test.ts`, `docs/architecture.md`,
  `docs/deployment.md`, `docs/governance-ui.md`, `docs/security-privacy.md`, and
  this checkpoint only.
- `protected_paths`: B24A/B24B/B24C query, catalog, DTO, validation, ordering,
  bounds, fingerprint, issuer, route, handle, and CLI semantics;
  `GovernanceHttpServer`; `GovernanceScopeHandleRegistry`; generic session/
  CSRF/scope/resource/preview dispatch; summary-policy repository/service/
  worker enforcement and every mutation; other routes; config/schema/
  migrations/dependencies/lockfile; browser/QQ/Provider code, private/live data,
  deployment, restart, container behavior, and every prior slice.
- `assertions`: unauthenticated access is `401`; a query and missing or malformed
  scope header are `400`; unknown, cross-session, or cross-purpose authority is
  `404`; all rejection paths precede B24A. A B24C handle returns `200`, passes
  only its resolved exact group scope to B24A, and returns the unchanged fixed
  effective state, stored flag, nullable generation/eligibility, and ISO dates.
  Chat-only policy absence remains effective disabled with null evidence; stored
  enabled/disabled evidence remains exact. The response omits raw group, actor,
  audit, job, conversation, platform, session, secret, and unrestricted fields.
  Repeated reads are byte-stable and write-free; integrity/FKs stay clean; B24C
  discovery and CLI status remain exact; POST at the same path stays `404`; the
  policy mutation owner remains uncalled; and the listener remains default-
  unreachable.
- `focused_commands`: capture and verify the six-path B24C candidate baseline;
  add one named fail-first integration regression and run it before source/docs
  edits; rerun the focused regression, full governance HTTP wiring plus query/
  service/HTTP/scope-registry adjacency, canonical P8 gate, typecheck/lint/
  release, hygiene, and exact six-path reverse/forward proof.
- `schema_dependency_live_boundary`: read-only route wiring, synthetic migrated
  SQLite, integration tests, and canonical route docs only. No query-service or
  persistence write, summary-policy mutation, schema, migration, dependency,
  CLI, browser, Provider, QQ, private/live data, deployment, restart, or
  container operation is permitted.
- `rollback_boundary`: preserve exact pre-B24D bytes and modes for all six
  product/test/document paths and prove independent reverse-first and forward-
  first restoration without touching the B24C/B24B/B24A/B23O artifacts or the
  workspace.
- `baseline_evidence`: the private baseline is
  `/tmp/lethebot-gov-ux-01b24d-baseline-RwgwmF`; its root and every tree are
  mode `0700`, snapshots are mode `0644`, and source/test/architecture/
  deployment/governance/security SHA-256 values are
  `3d286f80d2ba76d0aa2c99804cb8658db23c6817b0d48af193f655ee73de8bcb`,
  `2f908f771100684619bfc4c8e97e0aaa92d32ed203104bf1186e017096f3480f`,
  `f92b729eba079b9b6c037457c24332373387a49bc825323270abbb58f471e682`,
  `a294929fb4fa61fc795f9cb52983f745e34a730a9a454806b252ae0615a39ea4`,
  `3da90522919fc9cf487567d3c4b8279b17e783ecca32b0927d11f5de6e13b48b`,
  and `5b5d11b6bef090d6c90151eb132c42731db51b3fbc7894fdf49ca0ff791415ae`.
  Baseline/workspace bytes and modes matched on all six paths, inventory stayed
  72 modified tracked plus 59 untracked with zero staged, and no B24D source,
  test, or canonical-document edit existed.
- `fail_first_evidence`: the named status-route regression collected 30
  governance-wiring tests, skipped 29 unrelated cases, and failed 1 / 1 because
  unauthenticated `GET /governance/api/v1/group-summary/policy` returned `404`
  instead of the registered-route `401`. B24A, the scope resolver, and the
  summary-policy mutation owner remained uncalled; `src/index.ts` and all four
  canonical route documents still matched the verified baseline.
- `final_evidence`: the named B24D regression passed 1 / 1 with 29 skipped,
  full governance HTTP wiring passed 30 / 30, corrected five-file query/service/
  HTTP/scope-registry/wiring adjacency passed 104 / 104, and the canonical P8
  gate passed 7 files / 308 tests. `pnpm typecheck`, `pnpm lint`, and
  `git diff --check` passed. A fresh normal-umask `pnpm release:check` passed
  137 / 1 skipped files with 2803 / 10 skipped tests; its private log is
  `/tmp/lethebot-gov-ux-01b24d-release.log` at mode `0600`. The registered route
  resolves only a current-session
  `governance.group_summary_policy.status.read` handle, delegates only to B24A,
  preserves stored-enabled, stored-disabled, and effective-default-off
  projections, maps defensive null to fixed `404`, and remains identifier-free,
  byte-stable, write-free, integrity-clean, and FK-clean. POST and every policy
  mutation remain absent. The exact six-path patch is
  `/tmp/lethebot-gov-ux-01b24d-roundtrip-SVf66k/gov-ux-01b24d.patch`
  (`sha256:9534bc01e901741818ef2b07b271c8f3386463f0bca02a08dd0b99a9c188a9c7`,
  mode `0600`, size `22377`); independent candidate-to-baseline-to-candidate and
  baseline-to-candidate-to-baseline cycles passed with mode-`0700` trees,
  mode-`0644` snapshots, and candidate/workspace equality. Inventory remains
  72 modified tracked plus 59 untracked with zero staged; the patch has exactly
  six ASCII-only paths, no NUL/reject/original artifact, and clean filenames.
  Pi packages remain `0.80.2`; migrations 007/008 retain their recorded hashes.
  No summary-policy owner/repository/worker behavior, generic HTTP/registry
  behavior, CLI/QQ, schema, dependency, browser, live, or prior-slice behavior
  changed.
- `exact_next_action`: completed; the B24C/B24D discovery-and-read pair
  reassessment selects `GOV-UX-01B24E`, an HTTP-disabled write-free exact-group
  policy-change preview. Prove a fixed current/target/semantic-rollback
  projection and domain-separated digest before adding any expected-snapshot
  owner, mutation-purpose handle, route, or durable effect.

## Completed Slice: `GOV-UX-01B24E`

- `requirement`: `GOV-UX-01B24E` HTTP-disabled exact-group summary-policy change
  preview projection.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: B24D completes the safe read workflow, while the existing
  `setGroupSummaryPolicyAsLocalAdmin` owner mutates immediately from a raw group
  ID. No fixed preview captures the current stored/default-off state, intended
  target, next generation, enforcement consequences, and semantic rollback
  target for a future current-session confirmation boundary.
- `selection_reason`: a read-only projection over B24A is the smallest
  dependency-ready continuation. It makes no-op, malformed durable evidence,
  version overflow, response privacy, and digest stability explicit before any
  expected-snapshot owner or HTTP mutation authority exists.
- `implementation_contract`: add only exported action
  `GROUP_SUMMARY_POLICY_CHANGE_ACTION`, fixed
  `GroupSummaryPolicyChangePreviewProjection`, and
  `GovernanceQueryService.getGroupSummaryPolicyChangePreviewForScope({ scope,
  targetState })`. Accept only target `enabled` or `disabled`; call only B24A;
  return null for invalid scope/evidence, a no-op target, or a stored generation
  that cannot advance safely. Otherwise return a fixed identifier-free
  current/target/next-generation/effect/rollback projection and a SHA-256 digest
  under a new versioned domain. Register no route, handle, preview authority,
  owner call, mutation, or audit.
- `allowed_paths`: `src/governance/query-service.ts`,
  `tests/unit/governance/query-service.test.ts`, `docs/governance-ui.md`, and this
  checkpoint only.
- `protected_paths`: B24A/B24B/B24C/B24D behavior; all `GovernanceService`,
  summary-policy repository/job/worker/CLI/QQ mutation and enforcement behavior;
  all registries and HTTP/session/CSRF dispatch; config/schema/migrations/
  dependencies/lockfile; browser, Provider/QQ live, private data, deployment,
  restart, container behavior, and every prior slice.
- `assertions`: invalid target returns null before B24A; B24A null, same-state
  target, and `Number.MAX_SAFE_INTEGER` stored generation return null. Effective
  default-off to enabled has current disabled/stored false/null version and next
  generation 1. Stored disabled-to-enabled and enabled-to-disabled preserve the
  exact current version and advance generation by one. Enabling states no
  backfill; disabling states immediate generation/retrieval blocking plus
  pending-job cancellation; both state the existing policy/audit durable effects
  and a separate-confirmation semantic rollback to the prior state. The digest
  is stable, independently recomputable, target/state/version-bound, and domain-
  separated. Output omits raw group, actor, audit/job IDs, session, platform,
  secret, and unrestricted fields. Repeated previews are byte-stable and write-
  free with clean integrity/FKs. No HTTP path, handle, mutation, or audit appears.
- `focused_commands`: named fail-first and final query-service regression; full
  query-service and query/service/repository adjacency; canonical P8; typecheck/
  lint/release; hygiene; and exact three-path reverse/forward proof.
- `schema_dependency_live_boundary`: shared read projection, synthetic migrated
  SQLite, tests, and canonical governance contract only. No persistence write,
  owner/repository/job behavior, HTTP/registry/session behavior, schema,
  migration, dependency, CLI/QQ, browser, live, private-data, deployment,
  restart, or container operation is permitted.
- `rollback_boundary`: exact B24D bytes and `0644` modes for the three allowed
  product/test/document paths are captured under mode-`0700`
  `/tmp/lethebot-gov-ux-01b24e-baseline-mQpRC8`; the final patch must prove both
  complete cycles without touching the workspace or any B24D artifact.
- `baseline_evidence`: source/test/governance SHA-256 values are
  `afc21100551de29400a298aedf0cf65f2e5556a500167d5d433dc0dfb5a3ee67`,
  `b8812f96d0fc56e7fc94fecafb888161d529f88058aa91bff9bb5f51bb9f1996`, and
  `74b94d6f45b665b6a346d0a73e04169101090a827c0cf88a496e400b160694c7`; all
  match the workspace byte-for-byte and inventory is 72/59/0.
- `fail_first_evidence`: the named query-service run collected 46 tests,
  skipped 45 unrelated cases, and failed 1 / 1 only because
  `getGroupSummaryPolicyChangePreviewForScope` was absent. Production source and
  the canonical behavior contract still matched the verified baseline; no HTTP,
  registry, owner, repository, audit, or durable effect existed.
- `final_evidence`: the named final regression passed 1 / 1 with 45 unrelated
  cases skipped, full query-service coverage passed 46 / 46, query/service/
  summary-policy-repository adjacency passed 68 / 68, and the canonical P8 gate
  passed 7 files / 309 tests. `pnpm typecheck`, `pnpm lint`, and
  `git diff --check` passed. A fresh normal-umask `pnpm release:check` passed
  137 / 1 skipped files with 2804 / 10 skipped tests; its private mode-`0600`
  log is `/tmp/lethebot-gov-ux-01b24e-release.log`. The fixed preview handles
  implicit default-off, stored disabled, and stored enabled evidence; rejects
  invalid, no-op, or exhausted-generation evidence; advances the intended
  generation; distinguishes enable no-backfill from disable enforcement and
  pending-job cancellation; and remains identifier-free, digest-bound, stable,
  write-free, integrity-clean, and FK-clean. The exact three-path patch is
  `/tmp/lethebot-gov-ux-01b24e-roundtrip-u2EOJG/gov-ux-01b24e.patch`
  (`sha256:4ed674cf08068f1c90f0559a5b49a009cdb913b2309a2ea6487e92a8789c2219`,
  mode `0600`, size `15948`). Independent reverse-first and forward-first
  cycles restored baseline and candidate bytes/modes with mode-`0700` trees,
  mode-`0644` snapshots, and candidate/workspace equality. The patch contains
  exactly the three allowed ASCII-only paths with no NUL, reject, original, or
  sensitive-filename artifact. Inventory remains 72 modified tracked plus 59
  untracked with zero staged; Pi packages remain `0.80.2`, and migrations 007/
  008 retain their recorded hashes. No service, repository, job, CLI/QQ, HTTP,
  registry, schema, dependency, browser, live, or prior-slice behavior changed.
- `exact_next_action`: completed; dependency reassessment selects
  `GOV-UX-01B24F`, the HTTP-disabled atomic expected-snapshot overload for the
  shared group-summary-policy mutation. Preserve legacy CLI/QQ behavior while
  comparing implicit-default or exact stored state/generation/update-time
  evidence inside the transition/cancellation/audit transaction.

## Completed Slice: `GOV-UX-01B24F`

- `requirement`: `GOV-UX-01B24F` HTTP-disabled atomic expected-snapshot exact-
  group summary-policy mutation.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: B24E binds a prospective change to effective state, stored
  presence, generation, update time, and target, but the existing shared local-
  admin mutation accepts only a raw group ID and target boolean. A future
  confirmation handler could therefore recompute a fresh preview and still lose
  a race before the policy upsert, pending-job terminalization, and audit append.
- `selection_reason`: the repository already owns all three effects in one
  immediate transaction. Adding one compare-and-set overload there and exposing
  it only through the existing service owner is the smallest dependency-ready
  continuation; preview authority, HTTP consumption, and result projection stay
  separate.
- `implementation_contract`: preserve the legacy service call and repository
  `setEnabled` input, result, validation, authority, timestamp, generation,
  cancellation, audit, and error behavior. Add one discriminated expected-
  version overload to both owners. The expected call accepts one canonical group
  ID, target boolean, opposite expected state, bounded reason code, optional safe
  clock, and either exact implicit-default evidence with null generation/update
  time or exact stored-policy evidence with positive safe generation and update
  time. Invalid service/repository evidence is fixed `stale` and write-free. In
  one `BEGIN IMMEDIATE` transaction the repository distinguishes absence from a
  stored disabled row, validates the durable row, compares state/generation/
  update time exactly, advances generation and the existing logical time fence,
  applies the existing upsert, terminalizes only matching pending summary jobs
  on disable, and appends the existing redacted audit with the bounded reason
  code. Success returns fixed `updated` evidence; drift, no-op, unsafe transition
  time, or exhausted generation returns `stale`. Register no route or handle.
- `allowed_paths`: `src/governance/service.ts`,
  `src/storage/group-summary-policy-repository.ts`,
  `tests/unit/governance/service.test.ts`,
  `tests/unit/storage/group-summary-policy-repository.test.ts`,
  `docs/governance-ui.md`, and this checkpoint only.
- `protected_paths`: B24A-E query projections, digest, catalog, route, and scope-
  handle behavior; all generic HTTP/session/CSRF/resource/preview registry code;
  existing QQ and CLI grammar/output/authority and legacy service/repository
  behavior; worker execution fencing outside the existing cancellation effect;
  schema, migrations, dependencies, lockfile, browser, Provider/QQ live, private
  data, deployment, restart, container behavior, and every prior slice.
- `assertions`: implicit default-off enables only while the row remains absent;
  a stored disabled row is never equivalent to absence. Exact stored disabled-
  to-enabled and enabled-to-disabled transitions bind state, generation, and
  update time and advance generation once. Stale retry, concurrent winner/loser,
  row appearance/disappearance, wrong state/generation/time/source, malformed
  ID/version/reason/clock, no-op target, exhausted generation, and an unsafe
  logical time fence are repeatably write-free. Enable preserves no-backfill and
  exact-group ingress fencing. Disable immediately blocks generation/retrieval,
  terminalizes only exact-group pending summary jobs, and leaves running,
  completed, failed, other-group, and unrelated jobs exact. Success writes one
  policy transition and one redacted audit atomically with the reason code; an
  audit trigger failure rolls back policy and cancellation. State, generation,
  timestamp, audit, enforcement, and cancellation persist after database reopen;
  integrity is `ok` and foreign-key checks are empty. Legacy service, CLI, QQ,
  and repository calls retain exact results/effects/errors; B24E and every HTTP
  path remain write-free/absent.
- `focused_commands`: add and run one named fail-first expected-snapshot
  repository regression plus one named service-owner regression before product
  or canonical-document edits; rerun both named tests and full repository/
  service files; run query/service/repository plus CLI/QQ adjacency, the canonical
  P8 gate, typecheck, lint, release, hygiene, migration/dependency checks, and an
  exact five-path reverse-first/forward-first rollback proof.
- `schema_dependency_live_boundary`: existing policy/service owners, synthetic
  migrated SQLite, tests, and the canonical governance contract only. No query,
  route, registry, schema, migration, dependency, lockfile, browser, Provider,
  QQ, private-data, deployment, restart, container, or live operation is
  permitted.
- `rollback_boundary`: the exact B24E candidate for all five product/test paths
  is captured under mode-`0700`
  `/tmp/lethebot-gov-ux-01b24f-baseline-NqhbB6`; all directories are mode `0700`
  and files are mode `0644`. Service/repository/service-test/repository-test/
  governance-doc SHA-256 values are
  `aedcc14c30faa05fae8e1f0c722beca76c317109513c072a2f0df698792c5a12`,
  `5abe2675e29fc15274d7734f643fdd2aa525f4f963b1788f00f75172dc27c891`,
  `824079a218a063e658fbba49ffb94f56017bad9e6d290cf44ef816de0196eedc`,
  `8665a13b628e186c44a81cb73ce7e0a997bf2f0625bad44c9c792c7c787bfc9a`, and
  `09f967bce3f49eff1ac08ea95273803406d14480ba7590290fd7f287da3e2eb5`.
  Each snapshot matches the workspace. Inventory is 72/59/0.
- `fail_first_evidence`: the two named regressions collected 24 cases across the
  repository and service files, skipped 22 unrelated cases, and failed 2 / 2 at
  their first result assertions. Both legacy owners performed an immediate
  transition and returned the legacy `changed`/policy shape instead of the
  expected-snapshot `updated` result, proving that neither overload exists and
  that extra expected evidence is currently ignored. The private fail-first log
  is `/tmp/lethebot-gov-ux-01b24f-fail-first.log` at mode `0600`. Both production
  owners and the canonical governance contract still match their captured B24E
  baseline hashes exactly; only the two allowed test files and this checkpoint
  changed.
- `final_evidence`: the refreshed named regressions passed 2 / 2 with 22
  unrelated cases skipped, both complete owner files passed 24 / 24, the five-
  file query/service/repository/CLI adjacency passed 251 / 251, and the canonical
  P8 gate passed 7 files / 310 tests. `pnpm typecheck`, `pnpm lint`, and
  `git diff --check` passed. Fresh normal-umask `pnpm release:check` passed 137 /
  1 skipped files with 2806 / 10 skipped tests; its mode-`0600` log is
  `/tmp/lethebot-gov-ux-01b24f-final-release.log`. The expected overload handles
  implicit absence, exact stored disabled/enabled transitions, generation and
  logical-time fencing, enable ingress/no-backfill behavior, disable enforcement
  and exact pending-job cancellation, stale/disappeared/exhausted evidence, and
  audit-trigger rollback in one immediate transaction. A second SQLite
  connection retrying the original implicit snapshot loses with `stale` and
  leaves exactly one transition audit. Reopen persistence, integrity `ok`, and
  empty foreign-key checks pass; every legacy repository/service call retains
  its prior result and effects.
- `rollback_evidence`: the exact five-path patch is
  `/tmp/lethebot-gov-ux-01b24f-roundtrip-h8PL67/gov-ux-01b24f.patch`
  (`sha256:e1965a2c36fef86cc5d22950813810aee80f77edd768bf07f91e405252ac66fb`,
  mode `0600`, size `38664`). Independent reverse-first candidate-to-baseline-
  to-candidate and forward-first baseline-to-candidate-to-baseline cycles passed
  in mode-`0700` trees with mode-`0644` files, exact manifests, and final
  candidate/workspace equality. The patch contains exactly the five allowed
  ASCII-only paths with no NUL, reject, or original artifact. The first proof-
  harness run exposed only that restrictive umask `077` made a recreated file
  mode `0600`; the corrected harness used normal apply umask `022` while keeping
  private evidence mode-restricted and passed without touching the workspace.
  Final inventory is 74 modified tracked plus 59 untracked paths with zero
  staged: the repository owner and its test were clean at the captured B24E
  baseline and are the two newly recognized tracked modifications. Pi packages
  remain `0.80.2`; migrations 007/008 retain SHA-256
  `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`/
  `95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`.
- `exact_next_action`: completed; dependency reassessment selects
  `GOV-UX-01B24G`, exposing only B24E through the existing exact-scope HTTP and
  preview-authority owners while keeping confirmation, preview consumption, and
  B24F invocation absent.

## Completed Slice: `GOV-UX-01B24G`

- `requirement`: `GOV-UX-01B24G` exact-group summary-policy change-preview route
  and current-session authority issuance.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: B24D exposes fixed exact-group status, B24E owns the bounded
  write-free target-state preview, and B24F owns atomic expected-snapshot
  mutation, but the HTTP boundary has no scoped POST that can request B24E or
  bind its state/version/digest to one current session. The existing POST at the
  policy path is therefore still `404` and no summary-policy preview authority
  exists.
- `selection_reason`: after reassessing the B24E/B24F pair and the wider P8
  critical path, route wiring is the smallest dependency-ordered continuation.
  It reuses the reviewed session, Origin/CSRF, scope, query, and preview-registry
  owners and leaves confirmation plus durable mutation for the next independent
  slice.
- `implementation_contract`: register only mutation-marked
  `POST /governance/api/v1/group-summary/policy` beside the unchanged GET, using
  purpose `governance.group_summary_policy.status.read`. Let the shared boundary
  authenticate, reject queries, require exact Origin/CSRF and JSON, enforce the
  configured body bounds, and resolve one current-session exact-group scope
  handle. Accept only exact body `{ action: 'change', targetState }` where the
  target is `enabled` or `disabled`, then call only B24E with that scope and
  target. Conceal a null projection as fixed `404`. For a projection, issue one
  current-session binding with action `group.summary_policy.change`, resource
  kind `group_summary_policy`, fixed resource ID `policy`, exact scope, current
  state, positive current-generation-or-default revision, and preview digest.
  Return `201` with the unchanged B24E projection plus only a fresh opaque handle
  and expiry. Add no confirmation, authority consumption, service/repository
  call, policy/job/audit write, or enforcement change.
- `allowed_paths`: `src/index.ts`,
  `tests/integration/governance-http-wiring.test.ts`, `docs/architecture.md`,
  `docs/deployment.md`, `docs/governance-ui.md`, `docs/security-privacy.md`, and
  this checkpoint only.
- `protected_paths`: B24A-F query/service/repository implementations, DTOs,
  digest, expected-snapshot semantics, tests, and legacy CLI/QQ behavior;
  `GovernanceHttpServer`, scope/preview registry implementations, generic
  session/Origin/CSRF/body/exact-scope dispatch; every Memory, maintenance,
  Privacy, Explain, Activity, Operations, backup/restore, retention, and identity
  route; config, schema, migrations, dependencies, lockfile, browser, Provider,
  QQ live, private data, deployment, restart, container behavior, and every
  prior slice.
- `assertions`: unauthenticated POST is `401`. Query, wrong/missing Origin or
  CSRF, missing/non-JSON/invalid/non-exact/oversized body, missing/malformed
  scope, and unknown, cross-session, or cross-purpose scope authority fail
  before B24E or preview issuance. A no-op, invalid, or exhausted projection is
  concealed as `404` with no authority. Valid stored and implicit-default
  transitions call B24E once with only the resolved exact group scope and fixed
  target, return its projection unchanged plus fresh authority, and bind exactly
  the action/resource/scope/current-state/positive-generation-or-default/digest
  tuple. Repeated previews have stable digests and distinct handles but produce
  no policy, job, audit, or other durable change; GET/discovery remain byte-
  compatible; unrelated purposes cannot resolve or consume the authority;
  confirmation, B24F, CLI, and QQ remain absent/unchanged; integrity/FKs stay
  clean; and the listener remains default-unreachable.
- `focused_commands`: add and run only the named B24G wiring regression fail-
  first, then final; run full governance HTTP wiring; query/service/HTTP/scope/
  preview-registry adjacency; the canonical P8 command; `pnpm typecheck`;
  scoped and full lint; `pnpm release:check`; `git diff --check`; migration/
  dependency/inventory/privacy hygiene; and an exact six-path private reverse-
  first/forward-first rollback proof.
- `schema_dependency_live_boundary`: route wiring, synthetic integration
  coverage, and canonical route/security documentation only. No query/service/
  repository or registry implementation, preview consumption, confirmation,
  durable mutation, schema, migration, dependency, lockfile, CLI/QQ, browser,
  Provider, private DB, deployment, restart, container, or live operation is
  permitted.
- `rollback_boundary`: the exact B24F candidate for all six product/test paths
  is captured under mode-`0700`
  `/tmp/lethebot-gov-ux-01b24g-baseline-7rJMGr`; every directory is mode `0700`
  and each file is mode `0644`. Index, wiring-test, architecture, deployment,
  governance-UI, and security/privacy SHA-256 values are
  `1f124436219328dc5ad1834391f637e2730a11a11c0315b1bf2fc11abb12fe68`,
  `9bee177e076d556d3211c537c4518147a774aad7742affa68c7c21a5b2f14bfa`,
  `60ada90e5126d33b08e7a5c7da65b032d43fca57420bb93a6fd325aed232094a`,
  `f50c02fca5633aa0c504be20bc107fa92768559b6045e230ada274be59673b11`,
  `c6d9f07db04d2bcc7657a8aa40163facaae5e7a94321bf272821690bb1fb9969`,
  and `6434434cd3268578aef4bdf7ec4317e36c0e382670e0ebc4d37dbcb0fc099035`.
  Every snapshot matches the workspace; candidate inventory is 74/59/0. The
  final patch must prove both complete cycles without touching the workspace or
  any prior evidence.
- `exact_fail_first_action`: add only
  `issues exact-group summary-policy change previews only through current scope authority`
  to `tests/integration/governance-http-wiring.test.ts`, then run it alone before
  changing production or canonical route documentation. The first assertion
  must establish the absent scoped POST as `404` rather than the required `401`.
- `fail_first_evidence`: the named Vitest run collected 31 governance wiring
  cases, skipped 30 unrelated cases, and failed 1 / 1 only because
  unauthenticated `POST /governance/api/v1/group-summary/policy` returned `404`
  instead of the required pre-handler `401`. B24E, the preview issuer, and B24F
  remained uncalled. The mode-`0600` log is
  `/tmp/lethebot-gov-ux-01b24g-fail-first.log`; production and all four canonical
  route/security documents still match their captured B24F baseline hashes, and
  only the allowed wiring test plus this checkpoint changed.
- `final_evidence`: the final named regression passed 1 / 1 with 30 unrelated
  cases skipped, complete governance HTTP wiring passed 31 / 31, and six-file
  query/service/HTTP/scope/preview-registry adjacency passed 111 / 111. The
  canonical P8 gate passed 7 files / 310 tests. Typecheck, scoped/full lint, and
  `git diff --check` passed; fresh normal-umask `pnpm release:check` passed 137 /
  1 skipped files with 2807 / 10 skipped tests. The first complete-wiring run
  found two earlier B24C/B24D assertions that still expected all POSTs to be
  absent; both now assert the registered route's fixed `400` rejection for their
  legacy body while retaining their zero-query/zero-mutation checks, and the
  unchanged rerun passed. The final route rejects every auth/query/Origin/CSRF/
  JSON/body/scope failure before B24E or issuance; valid stored and implicit-
  default transitions return unchanged B24E evidence with exact current-session
  bindings, stable digest and fresh handle, while no-op and confirmation paths
  remain write-free/absent. Integrity is `ok` and foreign keys are empty.
- `rollback_evidence`: the exact six-path patch is
  `/tmp/lethebot-gov-ux-01b24g-roundtrip-E1MAh3/gov-ux-01b24g.patch`
  (`sha256:1ee2504a40e7f70a9f0020a6348786c793f1756ed034c965d5c354112eda8fe5`,
  mode `0600`, size `28063`). Independent reverse-first and forward-first cycles
  restored the B24F baseline and B24G candidate byte-for-byte in mode-`0700`
  trees with mode-`0644` product files and final candidate/workspace equality.
  The patch contains exactly six ASCII-only paths with no NUL, reject, or
  original artifact. Inventory remains 74/59/0, Pi packages remain `0.80.2`,
  and migrations 007/008 retain their recorded hashes.
- `exact_next_action`: completed; dependency reassessment selects
  `GOV-UX-01B24H`, the separate exact-scope confirmation that consumes B24G
  authority once, recomputes B24E, and invokes only B24F.

## Completed Slice: `GOV-UX-01B24H`

- `requirement`: `GOV-UX-01B24H` exact-group summary-policy change confirmation
  through current-session single-use authority.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: B24G now issues exact session/action/resource/group/current-
  state/generation/digest authority, and B24F owns the atomic expected-snapshot
  mutation, but no route consumes that authority. Operators can preview a
  summary-policy transition but cannot confirm it through the local governance
  boundary.
- `selection_reason`: projection, authority issuance, and atomic mutation are
  independently proved. One separate confirmation route is the smallest
  dependency-complete continuation and requires no new query, registry,
  repository, schema, or policy behavior.
- `implementation_contract`: register only mutation-marked
  `POST /governance/api/v1/group-summary/policy/confirm` under the existing
  `governance.group_summary_policy.status.read` exact-scope purpose. Let the
  shared boundary authenticate, reject queries, enforce exact Origin/CSRF and
  bounded JSON, parse only exact `{ confirm: true, previewHandle: HANDLE,
  targetState: STATE }`, and resolve one current-session exact-group handle.
  Consume only matching `group.summary_policy.change` / `group_summary_policy` /
  fixed `policy` / exact-scope authority once. Recompute B24E with the resolved
  scope and target; compare current state, positive generation-or-default
  registry revision, and digest to the consumed binding. Invoke only B24F with
  the recomputed implicit-default or stored-policy version and fixed reason code
  `governance_http_group_summary_policy_change_confirmed`. Unknown or mismatched
  authority is concealed as `404`; reuse, drift, no-op, or B24F stale evidence
  is `409`. Success is `200` with only action, fixed `updated` outcome, current
  stored state/version/eligibility, copied durable/enforcement effects, fixed
  audit event/generation/update/cancellation evidence, and the preview's
  semantic rollback boundary. Omit group, actor, reason, audit/job IDs, preview
  binding, raw repository rows, and unrestricted data.
- `allowed_paths`: `src/index.ts`,
  `tests/integration/governance-http-wiring.test.ts`, `docs/architecture.md`,
  `docs/deployment.md`, `docs/governance-ui.md`, `docs/security-privacy.md`, and
  this checkpoint only.
- `protected_paths`: B24A-G query/service/repository/preview/route behavior,
  B24F validation and transaction semantics, legacy CLI/QQ grammar and output;
  `GovernanceHttpServer`, scope/preview registry implementations and generic
  session/Origin/CSRF/body/scope dispatch; all other governance routes; config,
  schema, migrations, dependencies, lockfile, browser, Provider, QQ live,
  private data, deployment, restart, container behavior, and every prior slice.
- `assertions`: unauthenticated confirmation is `401`; query, invalid Origin/
  CSRF, malformed/non-exact/oversized body, missing/malformed scope, and unknown,
  cross-session, or cross-purpose scope authority fail before consumption, B24E,
  or B24F. Wrong action/resource/scope/session handle and foreign target are
  concealed; reuse conflicts without a second write. Valid implicit enable,
  stored enable, and stored disable transitions consume once, recompute once,
  invoke only B24F with the exact source/state/generation/update timestamp and
  fixed reason, advance generation once, preserve enable no-backfill fencing,
  and apply disable enforcement/exact pending-job cancellation plus one audit
  atomically. Row/state/source/generation/time or digest drift conflicts after
  consumption and before service; a race after recomputation is rejected by
  B24F. Success is identifier-free and reflects exact persisted state,
  eligibility, generation, update time, cancellation count, effects, audit type,
  and rollback boundary. B24G issuance, GET/discovery, CLI/QQ, unrelated routes,
  and generic owners remain exact; reopen persistence, integrity, and FKs pass;
  the listener remains default-unreachable.
- `focused_commands`: add and run only one named B24H wiring regression fail-
  first then final; run full governance HTTP wiring; query/service/repository/
  HTTP/scope/preview adjacency; canonical P8; typecheck; scoped/full lint;
  release; diff, dependency/migration/inventory/privacy hygiene; and exact six-
  path private reverse-first/forward-first proof.
- `schema_dependency_live_boundary`: route wiring, synthetic migrated SQLite,
  integration coverage, and canonical route/security documentation only. No
  query/service/repository/registry implementation, schema, migration,
  dependency, lockfile, CLI/QQ/browser, Provider, private DB, deployment,
  restart, container, persistence-format, or live operation is permitted.
- `rollback_boundary`: the exact B24G candidate for all six product/test paths
  is captured under mode-`0700`
  `/tmp/lethebot-gov-ux-01b24h-baseline-KtWiyX`; every directory is `0700` and
  each file is `0644`. Index, wiring-test, architecture, deployment, governance-
  UI, and security/privacy SHA-256 values are
  `033e44e41f9d237b968999b468b23f63a5fb69a3515f171ea75e8d49b18062d9`,
  `076275ee7f1ebfae537c6ebc54928756980651668f70b2a5159d0e3bfb66a223`,
  `3a8d83460f0cfd868ee348e6e6334fefa791181f5836b03d0dc71c217ed3db94`,
  `6b02e575d3960e366bb431f668b9a39b5db4ff3c50ff7f023be8ad91b9d67862`,
  `6861b8379947ad7c7cfd55d7a9b9fd02c2b18c49b95082b17a27b30b35e92ca6`,
  and `d33ead83b0fb71a6c8c6374868ca914d4b8e68bbc533d3b96a40e7871db4fbfa`.
  Every snapshot matches the workspace at 74/59/0. The final patch must prove
  both complete cycles without touching the workspace or prior evidence.
- `exact_fail_first_action`: add only
  `confirms exact-group summary-policy changes through current snapshot authority`
  to the governance HTTP wiring file and run it alone before production or
  canonical document edits. Its first assertion must prove the absent
  confirmation returns `404` instead of required `401`, with zero consumption,
  B24E/B24F call, policy/job/audit write, or enforcement change.
- `fail_first_evidence`: the named Vitest run collected 32 governance wiring
  cases, skipped 31 unrelated cases, and failed 1 / 1 only because
  unauthenticated `POST /governance/api/v1/group-summary/policy/confirm`
  returned absent-route `404` instead of required pre-handler `401`. Preview
  consumption, B24E recomputation, B24F, and every durable effect remained
  unreachable. The mode-`0600` log is
  `/tmp/lethebot-gov-ux-01b24h-fail-first.log`; production and all four canonical
  docs still match the B24G baseline, and only the allowed wiring test plus this
  checkpoint changed.
- `final_evidence`: the named B24H regression passed 1 / 1 with 31 unrelated
  cases skipped, complete governance HTTP wiring passed 32 / 32, and seven-file
  repository/query/service/HTTP/scope/preview adjacency passed 124 / 124. The
  canonical P8 gate passed 7 files / 310 tests. Typecheck, scoped/full ESLint,
  and `git diff --check` passed; `pnpm release:check` passed 137 / 1 skipped
  files with 2808 / 10 skipped tests. The route accepts only the exact
  confirmation body after session, Origin/CSRF, JSON, query, and exact-group
  scope checks; consumes exact session/action/resource/group authority once;
  recomputes and compares B24E state, positive generation-or-default, and
  digest; converts implicit/stored versions for B24F; and invokes only
  `setGroupSummaryPolicyAsLocalAdmin` with fixed reason
  `governance_http_group_summary_policy_change_confirmed`. Unknown authority is
  `404`; reuse, drift, no-op, and stale mutation are `409`; success exposes only
  bounded identifier-free persisted/effect/audit/cancellation/rollback
  evidence. The mode-`0600` fail-first, adjacency, P8, and release logs are
  `/tmp/lethebot-gov-ux-01b24h-fail-first.log`,
  `/tmp/lethebot-gov-ux-01b24h-adjacency.log`,
  `/tmp/lethebot-gov-ux-01b24h-p8.log`, and
  `/tmp/lethebot-gov-ux-01b24h-release.log`. The exact six-path ASCII patch is
  `/tmp/lethebot-gov-ux-01b24h-roundtrip-1Dd9sZ/gov-ux-01b24h.patch`
  (`sha256:fee575e47eea170dc57201a485c6ecd9c859765a5aa4d09955673c7c3862dad8`,
  mode `0600`, size `37706`); its mode-`0700` proof trees and mode-`0600`
  manifests independently passed reverse-first and forward-first cycles, and
  the final candidate equals the workspace byte-for-byte and mode-for-mode.
  Inventory remains 74 tracked plus 59 untracked paths with zero staged; Pi
  packages remain `0.80.2`, and migrations 007/008 retain SHA-256
  `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`/
  `95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`.
- `exact_next_action`: completed; reassess the full P8 critical path and select
  the smallest behavior-preserving shared-governance prerequisite before any
  further HTTP or browser exposure.

## Completed Slice: `GOV-UX-01B25A`

- `requirement`: `GOV-UX-01B25A` behavior-preserving shared local-admin display-
  profile redaction operation and CLI delegation.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: the existing `GovernanceCLI.redactDisplayProfile` directly
  owns both profile/history SQL updates, transaction scope, and durable audit
  append. This is the remaining display-profile privacy mutation named by the
  P8 operator model, but it has no typed shared mutation owner, so a future HTTP
  workflow would either instantiate CLI code or duplicate its semantics.
- `selection_reason`: critical-path reassessment after completing the exact-
  group summary-policy workflow selects the smallest existing audit mutation
  prerequisite. Moving one already-characterized operation behind
  `GovernanceService` is independently reversible, preserves the current CLI,
  and establishes one shared owner without adding preview, expected-snapshot,
  authority, route, or browser behavior.
- `implementation_contract`: add only typed
  `GovernanceService.redactDisplayProfileAsLocalAdmin(input)`, accepting the
  exact canonical-user identifier and optional exact group identifier and
  returning the total changed row count. Preserve `undefined` group as the
  existing empty-string global key, capture one update timestamp before the
  transaction, update the exact matching `display_profiles` row to
  `[redacted]` / `user_set`, update every exact matching `nickname_history` row
  to `[redacted]` while closing only open intervals through `COALESCE`, and
  append the current `display_profile.redact` audit inside the same ordinary
  database transaction. Preserve the raw `<canonicalUserId>:<groupId>` audit
  lookup key, a separately captured append timestamp, category/level/actor/
  context/risk/redacted fields, summary text, optional omission of empty
  `groupId`, per-table counts, and total count exactly. Pre-redact durable
  summary/details with the existing governance display/structured redactors;
  do not substitute the `AuditRepository`-only numeric-ID rules. A zero-row
  request still appends the same audit and returns zero. Audit failure rolls
  back both table updates. Make `GovernanceCLI.redactDisplayProfile` call that
  operation once and preserve its database-missing result, success message,
  thrown-error text, asynchronous shape, and public options type without
  reshaping or adding validation.
- `allowed_paths`: `src/governance/service.ts`, `src/cli/governance.ts`,
  `tests/unit/governance/service.test.ts`, `tests/unit/cli/governance.test.ts`,
  `docs/governance-ui.md`, and this checkpoint only.
- `protected_paths`: `GovernanceQueryService` redactor implementations and all
  existing read projections; `AuditRepository`; identity unlink and its CLI-
  owned audit helper; CLI grammar/main/output redaction; integration tests;
  every HTTP/session/scope/resource/preview route or registry; QQ governance;
  repositories, schema, migrations, dependencies, lockfile, retention, browser,
  Provider, private/live data, deployment, restart, container behavior, and
  every prior slice.
- `assertions`: direct service calls for the global key and one exact group
  change only matching rows, preserve distractors, use one update timestamp,
  retain an already-closed history timestamp, set profile trust to `user_set`,
  and return the exact combined SQLite change count. Every invocation, including
  zero-row and partial matches, appends exactly one audit with the existing raw
  event key and fixed metadata, governance-redacted summary/details, omitted
  empty group field, and exact per-table counts. Secret, assignment-shaped, and
  standalone or prefixed 5-12 digit platform identifiers remain absent from
  durable display fields while the raw event lookup key remains unchanged.
  Synthetic audit failure restores profile/history bytes and adds no audit.
  Reopen state, integrity, and FKs pass. The CLI delegates once with the exact
  input, returns its unchanged redacted success/error output, and owns no
  profile/history SQL; missing-DB behavior and the separate identity-unlink
  audit path remain exact. No query, preview, handle, route, HTTP, QQ, browser,
  schema, or dependency behavior appears.
- `focused_commands`: run the existing focused CLI unit and ten integration
  display-profile characterizations before edits; add and run one named service
  fail-first regression; run that service regression and one CLI-delegation
  regression final; run both complete unit files plus the unchanged CLI-main
  display-profile cases; run governance/service/CLI adjacency, canonical P8,
  typecheck, scoped/full lint, release, diff/dependency/migration/inventory/
  privacy hygiene, and exact five-path private reverse-first/forward-first proof.
- `schema_dependency_live_boundary`: behavior-preserving local service/CLI
  extraction, synthetic migrated SQLite tests, and canonical ownership
  documentation only. No query behavior, persistence format, schema, migration,
  dependency, lockfile, expected snapshot, preview, authority, HTTP/QQ/browser,
  Provider, private DB, deployment, restart, container, or live operation is
  permitted.
- `rollback_boundary`: the exact B24H candidate for all six allowed paths is
  captured byte-for-byte and mode-for-mode under mode-`0700`
  `/tmp/lethebot-gov-ux-01b25a-baseline-N4my6a`; its mode-`0600` manifest records
  service, CLI, service-test, CLI-test, governance-UI, and checkpoint SHA-256
  values
  `e71fa671406edde198e7d0df2dcdbec84e0aaa9f66630c78b2dd74c294affbf2`,
  `6793ea5892ed58356d0236e07ca3a72526f322197eddad8b28227a24b5f890bc`,
  `9e5733277ec7b5bc44ff02a632e97c0964f74aa69d580533b374c08333e726da`,
  `bf155e81c460f8028862378055088b9afb2aafa284499e049f322cad6e43f22d`,
  `de6f09d9889d509d47ce664682944e37bffa6cb3c9e5519a9a2153badfe863d4`,
  and `e2326ba2b0adee2a3b42edb263b0de30f21d7c4a9ef33f26eb6cff3bc4d19f91`.
  All source and captured files are mode `0644` and match at 74/59/0. The final
  ASCII patch covers the five immutable product/test/doc paths and deliberately
  excludes this mutable checkpoint so its evidence hash remains stable. It must
  independently prove complete reverse-first and forward-first restoration
  without touching the workspace or any earlier evidence.
- `characterization_evidence`: the unchanged focused CLI unit case passed 1 / 1
  with 51 unrelated cases skipped; the unchanged CLI-main group/global, zero-
  row, partial, and history-only display-profile matrix passed 10 / 10 with 119
  unrelated cases skipped. The mode-`0600` combined log is
  `/tmp/lethebot-gov-ux-01b25a-characterization.log`.
- `exact_fail_first_action`: add only
  `owns local-admin display-profile redaction with exact CLI-compatible transaction and audit semantics`
  to `tests/unit/governance/service.test.ts`, then run it alone before changing
  production, the CLI test, or canonical documentation. It must fail only
  because `service.redactDisplayProfileAsLocalAdmin` is absent after the fixture
  has established exact global/group rows, timestamps, audit/redaction/count,
  rollback, reopen, integrity, and FK assertions.
- `fail_first_evidence`: the named Vitest run collected 13 service cases,
  skipped 12 unrelated cases, and failed 1 / 1 only because
  `service.redactDisplayProfileAsLocalAdmin is not a function`. The migrated
  synthetic fixture and exact global/group/distractor/rollback rows were
  created before the absent call; no production, CLI test, or canonical
  documentation changed. The mode-`0600` log is
  `/tmp/lethebot-gov-ux-01b25a-fail-first.log`.
- `final_evidence`: the focused service/delegation regressions passed 2 / 2,
  both complete unit files passed 66 / 66, the unchanged CLI profile subprocess
  matrix passed 10 / 10, five-file adjacency passed 143 / 143, and the canonical
  P8 gate passed 312 / 312. Typecheck, scoped and full lint, `git diff --check`,
  and the corrected normal-umask release gate passed; release recorded 137 / 1
  skipped files and 2810 / 10 skipped tests. Pi packages remain `0.80.2`,
  migrations 007/008 retain their recorded hashes, and inventory remains
  74 tracked plus 59 untracked paths with zero staged. The exact five-path patch
  is `/tmp/lethebot-gov-ux-01b25a-roundtrip-nyxeD1/gov-ux-01b25a.patch`
  (`sha256:7dc8caa6eac3796579a1c9e0052aff05f41ff602fec58de1797704db4ce31524`,
  mode `0600`, size `17142`); its mode-`0700` proof root and trees passed
  independent reverse-first and forward-first cycles, every file manifest
  matched, the patch paths and ASCII boundary were exact, and the candidate
  snapshot equals the workspace byte-for-byte and mode-for-mode.
- `exact_next_action`: completed; reassess the remaining P8 shared profile,
  identity, and retention prerequisites and select the smallest independently
  reversible dependency before adding HTTP or browser authority.

## Completed Slice: `GOV-UX-01B25B`

- `requirement`: `GOV-UX-01B25B` behavior-preserving shared local-admin
  platform-account unlink operation and CLI delegation.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: `GovernanceCLI.unlinkPlatformAccount` still directly owns the
  exact account lookup, guarded status update, transaction, audit append, and
  outcome mapping. Identity unlinking is named by the P8 operator model, but a
  future local UI would have to instantiate CLI code or duplicate this mutation.
- `selection_reason`: B25A completed the same bounded extraction for display-
  profile redaction. Identity unlink is the next smallest existing destructive
  mutation with complete CLI/unit characterization and no unresolved product
  semantics. Profile preview/confirmation needs a new expected-snapshot and
  irreversible-effect contract, while retention spans the separate operations
  owner; neither belongs in this behavior-preserving slice.
- `implementation_contract`: add only typed
  `GovernanceService.unlinkPlatformAccountAsLocalAdmin(input)`, accepting the
  existing exact `qq` platform and platform-account identifier and returning
  `unlinked` or `not_found`. In one ordinary database transaction, select only
  the exact mapping, return `not_found` without writes for missing, disabled, or
  deleted state, guard the update with `status = 'active'`, and treat a non-one
  update count as `not_found`. On the one-row path, set only status to
  `disabled`, append exactly one existing `identity.platform_account.unlinked`
  audit in the same transaction with a fresh `identity-unlink-<ulid>` key,
  separately captured append timestamp, fixed system/summary/admin/admin_cli/
  medium/redacted metadata, unchanged summary, and unchanged detail fields.
  Pre-redact summary/details through the existing governance display/structured
  redactors; omit the raw platform-account identifier entirely and preserve the
  raw mapping only as the SQL lookup key. Audit failure must throw from the
  service and roll back the status update. Make
  `GovernanceCLI.unlinkPlatformAccount` call the shared operation once, map its
  two outcomes to the current success/not-active results, and preserve its
  database-missing throw, generic caught-error result, async/public option
  shape, and output text. Remove only the CLI audit helper and `ulid` import if
  this extraction makes them unused.
- `allowed_paths`: `src/governance/service.ts`, `src/cli/governance.ts`,
  `tests/unit/governance/service.test.ts`, `tests/unit/cli/governance.test.ts`,
  `docs/governance-ui.md`, and this checkpoint only.
- `protected_paths`: `IdentityRepository` and all identity ingestion/resolution
  behavior; B25A profile redaction; every other governance service/query/CLI/QQ
  operation; CLI grammar/main/output redaction and integration tests; HTTP,
  session, scope, resource, and preview registries/routes; operations retention;
  schema, migrations, dependencies, lockfile, Provider, private/live data,
  deployment, restart, container behavior, and every prior slice.
- `assertions`: a direct service call disables only one exact active mapping,
  preserves every other column and distractor, returns `unlinked`, and appends
  one exact durable audit without the platform-account identifier. Missing,
  disabled, and deleted mappings return `not_found` with byte-identical mapping
  and audit tables. Secret-like and 5-12 digit canonical-user material is absent
  from durable summary/details while the event key remains the existing fresh
  unlink ULID form. Synthetic audit failure restores the active row and adds no
  audit; successful state/audit survive reopen; integrity and FKs pass. CLI
  delegates once with the exact input, maps both outcomes and thrown failures to
  its unchanged results, and owns no platform-account SQL or identity audit.
  Existing CLI-main platform validation/redaction and the separate display-
  profile path remain exact. No preview, expected snapshot, restore/relink,
  handle, route, HTTP, QQ, browser, retention, schema, or dependency behavior
  appears.
- `focused_commands`: run the three existing CLI unit unlink cases and the two
  existing CLI-main unlink cases before edits; add and run one named service
  fail-first regression; run that service regression and one CLI-delegation
  regression final; run both complete unit files plus the unchanged CLI-main
  unlink cases; run governance/service/CLI/identity/audit adjacency, canonical
  P8, typecheck, scoped/full lint, release, diff/dependency/migration/inventory/
  privacy hygiene, and exact five-path private reverse-first/forward-first proof.
- `schema_dependency_live_boundary`: behavior-preserving local service/CLI
  extraction, synthetic migrated SQLite tests, and canonical ownership
  documentation only. No identity repository or persistence behavior, schema,
  migration, dependency, lockfile, preview, authority, HTTP/QQ/browser,
  retention, Provider, private DB, deployment, restart, container, or live
  operation is permitted.
- `rollback_boundary`: the exact B25A candidate for all six allowed paths is
  captured byte-for-byte and mode-for-mode under mode-`0700`
  `/tmp/lethebot-gov-ux-01b25b-baseline-6h7jr2`; its mode-`0600` manifest records
  service, CLI, service-test, CLI-test, governance-UI, and checkpoint SHA-256
  values
  `ac9f468823ee86d0f9c65e27626985c3f9ac18d6b64cac1b167ac31f15e36edd`,
  `1288f263cb4db406cfaf8e4ddb4bc8000d8e73c185aeaea0c088f3084c39e8d7`,
  `c457abe7165f8a32f6ede0860933e78b5c168e6b22ff1da4893ad225d10ec320`,
  `74726b0d4b81281b79ae7a64b3895f68f2c8af0c95ef55e6854c14398334797e`,
  `976f120feba8e75f44412a3f6fbea614c9dab22f23136368cc35fc4361c7b655`,
  and `ca5347057b4c3c1403d9004f6be0bf10fe4984cae6aa01c2c24b76b0e7e2e62d`.
  All source and captured files are mode `0644` and match at 74/59/0. The final
  ASCII patch will cover the five immutable product/test/doc paths, exclude this
  mutable checkpoint, and independently prove reverse-first and forward-first
  restoration without touching the workspace or earlier evidence.
- `exact_fail_first_action`: add only
  `owns local-admin platform-account unlink with exact CLI-compatible transaction and audit semantics`
  to `tests/unit/governance/service.test.ts`, then run it alone before changing
  production, the CLI test, or canonical documentation. It must fail only
  because `service.unlinkPlatformAccountAsLocalAdmin` is absent after the
  fixture establishes active/inactive/distractor rows, exact audit/redaction,
  no-op, rollback, reopen, integrity, and FK assertions.
- `characterization_evidence`: the three unchanged CLI unit unlink cases passed
  3 / 3 with 50 unrelated cases skipped, and the unchanged CLI-main unlink and
  unsupported-platform cases passed 2 / 2 with 127 unrelated cases skipped.
  The combined mode-`0600` log is
  `/tmp/lethebot-gov-ux-01b25b-characterization.log`.
- `fail_first_evidence`: the named Vitest run collected 14 service cases,
  skipped 13 unrelated cases, and failed 1 / 1 only because
  `service.unlinkPlatformAccountAsLocalAdmin is not a function`. The migrated
  synthetic fixture had created its exact active, inactive, distractor, and
  rollback rows before the absent call; production, CLI test, and canonical
  documentation still matched the B25A baseline. The mode-`0600` log is
  `/tmp/lethebot-gov-ux-01b25b-fail-first.log`.
- `final_evidence`: the refreshed focused service/delegation regressions passed
  2 / 2, both complete unit files passed 68 / 68, the unchanged CLI unlink
  subprocess matrix passed 2 / 2, five-file adjacency passed 145 / 145, and the
  canonical P8 gate passed 314 / 314. Typecheck, scoped and full lint,
  `git diff --check`, and the normal-umask release gate passed; release recorded
  137 / 1 skipped files and 2812 / 10 skipped tests. Pi packages remain
  `0.80.2`, migrations 007/008 retain their recorded hashes, inventory remains
  74 tracked plus 59 untracked paths with zero staged, and no sensitive
  untracked artifact filename was found. The exact five-path patch is
  `/tmp/lethebot-gov-ux-01b25b-roundtrip-e50Mit/gov-ux-01b25b.patch`
  (`sha256:75a991c2c7a5f3fa13791c3d147b82bd3b79a96b6441b875ace5a8237f9d2974`,
  mode `0600`, size `18311`); its mode-`0700` proof root and trees passed
  independent reverse-first and forward-first cycles, every file manifest
  matched, the patch paths and ASCII boundary were exact, and the candidate
  snapshot equals the workspace byte-for-byte and mode-for-mode.
- `exact_next_action`: completed; after two consecutive shared-mutation
  extractions, reassess the full P8 critical path before selecting any profile,
  identity, retention, HTTP, or browser continuation.

## Completed Slice: `GOV-UX-01C1`

- `requirement`: `GOV-UX-01C1` dependency-free local governance browser shell,
  session workflow, and read-only aggregate Overview.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: P8 has a default-off loopback listener, hardened independent
  session/CSRF boundary, and a broad set of fixed APIs, but every browser path
  still returns `404`. Operators must use CLI or raw HTTP and cannot perform the
  already-proved login/session/aggregate-health workflow in a browser.
- `selection_reason`: the required reassessment after B25A/B25B finds that more
  shared mutation prerequisites would not create a usable local experience.
  The canonical dependency strategy already selects server-rendered semantic
  HTML, one same-origin CSS asset, and one small local ES module with no package
  change. A login plus read-only Overview is the smallest real browser workflow
  over existing authority and query semantics; profile, identity, retention,
  scoped views, and destructive actions remain separate slices.
- `implementation_contract`: add one typed static asset module and teach only
  `GovernanceHttpServer` to serve exact sessionless `GET /governance`,
  `GET /governance/`, `GET /governance/app.css`, and
  `GET /governance/app.js` when the existing listener is running. The two root
  paths return the same HTML with absolute same-origin asset URLs. Every asset
  uses the existing no-store/referrer/nosniff/CSP headers plus an exact MIME
  type and byte length. Queries, other methods, unknown assets, and API-like
  prefixes remain fixed `404`; static dispatch performs no authentication,
  session creation, handle resolution, domain callback, database access, or
  audit. All existing session and API paths retain their exact ordering and
  behavior.

  The HTML is the actual compact LetheBot Governance application, not a landing
  page: semantic skip link/header/navigation/main regions, a labelled password
  login form, stable status/live regions, an authenticated Overview toolbar,
  attention summary, and fixed aggregate sections. It references no inline or
  external script/style/font/image and contains no credential, session, CSRF,
  handle, raw identifier, private row, or diagnostic. The local ES module calls
  only the existing session and Overview endpoints with same-origin credentials;
  retains the returned CSRF value only in module memory; clears the token input;
  uses fixed labels plus `textContent`/DOM creation for response values; never
  reads cookies or uses local/session storage, HTML injection, dynamic code, or
  URLs containing authority. It implements login, refresh, logout, session-
  expired, loading, empty/zero, bounded unavailable, and attention/healthy
  states without reflecting server payloads in errors. Logout uses only the
  current in-memory CSRF value. Reload deliberately returns to login so a lost
  CSRF value is never reconstructed or persisted.

  The CSS follows the locked quiet operations design: neutral surface and
  graphite text with restrained teal/green/amber/red roles, no gradients or
  decoration, radii at most 8px, zero letter spacing, fixed-size controls,
  visible focus, 44px targets, skip-link behavior, stable loading rows, wrapped
  labels, reduced-motion handling, and a mobile layout at 390px without overlap.
  No frontend/runtime/build dependency, icon set, chart library, analytics,
  service worker, manifest, route router, or asset pipeline is added.
- `allowed_paths`: new `src/http/governance-browser-assets.ts`,
  `src/http/governance-http-server.ts`,
  `tests/integration/governance-http.test.ts`, `docs/architecture.md`,
  `docs/deployment.md`, `docs/governance-ui.md`, `docs/security-privacy.md`, and
  this checkpoint only.
- `protected_paths`: every registered governance domain route and handler in
  `src/index.ts`; session token/cookie/CSRF generation, validation, TTL, and
  revocation semantics except dispatching the four static paths before API
  lookup; scope/resource/preview registries; GovernanceQueryService and
  GovernanceService; CLI/QQ; application listener; config/env; schema,
  migrations, dependencies, lockfile, build scripts, Provider, private/live
  data, deployment/restart/container behavior, and every prior slice.
- `assertions`: before implementation the named browser test gets JSON `404`
  for `/governance/` and all callbacks/handle stores remain untouched. Final
  exact GETs return bounded non-empty UTF-8 HTML/CSS/JavaScript with correct
  byte lengths, fixed security headers, no Set-Cookie, and no secret fixture or
  authority value. Both root forms are byte-identical. Query-bearing, traversal,
  unknown, and non-GET asset requests remain `404` without reflecting input or
  creating a session/effect; unauthenticated `/governance/api/v1/overview`
  remains `401`. Static source contains only same-origin fixed endpoints, safe
  DOM operations, in-memory authority, fixed bounded labels, and all required
  semantic/accessibility/responsive selectors; total asset bytes remain bounded.
  Existing complete HTTP/session/resource tests and application wiring stay
  exact. Synthetic desktop and mobile browser runs prove login, populated and
  zero/attention Overview rendering, refresh, logout/session expiry, keyboard
  focus, no console/page errors, no overlap, and no external request. The
  listener remains default-disabled and loopback-only.
- `focused_commands`: first run the unchanged complete governance HTTP boundary
  characterization; add and run one named browser-surface regression fail-first;
  run it final and the complete HTTP file; run HTTP/application/wiring adjacency
  and the canonical P8 command; run typecheck, scoped/full lint, release,
  `git diff --check`, dependency/migration/inventory/privacy hygiene, synthetic
  desktop/mobile Chromium QA, and exact seven-path private reverse-first/
  forward-first proof.
- `schema_dependency_live_boundary`: optional same-listener static responses,
  browser-only existing login/session/Overview calls, synthetic tests, and
  canonical browser/security documentation only. No domain/query/mutation,
  scope, persistence, schema, migration, dependency, lockfile, build, CLI/QQ,
  Provider, private DB, deployment, restart, container, or live operation is
  permitted.
- `rollback_boundary`: the exact B25B candidate for all eight allowed paths is
  captured under mode-`0700`
  `/tmp/lethebot-gov-ux-01c1-baseline-jPqFrx`; the asset module is explicitly
  absent and the other seven files are byte-for-byte and mode-for-mode copies.
  Its mode-`0600` manifest records HTTP server, HTTP test, architecture,
  deployment, governance-UI, security/privacy, and checkpoint SHA-256 values
  `2fcdd8e40cabccae8df569f20dfb4863c626c93cc6dd59104b4d90c4dd54897e`,
  `5a5b7111cffed2f491d8799383a9968dcafcdfa210a361070b8e7f9e5fa24f18`,
  `cb1133641d88691e4718e015c117fc5ea9a0240185071cb4b97dbc85b374a695`,
  `95948e9f398819afdf8cc0e9f570b6afaa6e5e7b1e4dcee5590358ea048ddcce`,
  `336493cfb83397a82f84ab290ecb1c4fc003ad5fa6a215d3032b8aa65a33f8bc`,
  `d60a8e6daec5e90c01f2f48644aea8d88f52b8a04469e745f03d1d111ead3433`,
  and `9b6ee35117f4f1f42beea377ed3a75dad31fb4e7c8c77760ea27c9894d0b8c3f`.
  Existing files are mode `0644` and baseline inventory was 74/59/0. The final
  ASCII patch covers seven immutable product/test/doc paths while excluding
  this mutable checkpoint and proves creation/deletion plus both complete
  cycles without touching the workspace or earlier evidence.
- `exact_fail_first_action`: add only
  `serves a dependency-free browser shell without creating API authority or effects`
  to `tests/integration/governance-http.test.ts`, then run it alone before
  creating the asset module or editing production/docs. Its first assertion
  must expect HTML `200` at `/governance/` but observe the current JSON `404`,
  with zero session cookie, scope/resource resolution, authorized/unscoped
  callback, or handle-store lifecycle effect.
- `characterization_evidence`: the unchanged complete governance HTTP boundary
  passed 15 / 15 before the C1 regression. The mode-`0600` log is
  `/tmp/lethebot-gov-ux-01c1-characterization.log`.
- `fail_first_evidence`: the named Vitest run collected 16 HTTP cases, skipped
  15 unrelated cases, and failed 1 / 1 at its first assertion because
  `GET /governance/` returned the current JSON `404` instead of required HTML
  `200`. The server had started with only the existing API boundary, and no
  asset module, production source, or canonical document had changed. The mode-
  `0600` log is `/tmp/lethebot-gov-ux-01c1-fail-first.log`.
- `final_evidence`: the named browser regression and complete HTTP boundary
  passed 1 / 1 and 16 / 16; HTTP/application/wiring adjacency passed 55 / 55;
  the canonical P8 gate passed 315 / 315; and the final normal-umask release
  gate passed typecheck, lint, build, preflight, `git diff --check`, 137 / 1
  skipped files, and 2813 / 10 skipped tests. Synthetic Chromium 150 runs at
  1440x900 and 390x844 exercised login, zero/healthy, loading, attention,
  refresh, reload, bounded unavailable, expiry, and logout states. They proved
  visible keyboard focus, 44px controls, no horizontal overflow, clipping or
  control overlap, zero unexpected console/page/request failures, and zero
  external requests. The first run exposed a real hidden-navigation login-grid
  overlap; the final CSS spans both tracks while logged out and its regression
  is covered. Visually reviewed synthetic screenshots are
  `/tmp/lethebot-gov-ux-01c1-browser-desktop.png` and
  `/tmp/lethebot-gov-ux-01c1-browser-mobile.png`; the mode-`0600` browser log is
  `/tmp/lethebot-gov-ux-01c1-browser.log`.

  The exact seven-path ASCII patch is
  `/tmp/lethebot-gov-ux-01c1-roundtrip-FeCIIv/gov-ux-01c1.patch`
  (`sha256:17b64029b2b0368334835d005e33b7783130a8660f52adbda6c13d5dfd459e93`,
  mode `0600`, size `38355`). Its mode-`0700` proof root passed independent
  reverse-first and forward-first cycles, including asset deletion/creation;
  every baseline/candidate manifest matched byte-for-byte and mode-for-mode,
  and the candidate equals the workspace. Inventory is 74 tracked plus 60
  untracked paths with zero staged; Pi packages remain `0.80.2`; migrations
  007/008 retain SHA-256
  `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b` and
  `95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`;
  no sensitive untracked filename was found; all final logs and screenshots are
  mode `0600`; and no schema, dependency, lockfile, CLI/QQ, Provider, private
  data, deployment, restart, container, or live behavior changed.
- `exact_next_action`: completed; the post-shell P8 reassessment selects
  `GOV-UX-01C2`, a read-only browser Activity model-invocation view over the
  already-proved authenticated-unscoped B14A endpoint. It is the smallest
  required non-Overview workflow and needs no scope selector or mutation;
  other Activity pages, scoped Memory/Explain/Privacy views, and all browser
  mutations remain separate slices.

## Completed Slice: `GOV-UX-01C2`

- `requirement`: `GOV-UX-01C2` read-only browser Activity model-invocation
  aggregate view.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: C1 provides the real login/session shell and aggregate
  Overview, while the already-proved B14A model-invocation endpoint is still
  reachable only through raw HTTP. The browser has no Activity destination,
  view state, or rendering for that payload-free aggregate.
- `selection_reason`: B14A is authenticated-unscoped, bounded, payload-free,
  write-free, and already wired. It therefore needs no scope/resource handle,
  query, service, repository, schema, or mutation work. Exposing only this
  aggregate is the smallest post-shell workflow and keeps every other Activity
  page and scoped or mutating browser workflow independent.
- `implementation_contract`: extend only the existing fixed browser HTML, CSS,
  and local ES module. Add an Activity navigation button beside Overview and a
  semantic Model invocations view with its own refresh command, generated time,
  loading, empty, unavailable, and fixed aggregate content states. Navigation
  updates `aria-current`, hides the inactive view, focuses the existing main
  region, and preserves Overview as the post-login default. Selecting or
  refreshing Activity sends one same-origin credentialed GET to exact
  `/governance/api/v1/activity/model-invocations`, with no query, body, scope
  header, CSRF value, or new authority. A `401` returns to the existing
  session-expired login state; other transport, status, or malformed-body
  failures render one bounded fixed unavailable state.

  Render only predeclared DOM fields: total; the fixed `summary`, `evaluator`,
  and `pi_turn` purpose buckets; fixed `running`, `completed`, `failed`, and
  `aborted` status buckets; completed known/unknown usage; Provider-latency
  count, sum, and maximum; and generated time. Missing known buckets render as
  zero, an exact zero total uses the explicit empty state, and response keys are
  never inserted as labels or markup. Reuse fixed text nodes, safe numeric/date
  formatting, request sequencing, in-memory session behavior, and existing
  focus/live-region conventions. Add no router, filter, chart, table library,
  dependency, storage, cookie read, dynamic HTML/code, external request, or
  backend change.
- `allowed_paths`: `src/http/governance-browser-assets.ts`,
  `tests/integration/governance-http.test.ts`, `docs/architecture.md`,
  `docs/deployment.md`, `docs/governance-ui.md`, `docs/security-privacy.md`, and
  this checkpoint only.
- `protected_paths`: `GovernanceHttpServer`, `src/index.ts`, B14A route/query
  service/DTO semantics, every other Overview/Activity/scoped/detail/mutation
  API, session/CSRF/scope/resource/preview registries, GovernanceService,
  CLI/QQ, application listener, config/env, schema/migrations, dependencies/
  lockfile/build, Provider, private/live data, deployment/restart/container
  behavior, and every prior slice.
- `assertions`: the named fail-first browser-surface regression sees the C1
  assets but no Activity navigation/view/endpoint reference. Final fixed assets
  contain the semantic navigation and all fixed loading/empty/error/content
  regions, use only the exact existing endpoint, retain same-origin credentials
  and prohibited-DOM/storage exclusions, and remain below the existing total
  byte bound. Browser QA at 1440x900 and 390x844 proves Overview-to-Activity-to-
  Overview keyboard navigation and focus, populated and zero Activity data,
  loading, refresh, bounded unavailable, session expiry, logout, visible focus,
  44px controls, stable layout, no clipping/overlap/overflow, no unexpected
  console/page/request error, and no external request. The C1 shell and B14A
  server characterizations, complete HTTP boundary, application wiring,
  canonical P8, type/lint/release, dependency/migration, inventory/privacy, and
  exact rollback gates remain green.
- `focused_commands`: unchanged named C1 shell and B14A wiring
  characterizations; one named C2 browser-surface regression fail-first and
  final; complete governance HTTP plus application/wiring adjacency; canonical
  P8; typecheck; scoped and full lint; normal-umask release; `git diff --check`;
  dependency/migration/inventory/privacy hygiene; synthetic desktop/mobile
  Chromium QA; and exact six-path private reverse-first/forward-first proof.
- `schema_dependency_live_boundary`: fixed browser assets, synthetic static and
  browser tests, and canonical browser/security documentation only. No HTTP
  route or server/query/service/repository behavior, write, scope, mutation,
  schema, migration, dependency, lockfile, build, CLI/QQ, Provider, private DB,
  deployment, restart, container, or live operation is permitted.
- `rollback_boundary`: the exact C1 candidate for the six immutable product,
  test, and canonical-document paths plus this mutable checkpoint is captured
  under mode-`0700`
  `/tmp/lethebot-gov-ux-01c2-roundtrip-O62kyk`. All seven files are mode `0644`;
  baseline inventory is 74 tracked modifications, 60 untracked paths, and zero
  staged at `main@cfebb77`. Baseline SHA-256 values are
  `25b88e324851f7d95273a3dfc7299ac824e660f1e6e6b6c7a6f2ba48867ff399`,
  `9f5036549eddba9449fbd0323c51bac91e63cddbace0562fdbd0d9785f017172`,
  `74d9ca1c61f0169a25451a31aeaed632f392ead7444e642bd0ee63c20a87aea4`,
  `c7beccd6d3820a6dad298486f4a611ba9bee8fca155fec9fb464e0b31da6c7d1`,
  `b096bbfa820020906d314a33a8481ad5b333d8b74eae7a0e4733877c400998d6`,
  `41ebf73ca5abc79d103fa2fb35331a6db22d35ea221301d5b3a885f4b835490c`,
  and `954ccd070a00837f1a005836cbbfec15ffc68522cac8f9f98d9b47822c7efed1`
  in allowed-path order. The final immutable six-path ASCII patch must prove
  both complete application orders without touching the workspace; checkpoint
  evidence remains excluded from that patch.
- `exact_fail_first_action`: add only
  `exposes a fixed read-only model-invocation Activity browser view` to
  `tests/integration/governance-http.test.ts`, then run it alone before editing
  the browser asset or canonical docs. Its first assertion must require the
  Activity navigation control in the already-served HTML and fail because C1
  contains only Overview; subsequent source assertions define the fixed view,
  states, exact endpoint, safe DOM, and byte boundary without invoking a domain
  callback.
- `characterization_evidence`: the unchanged named C1 browser-shell regression
  passed 1 / 1 with 15 skipped, and the unchanged named B14A route regression
  passed 1 / 1 with 31 skipped. Mode-`0600` logs are
  `/tmp/lethebot-gov-ux-01c2-c1-characterization.log`
  (`sha256:20493930ec578b462af4c2bcfce41fcbec4ef6ece6aa404f06cb8ad1e5868488`)
  and `/tmp/lethebot-gov-ux-01c2-b14a-characterization.log`
  (`sha256:a098adc9e7541a1fa818d391837a3e9b8c7613126bb9445b85290e7f3911d6c0`).
- `fail_first_evidence`: the named C2 run collected 17 HTTP cases, skipped 16
  unrelated cases, and failed only at its first assertion because the unchanged
  C1 HTML contains no `id="activity-nav"`. The static assets still returned
  `200`, and no production asset or canonical document had changed. The mode-
  `0600` log is `/tmp/lethebot-gov-ux-01c2-fail-first.log`
  (`sha256:c7c7ef3c59d2303b4caf2293f407948710a98d55cea3a940414577b193c5439a`).
- `final_evidence`: the named C2 regression passed 1 / 1 with 16 skipped, the
  complete governance HTTP boundary passed 17 / 17, four-file HTTP/application/
  wiring adjacency passed 56 / 56, and the canonical P8 gate passed 316 / 316.
  Typecheck, scoped and full ESLint, build, release preflight, and final
  `git diff --check` passed. The normal-umask release gate recorded 137 / 1
  skipped files and 2814 / 10 skipped tests. The three browser assets total
  36,088 UTF-8 bytes and remain dependency-free.

  Synthetic Chromium at 1440x900 and 390x844 exercised login, Overview-to-
  Activity-to-Overview keyboard navigation, populated/empty/loading/refresh/
  unavailable/expired Activity states, and logout. It verified every fixed
  purpose/status/usage/latency value, `aria-current`, main-region focus, visible
  3px focus, 44px controls, and no overflow, clipping, overlap, external
  request, failed request, page error, or unexpected console error. Visually
  reviewed screenshots are `/tmp/lethebot-gov-ux-01c2-browser-desktop.png`
  (`sha256:faabdc9888191d05fe81b622506610376d5476cc528e25a2a9ca73644e4e70ff`)
  and `/tmp/lethebot-gov-ux-01c2-browser-mobile.png`
  (`sha256:d8ad73c7e02c2c98ea91b346e0716cb10e027eadd03afa07d412bf49cd0ee58f`).
  The mode-`0600` browser log is
  `/tmp/lethebot-gov-ux-01c2-browser.log`
  (`sha256:4dc73126f5c2a2a62a6896b4d99b552d8a8543837d568dd068d4afb5bb4c4737`).

  The exact six-path ASCII patch is
  `/tmp/lethebot-gov-ux-01c2-roundtrip-O62kyk/gov-ux-01c2.patch`
  (`sha256:bdcc156bdf7424ff9be8896711723ccb22287336a04a851bda60231da46b80ba`,
  mode `0600`, size `23072`). Its mode-`0700` proof trees and mode-`0644`
  product files passed independent reverse-first and forward-first complete
  byte/mode cycles using the explicit zero-context parser contract; its exact
  path inventory excludes the checkpoint and every protected path, and the
  candidate snapshot matches the workspace. Inventory remains 74 tracked
  modifications plus 60 untracked paths with zero staged; Pi packages remain
  `0.80.2`; migrations 007/008 retain SHA-256
  `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b` and
  `95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`;
  sensitive/reject/original filename hygiene is clean; all final evidence is
  mode `0600`; and no route, service, repository, schema, dependency, CLI/QQ,
  Provider, private/live, deployment, restart, or container behavior changed.
- `exact_next_action`: completed; the post-C1/C2 browser reassessment selects
  `GOV-UX-01C3`, a read-only browser Activity tool-call view over the existing
  bounded payload-free B14D endpoint. Establish one accessible bounded-list
  rendering pattern with loading/empty/error/expiry and responsive QA before
  adding action, job/worker, audit/failure, scoped, or mutating browser views.

## Completed Slice: `GOV-UX-01C3`

- `requirement`: `GOV-UX-01C3` read-only browser Activity tool-call list.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: C2 provides Activity navigation and the aggregate Model
  invocations view, while the already-proved B14D bounded tool-call endpoint is
  still reachable only through raw HTTP. The browser has no secondary Activity
  navigation or bounded-list rendering pattern.
- `selection_reason`: B14D is authenticated-unscoped, capped at 100 newest-first
  records, payload-free, recursively redacted, write-free, and already wired.
  It therefore requires no scope/resource authority, query, service,
  repository, schema, or mutation change. A single tool-call list is the
  smallest useful browser list pattern before action, job/worker,
  audit/failure, scoped, or mutating views.
- `implementation_contract`: extend only the fixed browser HTML, CSS, and local
  ES module. Activity keeps Model invocations as its default subview and gains
  semantic secondary navigation for Tool calls. Selecting or refreshing Tool
  calls sends one same-origin credentialed GET to exact
  `/governance/api/v1/activity/tool-calls`, with no query, body, scope header,
  CSRF value, or new authority. A `401` returns to the existing session-expired
  login state; other transport, status, oversized-list, unknown-field,
  malformed-field, or malformed-date failures render one fixed unavailable
  state. Request sequencing prevents an inactive or stale response from
  changing the current view.

  Accept at most 100 records with only the exact B14D keys and fixed requester,
  actor-class, invocation-context, and status classifications. Bound all text,
  numeric timing, and ISO date inputs before creating rows. Render a semantic
  table with fixed Tool, Actor and context, Outcome, Duration, and Recorded
  columns. Tool name plus requester, actor class plus context, fixed status,
  optional bounded redacted error code/message, persisted redaction signal,
  execution time, and creation time use only `textContent` and DOM creation.
  Call, turn, and canonical-user identifiers remain absent from the rendered
  DOM even though B14D validates their redacted response fields. Zero records
  use an explicit empty state. Desktop uses a dense fixed-layout table; mobile
  reflows every cell into a labelled row without horizontal scrolling. Add no
  router, filter, detail link, caller limit, chart, dependency, storage, cookie
  read, dynamic HTML/code, external request, or backend change.
- `allowed_paths`: `src/http/governance-browser-assets.ts`,
  `tests/integration/governance-http.test.ts`, `docs/architecture.md`,
  `docs/deployment.md`, `docs/governance-ui.md`, `docs/security-privacy.md`, and
  this checkpoint only.
- `protected_paths`: `GovernanceHttpServer`, `src/index.ts`, B14D route/query
  service/DTO/order/limit/redaction semantics, every other Activity/scoped/
  detail/mutation API, session/CSRF/scope/resource/preview registries,
  GovernanceService, CLI/QQ, application listener, config/env, schema,
  migrations, dependencies/lockfile/build, Provider, private/live data,
  deployment/restart/container behavior, and every prior slice.
- `assertions`: the named fail-first browser regression sees the complete C2
  assets but no Tool calls subview or B14D endpoint reference. Final assets
  preserve the C2 default, add semantic secondary navigation and fixed loading,
  empty, unavailable, table, count, refresh, and redaction states, call only the
  exact existing endpoint, strictly validate the bounded response, render no
  call/turn/user identifier or input/output payload, retain safe DOM and
  in-memory authority rules, and stay below the static byte bound. Browser QA
  at 1440x900 and 390x844 proves keyboard navigation through both Activity
  subviews, populated and empty tool-call lists, loading, refresh, bounded
  unavailable, expiry, logout, visible focus, 44px controls, stable labelled
  mobile rows, no clipping/overlap/overflow, no unexpected console/page/request
  error, and no external request. C2, B14D, complete HTTP, application wiring,
  canonical P8, type/lint/release, dependency/migration, inventory/privacy, and
  exact rollback gates remain green.
- `focused_commands`: unchanged named C2 browser and B14D route
  characterizations; one named C3 browser-surface regression fail-first and
  final; complete governance HTTP plus HTTP/application/wiring adjacency;
  canonical P8; typecheck; scoped and full lint; normal-umask release;
  `git diff --check`; dependency/migration/inventory/privacy hygiene; synthetic
  desktop/mobile Chromium QA; and exact six-path private reverse-first/
  forward-first proof.
- `schema_dependency_live_boundary`: fixed browser assets, synthetic static and
  browser tests, and canonical browser/security documentation only. No HTTP
  route or server/query/service/repository behavior, write, scope, mutation,
  schema, migration, dependency, lockfile, build, CLI/QQ, Provider, private DB,
  deployment, restart, container, or live operation is permitted.
- `rollback_boundary`: the exact C2 candidate for the six immutable product,
  test, and canonical-document paths plus this mutable checkpoint is captured
  byte-for-byte and mode-for-mode under mode-`0700`
  `/tmp/lethebot-gov-ux-01c3-roundtrip-PGs843`. All seven files are mode `0644`;
  baseline inventory is 74 tracked modifications, 60 untracked paths, and zero
  staged at `main@cfebb77`. Baseline SHA-256 values are
  `7e25284b683bd1834951630016a216946498f13f84bed26ac62b43533ea0efd5`,
  `4fb0ccef8c3e094e4f674cc9d434e5e62c12ebf06df4a7ef85f8874d6c483f25`,
  `2ea6470c639a95d730f528686ebdff6be75cd81aab2f5e7f14fa4a32b603488e`,
  `6617113e5292b3bab8ab8465cb17444dd31c65d45920d488ee9c5b6a0901cb84`,
  `7775550f0e10587bc29c8fb9cde1c092896096927420d86a44b96c590db7d598`,
  `8366d9ffe900c99960669980d30b2055d0a7c4fce8a3d971b7bdea865c707f2d`,
  and `2fca9651405781427bb37972c719de3d8fb5b2b0d8d6b7d9b06488f44a00dc3e`
  in allowed-path order. The final immutable six-path ASCII patch must prove
  both complete application orders without touching the workspace; checkpoint
  evidence remains excluded from that patch.
- `baseline_evidence`: the fresh resume `pnpm release:check` passed typecheck,
  lint, build, preflight, `git diff --check`, 137 / 1 skipped files, and 2814 /
  10 skipped tests. The unchanged C2 browser regression passed 1 / 1 with 16
  skipped, and the unchanged B14D route regression passed 1 / 1 with 31
  skipped. Mode-`0600` logs are
  `/tmp/lethebot-gov-ux-01c3-c2-characterization.log`
  (`sha256:f91e05037089b632c68afa190c35acd06384fc245377dbaea63b5a01f3c9d99b`)
  and `/tmp/lethebot-gov-ux-01c3-b14d-characterization.log`
  (`sha256:d0403cdcb095504393a92a10153a4a89b886509ddd9e46c543c5fb346d67cf9b`).
- `fail_first_evidence`: the named C3 run collected 18 HTTP cases, skipped 17
  unrelated cases, and failed only at its first assertion because the unchanged
  C2 HTML contains no `id="tool-calls-tab"`. Static assets still returned `200`,
  and no production asset or canonical behavior document had changed. The
  mode-`0600` log is `/tmp/lethebot-gov-ux-01c3-fail-first.log`
  (`sha256:b1057ebbac574fecce23471dd002e795505615d16262d9916dcc382b3a21242d`).
- `final_evidence`: the named C3 browser regression passed 1 / 1 with 17
  skipped, complete governance HTTP passed 18 / 18, HTTP/application/wiring
  adjacency passed 57 / 57, and canonical P8 passed 7 files / 317 tests.
  Typecheck, scoped and full ESLint, build, release preflight, and
  `git diff --check` passed. The normal-umask release gate passed 137 / 1
  skipped files and 2815 / 10 skipped tests; its mode-`0600` log is
  `/tmp/lethebot-gov-ux-01c3-release.log`
  (`sha256:e35151d14005026d1d895fb4271b29422c0b3466a7e4de0bb5c0f7740b377d9f`).
  The final fixed assets are 54,420 bytes and their embedded JavaScript parses.

  Synthetic Chromium at 1440x900 and 390x844 exercised the Model invocations
  default plus Tool calls loading, populated, empty, refresh, malformed,
  oversized, unavailable, stale-cancelled, expired, and logout states. It
  verified ArrowLeft/ArrowRight/Home/End tab behavior, main/tab/command focus,
  44px controls, exact rendered status/diagnostic/redaction/timing fields,
  omitted call/turn/canonical-user identifiers and payloads, labelled mobile
  rows, and no clipping, overlap, horizontal overflow, external request,
  failed request, page error, or unexpected console error. Visually reviewed
  mode-`0600` screenshots are
  `/tmp/lethebot-gov-ux-01c3-browser-desktop.png`
  (`sha256:1b32f975ecd7d8c305533c891c62c17f887f048a66e64d28a38c25daa71a7cae`)
  and `/tmp/lethebot-gov-ux-01c3-browser-mobile.png`
  (`sha256:6ceb9270a39fbbb31056406ddefcf25f52d471242e8003b41fa3d6e61f1edbd3`);
  the mode-`0600` browser log is
  `/tmp/lethebot-gov-ux-01c3-browser.log`
  (`sha256:aa25cd70ec05507fcf9fcffdef89cc68528b08ef7734bbafe85150bca8be388c`).

  The exact six-path ASCII patch is
  `/tmp/lethebot-gov-ux-01c3-roundtrip-PGs843/gov-ux-01c3.patch`
  (`sha256:a66fede8e6045b0dec8e6a4b972b74dbbfb43f0d7f591523c1723d89f9736c6f`,
  mode `0600`, size `37754`). Its exact inventory excludes this checkpoint and
  every protected path. Independent reverse-first and forward-first cycles
  restored all six paths byte-for-byte and mode-for-mode with mode-`0700`
  roots, mode-`0644` product files, and candidate/workspace equality. The first
  proof-harness attempt correctly rejected mode-`0600` post-apply files under a
  restrictive umask before completing a cycle; that evidence is preserved at
  mode `0700` under `/tmp/lethebot-gov-ux-01c3-roundtrip-failed-umask`. The
  corrected proof explicitly uses the normal patch-application umask and its
  mode-`0600` log is `/tmp/lethebot-gov-ux-01c3-roundtrip.log`
  (`sha256:c7707fe6c56b5b6914f926dd23173d9952914ebcf3134fdd74285a2bda744885`).
  Inventory remains 74 tracked modifications plus 60 untracked paths with zero
  staged at `main@cfebb77`; Pi packages remain `0.80.2`; migrations 007/008
  retain SHA-256
  `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`
  and `95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`;
  VCS-visible sensitive/reject/original filename hygiene is clean; and no
  backend route, service, repository, schema, dependency, CLI/QQ, Provider,
  private/live, deployment, restart, or container behavior changed.
- `exact_fail_first_action`: add only
  `exposes a fixed read-only tool-call Activity browser view` to
  `tests/integration/governance-http.test.ts`, then run it alone before editing
  the browser asset or canonical docs. Its first assertion must require the
  Tool calls secondary navigation control in the already-served HTML and fail
  because C2 contains only the Model invocations view; subsequent source
  assertions define the fixed states, table, exact endpoint, safe DOM, omitted
  identifier/payload rendering, and byte boundary without invoking a domain
  callback.
- `exact_next_action`: completed; the post-C2/C3 browser reassessment selects
  `GOV-UX-01C4`, a read-only Worker heartbeats Activity view over the existing
  bounded, details-hidden B14B endpoint. Reuse the proved list/tab pattern for
  the smallest operational-health list before adding Jobs, action pairs,
  audit/failures, scoped views, details, or mutations.

## Completed Slice: `GOV-UX-01C4`

- `requirement`: `GOV-UX-01C4` read-only browser Activity worker-heartbeat
  list.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: C3 proves the accessible bounded-list pattern for Tool calls,
  while the already-proved B14B worker-heartbeat endpoint remains available
  only through raw HTTP. The Activity browser has no worker-health subview even
  though Overview already signals aggregate worker errors.
- `selection_reason`: B14B is authenticated-unscoped, capped at 100, details-
  hidden, recursively redacted, write-free, and already wired. Its five-field
  operational projection is the smallest reuse of C3's strict list pattern and
  directly connects aggregate Overview attention to bounded evidence. Jobs,
  attempts, paired action history, audit/failures, scoped views, and mutations
  remain independent later slices.
- `implementation_contract`: extend only the fixed browser HTML, CSS, and local
  ES module. Activity retains Model invocations as its default and Tool calls as
  its existing second tab, then gains one keyboard-operable Worker heartbeats
  tab. Selecting or refreshing it sends one exact same-origin credentialed GET
  to `/governance/api/v1/activity/worker-heartbeats`, with no query, body, scope
  header, CSRF value, or new authority. Reuse the existing `401` expiry path,
  fixed unavailable state, request sequencing, safe DOM construction, refresh,
  count, loading, empty, responsive table, focus, and live-region behavior.

  Accept at most 100 records containing only bounded `workerId`, `workerType`,
  fixed `idle`/`running`/`stopping`/`error` status, optional bounded
  `currentJobId`, and one valid ISO heartbeat date. Reject unknown keys,
  details, malformed fields/dates, and oversized arrays as unavailable. Render
  fixed Worker, Status, Current job, and Last heartbeat columns using only DOM
  creation and `textContent`; render the redacted worker/type/current-job
  strings but never `details`. Mobile rows keep stable visible labels and no
  horizontal scroll. Add no worker age inference, filter, detail link, caller
  limit, router, chart, dependency, storage, cookie read, dynamic HTML/code,
  external request, backend route, or DTO change.
- `allowed_paths`: `src/http/governance-browser-assets.ts`,
  `tests/integration/governance-http.test.ts`, `docs/architecture.md`,
  `docs/deployment.md`, `docs/governance-ui.md`, `docs/security-privacy.md`, and
  this checkpoint only.
- `protected_paths`: `GovernanceHttpServer`, `src/index.ts`, B14B route/query
  service/DTO/order/limit/redaction semantics, C1-C3 behavior, every other
  Activity/scoped/detail/mutation API, session/CSRF/scope/resource/preview
  registries, GovernanceService, CLI/QQ, application listener, config/env,
  schema, migrations, dependencies/lockfile/build, Provider, private/live data,
  deployment/restart/container behavior, and every prior slice.
- `assertions`: unchanged C3 browser and B14B route characterizations pass
  before editing. One named fail-first browser regression sees complete C3
  assets but no Worker heartbeats tab or endpoint reference. Final fixed assets
  preserve both existing subviews, add semantic roving-tab navigation and
  fixed loading/empty/unavailable/count/refresh/table states, call only the
  exact B14B endpoint, strictly validate its bounded response, render no
  details, retain safe DOM/in-memory authority rules, and stay below the static
  byte bound. Chromium at 1440x900 and 390x844 must cover populated, empty,
  loading, refresh, malformed, oversized, unavailable, stale cancellation,
  expiry, logout, keyboard tabs, visible focus, 44px controls, labelled mobile
  rows, layout/network/error hygiene, and visual review. C3, B14B, HTTP/wiring,
  canonical P8, type/lint/release, dependency/migration/inventory/privacy, and
  exact rollback gates remain green.
- `focused_commands`: named C3 browser and B14B route characterizations; one
  named C4 browser-surface regression fail-first and final; complete governance
  HTTP plus HTTP/application/wiring adjacency; canonical P8; typecheck; scoped
  and full lint; normal-umask release; `git diff --check`; dependency/migration/
  inventory/privacy hygiene; synthetic desktop/mobile Chromium QA; and an exact
  six-path private reverse-first/forward-first proof.
- `schema_dependency_live_boundary`: fixed browser assets, synthetic tests, and
  canonical browser/security documentation only. No backend, persistence,
  schema, migration, dependency, lockfile, build contract, CLI/QQ, Provider,
  private DB, deployment, restart, container, or live operation is permitted.
- `rollback_boundary`: the exact C3 candidate for the six immutable product,
  test, and canonical-document paths plus this mutable checkpoint is captured
  byte-for-byte and mode-for-mode under mode-`0700`
  `/tmp/lethebot-gov-ux-01c4-roundtrip-MroTXn`. All seven files are mode `0644`;
  baseline inventory is 74 tracked modifications, 60 untracked paths, and zero
  staged at `main@cfebb77`. Baseline SHA-256 values are
  `bf52f88e8f06f4a56a493219eb64e9ccc37ad48bf6e8c97fe35500377373b4f7`,
  `8fd53e7f54c6cff2f0bd67d9379f0b9148f8623357724260b8d675eb56443e1a`,
  `be9c2e3569521f1731141be667e41cdae68aa3abbc3ab0a4e445180517a395ff`,
  `5ba268f4ef8f82d5dc23316fbdc947e01b2f2494c5cfb8a737ff4bf48a18dcfe`,
  `149b52f1735f2e97dc7252cf996af88374a02233f8311e6b612aed5ec008f461`,
  `8a91b3b87ae73bcb971106457ad637da64cc5a4c40b486d39526da12efa1a3fc`,
  and `bdb7fbfd825ca95cad0ec1f1bf4b337ec0062d4cafd3d5530bf6d4901864cd3b`
  in allowed-path order. The final immutable six-path ASCII patch must
  independently prove complete reverse-first and forward-first byte/mode
  cycles without touching the workspace; checkpoint evidence remains excluded.
- `baseline_evidence`: the unchanged C3 browser regression passed 1 / 1 with
  17 skipped, and the unchanged B14B route regression passed 1 / 1 with 31
  skipped. Their mode-`0600` logs are
  `/tmp/lethebot-gov-ux-01c4-c3-characterization.log`
  (`sha256:4d5d0e555f0db9612d92dd3f18f2b2f408c008c18affa14a9ed0f51b478b1985`)
  and `/tmp/lethebot-gov-ux-01c4-b14b-characterization.log`
  (`sha256:84e2e7dfc6f9bb59edefcfa9b445dda3e0cc6f5b300be36d05541293cf4fd8ec`).
- `fail_first_evidence`: the named C4 run collected 19 HTTP cases, skipped 18
  unrelated cases, and failed only at its first assertion because the unchanged
  C3 HTML contains no `id="worker-heartbeats-tab"`. Static assets still returned
  `200`; every production asset and canonical behavior document remained equal
  to the captured baseline. The mode-`0600` log is
  `/tmp/lethebot-gov-ux-01c4-fail-first.log`
  (`sha256:dc6b89a6a07f6d98c0090dd5585770e45aba6c3617d33cbad42047b3e82b3066`).
- `exact_fail_first_action`: after baseline capture and the two unchanged
  characterizations, add only `exposes a fixed read-only worker-heartbeat
  Activity browser view` to `tests/integration/governance-http.test.ts`. Its
  first assertion must require `id="worker-heartbeats-tab"` and fail because C3
  has only Model invocations and Tool calls; subsequent source assertions define
  the fixed states, table, exact endpoint, strict/safe rendering, details
  exclusion, and byte boundary without invoking a domain callback.
- `final_evidence`: the named C4 regression passed 1 / 1 with 18 skipped, full
  governance HTTP passed 19 / 19, HTTP/application/wiring adjacency passed 6
  files / 49 tests, and the final canonical P8 gate passed 7 files / 318 tests.
  The unchanged C3 and B14B characterizations each passed 1 / 1. Typecheck,
  scoped and full ESLint, build, release preflight, and `git diff --check`
  passed. The normal-umask release gate passed 137 / 1 skipped files and 2816 /
  10 skipped tests; its mode-`0600` log is
  `/tmp/lethebot-gov-ux-01c4-release.log`
  (`sha256:53f0afaa873503681d878431ae7c8a7f2d27c6e70e7ad78e9b92a85fbea6a509`).
  The final fixed assets are 65,491 bytes and their embedded JavaScript parses.

  Synthetic Chromium at 1440x900 and 390x844 exercised Model invocations as the
  default, Tool calls as the second tab, and Worker heartbeats loading,
  populated, empty, refresh, malformed-date, details-rejected, oversized,
  unavailable, stale-cancelled, expired, and logout states. It verified all four
  worker statuses, optional current jobs, safe text rendering, ArrowLeft/
  ArrowRight/Home/End navigation, visible focus, 44px controls, equal wrapped
  mobile tabs, labelled mobile rows, and no clipping, overlap, page overflow,
  external request, failed request, page error, or unexpected console error.
  Visually reviewed mode-`0600` screenshots are
  `/tmp/lethebot-gov-ux-01c4-browser-desktop.png`
  (`sha256:dd397a11636750f24284c9d7a09f3229b66fc415ca3172f10e252a0269c5d2f0`)
  and `/tmp/lethebot-gov-ux-01c4-browser-mobile.png`
  (`sha256:dbff0b5cbdd55b01dc22a581177386ae98d9f4260592b6d5a9888557bb951367`);
  the mode-`0600` browser log is
  `/tmp/lethebot-gov-ux-01c4-browser.log`
  (`sha256:2cf008ca0b2bdb24dc406d6fa897e0a278c10bbb68ac31a0d1d9d90d6e16de0a`).

  The exact six-path ASCII patch is
  `/tmp/lethebot-gov-ux-01c4-roundtrip-MroTXn/gov-ux-01c4.patch`
  (`sha256:9ad51f1583ae5268880bf7cfc9b274ce180edba7d92657bf6cb094081bc90efc`,
  mode `0600`, size `35822`). Independent forward-first and reverse-first
  cycles restored the captured C3 baseline and C4 candidate byte-for-byte and
  mode-for-mode under mode-`0700` proof trees with mode-`0644` product files;
  the workspace stayed equal to the candidate. The mode-`0600` proof log is
  `/tmp/lethebot-gov-ux-01c4-roundtrip.log`
  (`sha256:7a4c62b6ba76a45b40b1e127bfd2f6d729c7747d162446aff929b98facd62b08`).
  Inventory remains 74 tracked modifications plus 60 untracked paths with zero
  staged at `main@cfebb77`; Pi packages remain `0.80.2`; migrations 007/008
  retain SHA-256
  `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`/
  `95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`;
  VCS-visible sensitive/reject/original filename hygiene is clean; and no
  backend, route, DTO, service, repository, schema, dependency, CLI/QQ,
  Provider, private/live, deployment, restart, or container behavior changed.
- `exact_next_action`: completed; the required post-C3/C4 browser reassessment
  selects `GOV-UX-01C5A`, a behavior-preserving fixed-asset compaction
  prerequisite. C4 leaves only 45 bytes below the established 65,536-byte
  initial-asset ceiling, so adding the otherwise dependency-ready Jobs view
  first would violate the existing bounded-shell contract.

## Active Slice: `GOV-UX-01C5A`

- `requirement`: `GOV-UX-01C5A` behavior-preserving browser asset compaction.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: the final C4 HTML, CSS, and JavaScript total 65,491 bytes,
  only 45 bytes below the strict 65,536-byte initial-shell bound. B14C Jobs is
  the next dependency-ready Activity list, but even its fixed tab and empty/
  unavailable states cannot fit without first removing formatting-only bytes.
- `selection_reason`: C3 and C4 are two consecutive browser-list slices, so the
  program requires a full P8 critical-path reassessment. The remaining
  Memory, Explain, Activity, and Privacy & Operations views all need bounded
  client code. Compacting the three already-fixed static bodies is the smallest
  reversible prerequisite; it adds no route, dependency, product behavior, or
  authority and avoids weakening the established size ceiling.
- `implementation_contract`: add one deterministic local compactor in
  `governance-browser-assets.ts` and apply it only when constructing the three
  existing HTML, CSS, and JavaScript assets. Remove leading horizontal
  indentation from each line, collapse repeated blank lines, and trim outer
  whitespace. Do not rewrite tokens, selectors, markup nodes, visible text,
  paths, headers, content types, CSP, request behavior, DOM state, or source
  template readability. No generic minifier, parser, dependency, new static
  path, lazy loader, dynamic import, or runtime-configured transformation is
  allowed.
- `allowed_paths`: `src/http/governance-browser-assets.ts`,
  `tests/integration/governance-http.test.ts`, and this checkpoint only.
- `protected_paths`: all canonical behavior documents; `GovernanceHttpServer`,
  `src/index.ts`, every route/query/service/DTO/repository and Activity payload;
  session/CSRF/scope/resource/preview behavior; CLI/QQ; application listener;
  config/env; schema/migrations; dependencies/lockfile/build contract;
  Provider, private/live data, deployment/restart/container behavior; C1-C4
  visible, accessibility, request, and security behavior; and every prior slice.
- `assertions`: unchanged C4 static and full governance HTTP tests plus the
  final C4 Chromium harness pass before editing. A named fail-first regression
  requires all three served bodies to have no leading horizontal indentation,
  no repeated blank lines, and a combined size below 58,000 bytes while
  preserving the existing exact-path/content-type/security/callback rules. The
  final bodies remain deterministic and parseable, all C1-C4 tests pass, and
  the same desktop/mobile harness produces layout-equivalent populated C4
  screenshots and covers every prior state. Canonical P8, type/lint/release,
  inventory/privacy, dependency/migration, and exact rollback gates remain
  green.
- `focused_commands`: unchanged named C4 regression and full governance HTTP
  characterization; one named C5A fail-first and final regression; embedded
  JavaScript parse and exact byte measurement; the unchanged C4 desktop/mobile
  Chromium harness; canonical P8; typecheck; scoped/full lint; normal-umask
  release; `git diff --check`; dependency/migration/inventory/privacy hygiene;
  and exact two-path private reverse-first/forward-first proof.
- `schema_dependency_live_boundary`: fixed formatting of the three current
  browser response bodies plus synthetic tests and checkpoint evidence only. No
  visible/browser-request behavior, canonical contract, route, backend,
  persistence, schema, migration, dependency, lockfile, build contract, CLI/QQ,
  Provider, private DB, deployment, restart, container, or live operation may
  change.
- `rollback_boundary`: the exact C4 candidate for the two immutable product/test
  paths plus this mutable checkpoint was captured byte-for-byte and mode-for-
  mode under mode-`0700`
  `/tmp/lethebot-gov-ux-01c5a-roundtrip-MGNRDr` before the fail-first edit. All
  three files are mode `0644`; their baseline SHA-256 values in allowed-path
  order are `fba2bca7dc9c24ef8b28bc849cabf2bb49362f94a3d5390d7eb7d05373355cdc`,
  `a4920988fec2ca194ed34e3e1991dd45d2f24c7385c5e1b7908f0e5c54fa775a`,
  and `4a21e7e59dfee2a2891d91bbf7b5a4c671d845eeb672507eddff5c44e9d55a84`.
  The final checkpoint is excluded from the immutable two-path patch.
- `baseline_evidence`: the unchanged named C4 regression passed 1 / 1 with 18
  skipped and the complete unchanged governance HTTP file passed 19 / 19. Their
  mode-`0600` logs are `/tmp/lethebot-gov-ux-01c5a-c4-characterization.log`
  (`sha256:1c4f8a08e9a92385c7f668470f23a236a464c74c50e6708bb4378c79c6ddd890`)
  and `/tmp/lethebot-gov-ux-01c5a-http-characterization.log`
  (`sha256:90696898784f77ca6ab137c7a7429f453aa57bbf42beb1e353e778c1ec48e61a`).
  The unchanged C4 Chromium harness also passed both viewports and every prior
  state with zero unexpected browser failures; its mode-`0600` log is
  `/tmp/lethebot-gov-ux-01c5a-browser-characterization.log`
  (`sha256:2cf008ca0b2bdb24dc406d6fa897e0a278c10bbb68ac31a0d1d9d90d6e16de0a`).
- `exact_fail_first_action`: after baseline capture and unchanged C4/full-HTTP/
  browser characterizations, add only `serves compact fixed browser assets with
  bounded extension headroom` to `tests/integration/governance-http.test.ts`.
  Its first assertion must require a combined size below 58,000 bytes and fail
  against the 65,491-byte C4 assets before production changes.
- `fail_first_evidence`: the named C5A run collected 20 HTTP cases, skipped 19,
  and failed only at its first assertion because the unchanged C4 bodies total
  65,491 bytes rather than less than 58,000. The asset module and checkpoint
  remained byte-equal to the captured baseline. The mode-`0600` log is
  `/tmp/lethebot-gov-ux-01c5a-fail-first.log`
  (`sha256:ad195b0de59b1e054ae7ed0c27d2d55ee0cef9358374a55a9f0ce73d31aa190d`).
- `final_evidence`: one local `compactAssetBody` removes line-leading horizontal
  whitespace, repeated newlines, and outer whitespace only while the three
  fixed assets are constructed. The named C5A regression passed 1 / 1 with 19
  skipped, complete governance HTTP passed 20 / 20, HTTP/application/wiring
  adjacency passed 6 files / 49 tests, and canonical P8 passed 7 files / 319
  tests. Typecheck, scoped and full ESLint, build, preflight, and final diff
  hygiene passed. The final served HTML/CSS/JavaScript sizes are
  12,438/14,545/30,623 bytes, 57,606 total; every compactness invariant holds
  and the extracted JavaScript parses.

  Rebuilt Chromium at 1440x900 and 390x844 covered the complete C4 state,
  keyboard, focus, request, and layout matrix with zero unexpected console,
  page, failed-request, or external-request events. Pre/post desktop and mobile
  screenshots are byte-identical. The final mode-`0600` screenshots are
  `/tmp/lethebot-gov-ux-01c5a-browser-desktop.png`
  (`sha256:55907f3ab2801c3e03089f97083f13f86addd3a5d1df49096cdf608e6493b6db`)
  and `/tmp/lethebot-gov-ux-01c5a-browser-mobile.png`
  (`sha256:dbff0b5cbdd55b01dc22a581177386ae98d9f4260592b6d5a9888557bb951367`).

  The first normal-umask release attempt passed 2,816 tests and hit one
  process-level application-release rehearsal failure unrelated to the two C5A
  paths. That exact case then passed 1 / 1 in isolation, and the complete
  normal-umask rerun passed 137 / 1 skipped files and 2,817 / 10 skipped tests.
  The final mode-`0600` release log is
  `/tmp/lethebot-gov-ux-01c5a-release.log`
  (`sha256:10f6df2ae327717d99cde0aa4730431bea36f41991317a5e228f852650751800`).

  The exact two-path ASCII patch is
  `/tmp/lethebot-gov-ux-01c5a-roundtrip-MGNRDr/gov-ux-01c5a.patch`
  (`sha256:05308602bead9d2df0d24390ec91d8d4c211bd97476aefdee87b72697f35abdf`,
  mode `0600`, size `4021`). Independent forward-first and reverse-first cycles
  restored the captured C4 baseline and C5A candidate byte-for-byte and mode-
  for-mode under mode-`0700` proof trees with mode-`0644` product files; the
  workspace stayed equal to the candidate. The proof log is
  `/tmp/lethebot-gov-ux-01c5a-roundtrip.log`
  (`sha256:67887e1bee3ece234323b6de7b363dbc55e2e71c24a8f75617bdb0d57c55b7b7`).
  Inventory remains 74 tracked modifications plus 60 untracked paths with zero
  staged at `main@cfebb77`; Pi remains `0.80.2`, migration hashes are unchanged,
  VCS-visible sensitive/reject/original filename hygiene is clean, and no
  canonical contract, backend, route, persistence, dependency, CLI/QQ,
  Provider, private/live, deployment, restart, or container behavior changed.
- `exact_next_action`: completed; the reclaimed 7,930-byte shell headroom makes
  the existing B14C Jobs list the smallest dependency-ready next browser view,
  selected below as `GOV-UX-01C5B`.

## Completed Slice: `GOV-UX-01C5B`

- `requirement`: `GOV-UX-01C5B` read-only Jobs Activity browser view.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: authenticated B14C already exposes the bounded, payload-free
  Jobs list at `/governance/api/v1/activity/jobs`, but the browser Activity view
  shows only Model invocations, Tool calls, and Worker heartbeats. Operators
  cannot inspect pending, running, completed, or failed job lifecycle evidence
  without leaving the browser workflow.
- `selection_reason`: C5A reduced the fixed shell to 57,606 bytes while leaving
  its 65,536-byte ceiling intact. Jobs is the already-proved counterpart to the
  current Worker heartbeats view, needs no new backend authority or dependency,
  and is smaller and more operationally useful than starting an unproved
  mutation, scoped-memory, privacy, or Explain browser workflow.
- `implementation_contract`: add Jobs as the fourth Activity tab after Worker
  heartbeats while preserving Model invocations as the default, Tool calls
  second, and Worker heartbeats third. The fixed client calls only authenticated
  same-origin `GET /governance/api/v1/activity/jobs`; accepts an array of at
  most 100 exact records; and strictly validates required `id`, `type`, `status`,
  `attempts`, `maxAttempts`, `createdAt`, `updatedAt`, and `scheduledAt` fields
  plus optional `idempotencyKey`, `leaseOwner`, `leaseExpiresAt`, `heartbeatAt`,
  `startedAt`, `completedAt`, and `error`. Reject unknown keys, `payload`,
  `result`, invalid statuses/numbers/dates/strings, and oversized arrays before
  rendering any row.

  Render fixed loading, empty, unavailable, count, refresh, and responsive
  semantic table states using DOM creation and `textContent`. Show the redacted
  job/type/status, attempt progress, schedule/update lifecycle, and applicable
  lease, heartbeat, completion, and error evidence without inference or raw
  payload/result. Preserve shared request sequencing, stale cancellation,
  session expiry/logout, roving-tab keyboard behavior, focus, mobile labels, and
  the compact fixed-body construction. Add no filter, detail link, caller limit,
  polling, router, chart, dependency, cookie read, dynamic HTML/code, external
  request, backend route, or DTO change.
- `allowed_paths`: `src/http/governance-browser-assets.ts`,
  `tests/integration/governance-http.test.ts`, `docs/architecture.md`,
  `docs/deployment.md`, `docs/governance-ui.md`, `docs/security-privacy.md`, and
  this checkpoint only.
- `protected_paths`: `GovernanceHttpServer`, `src/index.ts`, the B14C
  route/query-service/DTO/order/limit/redaction semantics, C1-C5A behavior,
  every other Activity/scoped/detail/mutation API, session/CSRF/scope/resource/
  preview registries, GovernanceService, CLI/QQ, application listener,
  config/env, schema, migrations, dependencies/lockfile/build contract,
  Provider, private/live data, deployment/restart/container behavior, and every
  prior slice.
- `assertions`: unchanged named C5A browser and B14C route characterizations
  pass before editing. One named fail-first browser regression sees complete
  C5A assets but no Jobs tab or endpoint reference. Final fixed assets preserve
  the first three Activity tabs, add accessible fourth-tab navigation and all
  fixed Jobs states, call only the exact B14C endpoint, strictly validate its
  bounded response, omit payload/result, retain safe DOM/in-memory authority
  rules, and remain below 65,536 bytes. Chromium at 1440x900 and 390x844 covers
  populated, empty, loading, refresh, malformed, oversized, unavailable, stale
  cancellation, expiry, logout, keyboard tabs, visible focus, 44px controls,
  labelled mobile rows, and layout/network/error hygiene. C5A, B14C,
  HTTP/wiring, canonical P8, type/lint/release, dependency/migration/inventory/
  privacy, and exact rollback gates remain green.
- `focused_commands`: named C5A browser and B14C route characterizations; one
  named C5B browser regression fail-first and final; complete governance HTTP
  plus HTTP/application/wiring adjacency; embedded JavaScript parse and exact
  byte measurement; expanded desktop/mobile Chromium QA; canonical P8;
  typecheck; scoped/full lint; normal-umask release; `git diff --check`;
  dependency/migration/inventory/privacy hygiene; and an exact six-path private
  reverse-first/forward-first proof.
- `schema_dependency_live_boundary`: fixed browser assets, synthetic tests, and
  canonical browser/security documentation only. No backend, persistence,
  schema, migration, dependency, lockfile, build contract, CLI/QQ, Provider,
  private DB, deployment, restart, container, or live operation may change.
- `rollback_boundary`: capture the exact C5A candidate for the six immutable
  product/test/canonical-document paths plus this mutable checkpoint byte-for-
  byte and mode-for-mode before the fail-first edit. The final immutable six-
  path ASCII patch must independently prove reverse-first and forward-first
  byte/mode cycles without touching the workspace; checkpoint evidence remains
  excluded.
- `exact_fail_first_action`: after baseline capture and unchanged C5A/B14C
  characterizations, add only `exposes a fixed read-only job Activity browser
  view` to `tests/integration/governance-http.test.ts`. Its first assertion must
  require `id="jobs-tab"` and fail because C5A has only the first three tabs;
  subsequent assertions define the fixed states, exact endpoint, strict/safe
  rendering, payload/result exclusion, and byte boundary without invoking a
  domain callback.
- `baseline_evidence`: mode-`0700`
  `/tmp/lethebot-gov-ux-01c5b-roundtrip-zOYCt0` captures the exact seven-file
  C5A baseline byte-for-byte and mode-for-mode. Its manifest records
  `main@cfebb77`, 74 tracked modifications, 60 untracked paths, zero staged,
  and mode-`0644` source files. The unchanged named C5A browser and B14C route
  characterizations each passed 1 / 1. Their mode-`0600` logs are
  `/tmp/lethebot-gov-ux-01c5b-c5a-characterization.log`
  (`sha256:9276d4e615b6728aff8e10df7992d8e43b96cc242e9006b4f4611edb617121e4`)
  and `/tmp/lethebot-gov-ux-01c5b-b14c-characterization.log`
  (`sha256:2623a5b8ad32431892f40b4e2816a6e33b294b0a82687803fece8b7e3a2fc83f`).
- `fail_first_evidence`: the named C5B run collected 21 HTTP cases, skipped 20,
  and failed only because the unchanged C5A HTML lacked `id="jobs-tab"`. The
  existing first three tabs, B14C route, production asset, and canonical docs
  remained at the captured baseline. The mode-`0600` log is
  `/tmp/lethebot-gov-ux-01c5b-fail-first.log`
  (`sha256:1b81e097bf9c0580d53c6b926bf7f54c8da1fa5bfe5fb69b6ce12ddb6e2da0a1`).
- `final_evidence`: the final named C5B, unchanged C5A, and unchanged B14C
  regressions each passed 1 / 1. Complete governance HTTP passed 21 / 21, exact
  HTTP/application/wiring adjacency passed 6 files / 49 tests, and canonical
  P8 passed 7 files / 320 tests. Typecheck, scoped and full ESLint, build,
  release preflight, and `git diff --check` passed. The normal-umask release
  gate passed 137 / 1 skipped files with 2818 / 10 skipped tests; its private
  mode-`0600` log is `/tmp/lethebot-gov-ux-01c5b-release.log`
  (`sha256:e55dfc9ea8917a27b3fcb637486d79b173e069bb8acc8de5307989aab8735920`).

  The final fixed HTML/CSS/JavaScript bodies are 14,047/14,794/36,473 UTF-8
  bytes, 65,314 total with 222 bytes below the permanent 65,536-byte boundary;
  the extracted JavaScript parses. Chromium at 1440x900 and 390x844 made 34
  requests per viewport and covered every Worker and Jobs loading, populated,
  empty, malformed, hidden-payload, oversized, unavailable, stale, expiry,
  logout, keyboard, focus, mobile-row, and layout state with zero page errors,
  failed requests, or external requests. The mode-`0600` browser log is
  `/tmp/lethebot-gov-ux-01c5b-browser.log`
  (`sha256:d5c5a97dbe79df57eebe0fb2e7f38d28d45887e4ae1c4707bcc211dccdac8e19`);
  final screenshots are
  `/tmp/lethebot-gov-ux-01c5b-browser-desktop.png`
  (`sha256:bb8bef9e9d4fe7d2daa818fb166e28f90cdd0b8a5f474ecc3e440b38ece5c79d`)
  and `/tmp/lethebot-gov-ux-01c5b-browser-mobile.png`
  (`sha256:ff857ac1dfac5236b3770d8c6b8622cbe9c04818e4a2330fbe28ffa4d8fd8f85`).

  The exact six-path ASCII patch is
  `/tmp/lethebot-gov-ux-01c5b-proof-nVHZXT/gov-ux-01c5b.patch`
  (`sha256:1c19775507365b61054c106456ba40e358710a00845c8da7b3f4f2120f684aab`,
  mode `0600`, size `27408`). Independent reverse-first and forward-first
  cycles restored the captured C5A baseline and C5B candidate byte-for-byte
  and mode-for-mode under mode-`0700` proof trees with mode-`0644` snapshots;
  before/after workspace manifests are identical. Inventory remains 74/60/0
  at `main@cfebb77`; Pi dependencies remain `0.80.2`; migrations 007/008 retain
  SHA-256
  `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`/
  `95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`;
  sensitive/reject/original filename and patch-content hygiene are clean.
- `exact_next_action`: completed; after the consecutive C5A/C5B browser slices,
  the full P8 critical-path reassessment leaves only 222 bytes under the fixed
  shell boundary and selects the HTTP-disabled display-profile user catalog
  below. Do not weaken the shell bound or add another browser view first.

## Completed Slice: `GOV-UX-01B26A`

- `requirement`: `GOV-UX-01B26A` HTTP-disabled display-profile exact-user scope
  catalog.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: B25A owns the shared exact-user/global-or-group display-profile
  redaction mutation, but the future preview/confirm workflow has no bounded
  identifier-free target discovery. Reusing the Privacy catalog would grant a
  different purpose and include users with no profile/history evidence; using
  raw canonical-user selectors would expose identifiers and bypass the existing
  scope-handle boundary.
- `selection_reason`: C5A/C5B are two consecutive browser-shell slices and the
  fixed initial shell now has only 222 bytes of headroom. The remaining Activity
  APIs are already bounded, while display-profile redaction still lacks its
  first authority-safe prerequisite. A read-only, HTTP-disabled exact-user
  catalog is the smallest independent dependency and adds no route, mutation,
  registry type, or frontend byte.
- `implementation_contract`: add only
  `GovernanceQueryService.listDisplayProfileScopeHandles(issueHandle)`. Read the
  distinct canonical users represented by at least one `display_profiles` or
  `nickname_history` row. Validate one exact 1-256-character, fully trimmed,
  control-free text identifier before deduplication; order valid users by raw
  canonical identifier only after the union; and apply a fixed 101-row probe
  returning at most 100. For each user, pass only
  `{ kind: 'user', canonicalUserId }` to the trusted issuer for future purpose
  `governance.display_profile.targets.read`. Return only the existing
  16-character user-scope fingerprint domain, fixed `user` scope kind, fixed
  `User display data` label, opaque handle, expiry, and catalog truncation.
  Users represented only by profile rows or only by history rows remain
  discoverable; duplicates across tables collapse. Canonical users without any
  profile/history evidence remain absent. Register no HTTP route and expose no
  profile name, group identifier, history text, counts, timestamps, trust, raw
  user/session values, or issuer extras.
- `allowed_paths`: `src/governance/query-service.ts`,
  `tests/unit/governance/query-service.test.ts`, `docs/governance-ui.md`, and
  this checkpoint only.
- `protected_paths`: B25A/B25B shared mutations and CLI behavior; all Privacy,
  Memory, Explain, summary-policy, Operations, and Activity projections; scope/
  resource/preview registry implementations; every HTTP/application route and
  browser asset; identity repository behavior; schema, migrations, dependencies,
  lockfile, config, QQ, Provider, private/live data, deployment, restart,
  container, retention, and every prior slice.
- `assertions`: synthetic profile-only, history-only, duplicate, unrelated
  canonical-user, and malformed legacy identifiers prove union, filtering,
  deduplication, stable raw-ID ordering, and exact 100/101 truncation before
  issuance. A real scope registry resolves every handle only for its issuing
  session and `governance.display_profile.targets.read`; another session and
  Privacy, Memory, or maintenance purposes fail. Repeated calls are byte-stable;
  exact DTO keys omit every raw user/group/profile/history/session/secret value
  and issuer extra. Reads produce zero writes, integrity is `ok`, FKs are empty,
  B25A/B25B and existing catalogs remain unchanged, and no HTTP or browser route
  appears.
- `focused_commands`: add and run one named fail-first query-service regression,
  then its final form; run complete query-service and scope-registry adjacency;
  canonical P8; typecheck; scoped/full ESLint; build/preflight/release;
  `git diff --check`; dependency/migration/inventory/privacy hygiene; and exact
  three-path private reverse-first/forward-first proof.
- `schema_dependency_live_boundary`: one read-only query-service catalog,
  synthetic tests, canonical contract, and checkpoint only. No persistence
  write or format change, repository behavior, route, mutation, registry
  implementation, schema, migration, dependency, lockfile, browser, CLI/QQ,
  Provider, private DB, deployment, restart, container, or live operation is
  permitted.
- `rollback_boundary`: before the fail-first edit, capture the exact C5B
  candidate for the three immutable product/test/canonical-document paths plus
  this mutable checkpoint byte-for-byte and mode-for-mode under a new mode-
  `0700` private root. The final three-path ASCII patch must independently prove
  reverse-first and forward-first byte/mode cycles without touching the
  workspace; checkpoint evidence remains excluded.
- `exact_fail_first_action`: after baseline capture, add only
  `issues a bounded display-profile user catalog without exposing identifiers`
  to `tests/unit/governance/query-service.test.ts`. Its first invocation must
  fail only because `service.listDisplayProfileScopeHandles` is absent after the
  synthetic fixture and every union/filter/bound/privacy/registry/no-write/FK
  assertion are established.
- `baseline_evidence`: mode-`0700`
  `/tmp/lethebot-gov-ux-01b26a-roundtrip-UpCQdY` captured the exact C5B
  candidate for the three immutable product/test/canonical-document paths plus
  this checkpoint byte-for-byte and mode-for-mode before the regression. The
  immutable source files were mode `0644`; inventory was 74 tracked
  modifications, 60 untracked paths, and zero staged at `main@cfebb77`.
- `fail_first_evidence`: the named focused run collected 47 query-service
  cases, skipped 46 unrelated cases, and failed only because
  `service.listDisplayProfileScopeHandles is not a function`. The synthetic
  fixture and every union, malformed-ID, bound, registry-isolation, omission,
  no-write, integrity, and FK assertion were already present; production source
  and the canonical contract remained equal to the captured baseline.
- `final_evidence`: the named final regression passed 1 / 1 with 46 skipped,
  full query-service plus scope-registry adjacency passed 50 / 50, and canonical
  P8 passed 7 files / 321 tests. Typecheck, scoped and full ESLint, build, release
  preflight, and `git diff --check` passed. The first unchanged release attempt
  reached one environment-only `EADDRINUSE` collision at `127.0.0.1:19825`;
  its exact unchanged timeout case passed alone, then the corrected complete
  release passed 137 / 1 skipped files and 2819 / 10 skipped tests.

  The query service now validates and unions exact current-profile and nickname-
  history user evidence before deduplication, orders by raw user ID, probes 101
  rows, and issues at most 100 purpose-bound exact-user handles. Profile-only
  and history-only users remain discoverable; malformed, duplicate, unrelated,
  and 101st evidence cannot expand authority. Fixed entries expose only the
  existing user-scope fingerprint, kind/label, opaque handle/expiry, and
  truncation; raw user/group/profile/history/session values and issuer extras
  remain absent. Reads are stable and write-free with clean integrity/FKs; no
  route, registry implementation, mutation, schema, dependency, or persistence
  format changed.

  The exact three-path ASCII patch is
  `/tmp/lethebot-gov-ux-01b26a-roundtrip-UpCQdY/gov-ux-01b26a.patch`
  (`sha256:76531a80532df1499ecc528323323fee13d28aad9852cb877e12c101bb23a031`,
  mode `0600`, size `14177`). Independent reverse-first and forward-first
  cycles restored the captured C5B baseline and B26A candidate byte-for-byte
  and mode-for-mode without touching the workspace. Inventory remains 74/60/0
  at `main@cfebb77`; Pi dependencies remain `0.80.2`; migrations 007/008 retain
  SHA-256
  `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`/
  `95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`;
  patch, inventory, and sensitive-filename hygiene are clean.
- `exact_next_action`: completed; the catalog authority prerequisite selects
  the HTTP-disabled exact-user target page below before any route, preview,
  confirmation, mutation overload, or browser work.

## Completed Slice: `GOV-UX-01B26B`

- `requirement`: `GOV-UX-01B26B` HTTP-disabled exact-user display-profile
  target page.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: B26A can discover an exact user and issue future target-read
  authority, while B25A requires a separate exact global-or-group target. No
  fixed identifier-free projection enumerates which targets have current-
  profile or history evidence, so a future preview would either accept a raw
  group selector or expose unrestricted profile/history rows.
- `selection_reason`: the exact-user catalog is independently proved and no
  HTTP route consumes it yet. A bounded target page is the smallest next
  dependency: it establishes exact-user selection, safe target classification,
  and finite evidence metadata before designing resource authority, preview,
  expected-snapshot mutation, confirmation, or browser behavior.
- `implementation_contract`: add only
  `GovernanceQueryService.listDisplayProfileTargetsForScope(scope)`. Accept one
  exact B26A-valid user scope; every other scope or malformed user returns the
  fixed empty page before a profile/history read. For that exact user, union
  valid `source_group_id` targets represented in `display_profiles` or
  `nickname_history`. The exact empty string denotes one private/global target;
  a non-empty target must be 1-256 characters, fully trimmed, and control-free.
  Discard malformed target or metadata evidence, deduplicate across and within
  tables, order private/global first and groups by raw ID, probe 101 distinct
  targets, and return at most 100.

  Each fixed entry contains only a domain-separated 16-character target
  fingerprint, fixed `private_or_global` or `group` kind and corresponding
  label, current-profile presence plus valid trust classification and observed
  date (or fixed nulls), and nickname-history metadata. History metadata has a
  count capped at 100 plus truncation, fixed `absent`/`open`/`closed`/`mixed`
  lifecycle classification, and latest valid observed date. Valid dates are
  safe integer milliseconds in the JavaScript Date range; a non-null history
  end must also be safe and no earlier than its observation. Select or return no
  profile/history text. Fingerprint the exact user/target tuple, but expose no
  raw user/group ID, row ID, display value, end time, session value, secret, or
  unrestricted field. Return page truncation only; issue no handle and register
  no route.
- `allowed_paths`: `src/governance/query-service.ts`,
  `tests/unit/governance/query-service.test.ts`, `docs/governance-ui.md`, and
  this checkpoint only.
- `protected_paths`: B26A catalog DTO/query/order/handle behavior; B25A/B25B
  shared mutations and CLI behavior; all other query projections; scope/
  resource/preview registries; every HTTP/application route and browser asset;
  identity repository behavior; schema, migrations, dependencies, lockfile,
  config, QQ, Provider, private/live data, deployment, restart, container,
  retention, persistence format, and every prior slice.
- `assertions`: exact-user synthetic profile-only, history-only, duplicate,
  private/global, group, cross-user, malformed-target, and malformed-metadata
  fixtures prove exact selection, filtering, deduplication, and global-first/raw-
  group ordering. Exact 100/101 target probes prove page bounds; 100/101 history
  rows prove independently capped count/truncation. Current-profile absent/
  present and all trust and history lifecycle classifications have exact dates
  and keys. Non-user and malformed-user scopes return a fixed empty page.
  Repeated reads are byte-stable and zero-write; exact DTO keys omit every raw
  identifier, text, secret, end time, and injected value; integrity is `ok` and
  FKs are empty. B26A/B25A/B25B remain unchanged, and no issuer or HTTP/browser
  route appears.
- `focused_commands`: capture a fresh baseline, add and run one named B26B
  fail-first query-service regression, then its final form; run full query-
  service coverage and B26A scope-registry adjacency; canonical P8; typecheck;
  scoped/full ESLint; build/preflight/release; `git diff --check`; dependency/
  migration/inventory/privacy hygiene; and exact three-path private reverse-
  first/forward-first proof.
- `schema_dependency_live_boundary`: one read-only query-service page, synthetic
  tests, canonical contract, and checkpoint only. No persistence write or
  format change, repository behavior, authority issuer, route, mutation,
  registry implementation, schema, migration, dependency, lockfile, browser,
  CLI/QQ, Provider, private DB, deployment, restart, container, or live
  operation is permitted.
- `rollback_boundary`: after this pre-edit contract and a clean diff check,
  capture the exact B26A candidate for the three immutable product/test/
  canonical-document paths plus this mutable checkpoint byte-for-byte and mode-
  for-mode under a new mode-`0700` private root. The final immutable three-path
  ASCII patch must independently prove reverse-first and forward-first byte/mode
  cycles without touching the workspace; checkpoint evidence remains excluded.
- `baseline_evidence`: `git diff --check` passed, then corrected mode-`0700`
  `/tmp/lethebot-gov-ux-01b26b-roundtrip-70mZ8w` captured all four paths at
  mode `0644` with inventory 74 tracked modifications, 60 untracked paths, and
  zero staged at `main@cfebb77`. The immutable source/test/document hashes are
  `dd74dba1ab40d2082b93842fbd114c4c5560e96bdbd6cd0b80850f571564c13e`,
  `e7ac07967ac301391a31f173880bf1373b3f4fb3b442848c044ae16694f12374`,
  and `5b03b8d31a0137c156f303fee7fc3f21f959113f9daaed02c4e998358909c4d3`;
  the pre-edit checkpoint hash is
  `300a233653ac9ad3c4066d4663823d281be6b9545a31950d5b6e7feb8adcb179`.
  A first scratch-root attempt stopped before copying any file because `path`
  is zsh's special executable-search array; the corrected harness uses
  `file_path`. No workspace file changed during either capture attempt.
- `fail_first_evidence`: the named run collected 48 query-service cases, skipped
  47 unrelated cases, and failed only because
  `service.listDisplayProfileTargetsForScope is not a function`. Every exact-
  scope, target/metadata filter, ordering, 100/101 bound, DTO omission, zero-
  write, integrity, and FK assertion was already present. Its mode-`0600` log is
  `/tmp/lethebot-gov-ux-01b26b-focused-fail-first.log`
  (`sha256:b8dd34b4f949ab714d060acfb093963b61f30f99911e4219fa061d24b7d2ad3e`,
  size `1292`). A preceding command with a literal separator collected the full
  repository and is excluded from focused fail-first evidence.
- `final_evidence`: the named B26B regression passed 1 / 1 with 47 skipped, full
  query-service coverage passed 48 / 48, query-service plus scope-registry
  adjacency passed 51 / 51, and canonical P8 passed 7 files / 322 tests.
  Typecheck, scoped and full ESLint, build, release preflight, and
  `git diff --check` passed. The first full release reached one unrelated
  permission-fixture failure in `governance-restore-handoff.test.ts`; the exact
  unchanged case then passed 1 / 1. A restrictive `umask 077` log wrapper
  reproduced that fixture-only result, while the corrected normal-umask release
  passed 137 / 1 skipped files with 2820 / 10 skipped tests. The recovery and
  corrected-release logs are mode `0600` at
  `/tmp/lethebot-gov-ux-01b26b-restore-handoff-recovery.log`
  (`sha256:65a2357cd294a0426bda681ecdcf8f1265db65c31f8111e3630142916b1e79ac`)
  and `/tmp/lethebot-gov-ux-01b26b-release-corrected.log`
  (`sha256:7a321c3458b76225eba8ab2ef9ced04cf56ad5416135d12203d14490eb08c42d`).

  The final query validates the exact user before preparing SQL, unions only
  valid current-profile and nickname-history evidence, deduplicates targets,
  orders private/global first and raw group IDs second, and probes 101 targets.
  Each selected target reports only its separate 16-character fingerprint,
  fixed kind/label, valid current-profile trust/date evidence, and independently
  bounded history count/truncation/lifecycle/latest date. Malformed targets,
  dates, trust, or end evidence create no target; 101st targets and history rows
  affect only truncation. Raw identifiers, profile/history text, row IDs, end
  times, secrets, session values, handles, issuer extras, and unrestricted
  fields remain absent. Reads are byte-stable and write-free with clean
  integrity/FKs; no B26A/B25A/B25B behavior, authority, route, mutation,
  registry, schema, dependency, or persistence format changed.

  The exact three-path ASCII patch is
  `/tmp/lethebot-gov-ux-01b26b-roundtrip-70mZ8w/gov-ux-01b26b.patch`
  (`sha256:f060889602d2ea7dd314d90c748aabcbe2e57e50ea6755a19f2783f876e3c634`,
  mode `0600`, size `25912`). An initial proof comparison recorded the expected
  byte transition but exposed mode `0600` creation under the private umask; the
  corrected isolated mode-`0700` trees use normal apply semantics and prove both
  complete reverse-first and forward-first byte/mode cycles with mode-`0644`
  product files. The candidate manifest matches the workspace. Inventory is
  74/60/0 at `main@cfebb77`; Pi dependencies remain `0.80.2`; migrations
  007/008 retain SHA-256
  `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`/
  `95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`;
  patch/path/ASCII/sensitive-filename hygiene is clean.
- `exact_next_action`: completed; B26B selects the HTTP-disabled target
  resource-handle page below before any route, preview, expected-snapshot
  mutation, confirmation, or browser work.

## Completed Slice: `GOV-UX-01B26C`

- `requirement`: `GOV-UX-01B26C` HTTP-disabled exact-user display-profile
  target resource-handle page.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: B26B exposes a bounded identifier-free target projection, but
  its 16-character fingerprint is display-only and deliberately irreversible.
  Treating that short reference as mutation authority would create a separate
  selector contract and bypass the current-session resource registry. A future
  exact-target preview therefore still has no purpose-, session-, scope-, and
  target-bound authority.
- `selection_reason`: B26A and B26B independently prove exact-user and exact-
  target selection while all display-profile HTTP routes remain absent. Adding
  resource handles over only B26B-selected targets is the smallest next
  dependency. Scope-catalog wiring, target-list wiring, preview projection,
  expected-snapshot mutation, confirmation, and browser work remain separate
  authorization or effect changes.
- `implementation_contract`: add only
  `GovernanceQueryService.listDisplayProfileTargetResourceHandlePage(scope,
  issueHandle)`. Reuse one shared B26B selector and projector so the exact-scope
  validation, metadata filtering, private/global-first ordering, target and
  history 100/101 probes, DTO fields, dates, and truncation stay byte-identical.
  Invalid, non-user, empty, and malformed scopes return the fixed empty page
  before SQL or issuer invocation.

  For each of at most 100 selected targets, derive a separate full 64-character
  SHA-256 target resource ID under domain
  `lethebot-governance:display-profile-target-resource:v1\0` over the exact
  normalized user/target tuple. Pass only `{ scope, targetId }` to the trusted
  issuer. The full target ID is internal authority evidence and must not reuse
  or replace B26B's 16-character display fingerprint. Add only the issuer's
  opaque `handle` and numeric `handleExpiresAt` to each unchanged B26B entry;
  omit `targetId` and every issuer extra. The future binding uses purpose
  `governance.display_profile.targets.read` and resource kind
  `display_profile_target`, but this slice changes no registry or route.
- `allowed_paths`: `src/governance/query-service.ts`,
  `tests/unit/governance/query-service.test.ts`, `docs/governance-ui.md`, and
  this checkpoint only.
- `protected_paths`: B26A catalog and B26B query/order/filter/projector/DTO
  behavior; B25A/B25B service mutations and CLI behavior; every other query
  projection; scope/resource/preview registry implementations; all HTTP/
  application routes and browser assets; identity repositories; schema,
  migrations, dependencies, lockfile, config, QQ, Provider, private/live data,
  deployment, restart, container, retention, persistence format, and every
  prior slice.
- `assertions`: issuer inputs contain exactly the normalized exact-user scope
  and distinct full target ID for each B26B-selected target in the same stable
  order; private/global and group IDs remain distinct, and duplicate evidence
  reuses one target. Invalid scopes, malformed target/metadata evidence, and the
  101st target issue nothing. Stripping `handle` and `handleExpiresAt` yields the
  exact B26B page, including independently bounded history metadata. Exact DTO
  keys omit the full target ID, raw user/group/row/display/end/session/secret
  values, and issuer extras. A real resource registry proves stable same-session
  issuance and cross-session, cross-purpose, cross-kind, and cross-scope denial
  for purpose `governance.display_profile.targets.read` and kind
  `display_profile_target`. Repeated calls are write-free with clean integrity/
  FKs; B26A/B26B/B25A/B25B and all routes remain unchanged.
- `focused_commands`: capture a fresh four-file B26B baseline; add and run one
  named B26C fail-first query-service regression, then its final form; run full
  query-service plus resource- and scope-registry adjacency; canonical P8;
  typecheck; scoped/full ESLint; build/preflight/normal-umask release;
  `git diff --check`; dependency/migration/inventory/privacy hygiene; and exact
  three-path private reverse-first/forward-first proof.
- `schema_dependency_live_boundary`: one read-only query-service resource-handle
  page, synthetic tests, canonical contract, and checkpoint only. No persistence
  write or format change, route/session wiring, registry implementation, preview,
  mutation, schema, migration, dependency, lockfile, browser, CLI/QQ, Provider,
  private DB, deployment, restart, container, or live operation is permitted.
- `rollback_boundary`: after this pre-edit contract and a clean diff check,
  capture the exact B26B candidate for the three immutable product/test/
  canonical-document paths plus this mutable checkpoint byte-for-byte and mode-
  for-mode under a new mode-`0700` private root. The final immutable three-path
  ASCII patch must independently prove reverse-first and forward-first byte/mode
  cycles without touching the workspace; checkpoint evidence remains excluded.
- `baseline_evidence`: `git diff --check` passed, then mode-`0700`
  `/tmp/lethebot-gov-ux-01b26c-roundtrip-ilpMRV` captured all four paths at
  mode `0644` with inventory 74 tracked modifications, 60 untracked paths, and
  zero staged at `main@cfebb77`. The immutable source/test/document hashes are
  `5fe6f877984f5442f40c239425aa429925416d890a152ffed8acd42920122922`,
  `e56b0bc06373ac76f19a6bffb4ca17e2f107d0e39e71566601b1216fbeced03c`,
  and `524e594b7661182ea8629755468dfa55fe953d79170dde4fd3f5a5e0bcbad5ca`;
  the pre-fail-first checkpoint hash is
  `18fe45ccff3f58dc3c722247b7e6438f8e66a280ba044e474bd140a54b254e49`.
- `exact_fail_first_action`: after baseline capture, add only `issues bounded
  display-profile target resource handles without exposing target identifiers`
  to `tests/unit/governance/query-service.test.ts`. Its first invocation must
  fail only because `service.listDisplayProfileTargetResourceHandlePage` is
  absent after every B26B parity, issuer-input, registry-isolation, bound,
  omission, zero-write, integrity, and FK assertion is present.
- `fail_first_evidence`: the named regression collected 49 query-service cases,
  skipped 48 unrelated cases, and failed only because
  `service.listDisplayProfileTargetResourceHandlePage is not a function`. The
  complete B26B parity, issuer-input, registry-isolation, bound, omission, zero-
  write, integrity, and FK assertions were already present while production
  source and the canonical document still matched baseline. Its mode-`0600` log
  is `/tmp/lethebot-gov-ux-01b26c-focused-fail-first.log`
  (`sha256:45b89ad1cdb34950dc158555d5f8babb738aacf5e62f7170bc8795f6d2922e91`,
  size `1259`).
- `final_evidence`: the named final regression passed 1 / 1 with 48 skipped;
  query-service/resource-registry/scope-registry adjacency passed 3 files / 55
  tests; and canonical P8 passed 7 files / 323 tests. Typecheck, scoped and full
  ESLint, build, release preflight, and `git diff --check` passed. The first
  normal-umask release run had one unrelated transient failure in
  `application-release-cli.test.ts` at the successful activation/readiness-
  rollback rehearsal while every other test passed. That exact unchanged case
  then passed 1 / 1 with 22 skipped, and the corrected complete normal-umask
  release passed 137 / 1 skipped files with 2821 / 10 skipped tests. The three
  mode-`0600` logs are `/tmp/lethebot-gov-ux-01b26c-release.log`
  (`sha256:47b75ba77e96c07415c9c87c7d83bd0711b7a4f89a86d6fafb45f115b03a6bbe`,
  size `352626`), `/tmp/lethebot-gov-ux-01b26c-release-recovery.log`
  (`sha256:ba426152935908d28906e12986561ed6a8ba746e1ee5be80a5fe67bcaeeb543b`,
  size `465`), and `/tmp/lethebot-gov-ux-01b26c-release-corrected.log`
  (`sha256:dfb4401ef500d9657a672294399a51d8a3321abe49b9cdacef0819a47a034f31`,
  size `356308`).

  One shared selector/projector now preserves B26B byte-for-byte while B26C
  derives a distinct full domain-separated target ID for each selected target,
  passes only exact `{ scope, targetId }` issuer inputs, and returns only the
  opaque handle and expiry in addition to B26B fields. Invalid scopes reject
  before SQL or issuance; malformed and overflow targets issue nothing. Real
  registry coverage proves same-session stability plus cross-session, cross-
  purpose, cross-kind, and cross-scope denial. Full target IDs, raw identifiers,
  text, row/end/session/secret evidence, and issuer extras stay absent; repeated
  calls are write-free with clean integrity and FKs; all routes remain absent.
- `rollback_evidence`: the exact three-path patch is
  `/tmp/lethebot-gov-ux-01b26c-roundtrip-ilpMRV/gov-ux-01b26c.patch`
  (`sha256:fa36c82d7f1f97cebba097c9856f27c8f09a399baeebf87f6cf19680be7bc2c8`,
  mode `0600`, size `20822`) and contains only `docs/governance-ui.md`,
  `src/governance/query-service.ts`, and
  `tests/unit/governance/query-service.test.ts`. Independent reverse-first and
  forward-first cycles restored baseline and candidate byte-for-byte and mode-
  for-mode, candidate snapshots equal the workspace, proof trees are mode
  `0700`, and product files are mode `0644`. Candidate hashes in path order are
  `856194b77023abc8399a33d4f434fcbe4a2a53f189b3f8abd780e57f41514cdc`,
  `1bca5303dec548f7f00fe37d253534528f4ae24638501901b8bc828f2182cec5`,
  and `b36d3320ff0c98dbcb810d2465c9c2e32b3aef1fdce6509f652a559708ef4ad4`.
- `inventory_evidence`: inventory remains 74 tracked modifications, 60
  untracked paths, and zero staged at `main@cfebb77`; sensitive untracked-
  filename and ASCII/path hygiene are clean. Pi dependencies remain `0.80.2`;
  migration 007/008 hashes remain
  `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b` /
  `95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`.
  No stage, commit, deployment, restart, Provider/QQ, private DB, schema,
  dependency, registry implementation, route, preview, or mutation occurred.
- `exact_next_action`: completed; B26C makes authenticated exact-user scope
  discovery the next dependency before any target-list route can consume B26C
  resource authority.

## Completed Slice: `GOV-UX-01B26D`

- `requirement`: `GOV-UX-01B26D` authenticated exact-user display-profile scope
  discovery route.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: B26A owns the bounded identifier-free exact-user catalog and
  trusted scope issuer callback, while B26B/B26C own HTTP-disabled target pages.
  With no display-profile route registered, an authenticated governance session
  cannot obtain exact-user target-read authority without a raw user selector.
- `selection_reason`: exposing B26A through the existing authenticated-unscoped
  boundary is the direct dependency after the catalog/target/resource chain and
  mirrors the proved B24C discovery-route pattern. It adds only scope authority;
  target-list routing, resource resolution, preview, mutation, confirmation, and
  browser work remain separate slices.
- `implementation_contract`: register only read-only
  `GET /governance/api/v1/display-profile/scopes` with discovery purpose
  `governance.display_profile.scopes`. Reuse the shared authenticated-unscoped
  boundary so authentication, query rejection, scope-header rejection, fixed
  failures, session lifetime, and listener lifecycle remain unchanged. Call only
  `GovernanceQueryService.listDisplayProfileScopeHandles`; bind every issuer
  input to the current session digest and expiry, future purpose
  `governance.display_profile.targets.read`, and the exact user scope. Return
  B26A unchanged. Add no target-list or target-resource route, resource
  resolution, preview, body, CSRF, mutation, registry implementation, query-
  service behavior, schema, dependency, CLI/QQ, or browser behavior.
- `allowed_paths`: `src/index.ts`,
  `tests/integration/governance-http-wiring.test.ts`, `docs/architecture.md`,
  `docs/deployment.md`, `docs/governance-ui.md`, `docs/security-privacy.md`, and
  this checkpoint only.
- `protected_paths`: B26A/B26B/B26C query-service selection, DTO, order, bounds,
  fingerprint, target-ID, and issuer semantics; B25A/B25B mutations and CLI
  behavior; `GovernanceHttpServer`; generic session/CSRF/scope/resource/preview
  registry behavior; every existing route and purpose; all display-profile
  target/read/preview/mutation routes and browser assets; identity repositories;
  config, schema, migrations, dependencies/lockfile, Provider, QQ, private/live
  data, deployment, restart, container, retention, persistence format, and every
  prior slice.
- `assertions`: unauthenticated access is `401`; any query or scope header is
  `400`; every rejection precedes B26A and issuance. A current session returns
  the unchanged fixed B26A catalog, calls B26A once, and binds each exact user
  scope only to that session digest, expiry, and
  `governance.display_profile.targets.read`. Same-session exact-purpose
  resolution succeeds; cross-session and cross-purpose resolution fail.
  Repeated discovery is byte-stable and write-free; raw user/platform/profile/
  history/session/secret values and issuer extras stay absent. B26B/B26C, the
  resource registry, B25A/B25B, and identity mutation remain uncalled; target,
  preview, confirmation, and mutation routes stay absent; integrity is `ok` and
  FKs are empty; every existing route remains unchanged.
- `focused_commands`: after this full contract, run `git diff --check` and
  capture a private seven-file baseline; add and run one named B26D fail-first
  integration regression before source or canonical route-doc edits; rerun the
  focused regression, full governance HTTP wiring, query/service/HTTP/scope-
  registry adjacency, canonical P8, typecheck, scoped/full ESLint, build, release
  preflight, normal-umask release, dependency/migration/inventory/privacy
  hygiene, and exact six-path reverse-first/forward-first proof.
- `schema_dependency_live_boundary`: route wiring, synthetic migrated SQLite,
  integration tests, canonical route docs, and checkpoint only. No query-service
  or registry implementation change, persistence write or format change, target
  read, resource resolution, preview, mutation, schema, migration, dependency,
  lockfile, CLI/QQ, browser, Provider, private DB, deployment, restart, container,
  or live operation is permitted.
- `rollback_boundary`: after this checkpoint contract and clean diff check,
  capture the exact B26C candidate bytes and modes for all six immutable
  product/test/canonical-document paths plus this mutable checkpoint under one
  new mode-`0700` private root. The final immutable six-path ASCII patch must
  independently prove reverse-first and forward-first byte/mode restoration
  without touching the workspace; checkpoint evidence remains excluded.
- `baseline_evidence`: `git diff --check` passed, then mode-`0700`
  `/tmp/lethebot-gov-ux-01b26d-roundtrip-I16e0a` captured all seven paths at
  mode `0644` with inventory 74 tracked modifications, 60 untracked paths, and
  zero staged at `main@cfebb77`. Source/test/architecture/deployment/governance/
  security hashes are
  `fb86f8dfebd764bb4c230b45483b991bce80ebeeee04eabaa9efa4d99ecf2603`,
  `3e1607942848c6ac3817ced8bfa562ae736d277071ab184db33c21f015907cec`,
  `02590f9a45a216ad10b570f179e665e86df3a7353e4b6153bc1ac00212410784`,
  `e4d9ed4775a84e8a0493331a220bf1fe2a86e18ad794dcbfce56225f706a9963`,
  `856194b77023abc8399a33d4f434fcbe4a2a53f189b3f8abd780e57f41514cdc`,
  and `dd8e68b4de0a8e64b78d59ae2c34850fe06a73b9592ee42c88c5b355e187e6db`;
  the pre-fail-first checkpoint hash is
  `63653d73bc6dd840a368d4931fe6e62308e7576570f30bbb7550533aea1f8b09`.
- `exact_fail_first_action`: add only `issues display-profile scope handles only
  to an authenticated session` to
  `tests/integration/governance-http-wiring.test.ts`, then run it alone before
  editing `src/index.ts` or canonical route docs. Its first request must expect
  unauthenticated `GET /governance/api/v1/display-profile/scopes` to return
  `401` and fail only because the absent route returns `404`, while B26A, B26B,
  B26C, both handle registries, and both mutation owners remain uncalled.
- `fail_first_evidence`: the named run collected 33 governance-wiring cases,
  skipped 32 unrelated cases, and failed only because unauthenticated
  `GET /governance/api/v1/display-profile/scopes` returned absent-route `404`
  instead of registered-route `401`. At that assertion B26A, B26B, B26C, both
  handle registries, display-profile redaction, and identity unlink were all
  uncalled. `src/index.ts` and all four canonical route documents still matched
  the recorded baseline. The mode-`0600` log is
  `/tmp/lethebot-gov-ux-01b26d-focused-fail-first.log`
  (`sha256:7e1652f6d1da312c8183eba824ef3c81473b6df041ceba6c526f0cc370d1b325`,
  size `1693`). The first log wrapper stopped after the completed test because
  zsh reserves `status`; a corrected command set the preserved log to `0600`
  without rerunning or changing the workspace.
- `final_evidence`: the named final regression passed 1 / 1 with 32 skipped;
  full governance HTTP wiring passed 33 / 33; query/service/HTTP/scope-registry
  adjacency passed 5 files / 120 tests; and canonical P8 passed 7 files / 323
  tests. Typecheck, scoped and full ESLint, build, release preflight, and
  `git diff --check` passed. The complete normal-umask release gate passed 137 /
  1 skipped files with 2822 / 10 skipped tests. Mode-`0600` logs include the
  focused final at `/tmp/lethebot-gov-ux-01b26d-focused-final.log`
  (`sha256:09460329a53087718b2cb116a2152e67cfc668f024321d2cd30969db65df6435`,
  size `733`), adjacency at `/tmp/lethebot-gov-ux-01b26d-adjacency.log`
  (`sha256:9ecffaf937e59ab02bbbe5bdf95ad85afb3a91d52713f53a76ec7051e1339d94`,
  size `93927`), P8 at `/tmp/lethebot-gov-ux-01b26d-p8.log`
  (`sha256:0a792b506e160b261398a27457895940019f70614306e0bf62148f4997e917c8`,
  size `16997`), and release at `/tmp/lethebot-gov-ux-01b26d-release.log`
  (`sha256:133359a6c2e6b9f803328e592e094d780e543be6d5de90bbb3f25999ca8981d8`,
  size `352185`).

  The exact authenticated GET rejects missing sessions with `401` and queries
  or scope headers with `400` before B26A or issuance. A current session calls
  only B26A and binds each exact user to its digest/expiry and
  `governance.display_profile.targets.read`; same-session exact-purpose
  resolution succeeds while cross-session/purpose fails. The catalog remains
  identifier-free, stable, write-free, integrity/FK-clean, and separate from
  every legacy catalog. B26B/B26C, resource issuance, profile redaction, identity
  unlink, and target/preview/mutation routes remain uncalled or absent.
- `rollback_evidence`: the exact six-path patch is
  `/tmp/lethebot-gov-ux-01b26d-roundtrip-I16e0a/gov-ux-01b26d.patch`
  (`sha256:0c0098028eee9dcab6bf50f14e6f4ff645198f18488c7addf79f48ad06831470`,
  mode `0600`, size `20459`) and contains only `docs/architecture.md`,
  `docs/deployment.md`, `docs/governance-ui.md`, `docs/security-privacy.md`,
  `src/index.ts`, and `tests/integration/governance-http-wiring.test.ts`. It is
  ASCII-only with zero NUL bytes. Independent reverse-first and forward-first
  cycles restored baseline and candidate byte-for-byte and mode-for-mode, the
  candidate equals the workspace, proof roots/trees are mode `0700`, and product
  files are mode `0644`. Candidate hashes in path order are
  `8ff2ed1a2edbf1f8a2bc6bf10bf58d5a3fa15be12740e39b31e96cbd0220323f`,
  `4d49c1b046b36ed21c046c893671d8c3ebfd5138f71f06799d62ef4f897bffca`,
  `558189edfc0a8a9ec7b86315f1a0eb4dca374ac5980b4c70bf62dabae30e6035`,
  `90cfd1f4a2777bdb91835dce1961a86182e0f28855e93d932b0bdcef215931a1`,
  `14553a091f1f7aa8e56253f2256e0d1ae4d1265216293266a62d10ba06249fa4`,
  and `ab32989584f39ff4ab4a7c64cd42671a76e370f3a2907125d02e730a5bea975f`.
- `inventory_evidence`: inventory remains 74 tracked modifications, 60
  untracked paths, and zero staged at `main@cfebb77`; only the canonical
  `.env.example` matches broad sensitive-filename screening. Pi dependencies
  remain `0.80.2`; migrations 007/008 retain SHA-256
  `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b` /
  `95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`.
  No stage, commit, Provider/QQ, private DB, deployment, restart, schema,
  dependency, registry implementation, target route, resource resolution,
  preview, mutation, or browser operation occurred.
- `exact_next_action`: completed; B26D scope authority makes the exact-scope
  target resource-handle route below the next dependency before target preview
  or mutation work.

## Completed Slice: `GOV-UX-01B26E`

- `requirement`: `GOV-UX-01B26E` exact-user scoped display-profile target
  resource-handle route.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: B26D now issues current-session exact-user scope handles for
  the target-read purpose, and B26C owns the bounded identifier-free target page
  with internal target authority, but no scoped target route consumes either.
- `selection_reason`: changing no query or registry owner, a single static
  exact-scope GET can connect the proved B26D and B26C boundaries. This mirrors
  the established Memory-record and Explain-turn resource-handle list wiring and
  is the smallest dependency before target detail/preview, mutation,
  confirmation, or browser work.
- `implementation_contract`: register only read-only
  `GET /governance/api/v1/display-profile/targets` in the existing exact-scope
  route class with purpose `governance.display_profile.targets.read`. Let the
  shared boundary authenticate, reject queries, require and resolve exactly one
  B26D scope handle against the current session and purpose, and pass only the
  resolved user scope to the callback. Call only
  `GovernanceQueryService.listDisplayProfileTargetResourceHandlePage(scope,
  issuer)`. Bind each B26C-offered exact normalized scope and full internal
  target ID to the callback session digest and absolute expiry, the same purpose,
  resource kind `display_profile_target`, and resource ID `targetId` through the
  existing `GovernanceResourceHandleRegistry`. Return B26C unchanged. Add no
  dynamic resource/detail route, resource resolution, preview, expected
  snapshot, body, CSRF, mutation, confirmation, query-service or registry
  implementation change, CLI/QQ, or browser behavior.
- `allowed_paths`: `src/index.ts`,
  `tests/integration/governance-http-wiring.test.ts`, `docs/architecture.md`,
  `docs/deployment.md`, `docs/governance-ui.md`, `docs/security-privacy.md`, and
  this checkpoint only.
- `protected_paths`: B26A/B26B/B26C query selection, projection, order, bounds,
  fingerprints, target IDs, issuer inputs, and DTOs; B26D catalog/route/purpose/
  scope issuance; B25A/B25B mutation and CLI behavior; `GovernanceHttpServer`;
  all scope/resource/preview registry implementations; session, CSRF, and
  generic dispatch behavior; every existing route and purpose; display-profile
  detail/preview/mutation/confirmation/browser paths; identity repositories;
  config, schema, migrations, dependencies/lockfile, Provider, QQ, private/live
  data, deployment, restart, container, retention, persistence format, and every
  prior slice.
- `assertions`: unauthenticated access is `401`; query, missing, or malformed
  scope authority is `400`; unknown, cross-session, and cross-purpose scope
  authority is `404`; all failures precede B26C and resource issuance. A valid
  B26D scope calls only B26C and binds each B26C-selected target to the callback
  session digest/expiry, exact normalized scope, same purpose,
  `display_profile_target` kind, and full internal target ID. Matching resource
  resolution succeeds; cross-session, cross-purpose, cross-kind, and cross-scope
  resolution fails. The response preserves B26B/B26C bounds, order, target and
  history metadata, dates, classifications, truncation, and identifier omission
  while adding only stable opaque resource handle/expiry. B26B is not called
  directly; dynamic target, preview, confirmation, and mutation paths remain
  `404` without resource resolution; B25A/B25B remain uncalled; repeated reads
  are byte-stable and write-free; integrity is `ok` and FKs are empty; existing
  discovery and every prior route remain unchanged.
- `focused_commands`: after this full contract, run `git diff --check` and
  capture a private seven-file B26D baseline; add and run one named B26E fail-
  first integration regression before source or canonical route-doc edits;
  rerun the focused regression, full governance HTTP wiring, query/service/HTTP/
  scope/resource-registry adjacency, canonical P8, typecheck, scoped/full
  ESLint, build, release preflight, normal-umask release, dependency/migration/
  inventory/privacy hygiene, and exact six-path reverse-first/forward-first
  proof.
- `schema_dependency_live_boundary`: exact-scope route wiring, synthetic
  migrated SQLite, integration tests, canonical route docs, and checkpoint only.
  No query-service or registry implementation change, persistence write or
  format change, resource detail/resolution, preview, mutation, schema,
  migration, dependency, lockfile, CLI/QQ, browser, Provider, private DB,
  deployment, restart, container, or live operation is permitted.
- `rollback_boundary`: after this checkpoint contract and a clean diff check,
  capture exact B26D candidate bytes and modes for the six immutable product/
  test/canonical-document paths plus this mutable checkpoint under one new mode-
  `0700` private root. The final immutable six-path ASCII patch must independently
  prove reverse-first and forward-first byte/mode restoration without touching
  the workspace; checkpoint evidence remains excluded.
- `baseline_evidence`: `git diff --check` passed, then mode-`0700`
  `/tmp/lethebot-gov-ux-01b26e-roundtrip-myoDj5` captured all seven paths at
  mode `0644` with inventory 74 tracked modifications, 60 untracked paths, and
  zero staged at `main@cfebb77`. Source/test/architecture/deployment/governance/
  security hashes are
  `14553a091f1f7aa8e56253f2256e0d1ae4d1265216293266a62d10ba06249fa4`,
  `ab32989584f39ff4ab4a7c64cd42671a76e370f3a2907125d02e730a5bea975f`,
  `8ff2ed1a2edbf1f8a2bc6bf10bf58d5a3fa15be12740e39b31e96cbd0220323f`,
  `4d49c1b046b36ed21c046c893671d8c3ebfd5138f71f06799d62ef4f897bffca`,
  `558189edfc0a8a9ec7b86315f1a0eb4dca374ac5980b4c70bf62dabae30e6035`,
  and `90cfd1f4a2777bdb91835dce1961a86182e0f28855e93d932b0bdcef215931a1`;
  the pre-fail-first checkpoint hash is
  `783e71c8fa637a64e653b269a3f37ae09664edad55e3f646fea6d4eec818aec9`.
- `exact_fail_first_action`: add only `serves display-profile target resource
  handles only through current user-scope authority` to
  `tests/integration/governance-http-wiring.test.ts`, then run it alone before
  editing `src/index.ts` or canonical route docs. Its first request must expect
  unauthenticated `GET /governance/api/v1/display-profile/targets` to return
  `401` and fail only because the absent route returns `404`, while B26C, B26B,
  both handle registries, display-profile redaction, and identity unlink remain
  uncalled.
- `fail_first_evidence`: the named run collected 34 governance-wiring cases,
  skipped 33 unrelated cases, and failed only because unauthenticated
  `GET /governance/api/v1/display-profile/targets` returned absent-route `404`
  instead of registered-route `401`. At that assertion B26B/B26C, scope
  resolution/issuance, resource issuance/resolution, display-profile redaction,
  and identity unlink were all uncalled. Production source and all canonical
  route documents still matched the baseline. The mode-`0600` log is
  `/tmp/lethebot-gov-ux-01b26e-focused-fail-first.log`
  (`sha256:bf051020b20e1d12661ea094d14e4763debfb968f89f56315703ad1fbb48a950`,
  size `1792`).
- `final_evidence`: the named final regression passed 1 / 1 with 33 skipped;
  full governance HTTP wiring passed 34 / 34; query/service/HTTP/scope/resource-
  registry adjacency passed 5 files / 110 tests; and canonical P8 passed 7
  files / 323 tests. Typecheck, scoped and full ESLint, build, release preflight,
  and `git diff --check` passed. The complete normal-umask release gate passed
  137 / 1 skipped files with 2823 / 10 skipped tests. Mode-`0600` logs include
  the focused final at `/tmp/lethebot-gov-ux-01b26e-focused-final.log`
  (`sha256:0d64f6ed1cc7d76167dc8c33a5667dca035cdbfa4c21a244d124238b67e513d0`,
  size `771`), full wiring at `/tmp/lethebot-gov-ux-01b26e-wiring.log`
  (`sha256:62c2c8d865e1cdbd041080aee43eca255a337e624e9f3fd076cd2c4c58610191`,
  size `96505`), adjacency at
  `/tmp/lethebot-gov-ux-01b26e-adjacency.log`
  (`sha256:6404a627c1d5c8715ed60530d1301c4e1426aa855f9760a08ff67d6a3c90fb25`,
  size `96798`), P8 at `/tmp/lethebot-gov-ux-01b26e-p8.log`
  (`sha256:1bd6b14af9c1d4faea69fc5e82bcfd2fc815aae3de90104fda53331163e1d794`,
  size `17000`), and release at
  `/tmp/lethebot-gov-ux-01b26e-release.log`
  (`sha256:905b87f17f4bf6288f3d5b45ecffdb7437181d4aa0971563815d79999bacb8d8`,
  size `358578`).

  The exact scoped GET rejects unauthenticated, query-bearing, missing,
  malformed, unknown, cross-session, and cross-purpose authority before B26C
  or resource issuance. A current B26D handle calls only B26C with its resolved
  exact-user scope, then binds each selected full target ID to that callback
  session digest/expiry, `governance.display_profile.targets.read`, resource
  kind `display_profile_target`, and the same scope. Same-session exact-purpose/
  kind/scope resolution succeeds; cross-session/purpose/kind/scope resolution
  fails. The unchanged B26C page is identifier-free, byte-stable, write-free,
  and integrity/FK-clean. B26B is not called directly, B25A/B25B stay uncalled,
  and dynamic target detail, preview, confirmation, and mutation routes remain
  `404` without resource resolution.
- `rollback_evidence`: the exact six-path patch is
  `/tmp/lethebot-gov-ux-01b26e-roundtrip-myoDj5/gov-ux-01b26e.patch`
  (`sha256:99dd113208596df6f8ca5f97007808f1f9efdd502b4a3375e408c65e9622e394`,
  mode `0600`, size `28037`) and contains only `src/index.ts`,
  `tests/integration/governance-http-wiring.test.ts`, `docs/architecture.md`,
  `docs/deployment.md`, `docs/governance-ui.md`, and
  `docs/security-privacy.md`. It is ASCII-only with zero NUL bytes. Independent
  reverse-first and forward-first cycles restored baseline and candidate byte-
  for-byte and mode-for-mode, the candidate equals the workspace, all 21 proof
  directories are mode `0700`, and all 24 proof files are mode `0644`.
  Candidate hashes in source/test/architecture/deployment/governance/security
  order are
  `30c5a86baa3a1246cd183a5ea4176e40e38f99551f970cf0b3c35daa491ec470`,
  `60a42261a15ce42cfd7384ab69a2d41eb12bf77515d6e438183423a4d6336cc0`,
  `000b4349453ee59accee7e5b0fbb5653bce181c92b0b589df7c06e0b45c28332`,
  `2f57ec4a63a10a6b7e7f42d61524047b3dd1cd138ca1e1d4e170a8ff7ab17181`,
  `1d3530646759cea9cee990ab2a361597ab759af51c9232b48373b61a59fb44ad`,
  and `6a6999d35870cac49bb43d7ba45839b0a7656bed6ef62f195df2c3cebedca97f`.
- `inventory_evidence`: inventory remains 74 tracked modifications, 60
  untracked paths, and zero staged at `main@cfebb77`; sensitive untracked-
  filename screening is empty. Pi dependencies remain `0.80.2`; migrations
  007/008 retain SHA-256
  `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b` /
  `95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`.
  Protected B26C/query/scope-registry/resource-registry owner hashes remain
  exact. No stage, commit, Provider/QQ, private DB, deployment, restart, schema,
  dependency, registry implementation, resource resolution, preview, mutation,
  or browser operation occurred.
- `exact_next_action`: completed; the dependency reassessment selects B26F
  below, an HTTP-disabled exact-target detail projection before any dynamic
  resource route, preview, expected-snapshot mutation, confirmation, or browser
  work.

## Completed Slice: `GOV-UX-01B26F`

- `requirement`: `GOV-UX-01B26F` HTTP-disabled exact-target display-profile
  detail projection.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: B26E now exposes B26C resource authority for each bounded
  display-profile target, but no shared query resolves one full internal target
  ID to a fixed detail DTO. Direct table or legacy CLI-shaped reads would expose
  raw canonical-user, group, profile/history row IDs and unrestricted display
  strings, so a dynamic target route or mutation preview has no safe detail
  dependency.
- `selection_reason`: after the B26D/B26E discovery-and-target route pair, the
  established Memory and Explain sequence proves a fixed detail projection
  before dynamic resource resolution. A query-service-only read is smaller than
  combining detail, HTTP routing, preview authority, or mutation semantics and
  keeps B25A unchanged.
- `implementation_contract`: add only
  `GovernanceQueryService.getDisplayProfileTargetDetailForScope({ scope,
  targetId })` and fixed DTO types. Accept only a B26A-valid exact-user scope and
  a 64-character lowercase hexadecimal full target ID. Reuse the B26B selector,
  its validation, ordering, 100/101 bound, and full B26C target-ID domain to
  locate only a currently selected target; invalid, unknown, cross-scope,
  malformed, or omitted targets return `null` before secondary reads. Return
  the exact B26B target projection plus the current display value, when present,
  and at most 32 valid nickname-history entries from a 33-row probe ordered by
  observation time descending and internal-ID tie-breaker. Every display value
  uses the existing recursive secret/platform redaction and a fixed 160-Unicode-
  code-point bound with explicit `redacted` and `truncated` signals. History
  entries expose only a domain-separated 16-character fingerprint, bounded
  display value, observed time, and optional valid observed-until time, plus
  explicit page truncation. Raw canonical-user, group, target, profile/history
  row IDs, session values, secrets, malformed metadata, and unrestricted fields
  remain absent. Register no route, issue or resolve no handle, call no mutation,
  and add no preview, expected snapshot, confirmation, CLI/QQ, or browser
  behavior.
- `allowed_paths`: `src/governance/query-service.ts`,
  `tests/unit/governance/query-service.test.ts`, `docs/governance-ui.md`, and
  this checkpoint only.
- `protected_paths`: B26A/B26B/B26C selectors, catalog/list/resource DTO
  behavior, bounds, order, fingerprints, target-ID and issuer semantics; B26D/
  B26E application routes and authority; B25A/B25B service and CLI behavior;
  HTTP/session/CSRF/scope/resource/preview registries and dispatch; identity
  repositories; every existing route; schema, migrations, dependencies/
  lockfile, Provider, QQ, private/live data, deployment, restart, container,
  retention, persistence format, and every prior slice.
- `assertions`: valid private/global and group target IDs return only their exact
  target detail after complete scope-and-target selection. Invalid scope/ID,
  another user's same group, a valid but unselected 101st target, malformed
  target metadata, and missing evidence return `null` before current-profile or
  history detail queries. The base target equals B26B exactly. Current and
  history text preserves existing redaction markers, caps at 160 Unicode code
  points, and reports redaction/truncation; history uses a stable 32/33 bound and
  deterministic tie order while excluding malformed dates/lifecycle evidence.
  The fixed DTO contains no raw user/group/target/row/session/platform/secret
  values or issuer extras. Repeated reads are byte-stable and write-free;
  integrity is `ok` and FKs are empty; B26A-E and B25A/B25B remain unchanged;
  dynamic target, preview, confirmation, and mutation routes stay absent.
- `focused_commands`: record a clean diff and private four-file B26E baseline;
  add and run one named fail-first query-service regression before production or
  canonical behavior-doc edits; rerun the focused regression, full query-service
  and query/resource-registry/HTTP-wiring adjacency, canonical P8, typecheck,
  scoped/full ESLint, build, release preflight, normal-umask release, dependency/
  migration/inventory/privacy hygiene, and exact three-path reverse-first/
  forward-first proof.
- `schema_dependency_live_boundary`: read-only query-service code, synthetic
  migrated SQLite, unit tests, canonical query contract, and checkpoint only.
  No application/HTTP/registry behavior, persistence write or format change,
  route, preview, mutation, schema, migration, dependency, lockfile, CLI/QQ,
  browser, Provider, private DB, deployment, restart, container, or live
  operation is permitted.
- `rollback_boundary`: after this checkpoint contract and a clean diff check,
  capture exact B26E candidate bytes and modes for the three immutable product/
  test/canonical-document paths plus this mutable checkpoint under a new mode-
  `0700` private root. The final immutable three-path patch must independently
  prove reverse-first and forward-first byte/mode restoration without touching
  the workspace; checkpoint evidence remains excluded.
- `baseline_evidence`: `git diff --check` passed, then mode-`0700`
  `/tmp/lethebot-gov-ux-01b26f-roundtrip-3qLr2Y` captured all four paths at mode
  `0644` under mode-`0700` parent directories. Query source, unit test,
  governance contract, and pre-evidence checkpoint hashes are
  `1bca5303dec548f7f00fe37d253534528f4ae24638501901b8bc828f2182cec5`,
  `b36d3320ff0c98dbcb810d2465c9c2e32b3aef1fdce6509f652a559708ef4ad4`,
  `1d3530646759cea9cee990ab2a361597ab759af51c9232b48373b61a59fb44ad`,
  and `efa838afca222581453296f77435d2c61b41dc247e59785c69649c89ecb2a2a0`.
  Workspace and baseline hashes match. Inventory is 74 tracked modifications,
  60 untracked paths, and zero staged at `main@cfebb77`; Pi dependencies remain
  `0.80.2`, and migrations 007/008 retain the recorded hashes.
- `exact_fail_first_action`: add only `returns bounded redacted display-profile
  target detail only for the exact user and full target authority` to
  `tests/unit/governance/query-service.test.ts`, then run it alone before editing
  `src/governance/query-service.ts` or `docs/governance-ui.md`. It must collect
  and fail only because
  `GovernanceQueryService.getDisplayProfileTargetDetailForScope` is absent.
- `fail_first_evidence`: the named run collected 50 query-service cases, skipped
  49 unrelated cases, and failed only with
  `service.getDisplayProfileTargetDetailForScope is not a function`. Production
  query source and the canonical governance contract retained their baseline
  hashes. The mode-`0600` log is
  `/tmp/lethebot-gov-ux-01b26f-focused-fail-first.log`
  (`sha256:0fa735969d83b198493b7843189a82a14c83ab84cb9f363dad67af5faed629fc`,
  size `1297`).
- `final_evidence`: the named final regression passed 1 / 1 with 49 skipped;
  full query-service coverage passed 50 / 50; query/scope/resource-registry/HTTP-
  wiring adjacency passed 4 files / 90 tests; and canonical P8 passed 7 files /
  324 tests. Typecheck, scoped and full ESLint, build, release preflight, and
  `git diff --check` passed. The complete normal-umask release gate passed 137 /
  1 skipped files with 2824 / 10 skipped tests. Mode-`0600` logs are the focused
  final at `/tmp/lethebot-gov-ux-01b26f-focused-final.log`
  (`sha256:6a0ed567c1ddf3c64df44dd92d4e72520b4511a33f8aa8dbf83c9056791716b7`,
  size `320`), full query service at
  `/tmp/lethebot-gov-ux-01b26f-query-service.log`
  (`sha256:1c129750efa97222a0475ba1817a98e983eec3cafad4ffbe40c7fcdb2a982f26`,
  size `296`), adjacency at `/tmp/lethebot-gov-ux-01b26f-adjacency.log`
  (`sha256:7787210ada146d6fab2196cd9be242c70234c9a927514537cee50e0a9031f786`,
  size `96875`), P8 at `/tmp/lethebot-gov-ux-01b26f-p8.log`
  (`sha256:fbf078d8c360cd8733083bd992b3f628c3e26a577b8069f2a31c740c83069792`,
  size `16999`), and release at
  `/tmp/lethebot-gov-ux-01b26f-release.log`
  (`sha256:2380a7e07b654ca786e8eac95d7dd369f3d94f0fe9ea9bbd59c42623c1cea158`,
  size `363941`).

  The method rejects malformed scope or target authority before SQL and rejects
  unknown, cross-user, malformed-evidence, missing, and unselected targets after
  only the shared B26B selector. Exact private/global, group, and history-only
  targets return the unchanged B26B target plus optional current display and a
  deterministic 32/33 nickname-history page. Values preserve governance
  redaction markers, cap at 160 Unicode code points, and expose explicit signals;
  history exposes only a separate 16-character fingerprint and valid dates. The
  first implementation run exposed a `text` versus `value` DTO-field mismatch;
  one private projector corrected both current and history fields without
  weakening the regression. Raw scope/target/row/session/platform/secret values
  remain absent; reads are stable and write-free with clean integrity/FKs; B26A-
  E, B25A/B25B, every route, registry, mutation, schema, migration, dependency,
  CLI/QQ, browser, live, and private-data boundary remains unchanged.
- `rollback_evidence`: the exact three-path patch is
  `/tmp/lethebot-gov-ux-01b26f-roundtrip-3qLr2Y/gov-ux-01b26f.patch`
  (`sha256:5c80660426b31848295de9e622458732b8bc98162ff3318f5b7a24bf519522ec`,
  mode `0600`, size `23506`) and contains only `docs/governance-ui.md`,
  `src/governance/query-service.ts`, and
  `tests/unit/governance/query-service.test.ts`. It is ASCII-only with zero NUL
  bytes. Independent reverse-first and forward-first cycles restored baseline
  and candidate byte-for-byte and mode-for-mode, and candidate snapshots equal
  the workspace. All 29 proof directories are mode `0700`; all 13 non-patch
  proof files are mode `0644`. Candidate hashes in document/source/test order
  are `cbfde1a45f6f888675787674c7679c76cefc30eab208598a8bb3419e808b3534`,
  `d560219dc6cc524d9412cccd0737bf733c72be184b2e945e7be53266211aa385`,
  and `793f2c582446c73500bda8beec816a657f7abfca7e28aa0d471b9d35e4965e9a`.
- `inventory_evidence`: inventory remains 74 tracked modifications, 60
  untracked paths, and zero staged at `main@cfebb77`; sensitive untracked-
  filename screening is empty. Pi dependencies remain `0.80.2`; migrations
  007/008 retain SHA-256
  `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b` /
  `95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`.
  No stage, commit, Provider/QQ, private DB, deployment, restart, schema,
  dependency, route, registry implementation, preview, mutation, or browser
  operation occurred.
- `exact_next_action`: completed; the dependency reassessment selects B26G
  below, one exact-scope read-only target-detail route before any display-profile
  preview, expected-snapshot mutation, confirmation, or browser work.

## Completed Slice: `GOV-UX-01B26G`

- `requirement`: `GOV-UX-01B26G` exact-scope read-only display-profile target-
  detail route.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: B26E issues current-session, target-read-purpose,
  `display_profile_target`-kind, exact-user resource handles and B26F owns the
  fixed bounded target-detail DTO, but
  `GET /governance/api/v1/display-profile/targets/:resourceHandle` remains
  absent. The target workflow cannot resolve its already-issued authority to
  the proved detail projection.
- `selection_reason`: B26C/E resource issuance and B26F detail selection are now
  independently proved. Registering one existing generic scoped-resource GET is
  the smallest dependency-ordered slice and requires no HTTP-server, registry,
  query-service, persistence, mutation, or browser change.
- `implementation_contract`: add only the path constant
  `` `${GOVERNANCE_DISPLAY_PROFILE_TARGETS_PATH}/:resourceHandle` ``, register
  one authenticated scoped read-only `GET` with purpose
  `governance.display_profile.targets.read` and resource kind
  `display_profile_target`, and add one application handler branch. After the
  shared boundary authenticates, rejects queries, resolves the exact current-
  session scope handle, and resolves the resource handle against the same
  session, purpose, kind, and scope, call only
  `GovernanceQueryService.getDisplayProfileTargetDetailForScope({ scope,
  targetId: resource.resourceId })`. Return B26F unchanged with `200`, or fixed
  `{ error: 'not_found' }` with `404` when valid resolved authority no longer has
  detail. Add no issuer, alternate selector, preview, expected snapshot,
  mutation, confirmation, CLI/QQ, or browser behavior.
- `allowed_paths`: `src/index.ts`,
  `tests/integration/governance-http-wiring.test.ts`, `docs/architecture.md`,
  `docs/deployment.md`, `docs/governance-ui.md`, `docs/security-privacy.md`, and
  this checkpoint only.
- `protected_paths`: B26A-F query-service selectors, DTOs, target IDs, bounds,
  redaction, ordering, and issuer behavior; B26D/E discovery/list routes and
  purposes; B25A/B25B service and CLI behavior; `GovernanceHttpServer`; scope/
  resource/preview registry implementations; generic authentication, session,
  CSRF, scope, resource, preview, and dispatch behavior; every other route;
  identity repositories, schema, migrations, dependencies/lockfile, Provider,
  QQ, private/live data, deployment, restart, container, retention, persistence
  format, browser assets, and every prior slice.
- `assertions`: unauthenticated detail is fixed `401` before scope or resource
  resolution and before B26F. Missing or malformed scope/resource handles and
  any query are fixed `400` before B26F. Unknown, cross-session, cross-purpose,
  cross-kind, and cross-scope authority is fixed `404` before B26F. A valid
  resolved handle for removed or invalidated target evidence returns fixed `404`
  after exactly one B26F call with only resolved scope and internal target ID.
  Valid private/global and group handles return the unchanged B26F DTO with
  `200`; the B26E list remains byte-stable; raw scope, target, row, session,
  platform, secret, and unrestricted values remain absent. POST, preview,
  confirmation, mutation, and other dynamic paths stay `404`; B25A/B25B remain
  uncalled; repeated reads are write-free; integrity is `ok` and FKs are empty.
- `focused_commands`: run `git diff --check` and capture a private seven-file
  B26F baseline; add and run one named B26G fail-first integration regression
  before source or canonical route-doc edits; rerun the focused regression, full
  governance HTTP wiring, five-file query/scope/resource-registry/HTTP-wiring
  adjacency, canonical P8, typecheck, scoped/full ESLint, build, release
  preflight, normal-umask release, dependency/migration/inventory/privacy
  hygiene, and exact six-path reverse-first/forward-first proof.
- `schema_dependency_live_boundary`: application route wiring, synthetic
  integration coverage, canonical route/security docs, and checkpoint only. No
  query-service, HTTP-server, registry implementation, persistence write or
  format change, preview, mutation, schema, migration, dependency, lockfile,
  CLI/QQ, browser, Provider, private DB, deployment, restart, container, or live
  operation is permitted.
- `rollback_boundary`: after this full contract and a clean diff check, capture
  exact B26F candidate bytes and modes for the six immutable product/test/
  canonical-document paths plus this mutable checkpoint under one new mode-
  `0700` private root. The final immutable six-path ASCII patch must independently
  prove reverse-first and forward-first byte/mode restoration without touching
  the workspace; checkpoint evidence remains excluded.
- `baseline_evidence`: `git diff --check` passed, then mode-`0700`
  `/tmp/lethebot-gov-ux-01b26g-roundtrip-Bsfoa0` captured all seven paths at mode
  `0644` under mode-`0700` parent directories. Source, test, architecture,
  deployment, governance, security, and pre-evidence checkpoint hashes are
  `30c5a86baa3a1246cd183a5ea4176e40e38f99551f970cf0b3c35daa491ec470`,
  `60a42261a15ce42cfd7384ab69a2d41eb12bf77515d6e438183423a4d6336cc0`,
  `000b4349453ee59accee7e5b0fbb5653bce181c92b0b589df7c06e0b45c28332`,
  `2f57ec4a63a10a6b7e7f42d61524047b3dd1cd138ca1e1d4e170a8ff7ab17181`,
  `cbfde1a45f6f888675787674c7679c76cefc30eab208598a8bb3419e808b3534`,
  `6a6999d35870cac49bb43d7ba45839b0a7656bed6ef62f195df2c3cebedca97f`,
  and `64cd4ffa09a096fc4c3aa6d3f223cf280335f97562fb06341fa1f2c4757958d6`.
  Workspace and baseline hashes match; inventory remains 74 tracked
  modifications, 60 untracked paths, and zero staged at `main@cfebb77`.
- `fail_first_evidence`: the named B26G regression failed 1 / 1 with 34
  skipped solely because unauthenticated
  `GET /governance/api/v1/display-profile/targets/<resource-handle>` returned
  absent-route `404` instead of registered-route `401`. All preceding no-call
  guards passed: B26F detail, B26C target issuance, B26B legacy target read,
  both handle registries, and B25A/B25B mutations remained untouched. The
  mode-`0600` log is
  `/tmp/lethebot-gov-ux-01b26g-focused-fail-first.log` with SHA-256
  `1e90e13dda062160c97e11af16119a44173d5f2ad0bcd8900fee3cd8c8d3688e`.
- `final_evidence`: the named B26G regression passed 1 / 1 with 34 skipped;
  full governance HTTP wiring passed 35 / 35; five-file query/scope/resource-
  registry/HTTP/wiring adjacency passed 112 / 112; and canonical P8 passed 7
  files / 324 tests. Typecheck, scoped and full ESLint, build, release preflight,
  and `git diff --check` passed. The complete normal-umask release gate passed
  137 / 1 skipped files with 2825 / 10 skipped tests. Mode-`0600` logs are the
  focused final at `/tmp/lethebot-gov-ux-01b26g-focused-final.log`
  (`sha256:49931cf58c571c32a690ea2556d9bfde7d068474e375279195d7d83c382e881f`),
  full wiring at `/tmp/lethebot-gov-ux-01b26g-http-wiring.log`
  (`sha256:646e9555a0ebd326905f43751b72d0e520dd7c0fcf54155e928f5028158eb7a3`),
  adjacency at `/tmp/lethebot-gov-ux-01b26g-adjacency.log`
  (`sha256:5c114c778fee4789e2f9f585ff8a64ca2fe304d4eb39a6ce93c50a3060f143a9`),
  P8 at `/tmp/lethebot-gov-ux-01b26g-p8.log`
  (`sha256:039ef67fb216d00619bfba5ee6456c109e33c61ccf9109038fcd444d340f764f`),
  and release at `/tmp/lethebot-gov-ux-01b26g-release.log`
  (`sha256:169e075467bae3f282b6202fea28fe45ab64e262cd218d5eb4e583941703d7ca`).
  The route authenticates and resolves exact current-session scope/resource
  authority before B26F, calls B26F only with the resolved user scope and full
  internal target ID, returns its private/global and group DTOs unchanged, and
  conceals missing detail with fixed `404`. Invalid authority never reaches
  B26F; list responses remain stable; every response stays identifier-free;
  preview, confirmation, and mutation remain absent; B25A/B25B stay uncalled;
  repeated reads are write-free with clean integrity/FKs.
- `rollback_evidence`: the exact six-path patch is
  `/tmp/lethebot-gov-ux-01b26g-roundtrip-Bsfoa0/gov-ux-01b26g.patch`
  (`sha256:8c2233d132c6c685888156da328df497b25353b5e1f539ce94916e6cb1e1b397`,
  mode `0600`, size `31848`). It contains only the four canonical documents,
  `src/index.ts`, and the governance HTTP wiring regression, is ASCII-only with
  zero NUL bytes, and independently passed reverse-first and forward-first
  byte/mode cycles without touching the workspace. All 21 proof directories
  are mode `0700`; all 25 non-patch proof files are mode `0644`; candidate
  snapshots equal current workspace bytes and modes.
- `inventory_evidence`: inventory remains 74 tracked modifications, 60
  untracked paths, and zero staged at `main@cfebb77`; sensitive untracked-
  filename screening is empty. Pi dependencies remain `0.80.2`; migrations
  007/008 retain SHA-256
  `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b` /
  `95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`.
  No stage, commit, Provider/QQ, private DB, deployment, restart, schema,
  dependency, registry implementation, preview, mutation, or browser operation
  occurred.
- `exact_fail_first_action`: add only `serves display-profile target detail only
  through exact current target-resource authority` to
  `tests/integration/governance-http-wiring.test.ts`, then run it alone before
  editing `src/index.ts` or canonical route docs. Its first request must expect
  unauthenticated dynamic target GET to return `401` and fail only because the
  absent route returns `404`, while B26F, B26C, both handle registries, B25A, and
  B25B remain uncalled.
- `exact_next_action`: completed; the dependency reassessment selects B26H
  below, an HTTP-disabled exact-target redaction preview with an opaque full-row
  snapshot before any expected-snapshot service overload or mutation route.

## Completed Slice: `GOV-UX-01B26H`

- `requirement`: `GOV-UX-01B26H` HTTP-disabled exact-target display-profile
  redaction preview and stable snapshot.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: B26G completes read-only target selection/detail and B25A owns
  the existing exact target redaction transaction, but no write-free projection
  describes the affected current-profile/history rows, irreversible value loss,
  audit effect, or a stable full-row snapshot. Mutation authority cannot be
  issued safely from the bounded 32-row detail alone.
- `selection_reason`: a preview is the first dependency after the read path and
  before expected-snapshot mutation, authority issuance, confirmation, or
  browser work. It can reuse B26B/B26C exact target selection while keeping
  B25A, HTTP, registries, schema, and persistence format unchanged.
- `implementation_contract`: add only exported action
  `display_profile.redact`, a fixed identifier-free preview DTO, and
  `GovernanceQueryService.getDisplayProfileTargetRedactionPreviewForScope({
  scope, targetId })`. Validate the same exact-user scope and full lowercase
  target ID as B26F, reuse B26B's selected target and B26C's target-ID domain,
  then fingerprint every exact matching display-profile and nickname-history
  row through typed/hex SQL fields in deterministic order without materializing
  raw values in the DTO. Return the unchanged B26B target, exact safe-integer
  profile/history/open-history counts, a domain-separated 64-character snapshot
  fingerprint, exact redaction/closure/audit effects, explicit irreversible
  rollback boundary, and a separate domain-separated preview digest. Add no
  internal selector output, expected-snapshot mutation, authority, route,
  registry, CLI/QQ, or browser behavior.
- `allowed_paths`: `src/governance/query-service.ts`,
  `tests/unit/governance/query-service.test.ts`, `docs/governance-ui.md`, and
  this checkpoint only.
- `protected_paths`: B26A-G selectors, DTOs, bounds, target-ID and route
  behavior; B25A/B25B service and CLI behavior; all HTTP/session/scope/resource/
  preview registry behavior; identity repository and durable schema; every
  other query/service projection; migrations, dependencies/lockfile, Provider,
  QQ, private/live data, deployment, restart, container, browser assets, and
  every prior slice.
- `assertions`: malformed scope/target authority returns `null` before SQL.
  Unknown, cross-user, malformed-evidence, missing, and B26B-unselected targets
  return `null` before full-row snapshot reads. Exact private/global, group,
  history-only, already-redacted, and targets with valid plus malformed matching
  rows produce stable previews whose counts and fingerprint cover every row the
  existing B25A transaction would update. Any value/type/time/trust/lifecycle/
  row addition or removal changes snapshot fingerprint and preview digest. The
  DTO contains no raw user/group/target/profile/history/session/platform/secret
  value or internal row ID; response size is fixed, iteration is constant-
  memory, safe-integer overflow fails closed, repeated reads are byte-stable and
  write-free, integrity is `ok`, and FKs are empty. B26A-G, B25A/B25B, HTTP,
  registries, CLI/QQ, and mutations remain uncalled or unchanged.
- `focused_commands`: run `git diff --check`, capture a private four-file B26G
  baseline, add and run one named B26H fail-first query-service regression
  before source or canonical-doc edits, rerun the named case, full query
  service, query/scope/resource-registry/HTTP adjacency, canonical P8,
  typecheck, scoped/full ESLint, build, release preflight, normal-umask release,
  hygiene, and exact three-path reverse-first/forward-first proof.
- `schema_dependency_live_boundary`: query projection, synthetic unit coverage,
  one canonical governance contract, and checkpoint only. No persistence write
  or format change, service mutation, HTTP/session/registry/authority path,
  schema, migration, dependency, lockfile, CLI/QQ, browser, Provider, private
  DB, deployment, restart, container, or live operation is permitted.
- `rollback_boundary`: after this contract and a clean diff check, capture exact
  B26G bytes and modes for the three immutable query/test/document paths plus
  this checkpoint under one new mode-`0700` private root. The final three-path
  ASCII patch must independently prove reverse-first and forward-first byte/mode
  restoration without touching the workspace; checkpoint evidence is excluded.
- `baseline_evidence`: `git diff --check` passed, then mode-`0700`
  `/tmp/lethebot-gov-ux-01b26h-roundtrip-Mz5vWT` captured all four paths at mode
  `0644` under mode-`0700` parent directories. Query source, unit test,
  governance contract, and pre-evidence checkpoint hashes are
  `d560219dc6cc524d9412cccd0737bf733c72be184b2e945e7be53266211aa385`,
  `793f2c582446c73500bda8beec816a657f7abfca7e28aa0d471b9d35e4965e9a`,
  `3a0f62ba9e2ea19bca5b157fbbd7b57b4105fe26488af65d7112d93aecd33195`,
  and `706f266226a578a4be528121c7a770e7dea7ffd5eb1bbdb95efdd9fb92da675f`.
  Workspace and baseline bytes match; inventory remains 74 tracked
  modifications, 60 untracked paths, and zero staged at `main@cfebb77`.
- `fail_first_evidence`: the named run collected 51 query-service cases,
  skipped 50 unrelated cases, and failed only with
  `service.getDisplayProfileTargetRedactionPreviewForScope is not a function`.
  Query source and the canonical governance contract retained their baseline
  hashes. The mode-`0600` log is
  `/tmp/lethebot-gov-ux-01b26h-focused-fail-first.log`
  (`sha256:a938bd87e6d2ab3255ba22167274e4215d0aa63813454438d2c7f5d5b242c285`,
  size `1271`).
- `final_evidence`: the named B26H regression passed 1 / 1 with 50 skipped;
  full query-service coverage passed 51 / 51; canonical four-file query/scope/
  resource-registry/HTTP-wiring adjacency passed 92 / 92; and canonical P8
  passed 7 files / 325 tests. An additional generic HTTP adjacency run passed
  78 / 78. Typecheck, scoped and full ESLint, build, release preflight, and
  `git diff --check` passed. The complete normal-umask release gate passed 137 /
  1 skipped files with 2826 / 10 skipped tests. Mode-`0600` logs are focused
  final `/tmp/lethebot-gov-ux-01b26h-focused-final.log`
  (`sha256:5da1446dfc346b6e440f777f2d9acf0f2eb08db8c2fbe56ccc3c8a2d48e73149`),
  full query `/tmp/lethebot-gov-ux-01b26h-query-service.log`
  (`sha256:784d4ed7494174ab956c3aa6cdf7544044d701fe036d98878b9660d40a178788`),
  canonical adjacency `/tmp/lethebot-gov-ux-01b26h-adjacency.log`
  (`sha256:f895bc7403406cebf602ba9d5646c589da726cb8269c38928f28f112a2ce7d89`),
  P8 `/tmp/lethebot-gov-ux-01b26h-p8.log`
  (`sha256:760a7fd9d58805625c6527a60c72134b2d28a393d70b54e96d77ed653aa3a762`),
  and release `/tmp/lethebot-gov-ux-01b26h-release.log`
  (`sha256:0fe15409e11c214c13e3c116270ec7059c212872e9f4b31392792f4ec0e69cc8`).
  Exact private/global, group, history-only, already-redacted, malformed-match,
  and overflow cases prove B26B authority before full-row reads; every matching
  profile/history row contributes typed storage class and exact bytes to a
  constant-memory domain-separated snapshot. Exact affected/open counts,
  conditional effects, irreversible rollback semantics, and preview digest are
  stable; value/type/time/trust/lifecycle/add/remove drift changes both opaque
  fingerprints. Outputs omit every raw identifier/value and all reads are
  write-free with clean integrity/FKs; B26A-G, B25A/B25B, HTTP, registries,
  CLI/QQ, and mutations remain unchanged.
- `rollback_evidence`: the exact three-path patch is
  `/tmp/lethebot-gov-ux-01b26h-roundtrip-Mz5vWT/gov-ux-01b26h.patch`
  (`sha256:3bdbfaf18a6f3d220d32e5cff6a0690afcea6bb0960c370c4fdd4e0a0a7fc622`,
  mode `0600`, size `28226`). It contains only the canonical governance
  contract, query source, and named unit regression, is ASCII-only with zero
  NUL bytes, and independently passed reverse-first and forward-first byte/mode
  cycles without touching the workspace. All 29 proof directories are mode
  `0700`; all 13 non-patch proof files are mode `0644`; candidate snapshots
  equal current workspace bytes and modes.
- `inventory_evidence`: inventory remains 74 tracked modifications, 60
  untracked paths, and zero staged at `main@cfebb77`; sensitive untracked-
  filename screening is empty. Pi dependencies remain `0.80.2`; migrations
  007/008 retain SHA-256
  `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b` /
  `95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`.
  No stage, commit, Provider/QQ, private DB, deployment, restart, service
  mutation, authority, route, schema, dependency, or browser operation occurred.
- `exact_fail_first_action`: add only `previews exact display-profile target
  redaction with stable irreversible effects` to
  `tests/unit/governance/query-service.test.ts`, then run it alone before editing
  query source or the canonical governance contract. It must fail only because
  `getDisplayProfileTargetRedactionPreviewForScope` is absent.
- `exact_next_action`: completed; the dependency reassessment selects B26I
  below, an atomic expected-snapshot overload on B25A before preview authority
  or any HTTP mutation route.

## Completed Slice: `GOV-UX-01B26I`

- `requirement`: `GOV-UX-01B26I` atomic expected-snapshot display-profile
  redaction service overload.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: B26H now binds every row the legacy B25A operation would
  update, but B25A accepts only raw user/group selection and always mutates. A
  later confirmation cannot atomically reject target drift between preview and
  redaction while preserving the existing CLI path.
- `selection_reason`: expected-snapshot mutation is the next dependency before
  authority issuance or confirmation. Reusing B26H's exact snapshot reader in
  one immediate transaction avoids duplicating fingerprint semantics and keeps
  route/session work separate.
- `implementation_contract`: extract B26H's constant-memory snapshot reader and
  target-ID derivation as trusted query-service helpers without changing its
  DTO or digest. Add an overload
  `GovernanceService.redactDisplayProfileAsLocalAdmin(input)` whose expected
  input contains exact canonical user, optional group, full target ID, B26H
  counts/fingerprint, fixed reason code, and optional clock. Strictly validate
  all fields, begin an immediate transaction, recompute the exact snapshot,
  return `not_found` for no rows or `stale` for invalid/drifted authority before
  writes, then perform only B25A's exact profile/history redaction and audit.
  Verify matched counts inside the lock and roll back on mismatch or audit
  failure. Return fixed affected counts and timestamp on success. Preserve the
  legacy input overload and exact CLI behavior byte-for-byte. Add no preview
  change, authority, route, registry, CLI/QQ grammar, or browser behavior.
- `allowed_paths`: `src/governance/query-service.ts`,
  `src/governance/service.ts`, `tests/unit/governance/query-service.test.ts`,
  `tests/unit/governance/service.test.ts`, `docs/governance-ui.md`, and this
  checkpoint only.
- `protected_paths`: B26A-H external selector/DTO/digest behavior; B26D-G HTTP
  behavior; B25A legacy input/output/audit/transaction and CLI behavior; B25B;
  all HTTP/session/scope/resource/preview registry implementations; identity
  repository and durable schema; other governance mutations; migrations,
  dependencies/lockfile, Provider, QQ, private/live data, deployment, restart,
  container, browser assets, and every prior slice.
- `assertions`: malformed user/group/target/reason/time/count/fingerprint input
  returns `stale` before writes. Exact current private/global, group, history-
  only, already-redacted, and valid-plus-malformed-row snapshots redact exactly
  the B26H counts, close only open history intervals, append one redacted fixed-
  reason audit, and return fixed count/time evidence. Missing rows return
  `not_found`; any value/type/time/trust/lifecycle/add/remove/count/fingerprint/
  target drift returns `stale` with zero profile/history/audit changes. Count
  mismatch and audit failure roll back atomically. A fresh process observes the
  committed success, integrity is `ok`, and FKs are empty. The existing legacy
  service/CLI cases remain unchanged; B26H before/after output is identical;
  HTTP, registries, authority, preview route, CLI/QQ grammar, and browser remain
  unchanged.
- `focused_commands`: run `git diff --check`, capture a private six-file B26H
  baseline, add and run one named B26I fail-first service regression before
  source/doc edits, rerun it plus the legacy B25A service/CLI characterization,
  full query and service suites, governance/CLI/HTTP adjacency, canonical P8,
  typecheck, scoped/full ESLint, build, release preflight, normal-umask release,
  hygiene, and exact four-path reverse-first/forward-first proof.
- `schema_dependency_live_boundary`: shared snapshot helper refactor, service
  overload, synthetic unit coverage, one canonical contract, and checkpoint
  only. No persistence format change, authority, HTTP/session/registry path,
  schema, migration, dependency, lockfile, CLI/QQ grammar, browser, Provider,
  private DB, deployment, restart, container, or live operation is permitted.
- `rollback_boundary`: after this contract and a clean diff check, capture exact
  B26H bytes and modes for query/service sources, their unit tests, the canonical
  governance contract, and this checkpoint under one new mode-`0700` private
  root. The final exact changed-path ASCII patch must independently prove
  reverse-first and forward-first byte/mode restoration without touching the
  workspace; checkpoint evidence is excluded.
- `baseline_evidence`: `git diff --check` passed, then mode-`0700`
  `/tmp/lethebot-gov-ux-01b26i-roundtrip-UC75tu` captured all six paths at mode
  `0644` under mode-`0700` parent directories. Query source, service source,
  query unit test, service unit test, canonical governance contract, and pre-
  evidence checkpoint hashes are
  `dbadbba54c994af5184fc2a076821aa17c7ab490f9a8757a8fa7a78a759de431`,
  `6d5b939af02663c2d795da08a51964661e28f28803af1463f595b95f7555ea81`,
  `c8d88fac4040881edd2f7aafe330b22f02e0500636946cf5b43b1d96955f4d07`,
  `02cda9f71cdd72d6cce6c72c5f1eadf1f2516cb6a605d47d87e7cb1515f3861d`,
  `0b1454a3692dd4923adf4ab36f730a5e28564ef2f48e116c6ceb54ba566236e9`,
  and `c3a08773d887be175891532e2311bdb747ca3bc10cf3caf6fe009b0241329289`.
  Workspace and baseline bytes match; the all-path inventory remains 74 tracked
  modifications, 60 untracked paths, and zero staged at `main@cfebb77`.
- `fail_first_evidence`: the named run collected 15 service cases, skipped 14
  unrelated cases, and failed only because the expected-snapshot object reached
  the legacy unconditional mutation and returned scalar `3` instead of fixed
  redaction count/time evidence. Query/service sources, the query unit test, and
  the canonical governance contract retained their baseline hashes. The mode-
  `0600` log is `/tmp/lethebot-gov-ux-01b26i-focused-fail-first.log`
  (`sha256:ee9d39bc73cac3d5b76d45c2233a9e87595831272a9da065cc31c466a8e02a5b`,
  size `1339`).
- `final_evidence`: the named expected-snapshot regression and focused legacy
  service characterization passed 2 / 2; B26H query parity passed 1 / 1; the
  legacy CLI characterization passed 2 / 2; full query/service coverage passed
  66 / 66; seven-file registry/CLI/HTTP adjacency passed 165 / 165; and the
  canonical P8 gate passed 7 files / 326 tests. Typecheck, scoped and full
  ESLint, build, release preflight, and `git diff --check` passed. The complete
  normal-umask release gate passed 137 / 1 skipped files with 2827 / 10 skipped
  tests. Mode-`0600` logs are focused final
  `/tmp/lethebot-gov-ux-01b26i-focused-final.log`
  (`sha256:9718085b8a20186bbd436b765610b47899663bf09fa44970138e448886888c37`),
  B26H parity `/tmp/lethebot-gov-ux-01b26i-query-parity.log`
  (`sha256:2653d6c30566dc28d72d91f0168d0d01961d5bc793675286e7ad7dd49ba57671`),
  legacy CLI `/tmp/lethebot-gov-ux-01b26i-legacy-cli.log`
  (`sha256:952524d6a65c6c3628499f601cfb5409baf4143dc461135fc486087e083e818c`),
  full query/service `/tmp/lethebot-gov-ux-01b26i-query-service.log`
  (`sha256:fb7f682697275fa83ba9c08fe18d60555cf7ae9200543e10bbe35558e671840e`),
  adjacency `/tmp/lethebot-gov-ux-01b26i-adjacency.log`
  (`sha256:4ac0a5eca23d840fdb2587541e9b5459de26fbed63bf971b341cc1a808fb2735`),
  P8 `/tmp/lethebot-gov-ux-01b26i-p8.log`
  (`sha256:70cc75968300b8d087dab14ed2b24558cca1e6285c3825112fa47e21ff62dae2`),
  static `/tmp/lethebot-gov-ux-01b26i-static.log`
  (`sha256:9b3c33997b2e4e8c3522b52bdf67f62d60f1a99ffb7e460f150bd9de616d6577`),
  and release `/tmp/lethebot-gov-ux-01b26i-release.log`
  (`sha256:d9fcbbba6432fe85a50f2f177d7a0a7a03a11e5128d25ce773e144f682913d14`).
  Exact private/global, group, history-only, already-redacted, malformed-match,
  and missing cases prove strict validation and exact B26H snapshot parity.
  Value/type/time/trust/lifecycle/add/remove/count/fingerprint/target drift,
  profile/history update-count mismatch, and audit failure are write-free or
  roll back atomically; committed success survives reopen with clean integrity
  and FKs. Legacy service and CLI behavior remain unchanged.
- `rollback_evidence`: the exact four-path patch is
  `/tmp/lethebot-gov-ux-01b26i-roundtrip-UC75tu/gov-ux-01b26i.patch`
  (`sha256:499e4f3982f515445d0865ffc11674f63538c3f1d739f569f4dbc7cba81aae3d`,
  mode `0600`, size `52137`). It contains only the canonical governance
  contract, query/service sources, and named service regression; the query
  unit test is byte-identical to baseline. Reverse-first and forward-first
  application restored baseline and candidate bytes/modes exactly without
  touching the workspace. All 29 proof directories are mode `0700`; all 20
  non-patch proof files are mode `0644`; candidate snapshots equal current
  workspace bytes and modes.
- `inventory_evidence`: inventory remains 74 tracked modifications, 60
  untracked paths, and zero staged at `main@cfebb77`. Pi dependencies remain
  `0.80.2`; migrations 007/008 retain SHA-256
  `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b` /
  `95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`.
  No stage, commit, Provider/QQ, private DB, deployment, restart, schema,
  migration, dependency, authority, HTTP route, or browser operation occurred.
- `exact_fail_first_action`: add only `redacts display-profile target only from
  the exact current snapshot` to `tests/unit/governance/service.test.ts`, then
  run it alone before source or canonical-doc edits. It must fail only because
  the expected-snapshot input still reaches the legacy unconditional mutation.
- `exact_next_action`: completed; the dependency reassessment selects B26J
  below, a preview-only exact-authority HTTP route before confirmation.

## Completed Slice: `GOV-UX-01B26J`

- `requirement`: `GOV-UX-01B26J` exact-authority display-profile redaction
  preview route and confirmation-handle issuance.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: B26G exposes exact current-session target detail, B26H owns a
  write-free full-row redaction preview, and B26I owns the atomic final fence,
  but no HTTP route invokes B26H or issues action-specific confirmation
  authority. The target resource path remains read-only.
- `selection_reason`: preview authority is the next dependency before a
  confirmation route. Reusing the established mutation-marked dynamic-resource
  boundary and generic single-use registry keeps B26I unreachable while binding
  the complete B26H digest and snapshot evidence for a later confirmation.
- `implementation_contract`: register only mutation-marked
  `POST /governance/api/v1/display-profile/targets/:resourceHandle` with the
  existing display-profile read purpose and `display_profile_target` kind. The
  shared boundary must authenticate, enforce exact Origin/CSRF, parse the
  bounded body, and resolve current-session exact user-scope and target-resource
  authority before the callback. Accept only exact `{ "action": "redact" }`,
  call only B26H with the resolved scope and registry-owned target ID, conceal a
  missing preview as `404`, and otherwise return the unchanged preview plus a
  fresh opaque handle/expiry with `201`. Bind the handle to the current session,
  actor, `display_profile.redact` action, resource kind/ID, exact scope,
  `expectedState = current.snapshotFingerprint`, `expectedRevisionNumber =
  expected.affectedRows.total`, and B26H `previewDigest`. Add no confirmation
  route, preview consumption, B26I/B25A invocation, mutation, service/query/
  registry behavior change, CLI/QQ grammar, or browser behavior.
- `allowed_paths`: `src/index.ts`,
  `tests/integration/governance-http-wiring.test.ts`, `docs/architecture.md`,
  `docs/deployment.md`, `docs/governance-ui.md`, `docs/security-privacy.md`, and
  this checkpoint only.
- `protected_paths`: B26A-I query/service helpers, projections, digests,
  snapshots, legacy and expected-snapshot mutations; all generic HTTP/session/
  scope/resource/preview registry implementations; every other route/action;
  CLI/QQ/browser assets; repository/schema/migrations/dependencies/lockfile;
  Provider, private/live data, deployment, restart, container, persistence
  format, and every prior slice.
- `assertions`: unauthenticated requests return `401`; missing/wrong Origin or
  CSRF returns `403`; query strings, missing scope, malformed handles, and every
  body other than exact `{ "action": "redact" }` fail before B26H or handle
  issuance. Unknown, cross-session, cross-purpose, cross-kind, or cross-scope
  authority is concealed. A missing current preview returns fixed `404` without
  issuance. Exact private/global and group authority calls B26H once with only
  resolved scope/target ID, returns its DTO byte-for-byte plus fresh opaque
  authority, and binds all registry fields exactly. Repeated valid previews are
  write-free, B26I/B25A and preview consumption remain uncalled, the future
  confirmation path stays absent, and integrity/FKs remain clean.
- `focused_commands`: run `git diff --check`, capture a private seven-file B26I
  baseline, add and run one named absent-route integration regression before
  production source or canonical route-doc edits, rerun it, full governance
  HTTP wiring, query/service/HTTP/scope/resource/preview-registry adjacency,
  canonical P8, typecheck, scoped/full ESLint, build, release preflight,
  normal-umask release, hygiene, and exact six-path reverse-first/forward-first
  proof.
- `schema_dependency_live_boundary`: route registration/wiring, synthetic
  integration coverage, four canonical route contracts, and checkpoint only.
  No query/service/registry implementation or persistence behavior, schema,
  migration, dependency, lockfile, confirmation, CLI/QQ/browser, Provider,
  private DB, deployment, restart, container, or live operation is permitted.
- `rollback_boundary`: after this contract and a clean diff check, capture exact
  B26I bytes/modes for source, integration test, four canonical route documents,
  and this checkpoint under one new mode-`0700` private root. The final exact
  six-path ASCII patch must independently prove reverse-first and forward-first
  byte/mode restoration without touching the workspace; checkpoint evidence is
  excluded.
- `baseline_evidence`: `git diff --check` passed, then mode-`0700`
  `/tmp/lethebot-gov-ux-01b26j-roundtrip-C5PQjb` captured all seven paths at
  mode `0644` under mode-`0700` parent directories. Source, integration test,
  architecture, deployment, governance, security, and pre-evidence checkpoint
  hashes are
  `1e53ba55a5ba4acb32b95d071c505d895b57993820b244d317b90138bd3ee4cd`,
  `54e1cb770a274138b559b4096271e9aaef8b4131f362f23cb4b3890a0a7e3005`,
  `1cee769d6ae3a25a9c4905f09ebbb6e3cfe43a77173fcb2db4f19641c1cbf364`,
  `8ab59af81039dcc31ecd65b588a367edc629e7f29e72fe81a6778a958b196ef5`,
  `246464cfe7f764377e015c60038c44c27d66871aad5bb6d49e42d3e2332d3030`,
  `8b10ffdd9029934246fb598cbdd8bb770e5777fb2570fba041c2c2944b91ccc0`,
  and `5111daf3b15c0d5e0ee4a56057c56cdbf3c0ab383956b1e78977fc40c8b3f820`.
  Workspace and baseline bytes match; inventory remains 74 tracked
  modifications, 60 untracked paths, and zero staged at `main@cfebb77`.
- `fail_first_evidence`: the named run collected 36 governance HTTP wiring
  cases, skipped 35 unrelated cases, and failed only because unauthenticated
  `POST /governance/api/v1/display-profile/targets/:resourceHandle` returned
  absent-route `404` instead of required `401`. B26H, preview issuance/
  consumption, scope/resource resolution, B26I/B25A, and B25B were uncalled at
  the assertion. Source and all four canonical route documents retained their
  baseline hashes. The mode-`0600` log is
  `/tmp/lethebot-gov-ux-01b26j-focused-fail-first.log`
  (`sha256:cd3bdaf794bf4d824af94aecbdf5c9b66920db34088c2301378dab4a8251a695`,
  size `1659`).
- `final_evidence`: the named regression passed 1 / 1 with 35 skipped; full
  governance HTTP wiring passed 36 / 36 after two superseded route-absence
  characterizations were narrowed to their still-valid malformed/confirmation
  boundaries; seven-file query/service/HTTP/scope/resource/preview-registry
  adjacency passed 133 / 133; and the canonical P8 gate passed 7 files / 326
  tests. Typecheck, scoped and full ESLint, build, release preflight, and
  `git diff --check` passed. The complete normal-umask release gate passed 137 /
  1 skipped files with 2828 / 10 skipped tests. Mode-`0600` logs are focused
  final `/tmp/lethebot-gov-ux-01b26j-focused-final.log`
  (`sha256:c1367f97b4935e17217d9e056e19a10b461964c1ae527b4de73241aaaab6f19e`),
  full wiring `/tmp/lethebot-gov-ux-01b26j-http-wiring.log`
  (`sha256:a92fca3bdc696959488bb7bc11b5dea17ca362c4e27cca7c13cd3c3d856a56d1`),
  adjacency `/tmp/lethebot-gov-ux-01b26j-adjacency.log`
  (`sha256:c9a009726446ab911c9ae17b4e4c119942698692ebc885e887d22482badf7880`),
  P8 `/tmp/lethebot-gov-ux-01b26j-p8.log`
  (`sha256:595d4cbba97836d94090049460789dfef7cd40b7d01e3bad6c69059fe6992918`),
  static `/tmp/lethebot-gov-ux-01b26j-static.log`
  (`sha256:9b3c33997b2e4e8c3522b52bdf67f62d60f1a99ffb7e460f150bd9de616d6577`),
  and release `/tmp/lethebot-gov-ux-01b26j-release.log`
  (`sha256:bf954aa752eabb04f18b7c9e845d3abe4e2e1c5efa4853ec198c7a7d73492579`).
  Exact authentication, Origin/CSRF, body, query, scope/resource/session/
  purpose/kind boundaries precede B26H and issuance. Private/global and group
  previews return B26H unchanged plus fresh authority bound exactly to the
  snapshot fingerprint, total affected rows, and digest. Missing targets remain
  concealed; repeated previews are write-free; outputs omit raw identifiers;
  confirmation, preview consumption, B26I/B25A/B25B, and durable effects remain
  absent with clean integrity/FKs.
- `rollback_evidence`: the exact six-path patch is
  `/tmp/lethebot-gov-ux-01b26j-roundtrip-C5PQjb/gov-ux-01b26j.patch`
  (`sha256:62280aaaa2771e35b3d42b6c1546cd2cb1924cd8bb80b1a33c188c97a5e4ffe2`,
  mode `0600`, size `33577`). It contains only source, the wiring regression,
  and four canonical route documents; it is ASCII-only and NUL-free. Strict
  candidate-to-baseline-to-candidate and baseline-to-candidate-to-baseline
  cycles applied with zero fuzz and restored all seven captured paths byte-for-
  byte and mode-for-mode without touching the workspace; the checkpoint stayed
  identical. All 21 proof directories are mode `0700`; all 28 non-patch proof
  files are mode `0644`; candidate product/test/doc snapshots equal current
  workspace bytes and modes.
- `inventory_evidence`: inventory remains 74 tracked modifications, 60
  untracked paths, and zero staged at `main@cfebb77`; sensitive untracked-
  filename screening is empty. Query/service, generic HTTP, all three registry
  implementations, lockfile, Pi dependencies `0.80.2`, and migrations 007/008
  remain unchanged; migration SHA-256 values remain
  `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b` /
  `95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`.
  No stage, commit, Provider/QQ, private DB, deployment, restart, schema,
  migration, dependency, confirmation, durable mutation, or browser operation
  occurred.
- `exact_fail_first_action`: add only `issues display-profile redaction preview
  authority without mutation` to
  `tests/integration/governance-http-wiring.test.ts`, then run it alone before
  editing `src/index.ts` or canonical route docs. Its first unauthenticated POST
  must expect `401` and fail only because the absent route returns `404`, while
  B26H, preview issuance/consumption, B26I, and B25A remain uncalled.
- `exact_next_action`: completed; dependency reassessment selects B26K below,
  an HTTP-disabled exact target-to-mutation selector before confirmation.

## Completed Slice: `GOV-UX-01B26K`

- `requirement`: `GOV-UX-01B26K` HTTP-disabled exact display-profile target
  mutation selector.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: B26J binds an opaque one-way target ID, B26H deliberately
  omits raw group identifiers, and B26I requires the exact canonical user plus
  optional group selector. A confirmation handler therefore cannot construct
  B26I input for a group target without domain SQL, weakening the target-ID
  boundary, or adding one shared trusted resolver.
- `selection_reason`: resolve that internal dependency before confirmation.
  Reusing B26B's bounded selected target rows and B26C's target-ID derivation in
  the query owner preserves identifier privacy and keeps the future handler
  free of SQL and selector inference.
- `implementation_contract`: add only exported internal DTO
  `DisplayProfileTargetRedactionMutationSelection` and
  `GovernanceQueryService.resolveDisplayProfileTargetRedactionMutationForScope({
  scope, targetId })`. Validate the same exact-user scope and full lowercase
  B26C target ID as B26H, reuse B26B selection, and match only through B26C's
  derivation. Return the exact canonical user, `groupId: null` for private/
  global or the exact non-empty group ID for group evidence, and the unchanged
  target ID. Return `null` for malformed, cross-user, missing, unselected, or
  malformed-evidence targets. This trusted internal result must never be added
  to B26A-J DTOs or serialized. Add no snapshot/preview change, authority,
  route, registry, service, mutation, CLI/QQ, or browser behavior.
- `allowed_paths`: `src/governance/query-service.ts`,
  `tests/unit/governance/query-service.test.ts`, `docs/governance-ui.md`, and
  this checkpoint only.
- `protected_paths`: B26A-J selectors' observable filters/order/bounds, every
  existing DTO/digest/snapshot and B26I service behavior; all HTTP/session/
  scope/resource/preview registry behavior; CLI/QQ/browser code; repository/
  schema/migrations/dependencies/lockfile; Provider, private/live data,
  deployment, restart, container, persistence format, and every prior slice.
- `assertions`: malformed scope/target returns `null` before SQL. Exact private/
  global, group, and history-only selected targets return only exact mutation
  selectors, with private group `null`; cross-user, missing, malformed-evidence,
  and B26B-unselected targets return `null`. B26B/B26C pages and B26H preview/
  digest remain byte-identical; no existing public response gains a raw user,
  group, target, session, platform, secret, or internal row identifier. Reads
  are stable and write-free with clean integrity/FKs; HTTP, registries, B26I,
  CLI/QQ, and browser remain unchanged.
- `focused_commands`: run `git diff --check`, capture a private four-file B26J
  baseline, add and run one named missing-method regression before source or
  canonical-doc edits, rerun it, full query service, B26H parity, query/service/
  HTTP/registry adjacency, canonical P8, typecheck, scoped/full ESLint, build,
  release preflight, normal-umask release, hygiene, and exact three-path reverse-
  first/forward-first proof.
- `schema_dependency_live_boundary`: one trusted read-only query projection,
  synthetic unit coverage, canonical governance contract, and checkpoint only.
  No route/authority/service/registry/persistence behavior, schema, migration,
  dependency, lockfile, CLI/QQ/browser, Provider, private DB, deployment,
  restart, container, or live operation is permitted.
- `rollback_boundary`: after this contract and a clean diff check, capture exact
  B26J bytes/modes for query source/test, canonical governance contract, and
  this checkpoint under one new mode-`0700` private root. The final exact three-
  path ASCII patch must independently prove reverse-first and forward-first
  byte/mode restoration without touching the workspace; checkpoint evidence is
  excluded.
- `baseline_evidence`: `git diff --check` passed, then mode-`0700`
  `/tmp/lethebot-gov-ux-01b26k-roundtrip-D9rPK8` captured all four paths at mode
  `0644` under mode-`0700` parent directories. Query source, query unit test,
  canonical governance contract, and pre-evidence checkpoint hashes are
  `9f36e47d515fa953c02e2763892f34b3ff4087389c21c4c9bf0557047fadcb49`,
  `c8d88fac4040881edd2f7aafe330b22f02e0500636946cf5b43b1d96955f4d07`,
  `d7f6e3810593924c15d8b781bf312327393b2accf201b088efb0607bf88a7d93`,
  and `56cdd5682c0dcad01b071da11be3adbdd0eaccc0b5a0844a0d0cdd7ef9596ffa`.
  Workspace and baseline bytes match; inventory remains 74 tracked
  modifications, 60 untracked paths, and zero staged at `main@cfebb77`.
- `fail_first_evidence`: the named run collected 52 query-service cases,
  skipped 51 unrelated cases, and failed only because
  `resolveDisplayProfileTargetRedactionMutationForScope` was not a function.
  Query source and the canonical governance contract retained their baseline
  hashes. The mode-`0600` log is
  `/tmp/lethebot-gov-ux-01b26k-focused-fail-first.log`
  (`sha256:ab9e3c81160c7e1444fcdb308da49c2b2b73a9a861929eddb9b36af2f5632909`,
  size `1223`).
- `final_evidence`: the named resolver regression passed 1 / 1 with 51
  skipped; full query-service coverage passed 52 / 52; seven-file query/
  service/HTTP/registry adjacency passed 134 / 134; and the canonical P8 gate
  passed 7 files / 327 tests. Typecheck, scoped and full ESLint, build, release
  preflight, and `git diff --check` passed. The complete normal-umask release
  gate passed 137 / 1 skipped files with 2829 / 10 skipped tests. Mode-`0600`
  logs are focused final `/tmp/lethebot-gov-ux-01b26k-focused-final.log`
  (`sha256:422366404c01f1dc23b66cb6d9f99c7c857e359f7a2c8ed98c1c5abd51f4c0fe`),
  full query `/tmp/lethebot-gov-ux-01b26k-query-service.log`
  (`sha256:a4b86de6a9d4f6ae3148064d641c623a88d7acc89f253b7b0699843b9cebc95d`),
  adjacency `/tmp/lethebot-gov-ux-01b26k-adjacency.log`
  (`sha256:53b6d1d832178bea52cbfb4cc53ac953f72e6217d1a849458143c6dac5d7e443`),
  P8 `/tmp/lethebot-gov-ux-01b26k-p8.log`
  (`sha256:e42bee4ad452a03f402b83b18f55def221ae00b47c778f144ef23422eedeca69`),
  static `/tmp/lethebot-gov-ux-01b26k-static.log`
  (`sha256:9b3c33997b2e4e8c3522b52bdf67f62d60f1a99ffb7e460f150bd9de616d6577`),
  and release `/tmp/lethebot-gov-ux-01b26k-release.log`
  (`sha256:35df5f66c9ea1bb44f46332a4998524ecbf5be3e26b61fccb62a8b0ea9f7f956`).
  Exact private/global, group, and history-only targets resolve to only their
  mutation selectors; malformed, cross-user, missing, malformed-evidence, and
  bounded-page-excluded targets return `null`, with validation before SQL.
  Repeated selectors are stable and write-free; B26B/B26H public outputs remain
  byte-identical and identifier-free; HTTP, registries, and B26I remain
  unchanged with clean integrity/FKs.
- `rollback_evidence`: the exact three-path patch is
  `/tmp/lethebot-gov-ux-01b26k-roundtrip-D9rPK8/gov-ux-01b26k.patch`
  (`sha256:929a28bd298da38e0d9b2b9dc10ba40815508680ebdaceb05032a1e53b5c236e`,
  mode `0600`, size `10288`). It contains only query source/test and the
  canonical governance contract, is ASCII-only and NUL-free, and passed strict
  candidate-to-baseline-to-candidate and baseline-to-candidate-to-baseline
  zero-fuzz byte/mode cycles without touching the workspace. All 29 proof
  directories are mode `0700`; all 16 non-patch proof files are mode `0644`;
  candidate snapshots equal current workspace bytes and modes. An initial
  candidate-copy command used proof-root-relative source paths and produced no
  usable patch; corrected absolute workspace inputs generated the verified
  artifact before either proof cycle.
- `inventory_evidence`: inventory remains 74 tracked modifications, 60
  untracked paths, and zero staged at `main@cfebb77`; sensitive untracked-
  filename screening is empty. Pi dependencies remain `0.80.2`; migrations
  007/008 retain SHA-256
  `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b` /
  `95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`.
  No stage, commit, Provider/QQ, private DB, deployment, restart, schema,
  migration, dependency, authority, route, service mutation, or browser
  operation occurred.
- `exact_fail_first_action`: add only `resolves exact display-profile mutation
  selectors without changing public previews` to
  `tests/unit/governance/query-service.test.ts`, then run it alone before source
  or canonical-doc edits. It must fail only because
  `resolveDisplayProfileTargetRedactionMutationForScope` is absent.
- `exact_next_action`: completed; B26K removes the final selector dependency,
  so the reassessment selects the exact confirmation route in B26L below.

## Completed Slice: `GOV-UX-01B26L`

- `requirement`: `GOV-UX-01B26L` exact display-profile redaction confirmation
  route.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: B26J now issues exact single-use preview authority, B26K can
  resolve the internal B26I selectors, and B26I owns the atomic final fence, but
  no route consumes authority or performs the confirmed redaction.
- `selection_reason`: all confirmation dependencies are now deterministic-
  ready. One route can compose them without SQL, duplicated mutation logic, or
  raw identifier exposure.
- `implementation_contract`: register only mutation-marked
  `POST /governance/api/v1/display-profile/targets/:resourceHandle/confirm`
  with B26J's purpose and resource kind. After the shared auth, Origin/CSRF,
  bounded JSON, exact-scope, and exact-resource boundary, accept only exact
  `{ "confirm": true, "previewHandle": HANDLE }`. Consume only matching
  current-session/local-admin/`display_profile.redact`/resource/scope authority
  once. Unknown authority is `404`; replay is `409`. Recompute B26H and require
  exact snapshot fingerprint, total affected rows, and preview digest parity;
  drift or disappearance is `409`. Resolve only B26K, then invoke only B26I with
  its exact selectors, B26H current snapshot, and fixed reason. Map B26I
  `not_found` to `404`, `stale` to `409`, and success to an identifier-free
  `200` result containing the unchanged target projection, exact affected/open-
  closure counts, ISO redaction time, preview effects/privacy consequences,
  fixed audit evidence, and irreversible rollback boundary. Add no legacy
  service/CLI change, alternate mutation, restore, registry implementation,
  QQ grammar, or browser behavior.
- `allowed_paths`: `src/index.ts`,
  `tests/integration/governance-http-wiring.test.ts`, `docs/architecture.md`,
  `docs/deployment.md`, `docs/governance-ui.md`, `docs/security-privacy.md`, and
  this checkpoint only.
- `protected_paths`: B26A-K query/service/DTO/snapshot/digest/selector behavior;
  B26J preview response and issuance; all generic HTTP/session/scope/resource/
  preview registry implementations; legacy CLI/QQ profile behavior; identity
  mutation; repository/schema/migrations/dependencies/lockfile; Provider,
  private/live data, deployment, restart, container, browser assets, and every
  prior slice.
- `assertions`: unauthenticated access is `401`; wrong/missing Origin or CSRF is
  `403`; query, scope/resource, and exact-body failures precede consumption or
  owners. Unknown/cross-session/action/resource/scope authority is `404`; replay
  is `409`. Snapshot/count/digest drift consumes once, returns `409`, and never
  calls B26K/B26I. Exact private/global and group confirmation calls B26H, B26K,
  and B26I once in order with only trusted inputs; redacts exact profile/history
  rows, closes only open histories, appends one fixed audit, returns exact fixed
  identifier-free evidence, survives reopen, and rejects replay without a
  second effect. B26I `not_found`/`stale` map to `404`/`409`; audit failure rolls
  back. B26J preview, legacy CLI/service, unrelated targets, integrity/FKs, and
  the absent restore/browser paths remain unchanged.
- `focused_commands`: run `git diff --check`, capture a private seven-file B26K
  baseline, add and run one named absent-route integration regression before
  source or canonical route-doc edits, rerun it, full governance HTTP wiring,
  B26H/B26I/B26K and CLI adjacency, query/service/HTTP/registry adjacency,
  canonical P8, typecheck, scoped/full ESLint, build, release preflight, normal-
  umask release, hygiene, and exact six-path reverse-first/forward-first proof.
- `schema_dependency_live_boundary`: confirmation route composition, synthetic
  integration coverage, four canonical route contracts, and checkpoint only.
  No query/service/registry implementation, persistence format, schema,
  migration, dependency, lockfile, CLI/QQ/browser, Provider, private DB,
  deployment, restart, container, or live operation is permitted.
- `rollback_boundary`: after this contract and a clean diff check, capture exact
  B26K bytes/modes for source, integration test, four canonical route documents,
  and this checkpoint under one new mode-`0700` private root. The final exact
  six-path ASCII patch must independently prove reverse-first and forward-first
  byte/mode restoration without touching the workspace; checkpoint evidence is
  excluded.
- `baseline_evidence`: `git diff --check` passed, then mode-`0700`
  `/tmp/lethebot-gov-ux-01b26l-roundtrip-y3gotv` captured all seven paths at
  mode `0644` under mode-`0700` parent directories. Source, integration test,
  architecture, deployment, governance, security, and pre-evidence checkpoint
  hashes are
  `b64f704d92e248b4c196aa4a7af03305c99b678c061242b5e8f181ab5ef727d4`,
  `59634cf7a2b7a0984c5655e59c0cb7dadfb02d041aa8bc0887d10fbd64e5f407`,
  `21037f6e389005aa6afde2a926bd0c5fc43b3bd1d91884292fbc5adea9c444b4`,
  `51fa2147188c93584678267671930af3e01bd2e36bf69fe32bb2032e2968bcf2`,
  `90e9e01b4eb1714c143208c0e482f3b76736c2474faff91c210bbc2853bd0309`,
  `bbf73b3f6bca09e1a582a94bf997c2baa86faf1a466a5f0f6c46cab4a501734a`,
  and `f97c4014091f171d003b6c865d6f2018e1f7bdb30e84c5314c71e7c4ffbdaa1c`.
  Workspace and baseline bytes match; inventory remains 74 tracked
  modifications, 60 untracked paths, and zero staged at `main@cfebb77`.
- `fail_first_evidence`: the named run collected 37 governance HTTP wiring
  cases, skipped 36 unrelated cases, and failed only because unauthenticated
  exact target confirmation returned absent-route `404` instead of required
  `401`. Preview consumption, B26H, B26K, B26I, and B25B were uncalled. Source
  and all four canonical route documents retained their baseline hashes. The
  mode-`0600` log is
  `/tmp/lethebot-gov-ux-01b26l-focused-fail-first.log`
  (`sha256:fcd74543880cc7fb8ce710aa4884a5f7f6829310ed85f346b2adb79db37af99c`,
  size `1708`).
- `final_evidence`: the named confirmation regression passed 1 / 1 with 36
  skipped, full governance HTTP wiring passed 37 / 37, seven-file query/service/
  HTTP/registry adjacency passed 135 / 135, and the canonical P8 gate passed
  327 / 327. Typecheck, scoped and full ESLint, build, release preflight, and
  `git diff --check` passed. The complete normal-umask release gate passed 137 /
  1 skipped files with 2830 / 10 skipped tests. Mode-`0600` logs are focused
  final `/tmp/lethebot-gov-ux-01b26l-focused-final.log`
  (`sha256:b0f9114fe3d45e818a1d487790f18e3365abbdc7a4cc31418c6f5b5485ec9ce2`),
  full wiring `/tmp/lethebot-gov-ux-01b26l-wiring.log`
  (`sha256:3e4b710d59877b4f0f05e0380114054b250750906a95b3567db3f97c63f8563c`),
  adjacency `/tmp/lethebot-gov-ux-01b26l-adjacency.log`
  (`sha256:f239d77a98c03e1018427850abd92da5c496106fa8a3ba6f1534eec63aa18d9d`),
  P8 `/tmp/lethebot-gov-ux-01b26l-p8.log`
  (`sha256:d162c96001aa76c25406d13d2583dde421c8fc304b457854b882c91c7f5ce9df`),
  static `/tmp/lethebot-gov-ux-01b26l-static.log`
  (`sha256:9b3c33997b2e4e8c3522b52bdf67f62d60f1a99ffb7e460f150bd9de616d6577`),
  and release `/tmp/lethebot-gov-ux-01b26l-release.log`
  (`sha256:e6d5f770636a1d28f3699aa5682149b587637864d9c2e6a01806956e799bfba4`).
  Exact private/global and group confirmations consume matching authority once,
  reject unknown/replay/drift and simulated owner failures with fixed status,
  redact only exact profile/history rows, close only open histories, append one
  fixed redacted audit per effect, omit every raw identifier/secret/handle, and
  preserve integrity/FKs. Full wiring first exposed two earlier absent-route
  characterizations and scoped lint exposed one unused new-test spy; only those
  stale test expectations/declaration were corrected before the final gates.
- `rollback_evidence`: the exact six-path patch is
  `/tmp/lethebot-gov-ux-01b26l-roundtrip-y3gotv/gov-ux-01b26l.patch`
  (`sha256:53706f1e332d04effbd089e9076339157cb2f6bbd0d65a758ecfba64c7445d6a`,
  mode `0600`, size `34171`). It contains only source, integration test, and the
  four canonical route contracts, is ASCII-only and NUL-free, and passed strict
  candidate-to-baseline-to-candidate and baseline-to-candidate-to-baseline
  zero-fuzz byte/mode cycles without touching the workspace. All 23 proof
  directories are mode `0700`; all 25 non-patch proof files are mode `0644`;
  candidate snapshots equal current workspace bytes and modes. The first two
  copy wrappers stopped before producing a patch because `install` was absent
  and a zsh-special loop variable replaced command lookup; the corrected
  `cp`/`rel` wrapper generated and verified the sole artifact.
- `inventory_evidence`: inventory remains 74 tracked modifications, 60
  untracked paths, and zero staged at `main@cfebb77`; sensitive untracked-
  filename screening is empty. Pi dependencies remain `0.80.2`; migrations
  007/008 retain SHA-256
  `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b` /
  `95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`.
  No stage, commit, Provider/QQ, private DB, deployment, restart, schema,
  migration, dependency, registry implementation, legacy CLI/QQ mutation, or
  browser operation occurred.
- `exact_fail_first_action`: add only `confirms display-profile redaction once
  through exact preview authority` to
  `tests/integration/governance-http-wiring.test.ts`, then run it alone before
  editing `src/index.ts` or canonical route docs. Its first unauthenticated POST
  must expect `401` and fail only because the absent confirmation route returns
  `404`, while preview consumption, B26H, B26K, and B26I remain uncalled.
- `exact_next_action`: completed; dependency reassessment selects the read-only
  exact active-account unlink preview in B27A below before atomic mutation or
  HTTP authority.

## Completed Slice: `GOV-UX-01B27A`

- `requirement`: `GOV-UX-01B27A` HTTP-disabled exact platform-account unlink
  preview.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: B25B owns the shared active-only account disable and redacted
  audit, and B26L completes display-profile redaction, but identity unlink still
  has no write-free current-state projection, snapshot fingerprint, preview
  digest, or explicit no-relink boundary suitable for later confirmation.
- `selection_reason`: a preview is the first independently testable dependency
  for identity confirmation. Keeping it in the shared query owner establishes
  exact target/state/effect semantics without changing B25B, exposing a route,
  or introducing a second mutation path.
- `implementation_contract`: add only an exported fixed identifier-free
  `PlatformAccountUnlinkPreviewProjection` and
  `GovernanceQueryService.getPlatformAccountUnlinkPreview({ platform,
  platformAccountId })`. Accept only platform `qq` and one normalized 5-12 digit
  QQ account identifier. Select only that exact active mapping and bind every
  stored platform-account field through deterministic typed/hex encoding into a
  domain-separated snapshot fingerprint. Return a separate stable opaque
  account fingerprint, fixed platform/type/verification/status and valid ISO
  observation metadata, expected guarded disable, immediate identity-resolution
  exclusion, redacted audit, retained-mapping privacy consequence, explicit
  unsupported relink/restore boundary, and a domain-separated preview digest.
  Omit raw platform-account and canonical-user identifiers. Add no authority,
  handle, route, expected-snapshot mutation, service/CLI change, QQ grammar, or
  browser behavior.
- `allowed_paths`: `src/governance/query-service.ts`,
  `tests/unit/governance/query-service.test.ts`, `docs/governance-ui.md`, and
  this checkpoint only.
- `protected_paths`: B25B service transaction, audit, inputs/results, and CLI
  delegation; B26A-L behavior; every HTTP/session/scope/resource/preview
  registry and route; identity repository/schema/migrations/dependencies/
  lockfile; QQ/browser code; Provider, private/live data, deployment, restart,
  container, persistence format, and every prior slice.
- `assertions`: malformed platform/account input returns `null` before SQL.
  Missing, disabled, deleted, malformed-row, or invalid-time mappings return no
  preview. One exact active private, group-member, or temporary-session mapping
  returns only the fixed bounded projection; changing any stored value, storage
  type, or timestamp changes snapshot and digest. Repeated reads are stable and
  write-free; raw account/canonical identifiers, secrets, unrestricted fields,
  authority, B25B, HTTP, CLI/QQ, and browser effects remain absent; integrity and
  FKs remain clean.
- `focused_commands`: run `git diff --check`, capture a private four-file B26L
  baseline, add and run one named missing-method unit regression before source
  or canonical-doc edits, rerun it, full query service, B25B/query/CLI adjacency,
  canonical P8, typecheck, scoped/full ESLint, build, release preflight, normal-
  umask release, hygiene, and exact three-path reverse-first/forward-first proof.
- `schema_dependency_live_boundary`: one trusted read-only query projection,
  synthetic unit coverage, canonical governance contract, and checkpoint only.
  No service/repository/HTTP/registry/persistence behavior, schema, migration,
  dependency, lockfile, CLI/QQ/browser, Provider, private DB, deployment,
  restart, container, or live operation is permitted.
- `rollback_boundary`: after this contract and a clean diff check, capture exact
  B26L bytes/modes for query source/test, canonical governance contract, and
  this checkpoint under one new mode-`0700` private root. The final exact three-
  path ASCII patch must independently prove reverse-first and forward-first
  byte/mode restoration without touching the workspace; checkpoint evidence is
  excluded.
- `baseline_evidence`: `git diff --check` passed, then
  `/tmp/lethebot-gov-ux-01b27a-roundtrip-rGwDc3` captured all four paths at mode
  `0644` under mode-`0700` parent directories. Query source, query unit test,
  canonical governance contract, and pre-evidence checkpoint hashes are
  `87ae19a5870fecdfd204f9bda7dc7f19c3d384f69a6507e96bbfb070937db61b`,
  `3503ad1262a35c8f815d449c7385e1e3afd77d58a44ad0ab93038cce6abba991`,
  `af624be9687cf4aa30ec19497fc06c8124cbc1c0c00556de3894dd10abad800e`,
  and `91ade8a92ba8db9d70d14a697d5ada2dbc8aa3547d89c3bc5d68302469cf7a8b`.
  Workspace and baseline bytes match; inventory remains 74 tracked
  modifications, 60 untracked paths, and zero staged at `main@cfebb77`.
  Three intermediate directories initially inherited mode `0755`; all proof
  directories were corrected and verified at `0700` before this evidence was
  recorded.
- `fail_first_evidence`: the named run collected 53 query-service cases,
  skipped 52 unrelated cases, and failed only because
  `getPlatformAccountUnlinkPreview` was not a function. Query source and the
  canonical governance contract retained their baseline hashes. The mode-`0600`
  log is `/tmp/lethebot-gov-ux-01b27a-focused-fail-first.log`
  (`sha256:86b68818f3627e21143c0f8c047180c9600272b61a27fb358e89811149859d0f`,
  size `1530`).
- `final_evidence`: the named regression passed 1 / 1 with 52 skipped, the full
  query-service suite passed 53 / 53, B25B service/CLI adjacency passed 7 with
  191 skipped, and the canonical P8 gate passed 328 / 328. Typecheck, scoped
  and full ESLint, build, release preflight, and `git diff --check` passed. The
  complete normal-umask release gate passed 137 / 1 skipped files with 2831 /
  10 skipped tests. Mode-`0600` logs are focused final
  `/tmp/lethebot-gov-ux-01b27a-focused-final.log`
  (`sha256:b599ec2fce23f39c49670da5618cf2699d4c627bf6957420285ba39b5b605313`),
  full query service `/tmp/lethebot-gov-ux-01b27a-query-service.log`
  (`sha256:60f3792edfa324be3a474770c49d222e7a272f0f3c9f700217ff9d7867d3db66`),
  B25B adjacency `/tmp/lethebot-gov-ux-01b27a-adjacency.log`
  (`sha256:e0785621421dcc929eb8c7f23b8d5a5a7ccb713c22b63d8a53bfce97d4086918`),
  P8 `/tmp/lethebot-gov-ux-01b27a-p8.log`
  (`sha256:7a5b27e1a5e82291e00732348d7b006a6597db3d56a462533bd15a49facae745`),
  static `/tmp/lethebot-gov-ux-01b27a-static.log`
  (`sha256:9b3c33997b2e4e8c3522b52bdf67f62d60f1a99ffb7e460f150bd9de616d6577`),
  and release `/tmp/lethebot-gov-ux-01b27a-release.log`
  (`sha256:70357b14f40508ff1f49d1a4eff5eaf1f366456d803152a1dbccbfac5b9044c2`).
  Exact active mappings alone produce a stable bounded projection. Every
  persisted field and SQLite storage type is validated and snapshot-bound; the
  public account fingerprint is also bound to the canonical owner so the
  low-entropy platform account alone cannot be enumerated. Outputs expose no
  raw account or canonical owner, reads make no writes, and integrity/FKs stay
  clean.
- `rollback_evidence`: the exact three-path patch is
  `/tmp/lethebot-gov-ux-01b27a-roundtrip-rGwDc3/gov-ux-01b27a.patch`
  (`sha256:23dc0478c92478b52e3f8ad9a45ac4470091442e3985d6bd9daed86ca6fd398b`,
  mode `0600`, size `22533`). It contains only query source, its unit test, and
  the canonical governance contract, is ASCII-only and NUL-free, and passed
  independent zero-fuzz reverse-first and forward-first byte/mode cycles
  without touching the workspace. All 31 proof directories are mode `0700`,
  all 13 non-patch proof files are mode `0644`, and candidate snapshots equal
  current workspace bytes and modes.
- `inventory_evidence`: inventory remains 74 tracked modifications, 60
  untracked paths under `--untracked-files=all`, and zero staged at
  `main@cfebb77`; sensitive untracked-filename screening is empty. Pi
  dependencies remain `0.80.2`; migrations 007/008 retain SHA-256
  `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b` /
  `95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`.
  No stage, commit, Provider/QQ, private DB, deployment, restart, service/CLI/
  HTTP/registry/browser behavior, schema, migration, dependency, or lockfile
  change occurred.
- `exact_fail_first_action`: add only `previews exact active platform-account
  unlink without exposing identity` to
  `tests/unit/governance/query-service.test.ts`, then run it alone before source
  or canonical-doc edits. It must fail only because
  `getPlatformAccountUnlinkPreview` is absent.
- `exact_next_action`: completed; reassess the identity dependency path and
  define the atomic expected-snapshot shared-mutation prerequisite before any
  HTTP authority or confirmation route.

## Completed Slice: `GOV-UX-01B27B`

- `requirement`: `GOV-UX-01B27B` HTTP-disabled atomic expected-snapshot
  platform-account unlink service overload.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: B27A binds every stored field of one exact active mapping,
  but B25B accepts only the legacy raw selector and mutates the current row
  unconditionally. A later confirmation cannot atomically reject mapping drift
  between preview and unlink while preserving existing CLI behavior.
- `selection_reason`: expected-snapshot mutation is the next independently
  testable dependency before identity authority or an HTTP confirmation route.
  Reusing B27A's exact reader under one immediate transaction keeps preview and
  mutation semantics identical without duplicating SQL or hash rules.
- `implementation_contract`: extract B27A's exact mapping read, validation, and
  snapshot calculation into a synchronous trusted query-service helper without
  changing its public projection or digest. Add a typed overload
  `GovernanceService.unlinkPlatformAccountAsLocalAdmin(input)` whose expected
  input contains exact `qq` platform/account selector, B27A snapshot, one fixed
  HTTP reason code, and optional clock. Strictly validate the exact object
  before SQL, begin an immediate transaction, recompute the exact active
  snapshot, return `not_found` for no active mapping and `stale` for malformed
  or drifted evidence, then perform only B25B's guarded status disable and
  redacted audit. Require one updated row inside the lock and roll back on count
  mismatch or audit failure. Return only `unlinked` plus disable time. Preserve
  the legacy input overload, result, ordinary transaction, timestamps, audit,
  arbitrary legacy selector handling, and CLI behavior. Add no authority,
  handle, route, registry, CLI/QQ grammar, or browser behavior.
- `allowed_paths`: `src/governance/query-service.ts`,
  `src/governance/service.ts`, `tests/unit/governance/service.test.ts`,
  `docs/governance-ui.md`, and this checkpoint only.
- `protected_paths`: B27A public DTO/fingerprint/snapshot/digest output and its
  unit test; B25B legacy service/CLI input, output, transaction, audit, and
  delegation; every HTTP/session/scope/resource/preview registry and route;
  identity repository, schema, migrations, dependencies/lockfile; other
  governance mutations; Provider, QQ, private/live data, deployment, restart,
  container, browser assets, and every prior slice.
- `assertions`: malformed expected input returns `stale` before SQL. Missing,
  disabled, or deleted mappings return `not_found`; malformed active rows and
  any active-row value/type/time/account-owner drift return `stale` with zero
  writes. One exact current active mapping is disabled once, preserves every
  other field and row, appends one fixed reason-bound redacted audit, and
  returns only the fixed outcome/time evidence. Replay is `not_found`; guarded
  update mismatch is `stale`; audit failure rolls back. Success survives reopen
  with clean integrity/FKs. B27A output remains byte-identical across the helper
  refactor, and all B25B legacy service/CLI assertions remain unchanged. HTTP,
  authority, registries, browser, schema, and dependencies stay absent.
- `focused_commands`: run `git diff --check`, capture a private six-file B27A
  baseline, add and run only the named expected-path service regression before
  production or canonical-doc edits, rerun it plus B25B legacy and B27A query
  parity, full query/service coverage, governance/CLI/HTTP adjacency, canonical
  P8, typecheck, scoped/full ESLint, build, release preflight, normal-umask
  release, hygiene, and exact changed-path reverse-first/forward-first proof.
- `schema_dependency_live_boundary`: shared snapshot-helper refactor, service
  overload, synthetic unit coverage, canonical governance contract, and
  checkpoint only. No persistence format, authority, HTTP/session/registry,
  schema, migration, dependency, lockfile, CLI/QQ/browser, Provider, private
  DB, deployment, restart, container, or live operation is permitted.
- `rollback_boundary`: after this contract and a clean diff check, capture exact
  B27A bytes/modes for query/service sources, their unit tests, the canonical
  governance contract, and this checkpoint under one new mode-`0700` private
  root. The final exact changed-path ASCII patch must independently prove
  reverse-first and forward-first byte/mode restoration without touching the
  workspace; checkpoint evidence is excluded.
- `baseline_evidence`: `git diff --check` passed, then mode-`0700`
  `/tmp/lethebot-gov-ux-01b27b-roundtrip-teJ6xm` captured all six paths at mode
  `0644` under mode-`0700` parent directories. Query source, service source,
  query unit test, service unit test, canonical governance contract, and pre-
  evidence checkpoint hashes are
  `82d76c7b85a0477de2fc789e5f9749b9b3bfd82136da1c6bbd481b5621fd6436`,
  `92781993c8829e4899b586648ee710cca2ef8fb16302c23c7f30990928af491e`,
  `6505430ef45fc6b62bf72f5642de082eb221f41bb461bc6bbdaab2f56413e804`,
  `4dba32c8ab3f655787bdc479f60ca2518bf748e2721637d9a27e2d0304b69876`,
  `30c42e8b3f7164ca5560b1473a4d34d1947ccc74d8241dbdacd4a10016e5b24b`,
  and `792ef0952b8fd6041f49264c38df9b0866a971e31b870ee88a949a5a3658c957`.
  Workspace and baseline bytes match; inventory remains 74 tracked
  modifications, 60 untracked paths, and zero staged at `main@cfebb77`.
- `fail_first_evidence`: the named run collected 16 service cases, skipped 15
  unrelated cases, and failed only because the valid expected-snapshot object
  reached B25B's legacy path and returned `{ outcome: 'unlinked' }` without the
  required fixed disable timestamp. Query/service sources, the query unit test,
  and canonical governance contract retained their baseline hashes. The mode-
  `0600` log is `/tmp/lethebot-gov-ux-01b27b-focused-fail-first.log`
  (`sha256:301dfba0a2e0edfa0f85b57ab45113a87dde4f0348bf9017e2f2af54356d95f0`,
  size `1521`).
- `final_evidence`: the named regression passed 1 / 1 with 15 skipped; B27A,
  B25B service, and B25B CLI parity passed 3 / 3 with 120 skipped; full query/
  service coverage passed 69 / 69; full HTTP lifecycle/wiring adjacency passed
  58 / 58; unchanged CLI subprocess adjacency passed 2 / 2 with 127 skipped;
  and the canonical P8 gate passed 329 / 329. Typecheck, scoped and full ESLint,
  build, release preflight, and `git diff --check` passed. The first complete
  release attempt reached 2831 passing tests before an unrelated fixed-port
  `EADDRINUSE` in the unchanged ingress-recovery case; that exact case then
  passed 1 / 1 with 11 skipped, and the corrected normal-umask release passed
  137 / 1 skipped files with 2832 / 10 skipped tests. Mode-`0600` logs are
  focused final `/tmp/lethebot-gov-ux-01b27b-focused-final.log`
  (`sha256:455737dc375b8b35d5a99e0a17db1adad1fc628e174335267f573bd41710c006`),
  parity `/tmp/lethebot-gov-ux-01b27b-parity.log`
  (`sha256:905692fdd6f0c9eb1b083258452b74ae6f8ed5386fcae71367c54e99f1c989a1`),
  query/service `/tmp/lethebot-gov-ux-01b27b-query-service.log`
  (`sha256:85a14443d3426d7bdbd3201b21d6739af4a78d5e65128e23f7030b2af94fba8f`),
  HTTP adjacency `/tmp/lethebot-gov-ux-01b27b-http-adjacency.log`
  (`sha256:5c2785dbc3542e5b2aacbe9c4ec816d1a8bce5a2cf7187e4da93722f36a2ff47`),
  CLI adjacency `/tmp/lethebot-gov-ux-01b27b-cli-adjacency.log`
  (`sha256:d0326ff6fba1131cfb97705179d69cd1196eab404932e39a9b2ebd340dc2195e`),
  P8 `/tmp/lethebot-gov-ux-01b27b-p8.log`
  (`sha256:1eb22f07407d0b5e2c71e66ea5a07f1d1baa67d390257161fb4d146e661386ab`),
  static `/tmp/lethebot-gov-ux-01b27b-static.log`
  (`sha256:9b3c33997b2e4e8c3522b52bdf67f62d60f1a99ffb7e460f150bd9de616d6577`),
  first release `/tmp/lethebot-gov-ux-01b27b-release.log`
  (`sha256:eaf3e1e34019523a5564743bdcb25138e1594c755271f5e41d834934d873a759`),
  and final release `/tmp/lethebot-gov-ux-01b27b-release-final.log`
  (`sha256:fb28ca659f9afb277e291f7f91b3024e7ed5319250e882dfe6ce18a6489239f0`).
  The expected path validates before SQL, recomputes B27A's exact snapshot in
  an immediate transaction, rejects malformed/drifted/inactive evidence without
  writes, fences the guarded update count, rolls back audit failure, and returns
  only outcome/time evidence. Committed effects survive reopen with clean
  integrity/FKs; B27A output and B25B legacy service/CLI behavior stay exact.
- `rollback_evidence`: the exact four-path patch is
  `/tmp/lethebot-gov-ux-01b27b-roundtrip-teJ6xm/gov-ux-01b27b.patch`
  (`sha256:741b9b0f4fef4935390d3f5e944cc207cbe975b1ce43a1c1e51bc32f17dd40c4`,
  mode `0600`, size `35671`). It contains only query/service sources, the named
  service regression, and canonical governance contract, is ASCII-only and
  NUL-free, and passed independent zero-fuzz reverse-first and forward-first
  byte/mode cycles without touching the workspace. All 29 proof directories
  are mode `0700`, all 20 non-patch proof files are mode `0644`, candidate
  snapshots equal current workspace bytes/modes, and the query unit test is
  byte-identical to its B27A baseline.
- `inventory_evidence`: inventory remains 74 tracked modifications, 60
  untracked paths, and zero staged at `main@cfebb77`; sensitive untracked-
  filename screening is empty. Pi dependencies remain `0.80.2`; migrations
  007/008 retain SHA-256
  `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b` /
  `95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`.
  No stage, commit, Provider/QQ, private DB, deployment, restart, HTTP/registry/
  CLI/QQ/browser behavior, persistence format, schema, migration, dependency,
  or lockfile change occurred.
- `exact_fail_first_action`: add only `unlinks platform account only from the
  exact current snapshot` to `tests/unit/governance/service.test.ts`, then run
  it alone before editing production or canonical documentation. It must fail
  only because the expected-snapshot object still takes B25B's legacy
  unconditional path and lacks fixed expected-path evidence.
- `exact_next_action`: completed; dependency reassessment selects B27C, one
  authenticated-unscoped preview route that binds the raw input only inside
  existing session-owned opaque resource and preview authority before a
  separate confirmation slice.

## Completed Slice: `GOV-UX-01B27C`

- `requirement`: `GOV-UX-01B27C` authenticated platform-account unlink preview
  route with opaque resource and confirmation authority.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: B27A owns the identifier-free exact preview and B27B owns the
  atomic final fence, but neither is reachable through the local HTTP boundary.
  No current-session authority binds a later confirmation to the raw selector,
  snapshot, or preview digest.
- `selection_reason`: preview-only authority is the next dependency before
  confirmation. The existing resource registry can retain the structured raw
  selector privately while returning only an opaque handle, and the existing
  single-use preview registry can bind B27A's snapshot/digest without changing
  either registry or making B27B reachable.
- `implementation_contract`: register only mutation-marked authenticated-
  unscoped `POST /governance/api/v1/identity/platform-accounts/unlink` with a
  fixed identity-unlink purpose. After shared authentication, exact Origin/
  CSRF, bounded JSON, no-query, and no-scope-header enforcement, accept only
  exact `{ "action": "unlink", "platform": "qq", "platformAccountId": ID }`
  with a normalized 5-12 digit ID. Call only B27A; conceal no active preview as
  `404`. Encode the validated selector as private structured registry data,
  issue one current-session resource handle bound to fixed system scope,
  identity purpose, and `platform_account` kind, then issue one preview handle
  bound to the same resource/scope, B27A action, snapshot fingerprint, fixed
  revision `1`, and preview digest. Return B27A unchanged plus only the two
  opaque handles and expiries with `201`. Add no confirmation route, handle
  resolution/consumption, B27B/B25B invocation, durable write, query/service/
  registry implementation change, CLI/QQ grammar, or browser behavior.
- `allowed_paths`: `src/index.ts`,
  `tests/integration/governance-http-wiring.test.ts`, `docs/architecture.md`,
  `docs/deployment.md`, `docs/governance-ui.md`, `docs/security-privacy.md`, and
  this checkpoint only.
- `protected_paths`: B27A/B27B query/service helpers, projections, snapshots,
  digests, mutations, tests, and legacy behavior; all generic HTTP/session/
  scope/resource/preview registry implementations; every other route/action;
  CLI/QQ/browser assets; repository/schema/migrations/dependencies/lockfile;
  Provider, private/live data, deployment, restart, container, persistence
  format, and every prior slice.
- `assertions`: unauthenticated access is `401`; missing/wrong Origin or CSRF is
  `403`; query strings, scope headers, content type, oversized/malformed JSON,
  and every non-exact body fail before B27A or issuance. Valid-format missing/
  inactive/malformed mappings return fixed `404` without authority. One exact
  active mapping calls B27A once, issues exact system-scoped resource and
  preview bindings in order, returns its DTO byte-for-byte plus opaque handles/
  expiries, and exposes no raw account/canonical identifier. Repeated valid
  previews are write-free, reuse only the resource handle, and issue distinct
  preview handles. Resource resolution, preview consumption, B27B/B25B, the
  future confirmation path, and durable effects remain absent; integrity/FKs
  stay clean. Logout/expiry/close keep existing registry revocation semantics.
- `focused_commands`: run `git diff --check`, capture a private seven-file B27B
  baseline, add and run one named absent-route wiring regression before source
  or canonical route-doc edits, rerun it, full governance HTTP wiring, query/
  service/HTTP/scope/resource/preview-registry adjacency, canonical P8,
  typecheck, scoped/full ESLint, build, release preflight, normal-umask release,
  hygiene, and exact six-path reverse-first/forward-first proof.
- `schema_dependency_live_boundary`: route registration/wiring, synthetic
  integration coverage, four canonical route contracts, and checkpoint only.
  No query/service/registry implementation or persistence behavior, schema,
  migration, dependency, lockfile, confirmation, CLI/QQ/browser, Provider,
  private DB, deployment, restart, container, or live operation is permitted.
- `rollback_boundary`: after this contract and a clean diff check, capture exact
  B27B bytes/modes for source, integration test, four canonical route documents,
  and this checkpoint under one new mode-`0700` private root. The final exact
  six-path ASCII patch must independently prove reverse-first and forward-first
  byte/mode restoration without touching the workspace; checkpoint evidence is
  excluded.
- `baseline_evidence`: `git diff --check` passed, then mode-`0700`
  `/tmp/lethebot-gov-ux-01b27c-roundtrip-ROItmY` captured all seven paths at
  mode `0644` under mode-`0700` parent directories. Source, integration test,
  architecture, deployment, governance, security, and pre-evidence checkpoint
  hashes are
  `ea083ba87375a7062d4415617a8a28da7c514904cb62b0381ee23e2150b21312`,
  `9219487ac77a7cbfda8d40b2b4616cda1cc66f84361e6d3c2708f671e3647acf`,
  `e40c8e6a4aa23c05a13e735a3b069d88b38c47aec90bbc7069dbe7069b690fca`,
  `d7cdd23b0fc83031c491610df1b725ebf8e413097ab5380aea37a8078e2be10d`,
  `4f8892065c11b5c3e9947fb2e8ed252fe61f0c094d6b66d6c8895bb6f72c288c`,
  `6031d08ea1e9a59a2f1d9ce3275fb4f0ff1b48f98588cac4e1cf2ad353158048`,
  and `b4d25e676fba5cc97f52aa1bf64d6696c33baebb84e18d6d12be5a523eaaaf5a`.
  Workspace and baseline bytes match; inventory remains 74 tracked
  modifications, 60 untracked paths, and zero staged at `main@cfebb77`.
- `fail_first_evidence`: the named run collected 38 governance HTTP wiring
  cases, skipped 37 unrelated cases, and failed only because unauthenticated
  exact unlink preview returned absent-route `404` instead of required `401`.
  B27A, both issuers, resource resolution, preview consumption, B27B, and B25B
  were uncalled. Source and all four canonical route documents retained their
  baseline hashes. The mode-`0600` log is
  `/tmp/lethebot-gov-ux-01b27c-focused-fail-first.log`
  (`sha256:3efb1156f7dc8df0c67f54886fdd7c1ae41186e72c081f6b7d18e72cb79ab79a`,
  size `1941`).
- `final_evidence`: the named regression passed 1 / 1 with 37 skipped; full
  governance HTTP wiring passed 38 / 38; six-file query/service/HTTP/scope/
  resource/preview-registry adjacency passed 100 / 100; and the canonical P8
  gate passed 329 / 329. Typecheck, scoped and full ESLint, build, release
  preflight, and `git diff --check` passed. The complete normal-umask release
  gate passed 137 / 1 skipped files with 2833 / 10 skipped tests. Mode-`0600`
  logs are focused final `/tmp/lethebot-gov-ux-01b27c-focused-final.log`
  (`sha256:925d91f59fd2d50fb6b94b940e132c7a82d2533d7075950fbe15af41ab5325dd`),
  full wiring `/tmp/lethebot-gov-ux-01b27c-wiring.log`
  (`sha256:86fbc540bb0230db227119f058176eb5e2a1918c313e6bdadfd13b23b5f6e3ee`),
  adjacency `/tmp/lethebot-gov-ux-01b27c-adjacency.log`
  (`sha256:00bc9c577eb1e77f06a138af32a10a046cf66e607533c922707af526bca558f3`),
  P8 `/tmp/lethebot-gov-ux-01b27c-p8.log`
  (`sha256:8d62c0c5b7157c637387cbd41876cf3be3737fcfe140cc5b76f864c55e5b7305`),
  static `/tmp/lethebot-gov-ux-01b27c-static.log`
  (`sha256:9b3c33997b2e4e8c3522b52bdf67f62d60f1a99ffb7e460f150bd9de616d6577`),
  and release `/tmp/lethebot-gov-ux-01b27c-release.log`
  (`sha256:5685d3f2a0b36fdf0472845dfec5456afb1c60e283528b7370b002ef7e5c5dc6`).
  Authentication, Origin/CSRF, content/body, query, scope-header, and exact-body
  checks precede B27A and issuance. Valid-format missing/inactive/malformed
  mappings are concealed; one exact active mapping returns B27A unchanged plus
  identifier-free current-session resource and preview authority. Repeats reuse
  only the resource handle and issue fresh preview handles. Resolution,
  consumption, B27B/B25B, confirmation, writes, and audits stay absent with
  clean integrity/FKs.
- `rollback_evidence`: the exact six-path patch is
  `/tmp/lethebot-gov-ux-01b27c-roundtrip-ROItmY/gov-ux-01b27c.patch`
  (`sha256:de2a051b1cc42868900d34fd94d3b12f2ab139daf91a84808a77a0d204594e51`,
  mode `0600`, size `24916`). It contains only source, the wiring regression,
  and four canonical route contracts, is ASCII-only and NUL-free, and passed
  independent zero-fuzz reverse-first and forward-first byte/mode cycles
  without touching the workspace. All 21 proof directories are mode `0700`,
  all 26 non-patch proof files are mode `0644`, and candidate snapshots equal
  current workspace bytes and modes.
- `inventory_evidence`: inventory remains 74 tracked modifications, 60
  untracked paths, and zero staged at `main@cfebb77`; sensitive untracked-
  filename screening is empty. Pi dependencies remain `0.80.2`; migrations
  007/008 retain SHA-256
  `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b` /
  `95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`.
  No stage, commit, Provider/QQ, private DB, deployment, restart, service/query/
  registry implementation, confirmation, durable mutation, browser, schema,
  migration, dependency, or lockfile change occurred.
- `exact_fail_first_action`: add only `issues platform-account unlink preview
  authority without mutation` to
  `tests/integration/governance-http-wiring.test.ts`, then run it alone before
  editing `src/index.ts` or canonical route docs. Its first unauthenticated POST
  must expect `401` and fail only because the absent route returns `404`, while
  B27A, both issuers, both consumers/resolvers, B27B, and B25B remain uncalled.
- `exact_next_action`: completed; the authority dependency is now complete, so
  reassessment selects B27D, the separate one-shot confirmation route that
  resolves B27C resource authority, consumes B27C preview authority, revalidates
  B27A, and invokes only B27B.

## Completed Slice: `GOV-UX-01B27D`

- `requirement`: `GOV-UX-01B27D` authenticated one-shot platform-account unlink
  confirmation through exact B27C resource and preview authority.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: B27C issues current-session resource and preview authority and
  B27B owns the atomic expected-snapshot unlink, but the static confirmation
  path is absent. The existing preview test proves a confirmation request is
  still an unmatched `404` with no resolution, consumption, or mutation.
- `selection_reason`: all local prerequisites now exist. The separate static
  route can resolve B27C's opaque resource handle, recover and strictly validate
  its private selector, consume the matching action handle once, revalidate
  B27A, and call only B27B without adding another selector or mutation owner.
- `implementation_contract`: register only mutation-marked authenticated-
  unscoped `POST /governance/api/v1/identity/platform-accounts/unlink/confirm`
  under B27C's purpose. After shared authentication, exact Origin/CSRF, bounded
  JSON, no-query, and no-scope-header enforcement, accept only exact
  `{ "confirm": true, "resourceHandle": HANDLE, "previewHandle": HANDLE }`.
  Resolve the resource handle first against the current session, identity-
  unlink purpose, `platform_account` kind, and fixed system scope. Parse its
  private resource ID as canonical JSON with exactly `platform: "qq"` and one
  normalized 5-12 digit `platformAccountId`; conceal missing, cross-boundary, or
  unsafe resource authority as `404` without consuming preview authority. Then
  consume matching current-session `identity.platform_account.unlink` preview
  authority once. Conceal unknown or cross-boundary preview authority as `404`
  and map replay to `409`. Recompute B27A for the recovered selector and require
  exact snapshot fingerprint, fixed revision `1`, and preview digest; map any
  missing or drifted evidence to `409`. Invoke only B27B with B27A's current
  snapshot and `PLATFORM_ACCOUNT_UNLINK_REASON_CODE`; map B27B `not_found` or
  `stale` to `409`. Success returns `200` with only the action, `unlinked`
  outcome, B27A's identifier-free account fingerprint/type/verification and
  observation evidence with status changed to `disabled`, one affected mapping,
  ISO disable time, B27A durable/identity/privacy consequences, fixed redacted
  audit event/reason evidence, and B27A's unsupported-relink rollback boundary.
- `allowed_paths`: `src/index.ts`,
  `tests/integration/governance-http-wiring.test.ts`, `docs/architecture.md`,
  `docs/deployment.md`, `docs/governance-ui.md`, `docs/security-privacy.md`, and
  this checkpoint only.
- `protected_paths`: B27A/B27B query/service implementations, projections,
  snapshots, digests, mutations, tests, and B25B legacy behavior; every generic
  HTTP/session/scope/resource/preview registry implementation; all other routes,
  actions, CLI/QQ/browser assets, repository/schema/migrations/dependencies/
  lockfile, Provider/private/live data, deployment, restart, container,
  persistence format, and every prior slice.
- `assertions`: unauthenticated confirmation is `401`; missing/wrong Origin or
  CSRF is `403`; query strings, scope headers, content type, oversized/malformed
  JSON, non-exact keys, false confirmation, or malformed handles fail before
  resource resolution. Unknown, expired, cross-session, cross-purpose,
  cross-kind, cross-scope, malformed-selector, and noncanonical-selector
  resource authority returns fixed `404` without preview consumption, B27A, or
  B27B. Unknown/cross-action/cross-resource preview authority returns `404`;
  replay returns `409`. Consumption precedes revalidation and B27B. Missing,
  inactive, malformed, or every snapshot/digest/revision drift returns `409`
  without mutation. One exact confirmation calls B27A once after consumption,
  invokes only B27B once with the raw selector, exact current snapshot, and
  fixed reason, disables exactly one active mapping, appends one fixed redacted
  audit, blocks future identity resolution immediately, returns only the fixed
  identifier-free DTO, and leaves distractors unchanged. B27B not-found/stale
  races return `409`; reuse never retries. Integrity/FKs stay clean, audit
  failure rolls back through B27B, and B27C preview behavior stays exact except
  that its former confirmation-absence assertion is narrowed after fail-first.
- `focused_commands`: run `git diff --check`, capture a private seven-file B27C
  baseline, add and run one named absent-route wiring regression before source
  or canonical route-doc edits, rerun it after the minimum implementation, then
  run full governance HTTP wiring, query/service/HTTP/scope/resource/preview-
  registry adjacency, canonical P8, typecheck, scoped/full ESLint, build,
  release preflight, normal-umask release, hygiene, and exact six-path reverse-
  first/forward-first proof.
- `schema_dependency_live_boundary`: one route constant/registration/handler,
  one synthetic integration regression, four canonical route contracts, and
  checkpoint evidence only. No query/service/registry implementation, public
  selector, CLI/QQ/browser, schema, migration, dependency, lockfile, Provider,
  private DB, deployment, restart, container, or live operation is permitted.
- `rollback_boundary`: after this contract and a clean diff check, capture exact
  B27C bytes and modes for source, integration test, four canonical route docs,
  and this checkpoint under one new mode-`0700` private root. The final exact
  six-product-path ASCII patch must independently prove zero-fuzz reverse-first
  and forward-first byte/mode restoration without touching the workspace;
  checkpoint evidence remains outside the rollback patch.
- `baseline_evidence`: `git diff --check` passed, then mode-`0700`
  `/tmp/lethebot-gov-ux-01b27d-roundtrip-H2rZKX` captured all seven paths at
  mode `0644` under private mode-`0700` parent directories. Source, integration
  test, architecture, deployment, governance, security, and checkpoint hashes
  are `a757fa69f5179e120b2a3ebca3c08f551217e328c31aa5192fd15c5fc1d39438`,
  `02770dd279f3f37d6622c1e56e896a9de2d552fc7d65f8bf4254d711458a735c`,
  `4dd03294b1e209c17d92df7e03adc9d32bae277f51fb79ae6cef9ebbe4905196`,
  `d1a96646722e2cb773b367a1b7596c443343ae2477a9e9bb81424240b6110829`,
  `01bf0632e62fb87d1de3f22da95fe10d151c7fcbcd4dbe1ede3dcf03f597ea06`,
  `608aa357e269b11a14c23fe323b697cc9b2c03514f9808029fd55b0e49c281df`,
  and `1e055ecddd02d53a6fd1eda4bc298bb193c6dd03b4eec839de17f3e47fd11bfe`.
  Baseline and workspace bytes match; inventory remains 74 tracked
  modifications, 60 untracked paths, and zero staged at `main@cfebb77`. The
  first capture command created only an unused private setup directory before a
  zsh reserved-variable error and copied no file; it is excluded from evidence.
- `fail_first_evidence`: the named run collected 39 governance HTTP wiring
  cases, skipped 38 unrelated cases, and failed only because the unauthenticated
  exact confirmation returned absent-route `404` instead of required `401` at
  the first assertion. Resource resolution, preview consumption, B27A, B27B,
  and B25B were uncalled. Source and all four canonical route documents retained
  their baseline hashes, and `git diff --check` passed. The mode-`0600` log is
  `/tmp/lethebot-gov-ux-01b27d-focused-fail-first.log`
  (`sha256:bc1492d0761327896cad335ae8f79218ba54e70aca51d1c8cd9570072fb7e023`,
  size `1743`). An earlier wrapper reran the same test but stopped only while
  assigning zsh's reserved `status` parameter; it is excluded from evidence.
- `final_evidence`: the named B27D regression passed 1 / 1 with 38 skipped; the
  unchanged B27C/B27D pair passed 2 / 2 with 37 skipped; full governance HTTP
  wiring passed 39 / 39; six-file query/service/HTTP/scope/resource/preview-
  registry adjacency passed 100 / 100; and canonical P8 passed 329 / 329.
  Typecheck, scoped and full ESLint, build, release preflight, and
  `git diff --check` passed. The complete normal-umask release gate passed 137 /
  1 skipped files with 2834 / 10 skipped tests. Mode-`0600` logs are focused
  final `/tmp/lethebot-gov-ux-01b27d-focused-final.log`
  (`sha256:94f0382446df7c651ee2ff0922112ff13c6a38a3cbfbe517f34fe6a8ab956e7a`),
  full wiring `/tmp/lethebot-gov-ux-01b27d-wiring.log`
  (`sha256:fa49f92d69ca40af2c5c7bdb5ebf0b266d772e5f25792439850b86aaf8ee8895`),
  adjacency `/tmp/lethebot-gov-ux-01b27d-adjacency.log`
  (`sha256:9561148d654bbd0cbf5989178f63e809940269a9108c425f031dd73ce9822b5a`),
  P8 `/tmp/lethebot-gov-ux-01b27d-p8.log`
  (`sha256:8309f278fcf8de0956643ee17b1c5a91b0b60715f342dddbb56f85d018b93c0c`),
  static `/tmp/lethebot-gov-ux-01b27d-static.log`
  (`sha256:9b3c33997b2e4e8c3522b52bdf67f62d60f1a99ffb7e460f150bd9de616d6577`),
  and release `/tmp/lethebot-gov-ux-01b27d-release.log`
  (`sha256:459bcf1199afc307885e9fc320c8beaa051176ae01efb12d2fd2d7fe08b015c3`).
  Exact resource resolution and canonical private-selector parsing precede
  single-use consumption; every authority/drift/race boundary maps as specified;
  exact success disables one mapping, appends one fixed redacted audit, blocks
  identity resolution immediately, returns only the fixed identifier-free DTO,
  and rejects replay with clean integrity/FKs.
- `rollback_evidence`: the exact six-product-path patch is
  `/tmp/lethebot-gov-ux-01b27d-roundtrip-H2rZKX/gov-ux-01b27d.patch`
  (`sha256:ca1e39b1730135db74ca517ca43f3ada6cea8ef9ad1faa7927d79e7f415496e3`,
  mode `0600`, size `36135`). It is ASCII-only and NUL-free and contains only
  source, the wiring regression, and four canonical route contracts. Independent
  `022`-umask reverse-first and forward-first zero-fuzz cycles restored exact
  baseline and candidate bytes/modes, and candidate snapshots equal the
  workspace. All 31 proof directories are mode `0700` and all 39 non-patch files
  are mode `0644`. The first proof cycle inherited private umask `077`, detected
  mode-`0600` product copies before accepting a cycle, and is excluded; its files
  were normalized and separate corrected proof trees provide the cited result.
- `inventory_evidence`: inventory remains 74 tracked modifications, 60
  untracked paths, and zero staged at `main@cfebb77`; sensitive untracked and
  reject/original filename screening is empty. Pi dependencies remain `0.80.2`;
  migrations 007/008 retain SHA-256
  `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b` /
  `95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`.
  No stage, commit, Provider/QQ, private DB, deployment, restart, registry/query/
  service implementation, CLI/QQ/browser, schema, migration, dependency, or
  lockfile change occurred.
- `exact_fail_first_action`: add only `confirms platform-account unlink once
  through exact preview authority` to
  `tests/integration/governance-http-wiring.test.ts`, then run it alone before
  editing `src/index.ts` or canonical route docs. Its first unauthenticated POST
  must expect `401` and fail only because the absent confirmation route returns
  `404`, while resource resolution, preview consumption, B27A, B27B, and B25B
  remain uncalled.
- `exact_next_action`: completed; the full P8 reassessment closes the identity
  HTTP workflow and selects `GOV-UX-01C5C`, a behavior-preserving fixed-browser-
  asset compaction before any fifth Activity or Privacy & Operations view. The
  current C5B assets are 65,314 bytes, leaving only 222 bytes under the fixed
  65,536-byte ceiling; retention mutation semantics remain a separate decision-
  complete backend sequence and are not guessed inside this compaction.

## Completed Slice: `GOV-UX-01C5C`

- `requirement`: `GOV-UX-01C5C` behavior-preserving Activity-list browser asset
  compaction.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: the final C5B HTML, CSS, and JavaScript total 65,314 bytes,
  leaving only 222 bytes below the permanent 65,536-byte fixed-shell boundary.
  The JavaScript separately binds, loads, cancels, and registers listeners for
  each of the otherwise homogeneous Tool calls, Worker heartbeats, and Jobs
  list views. A fifth Activity view cannot fit without either weakening the
  existing boundary or first removing that structural duplication.
- `selection_reason`: the full reassessment after the completed B27C/B27D
  identity-mutation pair returns to the already-selected browser prerequisite.
  The three current Activity lists have the same loading/error/empty/content,
  same-origin request, stale-cancellation, expiry, refresh, tab-selection, and
  keyboard-listener lifecycle. Consolidating only that repeated lifecycle is
  smaller and independently reversible; it adds no product behavior or
  authority and leaves ambiguous retention execution semantics untouched.
- `implementation_contract`: replace only the repeated Tool calls, Worker
  heartbeats, and Jobs client lifecycle with one readable fixed list-controller
  helper and three explicit configurations. Each configuration owns its fixed
  DOM bindings, exact endpoint request closure, render function, loading count,
  unavailable announcement, empty announcement, updated announcement, and
  request sequence. The shared helper performs the existing loading-state
  transition, same-origin request, stale-result cancellation, `401` session
  expiry, non-`200` and invalid-payload failure, empty/content selection, and
  announcement in the same order. Iterate the same explicit configurations for
  tab selection, refresh/click/keydown registration, and cancellation.

  Keep Overview and Model invocations on their existing bespoke paths. Preserve
  every HTML/CSS byte, DOM ID, visible string, tab order/default, endpoint,
  request options, validator/renderer, safe DOM construction, accessibility,
  responsive layout, session/logout behavior, and C1-C5B state transition. The
  prior static endpoint call assertions must remain true. Do not minify tokens,
  shorten domain names opaquely, alter the fixed-body compactor, split or add an
  asset, add lazy loading/dynamic import, or introduce a dependency.
- `allowed_paths`: `src/http/governance-browser-assets.ts`,
  `tests/integration/governance-http.test.ts`, `docs/architecture.md`,
  `docs/deployment.md`, `docs/governance-ui.md`, `docs/security-privacy.md`, and
  this checkpoint only.
- `protected_paths`: HTML and CSS served bodies; `GovernanceHttpServer`,
  `src/index.ts`, every route/query/service/DTO/repository and Activity payload;
  all validators and row renderers except the minimum call-site adaptation to
  the shared controller; session/CSRF/scope/resource/preview registries;
  GovernanceService; CLI/QQ; application listener; config/env; schema,
  migrations, dependencies/lockfile/build contract; Provider, private/live
  data, deployment/restart/container behavior; every visible C1-C5B behavior;
  and every prior slice.
- `assertions`: unchanged named C5A compact-asset and C5B Jobs-browser
  characterizations pass before editing and reproduce exact
  14,047/14,794/36,473-byte bodies. One named fail-first regression changes
  only the combined fixed-asset threshold to less than 61,500 bytes and fails
  against the 65,314-byte baseline. The final refactor reclaims at least 3,815
  bytes and leaves more than 4,036 bytes of extension headroom without opaque
  minification. HTML and CSS remain byte-identical; JavaScript remains
  deterministic and parseable. Every existing static characterization and the
  complete C5B desktop/mobile browser matrix must preserve loading, populated,
  empty, refresh, malformed, prohibited-field, oversized, unavailable, stale-
  cancelled, expiry, logout, keyboard, focus, labelled-mobile-row, layout,
  network, and error behavior across all four tabs. Complete governance HTTP,
  wiring/adjacency, canonical P8, type/lint/build/preflight/release, dependency/
  migration/inventory/privacy, diff hygiene, and exact rollback gates remain
  green.
- `focused_commands`: named unchanged C5A and C5B characterizations; exact
  served-body byte measurement and extracted-JavaScript parse; one named C5C
  threshold regression fail-first and final; complete governance HTTP plus
  HTTP/application/wiring adjacency; rebuilt C5B desktop/mobile Chromium QA;
  canonical P8; typecheck; scoped and full ESLint; build; release preflight;
  complete normal-umask release; `git diff --check`; dependency/migration/
  inventory/privacy hygiene; and an exact six-path private reverse-first/
  forward-first proof.
- `schema_dependency_live_boundary`: fixed browser JavaScript structure,
  synthetic regression coverage, four canonical browser/security documents,
  and checkpoint evidence only. No visible/browser-request behavior, HTML/CSS,
  backend, route, persistence, schema, migration, dependency, lockfile, build
  contract, CLI/QQ, Provider, private DB, deployment, restart, container, or
  live operation may change.
- `rollback_boundary`: after this contract and a clean diff check, capture the
  exact C5B bytes and modes for the browser asset source, integration test, four
  canonical documents, and this checkpoint under one new mode-`0700` private
  root. The final exact six-product-path ASCII patch must independently prove
  zero-fuzz reverse-first and forward-first byte/mode restoration without
  touching the workspace; checkpoint evidence remains outside the rollback
  patch.
- `baseline_evidence`: `git diff --check` passed and the unchanged named C5A
  compact-asset and C5B Jobs-browser characterizations each passed 1 / 1 with
  20 skipped. Their mode-`0600` logs are
  `/tmp/lethebot-gov-ux-01c5c-c5a-characterization.log`
  (`sha256:bf56b5fc44575607ff1f5ac04d89c28d89a33b4ba9b980d24f1a4c6e2ab399bd`)
  and `/tmp/lethebot-gov-ux-01c5c-c5b-characterization.log`
  (`sha256:f49096d902ac95ef2fd1458711e5d92874286c21558f0d0a008563a2df82a90d`).
  Direct source-module measurement reproduced exact served HTML/CSS/JavaScript
  sizes 14,047/14,794/36,473 bytes, 65,314 total with 222 bytes of headroom,
  and the extracted JavaScript parsed.

  Mode-`0700` `/tmp/lethebot-gov-ux-01c5c-roundtrip-Isniye` captures the exact
  seven-path C5B baseline byte-for-byte and mode-for-mode. All captured product
  files are mode `0644`; source, integration test, architecture, deployment,
  governance, security, and checkpoint SHA-256 values are
  `f11d18ebae14e6f6405dc87992be70fcc404538e7955d3f9e220f2d11f6c8c2a`,
  `e93a38dfe2b2595cf561165ef85e877179fa0607f97e8f35bc2abf357adef66c`,
  `44a2679469d0a466094859a31f5d089317e6385882cefdc32f98cda1d85dc911`,
  `0accc3ad0cc40b04c5988433c8542959c13569fe747b2eebe28fa663d99b27a7`,
  `281b8a17b96d7922ef38eb4f310e607f42568d5165b9f047e1684f9fa7faef03`,
  `1454e82e05e83508296c2aa950cbfe94a9d5a9eaa85cdebef2c0f4c9796fcd3e`,
  and `fe38d08a339bc3db01a951de161d86d4a68f2dcae83293ec54af88aeb67765d6`
  in allowed-path order. Baseline and workspace bytes match; every private
  snapshot directory is mode `0700`; inventory is 74 tracked modifications,
  60 untracked paths, and zero staged at `main@cfebb77`. The first setup root
  `/tmp/lethebot-gov-ux-01c5c-roundtrip-cByYux` stopped before any file copy
  when a zsh special variable replaced `PATH` and is excluded from evidence.
- `exact_fail_first_action`: after the baseline capture and unchanged C5A/C5B
  characterizations, add one assertion to `serves compact fixed browser assets
  with bounded extension headroom` requiring the combined UTF-8 body size to be
  less than 61,500 bytes. Run only that named case before changing browser
  assets; it must fail at that assertion with the exact 65,314-byte C5B total.
- `fail_first_evidence`: the named C5C run collected 21 governance HTTP cases,
  skipped 20, and failed only at the new first size assertion because the
  unchanged C5B bodies total 65,314 bytes rather than less than 61,500. The test
  delta is exactly the one added assertion; browser source and all four
  canonical documents remain byte-equal to the captured baseline, and
  `git diff --check` passes. The mode-`0600` log is
  `/tmp/lethebot-gov-ux-01c5c-fail-first.log`
  (`sha256:59906b14f7f1061c7c8312f8b4d9b35bd1f1702487c6e7551a3fe1ae7cdf462e`,
  size `1159`).
- `final_evidence`: one readable fixed controller now owns only the repeated
  DOM binding, loading, request, cancellation, state, and listener lifecycle for
  Tool calls, Worker heartbeats, and Jobs. Three explicit configurations retain
  their exact endpoint request closures, strict renderers, messages, elements,
  and independent request sequences. Overview, Model invocations, HTML, CSS,
  visible text, validators, row renderers, request options, and every backend
  path are unchanged. Direct source-module comparison proves HTML and CSS are
  byte-identical to C5B; final HTML/CSS/JavaScript sizes are
  14,047/14,794/32,543 bytes, 61,384 total. The slice reclaims 3,930 bytes and
  leaves 4,152 bytes below the permanent 65,536-byte boundary; the extracted
  JavaScript parses. The mode-`0600` asset log is
  `/tmp/lethebot-gov-ux-01c5c-assets.log`
  (`sha256:1496e8868e536c0b93677977becbe742fa43d5a381d7e71d71906c9ebe620d7f`).

  The final named C5C and unchanged C5B cases each passed 1 / 1 with 20
  skipped; complete governance HTTP passed 21 / 21; exact six-file HTTP/
  application/wiring adjacency passed 56 / 56; and canonical P8 passed 329 /
  329. Their mode-`0600` logs have SHA-256
  `71abec370eb22ad55a4c1763baa3b003fb1deb4c18256d603cade07f62bee967`,
  `cb5e9d1ee39f3367ecc2fe351fb6f9dd0e8dfcdd20d23bb783a493222b3f20ea`,
  `da81b0f1e8657d4be99ea272cf09363a4b66eb10dafec67781892421009e5681`,
  `b5d09e55bcb1457d765a7723311fe14f4f636575e45085c1157806dd3dcfbf23`,
  and `89cb9d9e432faa07f86827942befa414d101abf735934bbcb71b47c82ba74c98`
  in that order. Typecheck, scoped/full ESLint, build, release preflight, and
  `git diff --check` passed. The complete normal-umask release gate passed 137 /
  1 skipped files with 2,834 / 10 skipped tests; its mode-`0600` log is
  `/tmp/lethebot-gov-ux-01c5c-release.log`
  (`sha256:d93bed52c3ebfdb727f0614236da066f43993979336f1ac1a89b4a749a369e78`,
  size `369295`).

  Rebuilt Chromium at 1440x900 and 390x844 made 34 requests per viewport and
  covered every C5B loading, populated, empty, malformed, prohibited-field,
  oversized, unavailable, stale-cancelled, expiry, logout, keyboard, focus,
  mobile-row, and layout state. It reported zero unexpected console errors,
  page errors, failed requests, or external requests. The fresh screenshots are
  visually clean and byte-identical to C5B, confirming no rendered change. The
  mode-`0600` browser log is `/tmp/lethebot-gov-ux-01c5c-browser.log`
  (`sha256:3ee76157fbe1a2753cb3cc9b9e67d8da05f19ee4bd47dc69b3347e26c6969733`);
  desktop/mobile screenshot SHA-256 values are
  `bb8bef9e9d4fe7d2daa818fb166e28f90cdd0b8a5f474ecc3e440b38ece5c79d` /
  `ff857ac1dfac5236b3770d8c6b8622cbe9c04818e4a2330fbe28ffa4d8fd8f85`.

  The exact six-product-path zero-context ASCII patch is
  `/tmp/lethebot-gov-ux-01c5c-roundtrip-Isniye/gov-ux-01c5c.patch`
  (`sha256:031c89eb4738967c86cff574f2656b014ada2512b2d472613ed1ec3cc04c245e`,
  mode `0600`, size `18767`). Corrected `v2` reverse-first and forward-first
  zero-fuzz cycles restored exact baseline and candidate bytes and mode-`0644`
  files under mode-`0700` trees; the workspace remained equal to the candidate.
  The mode-`0600` proof log is
  `/tmp/lethebot-gov-ux-01c5c-roundtrip.log`
  (`sha256:00272216636c92195374e7273ea2ebdb5687040dc93a0da33acf3a69818be626`).
  An initial contextual patch was rejected before application because adjacent
  non-ASCII documentation entered the patch; the next setup omitted proof
  directories, and another used absolute-parent copies. None reached a product
  comparison and all are excluded; the separate `v2` trees provide the cited
  proof.

  Final hygiene remains 74 tracked modifications, 60 untracked paths, and zero
  staged at `main@cfebb77`; Pi dependencies remain `0.80.2`; migrations 007/008
  retain their recorded hashes; sensitive untracked and reject/original name
  screening is empty. No backend, route, DTO, persistence, schema, migration,
  dependency, lockfile, CLI/QQ, Provider, private/live, deployment, restart, or
  container behavior changed.
- `exact_next_action`: completed; the reclaimed 4,152-byte headroom and fixed
  controller make authenticated B14G Job Attempts the smallest already-backed
  Activity workflow. Define `GOV-UX-01C5D` as a separate read-only fifth-tab
  slice, first proving unchanged C5C and B14G behavior and a seven-path baseline;
  keep the 65,536-byte ceiling and reassess if the complete accessible view does
  not fit without another behavior-preserving prerequisite.

## Completed Slice: `GOV-UX-01C5D`

- `requirement`: `GOV-UX-01C5D` read-only Job Attempts Activity browser view.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: authenticated B14G already exposes the bounded, result-free
  Job Attempts list at `/governance/api/v1/activity/job-attempts`, but the
  browser stops at the Jobs schedule/lifecycle view. Operators cannot inspect
  per-attempt worker assignment, status, timing, or redacted failure evidence
  through the local browser workflow.
- `selection_reason`: C5C reduced the fixed shell to 61,384 bytes and left
  4,152 bytes below the permanent boundary while establishing one explicit
  fixed controller for homogeneous Activity lists. B14G is the already-proved
  execution-history counterpart to the current Jobs view, needs no backend or
  authority change, and can reuse the controller and all existing table/state/
  responsive styles. It is smaller and less ambiguous than starting a scoped
  or mutating browser workflow.
- `implementation_contract`: add Job attempts as the fifth Activity tab after
  Jobs while preserving Model invocations as the default and the first four tab
  positions. Add one explicit shared-controller configuration whose request
  closure calls only authenticated same-origin
  `GET /governance/api/v1/activity/job-attempts`. Accept only an array of at
  most 100 exact records with required bounded `id`, `jobId`, `workerId`, fixed
  `running`/`completed`/`failed` status, positive safe-integer `attemptNumber`,
  and canonical ISO `startedAt`; accept optional canonical ISO `completedAt`
  and `heartbeatAt` plus optional bounded `error`. Reject unknown keys,
  `result`, invalid strings/numbers/dates/status, and oversized arrays before
  replacing any row.

  Render fixed loading, empty, unavailable, count, refresh, and responsive
  semantic-table states using only DOM creation and `textContent`. Show the
  redacted job ID with exact attempt number, redacted worker ID, fixed status,
  optional redacted error, and started/heartbeat/completed times. Validate but
  do not display the separate attempt `id`; `jobId` plus `attemptNumber` is the
  useful operator identity. Reuse existing safe table/status/time styles and
  the C5C controller without a new CSS rule unless browser evidence proves one
  is required. Preserve per-view request sequencing, stale cancellation,
  session expiry/logout, roving-tab keyboard behavior, visible focus, mobile
  labels, compact fixed-body construction, and every C1-C5C state transition.
  Add no filter, detail link, caller limit, polling, router, chart, inference,
  dependency, cookie read, dynamic HTML/code, external request, backend route,
  or DTO change.
- `allowed_paths`: `src/http/governance-browser-assets.ts`,
  `tests/integration/governance-http.test.ts`, `docs/architecture.md`,
  `docs/deployment.md`, `docs/governance-ui.md`, `docs/security-privacy.md`, and
  this checkpoint only.
- `protected_paths`: `GovernanceHttpServer`, `src/index.ts`, B14G route/query-
  service/DTO/order/limit/redaction semantics and its wiring regression; C1-C5C
  behavior and fixed controller contract; every other Activity/scoped/detail/
  mutation API; session/CSRF/scope/resource/preview registries;
  GovernanceService; CLI/QQ; application listener; config/env; schema,
  migrations, dependencies/lockfile/build contract; Provider, private/live
  data, deployment/restart/container behavior; and every prior slice.
- `assertions`: unchanged named C5C fixed-asset and B14G route characterizations
  pass before editing and reproduce exact 14,047/14,794/32,543-byte bodies. One
  named fail-first browser regression sees complete C5D-predecessor assets but
  no `job-attempts-tab` or endpoint reference. Final fixed assets preserve the
  first four tabs, add accessible fifth-tab navigation and every fixed Job
  attempts state, call only B14G, strictly validate its exact bounded response,
  omit attempt ID/result, retain safe DOM/in-memory authority rules, and remain
  below the C5C 61,500-byte feature budget and permanent 65,536-byte boundary.
  C5D supersedes C5E's predecessor-only less-than-57,500 total assertion with
  explicit assertions for the retained C5E binding, validation, cell/status,
  presentation, and bounded-list primitives; adding a feature is not required
  to keep the predecessor bundle's exact size. Chromium at 1440x900 and
  390x844 covers all three
  statuses, populated/optional/empty/loading/refresh/malformed/unknown/result/
  oversized/unavailable/stale-cancelled/expiry/logout states, all five keyboard
  tabs, visible focus, 44px controls, labelled mobile rows, and layout/network/
  error hygiene while retaining the complete C5B matrix. C5C, B14G, HTTP/
  wiring, canonical P8, type/lint/build/preflight/release, dependency/migration/
  inventory/privacy, diff hygiene, and exact rollback gates remain green.
- `focused_commands`: named unchanged C5C browser-asset and B14G route
  characterizations; one named C5D browser regression fail-first and final;
  complete governance HTTP plus HTTP/application/wiring adjacency; embedded
  JavaScript parse and exact byte measurement; expanded desktop/mobile Chromium
  QA; canonical P8; typecheck; scoped/full ESLint; build; release preflight;
  complete normal-umask release; `git diff --check`; dependency/migration/
  inventory/privacy hygiene; and an exact six-path private reverse-first/
  forward-first proof.
- `schema_dependency_live_boundary`: fixed browser assets, synthetic tests, four
  canonical browser/security documents, and checkpoint evidence only. No
  backend, persistence, schema, migration, dependency, lockfile, build contract,
  CLI/QQ, Provider, private DB, deployment, restart, container, or live
  operation may change.
- `rollback_boundary`: after this resumed contract and a clean diff check,
  capture the exact C5E bytes and modes for browser source, integration test, four canonical
  documents, and this checkpoint under one new mode-`0700` private root. The
  final exact six-product-path ASCII patch must independently prove zero-fuzz
  reverse-first and forward-first byte/mode restoration without touching the
  workspace; checkpoint evidence remains outside the rollback patch.
- `baseline_evidence`: `git diff --check` passed; the unchanged named C5C fixed-
  asset and B14G route characterizations each passed 1 / 1 with 20 and 38
  skipped respectively. Their mode-`0600` logs are
  `/tmp/lethebot-gov-ux-01c5d-c5c-characterization.log`
  (`sha256:25028c32f53cd3aef5fde85b3a97b8b978ca24227e4335d9fb2c39187c7694ca`)
  and `/tmp/lethebot-gov-ux-01c5d-b14g-characterization.log`
  (`sha256:6da53a93b3e85ed4d1f2e13dbf940db9bc1402b098571d3f17fabe1f3bfa9af5`).
  Direct source-module measurement reproduced exact served HTML/CSS/JavaScript
  sizes 14,047/14,794/32,543 bytes, 61,384 total with 4,152 bytes of headroom,
  and the extracted JavaScript parsed.

  Mode-`0700` `/tmp/lethebot-gov-ux-01c5d-roundtrip-thatwe` captures the exact
  seven-path C5C baseline byte-for-byte and mode-for-mode. All source files are
  mode `0644`; source, integration test, architecture, deployment, governance,
  security, and checkpoint SHA-256 values are
  `bb91a7dddd198ee3572edb8cbaa081f38b605e6497c14e0db17f9229089ad37c`,
  `527f8de741da11887c81a6f79e842d94d1017c8f4c99049eff6b41f5373800c4`,
  `4d669c89ccc777331d246d4329ccba06cfbde907b4c96207492cd98b8eee21ec`,
  `bbed38e444dc8f93221ca37f9e29bd1bf35d92485193515a59e3ddc7aa9b5f29`,
  `25f62d8d76bcc8464070ab16225928a865e08933612db86f68ed3fba4ad18178`,
  `9bdaa7a3ce6d01fa26263613f53c07861a6389c303007b1751e8785a773fbdcb`,
  and `db8568fae47f58c0fd69df10618ba2ed1153ef0ea7126b58015d08b026ea4189`
  in allowed-path order. Baseline/workspace bytes match; every snapshot
  directory is mode `0700`; inventory is 74 tracked modifications, 60 untracked
  paths, and zero staged at `main@cfebb77`.
- `exact_fail_first_action`: after baseline capture and unchanged C5C/B14G
  characterizations, add only `exposes a fixed read-only job-attempt Activity
  browser view` to `tests/integration/governance-http.test.ts`. Its first
  assertion must require `id="job-attempts-tab"` and fail because C5C has only
  the first four tabs; subsequent assertions define fixed states, exact B14G
  endpoint, strict/safe rendering, attempt-ID/result omission, and byte boundary
  without invoking a domain callback.
- `fail_first_evidence`: the named C5D run collected 22 governance HTTP cases,
  skipped 21, and failed only at its first assertion because the unchanged C5C
  HTML has no `id="job-attempts-tab"`. Browser source and all four canonical
  documents remain byte-equal to the captured baseline, and `git diff --check`
  passes. The mode-`0600` log is
  `/tmp/lethebot-gov-ux-01c5d-fail-first.log`
  (`sha256:34cdd4b0dab956b11dd428cb3a6a6548e5d14cfa0c215feb3b912104438da553`,
  size `16201`).
- `resumed_fail_first_evidence`: after the fresh C5E baseline, the rebuilt
  named C5D contract again collected 22 cases, skipped 21, and failed only at
  its first assertion because the exact C5E HTML has no
  `id="job-attempts-tab"`. No browser-source or canonical-document edit had
  occurred. The mode-`0600`, 16,201-byte log is
  `/tmp/lethebot-gov-ux-01c5d-resume-fail-first.log` with SHA-256
  `9b4e45e2246c2de938725d58ac236f0f1bc05b5390f8d1d16975d6d6d210402a`.
- `implementation_probe_evidence`: the minimum contract-shaped browser probe
  added only the fifth tab/panel, one controller configuration, and the strict
  B14G validator/renderer. Its JavaScript parsed, CSS stayed exactly 14,794
  bytes, and the served HTML/JavaScript became 15,734/35,542 bytes. The 66,070-
  byte total is 534 bytes above the permanent 65,536-byte ceiling and 4,570
  above the preserved C5C 61,500-byte headroom regression. The named C5D, C5C,
  and C5B cases failed only at those static byte assertions; no runtime or
  browser claim was accepted.

  The unaccepted source and fail-first test were then removed with surgical
  patches before any canonical-document edit. After removing two formatting-
  only blank lines introduced by that removal, both paths compare byte-for-byte
  with the captured C5C baseline; direct measurement is again exact
  14,047/14,794/32,543 bytes, 61,384 total, and the named C5C case passes 1 / 1
  with 20 skipped. No C5D production or test delta remains in the workspace.
- `resume_evidence`: C5E is now independently deterministic-ready at exact
  14,047/14,794/28,617-byte bodies, 57,458 total. It reclaimed 3,926 bytes from
  C5C and leaves 4,042 bytes below C5C's 61,500-byte feature ceiling. The prior
  C5D probe added 4,686 bytes before the new shared primitives, so a resumed
  implementation must reuse them and remove at least 645 bytes from that delta
  to retain the documented feature budget. The less-than-57,500 check is a
  C5E predecessor acceptance threshold, not a zero-growth ceiling for C5D; C5D
  will replace it with structural primitive-retention assertions while keeping
  the existing less-than-61,500 and less-than-65,536 checks unchanged.
- `resumed_baseline_evidence`: the unchanged named C5E compact-assets and B14G
  route characterizations passed 1 / 1 with 20 and 38 skipped. Their mode-
  `0600` logs have SHA-256
  `db41e033cdde8e45b99b906e37f976874e13f0a5aa2d3541c05cd52d30e412a2` and
  `fb7421eae341e22e227bd854fd9d1063bdf84322b10716001b101eb01e7fe58d`.
  Direct measurement reproduced exact 14,047/14,794/28,617-byte bodies,
  57,458 total, and parseable JavaScript. Private mode-`0700` root
  `/tmp/lethebot-gov-ux-01c5d-resume-roundtrip-a1Fq16` captures the fresh
  seven-path C5E baseline; its mode-`0600`, 967-byte manifest has SHA-256
  `703b500a2728a659b137a61f87955177b4d7b74242d46bd6829a0334371dfe48`
  and records exact hashes, sizes, modes, `main@cfebb77`, and zero staged paths.
- `final_evidence`: Job attempts is now the fifth Activity tab after Jobs and
  calls only exact same-origin B14G. Its separate controller configuration uses
  the retained C5E binding, bounded text/date, accepted-value presentation,
  cell/status, and normalized-list primitives. The strict validator accepts at
  most 100 exact records with required bounded `id`, `jobId`, and `workerId`, a
  positive safe-integer attempt number, fixed running/completed/failed status,
  and canonical start time plus only the three documented optional fields. It
  rejects unknown keys, `result`, malformed fields, and oversized arrays before
  row replacement. Attempt `id` is validated but projected away; only redacted
  job/attempt, worker, status/error, and timeline evidence enters text nodes.
  HTML/JavaScript are 15,802/30,876 bytes, CSS remains exact 14,794 bytes, and
  the 61,472-byte total is 28 below the C5D feature threshold and 4,064 below
  the permanent boundary. JavaScript parses. The mode-`0600` asset log is
  `/tmp/lethebot-gov-ux-01c5d-assets.log` with SHA-256
  `eb3486651da2f38bc268ed17eccb36924fc01ea30527e54e43f71e1d2ee88ad5`.

  The named C5D and compact-assets cases each passed 1 / 1 with 21 skipped;
  full governance HTTP passed 22 / 22, corrected isolated six-file adjacency
  passed 56 / 56, and the canonical P8 command passed 7 files / 330 tests.
  Their accepted mode-`0600` log SHA-256 values are
  `09b0cd8673624b9a4bdcbc9d52e3707b4430bab82f436ef408ff1d14e6831e03`,
  `d8800fe5af1431810225e8bb76aafd67943acbd12588fcbbd3822f9c454b3153`,
  `d1405058bde09c19469183097bfd9289423e0e099b1fee1cad0b542361c5617c`,
  `4c0b1b27612f292fb7b0c6b1280058c99ea5b8e44ad25f40d4ca78ebd04e8e60`,
  and `d7a5a8b0e5a5f6be08bb1c1a3611dbf14eee9842fb2814bf0192cacf91e51de6`.
  Typecheck, scoped/full ESLint, build, release preflight, and diff hygiene pass.
  The complete normal-umask release gate passed 137 / 1 skipped files and
  2,835 / 10 skipped tests; its mode-`0600` log is
  `/tmp/lethebot-gov-ux-01c5d-release.log` with SHA-256
  `d81cfa50550ce53591a9e0e7962af2a6d9ddb42123fe5ad4176ce0df14b274f5`.
  One parallel adjacency/P8 attempt produced an isolated configuration failure
  and incomplete companion run; it is excluded, and the serial reruns plus the
  complete release gate are the accepted evidence.

  Chromium at 1440x900 and 390x844 made 53 requests per viewport and covered
  all predecessor states plus Job attempts loading, all three statuses,
  optional fields, empty, malformed, unknown-key, result, oversized,
  unavailable, stale-cancelled, expiry, and logout states. Five-tab Arrow/Home/
  End keyboard behavior, focus, 44px controls, labelled mobile rows, and safe
  text rendering passed with zero unexpected console, page, failed-request, or
  external-request events. The visually reviewed desktop/mobile screenshots
  are clean. The mode-`0600`, 1,276-byte browser log has SHA-256
  `6460c7dea4071a3044d8b2f81bf7718e8ae02c440a6605aaeee240bde88bec30`;
  the mode-`0600` desktop/mobile screenshots have sizes 91,876/50,789 and
  SHA-256 `bec2d6c2f72fde48cb27ea946e093a185b68db0884f9d3892bdb5b926928b106`/
  `11a3c9eaf1626b647a3030705a0efd966a5d2dccb959800e6ad93f46e097af38`.

  The exact six-product-path zero-context ASCII patch is
  `/tmp/lethebot-gov-ux-01c5d-resume-roundtrip-a1Fq16/gov-ux-01c5d.patch`
  (`sha256:ad944ef72461ef5573fa6114721f96e2d057f7b68bd0e8200dca28824fee94db`,
  mode `0600`, size `14478`). Accepted `v3` reverse-first and forward-first
  zero-fuzz cycles restored exact baseline/candidate bytes and modes, excluded
  the checkpoint, and proved candidate/workspace equality with every proof
  directory mode `0700`; its mode-`0600` log has SHA-256
  `745db2302e094d34aa768f22cccc2f5d2f3633ed2e1f5a64d73b905f99216977`.
  The first execution wrapper was rejected before running; v2 completed all
  file cycles but found `0755` intermediate proof directories and is excluded.
  Corrected hygiene remains 74 tracked modifications, 60 untracked paths, and
  zero staged at `main@cfebb77`; Pi remains `0.80.2`, dependency/lockfile and
  migration hashes are unchanged, both filename screens are empty, and no
  backend, route, DTO, schema, persistence, CLI/QQ, Provider, private/live,
  deploy, restart, or container behavior changed.
- `exact_next_action`: completed; add only the already-backed B14E Action
  decisions view as `GOV-UX-01C6A`, first capturing the exact C5D baseline and
  proving the missing sixth tab. Treat less-than-61,500 as C5D's acceptance
  threshold and retain its primitives structurally; keep the permanent 65,536-
  byte bound. Restore an over-budget complete probe before selecting any
  separate compaction prerequisite.

## Completed Slice: `GOV-UX-01C5E`

- `requirement`: `GOV-UX-01C5E` behavior-preserving Activity-record primitive
  compaction prerequisite.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: C5D reproduced a complete fifth-view fixed-asset total of
  66,070 bytes, 534 above the permanent boundary and 4,570 above C5C's preserved
  headroom regression. The restored C5C JavaScript remains 32,543 bytes and
  still repeats fixed `getElementById` syntax, enum label/class tables, bounded
  field checks, record-cell/status construction, and array normalization/count
  rendering across the three current Activity lists. C5D would repeat those
  same primitives again.
- `selection_reason`: changing or removing either existing byte assertion would
  discard proved extension headroom, while adding an asset or lazy loader would
  bypass the fixed-shell contract. Consolidating the remaining homogeneous
  Activity-record primitives is independently reversible, reduces current code
  and the resumed C5D delta, and changes no authority or product behavior. A
  less-than-57,500-byte target reclaims at least 3,885 bytes now, leaves more
  than 8,036 bytes below the permanent boundary, and gives resumed C5D a 4,001-
  byte budget under the preserved C5C regression; reusing the new primitives
  must remove at least 686 bytes from the measured 4,686-byte C5D probe.
- `implementation_contract`: refactor only the fixed JavaScript source template.
  Replace repeated top-level DOM lookup syntax with one explicit fixed-binding
  helper and a complete auditable key/ID table. Replace fixed requester/actor/
  context/status label and class duplication with explicit accepted-value
  domains plus one deterministic presentation helper and only the exceptional
  label overrides needed to preserve exact text. Introduce small shared helpers
  for required/optional bounded text/date checks, record cell/status creation,
  and bounded normalized-list rendering; make Tool calls, Worker heartbeats,
  and Jobs delegate only their repeated mechanics while retaining separate
  exact-key validators, domain checks, normalization semantics, row order,
  classes, text, and named renderer functions.

  Preserve the C5C controller unchanged except for the minimum call adaptation
  to shared record primitives. Keep source names and configuration readable;
  do not use Proxy/dynamic selectors, token renaming, punctuation or whitespace
  minification, eval/code generation, generic schema interpretation, a parser/
  minifier, another asset, compression, lazy loading, dynamic import, or a
  dependency. HTML and CSS served bodies must remain byte-identical. JavaScript
  must stay deterministic and parseable, and all existing endpoint-call/static
  assertions must remain true.
- `allowed_paths`: `src/http/governance-browser-assets.ts`,
  `tests/integration/governance-http.test.ts`, `docs/architecture.md`,
  `docs/deployment.md`, `docs/governance-ui.md`, `docs/security-privacy.md`, and
  this checkpoint only.
- `protected_paths`: every HTML/CSS source byte and visible DOM/string; endpoint
  constants, request options, exact-key arrays, accepted domains, normalization
  outcomes, row order/classes/text, fixed list controller states, tab/navigation/
  accessibility/session behavior, every backend route/query/service/DTO/
  repository, session/CSRF/scope/resource/preview registry, GovernanceService,
  CLI/QQ, application listener, config/env, schema, migrations, dependencies/
  lockfile/build contract, Provider, private/live data, deployment/restart/
  container behavior, C1-C5D contracts, and every prior slice.
- `assertions`: unchanged named C5C and complete governance HTTP tests plus the
  final C5C desktop/mobile browser harness pass before editing. One named fail-
  first size assertion requires combined fixed assets below 57,500 bytes and
  fails against exact 61,384-byte C5C output. Final HTML/CSS equal the captured
  baseline byte-for-byte; JavaScript parses, remains readable, preserves all
  exact-path/content-type/security/callback assertions, and brings the total
  below 57,500 without token/whitespace minification. The unchanged browser
  harness produces byte-identical screenshots and covers all C5B states,
  requests, stale cancellation, expiry/logout, keyboard/focus, mobile labels,
  and layout/error hygiene. Complete governance HTTP, HTTP/application/wiring
  adjacency, canonical P8, type/lint/build/preflight/release, dependency/
  migration/inventory/privacy, diff hygiene, and exact rollback gates remain
  green.
- `focused_commands`: unchanged named C5C and complete governance HTTP
  characterizations; one named C5E fail-first and final threshold assertion;
  exact body comparison/measurement and embedded-JavaScript parse; unchanged
  C5C desktop/mobile Chromium QA; HTTP/application/wiring adjacency; canonical
  P8; typecheck; scoped/full ESLint; build; release preflight; complete normal-
  umask release; `git diff --check`; dependency/migration/inventory/privacy
  hygiene; and an exact six-path private reverse-first/forward-first proof.
- `schema_dependency_live_boundary`: fixed JavaScript structure, synthetic
  regression coverage, four canonical browser/security documents, and
  checkpoint evidence only. No HTML/CSS, visible/browser-request behavior,
  backend, persistence, schema, migration, dependency, lockfile, build contract,
  CLI/QQ, Provider, private DB, deployment, restart, container, or live
  operation may change.
- `rollback_boundary`: after this contract and a clean diff check, capture exact
  C5C bytes and modes for browser source, integration test, four canonical
  documents, and this checkpoint under a new mode-`0700` private root. The final
  exact six-product-path ASCII patch must independently prove zero-fuzz reverse-
  first and forward-first byte/mode restoration without touching the workspace;
  checkpoint evidence remains outside the rollback patch.
- `baseline_evidence`: contract validation and the unchanged C5C baseline passed
  before editing: the named C5C case passed 1 / 1 with 20 skipped, complete
  governance HTTP passed 21 / 21, embedded JavaScript parsed, and exact served
  HTML/CSS/JavaScript sizes remained 14,047/14,794/32,543 bytes, 61,384 total.
  The unchanged desktop/mobile Chromium harness covered all 23 states, 34
  requests per viewport, keyboard tab navigation, expiry/logout, stale
  cancellation, mobile labels, and layout/error hygiene with zero unexpected
  console, page, failed-request, or external-request events. Its mode-`0600`
  log has SHA-256
  `a81604d038c6914926779d238afadce5feabddac33a9d8357ac14c1110d6472a`;
  desktop/mobile screenshots have SHA-256
  `bb8bef9e9d4fe7d2daa818fb166e28f90cdd0b8a5f474ecc3e440b38ece5c79d`/
  `ff857ac1dfac5236b3770d8c6b8622cbe9c04818e4a2330fbe28ffa4d8fd8f85`.
  The seven-path C5C baseline is under private mode-`0700` root
  `/tmp/lethebot-gov-ux-01c5e-roundtrip-SDWf58`; its mode-`0600`, 967-byte
  manifest has SHA-256
  `f6b527c566f01e83be468cbdc14eefb8f3e79f1759ad49a2376dcea4a6fd9630`
  and records `main@cfebb77`, zero staged paths, exact hashes, sizes, and modes.
- `exact_fail_first_action`: after the complete baseline, add one assertion to
  `serves compact fixed browser assets with bounded extension headroom` that
  requires the combined UTF-8 body size to be less than 57,500 bytes. Run only
  that named case before changing browser source or canonical documents; it must
  fail at that assertion with the exact 61,384-byte C5C total.
- `fail_first_evidence`: the single stricter assertion was added to the named
  compact-assets case after the baseline capture and before browser-source or
  canonical-document edits. Its isolated run failed exactly at that assertion
  with `expected 61384 to be less than 57500`; 20 adjacent cases were skipped.
  The mode-`0600`, 1,159-byte log is
  `/tmp/lethebot-gov-ux-01c5e-fail-first.log` with SHA-256
  `919becf1c54be7f34e17c23b36c11701391f27df12545eff656106b16ed0dd4c`.
- `final_evidence`: one explicit key/ID binding table, accepted-value domains
  with deterministic presentation, shared required/optional bounded text and
  date checks, shared record cell/status construction, and one bounded
  normalized-list renderer now own only repeated fixed JavaScript mechanics.
  Tool calls, Worker heartbeats, and Jobs retain named renderers, separate
  exact-key validators, accepted values, normalization outcomes, visible DOM
  text/classes/order, counts, limits, controller behavior, and endpoint calls.
  HTML and CSS are byte-identical to C5C. Final HTML/CSS/JavaScript sizes are
  14,047/14,794/28,617 bytes, 57,458 total: 3,926 bytes reclaimed, 4,042 bytes
  below C5C's 61,500-byte regression, and 8,078 below the permanent boundary.
  JavaScript parses. The mode-`0600` asset log is
  `/tmp/lethebot-gov-ux-01c5e-assets.log` with SHA-256
  `8f245eef57459e4a2dbd8a7a1d5ad481327dff261341f3ac1c5af717000c442d`.

  The final named/full-HTTP/six-file-adjacency/canonical-P8 gates passed
  1/21/56/329 tests. Their mode-`0600` log SHA-256 values are
  `89acc5bd0380ca9c4d69efa291ec7e64678658079b6e34ad97d17888657b682e`,
  `7ae7db6d2cbe8d3f710a83d370eee2595b3f9ed41da42c09c7c3e6deffaebce2`,
  `530a488fab9ddaddf2e400ac7f35d3edab153a1a7a2ad51c3674d592b0782bc9`,
  and `6a47ed3e158171799a512efd9acdaefbbd9d41cbd08ebee9f642d7e41c886bd2`.
  Typecheck, scoped/full ESLint, build, release preflight, and diff hygiene
  passed. The complete normal-umask release gate passed 137 / 1 skipped files
  with 2,834 / 10 skipped tests; its mode-`0600` log is
  `/tmp/lethebot-gov-ux-01c5e-release.log` with SHA-256
  `2d64865dcd86c5889212907c18a4292574e277a9b73bb7d8e613d7b1c81c201d`.

  Chromium at 1440x900 and 390x844 repeated all 23 C5B states and 34 requests
  per viewport, keyboard navigation, expiry/logout, stale cancellation, mobile
  labels, and layout/error hygiene with zero unexpected console, page, failed-
  request, or external-request events. Desktop/mobile screenshots are byte-
  identical to baseline with SHA-256
  `bb8bef9e9d4fe7d2daa818fb166e28f90cdd0b8a5f474ecc3e440b38ece5c79d`/
  `ff857ac1dfac5236b3770d8c6b8622cbe9c04818e4a2330fbe28ffa4d8fd8f85`;
  the mode-`0600` browser log has SHA-256
  `abf3a015d965e039bd876633b0f5dead3f2819e0ca2563358d595455ef927451`.

  The exact six-product-path zero-context ASCII patch is
  `/tmp/lethebot-gov-ux-01c5e-roundtrip-SDWf58/gov-ux-01c5e.patch`
  (`sha256:2272ca8c878399f1227614c1fbae299d547c018d8175639a01ed8e531ec5a1d0`,
  mode `0600`, size `28049`). Corrected `v4` reverse-first and forward-first
  zero-fuzz cycles restored exact baseline/candidate bytes and modes and proved
  workspace/candidate equality; its mode-`0600` log has SHA-256
  `9601adff7bf987f6099cd3438dace2ee2aad79005cf3997b2d4315ee9ada227c`.
  A partial `v3` setup used absolute-parent baseline copies and stopped before
  forward comparison; it is excluded. Final hygiene remains 74 tracked
  modifications, 60 untracked paths, and zero staged at `main@cfebb77`; Pi,
  dependency/lockfile, migration, schema, Provider, QQ, private/live, deploy,
  restart, container, and backend boundaries are unchanged.
- `exact_next_action`: completed; C5D resumed against the fresh C5E baseline
  and retained the documented primitives and permanent boundary.

## Completed Slice: `GOV-UX-01C6A`

- `requirement`: `GOV-UX-01C6A` read-only Action decisions Activity browser
  view.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: authenticated B14E already exposes the bounded, actions-free
  decision list at `/governance/api/v1/activity/action-decisions`, while the
  browser now covers model, tool, worker, job, and attempt evidence but no
  action-selection reason or suppressor evidence.
- `selection_reason`: Action decisions is the next highest-priority already-
  backed Activity workflow and precedes its B14F execution counterpart. It can
  reuse the fixed list controller and C5E/C5D validation, presentation, row,
  state, responsive, and keyboard primitives without backend or authority work.
  C5D leaves 4,064 bytes below the permanent boundary, so a complete accessible
  probe must establish whether the view fits before any prerequisite is chosen.
- `implementation_contract`: preserve the first five Activity tab positions
  and Model invocations as the default, then add Action decisions as the sixth
  tab after Job attempts. Add one explicit controller configuration that calls
  only authenticated same-origin
  `GET /governance/api/v1/activity/action-decisions`. Accept at most 100 exact
  B14E records with required bounded `id` and `turnId`, canonical `createdAt`,
  fixed attention/pi/evaluator `decidedBy`, fixed low/medium/high/prohibited
  risk, finite confidence in `0..1`, boolean `evaluatorRequired`, optional
  boolean `evaluatorPassed`, non-negative safe-integer `actionCount`, and
  bounded arrays of bounded reason and suppressor strings. Reject unknown keys,
  `actions`, malformed fields, unbounded nested values, and oversized records
  before replacing rows.

  Render only the already-redacted decision reference, actor/risk, confidence,
  evaluator state, action count, bounded reasons/suppressors, and decision time
  with DOM creation and `textContent`; validate but omit `turnId`. Reuse fixed
  loading, empty, unavailable, refresh, stale-cancellation, expiry/logout,
  roving-tab, focus, mobile-label, and layout behavior. Add no filter, caller
  limit, scope header, body, CSRF value, detail link, polling, inference,
  mutation, action payload, backend route, DTO, dependency, or external asset.
- `allowed_paths`: `src/http/governance-browser-assets.ts`,
  `tests/integration/governance-http.test.ts`, `docs/architecture.md`,
  `docs/deployment.md`, `docs/governance-ui.md`, `docs/security-privacy.md`, and
  this checkpoint only.
- `protected_paths`: `GovernanceHttpServer`, `src/index.ts`, B14E route/query-
  service/DTO/order/limit/redaction semantics and wiring regression; C1-C5E and
  C5D visible, validation, controller, accessibility, and authority behavior;
  every other Activity/scoped/detail/mutation API; GovernanceService; CLI/QQ;
  config/env; schema, migrations, dependencies/lockfile/build contract;
  Provider, private/live data, deployment/restart/container behavior; and every
  prior slice.
- `assertions`: unchanged named C5D fixed-asset and B14E route
  characterizations reproduce exact 15,802/14,794/30,876-byte bodies and route
  semantics before editing. One named fail-first regression sees the complete
  five-tab C5D assets but no `action-decisions-tab` or B14E endpoint reference.
  Final assets preserve all five predecessor views, add the complete strict
  sixth view, retain C5D primitives through structural assertions, and stay
  below the permanent 65,536-byte boundary. Less-than-61,500 remains C5D's
  feature acceptance threshold rather than a zero-growth C6A ceiling. If the
  minimum complete probe exceeds the permanent boundary, restore source/test
  bytes before defining a separate behavior-preserving prerequisite; do not
  weaken the permanent bound or mix compaction into C6A.
- `schema_dependency_live_boundary`: fixed browser assets, synthetic tests,
  four canonical browser/security documents, and checkpoint evidence only. No
  backend, route, persistence, schema, migration, dependency, lockfile, CLI/QQ,
  Provider, private DB, deployment, restart, container, or live operation may
  change.
- `rollback_boundary`: capture the exact C5D bytes and modes for browser source,
  integration test, four canonical documents, and this checkpoint under one new
  private mode-`0700` root before a fail-first edit. Any final six-product-path
  ASCII patch must independently prove reverse-first and forward-first exact
  byte/mode restoration without touching the workspace; checkpoint evidence is
  excluded from the patch.
- `baseline_evidence`: unchanged named C5D, compact-asset, and B14E route
  characterizations passed 3 / 3 with 58 skipped. Direct module measurement
  reproduced exact served HTML/CSS/JavaScript sizes 15,802/14,794/30,876 bytes,
  61,472 total, and parseable JavaScript. Private mode-`0700`
  `/tmp/lethebot-gov-ux-01c6a-baseline-2b7C8C` captures the exact seven-path C5D
  baseline byte-for-byte and mode-for-mode; every captured file is mode `0644`
  and every directory is mode `0700`. The baseline source/test/architecture/
  deployment/governance/security/checkpoint hashes are
  `eb79fccacd79d3a72073790f10b941232881c52214c37b52951eb6d24858b977`,
  `97b732cf7833c8b57843d4aea032859f60d7b639fcf871a3695f9e71f8534391`,
  `676d842fcfab85eb2293691303841729001b69a3b30ce2faf8d11ba248d3400a`,
  `8bcac13d33f3b178303455bebfbff8ff7c14365f3fda2007ada974aeac4ce911`,
  `051a6e4c7d7b3bba2e74612039e04730cea9aab6e67c3718837e7ec77a5fa3c7`,
  `1cee4b58f76e5baecf11d40887762fbcab92685f53d08e6482b7385e70721bd7`,
  and `aa85f6ecd6940349872044ab77e1b8bc844b69fc5cef0bc3248852cb4a5ffdae`.
- `fail_first_evidence`: the named C6A regression collected 23 governance HTTP
  cases, skipped 22, and failed only at its first assertion because the exact
  C5D HTML had no `id="action-decisions-tab"`. Browser source, all four
  canonical documents, and the checkpoint still matched the captured baseline;
  only the test differed and `git diff --check` passed. The mode-`0600`, 18,122-
  byte log is `/tmp/lethebot-gov-ux-01c6a-fail-first.log` with SHA-256
  `09b077ed07975a32019860e15c312ec572c054660dcb5a5da9c1f773100aab49`.
- `implementation_probe_evidence`: the first complete sixth-tab probe parsed
  but measured 17,631/14,794/34,383 bytes, 66,808 total and 1,272 above the
  permanent boundary. Feature-local removal of repeated C6A markup and one-use
  validation/rendering mechanics retained every required state and field while
  leaving all predecessor code and compacting behavior unchanged. The accepted
  candidate measures 17,249/14,794/33,454 bytes, 65,497 total with 39 bytes of
  headroom, and parses successfully. CSS remains byte-identical to C5D.
- `final_evidence`: Action decisions is the sixth Activity tab after Job
  attempts and calls only exact same-origin B14E. It accepts at most 100 exact
  records, exact keys, required 256-unit IDs, canonical creation time, fixed
  actors and risks, finite `0..1` confidence, required/optional evaluator
  booleans, non-negative safe action count, and at most 32 non-empty 512-unit
  reasons and suppressors. Unknown keys, actions, malformed fields, nested
  non-text/overlong values, and oversized rows fail before replacement. The
  normalizer projects away `turnId`; only the redacted decision reference/time,
  actor/risk, confidence/evaluator state, action count, and bounded evidence
  enter text nodes.

  The named C6A/C5D/compact cases passed 3 / 3, full governance HTTP passed
  23 / 23, B14E plus HTTP passed 24 / 24, HTTP/application/wiring adjacency
  passed 4 files / 69 tests, and canonical P8 passed 7 files / 331 tests.
  Typecheck, scoped/full ESLint, build, preflight, JavaScript parse, and diff
  hygiene passed. The canonical P8 log is mode `0600` with SHA-256
  `83990a5fb4c804463f4ad16da82a1368e78880021f0d0719a576399999ca286d`;
  the static log is mode `0600` with SHA-256
  `9b3c33997b2e4e8c3522b52bdf67f62d60f1a99ffb7e460f150bd9de616d6577`.
  The first full release wrapper used private umask `077`, invalidating one
  permissive-directory negative fixture, and is excluded. That case passed
  under normal umask, then the complete normal-umask release gate passed 137 / 1
  skipped files and 2,836 / 10 skipped tests. Its mode-`0600` log is
  `/tmp/lethebot-gov-ux-01c6a-release-normal.log` with SHA-256
  `bac1d1d10785cb7da28af36b86f6e125720b5ddd1f3a3d1e9fb706a2f09c47eb`.

  Fresh system Chromium at 1440x900 and 390x844 covered all four actors/risks,
  evaluator absent/pending/passed/rejected states, populated/loading/empty,
  malformed/missing/unknown/actions/nested/array/string/row bounds,
  unavailable, stale-cancelled, expiry, logout, six-tab Arrow/Home/End, visible
  focus, 44px controls, mobile labels, and layout/network/error hygiene. Each
  viewport made 33 requests; 38 Action reads were exact query/body/scope/CSRF-
  free GETs. The only console resource diagnostics were the deliberately tested
  `500` and `401`; unexpected console/page/request/external failures were zero.
  Final screenshots were visually reviewed and clean. Mode-`0700`
  `/tmp/lethebot-gov-ux-01c6a-browser-final-iRz80l` holds the mode-`0600` log
  (`sha256:4db0225a8884fb76a89f0a28fb2370b1b7cd5d9b4ca9d4c1f99d5ad0b3095b59`)
  and desktop/mobile screenshots with SHA-256
  `a87d797c8f686e33424d409f78bb4f8ebe4546b96dd33450bebed95be54c6880`/
  `04dc5fbbdc999f75b4d9c3bbe95241dc7ac5d0f72f3345ef240a839b50a7b6c9`.

  Mode-`0700` `/tmp/lethebot-gov-ux-01c6a-roundtrip-cZFg8L` holds the six-path
  zero-context ASCII patch (`sha256:c00fa482aebd261fbb7aabccc46fae506cd917a3d56d929c3ba615576cbddfd7`,
  mode `0600`, size `14348`). Reverse-first and forward-first zero-fuzz cycles
  restored exact baseline/candidate bytes and modes, candidate/workspace matched
  6 / 6, all proof directories were mode `0700`, and no reject/original file
  appeared. Final hygiene remains 74 tracked modifications, 60 untracked files,
  zero staged at `main@cfebb77`; Pi remains `0.80.2`, package/lockfile/migrations
  are unchanged, the refined sensitive-artifact and reject/original screens are
  empty, and no backend, route, DTO, schema, persistence, CLI/QQ, Provider,
  private/live, deploy, restart, container, dependency, or lockfile behavior
  changed.
- `exact_next_action`: completed; the required two-slice Activity reassessment
  selects the behavior-preserving C6B fixed-shell compaction prerequisite below.

## Completed Slice: `GOV-UX-01C6B`

- `requirement`: `GOV-UX-01C6B` behavior-preserving fixed Activity list-shell
  compaction prerequisite.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: C6A completes the sixth read-only Activity workflow but leaves
  only 39 bytes below the permanent 65,536-byte fixed-asset boundary. B14F
  already backs the next required Action executions workflow, but even its
  fixed accessible shell cannot fit without first reclaiming representation
  space.
- `selection_reason`: C5D and C6A are two consecutive browser Activity feature
  slices, so the full P8 dependency reassessment is due. A separate no-feature
  compaction is the smallest reversible dependency: it may change only how the
  five non-model list shells are constructed, while preserving all runtime DOM,
  requests, validation, rendering, session, accessibility, and authority
  behavior before C6C adds B14F.
- `implementation_contract`: remove only the five non-model list tabs and
  panels from static HTML and construct their exact fixed runtime DOM from one
  in-asset immutable definition table using `document.createElement`, fixed
  attributes, and `textContent` before list controllers bind. Preserve exact
  tab/panel order, IDs, tags, roles, ARIA links and selection, hidden/busy
  states, toolbars, headings/copy, refresh controls, counts, skeleton rows,
  error/empty states, table classes/roles/captions/colgroups/headers/list bodies,
  controller configurations, endpoints, messages, request sequences,
  validators, normalized fields, renderers, row DOM, CSS, and all C1-C6A output
  after module initialization. Model invocations remains static and default.
  Definitions are fixed local literals, never response-derived. Do not use
  `innerHTML`, HTML parsing, dynamic code, selectors or attributes from API
  data, another asset, lazy loading, compression, token minification, or a
  dependency.
- `allowed_paths`: `src/http/governance-browser-assets.ts`,
  `tests/integration/governance-http.test.ts`, `docs/architecture.md`,
  `docs/deployment.md`, `docs/governance-ui.md`, `docs/security-privacy.md`, and
  this checkpoint only.
- `protected_paths`: all C1-C6A runtime DOM/text/order/classes/states, endpoint/
  controller/request/validation/normalization/rendering/session/keyboard/focus/
  responsive behavior; `GovernanceHttpServer`, `src/index.ts`, B14 routes and
  query DTOs; every scoped/detail/mutation API; GovernanceService; CLI/QQ;
  config/env; CSS visible behavior; schema/migrations/dependencies/lockfile;
  Provider, private/live data, deployment/restart/container behavior; and every
  prior slice.
- `assertions`: unchanged named C6A/C5D/compact assets reproduce exact
  17,249/14,794/33,454-byte bodies and all current static contracts before
  editing. Fresh desktop/mobile Chromium captures the complete five generated
  tab/panel runtime structures and all six workflows before editing. One named
  fail-first compaction regression fails only because 65,497 is not below
  60,500 bytes. Final runtime structures are equivalent in tag/attribute/text/
  child order, all named C1-C6A cases and full browser state/keyboard/layout/
  network matrices remain green, CSS is byte-identical, JavaScript parses, the
  fixed bundle is below 60,500 and permanently below 65,536 bytes, and no new
  request, asset, authority, feature, or sink appears.
- `schema_dependency_live_boundary`: fixed browser representation, synthetic
  tests, four canonical browser/security documents, and checkpoint evidence
  only. No feature, backend, route, persistence, schema, migration, dependency,
  lockfile, CLI/QQ, Provider, private DB, deployment, restart, container, or
  live operation may change.
- `rollback_boundary`: after this contract and a clean diff check, capture the
  exact C6A bytes and modes for browser source, integration test, four canonical
  documents, and checkpoint under one new private mode-`0700` root. The final
  six-product-path ASCII patch must prove independent reverse-first and forward-
  first exact byte/mode restoration without touching the workspace; checkpoint
  evidence remains outside the patch.
- `baseline_evidence`: unchanged named C6A/C5D/compact characterizations passed
  before implementation and reproduced the exact 17,249/14,794/33,454-byte
  C6A assets, 65,497 total. Private mode-`0700`
  `/tmp/lethebot-gov-ux-01c6b-baseline-QBObvJ` captures the seven allowed C6A
  paths byte-for-byte and mode-for-mode at `0644`; its manifest records source,
  test, architecture, deployment, governance UI, security, and checkpoint
  SHA-256 values
  `db6ea9b902f9d05f15c5d2cfbc1b5a8dd1d11ba6dda3a5607d29e671afba8c86`,
  `f1ae535a744fdaf6c0e4942dd5aa3ae2b6690d7d3e6376551b33869a8ce2ac0a`,
  `81c8f02ac5a72ef5624eb73793c48460e15f3a43b87adb666eb23935ecba8b52`,
  `770ff0978a082112831db0eef9925657d936b9bb563580d141c44e93ff6fb524`,
  `4e5f372bd2b25fd2c24954030874f696f48cc4765e527ad75bfde907fc2f7d8d`,
  `9dc42cbf8a14241d27b038876e43ac77d8a8e21343ba2f9fae257acdf0188c9c`,
  and
  `5a7f717bddf19e3f8462ba63ccb80e03e0e1916685dbc8c643f73d42fa00d103`.
  Fresh pre-change Chromium captured the canonical tablist and five non-model
  panels at both viewports under mode-`0700`
  `/tmp/lethebot-gov-ux-01c6b-browser-before-FANVvJ`; both normalized structures
  have SHA-256
  `b18d22317c3205a77f9608e37b4a735c98f852fc8bd7a750c4889365e570b852`.
- `fail_first_evidence`: the new headroom characterization collected the full
  governance HTTP file and failed only because the exact 65,497-byte C6A bundle
  was not below 60,500. The mode-`0600` log is
  `/tmp/lethebot-gov-ux-01c6b-fail-first.log` with SHA-256
  `23d8f6365ac90a25aa3075ad894d0c5a6d87926e0b4eb8fcb9ec706019c29910`;
  source and canonical documents still matched the captured baseline.
- `final_evidence`: static HTML now retains only Model invocations. One fixed
  local definition table builds the other five tabs and panels before controller
  lookup using only `document.createElement`, fixed attributes, and
  `textContent`; the same entries retain exact endpoint, renderer, and state
  configuration. Runtime tag/attribute/text/child order, skeleton dimensions,
  table semantics, visible output, requests, validation, row replacement,
  stale cancellation, session expiry, keyboard/focus, CSS, and responsive
  behavior are unchanged. Final served HTML/CSS/JavaScript are
  8,641/14,794/37,000 bytes, 60,435 total: 5,062 bytes reclaimed from C6A and
  5,101 bytes below the permanent boundary. The new prerequisite assertion and
  full governance HTTP passed 24 / 24; four-file HTTP/application/wiring
  adjacency passed 70 / 70; canonical P8 passed 7 files / 332 tests; typecheck,
  scoped/full ESLint, build, preflight, JavaScript execution, and diff hygiene
  passed. The complete normal-umask release gate passed 137 / 1 skipped files
  and 2,837 / 10 skipped tests.

  System Chromium repeated the complete C6A decision/failure/stale/session/
  keyboard/responsive matrix at 1440x900 and 390x844 with 33 requests per
  viewport and no unexpected console, page, failed-request, or external-request
  events. Both post-change canonical runtime structures are byte-identical to
  both pre-change files and retain SHA-256
  `b18d22317c3205a77f9608e37b4a735c98f852fc8bd7a750c4889365e570b852`.
  Mode-`0700` `/tmp/lethebot-gov-ux-01c6b-browser-after-c10Cn6` holds the
  mode-`0600` browser log
  (`sha256:cde30ed89b83f3a32fa97bd630f609e68c5a8478c510abd97b9c90ea79b36841`)
  and visually reviewed desktop/mobile screenshots with SHA-256
  `e00c0cc2b937774998f7dfd2a5cb4fc9802a0f8cc4287c7bdbcb7cbc2201137b`/
  `95520636f28016f6a357b2c1518efc58cd632d23f4245f22114448d4764ff9ae`.

  Mode-`0700` `/tmp/lethebot-gov-ux-01c6b-roundtrip-zPNH3Z` holds the exact
  six-path ASCII patch
  (`sha256:bb2ff1fcb44250dd20e89993436ef3700ca6d9fb74ab882a27ba20edef286ea0`,
  mode `0600`, size `38937`). Independent reverse-first and forward-first
  cycles restored all six baseline/candidate files byte-for-byte and mode-for-
  mode, all 24 proof files are `0644`, all 26 proof directories are `0700`, and
  candidate/workspace equality is 6 / 6. Final hygiene remains 74 tracked
  modifications, 60 untracked paths, and zero staged at `main@cfebb77`; Pi
  remains `0.80.2`, migrations 007/008 retain their recorded hashes, and no
  feature, backend, route, accepted payload, schema, persistence, dependency,
  lockfile, CLI/QQ, Provider, private/live, deployment, restart, or container
  behavior changed.
- `exact_next_action`: completed; add the B14F Action executions browser view
  as `GOV-UX-01C6C` against this compact fixed-shell baseline.

## Completed Slice: `GOV-UX-01C6C`

- `requirement`: `GOV-UX-01C6C` read-only Action executions Activity browser
  view.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: authenticated B14F already exposes the bounded audit-entry-
  free execution list at `/governance/api/v1/activity/action-executions`, while
  the browser stops at Action decisions and cannot show whether selected
  actions succeeded, downgraded, failed, or were rejected.
- `selection_reason`: Action executions is the already-backed counterpart to
  the completed decision view and the next required Activity workflow. C6B
  deliberately reclaimed 5,101 bytes under the permanent boundary for this
  feature, so the smallest next slice is one strict list definition, validator,
  renderer, and browser characterization with no backend or authority change.
- `implementation_contract`: preserve the first six tab positions and Model
  invocations as default, then add Action executions after Action decisions.
  The new fixed definition calls only authenticated same-origin
  `GET /governance/api/v1/activity/action-executions`. Accept at most 100 exact
  B14F records. Require non-empty `id` and `actionDecisionId` bounded to 256
  UTF-16 code units; one of the 12 current `ActionType` values; fixed
  `success`/`downgraded`/`failed`/`rejected` status; fixed
  `summary`/`redacted_full`/`full` audit level; and canonical ISO `executedAt`.
  Optional executed-message, executed-memory, executed-job, and downgraded-from
  references must be bounded strings, with downgraded-from in the same fixed
  action domain; optional downgrade/error diagnostics are non-empty strings
  bounded to 512 units. Reject unknown keys, `auditEntry`, malformed values,
  nested data, or oversized arrays before row replacement.

  Render only the already-redacted execution and linked-decision references,
  execution time, action/status, optional executed-effect references, optional
  downgrade/error evidence, and audit level through existing safe record cells,
  DOM construction, and `textContent`. Reuse the fixed shell/controller,
  loading/empty/error/content, stale-cancellation, expiry/logout, roving-tab,
  focus, mobile-label, and responsive-table primitives. Add no filter, caller
  limit, scope/CSRF header, body, polling, detail link, inference, mutation,
  audit-entry access, backend route/DTO, external asset, or dependency.
- `allowed_paths`: `src/http/governance-browser-assets.ts`,
  `tests/integration/governance-http.test.ts`, `docs/architecture.md`,
  `docs/deployment.md`, `docs/governance-ui.md`, `docs/security-privacy.md`, and
  this checkpoint only.
- `protected_paths`: C1-C6B runtime behavior and fixed-shell semantics; B14F
  route/query/DTO/order/limit/redaction and wiring regression;
  `GovernanceHttpServer`, `src/index.ts`, every other Activity/scoped/detail/
  mutation API, GovernanceService, CLI/QQ, config/env, schema, migrations,
  dependencies/lockfile, Provider, private/live data, deployment/restart/
  container behavior, and every prior slice.
- `assertions`: unchanged C6A/C6B/compact and B14F route characterizations pass
  and reproduce exact 8,641/14,794/37,000-byte assets before editing. A named
  fail-first sees the complete six-tab browser and B14F backend but no Action
  executions tab or endpoint reference. Final coverage proves exact keys,
  fixed domains, string/date/row bounds, `auditEntry`/unknown/nested rejection,
  no replacement on invalid responses, every status/effect/diagnostic state,
  exact request shape, stale cancellation, session expiry/logout, seven-tab
  keyboard/focus order, mobile labels, no overflow, and a bundle below 65,536.
- `focused_commands`: named fail-first/final C6C regression; named C6A/C6B/
  compact and B14F route adjacency; full governance HTTP; four-file HTTP/
  application/wiring adjacency; canonical P8; `pnpm typecheck`; scoped/full
  ESLint; build/preflight; normal-umask `pnpm release:check`; JavaScript/browser
  QA; diff, inventory, dependency/migration/privacy hygiene; and exact six-path
  private reverse-first/forward-first rollback proof.
- `schema_dependency_live_boundary`: fixed browser assets, synthetic tests,
  four canonical browser/security documents, and checkpoint evidence only. No
  backend, route, persistence, schema, migration, dependency, lockfile, CLI/QQ,
  Provider, private DB, deployment, restart, container, or live operation may
  change.
- `rollback_boundary`: after this contract and a clean diff check, capture the
  exact C6B bytes and modes for browser source, integration test, four canonical
  documents, and checkpoint under one private mode-`0700` root. The final six-
  product-path ASCII patch must independently prove reverse-first and forward-
  first exact byte/mode restoration without touching the workspace; checkpoint
  evidence remains outside the patch.
- `baseline_evidence`: the existing five fixed-shell, Action decisions, compact-
  asset, and B14F route characterizations passed 7 / 7 before implementation.
  Served HTML/CSS/JavaScript reproduced 8,641/14,794/37,000 bytes, 60,435
  total; HTML and JavaScript parsed cleanly. Private mode-`0700`
  `/tmp/lethebot-gov-ux-01c6c-baseline-s2a4mk` captures all seven allowed C6B
  paths byte-for-byte and mode-for-mode, with source/test/architecture/
  deployment/governance/security/checkpoint SHA-256 values
  `9df5feea61e1b8657432343ff1bfc208896573e9f392fe49eab6526247dbbd3b`,
  `7fe8edcf9271b18ba05949fb0d7b1d86215ad6f0b684ebefbffbae5b878f85cd`,
  `bfa06b113995e756a6d8543da96a7220cf52543f42cb1882daf36cd79bbea1fa`,
  `6526e993ca8ba8b3dca4ab98e0228eb88c33ce3c047e17411e46a8d43e498445`,
  `ca67e615fb55d2efe67bcfe20db1042c81f31eb9efb64e95ba3b474c34d8900d`,
  `c0c982d13b8dabb7b635073e82c7ccfaa3588a30a3688969069e6c48559f13f0`,
  and `4fc5bf11bc25fa37844d81fb499473b70324462087d254a9a87a4f35437084dd`.
- `fail_first_evidence`: the new named Action executions browser test failed
  only because the fixed `executions` / `action-executions` / `Action
  executions` definition was absent. Mode-`0600`
  `/tmp/lethebot-gov-ux-01c6c-fail-first.log` has SHA-256
  `c3a5995b1ce5ad79251297acd4fb6036a89f276587cc5f32696846986096cc83`.
- `final_evidence`: the seventh tab calls only B14F's exact same-origin default
  GET. Its exact-key validator accepts at most 100 bounded audit-entry-free
  records across all 12 action types, four statuses, three audit levels,
  canonical dates, optional effect references, and bounded diagnostics; invalid
  responses do not replace existing rows. The safe renderer shows only the
  redacted execution/decision references, action/status, effects, evidence,
  audit level, and time. All four statuses have semantic badge styling. Final
  served HTML/CSS/JavaScript are 8,641/14,850/39,809 bytes, 63,300 total with
  2,236 bytes below the permanent boundary.

  The named/full browser HTTP, four-file HTTP/application/wiring adjacency, and
  canonical P8 gates passed 1/25/71/333 tests. Typecheck, scoped/full ESLint,
  build, preflight, asset parsing, and diff hygiene passed. The complete normal-
  umask release gate passed 137 / 1 skipped files and 2,838 / 10 skipped tests;
  its mode-`0600` log SHA-256 is
  `c9362e4207e0ac6e688dc9e34603118872a3dd279f0a8f2f9c5cf3c9a5051d99`.
  System Chromium exercised 20 invalid/unavailable states per viewport and 64
  exact Action execution requests across 1440x900 and 390x844, including every
  action/status/effect/evidence domain, maximum bounds, no-replacement failure,
  stale cancellation, session expiry/logout, seven-tab keyboard order, focus,
  and zero page/cell overflow. No unexpected console, page, failed-request, or
  external-request event occurred. Private mode-`0700`
  `/tmp/lethebot-gov-ux-01c6c-browser-final-bXnmbK` holds the mode-`0600` log
  (`sha256:9bddc51242bbf0b9fb0474ebdee334694d42f056b32e3bdfedebda07f53d2ba1`),
  desktop/mobile screenshots
  (`sha256:73e66973e5be9fefd490facb80a25d043352b5812f0ae5dda984db386be723a6`/
  `364ae453916a0bcfc2b63bc941d1394cfb9d5ba8057d3bda86ce0d77d1a3145f`),
  and identical runtime structures with SHA-256
  `558039fa5ff3d088ee99a67f4300921aed82411fb25f640b340bd909d79b0244`.

  Private mode-`0700` `/tmp/lethebot-gov-ux-01c6c-roundtrip-nx5FKY` holds the
  six-path ASCII patch
  (`sha256:3cdf665dc206758d293988c11f56958aa6f1f976665664add2e026c83e578769`,
  mode `0600`, size `19347`). Independent reverse-first and forward-first zero-
  fuzz cycles restored all 24 proof files byte-for-byte and mode-for-mode, and
  candidate/workspace equality passed 6 / 6. Final hygiene remains 74 tracked
  modifications, 60 untracked files, and zero staged at `main@cfebb77`; Pi
  remains `0.80.2`, lockfile and migration 007/008 hashes are unchanged, and no
  backend, route, DTO, persistence, schema, dependency, CLI/QQ, Provider,
  private/live, deployment, restart, or container behavior changed.
- `exact_next_action`: completed; add the B14H Event processing failures browser
  view as `GOV-UX-01C7A` against this seven-tab candidate.

## Completed Slice: `GOV-UX-01C7A`

- `requirement`: `GOV-UX-01C7A` read-only Event processing failures Activity
  browser view.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: authenticated B14H already exposes the bounded, details-free,
  hash-backed failure list at
  `/governance/api/v1/activity/event-processing-failures`, while the browser
  cannot inspect the aggregate attention signal's underlying processing stage,
  redacted error class, or fixed correlation hashes.
- `selection_reason`: failure triage is the next already-backed Activity task
  after action history and is independent of the later B14I audit view. The C6C
  bundle leaves 2,236 bytes under the permanent boundary, so this slice must add
  only one compact fixed definition, exact validator, and renderer over the
  existing shared primitives.
- `implementation_contract`: preserve the first seven tab positions and Model
  invocations as default, then add Event processing failures after Action
  executions. Call only authenticated same-origin
  `GET /governance/api/v1/activity/event-processing-failures`. Accept at most
  100 exact B14H records. Require non-empty `id` and `stage` bounded to 256
  UTF-16 code units, non-empty `errorName` bounded to 512 units, canonical ISO
  `occurredAt`, and a lowercase 64-hex `errorMessageHash`. Optional `rawEventId`
  and `turnId` are bounded to 256 units; optional `conversationType` is only
  `private` or `group`; optional message, sender, and conversation hashes are
  lowercase 64-hex. Reject unknown keys, `details`, nested/malformed fields,
  and oversized arrays before replacing rows.

  Render only the already-redacted failure reference and time, optional raw-
  event/turn references, stage and optional conversation type, error name, and
  fixed hash evidence through existing safe cells, DOM construction, and
  `textContent`. Reuse the shell/controller, loading/empty/error/content, stale-
  cancellation, expiry/logout, roving-tab, focus, mobile labels, and responsive
  table. Add no filter, caller limit, scope/CSRF header, body, polling, detail
  link, inference, mutation, details access, backend route/DTO, CSS token,
  external asset, or dependency, and keep the complete bundle below 65,536.
- `allowed_paths`: `src/http/governance-browser-assets.ts`,
  `tests/integration/governance-http.test.ts`, `docs/architecture.md`,
  `docs/deployment.md`, `docs/governance-ui.md`, `docs/security-privacy.md`, and
  this checkpoint only.
- `protected_paths`: C1-C6C browser behavior; B14H route/query/DTO/order/limit/
  redaction and wiring regression; B14I and every other Activity/scoped/detail/
  mutation API; `GovernanceHttpServer`, `src/index.ts`, GovernanceService,
  CLI/QQ, config/env, schema, migrations, dependencies/lockfile, Provider,
  private/live data, deployment/restart/container behavior, and every prior
  slice.
- `assertions`: unchanged C6C/fixed-shell/compact and B14H route tests reproduce
  exact 8,641/14,850/39,809-byte assets before editing. A named fail-first sees
  seven tabs and the B14H backend but no failure tab or endpoint reference.
  Final coverage proves exact keys, required/optional bounds, hash and
  conversation domains, canonical dates, `details`/unknown/nested/oversized
  rejection without row replacement, populated/empty/error/stale/expiry/logout
  states, exact request shape, eight-tab keyboard order, mobile labels, no
  overflow, and a bundle below 65,536.
- `focused_commands`: named C7A fail-first/final; C6C/fixed-shell/compact and
  B14H route adjacency; full governance HTTP; four-file HTTP/application/wiring
  adjacency; canonical P8; typecheck; scoped/full ESLint; build/preflight;
  normal-umask release; JavaScript/browser QA; diff, inventory, dependency/
  migration/privacy hygiene; and exact six-path reverse-first/forward-first
  rollback proof.
- `schema_dependency_live_boundary`: fixed browser assets, synthetic tests,
  four canonical browser/security documents, and checkpoint evidence only. No
  backend, route, persistence, schema, migration, dependency, lockfile, CLI/QQ,
  Provider, private DB, deployment, restart, container, or live operation.
- `rollback_boundary`: after this contract and a clean diff check, capture the
  exact C6C bytes and modes for browser source, integration test, four canonical
  documents, and checkpoint in a new private mode-`0700` root. The final six-
  product-path ASCII patch must prove independent reverse-first and forward-
  first exact byte/mode restoration without touching the workspace; checkpoint
  remains outside the patch.
- `baseline_evidence`: the unchanged C6C, fixed-shell, compact-asset, and B14H
  route characterizations passed 4 / 4 before implementation and reproduced
  exact 8,641/14,850/39,809-byte assets, 63,300 total. HTML and JavaScript
  parsed cleanly. Private mode-`0700`
  `/tmp/lethebot-gov-ux-01c7a-baseline-mePR7U` captures all seven allowed C6C
  paths byte-for-byte and mode-for-mode. Source/test/architecture/deployment/
  governance/security/checkpoint SHA-256 values are
  `16e7e316806405e0c9c646758004d643a01b36635a4f9f15c56c8ef529567b4f`,
  `9815f91799a5db1966c5377a1ba971ccb7e10f3894f7ac5a7c3f6c3266dbe3cd`,
  `b30c7d4844748eb87ca28d636e0e83d5c3e78dda48ee01c0487659adcd567ac1`,
  `508c2493de3487eb51faa03ba4e9248439fee964400182bbac818b95c0f71541`,
  `dcb3d0e7ed49d5fa462ee10f8308d7ccd606f680e500d7f51883604dae2f4608`,
  `a9caf8fa25482e3757a3a493a57a33ea3ed06411932c9a61486979435dd3cfb0`,
  and `29475e45920c50345ddea9929457ba3db7c5bb6da66308eb3c65898285dbb295`.
- `fail_first_evidence`: the named browser test collected the complete 26-test
  governance HTTP file, skipped 25 unrelated cases, and failed only because the
  fixed `failures` / `event-processing-failures` / `Event processing failures`
  definition was absent. Its mode-`0600` log is
  `/tmp/lethebot-gov-ux-01c7a-fail-first.log` with SHA-256
  `60740801cee1bee2cde550119ce724419550a029c35c34b49d3d8b4f25b21a91`.
- `final_evidence`: the eighth tab calls only B14H's exact same-origin default
  GET. Its exact-key validator accepts at most 100 bounded, details-free records
  with canonical occurrence time, fixed conversation type, and lowercase
  64-hex hash evidence; invalid responses do not replace existing rows. The
  safe renderer shows only the redacted failure/time, optional raw-event/turn
  references, stage/conversation type, error name, and fixed hashes. Final
  served HTML/CSS/JavaScript are 8,641/14,850/42,007 bytes, 65,498 total with
  38 bytes below the permanent boundary.

  The focused C7A/C6C/compact characterizations, B14H wiring characterization,
  full governance HTTP, four-file HTTP/application/wiring adjacency, and
  canonical P8 gates passed 4/1/26/72/334 tests. Typecheck, scoped/full ESLint,
  build, preflight, asset parsing, and diff hygiene passed. The complete normal-
  umask release gate passed 137 / 1 skipped files and 2,839 / 10 skipped tests;
  its mode-`0600` log `/tmp/lethebot-gov-ux-01c7a-release-normal.log` has
  SHA-256 `c614fb86ee0a0745a664ae679d23756c94108a960bd50aac756683f53d6fdc3f`.
  System Chromium exercised 31 invalid/unavailable states per viewport and 86
  exact failure requests across 1440x900 and 390x844, including all bounds,
  hash/conversation domains, no-replacement failure, stale cancellation,
  session expiry/logout, eight-tab keyboard order, focus, and zero page/cell
  overflow. No unexpected console, page, failed-request, or external-request
  event occurred. Private mode-`0700`
  `/tmp/lethebot-gov-ux-01c7a-browser-final-OwVHOQ` holds the mode-`0600` log
  (`sha256:c898e9b4d0cfdcba4e22919d5935b0028e78d3835a9a61ef2bab80eae82fe30e`),
  desktop/mobile screenshots
  (`sha256:e12d5f6be8d5605b99b035dfbd8077ff91beddc6f773f8fb458a1b7981f363ed`/
  `97773558c38960faeae3b657a50b2e4ede0adbe153a89f5ed83f38976bbe68ee`),
  and identical runtime structures with SHA-256
  `f11169afe4b1057432a7eaea753b2edb26816b8f160c83b634396d5f9b69763c`.

  Private mode-`0700` `/tmp/lethebot-gov-ux-01c7a-roundtrip-cof4cL` holds the
  six-path ASCII patch
  (`sha256:9c612fdb050a7d7318e8a99c7f9a819c0176883aa4d84e351d2bf11a8fce0bc3`,
  mode `0600`, size `16648`). Independent reverse-first and forward-first zero-
  fuzz cycles restored all 24 proof files byte-for-byte and mode-for-mode, and
  candidate/workspace equality passed 6 / 6. Final hygiene remains 74 tracked
  modifications, 60 untracked paths, and zero staged at `main@cfebb77`; Pi
  remains `0.80.2`, lockfile and migration 007/008 hashes are unchanged, and no
  backend, route, DTO, persistence, schema, dependency, CLI/QQ, Provider,
  private/live, deployment, restart, or container behavior changed.
- `exact_next_action`: completed; the required two-feature Activity reassessment
  selects the behavior-preserving C7B asset compaction prerequisite below before
  adding the B14I Audit browser view.

## Completed Slice: `GOV-UX-01C7B`

- `requirement`: `GOV-UX-01C7B` behavior-preserving fixed Activity asset
  compaction prerequisite.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: C7A completes the eighth read-only Activity workflow but
  leaves only 38 bytes below the permanent 65,536-byte fixed-asset boundary.
  B14I already backs the final default-safe Audit workflow, but its fixed tab,
  validator, and renderer cannot fit without first reclaiming representation
  space.
- `selection_reason`: C6C and C7A are two consecutive Activity feature slices,
  so the full P8 dependency reassessment is due. A separate no-feature
  compaction is the smallest reversible prerequisite: consolidate identical
  fixed bounds and repeated record-line classes, then remove only
  punctuation-delimited JavaScript line breaks that cannot separate tokens or
  trigger automatic semicolon insertion. Preserve every runtime behavior before
  B14I is added.
- `implementation_contract`: replace the seven legacy tool/worker/job constants
  representing the same current limits with fixed shared `MAX_RECORDS = 100`,
  `MAX_TEXT = 256`, and `MAX_DIAGNOSTIC = 512` constants, without changing any
  field, required/optional rule, or accepted bound. Replace only repeated
  `tool-call-primary`, `tool-call-secondary`, and `tool-call-diagnostic` row-line
  literals with fixed in-asset constants; emitted class strings remain exact.

  Extend fixed JavaScript asset compaction only after the existing leading-
  indentation/blank-line compaction: remove raw line terminators immediately
  after `(`, `{`, `[`, comma, or semicolon and immediately before `)`, `}`, `]`,
  comma, or semicolon. Retain every other line terminator. The fixed script has
  no multiline literal, and these delimiters make the removed whitespace
  lexically and ASI-insensitive. Do not rename tokens, rewrite expressions,
  parse or evaluate code, compress transport output, or compact HTML/CSS.

  Preserve byte-identical served HTML and CSS; exact JavaScript semantics,
  globals, endpoints, definitions, accepted payloads, normalizers, renderers,
  DOM tags/attributes/text/classes/order, states, requests, messages, session/
  stale-cancellation behavior, eight-tab keyboard/focus behavior, and responsive
  layout. Add no Audit definition, endpoint, validator, renderer, request,
  feature, authority, asset, lazy loading, external code, or dependency.
- `allowed_paths`: `src/http/governance-browser-assets.ts`,
  `tests/integration/governance-http.test.ts`, `docs/architecture.md`,
  `docs/deployment.md`, `docs/governance-ui.md`, `docs/security-privacy.md`, and
  this checkpoint only.
- `protected_paths`: all C1-C7A visible, request, validation, normalization,
  rendering, DOM, state, session, keyboard/focus, and responsive behavior; B14I
  route/query/DTO/order/limit/redaction and its wiring regression; every other
  Activity/scoped/detail/mutation API; `GovernanceHttpServer`, `src/index.ts`,
  GovernanceService, CLI/QQ, config/env, schema, migrations, dependencies/
  lockfile, Provider, private/live data, deployment/restart/container behavior,
  and every prior slice.
- `assertions`: unchanged named C7A/C6C/fixed-shell/compact characterizations
  reproduce exact 8,641/14,850/42,007-byte assets before editing. Fresh
  desktop/mobile Chromium captures the current complete eight-tab runtime
  structure with SHA-256
  `f11169afe4b1057432a7eaea753b2edb26816b8f160c83b634396d5f9b69763c`.
  One named fail-first headroom regression fails only because 65,498 is not
  below 63,300 bytes. Final HTML/CSS and runtime structures are byte-identical,
  JavaScript parses, every C1-C7A workflow and invalid/stale/session/keyboard/
  responsive matrix remains green, the complete bundle is below 63,300 and
  permanently below 65,536, and no new request, authority, feature, or sink
  appears.
- `focused_commands`: named C7B fail-first/final regression; C7A/C6C/fixed-
  shell/compact and B14I route adjacency; full governance HTTP; four-file HTTP/
  application/wiring adjacency; canonical P8; typecheck; scoped/full ESLint;
  build/preflight; normal-umask release; JavaScript/browser structure and visual
  QA; diff, inventory, dependency/migration/privacy hygiene; and exact six-path
  reverse-first/forward-first rollback proof.
- `schema_dependency_live_boundary`: fixed browser representation, synthetic
  tests, four canonical browser/security documents, and checkpoint evidence
  only. No feature, backend, route, accepted payload, persistence, schema,
  migration, dependency, lockfile, CLI/QQ, Provider, private DB, deployment,
  restart, container, or live operation may change.
- `rollback_boundary`: after this contract and a clean diff check, capture the
  exact C7A bytes and modes for browser source, integration test, four canonical
  documents, and checkpoint under one new private mode-`0700` root. The final
  six-product-path ASCII patch must independently prove reverse-first and
  forward-first exact byte/mode restoration without touching the workspace;
  checkpoint evidence remains outside the patch.
- `baseline_evidence`: the unchanged named C7A/C6C/fixed-shell/compact browser
  characterizations passed 4 / 4 and the B14I route characterization passed
  1 / 1 before implementation. Served HTML/CSS/JavaScript reproduced exact
  8,641/14,850/42,007-byte bodies, 65,498 total, and HTML/JavaScript parsed
  cleanly. Private mode-`0700`
  `/tmp/lethebot-gov-ux-01c7b-baseline-ZYLrUX` captures all seven allowed C7A
  paths byte-for-byte and mode-for-mode. Source/test/architecture/deployment/
  governance/security/checkpoint SHA-256 values are
  `5474aca9a2bca3ed453cbb8308769e5a7d8b20f4993e95ba023778f9c1ba0ad5`,
  `bf0910b83fa7c8bdb2319b8aa7bcaf29c6807e3b54c8dbdfd7a5b7c800f1d345`,
  `82449d1d4bba2e92a079ceb9f633b900531bb490bdce347194dcfe6f13f863e5`,
  `d8c2e44d2e59d4e2b5a59700706a574855e3175ed8ee1b20dfac2da0e2fc722e`,
  `f47395d2dd0d319421129edc4d69489cdd1035f84f927272d1713216c8058d1f`,
  `2a901a6aff1aab6bf256624d09131a5070be111ad39eb77ed93e2b7818ddb0d1`,
  and `7db1f1aa1cd5a4458982af3d5a9f51224990ba088ec779fd66e5f8504927e779`.
  Fresh desktop/mobile Chromium captured identical eight-tab runtime structures
  with SHA-256
  `f11169afe4b1057432a7eaea753b2edb26816b8f160c83b634396d5f9b69763c`
  under `/tmp/lethebot-gov-ux-01c7b-browser-before-nX28FH`.
- `fail_first_evidence`: the new named headroom regression collected the
  complete 27-test governance HTTP file, skipped 26 unrelated cases, and failed
  only because 65,498 was not below 63,300. Its mode-`0600` log
  `/tmp/lethebot-gov-ux-01c7b-fail-first.log` has SHA-256
  `9ea8d08b1bbcae499183beb8272d133864ef47d7ba4609b4799cb3a962bf308f`.
- `final_evidence`: the fixed browser script now shares exact 100/256/512
  bounds and the three emitted row-line class values, then removes only line
  terminators adjacent to the fixed punctuation delimiters after existing
  indentation compaction. Served HTML and CSS remain byte-identical at
  8,641/14,850 bytes; JavaScript is 39,788 bytes, for 63,279 total. This
  reclaims 2,219 bytes, passes the strict prerequisite by 21 bytes, and leaves
  2,257 bytes below the permanent boundary. JavaScript parses cleanly and the
  fixed-definition regressions remain representation-insensitive without
  weakening endpoint, field, or behavior assertions.

  Full governance HTTP, four-file HTTP/application/wiring adjacency, and the
  canonical P8 gate passed 27/73/335 tests. Typecheck, scoped/full ESLint,
  build, preflight, script parsing, and diff hygiene passed. The canonical P8
  log `/tmp/lethebot-gov-ux-01c7b-p8.log` is mode `0600` with SHA-256
  `ef785c1a95c8df5bcc105f39b82e13a043aaf4e1b2451481b9c33e82abc035f6`.
  The complete normal-umask release passed 137 / 1 skipped files and 2,840 / 10
  skipped tests; its mode-`0600` log
  `/tmp/lethebot-gov-ux-01c7b-release-normal.log` has SHA-256
  `7db8e5999d03ac73d67eea0826845cdb8009402e78f562829a17a1069e5fa801`.

  Final system Chromium repeated all 86 exact failure requests and 31 invalid/
  unavailable states per viewport at 1440x900 and 390x844, with unchanged
  eight-tab keyboard, focus, responsive, session, stale-request, and visual
  behavior. No unexpected console, page, failed-request, or external-request
  event occurred. Private mode-`0700`
  `/tmp/lethebot-gov-ux-01c7b-browser-after-naM3Uo` holds the mode-`0600` log
  (`sha256:90bf6c84abb101179af7b14eab30378d9ff77c9c3d956a40a4c15d2a784b94cb`),
  desktop/mobile screenshots
  (`sha256:48b5195b37eca73e1fac7d41a805a78383fc9dcd26d2767ac6cdba5a910f372b`/
  `bdb2a9b90e0fbb8072d791b834974a98d6c670aed06152baf928d876f6efb9ec`),
  and byte-identical pre/post runtime structures with SHA-256
  `f11169afe4b1057432a7eaea753b2edb26816b8f160c83b634396d5f9b69763c`.

  Private mode-`0700`
  `/tmp/lethebot-gov-ux-01c7b-roundtrip-v2-pO9rWR` holds the accepted six-path
  ASCII patch
  (`sha256:d4763ac687a8dbd0200e6390bc8f45e7c5fa389a166094e07605796b9a1bf1d6`,
  mode `0600`, size `26796`). Independent reverse-first and forward-first
  zero-fuzz cycles restored all 24 proof files byte-for-byte and mode-for-mode,
  and candidate/workspace equality passed 6 / 6. Final hygiene remains 74
  tracked modifications, 60 untracked paths, and zero staged at
  `main@cfebb77`; Pi remains `0.80.2`, lockfile and migration 007/008 hashes are
  unchanged, and no Audit tab, feature, backend, route, DTO, persistence,
  schema, dependency, CLI/QQ, Provider, private/live, deployment, restart, or
  container behavior changed.
- `exact_next_action`: completed; add the B14I Audit browser view as
  `GOV-UX-01C7C` against this compact eight-tab candidate.

## Completed Slice: `GOV-UX-01C7C`

- `requirement`: `GOV-UX-01C7C` read-only Audit Activity browser view.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: authenticated B14I already exposes the bounded, newest-first,
  details-hidden, recursively redacted default Audit list at
  `/governance/api/v1/activity/audit`, while the browser stops at the paired
  Event processing failures view and cannot inspect the corresponding event,
  actor, risk, summary, or explicit redaction evidence.
- `selection_reason`: this is the final already-backed default-safe Activity
  table and completes the B14H/B14I failure/audit pair. C7B leaves 2,257 bytes
  under the permanent fixed-asset boundary, so one compact definition, exact
  validator, and safe renderer over existing primitives is the smallest
  dependency-ordered slice.
- `implementation_contract`: preserve the first eight tab positions and Model
  invocations as default, then add Audit after Event processing failures. Call
  only authenticated same-origin `GET /governance/api/v1/activity/audit`.
  Accept at most 100 exact B14I records. Require non-empty `id`, `category`,
  `level`, `eventType`, and `eventId` strings bounded to 256 UTF-16 code units,
  canonical ISO `timestamp`, a non-empty `summary` of at most 512 units,
  exact required booleans `detailsRedacted` and `redacted`, and an exact `actor`
  object. Actor `canonicalUserId`, `actorClass`, and `context`, plus top-level
  `riskLevel` and `evaluatorDecisionId`, are optional non-empty strings bounded
  to 256 units. Reject unknown keys, `details`, unknown/nested actor fields,
  malformed values, and oversized arrays before replacing rows.

  Render only the already-redacted audit reference and time, category/level,
  event type/reference, optional actor fields, bounded summary, optional risk/
  evaluator reference, and explicit details-hidden/redacted signals through the
  existing safe cells, DOM construction, and `textContent`. Reuse the shell/
  controller, loading/empty/error/content, stale cancellation, expiry/logout,
  roving tabs, focus, mobile labels, and responsive table. Add no filter,
  caller limit, scope/CSRF header, body, polling, details access, detail link,
  inference, mutation, backend route/DTO, new CSS token, external asset, or
  dependency, and keep the complete bundle below 65,536 bytes.
- `allowed_paths`: `src/http/governance-browser-assets.ts`,
  `tests/integration/governance-http.test.ts`, `docs/architecture.md`,
  `docs/deployment.md`, `docs/governance-ui.md`, `docs/security-privacy.md`, and
  this checkpoint only.
- `protected_paths`: C1-C7B browser behavior and compaction; B14I route/query/
  DTO/order/limit/redaction and wiring regression; every other Activity/scoped/
  detail/mutation API; `GovernanceHttpServer`, `src/index.ts`,
  GovernanceService, CLI/QQ, config/env, schema, migrations, dependencies/
  lockfile, Provider, private/live data, deployment/restart/container behavior,
  and every prior slice.
- `assertions`: unchanged named C7B/C7A/C6C/fixed-shell/compact and B14I route
  characterizations reproduce exact 8,641/14,850/39,788-byte assets before
  editing. A named fail-first sees the complete eight-tab browser and B14I
  backend but no Audit tab or endpoint reference. Final coverage proves exact
  top-level and actor keys, required/optional bounds, canonical dates,
  `details`/unknown/nested/malformed/oversized rejection without row
  replacement, populated/optional/empty/error/stale/expiry/logout states, exact
  request shape, nine-tab keyboard order, mobile labels, no overflow, and a
  bundle below 65,536.
- `focused_commands`: named C7C fail-first/final; C7B/C7A/C6C/fixed-shell/
  compact and B14I route adjacency; full governance HTTP; four-file HTTP/
  application/wiring adjacency; canonical P8; typecheck; scoped/full ESLint;
  build/preflight; normal-umask release; JavaScript/browser QA; diff, inventory,
  dependency/migration/privacy hygiene; and exact six-path reverse-first/
  forward-first rollback proof.
- `schema_dependency_live_boundary`: fixed browser assets, synthetic tests,
  four canonical browser/security documents, and checkpoint evidence only. No
  backend, route, persistence, schema, migration, dependency, lockfile, CLI/QQ,
  Provider, private DB, deployment, restart, container, or live operation.
- `rollback_boundary`: after this contract and a clean diff check, capture the
  exact C7B bytes and modes for browser source, integration test, four canonical
  documents, and checkpoint under one new private mode-`0700` root. The final
  six-product-path ASCII patch must prove independent reverse-first and forward-
  first exact byte/mode restoration without touching the workspace; checkpoint
  remains outside the patch.
- `baseline_evidence`: the unchanged named C7B/C7A/C6C/fixed-shell/compact
  browser characterizations passed 5 / 5 and the B14I route characterization
  passed 1 / 1 before implementation. Served HTML/CSS/JavaScript reproduced
  exact 8,641/14,850/39,788-byte bodies, 63,279 total, and HTML/JavaScript
  parsed cleanly. Private mode-`0700`
  `/tmp/lethebot-gov-ux-01c7c-baseline-BrNul6` captures all seven allowed C7B
  paths byte-for-byte and mode-for-mode. Source/test/architecture/deployment/
  governance/security/checkpoint SHA-256 values are
  `145697041a1eda32853bfa311c71869034c56d9a63afdb9b7037e42379399d80`,
  `2599f3350e4c702b3d4c0fe4c7944bc0a72404068dd27292241df33896470aee`,
  `0b8c985c8cb56a63d6898860f9c68d1f4d90bd19e0262428bddb6149bd904e44`,
  `95621f46869a74174467f8fe95c9fc4d7e1a410cc9958d05d3eba8039cd91f51`,
  `ff0a6c73ca661402f5313bb72a7ddbbf0c90ce39b1aa2eb7dcfb0763eefa8604`,
  `0b8d86aba42bd2128436ffabec9f8b78a994d605e721927a855bc4bda8a144ae`,
  and `304c56ab2571edceb69c3c72bad4e652ee73bea0dca9cd37c410770ada9c2108`.
- `fail_first_evidence`: the named browser test collected the complete 28-test
  governance HTTP file, skipped 27 unrelated cases, and failed only because the
  fixed `audit` / `audit` / `Audit` definition was absent. Its mode-`0600` log
  `/tmp/lethebot-gov-ux-01c7c-fail-first.log` has SHA-256
  `2d3adbc4f480534fc2c25a0516397550d94f939f6ab29c030d92385a0f773fea`.
- `final_evidence`: the ninth tab calls only B14I's exact same-origin default
  GET. Its exact-key validator accepts at most 100 bounded records with a
  canonical timestamp, exact optional-only actor object, required redaction
  booleans, and no details; invalid responses do not replace existing rows.
  The safe renderer shows only the redacted audit/time, category/level, event,
  optional actor/risk/evaluator evidence, bounded summary, and explicit
  details-hidden/redacted signals. Final served HTML/CSS/JavaScript are
  8,641/14,850/41,778 bytes, 65,269 total with 267 bytes below the permanent
  boundary.

  The focused C7C regression, full governance HTTP, four-file HTTP/application/
  wiring adjacency, and canonical P8 gates passed 1/28/74/336 tests. Typecheck,
  scoped/full ESLint, build, preflight, script parsing, and diff hygiene passed.
  The canonical P8 log `/tmp/lethebot-gov-ux-01c7c-p8.log` is mode `0600` with
  SHA-256
  `eec1c346bd8ae70247444c1ddf4577ca3368247db8efc85d76b8e841d647c558`.
  The first aggregate release attempt is excluded because its restrictive
  process umask changed the deliberately permissive restore-handoff fixture;
  the unchanged isolated suite passed 24 / 24 under normal umask `022`. The
  corrected complete normal-umask release passed 137 / 1 skipped files and
  2,841 / 10 skipped tests. Its mode-`0600` log
  `/tmp/lethebot-gov-ux-01c7c-release.log` has SHA-256
  `b78cce50e4dcdcbd21d9c2d69cd736aafbbb8553a4179d5ddc01be0eadb05e96`.

  Final system Chromium exercised 45 invalid/unavailable states per viewport
  at 1440x900 and 390x844, made 114 exact Audit requests, and preserved
  nine-tab keyboard/focus, long-field overflow, no-replacement, stale-request,
  expiry, logout, and exact-header behavior. No unexpected console, page,
  failed-request, or external-request event occurred. Private mode-`0700`
  `/tmp/lethebot-gov-ux-01c7c-browser-final-kGW5Hp` holds the mode-`0600` log
  (`sha256:9a8d2528b2c1ef736733a90a44de51b543437af57820d68da8727b236e264eb6`),
  desktop/mobile screenshots
  (`sha256:69a8820c18d992fde63522bc95821606c61cff4620be4b734acac4a4abf95f14`/
  `0ba7518f032ea9081b66e204b7686ec21f924fbe34d29a26f7672f1fb549c0c4`),
  and byte-identical runtime structures with SHA-256
  `18fc451df066c3b27e333dcee2106ea762918cfc29b45075edab17a0465db4c1`.

  Private mode-`0700`
  `/tmp/lethebot-gov-ux-01c7c-roundtrip-JhULjJ` holds the accepted six-path
  two-context-line ASCII patch
  (`sha256:d4997d6baefa73849ae4d319a65a99a406a5225bb2334f5427d51095512433e1`,
  mode `0600`, size `14227`). Independent reverse-first and forward-first
  zero-fuzz cycles restored all 24 proof files byte-for-byte and mode-for-mode,
  every proof directory is mode `0700`, and candidate/workspace equality passed
  6 / 6. Two earlier setup roots stopped before patch creation, and one complete
  byte cycle was rejected before acceptance because default diff context copied
  an existing non-ASCII heading and intermediate directories were not mode
  `0700`; none is cited as evidence.

  Final hygiene remains 74 tracked modifications, 60 untracked paths, and zero
  staged at `main@cfebb77`; Pi dependencies remain `0.80.2`, lockfile SHA-256 is
  `f727570a279194362333b938822a2c7699853b1f7977c68e064fae8e8444fc17`,
  and migration 007/008 SHA-256 values remain
  `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`/
  `95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`.
  No backend, route, DTO, persistence, schema, dependency, lockfile, CLI/QQ,
  Provider, private/live, deployment, restart, or container behavior changed.
- `exact_next_action`: completed; the post-Activity P8 reassessment selects
  `GOV-UX-01C8A`, a behavior-preserving local Activity JavaScript module
  extraction prerequisite before the first scoped Memory browser workflow.

## Completed Slice: `GOV-UX-01C8A`

- `requirement`: `GOV-UX-01C8A` behavior-preserving fixed Activity JavaScript
  module extraction prerequisite.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: C7C completes every default-safe Activity view but leaves only
  267 bytes below the 65,536-byte fixed-shell boundary. The next priority is the
  scoped Memory review/record workflow, which needs scope discovery, selection,
  bounded list states, and later detail/mutation controls and cannot fit safely
  in the current monolithic script.
- `selection_reason`: the complete default-safe Activity surface makes a full
  P8 dependency reassessment due. The canonical dependency strategy already
  selects small same-origin local ES modules. Extracting only the now-stable
  Activity projection code into one static module is the smallest reversible
  prerequisite: it creates material shell headroom without mixing a scoped
  feature, authority, dependency, or backend change into the refactor.
- `implementation_contract`: preserve byte-identical served HTML and CSS. Add
  only exact static GET `/governance/activity.js` to the existing fixed asset
  map, and statically import its explicit Activity exports from `app.js`. The
  Activity module owns the fixed Activity endpoint definitions, strict payload
  normalizers, date/value formatting, safe row construction, and Model/list
  renderers. The shell retains session/CSRF state, login/logout, same-origin
  request transport, Overview, navigation, controller request sequences,
  stale cancellation, loading/error/empty/content transitions, listeners, and
  focus behavior.

  Preserve every endpoint, request/header, accepted/rejected payload, rendered
  DOM tag/attribute/text/class/order, visible state, announcement, tab order,
  session transition, responsive result, and C1-C7C runtime structure. Use one
  static import, not dynamic loading or code evaluation; add no feature, route
  authority, scope/resource/preview access, HTML/CSS token, external asset,
  build step, package, or dependency. Keep each fixed asset below 65,536 bytes,
  keep the HTML/CSS/`app.js` shell below 50,000 bytes, and avoid duplicated
  Activity definitions across the two modules.
- `allowed_paths`: `src/http/governance-browser-assets.ts`,
  `tests/integration/governance-http.test.ts`, `docs/architecture.md`,
  `docs/deployment.md`, `docs/governance-ui.md`, `docs/security-privacy.md`, and
  this checkpoint only.
- `protected_paths`: all C1-C7C visible, validation, rendering, request,
  controller, session, keyboard/focus, accessibility, and responsive behavior;
  every backend route/query/DTO and scoped/detail/mutation workflow;
  `GovernanceHttpServer`, `src/index.ts`, GovernanceService, CLI/QQ, config/env,
  schema, migrations, dependencies/lockfile, Provider, private/live data,
  deployment/restart/container behavior, and every prior slice.
- `assertions`: unchanged named C7C/C7B/C7A/C6C/fixed-shell/compact
  characterizations reproduce exact 8,641/14,850/41,778-byte assets and the
  complete C7C browser structure hash before editing. A named fail-first
  requires the absent exact `/governance/activity.js` asset, explicit static
  import/export boundary, per-asset bound, sub-50,000-byte shell, and no
  duplicated endpoint/normalizer/renderer definitions. Final HTML/CSS are
  byte-identical; both JavaScript modules parse; direct module and browser
  behavior cover every C1-C7C valid/invalid/no-replacement/stale/session/
  keyboard/responsive state; desktop/mobile runtime structures remain byte-
  identical to C7C; the new static request is exact and no other network,
  authority, sink, or feature appears.
- `focused_commands`: named C8A fail-first/final; unchanged C7C/C7B/C7A/C6C/
  fixed-shell/compact and all B14 route adjacency; full governance HTTP; four-
  file HTTP/application/wiring adjacency; canonical P8; typecheck; scoped/full
  ESLint; build/preflight; normal-umask release; module parsing and complete
  desktop/mobile C7C browser oracle; diff, inventory, dependency/migration/
  privacy hygiene; and exact six-path reverse-first/forward-first rollback
  proof.
- `schema_dependency_live_boundary`: fixed browser module ownership, synthetic
  tests, four canonical browser/security documents, and checkpoint evidence
  only. No visible feature, backend authority, persistence, schema, migration,
  dependency, lockfile, CLI/QQ, Provider, private DB, deployment, restart,
  container, or live operation.
- `rollback_boundary`: after this contract and a clean diff check, capture the
  exact C7C bytes and modes for browser source, integration test, four canonical
  documents, and checkpoint under one new private mode-`0700` root. The final
  six-product-path ASCII patch must independently prove reverse-first and
  forward-first exact byte/mode restoration without touching the workspace;
  checkpoint remains outside the patch.
- `baseline_evidence`: private mode-`0700`
  `/tmp/lethebot-gov-ux-01c8a-baseline-YuT6BD` captured all seven allowed paths
  at mode `0644`. The unchanged C7C/C7B/C7A/C6C/fixed-shell/compact
  characterization passed 6 / 6
  (`/tmp/lethebot-gov-ux-01c8a-characterization.log`, mode `0600`,
  `sha256:483786ff4ac1b52d9c9e6b4f29ffb43014dd19fafbcedc25b04e61729d0cfcce`).
  The served HTML/CSS/JavaScript measured 8,641/14,850/41,778 bytes
  (`/tmp/lethebot-gov-ux-01c8a-assets.log`, mode `0600`,
  `sha256:7f9b5577fddfd73220a4479c057c6cde8ec31845a6f6afb6e36a4385a8793e44`),
  parsed successfully, and the fresh desktop/mobile C7C baseline under
  `/tmp/lethebot-gov-ux-01c8a-browser-baseline-HqS3S1` reproduced runtime-
  structure SHA-256
  `18fc451df066c3b27e333dcee2106ea762918cfc29b45075edab17a0465db4c1`.
- `fail_first_evidence`: the named boundary regression failed alone because
  exact static `/governance/activity.js` returned `404` instead of `200`, while
  its structure and ownership assertions were already collected
  (`/tmp/lethebot-gov-ux-01c8a-fail-first.log`, mode `0600`,
  `sha256:5ca3df6c539b250d7aa03d477579989d6b63a2ea6fa4e8be21117542b4e64421`).
- `final_evidence`: the named C8A regression passed 1 / 1
  (`sha256:310d4f3d7ce67f997c439cc4dd0b7ea7144b806c6045c4ab002adb05d9bb4350`),
  full governance HTTP passed 29 / 29
  (`sha256:fa1a777e054f5d8692bf33a01a4de17057e708c64aa1eef562cd4761f85da9eb`),
  four-file HTTP/application/wiring adjacency passed 75 / 75
  (`sha256:770e051562b9d6b9127e52054709c87bc23013eead216023e95fa03018fc8744`),
  and canonical P8 passed 337 / 337
  (`sha256:2b807d0f1dae4b0afab488eae8e2c8311c2f59ba44fe058d8320a2d225e32726`);
  all cited logs are mode `0600`. Typecheck, scoped and full ESLint, build,
  preflight, both JavaScript parsers, and `git diff --check` passed. Fresh
  normal-umask release passed 137 / 1 skipped files with 2,842 / 10 skipped
  tests (`/tmp/lethebot-gov-ux-01c8a-release.log`, mode `0600`,
  `sha256:f8154c43b7488e29e32f005bd35a781c75d8e1f1865b0446de6234f81c3ebd65`).

  Exact static `/governance/activity.js` now owns the shared Activity prefix,
  endpoint definitions, validators, normalizers, and renderers; `app.js`
  statically imports `createActivityFeature` and retains session, transport,
  Overview, navigation, controller, stale-request, listener, and focus
  ownership. HTML/CSS remain byte-identical. Final HTML/CSS/`app.js`/
  `activity.js` sizes are 8,641/14,850/18,380/23,616 bytes; the fixed shell is
  41,871 bytes and all four assets total 65,487 bytes. No visible behavior,
  request, authority, dependency, or backend behavior changed.

  Private mode-`0700`
  `/tmp/lethebot-gov-ux-01c8a-browser-final-v2-MCD5K9` records 72 requests and
  45 invalid states per viewport, 114 exact Audit requests, zero unexpected
  console/page/request/external failures, and byte-identical desktop/mobile
  runtime structures with the C7C SHA-256 above. The mode-`0600` browser log,
  desktop screenshot, and mobile screenshot have SHA-256
  `4738cb0867842d5e58fcd95db7dbcf9287895672e900a02d3c1c1ce0dd689209`,
  `c79ddf41e016df8664455e2a43d7e76fcd8933e197c09194954e8c3cad1034cb`,
  and `f0c87c84e8462a21921e16be2d13f4d2c2d3fdd8c71055634bfb23025da88ae0`;
  visual review found no overlap, clipping, responsive, focus, or state defect.

  Private mode-`0700`
  `/tmp/lethebot-gov-ux-01c8a-roundtrip-5Tqkf1` holds the accepted six-path
  ASCII patch (`sha256:cdbb10a88cfd8c5cd0baf57c3872edccbc0e13b76cc38701d8189861f3e476ee`,
  mode `0600`, size `70438`). Independent reverse-first and forward-first
  zero-fuzz cycles restored all 24 proof files byte-for-byte and mode-for-mode;
  candidate/workspace equality passed 6 / 6 and every proof-directory mode
  check passed. Final hygiene remains 74 tracked modifications, 60 untracked
  paths, and zero staged at `main@cfebb77`; both installed Pi packages remain
  `0.80.2`, lockfile SHA-256 remains
  `f727570a279194362333b938822a2c7699853b1f7977c68e064fae8e8444fc17`, and
  migration 007/008 SHA-256 values remain
  `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`/
  `95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`.
- `exact_next_action`: completed; the post-C8A reassessment selects
  `GOV-UX-01C8B`, the first exact-scope read-only Memory Records browser view.

## Completed Slice: `GOV-UX-01C8B`

- `requirement`: `GOV-UX-01C8B` first exact-scope read-only Memory Records
  browser destination.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: C8A provides safe module headroom and the browser has complete
  default-safe Overview and Activity surfaces, but it has no Memory destination,
  persistent exact-scope selector, or bounded record list despite the complete
  B16B/B17C read-only scope and record authority.
- `selection_reason`: operator task priority places maintenance review before
  record inspection, while the canonical information architecture explicitly
  makes Records the Memory default. C8B deliberately follows that default and
  establishes the destination plus exact-scope interaction first. Maintenance
  review follows as a secondary Memory view because its `/scopes` catalog and
  `memory.maintenance.review` handles are purpose-isolated from B16B Memory-
  record handles; combining or reusing those authorities would obscure the
  boundary and exceed one reversible slice.
- `implementation_contract`: add only exact static GET
  `/governance/memory.js`, statically imported by `app.js`. Add Memory between
  Overview and Activity in primary navigation and make Records its only and
  therefore default view. The unframed Memory view contains one labelled scope
  selector, a refresh command, explicit catalog/selection/record states, and a
  responsive bounded record table. Fetch exact same-origin
  `/governance/api/v1/memory/scopes` only after entering Memory or refreshing;
  send no query, body, CSRF value, or scope header. Require an explicit operator
  selection before fetching exact `/governance/api/v1/memory/records`, and send
  only the selected opaque handle in `X-LetheBot-Scope`.

  The Memory module owns the fixed endpoints, exact catalog and record schemas,
  accepted domains/bounds, option labels, record cells, and safe renderers. The
  shell retains session/CSRF state, request transport, navigation, view and
  request-sequence cancellation, expiry/logout handling, announcements, and
  route-change focus. Preserve a still-valid selection by fingerprint across
  primary-navigation changes and refresh; never persist a handle outside
  process memory. Reject unknown keys, malformed/duplicate entries, over-100
  arrays, invalid booleans/numbers/dates/enums, mismatched conversation fields,
  malformed fingerprints or opaque handles, and unrestricted fields before
  replacing prior content. Render only the fixed redacted title/content preview,
  record reference, classifications, lifecycle state, confidence/importance,
  source/revision counts, dates, and truncation/redaction/hidden signals through
  DOM creation and `textContent`; keep resource handles internal and add no
  record detail control.

  Add no maintenance Review queue, record detail, preview, confirmation,
  mutation, filter, search, caller limit, polling, raw selector, dynamic import,
  external asset, build step, package, dependency, backend route, or authority.
  Preserve all C1-C8A HTML/CSS/Activity behavior and keep every individual asset
  below 65,536 bytes and the HTML/CSS/`app.js` shell below 50,000 bytes.
- `allowed_paths`: `src/http/governance-browser-assets.ts`,
  `tests/integration/governance-http.test.ts`, `docs/architecture.md`,
  `docs/deployment.md`, `docs/governance-ui.md`, `docs/security-privacy.md`, and
  this checkpoint only.
- `protected_paths`: all C1-C8A Overview/Activity validation, rendering,
  request, session, keyboard/focus, accessibility, and responsive behavior;
  B16B/B17C routes, DTOs, ordering, redaction, scope/resource registries, and
  purpose isolation; Memory detail and every mutation; maintenance/Privacy/
  Explain/Operations browser work; `GovernanceHttpServer`, `src/index.ts`,
  GovernanceService/query/repository code, CLI/QQ, config/env, schema,
  migrations, dependencies/lockfile, Provider, private/live data, deployment,
  restart, container behavior, and every prior slice.
- `assertions`: a named fail-first proves the absent Memory module, navigation,
  destination, exact endpoints, selector, and strict schema boundary. Final
  behavior covers catalog loading, populated, truncated, empty, malformed,
  unavailable, stale, expiry, and logout states; explicit selection and refresh;
  bounded record loading, populated, truncated, empty, malformed, unavailable,
  stale, and no-replacement states; selected-scope preservation; navigation and
  focus; desktop/mobile reflow; and exact request headers. A catalog request has
  no scope header, a record request has exactly the selected handle, handles
  never enter text/attributes/storage/logs, and no detail or mutation request is
  emitted. Existing Overview/Activity requests, DOM structures, tab behavior,
  and screenshots remain unchanged except for the new Memory navigation item.
- `focused_commands`: unchanged C8A/C7C/fixed-shell characterizations; named
  C8B fail-first/final; full governance HTTP; four-file HTTP/application/wiring
  adjacency; canonical P8; typecheck; scoped/full ESLint; build/preflight;
  normal-umask release; direct module parsing and complete synthetic desktop/
  mobile browser oracle; diff, inventory, dependency/migration/privacy hygiene;
  and exact six-path reverse-first/forward-first rollback proof.
- `schema_dependency_live_boundary`: fixed browser Memory presentation,
  synthetic tests, four canonical browser/security documents, and checkpoint
  evidence only. No backend authority, persistence, schema, migration,
  dependency, lockfile, CLI/QQ, Provider, private DB, deployment, restart,
  container, or live operation.
- `rollback_boundary`: after this contract and a clean diff check, capture the
  exact C8A bytes and modes for browser source, integration test, four canonical
  documents, and checkpoint under one new private mode-`0700` root. The final
  six-product-path ASCII patch must independently prove reverse-first and
  forward-first exact byte/mode restoration without touching the workspace;
  checkpoint remains outside the patch.
- `baseline_evidence`: private mode-`0700`
  `/tmp/lethebot-gov-ux-01c8b-baseline-NL60mQ` captured the seven allowed paths.
  The unchanged C8A/C7C/fixed-shell characterization and fresh Activity browser
  baseline passed; private mode-`0700`
  `/tmp/lethebot-gov-ux-01c8b-browser-baseline-wX67UX` reproduced desktop and
  mobile Activity runtime-structure SHA-256
  `18fc451df066c3b27e333dcee2106ea762918cfc29b45075edab17a0465db4c1`.
  The characterization log is mode `0600` with SHA-256
  `b5b3642934fc4e740e3e1b3516f8873bee8ac99e8d6a58078672d95f39a60898`.
- `fail_first_evidence`: the named C8B boundary regression failed alone because
  exact static `/governance/memory.js` returned `404`; every other structural
  assertion was collected before production edits. Mode-`0600`
  `/tmp/lethebot-gov-ux-01c8b-fail-first.log` has SHA-256
  `01ac3d7c394f439dfea786eae130dc7620e9e8bc6b8adccd7ccb854ca77b416d`.
- `final_evidence`: the focused C8B regression passed 1 / 1, full governance
  HTTP passed 30 / 30, four-file HTTP/application/wiring adjacency passed
  76 / 76, and canonical P8 passed 338 / 338. Their mode-`0600` logs have
  SHA-256 `b0ea4004823e19370ad776b17aa19fff349ce5353401a2b668963d2b96970fcb`,
  `92065ac5d70ad9055980fdb28ae266ca975334b26b0c82ac9fb47e090b643a3f`,
  `75867dd64f94434da7a205f84b50301ad9d4bc70414ed0fbab932c5b339be804`,
  and `00da0237aaf96fb3a2fbeea541b7792e57aa46f462b99086a68a4425acdd33b7`.
  Typecheck, scoped/full ESLint, build, preflight, all three JavaScript parsers,
  and `git diff --check` passed. Fresh normal-umask release passed 137 / 1
  skipped files with 2,843 / 10 skipped tests; mode-`0600`
  `/tmp/lethebot-gov-ux-01c8b-release.log` has SHA-256
  `d6bbd90020e70b18834cdac20a0c54e58f27b1179224a1612bdc81a5ff9a7775`.

  Exact static `/governance/memory.js` now owns the strict catalog/record
  schemas and safe Memory DOM construction. Memory sits between Overview and
  Activity; Records is the default, and no record request occurs before an
  explicit exact-scope selection. The catalog request carries no scope header;
  each record request carries only the selected current-session opaque handle
  in `X-LetheBot-Scope`. Handles remain in JavaScript memory and never enter
  DOM text/attributes, storage, or logs. Final HTML/CSS/`app.js`/`activity.js`/
  `memory.js` sizes are 8,641/16,415/20,939/23,616/16,720 bytes; the fixed
  shell is 45,995 bytes and every asset remains below 65,536 bytes.

  Final Chromium at 1440x900 and 390x844 exercised 18 invalid catalog states
  and 53 invalid record states per viewport, with 157 requests per viewport,
  162 total scope requests, and 124 total record requests. Private mode-`0700`
  `/tmp/lethebot-gov-ux-01c8b-browser-final-6L5ypJ` contains the mode-`0600`
  browser log (`sha256:bb7669ef9a620771435fe6913eb8ddc6848e056880a58a0c86dc341f8c7033b6`)
  and clean visually reviewed desktop/mobile screenshots with SHA-256
  `5a086254268f1e153382643e9554f626c547bb088fc4b24dd35f0e7c72e79497`/
  `0b5b905d477350146909a9f56dc6c676a39901a1072a88ae96a5bff3b14b55e7`.
  A separate Activity regression under private mode-`0700`
  `/tmp/lethebot-gov-ux-01c8b-activity-regression-QhrczO` repeated 73 requests
  per viewport and 114 Audit requests while retaining the exact C8A Activity
  structure hash above.

  Private mode-`0700`
  `/tmp/lethebot-gov-ux-01c8b-roundtrip-uZEkD2` holds the accepted six-path
  ASCII patch (`sha256:3bf2bfaa2fc6da0a856ebac335eeb6ff8a7c77154bab64f3100a80f8e1294b80`,
  mode `0600`, size `36758`). Independent reverse-first and forward-first
  zero-fuzz cycles passed 6 / 6, candidate/workspace equality passed 6 / 6,
  and all 24 proof files plus directory/file modes passed. Final hygiene remains
  74 tracked modifications, 60 untracked paths, and zero staged at
  `main@cfebb77`; Pi packages remain `0.80.2`, lockfile SHA-256 remains
  `f727570a279194362333b938822a2c7699853b1f7977c68e064fae8e8444fc17`,
  and migration 007/008 SHA-256 values remain
  `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`/
  `95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`.
  No detail, mutation, backend, persistence, schema, migration, dependency,
  lockfile, CLI/QQ, Provider, private/live, deployment, restart, or container
  behavior changed.
- `exact_next_action`: completed; the post-C8B reassessment selects
  `GOV-UX-01C8C`, the read-only purpose-isolated Memory Review queue.

## Completed Slice: `GOV-UX-01C8C`

- `requirement`: `GOV-UX-01C8C` read-only purpose-isolated Memory Review queue
  as the secondary Memory view.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: C8B makes exact-scope Memory Records usable in the browser,
  but operators still cannot inspect the already bounded B6B maintenance-review
  queue. Its scope catalog and review list use a different purpose from Records,
  so the existing Records selection and handles are not valid authority for it.
- `selection_reason`: maintenance review is the highest-priority remaining
  Memory task and its bounded read-only list already exists. Adding only the
  secondary view completes queue visibility while keeping review detail and all
  proposal/record mutations in later independently reviewable slices.
- `implementation_contract`: add a two-tab Memory subview with Records first and
  selected by default and Review second. Preserve the entire C8B Records DOM,
  schema, request, selection, refresh, and handle behavior. The Review view owns
  a separate labelled exact-scope selector and explicit catalog, selection,
  loading, populated, truncated, empty, malformed, unavailable, and stale
  states. Fetch exact same-origin `/governance/api/v1/scopes` only when Review
  is selected or refreshed, without query, body, CSRF value, or scope header.
  Require explicit Review-scope selection before fetching exact same-origin
  `/governance/api/v1/memory-reviews`; send only the selected Review opaque
  handle in `X-LetheBot-Scope`.

  Keep per-purpose catalog arrays, selected fingerprints, selected opaque
  handles, and request sequences independent. Never use or accept a
  `governance.memory.records.read` handle for `memory.maintenance.review`, and
  never infer Review selection from Records selection. Preserve a still-valid
  Review selection by fingerprint across Review refresh and primary-navigation
  changes, while fresh Memory entry still presents Records. Handles remain
  only in JavaScript memory and never enter DOM text/attributes, browser
  storage, or logs.

  Validate exact bounded Review payloads before replacing prior rows: reject
  unknown keys, malformed/duplicate/over-100 entries, invalid booleans,
  numbers, canonical dates, enums, references, fingerprints, reason codes, or
  opaque handles; reject scope-kind mismatches and inconsistent revision/count
  values. Render only proposal reference, kind, effect type, lifecycle state,
  scope kind, candidate fingerprint/count, confidence, reason codes, revision
  count/current revision, created/updated/optional expiry dates, and resource
  handle expiry. Keep resource handles internal. Use DOM construction and
  `textContent`; add no raw identifier or unrestricted field.

  Add no review detail, approve, reject, expire, apply, rollback, preview,
  confirmation, mutation, filtering, search, polling, caller limit, dynamic
  import, backend route, authority, package, dependency, or build step. Preserve
  all C1-C8B Overview/Activity/Memory Records behavior and keep every asset
  below 65,536 bytes and the HTML/CSS/`app.js` shell below 50,000 bytes.
- `allowed_paths`: `src/http/governance-browser-assets.ts`,
  `tests/integration/governance-http.test.ts`, `docs/architecture.md`,
  `docs/deployment.md`, `docs/governance-ui.md`, `docs/security-privacy.md`, and
  this checkpoint only.
- `protected_paths`: all C1-C8B Overview/Activity/Memory Records validation,
  rendering, requests, session, keyboard/focus, accessibility, responsive, and
  purpose-isolation behavior; all B6B/B6C and B16B/B17C routes, DTOs, ordering,
  registries, and authority; every review detail/preview/confirmation/mutation;
  all other browser workflows; `GovernanceHttpServer`, `src/index.ts`, service,
  query, repository, CLI/QQ, config/env, schema, migrations, dependencies,
  lockfile, Provider, private/live data, deployment/restart/container behavior,
  and every prior slice.
- `assertions`: a named fail-first proves the absent Review tab, separate
  selector/states, exact endpoints, schema boundary, and independent controller
  state. Final direct/browser behavior covers both catalogs and lists across
  loading, populated, truncated, empty, malformed, unavailable, stale, expiry,
  logout, refresh, retained selection, navigation, two-tab keyboard/focus, and
  desktop/mobile reflow. Catalog requests have no scope header; each Records or
  Review request has exactly its own selected handle; cross-purpose reuse is
  absent; handles never enter DOM/storage/logs; and no detail or mutation request
  is emitted. Existing Activity runtime structure and all C8B Records behavior
  remain exact.
- `focused_commands`: unchanged C8B/C8A/C7C/fixed-shell characterizations;
  named C8C fail-first/final; full governance HTTP; four-file HTTP/application/
  wiring adjacency; canonical P8; typecheck; scoped/full ESLint; build/preflight;
  normal-umask release; all JavaScript parsers and complete synthetic desktop/
  mobile Records/Review plus Activity regression oracles; diff, inventory,
  dependency/migration/privacy hygiene; and exact six-path reverse-first/
  forward-first rollback proof.
- `schema_dependency_live_boundary`: fixed browser Review presentation,
  synthetic tests, four canonical browser/security documents, and checkpoint
  evidence only. No backend authority, persistence, schema, migration,
  dependency, lockfile, CLI/QQ, Provider, private DB, deployment, restart,
  container, or live operation.
- `rollback_boundary`: after this contract and a clean diff check, capture the
  exact C8B bytes and modes for browser source, integration test, four canonical
  documents, and checkpoint under one new private mode-`0700` root. The final
  six-product-path ASCII patch must independently prove reverse-first and
  forward-first exact byte/mode restoration without touching the workspace;
  checkpoint remains outside the patch.
- `baseline_evidence`: private mode-`0700`
  `/tmp/lethebot-gov-ux-01c8c-baseline-To1uK8` captured all seven allowed paths
  at mode `0644`. The unchanged C8B/C8A/C7C/fixed-shell characterization passed
  4 / 4; mode-`0600`
  `/tmp/lethebot-gov-ux-01c8c-characterization.log` has SHA-256
  `ec9f317f756a32998d498e70a8ac6973da66d728b542feb36964609676dffc35`.
- `fail_first_evidence`: the named C8C regression failed alone because the
  Review scope endpoint definition was absent; the remaining boundary evidence
  was collected before product edits. Mode-`0600`
  `/tmp/lethebot-gov-ux-01c8c-fail-first.log` has SHA-256
  `52c8ae23fbdd299ee777dfd1c3f62ea8527a4e48dd91ffa22f0c04b9a4112b54`.
- `final_evidence`: the focused C8C regression passed 1 / 1, full governance
  HTTP passed 31 / 31, four-file HTTP/application/wiring adjacency passed
  77 / 77, and canonical P8 passed 339 / 339. Their mode-`0600` logs have
  SHA-256 `22c8779bfeea9139417560bf8293cdeacb113b1aa863849bb20e18e6159bde6f`,
  `6145bd595ad2ca3e7cedb1bee1bd3b6b8895168d579e580c0ec3795b47fe3159`,
  `4899cb0304a2533289ce44b8ac46cddc4dc2727e688849b5ad4c262edb3e895c`,
  and `fd50a56e35b42599e79ef12eec7430440d81e44f4297bd4a7941c42df9510f2e`.
  Typecheck, scoped/full ESLint, build, preflight, all three JavaScript parsers,
  and `git diff --check` passed; the aggregate static log has SHA-256
  `7df3e241b48db14072bd3abf7ae423c68ecb3e0e46cf2e8ca23b67dc0aa689eb`.
  Fresh normal-umask release passed 137 / 1 skipped files with 2,844 / 10
  skipped tests; mode-`0600`
  `/tmp/lethebot-gov-ux-01c8c-release.log` has SHA-256
  `33c11f63c4f9fc56f49d95f24bfc55d430dd83dd17446048f7b1dc7e0301dbd8`.

  Records remains the default Memory subview. Review owns separate catalog,
  fingerprint, handle, selection, and request-sequence state over only
  `/scopes` and `/memory-reviews`; explicit selection is required and the exact
  Review handle is the only scope header. Strict schemas and DOM construction
  expose only bounded proposal/reason/revision evidence plus handle expiry;
  scope and resource handles remain internal. Final HTML/CSS/`app.js`/
  `activity.js`/`memory.js` sizes are 8,641/16,918/24,328/23,616/31,215 bytes;
  the fixed shell is 49,887 bytes and every asset remains below 65,536 bytes.

  Private mode-`0700`
  `/tmp/lethebot-gov-ux-01c8c-browser-final-jK1wU1` contains clean mode-`0600`
  1440x900 and 390x844 screenshots plus the browser summary. Chromium exercised
  15 invalid catalog and 29 invalid Review-page states, made 4 Records-scope,
  3 Records, 56 Review-scope, and 38 Review requests, retained byte-identical
  Activity structure SHA-256
  `fa3ae562f41023eb0a44c5276e12ee7b3253aeae23d6448323d676c01aaad8a2`,
  and recorded exact purpose headers, no handle leakage, no detail/mutation
  request, no overflow, and zero console/page/request failures. The mode-`0600`
  browser log and desktop/mobile screenshots have SHA-256
  `feb77be93c6e8ccef60a7837285343d3492283e2cdbed823d1bb84f181f263ac`,
  `7e2fa12de9857cedbbbd278dce367db3c61721b092c0e29096f1dd91c5b94941`,
  and `2b562c3a851a226ffe2a88f36157991a3d5bf03cb5fea03eee8f7b0daf2c266b`.

  Private mode-`0700`
  `/tmp/lethebot-gov-ux-01c8c-roundtrip-doGpP4` holds the accepted six-path
  ASCII patch (`sha256:44a1a9d10ebbd1bcf3bc628c632b75b6d5a6c6eef956484f9ca36583df901c66`,
  mode `0600`, size `38850`). Independent reverse-first and forward-first
  zero-fuzz, zero-offset cycles restored all 24 proof files byte-for-byte and
  mode-for-mode; candidate/workspace equality passed 6 / 6 and the checkpoint
  is excluded. Final hygiene remains 74 tracked modifications, 60 untracked
  paths, and zero staged at `main@cfebb77`; both installed Pi packages remain
  `0.80.2`, lockfile SHA-256 remains
  `f727570a279194362333b938822a2c7699853b1f7977c68e064fae8e8444fc17`, and
  migration 007/008 SHA-256 values remain
  `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`/
  `95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`.
  No detail, mutation, backend, persistence, schema, migration, dependency,
  lockfile, CLI/QQ, Provider, private/live, deployment, restart, or container
  behavior changed.
- `exact_next_action`: completed; the post-C8C reassessment selects
  `GOV-UX-01C8D`, the read-only exact-scope Memory Review detail.

## Completed Slice: `GOV-UX-01C8D`

- `requirement`: `GOV-UX-01C8D` read-only exact-scope Memory Review detail.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: C8C makes the bounded Review queue visible and retains each
  row's current-session resource handle internally, but operators cannot inspect
  the already bounded candidate and proposal-revision evidence behind a row.
  The existing exact-scope GET detail route is therefore unreachable from the
  browser even though it requires the same Review purpose and resource kind.
- `selection_reason`: maintenance review remains the highest-priority Memory
  workflow. Exposing its existing read-only detail is the smallest dependency-
  ready step toward an informed decision while keeping every preview,
  confirmation, and mutation in later independently reviewable slices.
- `implementation_contract`: add only one read-only Review-detail interaction
  inside the existing Review subview. A queue-row command selects that row's
  internal resource handle and fetches exact same-origin
  `/governance/api/v1/memory-reviews/:resourceHandle` with the currently
  selected Review scope handle as the sole `X-LetheBot-Scope` value. Send no
  query, body, Origin, CSRF value, raw selector, or Records-purpose authority.
  Never infer a resource from the displayed proposal reference or construct a
  detail request from a Records scope/resource handle.

  The Memory module owns explicit unselected, loading, populated, malformed,
  unavailable, not-found, stale, and cleared detail states plus a request
  sequence independent from both list sequences. Scope changes, Review refresh,
  return to Records, primary navigation, expiry, and logout invalidate or clear
  detail authority and suppress late responses. A still-current queue refresh
  may retain detail selection only when the same proposal reference remains and
  the newly issued row supplies fresh resource authority; the old handle is
  never reused.

  Validate the exact fixed detail schema before replacing prior content and
  require its list-level proposal reference, kind, effect, lifecycle, scope,
  candidate fingerprint/count, confidence, reasons, revision counts, and dates
  to agree with the selected current row. Strictly bound and validate optional
  effect-memory reference, fixed effect-memory role, candidate ordinal/memory
  reference/effect role/expected state/record fingerprint/source count/source
  fingerprint entries, candidate truncation, revision number/transition/
  previous and new state/actor class/invocation context/reason/date entries,
  and revision truncation. Reject unknown keys, duplicates, invalid enums,
  non-finite or inconsistent numbers, noncanonical dates, malformed references
  or fingerprints, excess arrays, and row/detail drift without replacing the
  last valid detail.

  Render only fixed redacted proposal, candidate, effect, and revision evidence
  through DOM creation and `textContent`. Keep both scope and resource handles
  internal except for their exact request positions; do not place them in DOM
  text/attributes, storage, application logs, or announcements. Add no approve,
  reject, expire, apply, rollback, preview, confirmation, mutation, filter,
  search, polling, caller limit, dynamic import, backend route, authority,
  package, dependency, or build step. Preserve all C1-C8C Overview, Activity,
  Memory Records, Review-list, navigation, focus, accessibility, responsive,
  and purpose-isolation behavior. Keep every asset below 65,536 bytes and the
  HTML/CSS/`app.js` shell below 50,000 bytes.
- `allowed_paths`: `src/http/governance-browser-assets.ts`,
  `tests/integration/governance-http.test.ts`, `docs/architecture.md`,
  `docs/deployment.md`, `docs/governance-ui.md`, `docs/security-privacy.md`, and
  this checkpoint only.
- `protected_paths`: all C1-C8C validation, rendering, requests, session,
  keyboard/focus, accessibility, responsive, exact-scope, and purpose-isolation
  behavior; the existing Review catalog/list/detail routes, DTOs, ordering,
  registries, and authority; every preview/confirmation/mutation; all other
  browser workflows; `GovernanceHttpServer`, `src/index.ts`, service, query,
  repository, CLI/QQ, config/env, schema, migrations, dependencies, lockfile,
  Provider, private/live data, deployment/restart/container behavior, and every
  prior slice.
- `assertions`: a named fail-first proves the absent Review-detail control,
  endpoint use, exact schema, row-coherence boundary, and independent detail
  sequence before product edits. Final direct/browser behavior covers detail
  unselected/loading/populated/optional/truncated/malformed/unavailable/not-
  found/stale/no-replacement states; row changes, scope changes, list refresh,
  navigation, expiry, logout, keyboard/focus, and desktop/mobile reflow. Each
  request has exactly the selected Review scope handle and row resource handle;
  cross-purpose or old authority is absent, handles never render or persist,
  and no POST, preview, confirmation, or mutation request is emitted. Existing
  Activity runtime structure and all C8C Records/Review-list behavior remain
  exact.
- `focused_commands`: unchanged C8C/C8B/C8A/C7C/fixed-shell
  characterizations; named C8D fail-first/final; full governance HTTP;
  four-file HTTP/application/wiring adjacency; canonical P8; typecheck;
  scoped/full ESLint; build/preflight; normal-umask release; all JavaScript
  parsers and complete synthetic desktop/mobile Records/Review list/detail plus
  Activity regression oracles; diff, inventory, dependency/migration/privacy
  hygiene; and exact six-path reverse-first/forward-first rollback proof.
- `schema_dependency_live_boundary`: fixed browser Review-detail presentation,
  synthetic tests, four canonical browser/security documents, and checkpoint
  evidence only. No backend authority, persistence, schema, migration,
  dependency, lockfile, CLI/QQ, Provider, private DB, deployment, restart,
  container, or live operation.
- `rollback_boundary`: after this contract and a clean diff check, capture the
  exact C8C bytes and modes for browser source, integration test, four canonical
  documents, and checkpoint under one new private mode-`0700` root. The final
  six-product-path ASCII patch must independently prove reverse-first and
  forward-first exact byte/mode restoration without touching the workspace;
  checkpoint remains outside the patch.
- `baseline_evidence`: private mode-`0700`
  `/tmp/lethebot-gov-ux-01c8d-baseline-zz4uXC` captured all seven allowed paths
  at mode `0644`. The unchanged C8C/C8B/C8A/C7C/fixed-shell characterization
  passed 5 / 5; mode-`0600`
  `/tmp/lethebot-gov-ux-01c8d-characterization.log` has SHA-256
  `43ad93d0984445117785c0a020629a19fcafe12454898ef56b45488cbab44bf7`.
- `fail_first_evidence`: the named C8D regression failed alone because
  `memory.js` lacked the Review-detail endpoint definition and row control;
  every request, schema, state, coherence, and no-mutation assertion was
  collected before product or canonical-document edits. Mode-`0600`
  `/tmp/lethebot-gov-ux-01c8d-fail-first.log` has SHA-256
  `e4f9661194a8b2bc80c16781d4c68b6724fa5f62c149a316500b5caa4e161608`.
- `final_evidence`: the focused C8D regression passed 1 / 1, full governance
  HTTP passed 32 / 32, four-file HTTP/application/wiring adjacency passed
  78 / 78, and canonical P8 passed 340 / 340. Their accepted mode-`0600` logs
  have SHA-256
  `7469aaedd8596fcc06c86f79277d47db8b4007f089f1a59906a3a4f4c40512fe`,
  `a96dac0938f49d90307a2e598eb3a4ff3a4bf5e3b74085c0e4252d2c27b67431`,
  `37b231aef66c42a16b098fe1363182e74271a1177d281eac37a8d4dc87b3ece0`,
  and `0c5c9af2a55786e7aced9383343c490d1c97f7ec4e3d3d7858c5e187a28069bd`.
  Typecheck, scoped/full ESLint, build, preflight, all three JavaScript parsers,
  and `git diff --check` passed. Fresh normal-umask release passed 137 / 1
  skipped files with 2,845 / 10 skipped tests; mode-`0600`
  `/tmp/lethebot-gov-ux-01c8d-release.log` has SHA-256
  `36bd47b1a3e530a9b4392ee379d54d2b90bb128008d518d50079127d58cdeb56`.

  Review rows now select only their internal list-issued resource authority.
  The independent detail sequence sends exact GET-only requests with the
  current Review scope handle, preserves the last valid detail across malformed
  or drifted responses, uses fresh authority after list refresh, and clears on
  scope, subview, navigation, refresh, expiry, and logout changes. Strict
  list/detail coherence and fixed 32-entry candidate/revision bounds hold; the
  optional effect evidence is omitted when absent. Final HTML/CSS/`app.js`/
  `activity.js`/`memory.js` sizes are 8,641/16,918/24,227/23,616/50,900 bytes;
  the fixed shell is 49,786 bytes and every asset remains below 65,536 bytes.

  The browser oracle completed 18 assertions and recorded 12 detail GETs with
  empty queries, zero-byte bodies, one exact scope value, and no Review POST or
  mutation request. Desktop and mobile content widths were 1,425 / 1,440 and
  375 / 390, with no horizontal overflow, handle leakage, overlap, clipping,
  console, page, or request failure. The mode-`0600` CDP screenshots under
  `/tmp/lethebot-gov-ux-01c8d-browser-final-VbWgma` have SHA-256
  `aaed9d4ec16264632f783fe078ebcd266ed20336ab15aa3327092b59bb3005ec`
  and `3bf4a51cd50679be0323380bcc6b3cbd4e84fb36238238c53ba24392b6780e9c`.

  Private mode-`0700`
  `/tmp/lethebot-gov-ux-01c8d-roundtrip-YczfvE` holds the accepted six-path
  zero-context ASCII patch
  (`sha256:58a296cf709fedf10486a2d1b351eefe1c7391567f902c73df77a53fb8360ec9`,
  mode `0600`, size `35423`). Independent reverse-first and forward-first
  byte/mode cycles and candidate/workspace equality passed; the checkpoint is
  excluded. Earlier disposable proof roots and the superseded partial P8 log
  are excluded from accepted evidence. Final hygiene remains 74 tracked
  modifications, 60 untracked paths, and zero staged at `main@cfebb77`; both
  installed Pi packages remain `0.80.2`, lockfile SHA-256 remains
  `f727570a279194362333b938822a2c7699853b1f7977c68e064fae8e8444fc17`, and
  migration 007/008 SHA-256 values remain
  `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`/
  `95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`.
  No preview, confirmation, POST, mutation, CSS, backend, persistence, schema,
  migration, dependency, lockfile, CLI/QQ, Provider, private/live, deployment,
  restart, or container behavior changed.
- `exact_next_action`: completed; the post-C8D dependency reassessment selects
  `GOV-UX-01C8E`, the read-only exact-scope Memory Record provenance detail.

## Completed Slice: `GOV-UX-01C8E`

- `requirement`: `GOV-UX-01C8E` read-only exact-scope Memory Record provenance
  detail.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: C8B makes the bounded Records list visible and retains each
  row's current-session `memory_record` handle internally, but operators cannot
  inspect the already bounded source, revision, and audit provenance behind a
  row. B17D's exact-scope GET detail route remains unreachable from the browser
  while C8D now exposes the parallel Review detail workflow.
- `selection_reason`: this completes the existing read-only Records inspection
  path with already-proved authority and DTOs. Maintenance approval preview is
  mutation-adjacent and adds Origin, CSRF, body, and single-use preview
  authority; it remains a later independently reviewable slice after both
  Memory read paths are inspectable.
- `implementation_contract`: add only one read-only Record-detail interaction
  inside the existing Records subview. A record-row command selects that row's
  internal resource handle and fetches exact same-origin
  `/governance/api/v1/memory/records/:resourceHandle` with the currently
  selected Records scope handle as the sole `X-LetheBot-Scope` value. Send no
  query, body, Origin, CSRF value, raw selector, or Review-purpose authority.
  Never infer a resource from the displayed record reference or construct a
  detail request from a Review scope/resource handle.

  The Memory module owns explicit unselected, loading, populated, malformed,
  unavailable, not-found, stale, and cleared Record-detail states plus a
  request sequence independent from both Records list and Review list/detail
  sequences. Records scope changes, Records refresh, entry into Review, primary
  navigation, expiry, and logout invalidate or clear detail authority and
  suppress late responses. A still-current Records refresh may retain detail
  selection only when the same record reference remains and the newly issued
  row supplies fresh resource authority; the old handle is never reused.

  Validate the exact fixed B17A detail schema before replacing prior content
  and require its complete nested record projection to agree with the selected
  current row, excluding only the list-owned handle and handle expiry. Strictly
  bound each source, revision, and audit array to 32 entries and validate
  truncation signals, fixed references, enum domains, safe revision numbers,
  optional lifecycle/reason/risk fields and their redaction signals, canonical
  dates, deterministic ordering, evaluator-link booleans, and fixed-hidden
  audit details. Reject unknown keys, duplicate references or revision numbers,
  invalid enums, non-finite or inconsistent numbers, noncanonical dates,
  malformed references, excess arrays, and row/detail drift without replacing
  the last valid detail.

  Render only the fixed redacted record, source, revision, and audit evidence
  through DOM creation and `textContent`. Keep both scope and resource handles
  internal except for their exact request positions; do not place them in DOM
  text/attributes, storage, application logs, or announcements. Add no forget,
  restore, preview, confirmation, mutation, filter, search, polling, caller
  limit, dynamic import, backend route, authority, package, dependency, CSS, or
  build step. Preserve all C1-C8D Overview, Activity, Memory Records, Review
  list/detail, navigation, focus, accessibility, responsive, and purpose-
  isolation behavior. Keep HTML, CSS, and `activity.js` byte-identical, avoid
  fixed-shell growth, and keep every asset below 65,536 bytes and the fixed
  shell below 50,000 bytes.
- `allowed_paths`: `src/http/governance-browser-assets.ts`,
  `tests/integration/governance-http.test.ts`, `docs/architecture.md`,
  `docs/deployment.md`, `docs/governance-ui.md`, `docs/security-privacy.md`, and
  this checkpoint only.
- `protected_paths`: all C1-C8D validation, rendering, requests, session,
  keyboard/focus, accessibility, responsive, exact-scope, detail-coherence, and
  purpose-isolation behavior; the existing Memory catalog/list/detail routes,
  DTOs, ordering, registries, and authority; every preview/confirmation/
  mutation; all other browser workflows; `GovernanceHttpServer`, `src/index.ts`,
  service, query, repository, CLI/QQ, config/env, schema, migrations,
  dependencies, lockfile, Provider, private/live data, deployment/restart/
  container behavior, and every prior slice.
- `assertions`: a named fail-first proves the absent Record-detail control,
  endpoint use, exact schema, row-coherence boundary, and independent detail
  sequence before product edits. Final direct/browser behavior covers detail
  unselected/loading/populated/optional/truncated/malformed/unavailable/not-
  found/stale/no-replacement states; row changes, scope changes, list refresh,
  Review entry, navigation, expiry, logout, keyboard/focus, and desktop/mobile
  reflow. Each request has exactly the selected Records scope handle and row
  resource handle; cross-purpose or old authority is absent, handles never
  render or persist, and no POST, preview, confirmation, or mutation request is
  emitted. Existing Activity runtime structure and all C8D Records/Review
  list/detail behavior remain exact.
- `focused_commands`: unchanged C8D/C8C/C8B/C8A/C7C/fixed-shell
  characterizations; named C8E fail-first/final; full governance HTTP;
  four-file HTTP/application/wiring adjacency; canonical P8; typecheck;
  scoped/full ESLint; build/preflight; normal-umask release; all JavaScript
  parsers and complete synthetic desktop/mobile Records list/detail plus Review
  list/detail and Activity regression oracles; diff, inventory, dependency/
  migration/privacy hygiene; and exact six-path reverse-first/forward-first
  rollback proof.
- `schema_dependency_live_boundary`: fixed browser Record-detail presentation,
  synthetic tests, four canonical browser/security documents, and checkpoint
  evidence only. No backend authority, persistence, schema, migration,
  dependency, lockfile, CLI/QQ, Provider, private DB, deployment, restart,
  container, or live operation.
- `rollback_boundary`: after this contract and a clean diff check, capture the
  exact C8D bytes and modes for browser source, integration test, four canonical
  documents, and checkpoint under one new private mode-`0700` root. The final
  six-product-path ASCII patch must independently prove reverse-first and
  forward-first exact byte/mode restoration without touching the workspace;
  checkpoint remains outside the patch.
- `baseline_evidence`: private mode-`0700`
  `/tmp/lethebot-gov-ux-01c8e-baseline-InBWVt` captured all seven allowed paths
  at mode `0644`. The unchanged C8D/C8C/C8B/C8A/C7C/fixed-shell
  characterizations passed 6 / 6; mode-`0600`
  `/tmp/lethebot-gov-ux-01c8e-characterization-nmV9k5.log` has SHA-256
  `e838559a5c784ad8f245986aa75c6f4a9d590a77c6142a04e1b0249fe4d9d2d7`.
  Source/test/architecture/deployment/governance/security/checkpoint baseline
  SHA-256 values are
  `dd694fbece006ffdf7b5d1d4c9d8b725b16604e9cb1ca90c25ab4d30d2251133`,
  `6432bc520e39520ca3cf1b51183fb476b0d82e14f9b6e501ccb2644a9d75bd9e`,
  `2b8a824646e1663b6f538cdf10ac3b0d7463af60080a02ed2647cc24d60f1313`,
  `6d9078fb5e3197aa5edc430a838a6e6d10fe4bc2dc7af2d682d543477c34e46a`,
  `40c043f73ffce4a8ad15d88ed054db75d567ea6d8f3e7e11b2e7854922275793`,
  `6b73632d19152b5b944294afdb1cbaef303cb4d5f4a9cfdd1302aece0ea0997e`,
  and `02d701a0f91d45b790b0b0766e5f52623f35447e56562ad8e122e2789eac2ac1`.
- `fail_first_evidence`: the named C8E regression collected 33 tests, skipped
  32 unrelated cases, and failed on the absent Record-provenance control,
  endpoint definition, validator, renderer, controller, and state contract
  before product or canonical-document edits. Its mode-`0600` log
  `/tmp/lethebot-gov-ux-01c8e-fail-first-VibDP4.log` has SHA-256
  `21d4b2a74fc15898932a6195a100e8d68d56799c1e531bff72df7717fe757f95`.
- `final_evidence`: the named C8E regression passed 1 / 1, full governance HTTP
  passed 33 / 33, four-file HTTP/application/wiring adjacency passed 79 / 79,
  and canonical P8 passed 7 files / 341 tests. Their accepted mode-`0600` logs
  have SHA-256
  `1b9c85efc4c885411266d492fc564bd02b3cf12c52dc438494a890a49614ee05`,
  `c86e931b06898872e940af06099298acc224490975bde8a35dc1cf8b1ae04d5a`,
  `4c025c0b8ec13cd3bcaeb67b2e7085271f2f85acd2926d2b573417bcf479dc37`,
  and `f3bb82479f979620bf3956c2bdba27fbcac06c9276e70e0abdb8720c4fafab1f`.
  Typecheck, scoped/full ESLint, build, preflight, all three JavaScript parsers,
  exact current/baseline asset comparison, and `git diff --check` passed.
  Fresh normal-umask release passed 137 / 1 skipped files with 2,846 / 10
  skipped tests; mode-`0600`
  `/tmp/lethebot-gov-ux-01c8e-release.log` has SHA-256
  `29af8088a5fd3b5e606425f04c1b0e88647b1ca819957c56947a0cc85490e1d3`.

  Records rows now select only their internal list-issued resource authority.
  The independent detail sequence sends exact GET-only requests with the
  current Records scope handle, preserves the last valid detail across
  malformed or drifted responses, reloads only through fresh row authority
  after a list refresh, and clears on scope, Review, navigation, refresh,
  expiry, and logout changes. Strict nested row/detail coherence and fixed
  32-entry source/revision/audit bounds hold. Final HTML/CSS/`app.js`/
  `activity.js`/`memory.js` sizes are
  8,641/16,918/24,227/23,616/64,978 bytes; their SHA-256 values are
  `b623c37588ef24fd14f501ce2451c44f04c4f7ea835703d591f2a509af5853cb`,
  `381d7e04d6512a830f6d535080baf756f22add8e91a44238908f5bbd2bda8c65`,
  `218953947213bacd2b389fd811e5706946e9969c004d99dd62b91998efcbc74e`,
  `33a528afba5b31998778d6ac2b58c299cd62592364e4d069e1f29eea95300218`,
  and `43a900530e45520aa89685650d816e4bdf17e61e50834668ad9bb70d45d1ff5e`.
  The first four assets are byte-identical to C8D, the fixed shell remains
  49,786 bytes, and every asset remains below 65,536 bytes.

  The Chromium oracle completed 48 assertions. Desktop and mobile visual runs
  completed 15 assertions each with no overflow, overlap, clipping, handle
  leakage, console, page, request, or external failure. Private evidence root
  `/tmp/lethebot-gov-ux-01c8e-browser-accepted-G1zsKm` contains a mode-`0600`
  summary (`sha256:7b38c0b3ae9f6bdc0b048d3b422535074cef3cc0b4a68235459c8b1e66bbf198`)
  and accepted desktop/mobile screenshots with SHA-256
  `e1c3a4a5dae16af410d4a08a26bf64c4698444e35067eed4dcbd927c04aff681`/
  `0d6218c4132fe81f614c532cce5d370b0d62e2b5cf0ac64619c9f15500fb8153`.

  Private mode-`0700`
  `/tmp/lethebot-gov-ux-01c8e-roundtrip-final-mWkeTW` holds the accepted exact
  six-product-path zero-context ASCII patch
  (`sha256:11bc66374619fbcabf3736f5db77a3df500c66b27e8f7e91eaad8b347e9fc386`,
  mode `0600`, size `59793`) and mode-`0600` proof log
  (`sha256:9b9b63777e9da452c88466c855f761627e12f8daeaaa830955244df81fef1164`).
  Independent reverse-first and forward-first cycles restored baseline and
  candidate byte-for-byte and mode-for-mode, and candidate/workspace equality
  passed 6 / 6. Superseded setup/proof roots are excluded from accepted
  evidence. Final hygiene remains 74 tracked modifications, 60 untracked paths,
  and zero staged at `main@cfebb77`; no unexpected sensitive status path was
  found. Pi remains `0.80.2`; lockfile and migration 007/008 SHA-256 values
  remain `f727570a279194362333b938822a2c7699853b1f7977c68e064fae8e8444fc17`,
  `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`,
  and `95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`. No preview,
  confirmation, POST, mutation, CSS, backend, persistence, schema, migration,
  dependency, lockfile, CLI/QQ, Provider, private/live, deployment, restart, or
  container behavior changed.
- `exact_next_action`: completed; the post-C8E dependency reassessment selects
  the behavior-preserving C8F static Memory presentation-module extraction
  below before adding a maintenance-approval preview browser workflow.

## Completed Slice: `GOV-UX-01C8F`

- `requirement`: `GOV-UX-01C8F` behavior-preserving static Memory presentation-
  module extraction prerequisite.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: C8D and C8E complete the two read-only Memory detail paths,
  but `memory.js` is now 64,978 bytes and leaves only 558 bytes below the fixed
  65,536-byte per-asset boundary. The already-backed pending-review approval
  preview needs a visible command, strict response validation, safe preview
  rendering, Origin/CSRF body transport, and independent stale-state handling;
  that workflow cannot fit without first restoring material module headroom.
- `selection_reason`: C8D and C8E are two consecutive Memory-detail feature
  slices, so the full dependency reassessment is due. A separate static module
  extraction is smaller and safer than mixing representation work with the
  first browser mutation: it preserves behavior while separating fixed schema/
  presentation ownership from transport/controller ownership, following the
  already-proved C8A module boundary without adding a dependency or build step.
- `implementation_contract`: add exact static same-origin
  `/governance/memory-presentation.js` and make `/governance/memory.js` import it
  through one top-level static import. The presentation module owns the fixed
  Memory/Review value domains and key tables, exact-object/text/date/count
  validation, scope/list/detail normalization and coherence checks, safe DOM
  primitives, value/date formatting, and all Records/Review list/detail
  renderers. It exports only the fixed functions needed by `memory.js`.

  `memory.js` retains all API endpoint values, complete view construction,
  session-injected transport, scope/resource authority, separate Records and
  Review catalogs/selections/request sequences, loading/error/empty/detail
  states, refresh/navigation/expiry/logout cancellation, event listeners,
  announcements, keyboard/focus integration, and the public
  `createMemoryFeature` surface. Create no circular or dynamic import and no
  runtime code evaluation. The new static route creates no session, scope,
  resource, preview, CSRF, or domain authority.

  Preserve byte-identical served HTML, CSS, `app.js`, and `activity.js`; exact
  `memory.js` runtime requests, accepted/rejected payloads, render output, DOM
  tags/attributes/text/classes/order, state messages, navigation, focus,
  responsive layout, and handle non-disclosure. The pre/post desktop and mobile
  runtime structures must be byte-identical. Add no approval command, POST,
  preview/confirmation handle use, mutation, backend route/DTO, CSS token,
  package, dependency, or build step. Keep every served asset below 65,536
  bytes and both Memory modules below 35,000 bytes.
- `allowed_paths`: `src/http/governance-browser-assets.ts`,
  `tests/integration/governance-http.test.ts`, `docs/architecture.md`,
  `docs/deployment.md`, `docs/governance-ui.md`, `docs/security-privacy.md`, and
  this checkpoint only.
- `protected_paths`: all C1-C8E visible, request, validation, normalization,
  rendering, DOM, state, session, keyboard/focus, accessibility, responsive,
  exact-scope, resource-coherence, and purpose-isolation behavior; every
  preview/confirmation/mutation route and DTO; `GovernanceHttpServer`,
  `src/index.ts`, service, query, repository, CLI/QQ, config/env, schema,
  migrations, dependencies/lockfile, Provider, private/live data, deployment/
  restart/container behavior, and every prior slice.
- `assertions`: unchanged named C8E/C8D/C8C/C8B/C8A/C7C/fixed-shell
  characterizations reproduce the exact five C8E asset bodies before editing.
  A passing pre-refactor characterization fixes the complete served-definition
  union, exact requests/states/DOM/runtime structures, and current asset hashes.
  A named boundary/headroom regression then fails only because the presentation
  asset is absent and `memory.js` is not below 35,000 bytes. Final coverage
  proves the exact static import/export boundary, no duplicate owner or circular
  import, identical combined definitions and browser structures, unchanged
  requests/validation/session/stale/keyboard/responsive behavior, all four
  protected assets byte-identical, both Memory assets below 35,000 bytes, all
  modules parse, and no new API request, authority, feature, or sink appears.
- `focused_commands`: unchanged C8E/C8D/C8C/C8B/C8A/C7C/fixed-shell
  characterizations; passing extraction characterization; named C8F fail-first/
  final; full governance HTTP; four-file HTTP/application/wiring adjacency;
  canonical P8; typecheck; scoped/full ESLint; build/preflight; normal-umask
  release; all four JavaScript parsers and complete synthetic desktop/mobile
  Records/Review list/detail plus Activity regression oracles; diff, inventory,
  dependency/migration/privacy hygiene; and exact six-path reverse-first/
  forward-first rollback proof.
- `schema_dependency_live_boundary`: fixed static browser module ownership,
  synthetic tests, four canonical browser/security documents, and checkpoint
  evidence only. No feature, backend authority, persistence, schema, migration,
  dependency, lockfile, CLI/QQ, Provider, private DB, deployment, restart,
  container, or live operation may change.
- `rollback_boundary`: private mode-`0700`
  `/tmp/lethebot-gov-ux-01c8f-roundtrip-QtDimH` holds the accepted exact six-
  product-path zero-context ASCII patch. The checkpoint is outside that patch;
  independent reverse-first and forward-first proof trees leave the workspace
  untouched.
- `baseline_evidence`: private mode-`0700`
  `/tmp/lethebot-gov-ux-01c8f-baseline-CE2qEX` captured all seven allowed paths,
  and `/tmp/lethebot-gov-ux-01c8f-assets-rbzFgr` captured the five C8E assets.
  The unchanged C8E/C8D/C8C/C8B/C8A/C7C/fixed-shell characterizations passed
  7 / 7; mode-`0600` `/tmp/lethebot-gov-ux-01c8f-characterization.log` has
  SHA-256
  `5932f21e256e250a808377515740268bdde1516553ce2813aebefff40004018a`.
- `fail_first_evidence`: the pre-refactor extraction characterization passed
  1 / 1 and fixed the complete served-definition, request, state, DOM, and
  responsive-structure contract; its mode-`0600` log has SHA-256
  `f70c30d812a3d7ac787279a60a6d8c13fe7c5fd0a96e13465efa49bfc3afcb39`.
  The named C8F boundary regression then failed only because
  `/governance/memory-presentation.js` returned `404` and `memory.js` measured
  64,978 bytes, not less than 35,000. Its mode-`0600` log has SHA-256
  `5a11ced146a83b6ce5ceb86f7b8bd99419e428c87470ef97430d066c4fac031c`.
- `final_evidence`: the named C8F regression passed 1 / 1, full governance HTTP
  passed 35 / 35, four-file HTTP/application/wiring adjacency passed 81 / 81,
  and canonical P8 passed 7 files / 343 tests. Their accepted mode-`0600` logs
  have SHA-256
  `64225f85d50ed35989a91e965c55daf72df4d48495ebf29444e7f07f38f58756`,
  `40080852dc429d88b24461b9a705b768e9b4445dce932a2b5157146afebfda58`,
  `0729d01049ee23c92498d52c7e5ea269301c0a03e4d0f74e3b3cf938f93db826`,
  and `9d48d75023ac0685ab3cec024af04ec3954bfd10bd4a795096149a97d3b09d49`.
  Typecheck, scoped/full ESLint, build, preflight, all four JavaScript parsers,
  and `git diff --check` passed. Fresh normal-umask release passed 137 / 1
  skipped files with 2,848 / 10 skipped tests; mode-`0600`
  `/tmp/lethebot-gov-ux-01c8f-release.log` has SHA-256
  `6a8d73d89957b04fee1eae31f95e88589123486a72bbd6792bccb67d03893996`.

  Final mode-`0700` asset snapshot
  `/tmp/lethebot-gov-ux-01c8f-assets-final-sfTlch` contains HTML/CSS/`app.js`/
  `activity.js`/`memory.js`/`memory-presentation.js` at
  8,641/16,918/24,227/23,616/32,442/33,072 bytes; their SHA-256 values are
  `b623c37588ef24fd14f501ce2451c44f04c4f7ea835703d591f2a509af5853cb`,
  `381d7e04d6512a830f6d535080baf756f22add8e91a44238908f5bbd2bda8c65`,
  `218953947213bacd2b389fd811e5706946e9969c004d99dd62b91998efcbc74e`,
  `33a528afba5b31998778d6ac2b58c299cd62592364e4d069e1f29eea95300218`,
  `4f33b47380e342ae543e3bcfb06db71484f33de499a766c9ac8881e9f123b863`,
  and `2aa7b4746e23078f3c0043bbbe5574dc8174ae20f2270665fc0ce3afd65ee04f`.
  The first four are byte-identical to C8E. `memory.js` has one top-level
  static import; combined definitions, requests, states, rendering, focus,
  cancellation, authority isolation, and handle non-disclosure remain exact.

  Accepted browser roots are
  `/tmp/lethebot-gov-ux-01c8f-browser-baseline-nw3SPB` and
  `/tmp/lethebot-gov-ux-01c8f-browser-final-4XcxXN`. Desktop/mobile canonical
  structures are byte-identical before and after extraction with SHA-256
  `557c73529ebb962fe8ab3ea06fca0b86b8fa5ab55b026e0c88e6409c1f37a74a`.
  Final static/API request counts are 15 / 9 versus baseline 14 / 9, solely for
  the new static module. Both runs have zero errors, overflow, or handle
  disclosure. Desktop screenshots are byte-identical with SHA-256
  `cfaa93dc3ef065c93eb96b61165aaebae5deca1affc25622ebfcb5abea2a480e`;
  mobile screenshots are byte-identical with SHA-256
  `c954a3bde36fb6bf5b85738d02e4a65e6f2ba0bfa09e9d7efb5600f76517e256`.

  The accepted patch has SHA-256
  `16b383c84a2e81820d3cc96123164a9869107783a5b9fb27281de38ccff3fa8b`,
  mode `0600`, and size `18247`; its mode-`0600` proof log has SHA-256
  `dc324a7a8365b3add6a0cdf27386251c220c28c5d424b3615c5abf08dfea2d00`.
  Reverse-first and forward-first cycles passed 6 / 6 byte/mode checks, and
  candidate/workspace equality passed 6 / 6. Final hygiene remains 74 tracked
  modifications, 60 untracked paths, and zero staged at `main@cfebb77`; the
  patch is ASCII-only and the sensitive untracked filename scan is empty. Pi
  remains `0.80.2`; lockfile and migration 007/008 SHA-256 values remain
  `f727570a279194362333b938822a2c7699853b1f7977c68e064fae8e8444fc17`,
  `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`,
  and `95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`.
  No feature, POST, preview, confirmation, backend, CSS, persistence, schema,
  migration, dependency, lockfile, CLI/QQ, Provider, private/live, deployment,
  restart, or container behavior changed.
- `exact_next_action`: completed; the post-extraction dependency reassessment
  selects `GOV-UX-01C8G`, the first write-free browser maintenance-approval
  preview workflow, while leaving confirmation absent.

## Completed Slice: `GOV-UX-01C8G`

- `requirement`: `GOV-UX-01C8G` exact-scope browser maintenance-approval
  preview workflow.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: C8C/C8D expose the pending Review queue and exact bounded
  detail, and B7C already owns the write-free approval-preview POST, but the
  browser cannot request or inspect that preview. Operators therefore stop at
  read-only evidence even though C8F has restored static Memory-module headroom.
- `selection_reason`: approval preview is the smallest dependency-ready browser
  step after the two read-only detail paths and the C8F extraction. It reuses
  existing exact scope/resource/session/CSRF authority, performs no durable
  write, and keeps the separately reviewable approval confirmation absent.
- `implementation_contract`: add one approval-preview command to a coherent
  selected pending Review detail. The existing shell remains the sole owner of
  the in-memory CSRF token and injects a narrow same-origin mutation transport;
  the Memory controller supplies only the current Review scope handle, current
  list-issued review resource handle, and exact JSON body
  `{ "action": "approve" }`. Send one `POST` to the existing
  `/governance/api/v1/memory-reviews/:resourceHandle` path with no query or raw
  selector, exact `X-LetheBot-Scope`, `X-LetheBot-CSRF`, and JSON content type,
  plus the browser-derived exact Origin. Never reuse Records authority or build
  the path from the displayed proposal reference.

  Validate the exact B7C `201` response before rendering or retaining any
  authority: canonical approval action; scope; proposal kind/reference/effect;
  affected count/fingerprint; current `pending_review` state and safe revision;
  expected `approved` state at the next revision; the fixed three durable
  effects and unavailable memory-record mutation; unsupported direct rollback
  boundary; opaque preview handle; canonical digest; and safe absolute expiry.
  Require complete coherence with the selected Review row/detail and reject
  unknown keys, invalid domains, duplicate effects, unsafe counts/revisions,
  malformed references, digests, or handles, nonfuture expiries, and any row/
  detail/preview drift.

  The presentation module owns the fixed preview schema, normalization,
  coherence checks, formatting, and safe DOM renderer. The Memory controller
  owns the endpoint, selected scope/resource authority, command, preview state,
  and an independent request sequence. Expose explicit unrequested, loading,
  populated, malformed, unavailable, not-found, and stale states; disable
  duplicate in-flight requests; suppress late responses; and clear preview
  authority on review selection, scope, list/detail refresh, subview,
  navigation, expiry, session, and logout changes. Render only the bounded
  redacted action/effect/current/expected/rollback evidence and expiry. Keep the
  preview handle and digest out of DOM text/attributes, storage, logs, and
  announcements.

  Add no confirmation command or request, preview-handle consumption,
  maintenance transition, memory application, rollback, backend route/DTO,
  repository write, polling, package, dependency, or build step. Preserve all
  C1-C8F Overview, Activity, Records, Review, navigation, focus, accessibility,
  responsive, authority-isolation, and stale-response behavior. Keep every
  served asset below 65,536 bytes.
- `allowed_paths`: `src/http/governance-browser-assets.ts`,
  `tests/integration/governance-http.test.ts`, `docs/architecture.md`,
  `docs/deployment.md`, `docs/governance-ui.md`, `docs/security-privacy.md`, and
  this checkpoint only.
- `protected_paths`: all C1-C8F behavior and static module ownership; B7C/B7D
  routes, DTOs, scope/resource/preview registries, session and CSRF semantics;
  every confirmation or other preview/mutation family; `GovernanceHttpServer`,
  `src/index.ts`, service, query, repository, CLI/QQ, config/env, schema,
  migrations, dependencies/lockfile, Provider, private/live data, deployment/
  restart/container behavior, and every prior slice.
- `assertions`: unchanged named C8F/C8E/C8D/C8C/C8B/C8A/C7C/fixed-shell
  characterizations pass before editing and reproduce all six served assets. A
  named fail-first proves the absent approval command, exact POST, strict B7C
  schema/coherence boundary, safe preview renderer, independent request state,
  and confirmation absence. Final direct/browser coverage exercises populated,
  repeated-fresh-handle, malformed, unavailable, not-found, stale, expired,
  scope/resource drift, cancellation, navigation, expiry, and logout states;
  each request has exact same-origin/CSRF/Review-scope/current-resource/body
  values, while handles never render or persist and no confirm, transition,
  application, rollback, or durable write occurs. Existing Records/Review and
  Activity structures remain exact on desktop and mobile.
- `focused_commands`: unchanged C8F/C8E/C8D/C8C/C8B/C8A/C7C/fixed-shell
  characterizations; named C8G fail-first/final; full governance HTTP;
  four-file HTTP/application/wiring adjacency; canonical P8; typecheck;
  scoped/full ESLint; build/preflight; normal-umask release; all four JavaScript
  parsers and complete synthetic desktop/mobile Records/Review/Activity
  regression oracles; diff, inventory, dependency/migration/privacy hygiene;
  and exact six-path reverse-first/forward-first rollback proof.
- `schema_dependency_live_boundary`: fixed browser approval-preview transport,
  validation, state, and presentation; synthetic tests; four canonical browser/
  security documents; and checkpoint evidence only. No confirmation, durable
  write, backend authority, persistence, schema, migration, dependency,
  lockfile, CLI/QQ, Provider, private DB, deployment, restart, container, or
  live operation.
- `rollback_boundary`: private mode-`0700`
  `/tmp/lethebot-gov-ux-01c8g-roundtrip-ZGlen0` holds the accepted exact six-
  product-path zero-context ASCII patch. The checkpoint is outside that patch;
  independent reverse-first and forward-first proof trees leave the workspace
  untouched.
- `baseline_evidence`: private mode-`0700`
  `/tmp/lethebot-gov-ux-01c8g-baseline-rD3aPd` captured all seven allowed paths,
  and `/tmp/lethebot-gov-ux-01c8g-assets-V2fJya` captured the six C8F assets.
  The unchanged C8F/C8E/C8D/C8C/C8B/C8A/C7C/fixed-shell characterizations
  passed 8 / 8; mode-`0600`
  `/tmp/lethebot-gov-ux-01c8g-characterization.log` has SHA-256
  `bd930fd444f030b3e8f053687c6cb914dbb428260d020c06d0457889b14a723a`.
- `fail_first_evidence`: the named C8G regression failed only on the absent
  approval-preview browser command and transport while the predecessor
  characterizations stayed green. Its mode-`0600` log has SHA-256
  `ee9ae4263ad5ee97f6c8d1cd24ba1b1aa7dbc907d6aff0377fb61164e14f594e`.
- `final_evidence`: the named C8G regression passed 1 / 1, full governance HTTP
  passed 36 / 36, four-file HTTP/application/wiring adjacency passed 82 / 82,
  and canonical P8 passed 7 files / 344 tests. Their accepted mode-`0600` logs
  have SHA-256
  `4ca74b55c6363ed918162be519e816f538cfe77802a839d41b966adfc3b89630`,
  `081cbc7fecf45e1e780d4dd7cc9417c5584343b3b50c3157f77f31be290b62af`,
  `f23beebb2ad15416b21838ca70554634f148be48c908e7ca79d853ebb060511b`,
  and `49661529b3098a4b298c0e45cce1321a10052e24c1d58ae7123535e663c5868b`.
  Typecheck, scoped/full ESLint, build, preflight, all four JavaScript parsers,
  and `git diff --check` passed. Fresh normal-umask release passed 137 / 1
  skipped files with 2,849 / 10 skipped tests; mode-`0600`
  `/tmp/lethebot-gov-ux-01c8g-release.log` has SHA-256
  `a713e1143cae2f98f554a12d10a93f8fa6ddcc36b00514835089ab9fe6b17529`.

  Final mode-`0700` asset snapshot
  `/tmp/lethebot-gov-ux-01c8g-assets-final-tfvOH9` contains HTML/CSS/`app.js`/
  `activity.js`/`memory.js`/`memory-presentation.js` at
  8,641/16,918/24,428/23,616/39,220/38,754 bytes. Their SHA-256 values are
  `b623c37588ef24fd14f501ce2451c44f04c4f7ea835703d591f2a509af5853cb`,
  `381d7e04d6512a830f6d535080baf756f22add8e91a44238908f5bbd2bda8c65`,
  `1c70c67853baa5f484f810e35efd97129cbe8bd1ef9040c95604a47b7a42d376`,
  `33a528afba5b31998778d6ac2b58c299cd62592364e4d069e1f29eea95300218`,
  `59a13dd20c7282deec2231cf36190cc333629fb68a9e8fe584f1101ee5ff9e08`,
  and `7f3552f76e12a779ff9af04c1a20b4e18bd26f43f7455ec07a332f3882fc9e2b`.
  The fixed shell is 49,987 bytes and every asset remains below 65,536 bytes.
  The accepted asset log has SHA-256
  `fd0ba5ae446d6ae1134ebb344956a50da85a86633d4f81c338f0791e2da62024`.
  The independent presentation oracle passed 16 strict schema/coherence cases
  without retaining authority; its log has SHA-256
  `0ba9b5cd7f4115dc49a0bf99b36bff3486e1831421cd602dd6ccefdf9e0824db`.

  Accepted browser root
  `/tmp/lethebot-gov-ux-01c8g-browser-final-fWHAs3` proves all seven preview
  states plus fresh handles, malformed/coherence rejection, exact independent
  sequencing, cancellation, expiry, navigation, session, and logout handling.
  Desktop and mobile each made 13 exact approval-preview POSTs, had zero
  unexpected errors, no overflow, no handle disclosure, empty storage, and no
  confirmation request or durable write. Their canonical structures share
  SHA-256
  `95a964eb036d9549fe87ef9c8ad3535bd3644834ef00a950979efe263ee48023`;
  desktop/mobile screenshots have SHA-256
  `779ea7a1b853f57ec2e05da0f2dfcac6ca6f61d12c8135ba5e2dfb313d8262d1`
  and `dbd18b721d983bad85aaed20de5c0331b9181030fd7013dccc28a9defc41a708`.
  The accepted browser log has SHA-256
  `c35ec10a9058eb335bc449c9adb891006257f8847e66f6718076c7a1a79de04e`.

  The accepted patch has SHA-256
  `99c503bf3aca40968690b902b8a0a88d2eade1e1efa9608aa8d9ffffcfbb2ef1`,
  mode `0600`, and size `28373`; its mode-`0600` proof log has SHA-256
  `f581f3f54837f0bd4ac423d2053359f8a441a6324679eb791fa4c4fc4d66f0d1`.
  Reverse-first and forward-first cycles passed 6 / 6 byte/mode checks, and
  candidate/workspace equality passed 6 / 6. Final hygiene remains 74 tracked
  modifications, 60 untracked paths, and zero staged at `main@cfebb77`; the
  patch is ASCII-only and the sensitive untracked filename scan is empty. Pi
  remains `0.80.2`; lockfile and migration 007/008 SHA-256 values remain
  `f727570a279194362333b938822a2c7699853b1f7977c68e064fae8e8444fc17`,
  `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`,
  and `95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`.
  No confirmation, handle consumption, review transition, memory application,
  rollback, backend, CSS, persistence, schema, migration, dependency, lockfile,
  CLI/QQ, Provider, private/live, deployment, restart, or container behavior
  changed.
- `exact_next_action`: completed; the post-preview P8 reassessment selects
  `GOV-UX-01C8H`, the matching explicit browser approval confirmation through
  existing B7D authority, while leaving rejection and memory application absent.

## Completed Slice: `GOV-UX-01C8H`

- `requirement`: `GOV-UX-01C8H` exact-scope browser maintenance-approval
  confirmation workflow.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: C8G renders a complete write-free approval preview, and B7D
  already owns its exact single-use confirmation, but the browser deliberately
  discards the preview handle and exposes no explicit second action. Operators
  therefore cannot complete even the approval state transition from the local
  browser.
- `selection_reason`: the matching approval confirmation is the smallest
  dependency-complete mutation after C8G. It reuses the same current Review
  scope/resource/session/CSRF authority and existing shared governance owner,
  completes one preview/confirm pair, and does not introduce rejection, memory
  application, rollback, or a new backend contract.
- `implementation_contract`: after one coherent unexpired populated C8G preview,
  expose one explicit `Confirm approval` control. Split the already-validated
  response into the safe rendered preview and one private transient confirmation
  authority containing only the opaque preview handle, expiry, and current
  selection binding. Keep that authority outside DOM, attributes, storage,
  announcements, logs, exported feature state, and the presentation value;
  discard the digest as before. Clear the authority on every C8G invalidation
  boundary and before sending confirmation so double activation cannot reuse it.

  The Memory controller sends exactly one same-origin `POST` to the current
  list-issued
  `/governance/api/v1/memory-reviews/:resourceHandle/confirm` path through the
  existing shell transport, with the current Review scope, in-memory CSRF, JSON
  content type, browser-derived Origin, no query, and exact body
  `{ "confirm": true, "previewHandle": HANDLE }`. Never derive the path from
  displayed proposal evidence or reuse Records authority. A missing, expired,
  drifted, already-submitted, or noncurrent preview cannot enable the command.

  Validate the exact B7D `200` response before rendering: canonical approval
  action and approved outcome; proposal reference coherent with row/detail/
  preview; current `approved` state at the preview's expected revision; exact
  approve transition with bounded revision and audit references; explicit false
  memory-record mutation; and the unchanged unsupported rollback boundary.
  Reject unknown keys, invalid values/references/revisions, or any preview/result
  drift. Render only the bounded outcome, state/revision, redacted evidence
  references, memory-record non-effect, and rollback boundary through safe DOM
  construction.

  Own independent confirming, succeeded, malformed, unavailable, not-found, and
  conflict states; suppress late responses and disable both preview and confirm
  commands in flight. Success invalidates the selected row/detail/preview
  authorities and refreshes the Review queue after preserving the bounded result.
  Every failure requires a fresh preview before another confirmation attempt.
  Preserve the server's consume-before-freshness, one-winner, `404`, and `409`
  behavior. Add no rejection/expiration/application/rollback browser command,
  memory-record mutation, backend route/DTO, direct database/service call, CSS,
  package, dependency, polling, or build step. Keep every served asset below
  65,536 bytes.
- `allowed_paths`: `src/http/governance-browser-assets.ts`,
  `tests/integration/governance-http.test.ts`, `docs/architecture.md`,
  `docs/deployment.md`, `docs/governance-ui.md`, `docs/security-privacy.md`, and
  this checkpoint only.
- `protected_paths`: all C1-C8G navigation, presentation, preview, authority-
  isolation, responsive, focus, and stale-response behavior; B7C/B7D routes,
  DTOs, registries, session/CSRF semantics, consume-before-freshness, shared
  governance transaction/audit behavior, and every other mutation family;
  `GovernanceHttpServer`, `src/index.ts`, service, query, repository, CLI/QQ,
  config/env, schema, migrations, dependencies/lockfile, Provider, private/live
  data, deployment/restart/container behavior, and every prior slice.
- `assertions`: unchanged C8G/C8F/C8E/C8D/C8C/C8B/C8A/C7C/fixed-shell
  characterizations pass before editing. A named fail-first proves confirmation
  absence, then final direct/browser coverage proves explicit second activation,
  exact path/headers/body, private single-use authority, strict B7D schema and
  cross-stage coherence, duplicate suppression, success refresh, malformed/
  unavailable/not-found/conflict outcomes, scope/resource/selection drift,
  cancellation, expiry, navigation, session, and logout clearing. Existing B7D
  wiring proves exactly one shared approval revision/audit, immediate state
  change, consumed/stale conflict, unchanged memory records, and no apply or
  rollback. Handles never render or persist; rejection and application requests
  remain absent; Records, Review, Activity, focus, and responsive structures stay
  exact on desktop and mobile.
- `focused_commands`: unchanged C8G/C8F/C8E/C8D/C8C/C8B/C8A/C7C/fixed-shell
  characterizations; named C8H fail-first/final; full governance HTTP; B7D
  wiring and four-file HTTP/application adjacency; canonical P8; typecheck;
  scoped/full ESLint; build/preflight; normal-umask release; all four JavaScript
  parsers and complete synthetic desktop/mobile regression oracles; diff,
  inventory, dependency/migration/privacy hygiene; and exact six-path reverse-
  first/forward-first rollback proof.
- `schema_dependency_live_boundary`: existing B7D approval transition reached
  through fixed browser transport/state/presentation; synthetic tests; four
  canonical browser/security documents; and checkpoint evidence only. No new
  backend authority, memory-record mutation, apply/rollback, schema, migration,
  dependency, lockfile, CLI/QQ, Provider, private DB, deployment, restart,
  container, or live operation.
- `rollback_boundary`: private mode-`0700`
  `/tmp/lethebot-gov-ux-01c8h-roundtrip-qeFBGnnx` holds the accepted exact six-
  product-path zero-context ASCII patch. The checkpoint is outside that patch;
  independent reverse-first and forward-first proof trees leave the workspace
  untouched.
- `baseline_evidence`: private mode-`0700`
  `/tmp/lethebot-gov-ux-01c8h-baseline-mh2_qad7` captured all seven allowed
  C8G paths, `/tmp/lethebot-gov-ux-01c8h-assets-baseline-zYiSrD` captured the
  six served assets, and
  `/tmp/lethebot-gov-ux-01c8h-browser-baseline-9nvlef93` captured the accepted
  desktop/mobile C8G runtime. Nine unchanged predecessor characterizations
  passed before the named C8H regression was added.
- `fail_first_evidence`: the named regression
  `confirms a coherent current-review approval through private browser authority`
  failed only on the absent confirmation suffix before product implementation;
  the predecessor characterizations remained green.
- `final_evidence`: the refreshed named C8H regression passed 1 / 1, full
  governance HTTP passed 37 / 37, four-file HTTP/application/B7D adjacency
  passed 83 / 83, and the canonical P8 command passed 7 files / 345 tests.
  Their private mode-`0600` logs are under
  `/tmp/lethebot-gov-ux-01c8h-final-logs`; the named, HTTP, adjacency, and P8
  SHA-256 values are
  `c2a065b1c4384eedd183156b507225c2bc0cf765926a1f7f2e992b3c562450f6`,
  `23d6829867c19b8722cf37f29fe06536da816d6ec374a3023f6bb1f33e586100`,
  `890d0d132960d35bced6a31ae4bd1a6750c0154e91ab3903ff026c1710ed9e1c`,
  and `7c6ee8b23a332fa6114dc599001780555fbb2ee55f6d5c19938d089f86895159`.
  Typecheck, scoped/full ESLint, build, preflight, all four JavaScript parsers,
  and `git diff --check` passed. Fresh normal-umask `pnpm release:check` passed
  137 / 1 skipped files with 2,850 / 10 skipped tests; its log SHA-256 is
  `2b1e547a3937a9541e6edb414b2ebbf977e16cf69f6ca240b93c072c5bd4f053`.

  Final private mode-`0700` asset snapshot
  `/tmp/lethebot-gov-ux-01c8h-assets-final-mygeO8R9` contains HTML/CSS/`app.js`/
  `activity.js`/`memory.js`/`memory-presentation.js` at
  8,641/16,918/24,428/23,616/46,670/41,758 bytes. Their SHA-256 values are
  `b623c37588ef24fd14f501ce2451c44f04c4f7ea835703d591f2a509af5853cb`,
  `381d7e04d6512a830f6d535080baf756f22add8e91a44238908f5bbd2bda8c65`,
  `1c70c67853baa5f484f810e35efd97129cbe8bd1ef9040c95604a47b7a42d376`,
  `33a528afba5b31998778d6ac2b58c299cd62592364e4d069e1f29eea95300218`,
  `1309ae1120056c3b64cc6a5c5c99ce61dda013a03184c813d19191d4d228a97c`,
  and `83f3c2000710a7ea1702be680ec0c9317d10ab892f614afb2edbcd93432a07c0`.
  The fixed shell remains 49,987 bytes, every asset remains below 65,536 bytes,
  and the extracted Memory definition union remains 87,703 bytes with SHA-256
  `7ffbe4f3e32f14bd1307504257176f33359a4c13cedd61f502d12c724dc9e185`.

  C8G preview regression root
  `/tmp/lethebot-gov-ux-01c8h-preview-final-yhmlOV8Z` proves desktop/mobile each
  retained 13 exact preview POSTs, no confirmation or durable write, zero
  unexpected errors, no overflow, hidden handles, and empty storage. C8H browser
  root `/tmp/lethebot-gov-ux-01c8h-browser-final-5UWPCaT5` proves desktop/mobile
  each made 11 exact preview POSTs and 6 exact confirmation POSTs, suppressed
  duplicate activation, exercised success plus every failure/invalidation
  boundary, refreshed Review after success, made no rejection/application
  request, and retained zero handle/storage leakage or unexpected error. Both
  C8H structures have SHA-256
  `06221b8b7accd8ac9b40b38303fed8087605a573f1aa1d9827040e9a9a1be6c2`.
  The independent presentation oracle passed 16 preview and 15 confirmation
  schema/coherence cases. Preview-browser, C8H-browser, and presentation log
  SHA-256 values are
  `74a23d2272e41c480d9ac241f8542b09830f85618c91c4573684632b4208ecc3`,
  `6573a4c0804103c2eeabb84dd2c06e696767b59f7cd9053f85b5437931382aba`,
  and `545a71beff00ad752a0bebb6ade0bc40270bc50c7c1a21d1602daf7234211dc7`.

  The accepted patch has SHA-256
  `253e728ab96e3e75a9917563df89f543e62328c52561fe1b92e26acc4db2887f`,
  mode `0600`, and size 29,013; reverse-first and forward-first cycles each
  restored 6 / 6 exact bytes and modes, and candidate/workspace equality passed
  6 / 6. Inventory remains 74 tracked modifications, 60 untracked paths, and
  zero staged at `main@cfebb77`; the sensitive untracked filename scan is empty,
  the patch is ASCII-only, Pi remains `0.80.2`, and lockfile/migration 007/008
  SHA-256 values remain
  `f727570a279194362333b938822a2c7699853b1f7977c68e064fae8e8444fc17`,
  `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`,
  and `95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`.
  One over-broad local text search during final hygiene unintentionally printed
  matching lines from two ignored runtime log files; it changed and retained
  nothing, those lines were not used as evidence, and every subsequent search
  was restricted to explicit source/test/document or Git-inventory paths.
  No rejection, expiration, application, rollback, memory-record mutation,
  backend, CSS, persistence, schema, migration, dependency, lockfile, CLI/QQ,
  Provider, deployment, restart, or container behavior changed.
- `exact_next_action`: completed; post-confirmation P8 reassessment selects
  `GOV-UX-01C8I`, the matching write-free browser maintenance-rejection preview
  over existing B8A authority, while leaving rejection confirmation and memory
  application absent.

## Completed Slice: `GOV-UX-01C8I`

- `requirement`: `GOV-UX-01C8I` exact-scope browser maintenance-rejection
  preview workflow.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: C8H completes the approval preview/confirmation pair, and B8A
  already owns a distinct write-free rejection preview on the same exact Review
  resource, but the browser exposes no rejection action. Operators therefore
  cannot inspect the server-owned rejected-state projection before a later
  explicit rejection confirmation.
- `selection_reason`: rejection preview is the smallest remaining dependency-
  complete browser action. It reuses the current Review scope/resource/session/
  CSRF transport, performs no durable transition, and keeps B8B confirmation,
  memory application, expiration, and rollback independently reviewable.
- `implementation_contract`: add one `Preview rejection` command only for a
  coherent current pending Review detail. Send exactly one same-origin `POST`
  through the existing shell transport to the current list-issued
  `/governance/api/v1/memory-reviews/:resourceHandle` path with the current
  Review scope, in-memory CSRF, JSON content type, browser-derived Origin, no
  query, and exact body `{ "action": "reject" }`. Never derive the path from
  displayed proposal evidence or reuse Records authority.

  Strictly validate the exact B8A `201` response before rendering: canonical
  rejection action; current scope; proposal kind/reference/effect; affected
  count/fingerprint; current `pending_review@N`; expected `rejected@(N+1)`;
  the fixed proposal-transition, proposal-revision, and audit effects; explicit
  unavailable memory-record mutation; rejection-specific unsupported rollback
  boundary; opaque preview handle; canonical digest; and safe future expiry.
  Require row/detail/response coherence, exact keys and domains, bounded safe
  counts/revisions, and valid references/fingerprint/handle/digest/timestamp.

  The presentation module owns the rejection-preview schema, normalization,
  coherence, formatting, and safe DOM renderer. The Memory controller owns the
  endpoint action, command, current Review authority, independent request
  sequence, and explicit unrequested, loading, populated, malformed,
  unavailable, not-found, and stale states. Suppress duplicate/late requests.
  Starting a rejection preview clears any approval confirmation authority before
  transport; selection, scope, list/detail refresh, subview, navigation, expiry,
  session, and logout changes clear rejection state and any competing approval
  authority. Validate then discard the rejection handle and digest: C8I adds no
  rejection-confirm control or request and retains no rejection authority.
  Render only bounded redacted action/effect/current/expected/rollback evidence
  and expiry through safe DOM construction.

  Preserve the complete C8H approval flow, including single-use approval
  authority, its six confirmation states, success refresh, and bounded result;
  a fresh coherent approval preview can establish new approval authority after
  rejection-preview invalidation. Add no rejection transition, confirmation,
  expiration, application, rollback, memory-record mutation, backend route/DTO,
  direct database/service call, CSS, package, dependency, polling, or build
  step. Keep every served asset below 65,536 bytes.
- `allowed_paths`: `src/http/governance-browser-assets.ts`,
  `tests/integration/governance-http.test.ts`, `docs/architecture.md`,
  `docs/deployment.md`, `docs/governance-ui.md`, `docs/security-privacy.md`, and
  this checkpoint only.
- `protected_paths`: all C1-C8H navigation, presentation, approval preview/
  confirmation, authority isolation, responsive, focus, refresh, and stale-
  response behavior; B8A/B8B routes, DTOs, registries, session/CSRF semantics,
  shared governance behavior, and every other mutation family;
  `GovernanceHttpServer`, `src/index.ts`, service, query, repository, CLI/QQ,
  config/env, schema, migrations, dependencies/lockfile, Provider, ignored
  runtime/private data, deployment/restart/container behavior, and every prior
  slice.
- `assertions`: unchanged C8H/C8G/C8F/C8E/C8D/C8C/C8B/C8A/C7C/fixed-shell
  characterizations pass before editing. A named fail-first proves rejection-
  preview absence. Final direct/browser coverage proves the exact path/headers/
  body, strict B8A schema and cross-stage coherence, seven states, independent
  sequencing, duplicate suppression, malformed/unavailable/not-found/stale
  outcomes, scope/resource/selection drift, cancellation, expiry, navigation,
  session, and logout clearing. Rejection handles/digests never render or
  persist, approval authority cannot survive a competing rejection preview, no
  rejection confirmation or durable mutation occurs, and the full approval,
  Records, Review, Activity, focus, and responsive matrices remain exact on
  desktop and mobile.
- `focused_commands`: unchanged C8H/C8G/C8F/C8E/C8D/C8C/C8B/C8A/C7C/fixed-
  shell characterizations; named C8I fail-first/final; full governance HTTP;
  B8A wiring and four-file HTTP/application adjacency; canonical P8; typecheck;
  scoped/full ESLint; build/preflight; normal-umask release; all four JavaScript
  parsers and complete synthetic desktop/mobile regression oracles; diff,
  inventory, dependency/migration/privacy hygiene; and exact six-path reverse-
  first/forward-first rollback proof.
- `schema_dependency_live_boundary`: existing B8A rejection preview reached
  through fixed browser transport/state/presentation; synthetic tests; four
  canonical browser/security documents; and checkpoint evidence only. No new
  backend authority, preview confirmation/consumption, durable write, schema,
  migration, dependency, lockfile, CLI/QQ, Provider, database, deployment,
  restart, container, or live operation.
- `rollback_boundary`: private mode-`0700`
  `/tmp/lethebot-gov-ux-01c8i-roundtrip-BlEFIiKH` holds the accepted exact six-
  product-path zero-context ASCII patch. The checkpoint remains outside that
  patch; independent reverse-first and forward-first proof trees leave the
  workspace untouched.
- `baseline_evidence`: private mode-`0700`
  `/tmp/lethebot-gov-ux-01c8i-baseline-TIEA7BwW` captured all seven allowed C8H
  paths, `/tmp/lethebot-gov-ux-01c8i-assets-baseline-AAKBQ4yP` captured all six
  served assets, and
  `/tmp/lethebot-gov-ux-01c8i-browser-baseline-AESRy1SC` captured the accepted
  desktop/mobile C8H runtime. Ten protected predecessor characterizations
  passed before the named C8I regression was added; the characterization log
  SHA-256 is
  `30aeeeedda4449ada1104ec00cd8f55b219cf387ed864f9c63e767f5869df45d`.
- `fail_first_evidence`: the named regression
  `previews a coherent current-review rejection without retaining confirmation
  authority` failed only on the absent rejection-preview controller before
  product implementation. Its log SHA-256 is
  `478706ca8701943c55a0d0c88bb70c4634f535f5ffbf7f7aed43fbd36bde255a`.
- `final_evidence`: the refreshed named C8I regression passed 1 / 1, full
  governance HTTP passed 38 / 38, four-file HTTP/application/B8A adjacency
  passed 84 / 84, and canonical P8 passed 7 files / 346 tests. Their private
  mode-`0600` log SHA-256 values are
  `e15cb6333f4e83479defde2dfa57fba9e7bc4766f48458f67a2820918cd80644`,
  `c8444d42118bca12e4659ab4593dbc21027abf549f83e29441bd7fe2f7815c05`,
  `c1434c6e5c9722b0525f48c87124b36415b65a4fab5a3f23493ee6c5adb32c61`,
  and `d5dce4c3f65bd604029b0b928c86e64a0aba1833283ca929d16cb7ff75f74ef5`.
  Typecheck, scoped/full ESLint, build, preflight, all four JavaScript parsers,
  and `git diff --check` passed. Fresh normal-umask `pnpm release:check` passed
  137 / 1 skipped files with 2,851 / 10 skipped tests; its log SHA-256 is
  `9340ff7e7fc70cd26b1d5b45b58bcce00e591665130c7fdbb22a493b1c8b2a41`.

  Final private mode-`0700` asset snapshot
  `/tmp/lethebot-gov-ux-01c8i-assets-final-wdzdc8wa` contains HTML/CSS/`app.js`/
  `activity.js`/`memory.js`/`memory-presentation.js` at
  8,641/16,918/24,428/23,616/53,035/46,484 bytes. Their SHA-256 values are
  `b623c37588ef24fd14f501ce2451c44f04c4f7ea835703d591f2a509af5853cb`,
  `381d7e04d6512a830f6d535080baf756f22add8e91a44238908f5bbd2bda8c65`,
  `1c70c67853baa5f484f810e35efd97129cbe8bd1ef9040c95604a47b7a42d376`,
  `33a528afba5b31998778d6ac2b58c299cd62592364e4d069e1f29eea95300218`,
  `64aeeecef021e9c126610209eceed867c351e5a479040be74948aeb5c4b8d864`,
  and `5b0b03f58583c7c37dde157e2191aa4c4f236bd1df45fcb2cedfcce6788a94b3`.
  The fixed shell remains 49,987 bytes, every asset remains below 65,536 bytes,
  and the reconstructed Memory definition union is 98,704 bytes with SHA-256
  `13b185e88f4f1a56f043b4dcb1241dab5e8ff353c4f63733aca88d7df0a24461`.
  Asset and parser log SHA-256 values are
  `f56dbb6480c0d980901152f9e95e6a206680d72bd9eb803df75a04d3b16e7bbc`
  and `5b21919c5299f42a6e32818a2e90703680a277953f5cb3506f3aefaf1e5f8bcf`.

  C8G regression root `/tmp/lethebot-gov-ux-01c8i-c8g-final-VdZGkjLm`
  proves desktop/mobile each retained 13 exact approval-preview POSTs with no
  confirmation or durable write. C8H regression root
  `/tmp/lethebot-gov-ux-01c8i-c8h-final-dv3nt3hB` proves each retained 11 exact
  approval previews and 6 exact confirmations with no rejection/application
  request. Their structures have SHA-256 values
  `45bdcfabbb7e66d220182d2220915e800a4ba9a6657007b2310e1f075a67a03c`
  and `175ad93ecd77a2f6512972c46fa075c66f6a0655649e5a2f1a1029562ebfa0b9`.

  C8I browser root `/tmp/lethebot-gov-ux-01c8i-browser-final-HXrfCeYy`
  proves desktop/mobile each exercised all seven rejection-preview states,
  made 14 exact rejection and 3 exact approval-preview POSTs, suppressed
  duplicate/late responses, covered expiry plus scope/resource/navigation/
  refresh/session/logout invalidation, cleared competing approval authority,
  and re-established fresh approval authority. Both viewports made zero
  confirmation/application/durable-write request and retained zero rejection
  handle/digest, DOM, or storage leakage, zero overflow, and zero unexpected
  browser error. Their shared structure SHA-256 is
  `f299248271bb136fc8e3df8d050fd7c02decf684895e92908c6b4111e16c3b33`;
  desktop/mobile screenshots have SHA-256
  `9e674f9e09e9adaf687ae0bc705bd1dc56366039c6ca6e5b9021ae96eca47c2d`
  and `fbe924fb9fa7efc072336859f04f6ac6a7bdf343212443165ed8b274f078a844`.
  The independent presentation oracle passed 16 approval-preview, 15 approval-
  confirmation, and 18 rejection-preview strict schema/coherence cases without
  retaining rejection authority. C8G, C8H, presentation, and C8I browser log
  SHA-256 values are
  `b20bc3e1e009799ca2253b8576c5238ce7320cc780df5bbf587a4b61df8b2942`,
  `fef636048f0f34434ca329d5bd458f83070f1703355b943d620ff45d6390e5fd`,
  `f06b1271cdd5267483ae59e17917abb1209de4d3bf3bfe90b1f6000fe2d8642f`,
  and `435e8c275eb326aaa7250c3a3958c60a8ca53466e300ce6d3dcb9bfdc89d8ac8`.

  The accepted patch has SHA-256
  `6c2d3e1b75fdadeed7971d21e33f7503ffae5dacabe24a8714ddd982b8988142`,
  mode `0600`, and size 25,921; reverse-first and forward-first cycles each
  restored 6 / 6 exact bytes and modes, and candidate/workspace equality passed
  6 / 6. Inventory remains 74 tracked modifications, 60 untracked paths, and
  zero staged at `main@cfebb77`; the generated Vitest config-bundle artifact was
  removed without touching the inherited inventory. The sensitive untracked
  filename scan is empty, the patch is ASCII-only, Pi remains `0.80.2`, and
  lockfile/migration 007/008 SHA-256 values remain
  `f727570a279194362333b938822a2c7699853b1f7977c68e064fae8e8444fc17`,
  `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`,
  and `95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`.
  No rejection confirmation, handle consumption, review transition,
  expiration, application, rollback, memory-record mutation, backend, CSS,
  persistence, schema, migration, dependency, lockfile, CLI/QQ, Provider,
  private/live, deployment, restart, or container behavior changed.
- `exact_next_action`: completed; post-preview P8 reassessment selects
  `GOV-UX-01C8J`, the matching explicit browser rejection confirmation through
  existing B8B authority, while leaving memory application absent.

## Active Slice: `GOV-UX-01C8J`

- `requirement`: `GOV-UX-01C8J` exact-scope browser maintenance-rejection
  confirmation workflow.
- `status`: `DETERMINISTIC_VERIFIED`.
- `observed_gap`: C8I renders the complete write-free rejection projection, and
  B8B already owns its exact single-use confirmation, but the browser discards
  rejection preview authority and exposes no explicit second action. Operators
  therefore cannot complete the rejected-state transition from the browser.
- `selection_reason`: rejection confirmation is the smallest dependency-
  complete action after C8I. It reuses the current Review scope/resource/
  session/CSRF transport and existing B8B transition, completes the second
  preview/confirm pair, and leaves memory application independently reviewable.
- `implementation_contract`: after one coherent unexpired populated C8I
  preview, expose one explicit `Confirm rejection` control. Split the already-
  validated response into the safe rendered projection and one private
  transient rejection authority containing only the opaque preview handle,
  expiry, and current Review selection binding. Keep it outside presentation
  values, DOM, attributes, storage, announcements, logs, and exported feature
  state; continue discarding the digest. Clear it on every C8I invalidation,
  when a competing approval preview starts, and before confirmation transport.

  Send exactly one same-origin `POST` through the existing shell transport to
  the current list-issued
  `/governance/api/v1/memory-reviews/:resourceHandle/confirm` path with current
  Review scope, in-memory CSRF, JSON content type, browser-derived Origin, no
  query, and exact body
  `{ "confirm": true, "previewHandle": HANDLE, "action": "reject" }`. Never
  infer the path from displayed evidence or reuse Records/approval authority.
  Missing, expired, drifted, submitted, or noncurrent authority cannot enable
  the command.

  Strictly validate the exact B8B `200` response before rendering: canonical
  rejection action and rejected outcome; proposal reference coherent with row,
  detail, and preview; current `rejected` state at the preview's expected
  revision; exact reject transition with bounded revision/audit references;
  explicit false memory-record mutation; and the rejection-specific unsupported
  rollback boundary. Reject unknown keys, invalid values/references/revisions,
  or cross-stage drift. Render only bounded outcome, state/revision, redacted
  evidence references, memory-record non-effect, and rollback boundary.

  Own independent confirming, succeeded, malformed, unavailable, not-found,
  and conflict states; suppress late responses and disable competing controls
  while confirmation is in flight. Every failure consumes local rejection
  authority and requires a fresh preview. Success clears row/detail/preview/
  confirmation authority, preserves only bounded result evidence, and refreshes
  Review. Preserve the complete C8H approval flow and B8B consume-before-
  freshness behavior. Add no memory application, expiration, rollback,
  memory-record mutation, backend route/DTO, direct database/service call, CSS,
  package, dependency, polling, or build step. Keep every served asset below
  65,536 bytes.
- `allowed_paths`: `src/http/governance-browser-assets.ts`,
  `tests/integration/governance-http.test.ts`, `docs/architecture.md`,
  `docs/deployment.md`, `docs/governance-ui.md`, `docs/security-privacy.md`, and
  this checkpoint only.
- `protected_paths`: all C1-C8I navigation, presentation, approval and rejection
  preview behavior, approval confirmation, authority isolation, responsive,
  focus, refresh, and stale-response behavior; B8A/B8B routes, DTOs, registries,
  session/CSRF semantics, consume-before-freshness, shared governance behavior,
  and every other mutation family; `GovernanceHttpServer`, `src/index.ts`,
  service, query, repository, CLI/QQ, config/env, schema, migrations,
  dependencies/lockfile, Provider, private/live data, deployment/restart/
  container behavior, and every prior slice.
- `assertions`: unchanged C8I/C8H/C8G/C8F/C8E/C8D/C8C/C8B/C8A/C7C/fixed-shell
  characterizations pass before editing. A named fail-first proves rejection-
  confirmation absence. Final direct/browser coverage proves explicit second
  activation, exact path/headers/body, private single-use purpose-bound
  authority, strict B8B schema/coherence, duplicate suppression, success
  refresh, malformed/unavailable/not-found/conflict outcomes, scope/resource/
  selection drift, cancellation, expiry, navigation, session, and logout
  clearing. Existing B8B wiring continues to prove one shared rejection
  revision/audit, unchanged memory records, and no application. Approval and
  rejection authority cannot cross or coexist; handles never render or persist;
  Records, Review, Activity, focus, and responsive matrices remain exact.
- `focused_commands`: unchanged C8I/C8H/C8G/C8F/C8E/C8D/C8C/C8B/C8A/C7C/
  fixed-shell characterizations; named C8J fail-first/final; full governance
  HTTP; B8B wiring and four-file HTTP/application adjacency; canonical P8;
  typecheck; scoped/full ESLint; build/preflight; normal-umask release; all four
  JavaScript parsers and complete synthetic desktop/mobile regression oracles;
  diff, inventory, dependency/migration/privacy hygiene; and exact six-path
  reverse-first/forward-first rollback proof.
- `schema_dependency_live_boundary`: existing B8B rejection transition reached
  through fixed browser transport/state/presentation; synthetic tests; four
  canonical browser/security documents; and checkpoint evidence only. No new
  backend authority, memory application/rollback, schema, migration,
  dependency, lockfile, CLI/QQ, Provider, private DB, deployment, restart,
  container, or live operation.
- `rollback_boundary`: after a clean diff check, capture exact C8I bytes and
  modes for browser source, integration test, four canonical documents, and
  checkpoint under one new private mode-`0700` root. The final six-product-path
  ASCII patch must independently prove reverse-first and forward-first exact
  byte/mode restoration without touching the workspace; checkpoint remains
  outside the patch.
- `verification_evidence`: the served controller/presentation characterizations and
  native Chromium/CDP runtime test exercise the real `/governance/memory.js`
  module and DOM. Five fresh rejection previews produce exact reject bodies,
  each second activation sends the exact scope/resource-bound `/confirm` body,
  duplicate activation is suppressed, unavailable/404/409/malformed/succeeded
  states render independently, every failure requires a fresh preview, success
  clears the selected Review state, and the opaque handle is absent from rendered
  text. Final HTML/CSS/`app.js`/`activity.js`/`memory.js`/
  `memory-presentation.js` bodies are
  8,641/16,918/24,428/23,616/59,235/49,111 bytes, each below 65,536; the
  107,426-byte definition union has SHA-256
  `11f508f6d8b7b224ace17a75cb1a36c021d8ef2670e6aa561c12e22cb3384a9c`.
  Existing B8B HTTP/application wiring continues to prove action-bound
  single-use consumption, one rejected proposal revision/audit, unchanged memory
  records, and no application. Full governance HTTP/wiring passed 79 / 79 tests.
  `pnpm release:check` passed typecheck, full ESLint, build, release preflight,
  137 passed / 1 skipped files, 2,853 passed / 10 skipped tests, and diff hygiene.
  The worktree remains 74 tracked modifications plus 60 untracked paths, with
  zero staged paths at `main@cfebb77`.
- `exact_next_action`: completed; perform the post-C8J P8 critical-path
  reassessment before selecting any browser memory-application, rollback, or
  expiration slice. Live Provider/QQ, deployment/restart, and 72-hour soak
  evidence remain separately authorization-bound.

## Active Slice: `GOV-UX-01C9B`

- `requirement`: exact-scope browser maintenance-application preview and explicit
  confirmation over existing B9A/B9B.
- `status`: `DETERMINISTIC_VERIFIED`.
- `observed_gap`: C9A exposed a strict write-free application preview but had no
  browser control for consuming the existing B9B confirmation authority.
- `implementation_contract`: keep preview available only for coherent `approved`
  Review detail. Consolidation/decay send exact `{ "action": "apply" }`; conflict
  requires a non-default current public `memoryRef` and adds only
  `retainedMemoryRef`. A successful strict B9A projection creates private,
  action-bound authority tied to the current session, scope/resource, selected
  detail, approved/current and expected revision, expiry, and conflict selection.
  Confirmation sends exact `{ "confirm": true, "previewHandle": HANDLE,
  "action": "apply" }`, adding only the same conflict `retainedMemoryRef` when
  required. Revalidate every binding before dispatch, clear the authority and
  timer before one request, strictly match the bounded B9B result to the preview,
  preserve only redacted result evidence, and refresh Review on success. Expose
  no handle/digest/raw identifier and no direct rollback.
- `implementation_evidence`: application preview and confirmation live in the
  dedicated same-origin `/governance/memory-application.js` workflow, injected by
  the Memory controller and sharing only bounded DOM/transport/state callbacks.
  It owns independent request sequences, expiry, in-flight state, and authority;
  approval, rejection, and application controls mutually suppress competing
  work. Confirmation exposes `confirming`, `succeeded`, `malformed`,
  `unavailable`, `not-found`, and `conflict`; every failure requires a fresh
  preview. Fixed sidecar source files are loaded once from a filename allowlist
  and copied into `dist/http/governance-browser-assets/` during build, with no
  request-time filesystem lookup or external runtime dependency.
- `verification_evidence`: governance browser static/native-Chromium coverage is
  43 / 43 tests and governance HTTP plus wiring is 82 / 82. It proves exact
  conflict preview/confirm bodies, current-binding revalidation, double-activation
  suppression, fixed 503/404/409 states, unknown-key rejection, strict success,
  Review refresh, result preservation, and no preview-handle DOM text. Typecheck,
  scoped ESLint, JavaScript syntax checks, and an isolated temporary build-layout
  import passed. Served `/governance/`, `memory.js`, `memory-application.js`, and
  `memory-presentation.js` are 8,641 / 61,270 / 13,041 / 57,993 bytes, all below
  65,536. The 131,178-byte Memory definition union SHA-256 is
  `c18bbeb4b341b4c888a9f1ef4eef6302170443f264416dada1c92a31d0775e4f`.
  Typecheck, full ESLint (including an explicit browser-sidecar environment),
  JavaScript syntax checks, release preflight, 137 passed / 1 skipped files with
  2,856 passed / 10 skipped tests, and repository diff hygiene passed. An
  isolated temporary build-layout import also passed. A repository-local
  `pnpm build` and complete `pnpm release:check` are not claimed: the available
  permission boundary rejected overwriting pre-existing `dist`, so only the
  isolated build-layout check ran.
- `rollback_boundary`: remove the dedicated workflow registration/import and
  build copy step to restore the C9A browser boundary; B9A/B9B routes, DTOs,
  registry, query/service mutation semantics, schema, migrations, dependencies,
  CLI/QQ, and existing durable application effects are unchanged.
- `exact_next_action`: perform the post-C9B P8 critical-path reassessment. B10
  rollback, B11 expiration, later mutation browser views, retention prerequisites,
  complete accessibility QA, live Provider/QQ, deployment/restart, and soak
  evidence remain open and separately authorization-bound.

## Baseline

| Time | Check | Scope | Result |
|---|---|---|---|
| 2026-07-12 18:03 CST | worktree/HEAD inventory | protected current workspace | 155 tracked-dirty, 51 untracked; `main@10b5db588f48` |
| 2026-07-12 18:04 CST | `pnpm release:check` | typecheck, lint, build, preflight, deterministic tests, diff hygiene | exit 0; 89 passed / 1 skipped files, 1714 passed / 10 skipped tests |
| 2026-07-12 18:03 CST | Compose/runtime inspection | current Framework stack | LetheBot and SnowLuma healthy with zero restarts |
| 2026-07-12 18:06 CST | schema-v1 baseline image | complete current build context | image `sha256:8406929513b542ce5bf0d484a94ae09b02ad03e9f5dd6cbd58b5b24506ca0872`; schema target/readable range `1/1..1` |
| 2026-07-12 18:07 CST | immutable v1 image export | private neutral `/tmp` artifact | tar SHA256 `6b1495911be431e67931eca70f215dbf4ea657ecc11e81e189b8271ca9dd8e81`; mode `0600` |
| 2026-07-12 18:18 CST | `pnpm release:check` | post target-v1 sequential migration bridge | exit 0; 90 passed / 1 skipped files, 1727 passed / 10 skipped tests |
| 2026-07-12 22:56 CST | focused rollback/gate suite | journal v2, startup gate, release controller/CLI, deployment assets | exit 0; 4 files, 137 tests passed |
| 2026-07-12 22:59 CST | `pnpm release:check` | post rollback/startup/dependency hardening | exit 0; 91 passed / 1 skipped files, 1778 passed / 10 skipped tests |
| 2026-07-12 23:18 CST | frozen-v1 cross-version rollback rehearsal | immutable v1 image process, synthetic distinct schema-v2 candidate, disposable DB/root | exit 0; v2 observed then readiness rejected; DB/schema/metadata restored; frozen v1 restarted ready; operation artifacts removed |
| 2026-07-12 23:28 CST | frozen-v1 managed release bundle | current green v1 `dist`, 001 migration, manifest, lockfile, and release-local dependencies | managed digest `dea6333ca5ebb6f1fa888cb37c1f77f9978755c19b97dc94da28c403de0f4491`; private tar SHA256 `443c1d9843d9a26784cdbe4bd0d6c3e1cb46ae60849ca93ab382ebe3ec524efc` |
| 2026-07-13 00:22 CST | schema-v2 focused gate | sequential runner, migration/preflight, release lifecycle/CLI, static checks | exit 0; 6 files / 198 tests, typecheck and scoped lint passed |
| 2026-07-13 00:23 CST | final v2 managed release bundle | built current release with 001/002, exact 2/1..2 contract, lockfile, dependencies | managed digest `cf23e670ad5d7cfcf6aece256f37fd9372b62b8eeebe016dc2b25fa76c810a11`; private tar SHA256 `0680ea2ecb83acccc9c1ba7a00874cbded85c4602a154f4dc490c20af7ff0bcf` |
| 2026-07-13 00:24 CST | real frozen-v1/final-v2 rehearsal | three disposable roots using distinct immutable managed releases | exit 0; failure rollback, pending crash recovery, wrong/exact confirmation, marker-free restart, DB metadata/integrity/FKs, and cleanup all true |
| 2026-07-13 00:54 CST | `pnpm release:check` | post background evaluator authority cutover; typecheck, lint, build, preflight, deterministic tests, diff hygiene | exit 0 |
| 2026-07-13 01:15 CST | `pnpm release:check` | post exact job-attempt lease fencing; typecheck, lint, build, preflight, deterministic tests, diff hygiene | exit 0 |
| 2026-07-13 01:23 CST | `pnpm release:check` | post lifecycle mutation authority separation and immutable create-evidence retry; full deterministic release gate | exit 0 |
| 2026-07-13 02:01 CST | `pnpm release:check` | schema-v3 evaluator invocation linkage; typecheck, lint, build, preflight, deterministic tests, diff hygiene | exit 0; 93 passed / 1 skipped files, 1844 passed / 10 skipped tests |
| 2026-07-13 02:03 CST | opt-in real Provider evaluator suite | authorized local credential; content-free invocation/decision evidence | exit 0; 10/10 tests passed; one completed evaluator invocation linked to a rejected governed-tool decision; integrity/FKs clean |
| 2026-07-13 02:07 CST | final v3 managed release bundle | built current release with 001/002/003, exact 3/1..3 contract, lockfile, dependencies | managed digest `bfdca3e068a11b12dc59074e2214de35ef15d141676e21d5daff6b450636a01f`; private tar SHA256 `02638284b196081fd2f236903ccb61dd2295ef0e2a4ee08c6e2c42cfe8e655d7` |
| 2026-07-13 02:10 CST | real frozen-v2/final-v3 rehearsal | disposable roots using distinct immutable managed releases | exit 0; readiness rollback, crash recovery, restart denial, wrong/exact confirmation, marker-free restart, v2/v3 ledger/columns, sentinel preservation, integrity/FKs, and cleanup all true |
| 2026-07-13 03:23 CST | opt-in real Provider suite | authorized local credential after memory/evaluator fixes | exit 0; 10/10 tests passed; completed evaluator invocation linked to an approved successful governed tool; integrity/FKs clean |
| 2026-07-13 03:55 CST | `pnpm release:check` | final acceptance chronology, runtime verifier dependency boundary, build/preflight, deterministic tests, diff hygiene | exit 0; 93 passed / 1 skipped files, 1876 passed / 10 skipped tests |
| 2026-07-13 03:57 CST | final Framework image/recreation | only `lethebot`; inherited reviewed runtime configuration; credential injected from authorized local file | image `sha256:02473a27ce9313ee8d5de692e073fc2808eb3228f144c5555f1589a8b21c3b0a`; post-prune verifier import passed; SnowLuma container/session identity preserved |
| 2026-07-13 04:00 CST | post-deploy runtime verification | real Framework/OneBot WS, SQLite, metrics, permissions | health `ok`; readiness `ready`; standalone pruned-image verifier passed; schema 3, integrity `ok`, FK violations 0; JSON/Prometheus metrics 200; containers healthy |
| 2026-07-13 22:58 JST | fresh read-only usability audit | online backup, last-hour aggregate runtime/DB evidence, no new QQ sends | both containers healthy; schema 3; integrity `ok`; FK violations 0; transport/storage/delivery healthy; group reliability not accepted |
| 2026-07-13 23:25 JST | `pnpm release:check` | post reliability evidence/documentation control-plane update | exit 0; typecheck, lint, build, preflight, and diff hygiene passed; 93 passed / 1 skipped files, 1876 passed / 10 skipped tests |
| 2026-07-14 03:57 CST | `pnpm release:check` | completed R5 delayed Attention plus fresh decision-clock and CLI failure-stage review fixes | exit 0; typecheck, lint, build, preflight, diff hygiene, and 97 passed / 1 skipped files with 2002 passed / 10 skipped tests |
| 2026-07-14 05:22 CST | `pnpm release:check` | integrated R6 policy/job/worker/retrieval/action core plus explicit-policy fixture recovery | exit 0; typecheck, lint, build, preflight, diff hygiene, and 101 passed / 1 skipped files with 2048 passed / 10 skipped tests |
| 2026-07-14 05:58 CST | `pnpm release:check` | final R7A reply-independent high-precision extraction admission and interrogative hardening | exit 0; typecheck, lint, build, preflight, diff hygiene, and 101 passed / 1 skipped files with 2104 passed / 10 skipped tests |
| 2026-07-14 07:00 CST | `pnpm release:check` | final R7B frozen post-budget group-summary windows, route convergence, retention pins, and terminal-window hardening | exit 0; typecheck, lint, build, preflight, diff hygiene, and 101 passed / 1 skipped files with 2119 passed / 10 skipped tests |
| 2026-07-14 07:12 CST | `pnpm release:check` | final R7C fresh-application restart recall and exact-group isolation regression | exit 0; typecheck, lint, build, preflight, diff hygiene, and 101 passed / 1 skipped files with 2120 passed / 10 skipped tests |
| 2026-07-14 08:05 CST | `pnpm release:check` | final R8 query/FTS/scope ranking, trace evidence, cross-owner pre-limit filtering, and deterministic tie ordering | exit 0; typecheck, lint, build, preflight, diff hygiene, and 103 passed / 1 skipped files with 2127 passed / 10 skipped tests |
| 2026-07-14 12:54 CST | `pnpm release:check` | final credential-free R0-R8 completion-audit repairs, acceptance validator, process restart, memory truthfulness, delayed Attention evidence, build/preflight, and diff hygiene | exit 0; 106 passed / 1 skipped files and 2272 passed / 10 skipped opt-in live tests |
| 2026-07-23 21:01 CST | `pnpm release:check` | next-stage planning baseline on `main@cfebb77`; typecheck, lint, build, release preflight, deterministic tests, and diff hygiene | exit 0; 106 passed / 1 skipped files and 2272 passed / 10 skipped opt-in live tests |
| 2026-07-23 21:11 CST | `pnpm release:check` | final exact five-file planning worktree; typecheck, lint, build, release preflight, deterministic tests, and diff hygiene | exit 0; 106 passed / 1 skipped files and 2272 passed / 10 skipped opt-in live tests |
| 2026-07-24 20:21 CST | cold-start identity/dependency inventory | `main@cfebb77`, five known tracked documentation edits, Pi packages | branch/HEAD unchanged; no untracked paths; `@earendil-works/pi-agent-core` and `@earendil-works/pi-ai` both `0.80.2` |
| 2026-07-24 20:23 CST | `pnpm release:check` | fresh typecheck, lint, build, release preflight, deterministic tests, and diff hygiene | exit 0; 106 passed / 1 skipped files and 2272 passed / 10 skipped opt-in live tests |
| 2026-07-24 20:26 CST | P0 focused existing-test recheck | config, gateway, logger, job repository/worker, Pi adapter, model invocation repository | exit 0; 7 files and 275 tests passed; current tests still encode tokenless HTTP acceptance and shared FIFO/zero-usage behavior |
| 2026-07-24 20:39 CST | `SEC-INGRESS-01A` deterministic gate | fail-first config regression; focused config/gateway tests; typecheck, lint, full release gate, diff hygiene, and reverse-apply check | exit 0; default bind is loopback and enabled reverse HTTP ingress fails closed without a token on non-loopback; focused 3 files / 115 tests and full 106 passed / 1 skipped files with 2276 passed / 10 skipped tests; no live or persistence operation |
| 2026-07-24 20:53 CST | `SEC-INGRESS-01B` deterministic gate | fail-first WS route regression; strict config, WS/default/explicit and HTTP integration tests; typecheck, lint, full release gate, diff hygiene, and named-path reverse-apply check | exit 0; fail-first returned `503` rather than `404`; focused 4 files / 190 tests and full 106 passed / 1 skipped files with 2285 passed / 10 skipped tests; default WS has zero event-route effects, enabled modes remain usable, and no live or persistence operation occurred |
| 2026-07-24 21:04 CST | `SEC-BODY-01` deterministic gate | fail-first declared-length regression; config/auth unit tests; declared/streamed limit, pre-body auth, HMAC, abort, timeout/single-response, and shutdown integration tests; full release and reverse-apply checks | exit 0; fail-first returned `400` rather than `413`; focused 4 files / 245 tests and full 106 passed / 1 skipped files with 2299 passed / 10 skipped tests; all rejection paths had zero ingress/governance effects and no live or persistence-format operation occurred |
| 2026-07-24 21:17 CST | `PRIV-LOG-01` deterministic gate | fail-first 5-7 digit nested-value and parsed-ingress-event log regressions; bounded accepted/rejected ingress metadata; structured listener coordinates; focused logger/HTTP/WS tests; typecheck, lint, full release, diff hygiene, and named-path reverse-apply check | exit 0; fail-first evidence exposed raw short identifiers and the parsed event; focused 3 files / 146 tests and full 106 passed / 1 skipped files with 2301 passed / 10 skipped tests; no body/message/sender/card/media/secret/platform value entered reverse HTTP logs, 5-12 digit redaction and visible numeric ports were proved, and no live or persistence-format operation occurred |
| 2026-07-24 21:30 CST | `WORK-FAIR-01A` deterministic gate | fail-first configured interactive-lane regression; filtered repository/worker/scheduler/runtime wiring tests; broader worker integration; typecheck, lint, full release, diff hygiene, and named-path reverse-apply check | exit 0; fail-first claimed the older retention job instead of the due `attention_recheck`; focused 4 files / 70 tests, broader 4 files / 145 tests, and full 106 passed / 1 skipped files with 2305 passed / 10 skipped tests; distinct no-borrowing interactive/maintenance workers ran concurrently, shutdown drained both with zero running attempts, and no schema, config, dependency, live, or persistence-format operation occurred |
| 2026-07-24 21:46 CST | `PI-CONC-01A` deterministic gate | fail-first same-Agent concurrent-turn regression; isolated run/stream/event/tool/actor/deadline/abort tests; summary/runtime/shutdown consumers; typecheck, lint, build, preflight, full release, diff hygiene, and named-path reverse-apply check | exit 0; fail-first turn B reused turn A's blocked Agent; focused adapter 98 tests, broader 5 files / 150 tests, and full 106 passed / 1 skipped files with 2306 passed / 10 skipped tests; active turns use distinct resettable Agent sessions with isolated attribution and cancellation state, and no config, schema, dependency, live, or persistence-format operation occurred |
| 2026-07-24 22:34 CST | `PI-CONC-01B1/01B2` deterministic gate | full-workflow admission, delayed Attention convergence, bounded queue/deadline outcomes, provider suppression, shutdown/recovery, standalone lint, release gate, diff hygiene, and named-path reverse-apply check | exit 0; controller/config 91 tests, application concurrency 5 tests, and adjacent shutdown/recovery 19 tests passed; `pnpm release:check` passed 108 / 1 skipped files with 2340 / 10 skipped tests; reverse-apply passed for 31 tracked and three untracked add-file paths; no schema, dependency, Provider, QQ, private-data, deployment, restart, or container operation occurred |
| 2026-07-24 23:14 CST | `PI-OBS-01A` deterministic gate | fail-first schema-v7 migration, populated v6 backup/migrate/restore, adjacent persistence/release coverage, typecheck, lint, build, preflight, full release gate, and diff hygiene | exit 0; schema-v7 6 tests and focused 13 files / 255 tests passed; populated v6 backup restored its exact v6 ledger after a disposable v7 upgrade; `pnpm release:check` passed 109 / 1 skipped files with 2347 / 10 skipped tests; unknown usage remains distinct from zero, integrity/FKs are clean, and no Provider, QQ, private-data, deployment, restart, or container operation occurred |
| 2026-07-24 23:17 CST | fresh v6/v7 managed release rehearsal | independently built `main@cfebb77` schema-v6 prior and current schema-v7 candidate; three disposable activation roots | exit 0; prior digest `a9754bfa18383b38bf959ba6c360543eb199776fe8011741f33dc74813456104`, candidate digest `0a2dd88575e4c28224fd101c2ff31fb9d8d0a49f9b35a1aea119f08d67fe3623`; readiness failure rollback, pending-crash startup denial/recovery, wrong/exact confirmation, marker-free candidate restart, exact v6/v7 ledger and Pi-schema probes, sentinel preservation, integrity/FKs, process cleanup, and workspace cleanup all passed |
| 2026-07-24 23:19 CST | `PI-OBS-01A` final hygiene | current slice's named tracked and add-file paths | `git diff --check` passed; reverse-apply simulation passed for 16 tracked paths and two untracked add-file paths without changing the worktree |
| 2026-07-24 23:56 CST | `PI-OBS-01B` deterministic gate | fail-first repository/adapter/runtime wiring; per-request Pi ledger, typed usage, terminal/recovery, evaluator/summary adjacency, P3 and release gates | exit 0; fail-first exposed two missing repository APIs, six missing adapter stream-boundary behaviors, and one missing production injection; focused 4 files / 135 tests, adjacent evaluator/summary 5 files / 79 tests, stable P3 4 files / 266 tests, typecheck, lint, build, preflight, and full release gate passed with 109 passed / 1 skipped files and 2356 passed / 10 skipped tests; initial/tool-follow-up/continuation ordinals, known/unknown usage, fixed terminal codes, zero transport retry, source snapshotting, shared production wiring, idempotent stale recovery, integrity/FKs, and payload-free rows are deterministic; no Provider, QQ, private-data, deployment, restart, container, migration, dependency, or protected-path operation occurred |
| 2026-07-24 23:58 CST | `PI-OBS-01B` final hygiene | nine named tracked slice paths | `git diff --check` passed; reverse-apply simulation passed without changing the worktree |
| 2026-07-25 00:30 CST | `PI-OBS-01C` deterministic gate | ledger-backed turn completion, aggregate operations JSON/Prometheus/CLI, typecheck, lint, build, preflight, full release gate, and diff hygiene | exit 0; focused 4 files / 266 tests remained green; `pnpm release:check` passed 109 / 1 skipped files with 2363 / 10 skipped tests; known multi-round usage sums exactly, unknown usage stays nullable, local governance stays known zero, and aggregate outputs remain payload-free; no Provider, QQ, private-data, deployment, restart, container, migration, dependency, or protected-path operation occurred |
| 2026-07-25 00:53 CST | `PI-OBS-01D` deterministic gate | fail-first queue-wait, Provider-latency, and total-turn latency regressions; shared JSON/Prometheus/invocation-summary consumers; focused ledger/e2e/ops/CLI suite, typecheck, lint, build, preflight, full release gate, diff hygiene, and named-path reverse-apply check | exit 0; focused 6 files / 421 tests passed; `pnpm release:check` passed 109 / 1 skipped files with 2364 / 10 skipped tests; complete non-negative pairs aggregate by lifecycle start, terminal failure/abort rows remain measurable, running/partial/unrelated rows are excluded, valid zero latency is counted, and outputs remain payload-free; reverse-apply passed for 9 tracked paths; no Provider, QQ, private-data, deployment, restart, container, migration, dependency, or protected-path operation occurred |
| 2026-07-25 01:08 CST | `MEM-MAINT-01A` deterministic gate | fail-first conflict/consolidation/decay proposal previews; stable source/candidate fingerprints, retry deduplication, non-mutation, P5 repository/governance/retrieval/e2e adjacency, typecheck, lint, build, preflight, full release gate, and diff hygiene | exit 0; fail-first exposed a missing `proposals` result on all three scanners; focused 7 files / 211 tests passed; `pnpm release:check` passed 112 / 1 skipped files with 2367 / 10 skipped tests; unchanged candidates reuse one stable proposal ID and audit row, raw memory/source payloads remain absent, active state/revisions stay unchanged, and synthetic FKs are clean; reverse-apply passed for seven tracked and four add-file paths; no Provider, QQ, private-data, deployment, restart, container, schema, dependency, or protected-path operation occurred |
| 2026-07-25 01:19 CST | P5 resume gate | current `MEM-MAINT-01A` worker, repository, governance, retrieval, and e2e adjacency | exit 0; 7 files / 211 tests passed on `main@cfebb77`; schema-v8 work had not started and no live, private-data, deployment, container, or persistence operation occurred |
| 2026-07-25 01:46 CST | `MEM-MAINT-01B1` deterministic gate | fail-first schema-v8 contract; populated-v7 migration/restore; current release fixtures; independently built v7/v8 managed releases; three-root rollback/recovery/confirmation; release gate and hygiene | exit 0; schema-v8 7 tests and current release fixture 4 files / 136 tests passed; `pnpm release:check` passed 113 / 1 skipped files with 2375 / 10 skipped tests; the first cross-version run reproduced one stale v6/v7 assertion, then the corrected run proved exact v7/v8 ledgers, v7 Pi schema retention, v8 proposal schema, DB/pointer restoration, prior/candidate restart, sentinel/integrity/FKs, and cleanup; reverse-apply passed for 15 tracked and two add-file paths; zero sensitive status paths; no Provider, QQ, private-data, deployment, live DB, container, dependency, or migration-007 operation occurred |
| 2026-07-25 02:00 CST | `MEM-MAINT-01B2` deterministic gate | fail-first normalized scanner persistence; atomic proposal/audit writes; retry/reopen idempotency; injected child-write rollback; P5 adjacency; typecheck, scoped lint, release gate, diff hygiene, and reverse-apply simulation | exit 0; fail-first produced four expected failures including transaction rollback; scanner tests passed 3 files / 4 tests and P5 adjacency passed 7 files / 212 tests; `pnpm release:check` passed 113 / 1 skipped files with 2376 / 10 skipped tests; exact scope, candidates, reasons, fingerprints, effect, initial revision, and audit linkage are durable while memory states/revisions remain unchanged; integrity/FKs are clean; reverse-apply passed for eight tracked and five add-file paths; zero sensitive status paths; no Provider, QQ, private-data, deployment, live DB, container, schema, dependency, review, apply, rollback, or migration-007 operation occurred |
| 2026-07-25 02:20 CST | `MEM-MAINT-01C1` deterministic gate | fail-first normalized inspection and review lifecycle; SQL-before-limit authority; exact transition/retry/reopen/concurrency/transaction proofs; P5 adjacency; typecheck, scoped lint, release gate, diff hygiene, sensitive inventory, and reverse-apply simulation | exit 0; fail-first produced four expected missing-API failures while five existing governance tests stayed green; focused storage/governance passed 2 files / 9 tests and P5 adjacency passed 8 files / 216 tests; `pnpm release:check` passed 114 / 1 skipped files with 2380 / 10 skipped tests; approve/reject/expire commit proposal, redacted audit, and contiguous revision atomically, exact retry adds zero rows after reopen, a two-connection competing review has one winner, injected revision failure rolls back, and review changes no memory/effect rows; integrity/FKs are clean; reverse-apply passed for nine tracked and two add-file paths at `/tmp/lethebot-mem-maint-01c1-reverse-check-pcCSXy.patch`; zero sensitive status paths; no Provider, QQ, private-data, deployment, live DB, container, schema, dependency, command grammar, apply, rollback, or migration-007 operation occurred |
| 2026-07-25 02:41 CST | `MEM-MAINT-01C2` deterministic gate | fail-first exact approved application; shared candidate/source revalidation; conflict/consolidation/decay effects; retry/reopen/race/stale/transaction/retrieval proofs; P5 adjacency; typecheck, scoped lint, release gate, diff hygiene, sensitive inventory, and reverse-apply simulation | exit 0; fail-first produced eight expected missing-API failures while nine existing focused tests stayed green; final storage/governance passed 2 files / 17 tests and P5 adjacency passed 8 files / 237 tests; `pnpm release:check` passed 114 / 1 skipped files with 2388 / 10 skipped tests; exact approved proposals atomically write retained/superseded/disabled memory revisions and redacted audits, proposal apply revision/audit, and one effect link per candidate while preserving records/sources; explicit conflict selection, immediate active-retrieval exclusion, exact retry after reopen, a two-connection competing choice, stale record/source/state rejection, and injected memory/audit/proposal-revision/effect-link rollback are proved; integrity/FKs are clean; reverse-apply passed for nine tracked and four add-file paths at `/tmp/lethebot-mem-maint-01c2-reverse-check-aRf7U8.patch`; zero sensitive status paths; no Provider, QQ, private-data, deployment, live DB, container, schema, dependency, command grammar, rollback, or migration-007 operation occurred |
| 2026-07-25 02:56 CST | `MEM-MAINT-01C3` deterministic gate and P5 exit audit | fail-first exact applied rollback; normalized apply-link/latest-revision/current-snapshot/source/scope revalidation; conflict/consolidation/decay restore; retry/reopen/race/stale/transaction/retrieval proofs; canonical docs; P5 adjacency; typecheck, scoped lint, release gate, hygiene, worker-authority/command-grammar audit, and reverse-apply simulation | exit 0; fail-first produced eight expected missing-API failures while 16 existing focused tests stayed green; final storage/governance passed 2 files / 24 tests and P5 adjacency passed 8 files / 244 tests; `pnpm release:check` passed typecheck, lint, build, preflight, diff hygiene, 114 / 1 skipped files, and 2395 / 10 skipped tests; exact applied proposals restore every candidate to active through append-only memory/proposal revisions, redacted audits, and restored links while preserving records, sources, and apply history; exact retry/reopen, one-winner two-connection rollback, record/source/latest-revision/effect drift, four injected failure stages, immediate retrieval, integrity, and empty FKs are proved; all three scanners remain proposal-only and no QQ/CLI lifecycle grammar was added; reverse-apply returned ten C3 paths byte-for-byte to C2 at `/tmp/lethebot-mem-maint-01c3-final-reverse-check-dslsA7.patch` (`sha256:e656eb94d24829cf6d1b0ff3dc556d790b5b45c73d0f9f23c3cf463e548634f9`); 63 tracked plus 14 untracked paths remain recognized, zero sensitive status paths matched, and migration 007 retained SHA-256 `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`; P5 is `PHASE_COMPLETE` with no Provider, QQ, private-data, deployment, live DB, container, schema, dependency, command-grammar, or migration operation in C3 |
| 2026-07-25 03:14 CST | `TOOL-PROD-01A` deterministic gate | fail-first production inventory; fixed aggregate runtime status; exact permission/schema/failure/no-write behavior; Pi terminal linkage; P6 tool/Pi/storage/file adjacency; canonical docs; typecheck, lint, release gate, hygiene, and exact named-path reverse/forward simulation | exit 0; fail-first found the expected missing `runtime.status` while the other three application-wiring tests stayed green; final focused passed 3 files / 118 tests and P6 adjacency passed 14 files / 351 tests; `pnpm release:check` passed typecheck, lint, build, preflight, diff hygiene, 115 / 1 skipped files, and 2405 / 10 skipped tests; production registers exactly five reviewed tools while all legacy file/network handlers remain dormant; private owner/admin `runtime.status` exposes only fixed health/readiness and aggregate event/job/worker counts, performs no domain write, and returns one diagnostic-free unavailable shape on local reader/database failure; reverse and forward application proved all nine P6A paths byte-for-byte at `/tmp/lethebot-tool-prod-01a-final-reverse-check-Mbj1Py.patch` (`sha256:e62cfbc82f7d05a34366a836590911b088fa15071096becd6a48d507c62654ac`); 64 tracked plus 16 untracked paths remain recognized, zero sensitive status paths matched, and migration 007 retained SHA-256 `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`; no schema, dependency, Provider, QQ, private-data, deployment, restart, container, filesystem mutation, network, or catalog-toggle operation occurred |
| 2026-07-25 03:30 CST | `TOOL-PROD-01B1` deterministic gate | fail-first default-off/configured catalog and direct missing-module checks; bounded immutable-root workspace listing; exact metadata/permission/path/symlink/redaction/failure/cancellation/no-write behavior; Pi terminal linkage; P6 adjacency; canonical docs; typecheck, lint, release gate, hygiene, and exact named-path reverse/forward simulation | exit 0; fail-first catalog/config produced five intended failures while 84 existing assertions remained green, and the direct suite separately failed because the module was absent; final focused passed 4 files / 216 tests and P6 adjacency passed 17 files / 461 tests; `pnpm release:check` passed typecheck, lint, build, preflight, diff hygiene, 116 / 1 skipped files, and 2430 / 10 skipped tests; an unset root preserves the exact five-tool catalog while an existing absolute root adds only private owner/admin `workspace.list`; one non-hidden directory is listed non-recursively with at most 200 entries scanned and 100 returned, sorted relative coarse metadata, `lstat` symlink handling, fixed path-free failures, and redacted secret/platform-shaped names; Pi execution proved linked redacted tool/audit evidence, denied user/group exposure, unchanged workspace bytes/mtimes, zero domain writes, integrity `ok`, and empty FKs; reverse and forward application proved all 14 B1 paths byte-for-byte at `/tmp/lethebot-tool-prod-01b1-final-reverse-check-RPoPyv.patch` (`sha256:c6ed6e17e06e80789e2cbddb3d8a6336081f747e2911e9c41ebb90b6f4e4dbe5`); 65 tracked plus 18 untracked paths remain recognized, zero sensitive status paths matched, and migration 007 retained SHA-256 `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`; no schema, dependency, Provider, QQ, private-data, deployment, restart, container, filesystem mutation, network, read-file, write/delete, or legacy-handler registration operation occurred |
| 2026-07-25 03:46 CST | `TOOL-PROD-01B2` deterministic gate | fail-first configured catalog and direct missing-module checks; bounded immutable-root text read; exact metadata/permission/path/symlink/type/size/UTF-8/exclusion/redaction/failure/cancellation/no-write behavior; Pi terminal linkage; P6 adjacency; canonical docs; typecheck, lint, release gate, hygiene, and exact named-path reverse/forward simulation | exit 0; configured-catalog fail-first produced one intended failure while four existing assertions stayed green, and the direct suite separately failed because the module was absent; final focused passed 3 files / 147 tests and P6 adjacency passed 18 files / 496 tests; `pnpm release:check` passed typecheck, lint, build, preflight, diff hygiene, 117 / 1 skipped files, and 2465 / 10 skipped tests; an unset root preserves the exact five-tool catalog while an existing absolute root adds only private owner/admin `workspace.list` and `workspace.read_text`; one non-hidden regular file is read through a no-follow descriptor up to 2,048 source bytes as strict UTF-8, with every symlink component and credential/database/log/key/certificate/runtime-data path rejected, fixed path-free failures, and redacted path/content output; Pi execution proved linked redacted tool/audit evidence, denied user/group exposure, unchanged content and modification time, and zero domain writes while excluding OS-managed access time from the no-write claim; reverse and forward application proved all 13 B2 paths byte-for-byte at `/tmp/lethebot-tool-prod-01b2-final-reverse-check-RVdx3a.patch` (`sha256:4c0ff458fc789fb91a1bba9561bb4e2fef90b896465f328a40ff9fde82d5b0b9`), mode `0600`; 65 tracked plus 20 untracked paths remain recognized, zero sensitive status paths matched, and migration 007 retained SHA-256 `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`; no schema, dependency, Provider, QQ, private-data, deployment, restart, container, filesystem mutation, network, write/delete, or legacy-handler registration operation occurred |
| 2026-07-25 04:27 CST | `TOOL-PROD-01C1` deterministic gate | fail-first exact-origin config/catalog and direct missing-module checks; permission/evaluator boundary; URL/DNS/address-pinning/redirect/response/redaction/cancellation limits; configured Pi linkage; P6 adjacency; canonical docs; typecheck, lint, release gate, hygiene, and exact named-path reverse/forward proof | exit 0; config/catalog fail-first produced 13 intended failures while 87 existing assertions stayed green, and the direct/Pi checks separately failed because the module was absent; final focused passed 4 files / 276 tests, the direct suite passed 66 tests, and P6 adjacency passed 19 files / 575 tests; `pnpm release:check` passed typecheck, lint, build, preflight, diff hygiene, 118 / 1 skipped files, and 2544 / 10 skipped tests; blank configuration preserves the B2 catalogs while exact HTTPS origins add only private owner/admin evaluator-required `web.fetch_text`; URL-only fixed-header GET, all-answer public-address validation, pinned TLS/Host identity, three same-origin redirects, 2,048 source bytes, strict UTF-8/text/identity response rules, fixed failures, 8,192-byte redacted output, and zero retry are deterministic through injected fixtures with no real DNS/network call; Pi execution proved linked evaluator invocation/decision, terminal tool/audit/turn evidence and permission/evaluator denial before resolution; external observation is explicitly not a reversible local prepared effect; strict reverse and forward application proved all 16 C1 implementation/contract paths byte-for-byte at `/tmp/lethebot-tool-prod-01c1-final-reverse-check-R26wzZ.patch` (`sha256:4cd66be4a3603fe4a24227a87a21ef28f7a80d75d574283618a65fb1f6a2941b`), mode `0600`; 66 tracked plus 22 untracked paths remain recognized, no unexpected sensitive status path matched, and migration 007 retained SHA-256 `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`; no schema, dependency, Provider, QQ, private-data, deployment, restart, container, real network, filesystem mutation, durable domain effect, or legacy-handler registration operation occurred |
| 2026-07-25 04:47 CST | `TOOL-PROD-01D1` deterministic gate and P6 deterministic exit audit | fail-first exact default/configured catalog and direct missing-module checks; read-only registry projection, current-context availability, coarse optional state, redaction/bounds/fixed failures; Pi terminal linkage; canonical docs; exact P6 adjacency; typecheck, lint, release gate, hygiene, and exact named-path reverse/forward proof | exit 0; application fail-first produced four intended catalog failures while three existing checks stayed green, direct import then failed because the module was absent, and a later post-redaction name-bound regression failed before the 128-character fix; final direct/application/Pi coverage passed 3 files / 127 tests and the exact P6 focused gate passed 18 files / 485 tests; `pnpm release:check` passed typecheck, lint, build, preflight, diff hygiene, 119 / 1 skipped files, and 2554 / 10 skipped tests; the default catalog has six reviewed tools, workspace/origins/both configuration yields eight/seven/nine, and private owner/admin `runtime.tools` returns at most 32 bounded redacted names with capabilities, current-context availability, evaluator flags, counts, and coarse optional registration while omitting roots, origin values, permission IDs, descriptions, handlers, payloads, credentials, identifiers, and diagnostics; Pi execution proved linked terminal tool/audit/turn evidence, ordinary-user/group non-exposure, clean integrity/FKs, and zero registry or durable domain mutation; strict reverse and forward application proved all nine D1 implementation/contract paths byte-for-byte at `/tmp/lethebot-tool-prod-01d1-final-reverse-check-C9HPcg.patch` (`sha256:d085be6368a0b817b6847910a4609827ee5979c8070b698a3e4d08a081b0a664`), mode `0600`; 66 tracked plus 24 untracked paths remain recognized, no unexpected sensitive status path matched, and migration 007 retained SHA-256 `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`; P6 is deterministic-ready with live tool proof deferred to authorized P4/P9, and no schema, dependency, Provider, QQ, private-data, deployment, restart, container, DNS/network, filesystem mutation, durable domain effect, mutable catalog toggle, or legacy-handler registration operation occurred |
| 2026-07-25 05:00 CST | `ARCH-APP-01A1` deterministic gate | passing inline external HTTP characterization; extracted route/listener lifecycle; direct server contract; application/shutdown/recovery parity; canonical architecture; typecheck, lint, release gate, hygiene, and exact named-path reverse/forward proof | exit 0; after correcting one test-only expectation to the existing omission of undefined HTTP adapter fields, the untouched inline implementation passed 4 characterization tests; the extracted direct/application suites passed 2 files / 7 tests and the exact P7 focused gate passed 6 files / 159 tests; `pnpm release:check` passed typecheck, lint, build, preflight, diff hygiene, 121 / 1 skipped files, and 2561 / 10 skipped tests; `ApplicationHttpServer` now solely owns route/query dispatch, listener settlement/logging, `404`, and close while `LetheBotApp` retains response construction, metrics collection, OneBot body/auth/admission, startup order, and domain work; `src/index.ts` decreased from 3,149 to 3,083 lines with one production listener path; strict reverse and forward application restored all five A1 paths byte-for-byte at `/tmp/lethebot-arch-app-01a1-final-reverse-check-HKuBl4.patch` (`sha256:c1ee2acc4cf598023643ebdb4d5dc62e785c7c52b79fdc60c6ce209d9a60dff8`), mode `0600`; 66 tracked plus 27 untracked paths remain recognized, no unexpected sensitive status path matched, and migration 007 retained SHA-256 `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`; no behavior, schema, dependency, configuration, Provider, QQ, private-data, deployment, restart, container, catalog, persistence-format, or prior-slice change occurred |
| 2026-07-25 05:11 CST | `ARCH-APP-01A2A` deterministic gate | passing startup recovery characterization; extracted recovery transaction service; direct database contract; application/restart/race/failure parity; canonical architecture; typecheck, lint, release gate, hygiene, and exact named-path reverse/forward proof | exit 0; the untouched inline recovery passed 1 file / 12 tests, the extracted direct/application suites passed 2 files / 13 tests, and the exact P7 focused gate passed 6 files / 159 tests; `pnpm release:check` passed typecheck, lint, build, preflight, diff hygiene, 122 / 1 skipped files, and 2562 / 10 skipped tests; `EventAdmissionRecovery` now solely owns startup enumeration, strict stored-event validation, guarded processing reset, quarantine, linked turn abort, and aggregate logging while `LetheBotApp` retains recovery-before-adapter ordering, readiness-gated enqueue, raw claims, live scheduling/transitions, and all domain work; direct and application coverage retain atomic recovery/quarantine, restart/race/failure behavior, integrity, and empty FKs; `src/index.ts` decreased from 3,083 to 2,821 lines with one production recovery path; strict reverse and forward application restored all four A2A implementation/contract paths byte-for-byte at `/tmp/lethebot-arch-app-01a2a-final-reverse-check-njOm1v.patch` (`sha256:a5a164445bc1f5102839ec7161d3bfee4bbd085f198a09754a6b406f1418574e`), mode `0600`; 66 tracked plus 29 untracked paths remain recognized, no unexpected sensitive status path matched, and migration 007 retained SHA-256 `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`; no behavior, schema, dependency, configuration, Provider, QQ, private-data, deployment, restart, container, catalog, persistence-format, or prior-slice change occurred |
| 2026-07-25 05:21 CST | `ARCH-APP-01A2B` deterministic gate | passing ingress replay characterization; extracted raw-event claim transaction service; direct database contract; application/duplicate/rollback parity; canonical architecture; typecheck, lint, release gate, hygiene, and exact named-path reverse/forward proof | exit 0; the untouched inline claim passed the 4-test E2E characterization, the extracted direct plus characterization suites passed 6 tests with 124 skipped, and the exact P7 focused gate passed 6 files / 159 tests; `pnpm release:check` passed typecheck, lint, build, preflight, diff hygiene, 123 / 1 skipped files, and 2564 / 10 skipped tests; `EventIngressClaimService` now solely owns atomic canonical raw-event, per-delivery receipt, and accepted admission creation while `LetheBotApp` retains the ingress-open guard and newly accepted enqueue; scoped duplicate resolution, first-writer payload retention, cross-transport receipts, and receipt/admission rollback remain unchanged; `src/index.ts` decreased from 2,821 to 2,739 lines with one production claim path; strict reverse and forward application restored all four A2B implementation/contract paths byte-for-byte at `/tmp/lethebot-arch-app-01a2b-final-reverse-check-j8wG4x.patch` (`sha256:6b712d258aab368f6b9307e87ac28390dece3b26062f0bb3f8354e01a5f224c5`), mode `0600`; 66 tracked plus 31 untracked paths remain recognized, no unexpected sensitive status path matched, and migration 007 retained SHA-256 `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`; no behavior, schema, dependency, configuration, Provider, QQ, private-data, deployment, restart, container, catalog, persistence-format, or prior-slice change occurred |
| 2026-07-25 05:32 CST | `ARCH-APP-01B1` deterministic gate | passing admission/concurrency/shutdown/recovery characterization; extracted live turn-admission runtime; direct state-machine contract; application/restart/rejection/race parity; canonical architecture; typecheck, lint, release gate, hygiene, and exact named-path reverse/forward proof | exit 0; the untouched inline runtime passed 3 files / 19 tests, the extracted direct plus characterization suites passed 4 files / 23 tests, and the exact P7 focused gate passed 6 files / 159 tests; `pnpm release:check` passed typecheck, lint, build, preflight, diff hygiene, 124 / 1 skipped files, and 2568 / 10 skipped tests; `TurnApplicationService` now owns accepted-event scheduling, pending lifetime, deadline derivation, conversation-key selection, and guarded accepted/processing/terminal transitions while reusing the exact `TurnAdmissionController` shared with delayed Attention; direct tests prove completion/failure mapping, lost start and terminal compare-and-set behavior, overload/execution-time expiry routing, pending drain, integrity, and empty FKs; `LetheBotApp` retains claim/recovery ordering, rejection evidence, health construction, and the full turn pipeline; `src/index.ts` decreased from 2,739 to 2,676 lines with one production admission path; strict reverse and forward application restored all four B1 implementation/contract paths byte-for-byte at `/tmp/lethebot-arch-app-01b1-final-reverse-check-YmJsbt.patch` (`sha256:021f3279d0070a59e5363a659a388b3e109e4b12ca8c819a938f0b17e36b1f25`), mode `0600`; 66 tracked plus 33 untracked paths remain recognized, no unexpected sensitive status path matched, and migration 007 retained SHA-256 `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`; no behavior, schema, dependency, configuration, Provider, QQ, private-data, deployment, restart, container, catalog, persistence-format, queue-policy, or prior-slice change occurred |
| 2026-07-25 05:45 CST | `ARCH-APP-01B2A` deterministic gate | passing rejection/failure before-state characterization; fail-first direct ownership contract; extracted failure projection and evidence lifecycle; application/recovery/concurrency parity; canonical architecture; typecheck, lint, release gate, hygiene, and exact named-path reverse/forward proof | exit 0; the untouched B1 implementation passed 3 files / 145 tests, the new direct contract first produced the intended 5 failures while 2 prior tests stayed green, and the extracted direct plus characterization suites passed 4 files / 152 tests; the exact P7 focused gate passed 6 files / 159 tests; `pnpm release:check` passed typecheck, lint, build, preflight, diff hygiene, 124 / 1 skipped files, and 2571 / 10 skipped tests; `TurnApplicationService` now owns the redacted in-memory failure projection, getter/clear/count behavior, hashes-only generic failure record, shared durable insert/hash logic, and atomic overload/queue-timeout admission terminalization while `LetheBotApp` supplies the existing redaction and exact persistence/task log callbacks; direct tests prove projection-before-insert ordering, raw diagnostic hashing with redacted exposure, persistence degradation, exact rejection rows/timestamps, inert lost compare-and-set, insert rollback, integrity, and empty FKs; `src/index.ts` decreased from 2,676 to 2,497 lines with one production failure path; strict reverse and forward application restored all four B2A implementation/contract paths byte-for-byte at `/tmp/lethebot-arch-app-01b2a-final-reverse-check-47Rm9K.patch` (`sha256:b954b7f12bf5b1b7579ad726b97b714ce26dcad356bd84834c86aaf8ef161d61`), mode `0600`; 66 tracked plus 33 untracked paths remain recognized, no unexpected sensitive status path matched, and migration 007 retained SHA-256 `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`; no behavior, schema, migration, dependency, configuration, Provider, QQ, private-data, deployment, restart, container, catalog, persistence-format, queue-policy, context/Pi/action/memory/tool/governance/worker behavior, or prior-slice change occurred |
| 2026-07-25 05:59 CST | `ARCH-APP-01B2B` deterministic gate | passing complete-turn before-state characterization; extracted conversation application service and owned SQL/helpers; direct and delayed admission/recovery/concurrency parity; canonical architecture; typecheck, scoped/full lint, release gate, hygiene, inventory, and exact named-path reverse/forward proof | exit 0; direct application services passed 2 files / 9 tests, direct plus characterization passed 5 files / 154 tests, and the exact P7 focused gate passed 6 files / 159 tests; `pnpm release:check` passed typecheck, lint, build, preflight, diff hygiene, 125 / 1 skipped files, and 2573 / 10 skipped tests; `ConversationTurnService` now owns identity/chat/governance/Attention/context/Pi/action/response processing, turn SQL, deadline stages, completion selection, and caught failure coordination while `TurnApplicationService` remains the sole admission/failure-evidence owner; live ingress and delayed Attention call the same handler and replaceable Pi/action/social runtimes remain callback-resolved; `src/index.ts` decreased from 2,497 to 1,804 lines with no domain turn SQL or duplicate handler; strict reverse and forward application restored all four B2B implementation/contract paths byte-for-byte at `/tmp/lethebot-arch-app-01b2b-final-reverse-check-IhIDu3.patch` (`sha256:e414047c7137e7e4b13307ab8fde1e8aff2863f1728bcff16084dd5c3ff86daf`), mode `0600`; 66 tracked plus 35 untracked paths remain recognized under the all-files inventory, zero unexpected sensitive status paths matched, and migration 007 retained SHA-256 `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`; no behavior, schema, migration, dependency, configuration, Provider, QQ, private-data, deployment, restart, container, catalog, persistence-format, queue-policy, worker behavior, or prior-slice change occurred |

| 2026-07-25 06:16 CST | `ARCH-APP-01C` deterministic gate | passing focused background before-state characterization; fail-first direct service contract; extracted complete background runtime and narrow conversation enqueue dependency; unchanged worker/summary/shutdown/E2E parity; canonical architecture; typecheck, scoped/full lint, release gate, hygiene, inventory, and exact named-path reverse/forward proof | exit 0; the unchanged focused background gate passed 6 files / 187 tests, direct application services plus that gate passed 8 files / 192 tests, and the exact P7 focused gate passed 6 files / 159 tests; `pnpm release:check` passed typecheck, lint, build, preflight, diff hygiene, 126 / 1 skipped files, and 2576 / 10 skipped tests; `BackgroundRuntimeService` now solely owns the exact two worker lanes, scheduler catalog/lifecycle, summary discovery/execution, delayed-Attention replay and terminal-turn SQL, maintenance and retention routing, and payload validation while resolving replaceable Pi/extraction/conversation runtimes through narrow callbacks; `LetheBotApp` retains construction, startup ordering, lifecycle, and narrow test entry points, and `ConversationTurnService` depends only on an enqueue callback; `src/index.ts` decreased from 1,804 to 1,045 lines with no duplicate worker handler, scheduler, domain-worker SQL, or compatibility runtime; strict reverse and forward application restored all eight 01C implementation/contract paths byte-for-byte at `/tmp/lethebot-arch-app-01c-final-reverse-check-eJeRU3.patch` (`sha256:3cb21b690d017bb52dbbd16387c21339622575e21f8ba3df4ff3cf20e84be124`), mode `0600`; 67 tracked plus 37 untracked paths remain recognized under the all-files inventory, zero unexpected sensitive status paths matched, and migration 007 retained SHA-256 `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`; no behavior, schema, migration, dependency, configuration, Provider, QQ, private-data, deployment, restart, container, catalog, persistence-format, queue-policy, worker behavior, or prior-slice change occurred |

| 2026-07-25 06:26 CST | `ARCH-APP-01D` deterministic gate and P7 exit audit | static field/method/import/SQL ownership inventory; exact P7 focused gate; canonical architecture and Pi concurrency correction; release gate; hygiene; migration hash; exact documentation reverse/forward proof | exit 0; the initial focused run passed 158 / 159 tests and hit one environmental `EADDRINUSE` because the recovery suite randomly selected `127.0.0.1:19090`, already held by a local non-project process; the isolated 12-test recovery suite and the repeated exact 6-file / 159-test P7 gate passed; all 35 `LetheBotApp` fields and its constructor plus 27 methods classify as dependency construction, lifecycle, boundary adaptation, diagnostics, or narrow test access; the only root SQL is two `SELECT 1` liveness probes; HTTP, ingress, turn, and worker boundaries retain one production path and focused contracts; the retained OneBot body/auth callback is the deliberate A1 boundary adapter, so another extraction would add indirection without changing ownership; `src/index.ts` remains 1,045 lines with no domain SQL, turn/worker pipeline, scheduler implementation, dynamic import, duplicate runtime, or compatibility path; `docs/security-privacy.md` now matches the P2 isolated-session, per-conversation FIFO, bounded global admission, and ingress-absolute deadline contract; `pnpm release:check` passed typecheck, lint, build, preflight, diff hygiene, 126 / 1 skipped files, and 2576 / 10 skipped tests; strict reverse/forward proof covers the three 01D documentation paths; migration 007 retained SHA-256 `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`; P7 is `PHASE_COMPLETE`, with no runtime, test, schema, migration, dependency, configuration, Provider, QQ, private-data, deployment, restart, container, catalog, persistence-format, queue-policy, or prior-slice change |

| 2026-07-25 06:41 CST | `GOV-UX-01A0` deterministic planning gate | canonical operator tasks and information architecture; shared-service ownership; optional loopback listener; independent admin session, CSRF, and opaque scope/resource contracts; destructive preview/confirm/audit/rollback lifecycle; dependency strategy; documentation navigation; static checks; release gate; exact documentation reverse/forward proof | exit 0; `docs/governance-ui.md` defines the five required operator sections, default-off dedicated listener, exact loopback configuration, 15-minute default absolute session lifetime, strict same-origin and CSRF ordering, session-bound opaque handles, fixed redacted HTTP failures, stopped-service restore handoff, semantic dependency-free browser strategy, accessibility/responsive states, and the mandatory A1 fail-first matrix; all Markdown targets resolved, the new document is ASCII-only, and `git diff --check` passed; `pnpm release:check` passed typecheck, lint, build, preflight, diff hygiene, 126 / 1 skipped files, and 2576 / 10 skipped tests; strict reverse and forward application restored the navigation and new contract byte-for-byte at `/tmp/lethebot-gov-ux-01a0-final-reverse-check-FfEpMq.patch` (`sha256:b148d70de98725a9dd8e8ad219ab48c7a56a3541a24f85694664b7c4988366ca`), mode `0600`; 67 tracked plus 38 untracked paths remain recognized under the all-files inventory; no runtime, test, schema, migration, dependency, configuration, Provider, QQ, private-data, live DB, deployment, restart, container, generated UI, or prior-slice change occurred |

| 2026-07-25 07:00 CST | `GOV-UX-01A1` deterministic gate | fail-first auth/session/CSRF/scope matrix; isolated built-in HTTP listener; exact loopback/session/cookie/origin/body/scope boundary; fixed failures and callback ordering; application HTTP and CLI/QQ governance adjacency; canonical contract; typecheck, lint, release gate, hygiene, inventory, and exact named-path reverse/forward proof | exit 0; the new integration contract first failed before collection on the expected missing `governance-http-server` module; final A1 coverage passed 1 file / 10 tests, application HTTP adjacency passed 3 files / 17 tests, and the canonical P8 focused gate passed 5 files / 229 tests; `pnpm release:check` passed typecheck, lint, build, preflight, diff hygiene, 127 / 1 skipped files, and 2586 / 10 skipped tests; the isolated server opens no listener when disabled, accepts only exact loopback hosts, keeps at most eight absolute in-memory sessions with only cookie digests retained, rotates/logout/restart-invalidates sessions, applies exact-origin and session-bound CSRF checks before bodies or scopes, bounds declared/streamed bodies and receive time, validates exact session/purpose-bound scope shapes, ignores QQ/forwarded identity headers, and returns fixed security-headed failures without callback/domain effects; no production route is registered and injected resolver/handler failures expose no diagnostics; canonical docs name the exact `X-LetheBot-Scope` handle contract and unwired status; strict reverse and forward application restored the A0 governance contract and removed/recreated both new implementation/test files byte-for-byte at `/tmp/lethebot-gov-ux-01a1-final-reverse-check-9FZhxD.patch` (`sha256:ad3ee17370487f1d0226ad3141ec33403e1c1b7ae7e2ddf0971576305bdd63a7`), mode `0600`; 67 tracked plus 40 untracked paths remain recognized, zero unexpected sensitive status paths matched, and migration 007 retained SHA-256 `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`; no application/configuration wiring, domain route, SQL/database, schema, migration, dependency, lockfile, Provider, QQ, private-data, deployment, restart, container, generated UI, or prior-slice change occurred |

| 2026-07-25 07:08 CST | `GOV-UX-01A2` deterministic gate | fail-first configuration and lifecycle contract; default-off exact-loopback listener wiring; reciprocal startup/normal-shutdown cleanup; application HTTP/shutdown adjacency; canonical configuration, architecture, security, and deployment contracts; typecheck, lint, release gate, hygiene, migration hashes, and exact named-path reverse/forward proof | exit 0; the fail-first run produced the expected 34 missing or unenforced assertions while 102 existing assertions passed; final config/A1/A2 coverage passed 3 files / 137 tests, P7 lifecycle adjacency passed 6 files / 159 tests, and the canonical P8 gate passed 5 files / 229 tests; `pnpm release:check` passed typecheck, lint, build, preflight, diff hygiene, 128 / 1 skipped files, and 2620 / 10 skipped tests; the governance listener now defaults disabled at `127.0.0.1:6701` with a 900000-ms absolute session lifetime, enabled mode requires an exact loopback host, distinct port, and bounded admin token, and `LetheBotApp` starts the empty-route listener before the application listener while closing both after either startup failure or normal stop; CLI, QQ, database, tools, workers, and domain routes remain unchanged; strict reverse and forward application restored the A1 baseline and current candidate across all nine A2 implementation/test/doc paths byte-for-byte at `/tmp/lethebot-gov-ux-01a2-final-reverse-check-xnf2nq.patch` (`sha256:d43f2c5d9bf09dde515b5f9c4dfdce1fa430c336094decde288b29ce3c95148a`), mode `0600`, size 33775 bytes; 67 tracked plus 41 untracked paths remain recognized, migration 007 retained SHA-256 `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`, and migration 008 retained SHA-256 `95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`; no domain route, SQL/database, schema, migration, dependency, lockfile, Provider, QQ, private-data, deployment, restart, container, generated UI, or prior-slice behavior change occurred |

| 2026-07-25 07:35 CST | `GOV-UX-01B1` deterministic gate | fail-first shared-module contract; extracted typed aggregate-health query ownership; direct schema-v8 aggregate/redaction/read-only contract; full CLI command parity; canonical shared-service ownership; typecheck, scoped/full lint, release gate, hygiene, migration hashes, and exact named-path reverse/forward proof | exit 0; the new direct suite first failed before collection on the expected missing `query-service` module while all 36 existing CLI tests passed; final direct plus CLI coverage passed 2 files / 38 tests and the canonical P8 gate passed 6 files / 231 tests; `pnpm release:check` passed typecheck, lint, build, preflight, diff hygiene, 129 / 1 skipped files, and 2622 / 10 skipped tests; `GovernanceQueryService` now owns all aggregate-health SQL, fixed DTO construction, attention totals, current-time expired-lease evaluation, and shared dynamic count-key redaction while `GovernanceCLI.summarizeGovernanceHealth()` is a typed delegate retaining its public import/output surface; direct tests prove the typed review-summary call, exact aggregate values, no writes, redaction, current-time behavior, and empty FKs, while the 129-test CLI process suite preserves command behavior; no HTTP route or review-list move occurred; after normalizing a reconstruction-only `0600` source copy to the repository's `0644` mode, strict reverse and forward application restored the B1 four-path baseline and candidate byte-for-byte and mode-for-mode at `/tmp/lethebot-gov-ux-01b1-final-reverse-check-mNbG0A.patch` (`sha256:c76f4350d461aa6f53390165a19d53a45c855771f0803428f4222a63f348490b`), mode `0600`, size 40249 bytes; 67 tracked plus 43 untracked paths remain recognized, no unexpected sensitive status path matched, migration 007 retained SHA-256 `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`, and migration 008 retained SHA-256 `95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`; no HTTP/config/application/session/scope/mutation behavior, schema, migration, dependency, lockfile, Provider, QQ, private-data, live DB, deployment, restart, container, generated UI, or prior-slice change occurred |

| 2026-07-25 07:52 CST | `GOV-UX-01B2A` deterministic gate | fail-first shared review-query methods; extraction-only list/summary ownership; direct review filtering/resolution/redaction/read-only contract; unchanged CLI delegates and process behavior; canonical unscoped/HTTP-disabled boundary; typecheck, scoped/full lint, release gate, hygiene, migration hashes, and exact B1-to-B2A reverse/forward proof | exit 0; the direct suite first produced exactly two missing-method failures while both B1 direct tests and all 36 existing CLI tests passed, for 38 passing assertions; final direct plus CLI coverage passed 2 files / 40 tests and the canonical P8 gate passed 6 files / 233 tests; `pnpm release:check` passed typecheck, lint, build, preflight, diff hygiene, 129 / 1 skipped files, and 2624 / 10 skipped tests; `GovernanceQueryService` now owns the three review event filters, filter-before-final-limit behavior, stable ordering, resolution evidence, DTO aggregation, structured redaction, and memory-ID-group parsing, aggregate health calls its service-owned review summary, and both CLI review methods are typed delegates with preserved public re-exports; direct tests prove the default 100-row bound, a matching record beyond 101 newer rows, event/memory/status filters, supersede/disable evidence, exact totals, optional-detail and identifier redaction, zero writes, and empty FKs; the legacy reads remain intentionally unscoped and HTTP-disabled; strict reverse and forward application restored the exact B1 baseline and B2A candidate across four paths byte-for-byte and mode-for-mode at `/tmp/lethebot-gov-ux-01b2a-final-reverse-check-bP3vwL.patch` (`sha256:8c705ab34aa485c9bc77b9a8d0212d9264be0a34f3a9056ca2c90e7f7e141828`), mode `0600`, size 45468 bytes; the workspace stayed unchanged, 67 tracked plus 43 untracked paths remain recognized, no unexpected sensitive status path matched, the canonical governance document is ASCII-only with no Markdown link target, migration 007 retained SHA-256 `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`, and migration 008 retained SHA-256 `95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`; no HTTP/config/application/session/scope/mutation behavior, schema, migration, dependency, lockfile, Provider, QQ, private-data, live DB, deployment, restart, container, generated UI, or prior-slice change occurred |

| 2026-07-25 08:09 CST | `GOV-UX-01B2B` deterministic gate | fail-first exact-scope normalized maintenance-review list/detail projections; repository scope-before-limit authority; fixed bounded redacted DTOs; B2A/CLI and P5 adjacency; canonical scope contract; typecheck, scoped/full lint, release gate, hygiene, migration hashes, and exact B2A-to-B2B reverse/forward proof | exit 0; the direct suite first produced exactly two missing-method failures while all 40 B2A/CLI assertions remained green; final query/CLI coverage passed 2 files / 42 tests, P5 repository/governance adjacency passed 2 files / 24 tests, the combined focused gate passed 4 files / 66 tests, and the canonical P8 gate passed 6 files / 235 tests; `pnpm release:check` passed typecheck, lint, build, preflight, diff hygiene, 129 / 1 skipped files, and 2626 / 10 skipped tests; exact global, user, group, private/group conversation tuple, and system scopes are isolated before the maximum 100-row limit, tool-name scopes return no memory review rows, and detail candidates/revisions are capped at 32 with explicit truncation flags; DTOs omit raw proposal, memory, audit, revision, and owner IDs and expose only purpose-bound 16-character SHA-256 references; legacy B2A reads, CLI behavior, P5 mutations, HTTP routes, and scope handles remain unchanged; strict reverse and forward application restored the exact B2A baseline and B2B candidate across four paths byte-for-byte and mode-for-mode at `/tmp/lethebot-gov-ux-01b2b-final-reverse-check-a9Dp40.patch` (`sha256:62631a722b1f6c722016f2d6f2861ce0acf210bc07ac743679588c86532e2c37`), mode `0600`, size 30914 bytes, with proof directory `/tmp/lethebot-gov-ux-01b2b-rollback-nJ6VE2`; the workspace stayed unchanged, 67 tracked plus 43 untracked paths remain recognized, no unexpected sensitive status path matched, the canonical governance document is ASCII-only with no Markdown link target, migration 007 retained SHA-256 `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`, and migration 008 retained SHA-256 `95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`; no HTTP/config/application/session/handle/route/mutation behavior, schema, migration, dependency, lockfile, Provider, QQ, private-data, live DB, deployment, restart, container, generated UI, or prior-slice change occurred |

| 2026-07-25 08:25 CST | `GOV-UX-01B3` deterministic gate | fail-first bounded maintenance-review scope catalog; SQL normalization/deduplication/order-before-limit; fixed redacted fingerprint-only DTO; B2B/CLI and HTTP-security adjacency; canonical catalog contract; typecheck, scoped/full lint, release gate, hygiene, migration hashes, and exact B2B-to-B3 reverse/forward proof | exit 0; the focused suite first produced exactly two missing-method failures while all six B2B direct tests and all 36 governance CLI tests passed, for 42 passing assertions; final direct coverage passed 1 file / 8 tests, direct plus CLI passed 2 files / 44 tests, and the canonical P8 gate passed 6 files / 237 tests; `pnpm release:check` passed typecheck, lint, build, preflight, diff hygiene, 129 / 1 skipped files, and 2628 / 10 skipped tests; distinct normalized proposal scopes from any lifecycle state now produce at most 100 stable global, user, group, private/group conversation, and system catalog entries after a 101-row SQL truncation probe, duplicate proposal scopes collapse, tool rows remain omitted, and fixed labels plus purpose-bound 16-character fingerprints expose no raw owner/scope/proposal/memory/audit/revision/subject identifiers; reads make no writes and direct fixtures remain integrity/FK-clean; the listener remains empty-route with no raw-scope resolution or handle issuance; strict reverse and forward application restored the exact B2B baseline and B3 candidate across three paths byte-for-byte and mode-for-mode at `/tmp/lethebot-gov-ux-01b3-final-reverse-check-bjGGgm.patch` (`sha256:fd79831410b9ba6413b8d7b0fc48dfe46a5b7425f28a38f8eb0aea53a15c4148`), mode `0600`, size 14640 bytes, with proof directory `/tmp/lethebot-gov-ux-01b3-rollback-Wli9QC`; the workspace stayed unchanged, 67 tracked plus 43 untracked paths remain recognized, no unexpected sensitive status path matched, the canonical governance document is ASCII-only with no Markdown link target, migration 007 retained SHA-256 `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`, and migration 008 retained SHA-256 `95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`; no repository/mutation/CLI/HTTP/config/application/session/CSRF/handle/route behavior, schema, migration, dependency, lockfile, Provider, QQ, private-data, live DB, deployment, restart, container, generated UI, or prior-slice change occurred |
| 2026-07-25 08:39 CST | `GOV-UX-01B4` deterministic gate | fail-first standalone session-bound scope-handle registry; all-scope round-trip and defensive-copy contract; exact session/purpose/expiry/revocation/capacity boundaries; A1 HTTP-security adjacency; canonical unwired contract; typecheck, scoped/full lint, release gate, hygiene, migration hashes, and exact B3-to-B4 reverse/forward proof | exit 0; the new direct suite first failed before collection on the expected missing `governance-scope-handle-registry` module while all ten existing A1 HTTP security tests remained green; final direct registry coverage passed 1 file / 3 tests, registry plus HTTP security passed 2 files / 13 tests, and the canonical P8 gate passed 6 files / 237 tests; `pnpm release:check` passed typecheck, lint, build, preflight, diff hygiene, 130 / 1 skipped files, and 2631 / 10 skipped tests; the dependency-free registry issues random 256-bit 43-character base64url handles bound to a SHA-256 session digest, trusted exact scope, bounded route purpose, and absolute session expiry, returns stable handles for the same live binding, resolves defensive scope copies only across the exact boundary, and invalidates on expiry, session revocation, clear, or a new registry; all seven scope shapes, eight-session and 512-binding capacities, cleanup-before-capacity, fixed identifier-free errors, and non-evicting failures are covered; no HTTP, application, catalog, session-lifecycle, or route wiring changed; strict reverse and forward application removed/restored both add-file paths and restored the B3/B4 canonical document byte-for-byte and mode-for-mode at `/tmp/lethebot-gov-ux-01b4-final-reverse-check-T1xFnH.patch` (`sha256:570312dcace4acca0610b317847e16844b5c634304cba92d1c0fdff371ed565f`), mode `0600`, size 20514 bytes, with proof directory `/tmp/lethebot-gov-ux-01b4-rollback-T1xFnH`; the workspace stayed unchanged, 67 tracked plus 45 untracked paths remain recognized, no unexpected sensitive status path matched, the canonical governance document is ASCII-only with no Markdown link target, migration 007 retained SHA-256 `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`, and migration 008 retained SHA-256 `95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`; no HTTP/config/application/session/CSRF/catalog/query/route/mutation behavior, schema, migration, dependency, lockfile, Provider, QQ, private-data, live DB, deployment, restart, container, generated UI, or prior-slice change occurred |
| 2026-07-25 08:55 CST | `GOV-UX-01B5A` deterministic gate | fail-first scope-handle store ownership; exact HTTP session rotation/expiry/logout/close invalidation; non-revoking rejection/capacity boundaries; real empty-route application wiring; B4/A1/A2 and canonical P8 adjacency; typecheck, scoped/full lint, release gate, hygiene, migration hashes, and exact B4-to-B5A reverse/forward proof | exit 0; the new lifecycle case first observed a scoped `404`, one legacy-resolver call, zero store resolutions, zero revocations, and zero clear calls while all three B4 registry tests and ten A1 HTTP tests passed; final registry/HTTP/application coverage passed 3 files / 18 tests and the canonical P8 gate passed 6 files / 238 tests; `pnpm release:check` passed typecheck, lint, build, preflight, diff hygiene, 130 / 1 skipped files, and 2632 / 10 skipped tests; `GovernanceHttpServer` now accepts one validated resolve/revoke/clear store, centralizes successful session removal, revokes only existing session digests during rotation, lazy expiry pruning, and logout, clears all handle state on listener close, and leaves unknown-cookie, rejected-credential, and full-capacity failures non-revoking; `LetheBotApp` constructs one B4 registry with the server clock and retains an empty domain route table; strict reverse and forward application restored the exact B4 baseline and B5A candidate across five paths byte-for-byte and mode-for-mode at `/tmp/lethebot-gov-ux-01b5a-final-reverse-check-KULGmj.patch` (`sha256:7c7a539c00347fc7185db28d630b68b8455baec50db376e49541eb59fecfc0e8`), mode `0600`, size 14452 bytes, with proof directory `/tmp/lethebot-gov-ux-01b5a-rollback-KULGmj`; the workspace stayed unchanged, 67 tracked plus 45 untracked paths remain recognized, no unexpected sensitive status path matched, the canonical governance document is ASCII-only with no Markdown link target, migration 007 retained SHA-256 `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`, and migration 008 retained SHA-256 `95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`; no catalog/query/CLI/QQ/domain route/mutation/config/schema/migration/dependency/lockfile/Provider/private-data/live DB/deployment/restart/container/generated UI or prior-slice behavior changed |
| 2026-07-25 09:08 CST | `GOV-UX-01B5B` deterministic gate | fail-first authenticated-unscoped route dispatch; exact auth/header/query/callback and collision boundaries; empty application wiring; B4/B5A/A1/A2 and canonical P8 adjacency; typecheck, scoped/full lint, release gate, hygiene, migration hashes, and exact B5A-to-B5B reverse/forward proof | exit 0; the new route first returned four `404` responses instead of the required `401/400/400/200` sequence while 14 B4/B5A assertions passed; final registry plus HTTP coverage passed 2 files / 15 tests, application wiring passed 1 file / 4 tests, and the canonical P8 gate passed 6 files / 239 tests; `pnpm release:check` passed typecheck, lint, build, preflight, diff hygiene, 130 / 1 skipped files, and 2633 / 10 skipped tests; `GovernanceHttpServer` now owns disjoint scoped and authenticated-unscoped route classes, preserves mandatory scope resolution for every scoped route, rejects query strings and any scope header before an unscoped callback, never calls the resolver for that class, supplies only a server-owned local-admin actor and defensive session digest/expiry, and maps thrown or malformed handler results to fixed `503`; cross-map collisions, session-path reuse, and mutation/method mismatch are rejected; `LetheBotApp` still passes an empty unscoped table and `/scopes` remains unavailable; strict reverse and forward application restored the exact B5A baseline and B5B candidate at `/tmp/lethebot-gov-ux-01b5b-final-reverse-check-14290Q.patch` (`sha256:269b5797b693cfefe1ea820737e8760adc230d88962fdce88d69d9d367f6ce2f`), mode `0600`, size 15877 bytes, with proof directory `/tmp/lethebot-gov-ux-01b5b-rollback-14290Q`; the workspace stayed at 67 tracked plus 45 untracked paths, the canonical governance document remained ASCII-only with no Markdown link target, and migration 007/008 retained SHA-256 `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`/`95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`; no production route, catalog/query/registry semantics, CLI/QQ, mutation, config, schema, migration, dependency/lockfile, Provider, private-data, live DB, deployment, restart, container, generated UI, or prior-slice behavior changed |
| 2026-07-25 09:24 CST | `GOV-UX-01B5C` deterministic gate | fail-first trusted catalog issuance and first production route; all six supported exact-scope handle bindings; explicit bounded DTO; authenticated empty `/scopes`; B3-B5B and canonical P8 adjacency; typecheck, scoped/full lint, release gate, hygiene, migration hashes, and exact B5B-to-B5C reverse/forward proof | exit 0; the two-file fail-first command produced exactly the missing query-method error and application `404` instead of `401` while ten adjacent assertions passed; final query/application coverage passed 2 files / 12 tests, registry/HTTP/application coverage passed 4 files / 27 tests, and the canonical P8 gate passed 6 files / 239 tests; `pnpm release:check` passed typecheck, lint, build, preflight, diff hygiene, 130 / 1 skipped files, and 2633 / 10 skipped tests; `GovernanceQueryService` now reuses the B3 normalized rows and stable ordering, exposes exact raw scopes only to a trusted issuer callback, and explicitly returns fingerprint, scope kind, optional conversation type, fixed label, 43-character handle, and numeric expiry without issuer extras; global, user, group, private/group conversation, and system handles resolve through the real registry only for the issuing session and `memory.maintenance.review` purpose; `LetheBotApp` registers only authenticated unscoped `GET /governance/api/v1/scopes` with discovery purpose `memory.maintenance.review.scopes`, keeps every scoped and memory list/detail route absent, and returns an empty bounded catalog on a fresh DB; strict reverse and forward application restored the exact B5B baseline and B5C candidate across five paths at `/tmp/lethebot-gov-ux-01b5c-final-reverse-check-tmSTA3.patch` (`sha256:39371b20d4537c365399f8802d29ebb167078f1ee0dbf9263543fb54848b9b73`), mode `0600`, size 17098 bytes, with proof directory `/tmp/lethebot-gov-ux-01b5c-rollback-tmSTA3`; the workspace stayed at 67 tracked plus 45 untracked paths, the canonical governance document remained ASCII-only with no Markdown link target, and migration 007/008 retained SHA-256 `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`/`95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`; no repository/mutation/CLI/QQ/browser/config/schema/migration/dependency/lockfile/Provider/private-data/live DB/deployment/restart/container/persistence-format or prior-slice behavior changed |

| 2026-07-25 09:36 CST | `GOV-UX-01B6A` deterministic gate | fail-first authenticated system-wide overview route; explicit trusted-route dispatch; unchanged aggregate DTO; B1/B5C/HTTP/application and canonical P8 adjacency; typecheck, scoped/full lint, release gate, hygiene, migration hashes, and exact B5C-to-B6A reverse/forward proof | exit 0; fail-first returned `404` instead of the required pre-query `401` while the other three application lifecycle tests passed; application wiring passed 1 file / 4 tests, query/HTTP/application coverage passed 3 files / 24 tests, and the canonical P8 gate passed 6 files / 239 tests; `pnpm release:check` passed typecheck, lint, build, preflight, diff hygiene, 130 / 1 skipped files, and 2633 / 10 skipped tests; authenticated `GET /governance/api/v1/overview` now calls only the existing bounded redacted `GovernanceQueryService.summarizeGovernanceHealth` projection with no filters, scope header, body, query semantics, or write, while missing sessions reject before query work and `/scopes`, scoped routes, unknown routes, listener lifecycle, and fixed `503` handling remain unchanged; strict reverse and forward application restored the exact B5C baseline and B6A candidate across three paths byte-for-byte and mode-for-mode at `/tmp/lethebot-gov-ux-01b6a-final-reverse-check-GKNiF0.patch` (`sha256:bc415385fd0c52c13b42087fc3916cb09f9c479500fae54cd8e1cf78d5be5a90`), mode `0600`, size 9497 bytes, with proof directory `/tmp/lethebot-gov-ux-01b6a-rollback-GW85EJ`; the workspace stayed at 67 tracked plus 45 untracked paths, the canonical governance document remained ASCII-only with no Markdown link target, and migration 007/008 retained SHA-256 `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`/`95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`; no query/repository/mutation/CLI/QQ/browser/config/schema/migration/dependency/lockfile/Provider/private-data/live DB/deployment/restart/container/persistence-format or prior-slice behavior changed |
| 2026-07-25 10:01 CST | `GOV-UX-01B6B` deterministic gate | fail-first authenticated exact-scope maintenance-review list route; exact count-backed truncation; B2B/B5C/HTTP/application/P5 adjacency; canonical P8 contract; typecheck, scoped/full lint, release gate, hygiene, migration hashes, and exact seven-path B6A-to-B6B reverse/forward proof | exit 0; the first fail-first application run produced exactly two route-registration failures while three adjacent lifecycle tests passed, and the truncation audit then produced exactly the missing repository count, query page, and HTTP `truncated` failures while 27 adjacent assertions passed; final focused coverage passed 3 files / 30 tests, the six-file query/registry/HTTP/application/P5 boundary passed 52 tests, and the canonical P8 gate retained 6 files / 239 tests; `pnpm release:check` passed typecheck, lint, build, preflight, diff hygiene, 130 / 1 skipped files, and 2634 / 10 skipped tests; authenticated `GET /governance/api/v1/memory-reviews` now requires the issued exact session/purpose-bound scope handle, returns at most 100 pending-review DTOs with an exact same-scope/lifecycle `truncated` flag, accepts no query/body/raw identifier/limit/order input, exposes no raw proposal/memory/source value, performs no write, and rejects missing authentication, missing/malformed/cross-session handles, and query strings before query work; `/scopes`, `/overview`, HTTP security/lifecycle, B2B detail bounds, and P5 mutations remain unchanged; strict reverse and forward application restored the recorded B6A/B5C/B2B baseline and current candidate across seven paths byte-for-byte and mode-for-mode at `/tmp/lethebot-gov-ux-01b6b-final-reverse-check-FbrGoO.patch` (`sha256:459daf0060de08b53b2728dd2509bcd69a7180f67f92a58936e3a6124e6a8e9c`), mode `0600`, size 21510 bytes, with proof directory `/tmp/lethebot-gov-ux-01b6b-rollback-Zdrvzk`; the workspace stayed at 67 tracked plus 45 untracked paths, the canonical governance document remained ASCII-only with no Markdown link target, and migration 007/008 retained SHA-256 `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`/`95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`; no HTTP-dispatcher/scope-registry/mutation/CLI/QQ/browser/config/schema/migration/dependency/lockfile/Provider/private-data/live DB/deployment/restart/container/persistence-format or prior-slice behavior changed |
| 2026-07-25 10:34 CST | `GOV-UX-01B6C` deterministic gate | fail-first standalone resource registry, trusted exact-scope list issuance, dynamic detail dispatch, reciprocal session lifecycle, real list-to-detail wiring, B2B-B6B/P5 adjacency, canonical P8 contract, typecheck, scoped/full lint, release gate, hygiene, migration hashes, and exact nine-path B6B-to-B6C reverse/forward proof | exit 0; the fail-first four-file run collected 27 assertions, 22 existing assertions passed, and the expected missing registry module/query method, pre-auth dynamic-route `404`, zero resource lifecycle callbacks, and absent list handle were observed; final focused coverage passed 4 files / 31 tests, adjacency passed 8 files / 181 tests, and the canonical P8 gate passed 6 files / 316 tests; `pnpm release:check` passed typecheck, lint, build, preflight, diff hygiene, 131 / 1 skipped files, and 2640 / 10 skipped tests; the dependency-free registry issues stable random 256-bit handles bound to session digest, absolute expiry, exact scope, purpose, resource kind, and internal proposal ID with eight-session and 512-handle/session bounds, while rotation, lazy expiry, logout, listener close, and process restart revoke them; the exact-scope list adds only `handle` and `handleExpiresAt`, and authenticated `GET /governance/api/v1/memory-reviews/<resource-handle>` resolves scope before resource and reuses the bounded B2B detail DTO; malformed, missing, cross-session, cross-purpose, cross-kind, cross-scope, stale, and unavailable resources remain fixed pre-query `400`/`404` results with no raw identifier, query input, write, or diagnostic exposure; strict reverse and forward application restored the seven-file B6B baseline and nine-file B6C candidate byte-for-byte and mode-for-mode at `/tmp/lethebot-gov-ux-01b6c-final-reverse-check-CJOiqX.patch` (`sha256:f0dc4efe158bbf93170869182e623c397618c4999918f78a3c2bffe914489d0d`), mode `0600`, size 69824 bytes, with proof directory `/tmp/lethebot-gov-ux-01b6c-rollback-N77ZaZ` mode `0700`; the workspace is 67 tracked plus 47 untracked paths with zero staged paths, the only sensitive-name inventory match is the expected tracked `.env.example`, the canonical governance document is ASCII-only with no Markdown link target, and migration 007/008 retained SHA-256 `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`/`95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`; no repository/schema/migration/dependency/lockfile/mutation/CLI/QQ/browser/Provider/private-data/live DB/deployment/restart/container/persistence-format or prior-slice behavior changed |
| 2026-07-25 10:38 CST | `GOV-UX-01B7A` fail-first boundary | fresh single-use preview issuance, exact confirmation binding, lifecycle invalidation, fixed validation, and bounded capacity | expected nonzero; the new direct suite failed before collection on the exact missing `governance-preview-handle-registry` module while the B4 scope and B6C resource registries passed 2 files / 6 tests; only the locked test and mutable checkpoint changed, with no production, route, schema, dependency, persistence, live, or private-data operation |
| 2026-07-25 10:47 CST | `GOV-UX-01B7A` deterministic gate | fail-first standalone confirmation-preview registry; fresh single-use exact binding; expiry/revocation/clear/restart invalidation; bounded capacity and fixed validation; B4/B6C/HTTP lifecycle adjacency; canonical P8 contract; typecheck, scoped/full lint, release gate, hygiene, migration hashes, and exact three-path B6C-to-B7A reverse/forward proof | exit 0; the direct scope/resource/preview registry gate passed 3 files / 10 tests, registry plus HTTP lifecycle adjacency passed 4 files / 24 tests, and the canonical P8 gate passed 6 files / 242 tests; `pnpm release:check` passed typecheck, lint, build, preflight, diff hygiene, 132 / 1 skipped files, and 2644 / 10 skipped tests; the dependency-free registry issues a fresh random 256-bit base64url handle bound to the session digest, server-owned local-admin actor, exact scope, action, resource kind and internal ID, expected state/revision, SHA-256 preview digest, and the earlier of five minutes or session expiry; matching consumption returns only the trusted resource/state/revision/digest binding exactly once, while malformed, expired, revoked, cleared, restarted, cross-session, cross-scope, cross-action, cross-kind, consumed, over-capacity, and clock-invalid operations fail without eviction or input disclosure; strict reverse and forward application removed/restored both add-file paths and restored the exact B6C canonical document byte-for-byte and mode-for-mode at `/tmp/lethebot-gov-ux-01b7a-final-reverse-check-9R9qOl.patch` (`sha256:78782f2a7d607c2f1db74f7848e0a526857c7f573a7cf025d45d360b25d87af4`), mode `0600`, size 27247 bytes, with proof directory `/tmp/lethebot-gov-ux-01b7a-rollback-vhEY4F` mode `0700`; the workspace is 67 tracked plus 49 untracked paths with zero staged paths, the only sensitive-name inventory match is the expected tracked `.env.example`, the canonical governance document and both implementation paths are ASCII-only, the canonical document has no Markdown link target, and migration 007/008 retained SHA-256 `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`/`95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`; no HTTP/application/session-lifecycle wiring, route, DTO, governance/repository mutation, schema, migration, dependency/lockfile, CLI/QQ/browser, Provider, private-data, live DB, deployment/restart/container, persistence-format, or prior-slice behavior changed |
| 2026-07-25 10:53 CST | `GOV-UX-01B7B` fail-first boundary | validated preview-store dependency; exact parity across session rotation, lazy expiry, logout, failure non-revocation, listener clear, and real application ownership | expected nonzero; both integration files collected 19 tests, 16 existing tests passed, malformed preview-store options were accepted, the HTTP lifecycle spy observed zero preview revocations and zero clears instead of the exact three/one calls, and the real application never invoked `GovernancePreviewHandleRegistry.clear`; only the two locked integration tests and mutable checkpoint changed from the B7A baseline, with no production, route, DTO, registry, schema, dependency, persistence, live, or private-data operation |
| 2026-07-25 11:05 CST | `GOV-UX-01B7B` deterministic gate | explicit preview-store validation; exact session rotation/lazy-expiry/logout revocation; failure non-revocation; listener-close clearing; real application registry ownership; B7A/B4/B6C adjacency; canonical P8 contract; typecheck, scoped/full lint, release gate, hygiene, migration hashes, and exact five-path B7A-to-B7B reverse/forward proof | exit 0; the final HTTP lifecycle/application suites passed 2 files / 19 tests, registry plus lifecycle adjacency passed 5 files / 29 tests, and the documented canonical P8 command passed 6 files / 242 tests; `pnpm release:check` passed typecheck, lint, build, preflight, diff hygiene, 132 / 1 skipped files, and 2644 / 10 skipped tests; `GovernanceHttpServer` now validates one explicit lifecycle-only preview store, revokes the exact session digest after scope/resource stores on successful central session removal, clears it on listener close, and leaves unknown-cookie, rejected-credential, and full-capacity failures callback-free; `LetheBotApp` constructs one B7A registry with the shared clock for each application instance, while preview issue/consume, routes, DTOs, durable mutations, and all prior route behavior remain unchanged; strict reverse and forward application restored the exact B7A versions of the five implementation/contract paths byte-for-byte and mode-for-mode at `/tmp/lethebot-gov-ux-01b7b-final-reverse-check-wbb3IL.patch` (`sha256:966142da24f1f6a71a5e816134403777fbcc568cc91710a426c6eab140525491`), mode `0600`, size 11821 bytes, with proof directory `/tmp/lethebot-gov-ux-01b7b-rollback-GfRCZ5` mode `0700`; the workspace is 67 tracked plus 49 untracked paths with zero staged paths, the only sensitive-name inventory match is the expected tracked `.env.example`, the canonical governance document is ASCII-only with no Markdown link target, and migration 007/008 retained SHA-256 `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`/`95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`; no preview/confirmation/mutation route, DTO, issue/consume call, governance/repository call, schema, migration, dependency/lockfile, CLI/QQ/browser, Provider, private-data, live DB, deployment/restart/container, persistence-format, or prior-slice behavior changed |
| 2026-07-25 11:41 CST | `GOV-UX-01B7C` deterministic gate | fail-first bounded maintenance-approval preview, fixed query projection and digest, exact POST issuance, B7A/B7B lifecycle reuse, P5 non-mutation adjacency, canonical P8 contract, typecheck, scoped/full lint, release gate, hygiene, migration hashes, and exact six-path B7B-to-B7C reverse/forward proof | exit 0; the fail-first three-file run collected 29 tests with exactly the missing query method and absent POST route failures; final focused coverage passed 3 files / 29 tests, registry/P5 adjacency passed 7 files / 46 tests, and the canonical P8 gate passed 6 files / 243 tests; `pnpm release:check` passed typecheck, lint, build, preflight, diff hygiene, 132 / 1 skipped files, and 2645 / 10 skipped tests; authenticated exact-scope/resource `POST /governance/api/v1/memory-reviews/<resource-handle>` accepts only `{ "action": "approve" }`, issues a fresh B7A handle over one fixed redacted pending-review projection and SHA-256 digest, performs no preview consumption or durable write, and leaves approval confirmation, review transition, and memory application absent; strict reverse and forward application restored the B7B baseline and B7C candidate across the six fixed paths byte-for-byte and mode-for-mode at `/tmp/lethebot-gov-ux-01b7c-final-reverse-check-y3uuoO.patch` (`sha256:f5cb9b671c5e6543698fe3d9183e83d9a051b58ae296baf5b3e32f2f0c62adfd`), mode `0600`, size 32305 bytes, with proof directory `/tmp/lethebot-gov-ux-01b7c-rollback-R4oetk` mode `0700`; the restored candidate comparison passed for all six paths, the workspace is 67 tracked plus 49 untracked paths with zero staged paths, the canonical governance document is ASCII-only with no Markdown link target, and migration 007/008 retained SHA-256 `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`/`95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`; no schema, migration, dependency/lockfile, CLI/QQ/browser, Provider, private-data, live DB, deployment/restart/container, confirmation, or durable mutation behavior changed |
| 2026-07-25 12:40 CST | `GOV-UX-01B7D` deterministic gate | fail-first exact maintenance-approval confirmation; resource-bound single-use consumption; static-suffix dispatch; fixed redacted result; one existing governed review transition; B7A/B6C/P5 adjacency; canonical P8 contract; typecheck, scoped/full lint, release gate, hygiene, migration hashes, and exact nine-path B7C-to-B7D reverse/forward proof | exit 0; the fail-first four-file run collected 34 tests with 29 passing and five focused failures, final focused coverage passed 4 files / 34 tests, B7A/B6C/P5 adjacency passed 8 files / 64 tests, and the canonical P8 gate passed 6 files / 244 tests; `pnpm release:check` passed typecheck, lint, build, preflight, diff hygiene, 132 / 1 skipped files, and 2646 / 10 skipped tests; authenticated exact-scope/resource `POST /governance/api/v1/memory-reviews/<resource-handle>/confirm` accepts only `{ "confirm": true, "previewHandle": HANDLE }`, consumes a preview bound to the exact session, local-admin actor, approval action, resource kind and ID, and scope, recomputes state/revision/digest freshness, and invokes only `GovernanceService.reviewMemoryMaintenanceProposal` once with the fixed reason; success changes only `pending_review@1 -> approved@2` with one audit/revision, while unknown/cross-boundary previews are fixed `404`, consumed/stale previews are fixed `409`, and memory records, apply, and rollback remain untouched; strict reverse and forward application restored the exact B7C baseline and B7D candidate across nine paths byte-for-byte and mode-for-mode at `/tmp/lethebot-gov-ux-01b7d-final-reverse-check-OwfwTE.patch` (`sha256:af29cbeafafe1b0cb2c239da4301c3908217f49c7c1d5d9f88124b53576aa788`), mode `0600`, size 50042 bytes, with proof directory `/tmp/lethebot-gov-ux-01b7d-rollback-uZX5Zt` mode `0700`; the workspace remains 67 tracked plus 49 untracked paths with zero staged paths, the only sensitive-name inventory match is the expected tracked `.env.example`, all B7D additions are ASCII-only, the canonical governance document has no Markdown link target, and migration 007/008 retained SHA-256 `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`/`95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`; no memory application/rollback, additional mutation family, repository/schema/migration/dependency/lockfile, CLI/QQ/browser, Provider/private-data/live DB/deployment/restart/container/persistence-format, or prior-slice behavior changed |
| 2026-07-25 13:03 CST | `GOV-UX-01B8A` deterministic gate | fail-first bounded maintenance-rejection preview; fixed action/projection/digest; exact detail POST dispatch; fresh action-bound handle; B7A/B6C/B7D/P5 adjacency; canonical P8 contract; typecheck, scoped/full lint, release gate, hygiene, migration hashes, and exact five-path B7D-to-B8A reverse/forward proof | exit 0; the fail-first query/application command collected 16 tests with 14 existing assertions passing and exactly the missing query method plus existing `400` rejection dispatch failures; final focused coverage passed 2 files / 16 tests, B7A/B6C/B7D/P5 adjacency passed 8 files / 65 tests, and the canonical P8 gate passed 6 files / 245 tests; `pnpm release:check` passed typecheck, lint, build, preflight, diff hygiene, 132 / 1 skipped files, and 2647 / 10 skipped tests; authenticated exact-scope/resource `POST /governance/api/v1/memory-reviews/<resource-handle>` now accepts exact `{ "action": "reject" }` alongside the unchanged approval body, returns a fixed redacted `pending_review@N -> rejected@(N+1)` projection under a distinct domain-separated digest, and issues a fresh handle bound to the rejection action, session, local-admin actor, scope/resource, state/revision, and digest; repeated query projections are stable, approval/rejection digests differ, and issuance performs no consumption, governance call, durable write, memory application, or rollback; strict reverse and forward application restored the exact five-path B7D baseline and B8A candidate byte-for-byte and mode-for-mode at `/tmp/lethebot-gov-ux-01b8a-final-reverse-check-s8Tt1x.patch` (`sha256:2b856c581d28b2e30478fd6f0bc7b5f0f457bb352817113e654640272f702e60`), mode `0600`, size 25333 bytes, with proof directory `/tmp/lethebot-gov-ux-01b8a-rollback-s8Fb9V` mode `0700`; the workspace remains 67 tracked plus 49 untracked paths with zero staged paths, the only sensitive-name inventory match is the expected tracked `.env.example`, all B8A patch content is ASCII-only, the canonical governance document has no Markdown link target, and migration 007/008 retained SHA-256 `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`/`95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`; no rejection confirmation, memory application/rollback, registry/repository/schema/migration/dependency/lockfile, CLI/QQ/browser, Provider/private-data/live DB/deployment/restart/container/persistence-format, or prior-slice behavior changed |
| 2026-07-25 13:17 CST | `GOV-UX-01B8B` deterministic gate | fail-first exact maintenance-rejection confirmation; action-bound single-use consumption; fixed redacted result; one existing governed rejection transition; B7A/B6C/B7D/B8A/P5 adjacency; canonical P8 contract; typecheck, scoped/full lint, release gate, hygiene, migration hashes, and exact five-path B8A-to-B8B reverse/forward proof | exit 0; the fail-first query/application command collected 17 tests with 15 existing assertions passing and exactly the missing result projection plus approval-only body-validation failures; final focused coverage passed 2 files / 17 tests, adjacency passed 8 files / 66 tests, and the canonical P8 gate passed 6 files / 245 tests; `pnpm release:check` passed typecheck, lint, build, preflight, diff hygiene, 132 / 1 skipped files, and 2648 / 10 skipped tests; exact rejection confirmation consumes only the matching session/actor/action/scope/resource-bound B8A handle, recomputes its state/revision/digest freshness, and invokes `GovernanceService.reviewMemoryMaintenanceProposal` exactly once with fixed `reject` and `governance_http_rejection_confirmed`; success changes only `pending_review@1 -> rejected@2`, appends one proposal revision and one audit, returns a fixed redacted result, and leaves every memory record/revision plus apply/rollback untouched; invalid, cross-action, unknown, replayed, stale, and competing requests remain fixed `400`/`404`/`409` with no partial effect while the exact two-key approval confirmation stays compatible; strict reverse and forward application restored the five-path B8A baseline and B8B candidate byte-for-byte and mode-for-mode at `/tmp/lethebot-gov-ux-01b8b-final-reverse-check-fl6Bzm.patch` (`sha256:4867d50dba257c3711a3de4eaa3898b4105cde648f534a8df77cf4f696b0e8dd`), mode `0600`, size 30642 bytes, with proof directory `/tmp/lethebot-gov-ux-01b8b-rollback-vNGNq8` mode `0700`; the workspace remains 67 tracked plus 49 untracked paths with zero staged paths, the only sensitive-name inventory match is the expected tracked `.env.example`, all B8B patch content is ASCII-only, the canonical governance document has zero Markdown link targets, and migration 007/008 retained SHA-256 `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`/`95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`; no memory application/rollback, additional mutation family, registry/repository/schema/migration/dependency/lockfile, CLI/QQ/browser, Provider/private-data/live DB/deployment/restart/container/persistence-format, or prior-slice behavior changed |
| 2026-07-25 13:46 CST | `GOV-UX-01B9A` deterministic gate | fail-first bounded maintenance-application preview; approved-state projection; visible conflict selection; domain-separated digest and action-bound issuance; B7A/B6C/B7D/B8A/B8B/P5 adjacency; canonical P8 contract; typecheck, scoped/full lint, release gate, hygiene, migration hashes, and exact five-path B8B-to-B9A reverse/forward proof | exit 0; the fail-first query/application command collected 19 tests with 17 existing assertions passing and exactly the missing query method plus existing `400` application-dispatch failures; final focused coverage passed 2 files / 19 tests, adjacency passed 8 files / 68 tests, and the canonical P8 gate passed 6 files / 246 tests; `pnpm release:check` passed typecheck, lint, build, preflight, diff hygiene, 132 / 1 skipped files, and 2650 / 10 skipped tests; exact-scope authenticated detail POST accepts `{ "action": "apply" }` for approved consolidation/decay proposals and `{ "action": "apply", "retainedMemoryRef": REF }` for approved conflicts, projects fixed redacted affected-role counts/fingerprints, durable effects, retrieval consequences, and a separately-confirmed rollback boundary, and issues a fresh `memory.maintenance.apply` handle without consumption or durable mutation; conflict selection is limited to the maximum 32 candidates visible in the detail DTO, duplicate 16-character references deny preview, and a 33-candidate proof keeps the hidden candidate unselectable while aggregating all 33 effects; strict reverse and forward application restored the five-path B8B baseline and B9A candidate byte-for-byte and mode-for-mode at `/tmp/lethebot-gov-ux-01b9a-final-reverse-check-5Dwetc.patch` (`sha256:b4228e69886a28e5257710335b69c53de77a725661555899213540c36b03ff4f`), mode `0600`, size 52176 bytes, with proof directory `/tmp/lethebot-gov-ux-01b9a-rollback-TfTm9m` mode `0700`; the workspace remains 67 tracked plus 49 untracked paths with zero staged paths, the only sensitive-name inventory match is the expected tracked `.env.example`, all patch content is ASCII-only, the canonical governance document has zero Markdown link targets, and migration 007/008 retained SHA-256 `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`/`95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`; no application confirmation, preview consumption, governance apply/rollback, repository/schema/migration/dependency/lockfile, CLI/QQ/browser, Provider/private-data/live DB/deployment/restart/container/persistence-format, or prior-slice behavior changed |
| 2026-07-25 14:11 CST | `GOV-UX-01B9B` deterministic gate | fail-first exact maintenance-application confirmation; trusted conflict-reference resolution; action-bound single-use consumption; current plan/digest recomputation; existing governed apply operation; fixed redacted result; B7A/B6C/B7D/B8A/B8B/B9A/P5 adjacency; canonical P8 contract; typecheck, scoped/full lint, release gate, hygiene, migration hashes, and exact five-path B9A-to-B9B reverse/forward proof | exit 0; after correcting the new synthetic reference lookup to the production NUL-separated domain, the fail-first command collected 19 tests with 17 existing assertions passing and exactly the missing resolver plus existing `400` confirmation failures; final focused coverage passed 2 files / 19 tests, adjacency passed 8 files / 68 tests, and the canonical P8 gate passed 6 files / 246 tests; `pnpm release:check` passed typecheck, lint, build, preflight, diff hygiene, 132 / 1 skipped files, and 2650 / 10 skipped tests; exact application confirmation accepts the three-key apply body for consolidation/decay and requires the visible retained reference again for conflicts, consumes only the matching session/actor/action/scope/resource-bound B9A handle, resolves raw retained memory ID only inside the trusted query/application boundary, recomputes approved state/revision/selection/digest freshness, and invokes `GovernanceService.applyMemoryMaintenanceProposal` exactly once with fixed local-admin authority and reason; the mismatch proof runs while the proposal remains approved and produces `409` before any apply call, while three successes atomically append three apply proposal revisions/audits and five retained/superseded/disabled memory revisions/effect links, immediately exclude inactive memories from retrieval, return fixed redacted applied results, and keep replay at `409`; strict reverse and forward application restored the five-path B9A baseline and B9B candidate byte-for-byte and mode-for-mode at `/tmp/lethebot-gov-ux-01b9b-final-reverse-check-0AUatj.patch` (`sha256:82ce2ac4b64124d206167c6b11a4d51d7321d36f97ead9672ecd4f100a526fc7`), mode `0600`, size 37961 bytes, with proof directory `/tmp/lethebot-gov-ux-01b9b-rollback-wxe0C7` mode `0700`; the workspace remains 67 tracked plus 49 untracked paths with zero staged paths, the only sensitive-name inventory match is the expected tracked `.env.example`, all patch content is ASCII-only, the canonical governance document has zero Markdown link targets, and migration 007/008 retained SHA-256 `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`/`95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`; no rollback preview/confirmation, new governance operation, repository/schema/migration/dependency/lockfile, CLI/QQ/browser, Provider/private-data/live DB/deployment/restart/container/persistence-format, or prior-slice behavior changed |
| 2026-07-25 14:29 CST | `GOV-UX-01B10A` deterministic gate | fail-first bounded applied-maintenance rollback preview; restored-role aggregate and domain-separated digest; exact detail POST dispatch; fresh rollback-action handle; B7A/B6C/B7D/B8A/B8B/B9A/B9B/P5 adjacency; canonical P8 contract; typecheck, scoped/full lint, release gate, hygiene, migration hashes, and exact five-path B9B-to-B10A reverse/forward proof | exit 0; the fail-first query/application command collected 20 tests with 18 existing assertions passing and exactly the missing query method plus existing `400` rollback-preview dispatch failures; final focused coverage passed 2 files / 20 tests, expanded handle/HTTP/P5 adjacency passed 8 files / 73 tests, and the canonical P8 gate passed 6 files / 247 tests; `pnpm release:check` passed typecheck, lint, build, preflight, diff hygiene, 132 / 1 skipped files, and 2651 / 10 skipped tests; exact-scope authenticated detail POST accepts only `{ "action": "rollback" }` for an applied conflict/consolidation/decay proposal, projects every candidate under one restored-role count/fingerprint with the fixed durable writes and `restored_records_included` consequence, and issues a fresh `memory.maintenance.rollback` handle bound to session, actor, scope/resource, applied state/revision, and a distinct digest without consumption, governance rollback, durable mutation, or retrieval change; strict reverse and forward application restored the five-path B9B baseline and B10A candidate byte-for-byte and mode-for-mode at `/tmp/lethebot-gov-ux-01b10a-final-reverse-check-t5bm5H.patch` (`sha256:aec25bd9228a19781da7227ff7cc75824e471ccec66baaa8b3d0335ff419019b`), mode `0600`, size 24782 bytes, with proof directory `/tmp/lethebot-gov-ux-01b10a-rollback-YK8dUk` mode `0700`; the workspace remains 67 tracked plus 49 untracked paths with zero staged paths, the only sensitive-name inventory match is the expected tracked `.env.example`, all B10A patch content is ASCII-only, the canonical governance document has zero Markdown link targets, and migration 007/008 retained SHA-256 `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`/`95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`; no rollback confirmation, preview consumption, new governance/repository operation, schema/migration/dependency/lockfile, CLI/QQ/browser, Provider/private-data/live DB/deployment/restart/container/persistence-format, or prior-slice behavior changed |
| 2026-07-25 14:52 CST | `GOV-UX-01B10B` deterministic gate | fail-first exact maintenance-rollback confirmation; rollback-action single-use consumption; current projection/digest recomputation; existing governed rollback operation; fixed terminal result; B7A/B6C/B7D/B8A/B8B/B9A/B9B/B10A/P5 adjacency; canonical P8 contract; typecheck, scoped/full lint, release gate, hygiene, migration hashes, and exact five-path B10A-to-B10B reverse/forward proof | exit 0; the fail-first two-file command collected 20 tests with 18 existing assertions passing and exactly the missing confirmation projection plus existing `400` confirmation-dispatch failures; final focused coverage passed 2 files / 20 tests, expanded handle/HTTP/P5 adjacency passed 8 files / 73 tests, and the canonical P8 gate passed 6 files / 247 tests; `pnpm release:check` passed typecheck, lint, build, preflight, diff hygiene, 132 / 1 skipped files, and 2651 / 10 skipped tests; exact rollback confirmation accepts only `{ "confirm": true, "previewHandle": HANDLE, "action": "rollback" }`, consumes only the matching session/actor/action/scope/resource-bound B10A handle, recomputes applied state, revision, restored-role plan, and digest freshness, then invokes `GovernanceService.rollbackMemoryMaintenanceProposal` exactly once with fixed `governance_http_rollback_confirmed`; conflict, consolidation, and decay successes atomically restore all five synthetic candidates to active retrieval with proposal/audit/memory-revision/effect evidence, return one fixed redacted rolled-back result with `rollback_is_terminal`, reject cross-action handles as `404`, and reject replay as `409`; strict reverse and forward application restored the exact five-path B10A baseline and B10B candidate byte-for-byte and mode-for-mode at `/tmp/lethebot-gov-ux-01b10b-final-reverse-check-P50Oyj.patch` (`sha256:d99dd62591619a6b9ac076b33cdc3a85aa6467026fe4f663e64989402ce685cb`), mode `0600`, size 27468 bytes, with proof directory `/tmp/lethebot-gov-ux-01b10b-rollback-AT4Tkd` mode `0700`; the candidate remained identical to the workspace across all five paths, the workspace remains 67 tracked plus 49 untracked paths with zero staged paths, the only sensitive-name inventory match is the expected tracked `.env.example`, all B10B patch content is ASCII-only, the canonical governance document has zero Markdown link targets, and migration 007/008 retained SHA-256 `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`/`95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`; no new governance/repository operation, schema/migration/dependency/lockfile, CLI/QQ/browser, Provider/private-data/live DB/deployment/restart/container/persistence-format, or prior-slice behavior changed |
| 2026-07-25 15:18 CST | `GOV-UX-01B11A` deterministic gate | fail-first bounded maintenance-expiration preview; pending-review projection and distinct digest; exact detail POST dispatch; fresh expiration-action handle; B7A/B6C/B7D/B8A/B8B/B9A/B9B/B10A/B10B/P5 adjacency; canonical P8 contract; typecheck, scoped/full lint, release gate, hygiene, migration hashes, and exact five-path B10B-to-B11A reverse/forward proof | exit 0; the fail-first two-file command collected 20 tests with 18 existing assertions passing and exactly the missing query method plus existing `400` expiration-preview dispatch failures; final focused coverage passed 2 files / 20 tests, expanded handle/HTTP/P5 adjacency passed 8 files / 69 tests, and the canonical P8 gate passed 6 files / 247 tests; `pnpm release:check` passed typecheck, lint, build, preflight, diff hygiene, 132 / 1 skipped files, and 2651 / 10 skipped tests; exact-scope authenticated detail POST accepts only `{ "action": "expire" }` for a pending conflict/consolidation/decay proposal, predicts `pending_review@N -> expired@(N+1)` with the existing review-ledger writes, unavailable memory mutation, and `expiration_does_not_apply_memory_effects`, and issues a fresh `memory.maintenance.review.expire` handle bound to session, actor, scope/resource, current state/revision, and a distinct domain-separated digest without consumption, governance call, durable mutation, or retrieval change; strict reverse and forward application restored the exact five-path B10B baseline and B11A candidate byte-for-byte and mode-for-mode at `/tmp/lethebot-gov-ux-01b11a-final-reverse-check-p6nW0S.patch` (`sha256:87e6904c325b802ec829fc87d6652b66142d6829bd663c3def3cc91c8bc9255f`), mode `0600`, size 25797 bytes, with proof directory `/tmp/lethebot-gov-ux-01b11a-rollback-wOSCiz` mode `0700`; the candidate remained identical to the workspace across all five paths, the workspace remains 67 tracked plus 49 untracked paths with zero staged paths, the only sensitive-name inventory match is the expected tracked `.env.example`, all B11A patch content is ASCII-only, the canonical governance document has zero Markdown link targets, and migration 007/008 retained SHA-256 `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`/`95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`; no expiration confirmation, preview consumption, governance/repository mutation, schema/migration/dependency/lockfile, CLI/QQ/browser, Provider/private-data/live DB/deployment/restart/container/persistence-format, or prior-slice behavior changed |
| 2026-07-25 15:36 CST | `GOV-UX-01B11B` deterministic gate | fail-first exact maintenance-expiration confirmation; expiration-action single-use consumption; current projection/digest recomputation; existing governed expiration transition; fixed terminal result; B7A/B6C/B7D/B8A/B8B/B9A/B9B/B10A/B10B/B11A/P5 adjacency; canonical P8 contract; typecheck, scoped/full lint, release gate, hygiene, migration hashes, and exact five-path B11A-to-B11B reverse/forward proof | exit 0; the fail-first two-file command collected 20 tests with 18 existing assertions passing and exactly the missing confirmation projection plus existing `400` confirmation-dispatch failures; final focused coverage passed 2 files / 20 tests, expanded handle/HTTP/P5 adjacency passed 8 files / 69 tests, and the canonical P8 gate passed 6 files / 247 tests; `pnpm release:check` passed typecheck, lint, build, preflight, diff hygiene, 132 / 1 skipped files, and 2651 / 10 skipped tests; exact expiration confirmation accepts only `{ "confirm": true, "previewHandle": HANDLE, "action": "expire" }`, consumes only the matching session/actor/action/scope/resource-bound B11A handle, recomputes pending state, revision, and expiration digest freshness, then invokes `GovernanceService.reviewMemoryMaintenanceProposal` exactly once with transition `expire` and fixed `governance_http_expiration_confirmed`; success appends only one expired proposal revision and audit, returns fixed redacted proposal/revision/audit references, leaves all memory records, revisions, apply/rollback evidence, and retrieval unchanged, rejects cross-action handles as `404`, and rejects stale or replayed confirmation as `409`; strict reverse and forward application restored the exact five-path B11A baseline and B11B candidate byte-for-byte and mode-for-mode at `/tmp/lethebot-gov-ux-01b11b-final-reverse-check-e4qhZ4.patch` (`sha256:3bb3b07fe62147bc9854bce2c2868f6f89121d7a45ddd2f1bd6039c8fac35695`), mode `0600`, size 31116 bytes, with proof directory `/tmp/lethebot-gov-ux-01b11b-rollback-LBbSae` mode `0700` and candidate snapshot `/tmp/lethebot-gov-ux-01b11b-candidate-znlOH9/candidate` mode `0700`; the candidate remained identical to the workspace across all five paths, the workspace remains 67 tracked plus 49 untracked paths with zero staged paths, the only sensitive-name inventory match is the expected tracked `.env.example`, all B11B patch content is ASCII-only, the canonical governance document has zero Markdown link targets, and migration 007/008 retained SHA-256 `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`/`95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`; no new governance/repository operation, schema/migration/dependency/lockfile, CLI/QQ/browser, Provider/private-data/live DB/deployment/restart/container/persistence-format, or prior-slice behavior changed |
| 2026-07-25 15:51 CST | `GOV-UX-01B12A` deterministic gate | fail-first shared bounded audit-inspection read extraction; exact filter/order/limit/date and redaction parity; CLI delegation without reshaping; HTTP non-exposure; canonical P8 contract; typecheck, scoped/full lint, release gate, hygiene, migration hashes, and exact five-path B11B-to-B12A reverse/forward proof | exit 0; the fail-first query/CLI unit command collected 51 tests with 49 existing assertions passing and exactly the missing shared method plus missing delegation failures; final focused coverage passed 2 files / 51 tests, audit-focused CLI adjacency passed 25 tests with 104 unrelated tests skipped, HTTP security/lifecycle adjacency passed 2 files / 22 tests, and the canonical P8 gate passed 6 files / 249 tests; `pnpm release:check` passed typecheck, lint, build, preflight, diff hygiene, 132 / 1 skipped files, and 2653 / 10 skipped tests; `GovernanceQueryService.listAudit` now owns the existing category/level/event/actor/risk/time filters, stable timestamp-and-ID ordering, default 100-row bound, fixed date-bearing DTO, default-hidden details, and deterministic display/recursive redaction, while `GovernanceCLI.listAudit` delegates and re-exports the moved public types without reshaping; strict reverse and forward application restored the exact five-path B11B baseline and B12A candidate byte-for-byte and mode-for-mode at `/tmp/lethebot-gov-ux-01b12a-final-reverse-check-7zf8xl.patch` (`sha256:8a9217b0f65775c5499019377905aa79061f171b287aba6490cde53de22c4953`), mode `0600`, size 22801 bytes, with proof directory `/tmp/lethebot-gov-ux-01b12a-rollback-9eNjE8` mode `0700` and candidate snapshot `/tmp/lethebot-gov-ux-01b12a-candidate-tCSSMH/candidate` mode `0700`; the candidate remained identical to the workspace across all five paths, the workspace is 68 tracked plus 49 untracked paths with zero staged paths, the only sensitive-name inventory match is the expected tracked `.env.example`, all B12A patch content is ASCII-only, the canonical governance document has zero Markdown link targets, and migration 007/008 retained SHA-256 `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`/`95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`; no HTTP/browser route, governance/repository mutation, command grammar/output, schema/migration/dependency/lockfile, QQ, Provider/private-data/live DB/deployment/restart/container/persistence-format, or prior-slice behavior changed |

| 2026-07-25 16:32 CST | `GOV-UX-01B12B` deterministic gate | fail-first shared exact memory-provenance detail read extraction; record/source/revision/date/order/redaction parity; linked shared-audit reuse; CLI delegation without reshaping; HTTP non-exposure; canonical P8 contract; typecheck, scoped/full lint, release gate, recovery hygiene, migration hashes, and exact five-path B12A-to-B12B reverse/forward proof | exit 0; fail-first collected 2 files / 53 tests with 51 existing assertions passing and exactly the missing shared method plus missing delegation failures; final focused coverage passed 2 files / 53 tests, show/export CLI adjacency passed 2 tests with 127 unrelated tests skipped, HTTP security/lifecycle adjacency passed 2 files / 22 tests, and the canonical P8 gate passed 6 files / 251 tests; `pnpm release:check` passed typecheck, lint, build, preflight, diff hygiene, 132 / 1 skipped files, and 2655 / 10 skipped tests; `GovernanceQueryService.showMemory` now owns exact repository lookup, stable source/revision reads, recursive state redaction, and the linked `{ category: 'memory', eventId, includeDetails: false, limit: 100 }` audit call, while `GovernanceCLI.showMemory` delegates and re-exports the moved types without reshaping; an exploratory `vitest list --json` invocation was parsed with the protected NapCat test path as its output, so recovery removed the generated two-byte `true` artifact, reconstructed that 13-test file's sole current-program host-default assertion from HEAD/current config/collected names, and re-established 13 / 13 before the final gate; strict reverse and forward application restored all five B12A baseline and B12B candidate paths byte-for-byte and mode-for-mode at `/tmp/lethebot-gov-ux-01b12b-roundtrip-55iQ0k/gov-ux-01b12b.patch` (`sha256:2337723b6e2cddd0525b0d49301c716de119c21b9b89652fc849838b0f624133`, mode `0600`, size `25553`) with proof directory `/tmp/lethebot-gov-ux-01b12b-roundtrip-55iQ0k` mode `0700`; the workspace is 68 tracked plus 49 untracked paths with zero staged paths, the only sensitive-name inventory match is the expected tracked `.env.example`, the patch's sole non-ASCII line is a pre-existing CLI comment, the canonical governance document has zero Markdown link targets, and migration 007/008 retained SHA-256 `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`/`95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`; no HTTP/browser route, governance/repository mutation, command grammar/output, schema/migration/dependency/lockfile, QQ, Provider/private-data/live DB/deployment/restart/container/persistence-format, or prior-slice behavior changed |
| 2026-07-25 16:59 CST | `GOV-UX-01B12C` deterministic gate | fail-first shared database-backed memory-record list extraction; exact filter/join/default/order/limit/hydration/date/value parity; repository-only fallback and CLI delegation; HTTP non-exposure; canonical P8 contract; typecheck, scoped/full lint, release gate, hygiene, migration hashes, and exact five-path B12B-to-B12C reverse/forward proof | exit 0; fail-first collected 2 files / 55 tests with 53 existing assertions passing and exactly the missing shared method plus missing delegation failures; final focused coverage passed 2 files / 55 tests, list/show/export CLI adjacency passed 2 tests with 127 unrelated tests skipped, HTTP security/lifecycle adjacency passed 2 files / 22 tests, and the canonical P8 gate passed 6 files / 253 tests; `pnpm release:check` passed typecheck, lint, build, preflight, diff hygiene, 132 / 1 skipped files, and 2657 / 10 skipped tests; `GovernanceQueryService.listMemory` now owns the existing conditional source join, active default, exact state/user/group/conversation/scope/sensitivity/source-context/source-type/source-ID filters, importance/creation ordering, default 100-row SQL bound, repository hydration, and missing-row omission, while `GovernanceCLI.listMemory` delegates only its database branch and re-exports the moved option type without reshaping; the repository-only fallback remains unchanged and direct evidence covers source-less rows, individual source filters, exact dates/values, pre-hydration limiting, and zero writes; strict reverse and forward application restored all five B12B baseline and B12C candidate paths byte-for-byte and mode-for-mode at `/tmp/lethebot-gov-ux-01b12c-roundtrip-lMsTWS/gov-ux-01b12c.patch` (`sha256:b02357e3d5ca9cd979d571c267f8ce4e815238d1051337326d5ae3b6dfdf5b0c`, mode `0600`, size `19273`) with proof directory `/tmp/lethebot-gov-ux-01b12c-roundtrip-lMsTWS` and fresh baseline `/tmp/lethebot-gov-ux-01b12c-baseline-oNARY2`, both mode `0700`; the candidate remained identical to the workspace across all five paths, the workspace is 68 tracked plus 49 untracked paths with zero staged paths, the only sensitive-name inventory match is the expected tracked `.env.example`, all B12C patch content is ASCII-only, the canonical governance document has zero Markdown link targets, and migration 007/008 retained SHA-256 `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`/`95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`; no HTTP/browser route, governance/repository mutation, command grammar/output, export behavior, schema/migration/dependency/lockfile, QQ, Provider/private-data/live DB/deployment/restart/container/persistence-format, or prior-slice behavior changed |
| 2026-07-26 11:05 CST | `GOV-UX-01B12D` deterministic gate | fail-first shared memory-export projection extraction; default/explicit-state forwarding; secret/prohibited exclusion; deterministic redaction; fixed fields and ISO dates; database delegation and repository-only fallback parity; HTTP non-exposure; canonical P8 contract; typecheck, lint, release gate, hygiene, migration hashes, and exact five-path B12C-to-B12D reverse/forward proof | exit 0; fail-first collected 2 files / 57 tests with 55 existing assertions passing and exactly the missing shared method plus missing delegation failures; final focused coverage passed 2 files / 57 tests, CLI-main adjacency passed 3 tests with 126 unrelated tests skipped, HTTP security/lifecycle adjacency passed 2 files / 22 tests, and the canonical P8 gate passed 6 files / 255 tests; the current `pnpm release:check` passed typecheck, lint, build, preflight, diff hygiene, 132 / 1 skipped files, and 2659 / 10 skipped tests; `GovernanceQueryService.exportMemory` now composes the shared memory list with one pure fixed export projector, excludes secret/prohibited records, deterministically redacts retained fields, and emits exact ISO dates, while `GovernanceCLI.exportMemory` delegates its database branch without reshaping and retains repository-only retrieval through the same projector; the handoff-named B12D baseline directory was absent on resume, so the exact B12C boundary was reconstructed by reversing only B12D, proved with typecheck and 2 files / 55 tests, captured privately, and then used for strict reverse/forward application; all five B12C baseline and B12D candidate paths round-tripped byte-for-byte and mode-for-mode at `/tmp/lethebot-gov-ux-01b12d-roundtrip-eIsKMT/gov-ux-01b12d.patch` (`sha256:9fd1dff26da55150d54f872332402425872a505654e44374c24b897d4eb86bfd`, mode `0600`, size `17058`) with proof directory mode `0700`; the restored workspace matched the pre-reconstruction candidate exactly, remains 68 tracked plus 49 untracked paths with zero staged paths, the only sensitive-name inventory match is the expected tracked `.env.example`, the patch's sole non-ASCII line is a pre-existing CLI comment, the canonical governance document has zero Markdown link targets, and migration 007/008 retained SHA-256 `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`/`95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`; no HTTP/browser route, list/repository fallback behavior, governance/repository mutation, command grammar/output, schema/migration/dependency/lockfile, QQ, Provider/private-data/live DB/deployment/restart/container/persistence-format, or prior-slice behavior changed |
| 2026-07-26 11:18 CST | `GOV-UX-01B12E` deterministic gate | fail-first shared model-invocation summary extraction; exact filters/counts/finite grouping/known-unknown usage/latency parity; CLI delegation without reshaping; HTTP non-exposure; canonical P8 contract; typecheck, scoped/full lint, release gate, hygiene, migration hashes, and exact five-path B12D-to-B12E reverse/forward proof | exit 0; fail-first collected 2 files / 59 tests with 57 existing assertions passing and exactly the missing shared method plus missing delegation failures; final focused coverage passed 2 files / 59 tests, CLI-process adjacency passed 1 test with 128 unrelated tests skipped, HTTP security/lifecycle adjacency passed 2 files / 22 tests, and the canonical P8 gate passed 6 files / 257 tests; `pnpm release:check` passed typecheck, lint, build, preflight, diff hygiene, 132 / 1 skipped files, and 2661 / 10 skipped tests; `GovernanceQueryService.summarizeModelInvocations` now owns the exact filtered aggregate SQL, finite purpose/status grouping, all-fields-required known usage semantics, and unchanged `collectModelInvocationLatency` composition, while `GovernanceCLI.summarizeModelInvocations` is a typed delegate returning the shared object unchanged; strict reverse and forward application restored the exact B12D baseline and B12E candidate across all five paths byte-for-byte and mode-for-mode at `/tmp/lethebot-gov-ux-01b12e-roundtrip-tbz2t2/gov-ux-01b12e.patch` (`sha256:8a55e766855b848419d29d183c4481e4e3686dace07e09f49ad490129338dbd4`, mode `0600`, size `19295`) with proof directory mode `0700`; the workspace remains 68 tracked plus 49 untracked paths with zero staged paths, the only sensitive-name inventory match is the expected tracked `.env.example`, `git diff --check` is clean, and migration 007/008 retained SHA-256 `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`/`95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`; no HTTP/browser route, P3 ledger/latency behavior, governance/repository mutation, command grammar/output, schema/migration/dependency/lockfile, QQ, Provider/private-data/live DB/deployment/restart/container/persistence-format, or prior-slice behavior changed |
| 2026-07-26 11:32 CST | `GOV-UX-01B12F` deterministic gate | fail-first shared bounded tool-call inspection extraction; exact filters/order/limits/payload/redaction/date parity; shared pure projector with unchanged context explanation; CLI delegation without reshaping; HTTP non-exposure; canonical P8 contract; typecheck, scoped/full lint, release gate, hygiene, migration hashes, and exact five-path B12E-to-B12F reverse/forward proof | exit 0; after correcting the new fixture's schema-invalid `requested_by`, fail-first collected 2 files / 61 tests with 59 existing assertions passing and exactly the missing shared method plus missing delegation failures; final focused coverage passed 2 files / 61 tests, CLI-process list/explanation adjacency passed 2 tests with 127 unrelated tests skipped, HTTP security/lifecycle adjacency passed 2 files / 22 tests, and the canonical P8 gate passed 6 files / 259 tests; `pnpm release:check` passed typecheck, lint, build, preflight, diff hygiene, 132 / 1 skipped files, and 2663 / 10 skipped tests; `GovernanceQueryService.listToolCalls` now owns the exact conjunctive filters, descending timestamp/ID order, default 100-row bound, opt-in payload parsing, fixed DTO, and deterministic redaction, while `GovernanceCLI.listToolCalls` is a typed identity delegate and its ascending context-explanation query reuses the same pure projector without adopting the bounded list semantics; strict reverse and forward application restored the exact B12E baseline and B12F candidate across all five paths byte-for-byte and mode-for-mode at `/tmp/lethebot-gov-ux-01b12f-roundtrip-xOglZe/gov-ux-01b12f.patch` (`sha256:d8a6df59efbacaca70e98f58677c1285fb45ad65b073c18b9a67a077b35d6352`, mode `0600`, size `20329`) with proof directory mode `0700` and fresh baseline `/tmp/lethebot-gov-ux-01b12f-baseline-HMThnB`; the workspace remains 68 tracked plus 49 untracked paths with zero staged paths, `git diff --check` is clean, and migration 007/008 retained SHA-256 `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`/`95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`; no HTTP/browser route, tool-call persistence/policy/audit behavior, context selection/order, governance/repository mutation, command grammar/output, schema/migration/dependency/lockfile, QQ, Provider/private-data/live DB/deployment/restart/container/persistence-format, or prior-slice behavior changed |
| 2026-07-26 11:44 CST | `GOV-UX-01B12G` deterministic gate | fail-first shared bounded action-decision inspection extraction; exact filters/order/limits/action/redaction/date parity; shared pure projector with unchanged context explanation; CLI delegation without reshaping; HTTP non-exposure; canonical P8 contract; typecheck, scoped/full lint, release gate, hygiene, migration hashes, and exact five-path B12F-to-B12G reverse/forward proof | exit 0; fail-first collected 2 files / 63 tests with 61 existing assertions passing and exactly the missing shared method plus missing delegation failures; final focused coverage passed 2 files / 63 tests, CLI-process list/explanation adjacency passed 2 tests with 127 unrelated tests skipped, HTTP security/lifecycle adjacency passed 2 files / 22 tests, and the canonical P8 gate passed 6 files / 261 tests; `pnpm release:check` passed typecheck, lint, build, preflight, diff hygiene, 132 / 1 skipped files, and 2665 / 10 skipped tests; `GovernanceQueryService.listActionDecisions` now owns the exact conjunctive filters, descending timestamp/ID order, default 100-row bound, optional action parsing, fixed DTO, and deterministic redaction, while `GovernanceCLI.listActionDecisions` is a typed identity delegate and its linked-decision context-explanation query reuses the same pure projector without changing selection or its one-row bound; strict reverse and forward application restored the exact B12F baseline and B12G candidate across all five paths byte-for-byte and mode-for-mode at `/tmp/lethebot-gov-ux-01b12g-roundtrip-ccL9u7/gov-ux-01b12g.patch` (`sha256:183fb361c197f6b562c6aa7761a3c3838aa311d0551176f7cc2bccebb3503d14`, mode `0600`, size `20692`) with proof directory mode `0700` and fresh baseline `/tmp/lethebot-gov-ux-01b12g-baseline-og0JV1`; the workspace remains 68 tracked plus 49 untracked paths with zero staged paths, `git diff --check` is clean, the sensitive-name inventory contains only the expected `.env.example`, the B12G patch is ASCII-only, the canonical governance document has zero Markdown link targets, and migration 007/008 retained SHA-256 `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`/`95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`; no HTTP/browser route, action persistence/execution/evaluator behavior, context decision selection/execution lookup/effect labels, governance/repository mutation, command grammar/output, schema/migration/dependency/lockfile, QQ, Provider/private-data/live DB/deployment/restart/container/persistence-format, or prior-slice behavior changed |
| 2026-07-26 12:15 CST | `GOV-UX-01B12H` deterministic gate | fail-first shared bounded action-execution inspection extraction; exact filters/order/limits/audit/redaction/date parity; shared pure projector with unchanged context explanation; CLI delegation without reshaping; HTTP non-exposure; canonical P8 contract; typecheck, scoped/full lint, release gate, hygiene, migration hashes, and exact five-path B12G-to-B12H reverse/forward proof | exit 0; fail-first collected 2 files / 65 tests with 63 existing assertions passing and exactly the missing shared method plus missing delegation failures; final focused coverage passed 2 files / 65 tests, CLI-process list/explanation adjacency passed 2 tests with 127 unrelated tests skipped, HTTP security/lifecycle adjacency passed 2 files / 22 tests, and the canonical P8 gate passed 6 files / 263 tests; `pnpm release:check` passed typecheck, lint, build, preflight, diff hygiene, 132 / 1 skipped files, and 2667 / 10 skipped tests; `GovernanceQueryService.listActionExecutions` now owns the exact conjunctive filters, descending execution-time/ID order, default 100-row bound, opt-in audit entry, fixed nullable DTO, and deterministic redaction, while `GovernanceCLI.listActionExecutions` is a typed identity delegate and its context-explanation query retains `WHERE action_decision_id = ?`, ascending execution-time/ID order, and CLI-owned reaction effect labels while reusing the pure projector without audit entries; strict reverse and forward application restored the exact B12G baseline and B12H candidate across all five paths byte-for-byte and mode-for-mode at `/tmp/lethebot-gov-ux-01b12h-roundtrip-aTpXK9/gov-ux-01b12h.patch` (`sha256:736aa0cf4a121d0456840649fa02c931cd4f1a8bad540e2661bb7f344a9cbcaa`, mode `0600`, size `24371`) with proof directory mode `0700` and fresh baseline `/tmp/lethebot-gov-ux-01b12h-baseline-czglCi`; the workspace remains 68 tracked plus 49 untracked paths with zero staged paths, `git diff --check` is clean, the sensitive-name inventory contains only the expected `.env.example`, the B12H patch is ASCII-only, the canonical governance document has zero Markdown link targets, and migration 007/008 retained SHA-256 `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`/`95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`; no HTTP/browser route, action persistence/execution/evaluator behavior, context decision selection/execution lookup/order/effect labels, governance/repository mutation, command grammar/output, schema/migration/dependency/lockfile, QQ, Provider/private-data/live DB/deployment/restart/container/persistence-format, or prior-slice behavior changed |
| 2026-07-26 12:30 CST | `GOV-UX-01B12I` deterministic gate | fail-first shared bounded job inspection extraction; exact conjunctive filters, ascending order, limits, payload/result opt-in, recursive redaction, malformed-JSON fallback, nullable fields, and date parity; CLI identity delegation; job/worker and HTTP non-exposure adjacency; canonical P8 contract; typecheck, scoped/full lint, release gate, hygiene, migration hashes, and exact five-path B12H-to-B12I reverse/forward proof | exit 0; fail-first collected 2 files / 67 tests with 65 existing assertions passing and exactly the missing shared method plus missing delegation failures; final focused coverage passed 2 files / 67 tests, CLI-process job inspection adjacency passed 1 test with 128 unrelated tests skipped, job repository/background-worker/scheduler adjacency passed 3 files / 67 tests, HTTP security/lifecycle adjacency passed 2 files / 22 tests, and the canonical P8 gate passed 6 files / 265 tests; `pnpm release:check` passed typecheck, lint, build, preflight, diff hygiene, 132 / 1 skipped files, and 2669 / 10 skipped tests; `GovernanceQueryService.listJobs` now owns the exact status/type filters, ascending schedule/creation order, default 100-row bound, opt-in payload/result parsing, fixed nullable DTO, malformed-JSON fallback, and deterministic recursive redaction, while `GovernanceCLI.listJobs` is a typed identity delegate and re-exports the moved public types without reshaping; strict reverse and forward application restored the exact B12H baseline and B12I candidate across all five paths byte-for-byte and mode-for-mode at `/tmp/lethebot-gov-ux-01b12i-roundtrip-fYtwNq/gov-ux-01b12i.patch` (`sha256:24ad7cf4f4bac62a84086835b29439a8b529b33ea4dcd26a266ea82ab2459858`, mode `0600`, size `19607`) with proof directory mode `0700` and fresh baseline `/tmp/lethebot-gov-ux-01b12i-baseline-ZVg2NC`; the candidate remained identical to the workspace across all five paths, the workspace remains 68 tracked plus 49 untracked paths with zero staged paths, `git diff --check` is clean, the sensitive-name inventory contains only the expected `.env.example`, the B12I patch is ASCII-only, the canonical governance document has zero Markdown link targets, and migration 007/008 retained SHA-256 `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`/`95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`; no job repository/schema/enqueue/claim/lease/retry/result, job-attempt, worker/scheduler, HTTP/browser route, governance/repository mutation, command grammar/output, schema/migration/dependency/lockfile, QQ, Provider/private-data/live DB/deployment/restart/container/persistence-format, or prior-slice behavior changed |

| 2026-07-26 12:47 CST | `GOV-UX-01B12J` deterministic gate | fail-first shared bounded job-attempt inspection extraction; exact conjunctive filters, descending order, limits, result opt-in, recursive redaction, malformed-JSON fallback, nullable fields, and date parity; CLI identity delegation; job/attempt/worker and HTTP non-exposure adjacency; canonical P8 contract; typecheck, scoped/full lint, release gate, hygiene, migration hashes, and exact five-path B12I-to-B12J reverse/forward proof | exit 0; fail-first collected 2 files / 69 tests with 67 existing assertions passing and exactly the missing shared method plus missing delegation failures; final focused coverage passed 2 files / 69 tests, CLI-process job-attempt inspection adjacency passed 1 test with 128 unrelated tests skipped, job repository/background-worker/scheduler adjacency passed 3 files / 67 tests, HTTP security/lifecycle adjacency passed 2 files / 22 tests, and the canonical P8 gate passed 6 files / 267 tests; `pnpm release:check` passed typecheck, lint, build, preflight, diff hygiene, 132 / 1 skipped files, and 2671 / 10 skipped tests; `GovernanceQueryService.listJobAttempts` now owns the exact job-ID/worker-ID/status filters, descending start-time/ID order, default 100-row bound, opt-in result parsing, fixed nullable DTO, malformed-JSON fallback, and deterministic recursive redaction, while `GovernanceCLI.listJobAttempts` is a typed identity delegate and re-exports the moved public types without reshaping; strict reverse and forward application restored the exact B12I baseline and B12J candidate across all five paths byte-for-byte and mode-for-mode at `/tmp/lethebot-gov-ux-01b12j-roundtrip-R9MWcE/gov-ux-01b12j.patch` (`sha256:e34a121b72ec0b108211f567316a0d963811252bfd6270adbc354ac6c20d778f`, mode `0600`, size `18263`) with proof directory mode `0700` and fresh baseline `/tmp/lethebot-gov-ux-01b12j-baseline-XUG8PF`; the candidate remained identical to the workspace across all five paths, the workspace remains 68 tracked plus 49 untracked paths with zero staged paths, `git diff --check` is clean, the sensitive-name inventory contains only the expected `.env.example`, the B12J patch is ASCII-only, the canonical governance document has zero Markdown link targets, and migration 007/008 retained SHA-256 `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`/`95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`; no job/attempt repository/schema/enqueue/claim/lease/retry/result persistence, worker/scheduler, HTTP/browser route, governance/repository mutation, command grammar/output, schema/migration/dependency/lockfile, QQ, Provider/private-data/live DB/deployment/restart/container/persistence-format, or prior-slice behavior changed |
| 2026-07-26 12:59 CST | `GOV-UX-01B12K` deterministic gate | fail-first shared bounded worker-heartbeat inspection extraction; exact conjunctive filters, mixed-direction ordering, limits, details opt-in, recursive redaction, malformed-JSON fallback, nullable fields, and date parity; CLI identity delegation; worker persistence and HTTP non-exposure adjacency; canonical P8 contract; typecheck, scoped/full lint, release gate, hygiene, migration hashes, and exact five-path B12J-to-B12K reverse/forward proof | exit 0; fail-first collected 2 files / 71 tests with 69 existing assertions passing and exactly the missing shared method plus missing delegation failures; final focused coverage passed 2 files / 71 tests, CLI-process worker-heartbeat inspection adjacency passed 1 test with 128 unrelated tests skipped, job repository/background-worker/scheduler adjacency passed 3 files / 67 tests, HTTP security/lifecycle adjacency passed 2 files / 22 tests, and the canonical P8 gate passed 6 files / 269 tests; `pnpm release:check` passed typecheck, lint, build, preflight, diff hygiene, 132 / 1 skipped files, and 2673 / 10 skipped tests; `GovernanceQueryService.listWorkerHeartbeats` now owns the exact worker-ID/worker-type/status filters, descending heartbeat-time and ascending worker-ID order, default 100-row bound, opt-in details parsing, fixed nullable DTO, malformed-JSON fallback, and deterministic recursive redaction, while `GovernanceCLI.listWorkerHeartbeats` is a typed identity delegate and re-exports the moved public types without reshaping; strict reverse and forward application restored the exact B12J baseline and B12K candidate across all five paths byte-for-byte and mode-for-mode at `/tmp/lethebot-gov-ux-01b12k-roundtrip-9qXNEg/gov-ux-01b12k.patch` (`sha256:23b9c35d124e373b28b608c2c8bc1e8c424bd3a67b0d2766bc27ff3f93181a7b`, mode `0600`, size `16881`) with proof directory mode `0700` and fresh baseline `/tmp/lethebot-gov-ux-01b12k-baseline-QPbACJ`; the candidate remained identical to the workspace across all five paths, the workspace remains 68 tracked plus 49 untracked paths with zero staged paths, `git diff --check` is clean, the sensitive-name inventory contains only the expected `.env.example`, the B12K patch is ASCII-only, the canonical governance document has zero Markdown link targets, and migration 007/008 retained SHA-256 `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`/`95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`; no heartbeat repository/schema/write behavior, job/attempt persistence, worker/scheduler, HTTP/browser route, governance/repository mutation, command grammar/output, schema/migration/dependency/lockfile, QQ, Provider/private-data/live DB/deployment/restart/container/persistence-format, or prior-slice behavior changed |
| 2026-07-26 13:11 CST | `GOV-UX-01B12L` deterministic gate | fail-first shared bounded event-processing-failure inspection extraction; exact conjunctive filters, descending ordering, limits, details opt-in, recursive redaction, malformed-JSON fallback, nullable fields, and date parity; CLI identity delegation; failure-writer/turn and HTTP non-exposure adjacency; canonical P8 contract; typecheck, scoped/full lint, release gate, hygiene, migration hashes, and exact five-path B12K-to-B12L reverse/forward proof | exit 0; fail-first collected 2 files / 73 tests with 71 existing assertions passing and exactly the missing shared method plus missing delegation failures; final focused coverage passed 2 files / 73 tests, CLI-process event-failure inspection adjacency passed 1 test with 128 unrelated tests skipped, turn-application/conversation adjacency passed 2 files / 9 tests, HTTP security/lifecycle adjacency passed 2 files / 22 tests, and the canonical P8 gate passed 6 files / 271 tests; `pnpm release:check` passed typecheck, lint, build, preflight, diff hygiene, 132 / 1 skipped files, and 2675 / 10 skipped tests; `GovernanceQueryService.listEventProcessingFailures` now owns the exact stage/raw-event-ID/turn-ID filters, descending occurrence-time/ID order, default 100-row bound, opt-in details parsing, fixed nullable DTO, malformed-JSON fallback, and deterministic recursive redaction, while `GovernanceCLI.listEventProcessingFailures` is a typed identity delegate and re-exports the moved public types without reshaping; strict reverse and forward application restored the exact B12K baseline and B12L candidate across all five paths byte-for-byte and mode-for-mode at `/tmp/lethebot-gov-ux-01b12l-roundtrip-hTFRpv/gov-ux-01b12l.patch` (`sha256:6e36a2b06af601873d7f0bdf85223b25d873732a45aaba192c2f913b0dd92538`, mode `0600`, size `21036`) with proof directory mode `0700` and fresh baseline `/tmp/lethebot-gov-ux-01b12l-baseline-enjU46`; the candidate remained identical to the workspace across all five paths, the workspace remains 68 tracked plus 49 untracked paths with zero staged paths, `git diff --check` is clean, the sensitive-name inventory contains only the expected `.env.example`, the B12L patch is ASCII-only, the canonical governance document has zero Markdown link targets, and migration 007/008 retained SHA-256 `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`/`95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`; no event-failure schema/write behavior, ingress/turn execution, aggregate health, HTTP/browser route, governance/repository mutation, command grammar/output, schema/migration/dependency/lockfile, QQ, Provider/private-data/live DB/deployment/restart/container/persistence-format, or prior-slice behavior changed |
| 2026-07-26 22:55 CST | `GOV-UX-01B12M` deterministic gate | fail-first shared bounded privacy-preference inspection extraction; exact conjunctive user/type/state filters, `updated_at DESC, canonical_user_id ASC, preference_type ASC` ordering, default/explicit limits, fixed optional fields/dates, visible-field redaction, zero-write behavior, CLI identity delegation, HTTP non-exposure, canonical P8 contract, static/release gates, hygiene, migration hashes, and exact five-path reverse/forward proof | exit 0; fail-first collected 2 files / 75 tests with 73 existing assertions passing and exactly the missing shared method plus missing delegation failures; final focused coverage passed 2 files / 75 tests, CLI-process privacy adjacency passed 1 test with 128 unrelated tests skipped, privacy repository/action-enforcement adjacency passed 2 files / 72 tests, HTTP security/lifecycle adjacency passed 2 files / 22 tests, and the canonical P8 gate passed 6 files / 273 tests; `pnpm typecheck`, scoped ESLint, full `pnpm lint`, and `pnpm release:check` passed, with 132 / 1 skipped files and 2677 / 10 skipped tests; `GovernanceQueryService.listPrivacyPreferences` now owns the unchanged `PrivacyPreferenceRepository.list` read and fixed redacted projection, while `GovernanceCLI.listPrivacyPreferences` is a typed identity delegate and re-exports the public types without reshaping; the missing historical B12M baseline directory was reconstructed privately from the B12L boundary, and reverse/forward application restored all five paths byte-for-byte and mode-for-mode at `/tmp/lethebot-gov-ux-01b12m-roundtrip-9v0xN5/gov-ux-01b12m.patch` (`sha256:1d6c63ed8a9b51f427bf4b2dccfaf9e8824efc822d950fa078a805f11b2af3a3`, mode `0600`, size `15407`) with proof directory mode `0700`; the candidate snapshot matched the workspace across all five paths, the workspace remains 68 tracked plus 49 untracked paths with zero staged paths, `git diff --check` is clean, the only sensitive-name inventory match is the expected `.env.example`, the patch is ASCII-only, the canonical governance document has zero Markdown link targets, and migrations 007/008 retained SHA-256 `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`/`95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`; no privacy schema/write/audit/enforcement, HTTP/browser route, governance mutation, CLI grammar/output, dependency/lockfile, QQ, Provider/private-data/live DB/deployment/restart/container/persistence-format, or prior-slice behavior changed |

| 2026-07-26 23:50 CST | `GOV-UX-01B13B` deterministic gate | fail-first shared Explain action/tool evidence read extraction; exact linked-decision preference, ascending execution/tool ordering, fixed redacted projections, nullable/error/effect parity, zero-write query, CLI identity delegation without rebuild, `why` process coverage, action/tool adjacency, HTTP non-exposure, canonical P8 command, typecheck, scoped/full lint, release gate, hygiene, migration hashes, and exact five-path reverse/forward proof | exit 0; fail-first collected 2 files / 79 tests with 77 existing assertions passing and exactly the missing shared action/tool methods plus CLI delegation failures; final focused coverage passed 2 files / 79 tests, CLI `why` adjacency passed 11 selected tests (118 skipped), Explain unit selection passed 6 selected tests (73 skipped), action/tool adjacency passed 19 selected tests (56 skipped), governance HTTP adjacency passed 22 / 22 tests, and the canonical six-file P8 set passed 233 / 233 tests; `pnpm typecheck`, scoped ESLint, full `pnpm lint`, and `pnpm release:check` passed, with 132 / 1 skipped files and 2681 / 10 skipped tests; `GovernanceQueryService.explainToolCalls`, `explainActionDecision`, and `explainActionExecutions` now own the bounded Explain reads and pure projections, while `GovernanceCLI` delegates them through the shared service and preserves linked-decision selection, ascending ordering, redaction, nullable fields, reaction effect labels, stored-vs-rebuilt context behavior, and output shape; strict reverse and forward application restored the exact B13A candidate baseline and B13B candidate across all five paths byte-for-byte and mode-for-mode at `/tmp/lethebot-gov-ux-01b13b-roundtrip-W976OF/gov-ux-01b13b.patch` (`sha256:d8ce367715d78c6459efc0349c53b08fa9c7b0c54c32d55420449ad5e6a3a3af`, mode `0600`, size `27321`) with proof directory mode `0700`; the candidate snapshot matched the workspace across all five paths, the workspace remains 68 tracked plus 49 untracked paths with zero staged paths, `git diff --check` is clean, the only sensitive-name inventory match is the expected `.env.example`, the patch is ASCII-only, the canonical governance document has zero Markdown link targets, and migrations 007/008 retained SHA-256 `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`/`95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`; no action/tool persistence or execution behavior, HTTP/browser route, governance/repository mutation, command grammar/output, schema/migration/dependency/lockfile, QQ, Provider-private-data/live DB/deployment/restart/container/persistence-format, or prior-slice behavior changed |
| 2026-07-27 00:24 CST | `GOV-UX-01B13C` deterministic gate | fail-first shared Explain turn-resolution read extraction; exact/latest selection, `started_at DESC` ordering, joined nullable conversation metadata, missing-row behavior, zero-write integrity/FK evidence, private/group rebuild parity, stored-trace preference, CLI validation/error parity, HTTP non-exposure, canonical P8 command, typecheck, scoped/full lint, release gate, hygiene, migration hashes, and exact five-path reverse/forward proof | exit 0; fail-first collected 2 files / 81 tests with 79 existing assertions passing and exactly the missing shared `resolveExplainTurn` method plus CLI delegation failure; final focused coverage passed 2 files / 81 tests, `why` process coverage passed 16 selected tests (113 skipped), and the canonical six-file P8 set passed 235 / 235 tests; `pnpm release:check` passed with 132 / 1 skipped files and 2683 / 10 skipped tests; `GovernanceQueryService.resolveExplainTurn` and `projectGovernanceExplainTurnResolution` now own the bounded exact/latest `agent_turns` plus triggering `chat_messages` read and typed nullable DTO, while `GovernanceCLI.resolveExplainContextOptions` delegates without changing validation, canonical-user inference, ContextBuilder rebuild inputs, stored-trace preference, or output; no HTTP route, mutation, schema, dependency, QQ, Provider, private-data, deployment, restart, container, or persistence-format behavior changed |
| 2026-07-27 00:45 CST | fresh post-B13C baseline | `pnpm release:check` on `main@cfebb77` before B14A selection | exit 0; 132 passed / 1 skipped files, 2683 passed / 10 skipped tests, and diff hygiene passed; no product or persistence operation occurred |
| 2026-07-27 01:05 CST | `GOV-UX-01B14A` deterministic gate | fail-first authenticated Activity model-invocation route assertion; exact aggregate projection, authentication, unscoped/query rejection, purpose, payload/identifier exclusion, zero writes, disabled-listener behavior, route/session adjacency, canonical P8 command, typecheck, scoped lint, release gate, hygiene, migration hashes, and exact six-path reverse/forward proof | exit 0; fail-first returned `404` instead of the expected `401`, proving the route was absent; the final focused route test passed 1 / 1, four-file governance HTTP adjacency passed 104 / 104, and the canonical seven-file P8 set passed 287 / 287; `pnpm typecheck`, scoped ESLint, and `pnpm release:check` passed, with 132 / 1 skipped files and 2684 / 10 skipped tests; `LetheBotApp` now registers only authenticated-unscoped `GET /governance/api/v1/activity/model-invocations` with purpose `governance.activity.model_invocations.read`, rejects query strings and scope headers before invocation, and returns the default-filter `GovernanceQueryService.summarizeModelInvocations()` DTO without raw invocation IDs, prompts, responses, payloads, or writes; the fresh canonical audit caught and corrected the stale security statement before B14B selection; strict reverse and forward application restored the exact B13C baseline and B14A candidate across all six paths byte-for-byte and mode-for-mode at `/tmp/lethebot-gov-ux-01b14a-roundtrip-9mi0ry/gov-ux-01b14a.patch` (`sha256:0f3651455f0098f34f60e30613578039c5d99f3bf01e43d40be2adefc078dcce`, mode `0600`, size `13228`) with proof-root mode `0700`; the candidate snapshot matched the workspace across all six paths, the workspace remains 68 tracked plus 49 untracked paths with zero staged paths, `git diff --check` is clean, the only sensitive-name inventory match is the expected `.env.example`, the canonical governance document is ASCII-only with zero Markdown link targets, and migrations 007/008 retained SHA-256 `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`/`95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`; no query-service/CLI behavior, existing route/session lifecycle, schema/migration/dependency/lockfile, governance mutation, QQ, Provider/private-data/live DB/deployment/restart/container/persistence-format, or prior-slice behavior changed |

| 2026-07-27 01:13 CST | fresh resume baseline and B14A contract audit | required identity/dependency inventory, `pnpm release:check`, and canonical-doc reconciliation before B14B | release gate passed with 132 / 1 skipped files and 2684 / 10 skipped tests on `main@cfebb77`; the 68 tracked plus 49 untracked inventory and Pi packages `0.80.2` matched the checkpoint, but `docs/security-privacy.md` still claimed that production governance wiring exposed no domain route, contradicting B14A and reproducing an incomplete canonical-document boundary; no product, persistence, live, or private-data operation occurred |
| 2026-07-27 01:30 CST | `GOV-UX-01B14B` deterministic gate | fail-first authenticated Activity worker-heartbeat route assertion; exact default list, authentication, unscoped/query rejection, stable bound/order inheritance, hidden details, date/nullable-field parity, identifier redaction, zero writes, integrity/FKs, route/session adjacency, canonical P8 command, static/release gates, hygiene, migration hashes, and exact six-path reverse/forward proof | exit 0; fail-first returned `404` instead of the expected `401`, proving the route was absent; the final focused test passed 1 / 1, the full route file passed 9 / 9, four-file application/HTTP adjacency passed 35 / 35, and the canonical seven-file P8 set passed 288 / 288; `pnpm typecheck`, scoped ESLint, and `pnpm release:check` passed, with 132 / 1 skipped files and 2685 / 10 skipped tests; `LetheBotApp` now registers authenticated-unscoped `GET /governance/api/v1/activity/worker-heartbeats` with purpose `governance.activity.worker_heartbeats.read`, rejects query strings and scope headers before invocation, and returns `GovernanceQueryService.listWorkerHeartbeats()` with no options, preserving the default 100-row bound, mixed-direction ordering, ISO heartbeat dates, nullable current-job field, hidden details, recursive identifier redaction, and zero writes; strict reverse and forward application restored the exact B14A baseline and B14B candidate across all six paths byte-for-byte and mode-for-mode at `/tmp/lethebot-gov-ux-01b14b-roundtrip-9xZz8Z/gov-ux-01b14b.patch` (`sha256:1d6a35bfbfc3c59ddec9b05d941d78e89c4cfb4c8ea301f0f27b44b8dfc3e7f1`, mode `0600`, size `13113`) with proof-root mode `0700`; the candidate snapshot matched the workspace across all six paths, the workspace remains 68 tracked plus 49 untracked paths with zero staged paths, `git diff --check` is clean, changed lines are ASCII-only, the patch has one pre-existing non-ASCII context line, the canonical governance document has zero Markdown link targets, and migrations 007/008 retained SHA-256 `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`/`95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`; no query-service/CLI/DTO, worker/job persistence, generic HTTP/session lifecycle, schema/migration/dependency/lockfile, governance mutation, QQ, Provider/private-data/live DB/deployment/restart/container/persistence-format, browser-shell, or prior-slice behavior changed |
| 2026-07-27 01:49 CST | `GOV-UX-01B14C` deterministic gate | fail-first authenticated Activity job route assertion; exact default list, authentication, unscoped/query rejection, stable bound/order inheritance, hidden payload/result, date/nullable-field parity, identifier/diagnostic redaction, zero writes, integrity/FKs, route/session adjacency, canonical P8 command, static/release gates, hygiene, migration hashes, and exact six-path reverse/forward proof | exit 0; fail-first returned `404` instead of the expected `401`, proving the route was absent; the final focused test passed 1 / 1 with 9 skipped, the full route file passed 10 / 10, four-file application/HTTP adjacency passed 36 / 36, and the canonical seven-file P8 set passed 289 / 289; `pnpm typecheck`, scoped ESLint, and `pnpm release:check` passed, with 132 / 1 skipped files and 2686 / 10 skipped tests; `LetheBotApp` now registers authenticated-unscoped `GET /governance/api/v1/activity/jobs` with purpose `governance.activity.jobs.read`, rejects query strings and scope headers before invocation, and returns `GovernanceQueryService.listJobs()` with no options, preserving the default 100-row bound, `scheduled_at ASC, created_at ASC` ordering, ISO dates, nullable operational fields, hidden payload/result, recursive identifier/diagnostic redaction, and zero writes; strict reverse and forward application restored the exact B14B baseline and B14C candidate across all six paths byte-for-byte and mode-for-mode at `/tmp/lethebot-gov-ux-01b14c-roundtrip-0Rwbl4/gov-ux-01b14c.patch` (`sha256:d41420e0eeaad4fe9715336c8a8c77cbc72b83e167a22b761cd547072701c74f`, mode `0600`, size `12718`) with proof-root mode `0700`; the candidate snapshot matched the workspace across all six paths, the workspace remains 68 tracked plus 49 untracked paths with zero staged paths, `git diff --check` is clean, added lines are ASCII-only, the patch has one pre-existing non-ASCII context line, the canonical governance document has zero Markdown link targets, and migrations 007/008 retained SHA-256 `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`/`95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`; no query-service/CLI/DTO, job/attempt/worker persistence, generic HTTP/session lifecycle, schema/migration/dependency/lockfile, governance mutation, QQ, Provider/private-data/live DB/deployment/restart/container/persistence-format, browser-shell, or prior-slice behavior changed |
| 2026-07-27 02:04 CST | `GOV-UX-01B14D` deterministic gate | fail-first authenticated Activity tool-call route assertion; exact default list, authentication, unscoped/query rejection, stable bound/order inheritance, hidden input/output, date/nullable-field parity, identifier/classification/diagnostic redaction, persisted redaction signal, zero writes, integrity/FKs, route/session adjacency, canonical P8 command, static/release gates, hygiene, migration hashes, and exact six-path reverse/forward proof | exit 0; fail-first returned `404` instead of the expected `401`, proving the route was absent; the final focused test passed 1 / 1 with 10 skipped, the full route file passed 11 / 11, four-file application/HTTP adjacency passed 37 / 37, and the canonical seven-file P8 set passed 290 / 290; `pnpm typecheck`, scoped ESLint, and `pnpm release:check` passed, with 132 / 1 skipped files and 2687 / 10 skipped tests; `LetheBotApp` now registers authenticated-unscoped `GET /governance/api/v1/activity/tool-calls` with purpose `governance.activity.tool_calls.read`, rejects query strings and scope headers before invocation, and returns `GovernanceQueryService.listToolCalls()` with no options, preserving the default 100-row bound, `created_at DESC, id DESC` ordering, ISO dates, nullable diagnostics/timing, hidden input/output, recursive identifier/classification/diagnostic redaction, the persisted `secretsRedacted` signal, and zero writes; strict reverse and forward application restored the exact B14C baseline and B14D candidate across all six paths byte-for-byte and mode-for-mode at `/tmp/lethebot-gov-ux-01b14d-roundtrip-c86lm9/gov-ux-01b14d.patch` (`sha256:74f87a6f725bb713d22ca52f373bf433cb576889840ad814c8fa8e37be3235f2`, mode `0600`, size `13159`) with proof-root mode `0700`; the candidate snapshot matched the workspace across all six paths, the workspace remains 68 tracked plus 49 untracked paths with zero staged paths, `git diff --check` is clean, added lines are ASCII-only, the patch has one pre-existing non-ASCII context line, its one added Markdown link resolves to the existing canonical governance document, that document itself has zero Markdown link targets, and migrations 007/008 retained SHA-256 `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`/`95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`; no query-service/CLI/DTO, tool/action/audit/failure persistence, generic HTTP/session lifecycle, schema/migration/dependency/lockfile, governance mutation, QQ, Provider/private-data/live DB/deployment/restart/container/persistence-format, browser-shell, or prior-slice behavior changed |
| 2026-07-27 02:25 CST | `GOV-UX-01B14E` deterministic gate | fail-first authenticated Activity action-decision route assertion; exact default list, authentication, unscoped/query rejection, stable bound/order inheritance, hidden actions, date/nullable-evaluator parity, exact action count, identifier/classification/reason/suppressor redaction, zero writes, integrity/FKs, route/session adjacency, canonical P8 command, static/release gates, hygiene, migration hashes, and exact six-path reverse/forward proof | exit 0; fail-first returned `404` instead of the expected `401`, proving the route was absent; the final focused test passed 1 / 1 with 11 skipped, the full route file passed 12 / 12, four-file application/HTTP adjacency passed 38 / 38, and the canonical seven-file P8 set passed 291 / 291; `pnpm typecheck`, scoped ESLint, and `pnpm release:check` passed, with 132 / 1 skipped files and 2688 / 10 skipped tests; `LetheBotApp` now registers authenticated-unscoped `GET /governance/api/v1/activity/action-decisions` with purpose `governance.activity.action_decisions.read`, rejects query strings and scope headers before invocation, and returns `GovernanceQueryService.listActionDecisions()` with no options, preserving the default 100-row bound, `created_at DESC, id DESC` ordering, ISO dates, nullable evaluator outcome, exact action count, hidden actions, recursive identifier/classification/reason/suppressor redaction, and zero writes; strict reverse-first and forward-first application restored the exact B14D baseline and B14E candidate across all six paths byte-for-byte and mode-for-mode at `/tmp/lethebot-gov-ux-01b14e-roundtrip-7g1uku/gov-ux-01b14e.patch` (`sha256:5887abc6675fe6b6f729ddafdd77b0fd64618d0d8c18e655e04f9ef123db1286`, mode `0600`, size `13533`) with proof-root mode `0700`; the candidate snapshot matched the workspace across all six paths, the workspace remains 68 tracked plus 49 untracked files with zero staged paths, `git diff --check` is clean, no untracked sensitive-artifact filename exists, and migrations 007/008 retained SHA-256 `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`/`95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`; no query-service/CLI/DTO, action/tool/audit/failure persistence, generic HTTP/session lifecycle, schema/migration/dependency/lockfile, governance mutation, QQ, Provider/private-data/live DB/deployment/restart/container/persistence-format, browser-shell, or prior-slice behavior changed |
| 2026-07-27 02:39 CST | `GOV-UX-01B14F` deterministic gate | fail-first authenticated Activity action-execution route assertion; exact default list, authentication, unscoped/query rejection, stable bound/order inheritance, hidden audit entry, date/nullable-effect parity, retained audit level, identifier/classification/diagnostic redaction, zero writes, integrity/FKs, route/session adjacency, canonical P8 command, static/release gates, hygiene, migration hashes, and exact six-path reverse/forward proof | exit 0; fail-first returned `404` instead of the expected `401`, proving the route was absent; the final focused test passed 1 / 1 with 12 skipped, the full route file passed 13 / 13, four-file application/HTTP adjacency passed 39 / 39, and the canonical seven-file P8 set passed 292 / 292; `pnpm typecheck`, scoped ESLint, and `pnpm release:check` passed, with 132 / 1 skipped files and 2689 / 10 skipped tests; `LetheBotApp` now registers authenticated-unscoped `GET /governance/api/v1/activity/action-executions` with purpose `governance.activity.action_executions.read`, rejects query strings and scope headers before invocation, and returns `GovernanceQueryService.listActionExecutions()` with no options, preserving the default 100-row bound, `executed_at DESC, id DESC` ordering, ISO dates, nullable executed/downgrade/error fields, retained audit level, hidden audit entry, recursive identifier/classification/diagnostic redaction, and zero writes; strict reverse-first and forward-first application restored the exact B14E baseline and B14F candidate across all six paths byte-for-byte and mode-for-mode at `/tmp/lethebot-gov-ux-01b14f-roundtrip-2fIsrP/gov-ux-01b14f.patch` (`sha256:8950856cd22c89c18bfb4f4e61459eac9937c6569debaaf9b44625e97529f78d`, mode `0600`, size `13631`) with proof-root mode `0700`; the candidate snapshot matched the workspace across all six paths, the workspace remains 68 tracked plus 49 untracked files with zero staged paths, `git diff --check` is clean, all added lines are ASCII, the patch has one pre-existing non-ASCII context line, no untracked sensitive-artifact filename exists, and migrations 007/008 retained SHA-256 `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`/`95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`; no query-service/CLI/DTO, action/tool/audit/failure persistence, generic HTTP/session lifecycle, schema/migration/dependency/lockfile, governance mutation, QQ, Provider/private-data/live DB/deployment/restart/container/persistence-format, browser-shell, or prior-slice behavior changed |
| 2026-07-27 02:51 CST | `GOV-UX-01B14G` deterministic gate | fail-first authenticated Activity job-attempt route assertion; exact default list, authentication, unscoped/query rejection, stable bound/order inheritance, hidden result, attempt/date/nullable-field parity, identifier/classification/diagnostic redaction, zero writes, integrity/FKs, route/session adjacency, canonical P8 command, static/release gates, hygiene, migration hashes, and exact six-path reverse/forward proof | exit 0; fail-first returned `404` instead of the expected `401`, proving the route was absent; the final focused test passed 1 / 1 with 13 skipped, the full route file passed 14 / 14, four-file application/HTTP adjacency passed 40 / 40, and the canonical seven-file P8 set passed 293 / 293; `pnpm typecheck`, scoped ESLint, and `pnpm release:check` passed, with 132 / 1 skipped files and 2690 / 10 skipped tests; `LetheBotApp` now registers authenticated-unscoped `GET /governance/api/v1/activity/job-attempts` with purpose `governance.activity.job_attempts.read`, rejects query strings and scope headers before invocation, and returns `GovernanceQueryService.listJobAttempts()` with no options, preserving the default 100-row bound, `started_at DESC, id DESC` ordering, exact attempt number, ISO dates, nullable completion/heartbeat/error fields, hidden result, recursive identifier/classification/diagnostic redaction, and zero writes; the canonical audit also corrected B14F's duplicated deployment route-inventory wording before the final test ladder; strict reverse-first and forward-first application restored the exact B14F baseline and B14G candidate across all six paths byte-for-byte and mode-for-mode at `/tmp/lethebot-gov-ux-01b14g-roundtrip-XOMVJI/gov-ux-01b14g.patch` (`sha256:69851fcd551ab78e5c63c78af82199ff1913bd5ac83286144b959c3c5c44b2db`, mode `0600`, size `12491`) with proof-root mode `0700`; the candidate snapshot matched the workspace across all six paths, the workspace remains 68 tracked plus 49 untracked files with zero staged paths, `git diff --check` is clean, the patch is ASCII-only, no untracked sensitive-artifact filename exists, and migrations 007/008 retained SHA-256 `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`/`95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`; no query-service/CLI/DTO, job/attempt/worker persistence, generic HTTP/session lifecycle, schema/migration/dependency/lockfile, governance mutation, QQ, Provider/private-data/live DB/deployment/restart/container/persistence-format, browser-shell, or prior-slice behavior changed |
| 2026-07-27 03:03 CST | `GOV-UX-01B14H` deterministic gate | fail-first authenticated Activity event-processing-failure route assertion; exact default list, authentication, unscoped/query rejection, stable bound/order inheritance, hidden details, date/nullable-link parity, exact fixed hashes, identifier/classification/diagnostic redaction, zero writes, integrity/FKs, route/session adjacency, canonical P8 command, static/release gates, hygiene, migration hashes, and exact six-path reverse/forward proof | exit 0; fail-first returned `404` instead of the expected `401`, proving the route was absent; the final focused test passed 1 / 1 with 14 skipped, the full route file passed 15 / 15, four-file application/HTTP adjacency passed 41 / 41, and the canonical seven-file P8 set passed 294 / 294; `pnpm typecheck`, scoped ESLint, and `pnpm release:check` passed, with 132 / 1 skipped files and 2691 / 10 skipped tests; `LetheBotApp` now registers authenticated-unscoped `GET /governance/api/v1/activity/event-processing-failures` with purpose `governance.activity.event_processing_failures.read`, rejects query strings and scope headers before invocation, and returns `GovernanceQueryService.listEventProcessingFailures()` with no options, preserving the default 100-row bound, `occurred_at DESC, id DESC` ordering, ISO dates, nullable source/turn/conversation/hash fields, exact fixed hashes, hidden details, recursive identifier/classification/diagnostic redaction, and zero writes; strict reverse-first and forward-first application restored the exact B14G baseline and B14H candidate across all six paths byte-for-byte and mode-for-mode at `/tmp/lethebot-gov-ux-01b14h-roundtrip-J69BFH/gov-ux-01b14h.patch` (`sha256:f595b9d98099ad1561fce414c411755e61dd87a88fb4cc13c280604fd3259d5e`, mode `0600`, size `13889`) with proof-root mode `0700`; the candidate snapshot matched the workspace across all six paths, the workspace remains 68 tracked plus 49 untracked files with zero staged paths, `git diff --check` is clean, all added lines are ASCII, the patch has one pre-existing non-ASCII context line, no untracked sensitive-artifact filename exists, and migrations 007/008 retained SHA-256 `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`/`95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`; no query-service/CLI/DTO, failure/audit/job/worker persistence, generic HTTP/session lifecycle, schema/migration/dependency/lockfile, governance mutation, QQ, Provider/private-data/live DB/deployment/restart/container/persistence-format, browser-shell, or prior-slice behavior changed |
| 2026-07-27 03:16 CST | `GOV-UX-01B14I` deterministic gate | fail-first authenticated Activity audit route assertion; exact default list, authentication, unscoped/query rejection, stable bound/order inheritance, hidden details, date/nullable-field parity, redaction-signal parity, recursive redaction, zero writes, integrity/FKs, route/session adjacency, canonical P8 command, static/release gates, hygiene, migration hashes, and exact six-path reverse/forward proof | exit 0; fail-first collected 16 tests with 15 skipped and returned `404` instead of the expected `401`, proving the route was absent; the final focused test passed 1 / 1 with 15 skipped, the full route file passed 16 / 16, four-file application/HTTP adjacency passed 42 / 42, and the canonical seven-file P8 set passed 295 / 295; `pnpm typecheck`, scoped ESLint, and `pnpm release:check` passed, with 132 / 1 skipped files and 2692 / 10 skipped tests; `LetheBotApp` now registers authenticated-unscoped `GET /governance/api/v1/activity/audit` with purpose `governance.activity.audit.read`, rejects query strings and scope headers before invocation, and returns `GovernanceQueryService.listAudit()` with no options, preserving the default 100-row bound, `timestamp DESC, id DESC` ordering, ISO dates, nullable actor/risk/evaluator fields, hidden details, default details-redacted/redacted signals, recursive identifier/classification/summary redaction, and zero writes; strict reverse-first and forward-first application restored the exact B14H baseline and B14I candidate across all six paths byte-for-byte and mode-for-mode at `/tmp/lethebot-gov-ux-01b14i-roundtrip-gnPVfv/gov-ux-01b14i.patch` (`sha256:d0cb7c265a4bbabf80df2d9d449325688bb57ab9efe4a4e7029377bd5b9b621a`, mode `0600`, size `12981`) with proof-root mode `0700`; the candidate snapshot matched the workspace across all six paths, the workspace remains 68 tracked plus 49 untracked files with zero staged paths, `git diff --check` is clean, all added lines are ASCII, the patch has one pre-existing non-ASCII context line, no untracked sensitive-artifact filename exists, and migrations 007/008 retained SHA-256 `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`/`95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`; no query-service/CLI/DTO, audit/failure/action/tool/job/worker persistence, generic HTTP/session lifecycle, schema/migration/dependency/lockfile, governance mutation, QQ, Provider/private-data/live DB/deployment/restart/container/persistence-format, browser-shell, or prior-slice behavior changed |
| 2026-07-27 03:35 CST | `GOV-UX-01B15A` deterministic gate | fail-first purpose-specific Privacy user-scope catalog; canonical-user coverage independent of preference/proposal rows, stable newest-seen ordering, 100/101 bound, trusted exact-scope issuance, fixed identifier-free DTO, stable display fingerprint, privacy-purpose resolution, maintenance-purpose rejection, zero writes, integrity/FKs, HTTP non-exposure, canonical P8 command, static/release gates, hygiene, migration hashes, and exact three-path reverse/forward proof | exit 0; fail-first collected 30 tests with 29 skipped and failed only because the shared method was absent; the final focused test passed 1 / 1 with 29 skipped, query-service/scope-registry adjacency passed 33 / 33, and the canonical six-file P8 set passed 280 / 280; `pnpm typecheck`, scoped ESLint, and `pnpm release:check` passed, with 132 / 1 skipped files and 2693 / 10 skipped tests; `GovernanceQueryService.listPrivacyPreferenceScopeHandles` now reads valid canonical users in `last_seen_at DESC, id ASC` order before a 101-row truncation probe, includes users with no preference or maintenance row, passes only exact user scope to the trusted issuer, and returns at most 100 fixed entries containing the stable logical-scope fingerprint, `user` kind, `User privacy` label, handle, and expiry; real registry evidence resolves every handle only for the exact session and `governance.privacy.preferences.read` purpose and rejects `memory.maintenance.review`, while issuer extras and canonical/platform/session/secret values remain absent; no HTTP route is registered; strict reverse-first and forward-first application restored the exact B14I baseline and B15A candidate across all three paths byte-for-byte and mode-for-mode at `/tmp/lethebot-gov-ux-01b15a-roundtrip-IqRoSX/gov-ux-01b15a.patch` (`sha256:37757207d4ee7f227e08276ea466c207a534ac2041f4a5f8e87f9bb8a19f01e6`, mode `0600`, size `11486`) with proof-root and proof-tree mode `0700`; the candidate snapshot matched the workspace across all three paths, the workspace remains 68 tracked plus 49 untracked files with zero staged paths, `git diff --check` is clean, the patch is ASCII-only, no untracked sensitive-artifact filename exists, and migrations 007/008 retained SHA-256 `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`/`95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`; no HTTP/application/session lifecycle, privacy preference repository/schema/write/audit/enforcement, maintenance-review query/handle/route/mutation, CLI/QQ/browser, schema/migration/dependency/lockfile, Provider/private-data/live DB/deployment/restart/container/persistence-format, or prior-slice behavior changed |
| 2026-07-27 03:46 CST | `GOV-UX-01B15B` deterministic gate | fail-first exact-user HTTP-safe Privacy page; shared scope-before-limit delegation, 100/101 bound, fixed owner-identifier-free DTO, order/date/redaction parity, empty and unsupported-scope behavior, zero writes, integrity/FKs, B15A and HTTP non-exposure adjacency, canonical P8 command, static/release gates, hygiene, migration hashes, and exact three-path reverse/forward proof | exit 0; fail-first collected 31 tests with 30 skipped and failed only because the shared method was absent; the final focused test passed 1 / 1 with 30 skipped, query-service/privacy-repository adjacency passed 38 / 38, and the canonical six-file P8 set passed 281 / 281; `pnpm typecheck`, scoped ESLint, and `pnpm release:check` passed, with 132 / 1 skipped files and 2694 / 10 skipped tests; `GovernanceQueryService.listPrivacyPreferencesForScope` now rejects non-user scopes with a fixed empty page before the legacy read, delegates an exact user scope only as `{ canonicalUserId, limit: 101 }`, preserves stable order and dates, slices to 100 with explicit truncation, and returns only redacted preference type/state/reason, updater actor class/context, and timestamps; canonical owner/updater identifiers, raw platform/secret values, caller filters, and unrestricted fields are absent while the legacy CLI DTO stays unchanged; no HTTP route is registered; strict reverse-first and forward-first application restored the exact B15A baseline and B15B candidate across all three paths byte-for-byte and mode-for-mode at `/tmp/lethebot-gov-ux-01b15b-roundtrip-fJX3C8/gov-ux-01b15b.patch` (`sha256:366f985f57d0f454604d0b828fb8a367c8dd37a7998839038d87db01049f3ab9`, mode `0600`, size `11350`) with proof-root and proof-tree mode `0700`; the candidate snapshot matched the workspace across all three paths, the workspace remains 68 tracked plus 49 untracked files with zero staged paths, `git diff --check` is clean, the patch is ASCII-only, no untracked sensitive-artifact filename exists, and migrations 007/008 retained SHA-256 `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`/`95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`; no B15A catalog/fingerprint, HTTP/application/session lifecycle, privacy preference repository/schema/write/audit/enforcement, maintenance-review behavior, CLI/QQ/browser, schema/migration/dependency/lockfile, Provider/private-data/live DB/deployment/restart/container/persistence-format, or prior-slice behavior changed |

| 2026-07-27 04:04 CST | `GOV-UX-01B15C` deterministic gate | fail-first purpose-specific Privacy scope discovery route; authentication, query/scope rejection, exact B15A catalog delegation, current-session Privacy-read issuance, stable reissuance, legacy maintenance-catalog isolation, cross-purpose rejection, absent data route, zero writes, integrity/FKs, route/session adjacency, canonical P8 command, static/release gates, hygiene, migration hashes, and exact six-path reverse/forward proof | exit 0; fail-first collected 17 tests with 16 skipped and returned `404` instead of the expected `401`, proving the route was absent; the final focused test passed 1 / 1 with 16 skipped, the full governance wiring file passed 17 / 17, query/service/HTTP/scope-registry adjacency passed 4 files / 66 tests, and the canonical six-file P8 set passed 281 / 281; `pnpm typecheck`, scoped ESLint, and `pnpm release:check` passed, with 132 / 1 skipped files and 2695 / 10 skipped tests; `LetheBotApp` now registers only authenticated-unscoped `GET /governance/api/v1/privacy/scopes` with discovery purpose `governance.privacy.preferences.scopes`, rejects query strings and scope headers before invocation, calls only `GovernanceQueryService.listPrivacyPreferenceScopeHandles`, and binds every trusted issuer call to the current session digest/absolute expiry, exact user scope, and `governance.privacy.preferences.read`; the fixed response omits canonical/platform/session/secret values and issuer extras, stable repeated reads produce zero writes, the issued handle receives `404` from maintenance `/memory-reviews`, legacy `/scopes` remains proposal-backed and maintenance-only, and `/privacy/preferences` remains absent; strict reverse-first and forward-first application restored the exact B15B baseline and B15C candidate across all six paths byte-for-byte and mode-for-mode at `/tmp/lethebot-gov-ux-01b15c-roundtrip-kjwDIn/gov-ux-01b15c.patch` (`sha256:f47126730bf06698635f1a12011fb7015e328089df946a2ae6f812348ae69902`, mode `0600`, size `15215`) with proof-root and proof-tree mode `0700`; the candidate snapshot matched the workspace across all six paths, the workspace remains 68 tracked plus 49 untracked files with zero staged paths, `git diff --check` is clean, all added lines are ASCII while the patch has one pre-existing non-ASCII context line, no untracked sensitive-artifact filename exists, and migrations 007/008 retained SHA-256 `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`/`95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`; no B15A/B15B query-service or DTO behavior, generic HTTP/session/CSRF/scope registry behavior, maintenance-review behavior, privacy repository/schema/write/audit/enforcement, CLI/QQ/browser, schema/migration/dependency/lockfile, Provider/private-data/live DB/deployment/restart/container/persistence-format, or prior-slice behavior changed |

| 2026-07-27 04:19 CST | `GOV-UX-01B15D` deterministic gate | fail-first exact-scope Privacy preference route; authentication, query/scope syntax rejection, unknown/cross-session purpose-bound denial, exact B15C handle resolution, B15B-only delegation, fixed owner-identifier-free DTO, redaction/date/bound parity, stable repeated reads, zero writes, integrity/FKs, discovery/maintenance adjacency, canonical P8 command, static/release gates, hygiene, migration hashes, and exact six-path reverse/forward proof | exit 0; fail-first collected 18 tests with 17 skipped and returned `404` instead of the expected `401`, proving the route was absent; the final focused test passed 1 / 1 with 17 skipped, full query/service/HTTP/scope-registry adjacency passed 4 files / 67 tests, and the canonical six-file P8 set passed 281 / 281; `pnpm typecheck`, scoped ESLint, and `pnpm release:check` passed, with 132 / 1 skipped files and 2696 / 10 skipped tests; `LetheBotApp` now registers only exact-scope read-only `GET /governance/api/v1/privacy/preferences` with purpose `governance.privacy.preferences.read`; the unchanged HTTP boundary authenticates, rejects queries and invalid/missing authority, and resolves the handle against the exact session and purpose before dispatch, while the handler calls only `GovernanceQueryService.listPrivacyPreferencesForScope(scope)`; the fixed page preserves the B15B 100/101 bound, order, ISO dates, redacted reason/updater classification, and explicit truncation while canonical owner/updater, platform, session, secret, and unrestricted fields remain absent; repeated reads are byte-stable and write-free, integrity/FKs stay clean, Privacy discovery and maintenance isolation remain exact, and no mutation appears; strict reverse-first and forward-first application restored the exact B15C baseline and B15D candidate across all six paths byte-for-byte and mode-for-mode at `/tmp/lethebot-gov-ux-01b15d-roundtrip-kcIF4D/gov-ux-01b15d.patch` (`sha256:0e80c7382419e2f2b8e640e5acf37bdadb2a64494bd4c32094394195d671ace6`, mode `0600`, size `17155`) with proof-root and proof-tree mode `0700`; the candidate snapshot matched the workspace across all six paths, the workspace remains 68 tracked plus 49 untracked files with zero staged paths, `git diff --check` is clean, all added lines are ASCII while the patch has one pre-existing non-ASCII context line, no untracked sensitive-artifact filename exists, and migrations 007/008 retained SHA-256 `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`/`95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`; no B15A/B15B/B15C query-service/catalog/DTO/issuer behavior, generic HTTP/session/CSRF/scope registry implementation, maintenance-review behavior, privacy repository/schema/write/audit/enforcement, CLI/QQ/browser, schema/migration/dependency/lockfile, Provider/private-data/live DB/deployment/restart/container/persistence-format, or prior-slice behavior changed |

| 2026-07-27 04:55 CST | `GOV-UX-01B16A` deterministic gate | fail-first HTTP-disabled Memory-record exact-scope catalog; proposal-independent all-state scope coverage, normalization/deduplication/order, 100/101 bound, malformed/non-text/control/full-trim-set/overlong filtering, tool omission, stable shared fingerprints, exact Memory-read handle issuance, purpose/session isolation, fixed identifier-free DTO, zero writes, integrity/FKs, canonical P8 and release gates, hygiene, migration hashes, and exact three-path reverse/forward proof | exit 0; fail-first collected 33 tests with 31 skipped and both new cases failed only because the shared method was absent; the final focused cases passed 2 / 2, query-service/scope-registry adjacency passed 36 / 36, and the canonical six-file P8 set passed 283 / 283; `pnpm typecheck`, scoped ESLint, and `pnpm release:check` passed, with 132 / 1 skipped files and 2698 / 10 skipped tests; `GovernanceQueryService.listMemoryRecordScopeHandles` now reads distinct valid global/user/group/private-conversation/group-conversation/system scopes from `memory_records` across all lifecycle states with no proposal dependency, normalizes and deduplicates before stable maintenance-compatible ordering and a 101-row truncation probe, omits tool and malformed legacy rows, reuses the logical-scope fingerprint domain, passes raw exact scopes only to the trusted issuer, and returns at most 100 fixed handle entries; real registry evidence proves stable current-session `governance.memory.records.read` handles and cross-session/maintenance/Privacy denial; strict reverse-first and forward-first application restored the exact B15D baseline and B16A candidate across all three paths byte-for-byte and mode-for-mode at `/tmp/lethebot-gov-ux-01b16a-roundtrip-Zc6yXp/gov-ux-01b16a.patch` (`sha256:cbc3974cc9892d19c9e539cc33fe558c5619d7d9382790e591fd50119fbb74d2`, mode `0600`, size `23814`) with proof-root/tree mode `0700`; the candidate snapshot matches the workspace, inventory remains 68 tracked plus 49 untracked with zero staged, `git diff --check` is clean, the patch is ASCII-only, sensitive untracked filenames are absent, and migrations 007/008 retain SHA-256 `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`/`95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`; no HTTP/application/session lifecycle, B12C list/export, maintenance/Privacy catalog or handle behavior, repository/schema/migration/dependency/lockfile, governance mutation, CLI/QQ/browser, Provider/private-data/live DB/deployment/restart/container/persistence-format, or prior-slice behavior changed |
| 2026-07-27 05:16 CST | `GOV-UX-01B16B` deterministic gate | fail-first purpose-specific Memory-record scope discovery route; authentication, query/scope rejection, exact B16A delegation, current-session Memory-read issuance, stable reissuance, maintenance/Privacy isolation, absent Memory data route, zero writes, integrity/FKs, route/session adjacency, canonical P8 command, static/release gates, hygiene, migration hashes, and exact six-path reverse/forward proof | exit 0; fail-first collected 19 tests with 18 skipped and returned `404` instead of the expected `401`; the focused final test passed 1 / 1 with 18 skipped, full governance HTTP wiring passed 19 / 19, query/service/HTTP/scope-registry adjacency passed 4 files / 70 tests, and canonical P8 passed 283 / 283; typecheck, scoped ESLint, and release passed with 132 / 1 skipped files and 2699 / 10 skipped tests; `GET /governance/api/v1/memory/scopes` now uses the authenticated-unscoped boundary and calls only B16A, binding each exact scope to the current session digest/expiry and `governance.memory.records.read`; invalid authority is rejected before delegation, responses remain fixed and identifier-free, repeated reads are byte-stable and write-free, maintenance/Privacy catalogs and purposes remain isolated, and `/memory/records` stays absent; strict reverse-first and forward-first application restored the exact B16A baseline and B16B candidate across all six paths byte-for-byte and mode-for-mode at `/tmp/lethebot-gov-ux-01b16b-roundtrip-QhD7J7/gov-ux-01b16b.patch` (`sha256:52816192dd67ddf28ee3af46fe6cad9936da4bf11f252925fad80f2e1193fb3c`, mode `0600`, size `17336`) with proof-root/tree mode `0700`; candidate/workspace equality, 68 tracked plus 49 untracked with zero staged, clean diff, ASCII-only patch, sensitive-filename hygiene, and unchanged migration hashes passed; no B16A/B12C query/DTO behavior, generic HTTP/session/CSRF/scope-registry implementation, Memory data route, maintenance/Privacy behavior, schema/migration/dependency/lockfile, governance mutation, CLI/QQ/browser, Provider/private-data/live DB/deployment/restart/container/persistence-format, or prior-slice behavior changed |

| 2026-07-27 05:41 CST | `GOV-UX-01B16C` deterministic gate | fail-first HTTP-disabled exact-scope Memory-record page; all-state scope-before-bound isolation, fixed identifier-free DTO, bounded redaction, restricted-text hiding, source/revision counts, stable ordering, zero writes, integrity/FKs, canonical P8/release gates, hygiene, migration hashes, and exact three-path reverse/forward proof | exit 0; fail-first failed only on the missing method; final focused/query/adjacency/P8 gates passed 1/34/71/284 tests and release passed 132 / 1 skipped files with 2700 / 10 skipped tests; every supported exact scope now returns at most 100 stable records from all lifecycle states with only opaque references, bounded safe text, classifications, counts, and dates, while tool scope is empty and `/memory/records` stays absent; the mode-`0600` 28092-byte ASCII patch at `/tmp/lethebot-gov-ux-01b16c-roundtrip-NdMmvW/gov-ux-01b16c.patch` has SHA-256 `84260a29924ad87fb64d6ad08a05b16c2daaa12325922f463af3732e03d9e1be`, passed independent mode-`0700` reverse/forward proof, matched the workspace, preserved the 68/49/0 inventory and migration hashes, and changed no legacy query, route, mutation, schema, dependency, live, or prior-slice behavior |
| 2026-07-27 06:01 CST | `GOV-UX-01B16D` deterministic gate | fail-first exact-scope read-only Memory-record route; authentication, query/scope rejection, exact B16B authority, B16C-only delegation and DTO parity, absent detail, stable zero-write reads, integrity/FKs, adjacency/P8/static/release gates, hygiene, migration hashes, and exact six-path reverse/forward proof | exit 0; fail-first collected 20 tests with 19 skipped and returned `404` instead of `401`; final focused/full-wiring/four-file-adjacency/canonical-P8 gates passed 1/20/72/284 tests and release passed 132 / 1 skipped files with 2701 / 10 skipped tests; `GET /governance/api/v1/memory/records` now resolves only a current-session `governance.memory.records.read` handle, passes only its exact scope to `listMemoryRecordsForScope`, and returns the fixed bounded identifier-free page unchanged while invalid and cross-purpose authority fails before delegation, record detail remains absent, repeated reads are stable and write-free, and integrity/FKs stay clean; strict reverse-first and forward-first application restored the exact B16C baseline and B16D candidate across all six paths byte-for-byte and mode-for-mode at `/tmp/lethebot-gov-ux-01b16d-roundtrip-sFHkDD/gov-ux-01b16d.patch` (`sha256:1a0a98d458e17da83e5eb72f671fb39a1f16fb9038fcba6366abc565f5d28bae`, mode `0600`, size `21882`) with proof-root/tree mode `0700`; candidate/workspace equality, 68 tracked plus 49 untracked with zero staged, clean diff, ASCII-only additions with one pre-existing non-ASCII patch context line, sensitive-filename hygiene, and unchanged migration hashes passed; no B12C/B16A-C query/DTO behavior, generic HTTP/session/registry behavior, Memory detail/mutation, schema/migration/dependency/lockfile, CLI/QQ/browser/live/private behavior, or prior slice changed |

| 2026-07-27 06:23 CST | `GOV-UX-01B17A` deterministic gate | fail-first HTTP-disabled exact-scope Memory provenance detail; scope-before-provenance selection, three independent 32/33 bounds, B16C record parity, fixed references/classifications/redaction/restricted hiding, zero writes, integrity/FKs, route non-exposure, adjacency/P8/static/release gates, hygiene, migration hashes, and exact three-path reverse/forward proof | exit 0; fail-first collected 35 tests with 34 skipped and failed only on the missing method; final focused/full-query/two-file-adjacency/canonical-P8 gates passed 1/35/55/285 tests and release passed 132 / 1 skipped files with 2702 / 10 skipped tests; `getMemoryRecordDetailForScope` now matches the internal ID and complete exact scope before querying provenance, reuses the B16C record projection with exact source/revision totals, and returns at most 32 stable source/revision/audit entries with fixed redacted DTOs while raw IDs, snapshots, details, and unrestricted text remain absent; B12B/B16C, handles, and routes are unchanged; strict reverse-first and forward-first application restored the exact B16D baseline and B17A candidate across three paths byte-for-byte and mode-for-mode at `/tmp/lethebot-gov-ux-01b17a-roundtrip-P2prSJ/gov-ux-01b17a.patch` (`sha256:25413c97ff80c7089825869fb0089f3b5a9f9502ddbc6257eaac53a8d08cdf48`, mode `0600`, size `34212`) with proof-root/tree mode `0700`; candidate/workspace equality, 68 tracked plus 49 untracked with zero staged, clean diff, ASCII-only patch, sensitive-filename hygiene, and unchanged migration hashes passed; no B12B/B16A-D behavior, HTTP/session/registry/mutation, schema/migration/dependency/lockfile, CLI/QQ/browser/live/private behavior, or prior slice changed |
| 2026-07-27 06:38 CST | `GOV-UX-01B17B` deterministic gate | fail-first HTTP-disabled bounded exact-scope Memory-record resource-handle page; shared B16C 100/101 selection and DTO parity, issuer-input minimization, real resource-registry isolation, stable reissuance, zero writes, integrity/FKs, route non-exposure, adjacency/P8/static/release gates, hygiene, migration hashes, and exact three-path reverse/forward proof | exit 0; fail-first collected 36 tests with 35 skipped and failed only on the missing method; final focused/query-plus-registry/query-plus-Memory-route/canonical-P8 gates passed 1/39/56/286 tests and release passed 132 / 1 skipped files with 2703 / 10 skipped tests; `listMemoryRecordResourceHandlePage` now gives the trusted issuer only normalized exact scope plus each bounded selected internal memory ID and returns only the unchanged B16C DTO plus opaque handle/expiry, while tool, empty, malformed-owner, cross-scope, conversation-collision, and 101st rows issue no authority, issuer extras and raw identifiers remain absent, and real-registry cross-session/purpose/kind/scope denial is proved; B17A, B16D, application/session/registry behavior, and routes are unchanged; strict reverse-first and forward-first application restored the exact B17A baseline and B17B candidate across three paths byte-for-byte and mode-for-mode at `/tmp/lethebot-gov-ux-01b17b-roundtrip-cYkLx2/gov-ux-01b17b.patch` (`sha256:bdbbcc51609aa90b9e54c75ad2e5c5c9f2f5c9d6f6edc9abf7e7bbc1b40c7fe8`, mode `0600`, size `21587`) with proof-root/tree mode `0700`; candidate/workspace equality, 68 tracked plus 49 untracked with zero staged, clean diff, ASCII-only patch, sensitive-filename hygiene, and unchanged migration hashes passed; no B12B/B16A-D/B17A behavior, HTTP/session/registry/mutation, schema/migration/dependency/lockfile, CLI/QQ/browser/live/private behavior, or prior slice changed |
| 2026-07-27 06:52 CST | `GOV-UX-01B17C` deterministic gate | fail-first current-session Memory-record resource-handle list wiring; B17B-only delegation, exact session/purpose/kind/scope/resource binding, stable opaque response handles, legacy-delegate exclusion, absent detail dispatch, zero writes, integrity/FKs, adjacency/P8/static/release gates, hygiene, migration hashes, and exact six-path reverse/forward proof | exit 0; fail-first collected 20 tests with 19 skipped and failed only because the existing route made zero B17B calls; final focused/full-wiring/four-file-adjacency/canonical-P8 gates passed 1/20/57/286 tests and release passed 132 / 1 skipped files with 2703 / 10 skipped tests; `/memory/records` now passes only the resolved exact scope to B17B and binds each selected internal memory ID plus normalized scope to the callback's current session digest/expiry, existing Memory-read purpose, and `memory_record` kind, returning the bounded B16C DTO plus stable opaque handle/expiry while all invalid scope authority fails before B17B or issuance; B16C and B17A are uncalled, a returned handle detail path remains `404` without resource resolution, repeated reads are stable and write-free, and integrity/FKs stay clean; strict reverse-first and forward-first application restored the exact B17B baseline and B17C candidate across six paths byte-for-byte and mode-for-mode at `/tmp/lethebot-gov-ux-01b17c-roundtrip-fUgVQE/gov-ux-01b17c.patch` (`sha256:78326da798758b2f3133e7572a2ae110e311066f9b719c710a10237ea67d8e40`, mode `0600`, size `17968`) with proof-root/tree mode `0700`; candidate/workspace equality, 68 tracked plus 49 untracked with zero staged, clean diff, ASCII-only patch, sensitive-filename hygiene, and unchanged migration hashes passed; no B12B/B16A-D/B17A/B17B query behavior, HTTP-server/registry/session semantics, Memory detail/mutation, schema/migration/dependency/lockfile, CLI/QQ/browser/live/private behavior, or prior slice changed |
| 2026-07-27 07:06 CST | `GOV-UX-01B17D` deterministic gate | fail-first read-only exact-scope Memory provenance-detail route; authentication, query, scope/resource/session/purpose/kind isolation, B17A-only delegation, fixed bounded detail and missing-record response, restricted hiding, stable zero-write reads, integrity/FKs, B17C adjacency, canonical P8/static/release gates, hygiene, migration hashes, and exact six-path reverse/forward proof | exit 0; fail-first collected 20 tests with 19 skipped and failed only because the absent route returned `404` instead of `401`; final focused/full-wiring/four-file-adjacency/canonical-P8 gates passed 1/20/57/286 tests and release passed 132 / 1 skipped files with 2703 / 10 skipped tests; `GET /governance/api/v1/memory/records/:resourceHandle` now resolves only B17C's current-session `governance.memory.records.read` authority with kind `memory_record` and exact scope, then passes only that scope and internal ID to B17A, returning the unchanged fixed provenance detail or fixed `404`; invalid authority fails before B17A, list behavior remains unchanged, repeated reads are byte-stable and write-free, integrity/FKs stay clean, and no mutation route appears; strict reverse-first and forward-first application restored the exact B17C baseline and B17D candidate across all six paths byte-for-byte and mode-for-mode at `/tmp/lethebot-gov-ux-01b17d-roundtrip-0yx2wE/gov-ux-01b17d.patch` (`sha256:73f63879fb3ef527dc0192f7a14f2e0ad55090fc1a79be4ed678c511706daf8d`, mode `0600`, size `20158`) with proof-root/tree mode `0700`; candidate/workspace equality, 68 tracked plus 49 untracked with zero staged, clean diff, ASCII-only patch, sensitive-filename hygiene, and unchanged migration hashes passed; no B12B/B16A-D/B17A-C query behavior, HTTP-server/registry/session semantics, Memory mutation, schema/migration/dependency/lockfile, CLI/QQ/browser/live/private behavior, or prior slice changed |

| 2026-07-27 09:36 CST | `GOV-UX-01B19C` deterministic gate | fail-first HTTP-disabled exact-conversation Explain-turn detail; exact turn/trace authority before secondary reads, newest matching trace, fixed identifier-free DTO, independent 32/33 bounds, bounded redaction and token-budget validation, stable zero-write reads, integrity/FKs, HTTP non-exposure, adjacency/P8/static/release gates, hygiene, migration hashes, and exact three-path reverse/forward proof | exit 0; fail-first collected 40 tests with 39 skipped and failed only on the missing method; final focused/full-query/five-file-adjacency/canonical-P8 gates passed 1/40/83/290 tests and release passed 132 / 1 skipped files with 2709 / 10 skipped tests; `getExplainTurnDetailForScope` now proves exact conversation authority through the turn's linked trace, selects the newest matching trace, and returns only bounded context, decision, execution, and tool evidence with fixed classifications, counts, presence booleans, and redaction/truncation signals while invalid or cross-scope requests return `null` before secondary reads; B13A-B13C, ContextBuilder, registries, issuers, routes, rebuilds, and mutations remain unchanged; strict reverse-first and forward-first application restored the exact B19B baseline and B19C candidate across three paths byte-for-byte and mode-for-mode at `/tmp/lethebot-gov-ux-01b19c-roundtrip-HVqhDL/gov-ux-01b19c.patch` (`sha256:67f9b8df924fd93a5ba321990d338a5f3bb4dd4038dbf65ec3088b022eb351fc`, mode `0600`, size `47454`) with proof-root/tree mode `0700`; candidate/workspace equality, 68 tracked plus 49 untracked with zero staged, clean diff, ASCII-only patch, no reject/original artifacts, sensitive-filename hygiene, Pi dependency `^0.80.2`, and unchanged migration hashes passed; no HTTP/session/registry, schema/migration/dependency/lockfile, CLI/QQ/browser/live/private, or prior-slice behavior changed |

| 2026-07-27 11:40 CST | `GOV-UX-01B20D` deterministic gate | fail-first exact Memory forget confirmation route; existing auth/Origin/CSRF/body/scope/resource boundaries, single-use preview authority, B20A state/revision/digest recomputation, B20C-only atomic expected-snapshot mutation, fixed status/result mapping, immediate retrieval exclusion, adjacency/P8/static/release gates, hygiene, migration hashes, and exact six-path reverse/forward proof | exit 0; after correcting one synthetic retrieval assertion from FTS search to ordinary retrieval, the valid fail-first run collected 22 tests with 21 skipped and returned `404` instead of `401`; final focused/full-wiring/seven-file-adjacency/canonical-P8 gates passed 1/22/96/292 tests, and release passed 132 / 1 skipped files with 2711 / 10 skipped tests; `POST /governance/api/v1/memory/records/:resourceHandle/confirm` now accepts only exact confirmation under current-session exact-scope authority, consumes the forget preview once, recomputes B20A evidence, and invokes only B20C with fixed reason metadata; unknown/not-found is `404`, stale/reused is `409`, success is identifier-free and immediately retrieval-correct, and restore remains absent; strict reverse-first and forward-first application restored the exact B20C baseline and B20D candidate across six paths byte-for-byte and mode-for-mode at `/tmp/lethebot-gov-ux-01b20d-roundtrip-7gDUE1/gov-ux-01b20d.patch` (`sha256:d62d94309f09e6a7df07f546643606b86b6d5fb67a312915634d01ed218e15fa`, mode `0600`, size `31347`) with proof-root/tree mode `0700`; candidate/workspace equality, 68 tracked plus 49 untracked with zero staged, clean diff, ASCII-only additions with one pre-existing Chinese context heading, sensitive-filename hygiene, and unchanged migration hashes passed; no B20A/B20C, generic HTTP/registry/session, repository/schema/migration/dependency/lockfile, CLI/QQ/browser/live/private, or prior-slice behavior changed |

| 2026-07-27 12:01 CST | `GOV-UX-01B21A` deterministic gate | passing CLI restore characterization; fail-first shared exact-snapshot restore operation; all restorable states and exact scopes, stale/not-found isolation, atomic revision/audit/retrieval effects, CLI delegation parity, smoke recovery, HTTP non-exposure, P8/static/release gates, hygiene, migration hashes, and exact six-path reverse/forward proof | exit 0; two existing CLI cases passed before the new service case failed only on the absent method; final named/full-unit/HTTP-wiring/selected-process/smoke/canonical-P8 gates passed 1/61/22/4/3/293 tests, and release passed 132 / 1 skipped files with 2712 / 10 skipped tests after its first run exposed and the slice corrected the smoke script's missing existing DB option; `restoreMemoryAsLocalAdmin` now owns legacy and expected-snapshot restore, validates exact scope/restorable state/latest revision inside one immediate transaction, returns write-free fixed failures, and appends one active restore revision plus redacted audit with immediate retrieval re-entry on success; both CLI aliases delegate without output or durable-evidence drift and all restore routes remain absent; strict reverse-first and forward-first application restored the exact B20D baseline and B21A candidate across six paths byte-for-byte and mode-for-mode at `/tmp/lethebot-gov-ux-01b21a-roundtrip-2cFS5U/gov-ux-01b21a.patch` (`sha256:44b0173e7dd20f26839775e891a53ab7edaee98d3809f9401d25e9932286011f`, mode `0600`, size `26327`) with proof-root/tree mode `0700`; candidate/workspace equality, 69 tracked plus 49 untracked with zero staged, clean diff, zero non-ASCII added patch lines, VCS-visible sensitive-filename hygiene, Pi dependencies `0.80.2`, and unchanged migration hashes passed; no B20A-D, generic HTTP/registry/session, repository/schema/migration/dependency/lockfile, QQ/browser/live/private, or prior-slice behavior changed |

| 2026-07-27 12:18 CST | `GOV-UX-01B21B` deterministic gate | post-B20D/B21A P8 reassessment; fail-first HTTP-disabled exact-scope Memory restore preview; all restorable states and exact scopes, revision/lifecycle rejection, fixed purpose-isolated projection and digest, zero writes, integrity/FKs, HTTP non-exposure, P8/static/release gates, hygiene, migration hashes, and exact three-path reverse/forward proof | exit 0; fail-first collected 42 tests with 41 skipped and failed only on the missing method; final focused/full-query/four-file-adjacency/canonical-P8 gates passed 1/42/77/294 tests, and release passed 132 / 1 skipped files with 2713 / 10 skipped tests; `getMemoryRecordRestorePreviewForScope` now accepts only a clean bounded ID, exact non-tool scope, a current disabled/rejected/deleted state, and positive safe latest revision, then returns only a restore-purpose record reference, scope kind, current/expected active revision, fixed durable/retrieval/forget-boundary evidence, and a domain-separated digest; missing, cross-scope, non-restorable, revisionless, malformed, and unsafe cases return no preview with no writes, raw identifiers/content/provenance remain absent, and all restore routes remain absent; strict reverse-first and forward-first application restored the exact B21A baseline and B21B candidate across three paths byte-for-byte and mode-for-mode at `/tmp/lethebot-gov-ux-01b21b-roundtrip-emWfD8/gov-ux-01b21b.patch` (`sha256:ec7d681c4676aba092995eeb22fef5097fd4fbec106f079d13aa24526f365743`, mode `0600`, size `22089`) with proof-root/tree mode `0700`; candidate/workspace equality, 69 tracked plus 49 untracked with zero staged, clean diff, ASCII-only patch, VCS-visible sensitive-filename hygiene, Pi dependencies `0.80.2`, and unchanged migration hashes passed; no B12/B16/B17/B20A-D/B21A, generic HTTP/registry/session, repository/schema/migration/dependency/lockfile, CLI/QQ/browser/live/private, or prior-slice behavior changed |
| 2026-07-27 12:38 CST | `GOV-UX-01B21C` deterministic gate | fail-first exact Memory restore preview route; existing auth/Origin/CSRF/body/scope/resource boundaries, B21B-only delegation, current-session restore authority issuance, forget non-regression, zero writes, adjacency/P8/static/release gates, hygiene, migration hashes, and exact six-path reverse/forward proof | exit 0; after correcting one invalid synthetic FK deletion and one HTTP-token/domain-action comparison, the valid fail-first returned `400` instead of `201`; focused/full-wiring/five-file-adjacency/canonical-P8 gates passed 1/23/82/294 tests and release passed 132 / 1 skipped files with 2714 / 10 skipped tests; exact `{ action: 'restore' }` now returns the unchanged B21B projection plus fresh session/action/resource/scope/state/revision/digest-bound authority while malformed, missing, cross-boundary, and non-restorable cases remain fixed and write-free, forget behavior is unchanged, and restore confirmation remains absent; strict reverse-first and forward-first application restored the exact B21B baseline and B21C candidate across six paths byte-for-byte and mode-for-mode at `/tmp/lethebot-gov-ux-01b21c-roundtrip-6xJVC7/gov-ux-01b21c.patch` (`sha256:d0b36694807e6e76bcc1152711f23319bd50cf70adce44d96428aa11478bb4f8`, mode `0600`, size `29114`) with proof-root/tree mode `0700`; candidate/workspace equality, 69 tracked plus 49 untracked with zero staged, clean ASCII-only diff, VCS-visible sensitive-filename hygiene, installed Pi dependencies `0.80.2`, and unchanged migration hashes passed; no B21B/B21A/B20, generic HTTP/registry/session implementation, service/repository/schema/migration/dependency/lockfile, CLI/QQ/browser/live/private, or prior-slice behavior changed |
| 2026-07-27 12:54 CST | `GOV-UX-01B21D` deterministic gate | fail-first exact Memory restore confirmation; existing auth/Origin/CSRF/body/scope/resource boundaries, action-isolated single-use preview authority, B21B state/revision/digest recomputation, B21A-only expected-snapshot mutation, fixed status/result mapping, immediate retrieval restoration, forget non-regression, adjacency/P8/static/release gates, hygiene, migration hashes, and exact six-path reverse/forward proof | exit 0; the valid fail-first collected 23 tests with 22 skipped and returned `400` instead of expected `200`; focused/full-wiring/five-file-adjacency/canonical-P8 gates passed 1/23/82/294 tests and release passed 132 / 1 skipped files with 2714 / 10 skipped tests; exact three-key restore confirmation now consumes only matching restore authority once, recomputes B21B, invokes only B21A with fixed reason and exact expected snapshot, maps unknown/not-found to `404` and stale/reused to `409`, and returns identifier-free revision/audit/retrieval evidence after exactly one atomic restore while exact two-key forget remains unchanged; strict reverse-first and forward-first application restored the exact B21C baseline and B21D candidate across six paths byte-for-byte and mode-for-mode at `/tmp/lethebot-gov-ux-01b21d-roundtrip-iwnyMj/gov-ux-01b21d.patch` (`sha256:1cba0ea522f9b5af0cbbc982a4d40b2b3070225db2fd730c7e545be045889ab5`, mode `0600`, size `29417`) with proof-root/tree mode `0700`; candidate/workspace equality, 69 tracked plus 49 untracked with zero staged, clean ASCII-only additions, VCS-visible sensitive-filename hygiene, installed Pi dependencies `0.80.2`, and unchanged migration hashes passed; no B21A/B21B/B20, generic HTTP/registry/session implementation, query/service/repository/schema/migration/dependency/lockfile, CLI/QQ/browser/live/private, or prior-slice behavior changed |
| 2026-07-27 13:10 CST | `GOV-UX-01B22A` deterministic gate | passing CLI Privacy characterization; fail-first shared local-admin Privacy preference mutation owner; exact type/state/user/reason/actor/time delegation, repository transaction/redaction/audit/enforcement/error parity, CLI default/supplied reasons and output parity, HTTP non-use, adjacency/P8/static/release gates, hygiene, migration hashes, and exact five-path reverse/forward proof | exit 0; the existing CLI case passed 1 test with 51 skipped before the new service run failed only on the absent method; focused pair, three-file adjacency, selected spawned CLI, HTTP wiring, and canonical-P8 gates passed 2/69/3/23/295 tests, and release passed 132 / 1 skipped files with 2715 / 10 skipped tests; `setPrivacyPreferenceAsLocalAdmin` now delegates exactly once with local-admin actor/context and both CLI writes use it without command, message, durable-row, audit, enforcement, or error drift; strict reverse-first and forward-first application restored the exact B21D baseline and B22A candidate across five paths byte-for-byte and mode-for-mode at `/tmp/lethebot-gov-ux-01b22a-roundtrip-B1SanC/gov-ux-01b22a.patch` (`sha256:0ab4df940cca7cc9f0f91ea498a8a7ac36dd7889e5c6d3dfd823048b5111a63f`, mode `0600`, size `14769`) with proof-root/tree mode `0700`; candidate/workspace equality, 69 tracked plus 49 untracked with zero staged, clean ASCII-only additions, VCS-visible sensitive-filename hygiene, installed Pi dependencies `0.80.2`, and unchanged migration hashes passed; no query/HTTP/registry/session, repository/schema/migration/dependency/lockfile, QQ/browser/live/private, or prior-slice behavior changed |
| 2026-07-27 13:31 CST | `GOV-UX-01B22B` deterministic gate | fail-first HTTP-disabled exact-user Privacy preference-change preview; implicit-default and stored-state version evidence, fixed effects/rollback/digest, invalid/no-op isolation, stable zero-write reads, integrity/FKs, HTTP non-exposure, Privacy adjacency/P8/static/release gates, hygiene, migration hashes, and exact three-path reverse/forward proof | exit 0; fail-first collected 43 tests with 42 skipped and failed only on the absent method; focused/full-query/five-file-Privacy-adjacency/HTTP-wiring/canonical-P8 gates passed 1/43/127/23/296 tests and release passed 132 / 1 skipped files with 2716 / 10 skipped tests; `getPrivacyPreferenceChangePreviewForScope` now performs one exact-user left-join read, treats absence as effective opt-in, validates stored state/time, returns fixed upsert/audit/immediate-enforcement and semantic rollback evidence with a domain-separated digest, and rejects malformed, missing, or no-op cases without writes; strict reverse-first and forward-first application restored the exact B22A baseline and B22B candidate across three paths byte-for-byte and mode-for-mode at `/tmp/lethebot-gov-ux-01b22b-roundtrip-KoxfT4/gov-ux-01b22b.patch` (`sha256:d30005770256726e99237b00017dfe63dc681ce2ee58115b7a1a5fe75a474c54`, mode `0600`, size `20187`) after correcting two transient proof-script issues; candidate/workspace equality, 69 tracked plus 49 untracked with zero staged, clean ASCII-only diff, VCS-visible sensitive/reject/original-artifact hygiene, installed Pi dependencies `0.80.2`, and unchanged migration hashes passed; no route/authority/service mutation, repository/schema/migration/dependency/lockfile, CLI/QQ/browser/live/private, or prior-slice behavior changed |
| 2026-07-27 13:48 CST | `GOV-UX-01B22C` deterministic gate | fail-first exact-user Privacy preference-change preview route; shared authentication/query/Origin/CSRF/body/scope boundary, current-session purpose isolation, exact B22B delegation and preview binding, missing/no-op concealment, zero writes, integrity/FKs, adjacency/P8/static/release gates, hygiene, migration hashes, and exact six-path reverse/forward proof | exit 0; fail-first collected 23 tests with 22 skipped and failed only because the absent route returned `404` instead of `401`; focused/full-wiring/four-file-adjacency/canonical-P8 gates passed 1/23/65/296 tests and release passed 132 / 1 skipped files with 2716 / 10 skipped tests; `POST /governance/api/v1/privacy/preferences` now accepts only the exact change body and a current-session `governance.privacy.preferences.read` scope handle, calls only B22B, returns its unchanged projection with opaque authority, and binds exact action/resource/scope/current-state/positive-version/digest evidence without confirmation, consumption, service call, write, audit, or enforcement mutation; strict reverse-first and forward-first application restored the exact B22B baseline and B22C candidate across six paths byte-for-byte and mode-for-mode at `/tmp/lethebot-gov-ux-01b22c-roundtrip-Ayuzej/gov-ux-01b22c.patch` (`sha256:508570dade8b189ce386de7591738df394f2c41998176938b748cfeec3859021`, mode `0600`, size `27402`); current candidate hashes, candidate/workspace equality, 69 tracked plus 49 untracked with zero staged, clean ASCII-only diff and artifact hygiene, installed Pi dependencies `0.80.2`, and unchanged migration hashes passed; no confirmation/service/repository/schema/migration/dependency/lockfile, CLI/QQ/browser/live/private, or prior-slice behavior changed |
| 2026-07-28 13:53 CST | `GOV-UX-01B23A` deterministic gate | passing ordered-shape/read-only CLI characterization; behavior-preserving shared Operations doctor extraction; healthy/incomplete-schema owner coverage; exact CLI/rehearsal delegation, JSON/redaction/exit parity, zero writes, Operations adjacency/P8/static/release gates, hygiene, migration hashes, and exact six-path reverse/forward proof | exit 0; the pre-refactor named CLI case passed 1 / 1 with 23 skipped; owner plus CLI, Operations adjacency, and canonical P8 gates passed 26/46/297 tests; the first release run had one unrelated `EADDRINUSE` collision with `mihomo` PID 923 on random port `127.0.0.1:19090`, and the unchanged rerun passed 133 / 1 skipped files with 2720 / 10 skipped tests; `runOperationsDoctor` now owns the exact read-only integrity/FK/schema/count/configuration inspection while CLI output and rehearsal summaries delegate without behavioral drift; strict reverse-first and forward-first proof passed for the mode-`0600` patch at `/tmp/lethebot-gov-ux-01b23a-roundtrip-QGgGvK/gov-ux-01b23a.patch` (`sha256:906b5696c9d2d66b5e98b9eac8683c35af1f0dc6a04ebc303df11be004184a06`, size `23205`); candidate/workspace equality, 72 tracked plus 51 untracked with zero staged, clean ASCII-only additions, installed Pi dependencies `0.80.2`, and unchanged migration hashes passed; no coordinator, route, maintenance execution, schema/dependency, browser, or live behavior changed |

| 2026-07-28 14:26 CST | `GOV-UX-01B23B` deterministic gate | fail-first coordinator collection characterization; fixed path-free projection over the shared Operations doctor; healthy/attention classifications, bounded counts/configuration, backup availability and stopped-service restore boundary, zero-write repetition, Operations adjacency/P8/static/release gates, hygiene, migration hashes, and exact four-path reverse/forward proof | exit 0; the named fail-first regression failed only because the coordinator module was absent; final coordinator coverage passed 3 / 3, exact Operations adjacency passed 4 files / 49 tests, canonical P8 passed 7 files / 300 tests, and fresh `pnpm release:check` passed 134 / 1 skipped files with 2723 / 10 skipped tests; the fixed DTO omits paths, diagnostics, schema names, URLs, credentials, identifiers, payloads, and injected keys while repeated inspection preserves `total_changes()` and empty foreign-key checks; patch `/tmp/lethebot-gov-ux-01b23b-roundtrip-hFTA78/gov-ux-01b23b.patch` has SHA-256 `a6796df830b9e016a0d40db0f85a490610d8b4e5d01d12382262ceeb79270168`, mode `0600`, size `20945`, and independent reverse-first/forward-first proof passed with mode-`0700` trees; inventory is 72 modified tracked plus 53 untracked with zero staged, Pi packages remain `0.80.2`, migration hashes are unchanged, and no route, workflow execution, retention, audit, schema, dependency, browser, QQ, Provider, private, deployment, restart, or prior-slice behavior changed |
| 2026-07-28 15:05 CST | `GOV-UX-01B23C` deterministic gate | named fail-first missing-method characterization; typed verified-backup coordinator adapter; fixed path-free success/failure projection; real private artifact creation; malformed/injected metadata bounds; Operations adjacency/P8/static/release gates; hygiene, migration hashes, and exact four-path reverse/forward proof | exit 0; fail-first failed only because `createVerifiedBackup` was absent; final coordinator, Operations adjacency, and canonical P8 gates passed 6/52/303 tests; fresh `pnpm release:check` passed 134 / 1 skipped files with 2726 / 10 skipped tests; the coordinator delegates once to the existing owner, returns only fixed completion/integrity/size/page/restore evidence, and maps owner errors to one diagnostic-free attention result while private staging, `0600` publication, integrity validation, cleanup, and no-clobber behavior remain owner-only; patch `/tmp/lethebot-gov-ux-01b23c-roundtrip-ECYT5K/gov-ux-01b23c.patch` has SHA-256 `23183c23906bd6b9463af98425276134a423ed43b5625d018159403b1f1dac09`, mode `0600`, size `15043`, and independent reverse-first/forward-first proof passed with mode-`0700` trees; inventory remains 72 modified tracked plus 53 untracked with zero staged, Pi packages and migration hashes are unchanged, and no route, restore, retention, audit, schema, dependency, browser, live, or prior-slice behavior changed |
| 2026-07-28 15:25 CST | `GOV-UX-01B23D` deterministic gate | fail-first authenticated-unscoped Operations status route; exact B23B delegation; fixed path-free DTO; authentication/query/scope rejection; backup and write non-use; HTTP/Operations adjacency, canonical P8, release, hygiene, migration hashes, and exact four-path reverse/forward proof | exit 0; fail-first returned `404` instead of required `401`; the named route, four-file adjacency, and canonical P8 gates passed 1/47/303 tests; fresh `pnpm release:check` passed 134 / 1 skipped files with 2727 / 10 skipped tests; `GET /governance/api/v1/operations` now calls only `inspect()` after authenticated unscoped validation, returns the B23B DTO unchanged, leaves `/overview` separate, and performs no backup or database write; patch `/tmp/lethebot-gov-ux-01b23d-roundtrip-z5tiaV/gov-ux-01b23d.patch` has SHA-256 `255bded918749e549207426e419e81f4b25082ebc1f5a428ce78a87c16e03207`, mode `0600`, size `12628`, and corrected independent reverse-first/forward-first proof passed with mode-`0700` trees and mode-`0644` files; inventory remains 72 modified tracked plus 53 untracked with zero staged, Pi packages and migration hashes are unchanged, and no coordinator/doctor behavior, backup/restore/retention/audit write, schema, dependency, browser, live, or prior-slice behavior changed |
| 2026-07-28 15:48 CST | `GOV-UX-01B23E` deterministic gate | prior coordinator baseline and named missing-method fail-first; fixed HTTP-disabled verified-backup preview; domain-separated digest; zero owner/database/filesystem/route effects; Operations adjacency/P8/static/release gates; hygiene, migration hashes, and exact four-path reverse/forward proof | exit 0; the prior six coordinator tests passed before the seventh failed only because `previewVerifiedBackup` was absent; final coordinator, Operations adjacency, and canonical P8 gates passed 7/53/304 tests; fresh `pnpm release:check` passed 134 / 1 skipped files with 2728 / 10 skipped tests; the no-input method returns only fixed action/state/version/effect/restore/rollback evidence and a stable independently recomputable digest while making no doctor, backup-owner, destination, filesystem, route, or database call; patch `/tmp/lethebot-gov-ux-01b23e-roundtrip-CkTBSL/gov-ux-01b23e.patch` has SHA-256 `6907b7ad9d4386818d74673eb1a785d5ec8b22aee38bc1dd0c18fc9c3b243ba7`, mode `0600`, size `11799`, and independent reverse-first/forward-first proof passed with mode-`0700` trees and mode-`0644` files; inventory remains 72 modified tracked plus 53 untracked with zero staged, Pi packages and migration hashes are unchanged, and no destination, authority, route, backup/restore/retention/audit write, schema, dependency, browser, live, or prior-slice behavior changed |
| 2026-07-28 16:06 CST | `GOV-UX-01B23F` deterministic gate | fail-first authenticated-unscoped verified-backup preview route; exact auth/Origin/CSRF/JSON/query/scope/body boundary; fixed system-operation authority; GET/non-confirmation/non-effect isolation; HTTP/Operations adjacency, canonical P8, release, hygiene, migration hashes, and exact four-path reverse/forward proof | exit 0; fail-first returned `404` instead of required `401`; the named route, five-file HTTP/Operations adjacency, and canonical P8 gates passed 1/78/304 tests; final fresh `pnpm release:check` passed 134 / 1 skipped files with 2729 / 10 skipped tests; exact POST now calls only B23E and returns its unchanged projection plus fresh current-session authority bound to the fixed action/system resource/state/version/digest, while GET remains unchanged and confirmation/B23C/filesystem/database/audit effects remain absent; patch `/tmp/lethebot-gov-ux-01b23f-roundtrip-qhFMgB/gov-ux-01b23f.patch` has SHA-256 `13520a7dca7c2f37e68482980d2a821d9c8337455bda95ee984fe7fa89cd5443`, mode `0600`, size `17706`, and independent reverse-first/forward-first proof passed with mode-`0700` trees and mode-`0644` files; inventory remains 72 modified tracked plus 53 untracked with zero staged, Pi packages and migration hashes are unchanged, and no destination, confirmation, backup/restore/retention/audit write, schema, dependency, browser, live, or prior-slice behavior changed |
| 2026-07-28 16:25 CST | `GOV-UX-01B23G` deterministic gate | prior coordinator baseline and named missing-method fail-first; no-input server-owned private-directory/reference adapter; fixed path-free success/attention evidence; safe-directory reuse and unsafe-entry rejection; Operations/HTTP adjacency, canonical P8, static/release gates, hygiene, migration hashes, and exact four-path reverse/forward proof | exit 0; the seven-test coordinator baseline passed before the eighth test failed only because `createServerOwnedVerifiedBackup` was absent; the named final regression, five-file adjacency, and canonical P8 gates passed 1/79/305 tests; fresh `pnpm release:check` passed 134 / 1 skipped files with 2730 / 10 skipped tests; the adapter creates or validates only the fixed owned non-symlink `0700` sibling directory, derives a random 256-bit reference and private destination, delegates once to B23C, and returns the reference only for completed verified `0600` publication while mapping unsafe/local failures to fixed null-reference evidence; patch `/tmp/lethebot-gov-ux-01b23g-roundtrip-gZwFFM/gov-ux-01b23g.patch` has SHA-256 `85133246acf2eef0b579fd19b1a8df257de1d6cf775cbc8d7e70642e53be83cf`, mode `0600`, size `18537`, and independent reverse-first/forward-first proof passed with mode-`0700` trees and mode-`0644` files; inventory remains 72 modified tracked plus 53 untracked with zero staged, Pi packages and migration hashes are unchanged, and no route, authority consumption, audit/SQLite write, restore, retention, schema, dependency, browser, live, or prior-slice behavior changed |
| 2026-07-28 16:49 CST | `GOV-UX-01B23H` deterministic gate | fail-first authenticated-unscoped verified-backup confirmation; exact auth/Origin/CSRF/JSON/query/scope/body boundary; action/resource/system-bound single-use authority; B23E recomputation; B23G-only execution; completed/attention no-retry behavior; Operations/HTTP adjacency, canonical P8, static/release gates, hygiene, migration hashes, and exact four-path reverse/forward proof | exit 0; the untouched wiring baseline passed 25 / 25 before the named fail-first returned `404` instead of required `401`; named final, full wiring, five-file adjacency, and canonical P8 gates passed 1/26/80/305 tests; fresh `pnpm release:check` passed 134 / 1 skipped files with 2731 / 10 skipped tests; exact confirmation consumes current authority once, recomputes B23E, calls only B23G once with no input, returns unchanged fixed evidence, creates one private `0600` verified artifact on success, and never retries completed or attention results; patch `/tmp/lethebot-gov-ux-01b23h-roundtrip-eBk9oo/gov-ux-01b23h.patch` has SHA-256 `7f00270cb2d9757ecbedfc5abb57da195a46b05f60150a7e5bc473300b429534`, mode `0600`, size `24700`, and independent reverse-first/forward-first proof passed with mode-`0700` trees and mode-`0644` files; inventory remains 72 modified tracked plus 53 untracked with zero staged, Pi packages and migration hashes are unchanged, and no client path, direct B23C call, audit/SQLite write, restore, retention, artifact deletion, schema, dependency, browser, live, or prior-slice behavior changed |
| 2026-07-28 17:08 CST | `GOV-UX-01B23I` deterministic gate | prior coordinator baseline and named missing-method fail-first; exact-reference private-artifact validation; buffer-only SQLite integrity verification; fixed path-free stopped-service handoff preview; Operations/HTTP adjacency, canonical P8, static/release gates, hygiene, migration hashes, and exact six-path reverse/forward proof | exit 0; the untouched coordinator baseline passed 8 / 8 before the ninth test failed only because `previewServerOwnedBackupRestore` was absent; named coordinator/snapshot regressions, five-file adjacency, and canonical P8 gates passed 1/1/73/306 tests; fresh `pnpm release:check` passed 134 / 1 skipped files with 2733 / 10 skipped tests; exact-reference reads require the fixed owned `0700` directory and contained owned single-link non-symlink `0600` artifact, verify copied snapshot bytes in memory without sidecars, and return only fixed stopped-service semantics plus a reference/bytes/semantics-bound digest; patch `/tmp/lethebot-gov-ux-01b23i-roundtrip-eSt6Xt/gov-ux-01b23i.patch` has SHA-256 `6f4374be1e54f5e898f18879cd0a4ee9ae956ebfdb903fd51309ed545ead3f57`, mode `0600`, size `30027`, and independent reverse-first/forward-first proof passed with mode-`0700` trees and mode-`0644` files; inventory remains 72 modified tracked plus 53 untracked with zero staged, Pi packages and migration hashes are unchanged, and no catalog, route, authority, handoff mutation, audit/SQLite write, deletion, restore, retention, schema, dependency, browser, live, or prior-slice behavior changed |

| 2026-07-28 17:46 CST | `GOV-UX-01B23J` deterministic gate | fail-first authenticated-unscoped restore-preview route; exact B23I delegation and current-session reference-bound authority; Operations/HTTP adjacency, canonical P8, static/release gates, hygiene, migration hashes, and exact four-path reverse/forward proof | exit 0; the untouched wiring baseline passed 26 / 26 before the named fail-first returned `404` instead of required `401`; named final, full wiring, five-file adjacency, and canonical P8 gates passed 1/27/74/306 tests; fresh `pnpm release:check` passed 134 / 1 skipped files with 2734 / 10 skipped tests; exact POST returns fixed `404` for missing/unsafe references and otherwise returns unchanged path-free B23I evidence plus distinct exact current-session authority without confirmation or any file/database/audit/restore effect; patch `/tmp/lethebot-gov-ux-01b23j-roundtrip-DDmJeU/gov-ux-01b23j.patch` has SHA-256 `a6319132eeecc37fb2a8eed7158791f86734e98be780aec0b9acc91a6cec8bc6`, mode `0600`, size `23971`, and independent reverse-first/forward-first proof passed with mode-`0700` trees and mode-`0644` files; inventory remains 72 modified tracked plus 53 untracked with zero staged, Pi packages and migration hashes are unchanged, and no confirmation, handoff, artifact mutation, restore, retention, schema, dependency, browser, live, or prior-slice behavior changed |
| 2026-07-28 17:46 CST | `GOV-UX-01B23K` deterministic gate | fail-first authenticated-unscoped restore-confirm route; exact current-session/action/resource/reference/system authority consumption; B23I state/version/digest recomputation; zero handoff/restore effects; Operations/HTTP adjacency, canonical P8, static/release gates, hygiene, migration hashes, and exact four-path reverse/forward proof | exit 0; the untouched wiring baseline passed 27 / 27 before the named fail-first returned `404` instead of required `401`; named final, full wiring, five-file adjacency, and canonical P8 gates passed 1/28/75/306 tests; fresh `pnpm release:check` passed 134 / 1 skipped files with 2735 / 10 skipped tests; unknown/mismatched authority returns `404`, consumed/stale authority returns `409`, and exact current confirmation returns unchanged path-free B23I evidence without a handoff file, artifact/database/audit mutation, restore, retention, or lifecycle control; patch `/tmp/lethebot-gov-ux-01b23k-roundtrip-iCKPCv/gov-ux-01b23k.patch` has SHA-256 `95eb20e31cef0087a6d2022d2820a349ecde7aef251c9f58e054980621827998`, mode `0600`, size `21224`, and independent reverse-first/forward-first proof passed with candidate/workspace equality, mode-`0700` trees, and mode-`0644` product files; inventory remains 72 modified tracked plus 53 untracked with zero staged, Pi packages and migration hashes are unchanged, and no handoff, restore, retention, schema, dependency, browser, live, or prior-slice behavior changed |
| 2026-07-28 18:16 CST | `GOV-UX-01B23L` deterministic gate | fail-first typed private handoff-owner import; strict version-1 envelope; fixed private directory/file ownership and modes; bounded timestamps; exclusive staging/fsync/no-clobber publication; unsafe/repeated/unchanged-state matrix; Operations/HTTP adjacency, canonical P8, static/release gates, hygiene, migration hashes, and exact four-path reverse/forward proof | exit 0; the untouched SQLite Operations baseline passed 21 / 21, then the named fail-first produced only the expected missing-module resolution failure; final owner coverage passed 1 file / 24 tests, six-file Operations/HTTP adjacency passed 99 / 99, and canonical P8 passed 7 files / 306 tests; fresh `pnpm release:check` passed 135 / 1 skipped files with 2759 / 10 skipped tests; valid input creates exactly one owned `0700` sibling directory and owned single-link `0600` `pending.json` with schema/state, random 256-bit ID, exact reference/digest/version, 15-minute bounded expiry, and stopped-service/no-restore evidence, while invalid, unsafe, existing, raced, malformed, or owner-error states return one fixed path-free attention receipt without repair, overwrite, deletion, database/backup/audit/route/service effect; patch `/tmp/lethebot-gov-ux-01b23l-roundtrip-5d453f/gov-ux-01b23l.patch` has SHA-256 `a478009cf7c1847c3829a4e8b21817f333c055c51ed751dbcf9d6d46e49b8358`, mode `0600`, size `31502`, and independent reverse-first/forward-first deletion/recreation proof passed with mode-`0700` trees and mode-`0644` product files; inventory remains 72 modified tracked plus 55 untracked with zero staged, added files/docs are ASCII-only, Pi packages remain `0.80.2`, and migrations 007/008 retain their recorded hashes; no B23K/HTTP/CLI/supervisor/restore/retention/schema/dependency/browser/live/prior-slice behavior changed |
| 2026-07-28 18:40 CST | `GOV-UX-01B23M` deterministic gate | fail-first missing reader import; strict read-only private-envelope reopen; canonical path/UID/mode/link/no-follow/identity/size/schema/time validation; fresh-process proof; Operations/HTTP adjacency, canonical P8, static/release gates, hygiene, migration hashes, and exact four-path reverse/forward proof | exit 0; fail-first failed only on the absent reader module; final reader and combined B23L/B23M coverage passed 23/47 tests, seven-file Operations/HTTP adjacency passed 122 / 122, canonical P8 passed 7 files / 306 tests, and fresh `pnpm release:check` passed 136 / 1 skipped files with 2782 / 10 skipped tests; valid state reopens after a fresh process and returns only fixed pending ID/expiry/version/boundary/no-restore evidence, while missing, expired, malformed, changed, oversized, unsafe, or owner-error state returns fixed attention evidence without reference/digest/path exposure or any write; patch `/tmp/lethebot-gov-ux-01b23m-roundtrip-cf720d/gov-ux-01b23m.patch` has SHA-256 `5b488dd7adcf3180dd641e9bfdaaac894b7bc77cf22d4910f667eb4bec94f842`, mode `0600`, size `32417`, and independent reverse-first/forward-first deletion/recreation proof passed with mode-`0700` trees; inventory is 72 modified tracked plus 57 untracked with zero staged, Pi packages and migration hashes are unchanged, and no B23K/L/HTTP/CLI/supervisor/restore/retention/schema/dependency/browser/live/prior-slice behavior changed |

| 2026-07-28 18:58 CST | `GOV-UX-01B23N` deterministic gate | untouched 28-test wiring baseline and named pre-wiring projection fail-first; exact B23K-to-B23L publication after B23I equality; fixed pending/attention receipt; B23M fresh-process reopen; stale/reuse/no-clobber isolation; Operations/HTTP adjacency, canonical P8, static/release gates, hygiene, migration hashes, and exact four-path reverse/forward proof | exit 0; the named B23N regression passed 1 / 1, full wiring passed 28 / 28, seven-file Operations/HTTP adjacency passed 122 / 122, canonical P8 passed 7 files / 306 tests, and fresh `pnpm release:check` passed 136 / 1 skipped files with 2782 / 10 skipped tests; current confirmation calls B23L exactly once only after authority consumption and exact preview equality, returns its fixed receipt with `200`, and publishes one private envelope that B23M reopens, while stale/reused/unsafe/existing state adds no retry, database/audit/artifact/restore/retention/service effect; patch `/tmp/lethebot-gov-ux-01b23n-roundtrip-BvWmvu/gov-ux-01b23n.patch` has SHA-256 `6f21f382bcd3849194939ecb395e56e982aa1d434171020c7d31d3f17602f289`, mode `0600`, size `14049`, and independent reverse-first/forward-first proof passed with mode-`0700` trees, mode-`0644` product files, and candidate/workspace equality; inventory remains 72 modified tracked plus 57 untracked with zero staged, Pi packages and migration hashes are unchanged, and no B23L/M owner, route registry, CLI/supervisor, restore/retention, schema/dependency, browser/live, or prior-slice behavior changed |
| 2026-07-28 19:14 CST | `GOV-UX-01B23O` fresh deterministic baseline | unchanged focused recovery after an environment-only listener collision, followed by the full release gate | the first `pnpm release:check` reached one `EADDRINUSE` failure because unrelated `mihomo` PID 923 held `127.0.0.1:19090`; the unchanged focused recovery test passed, then the full rerun passed typecheck, lint, build, release preflight, tests, and `git diff --check`, recording 136 / 1 skipped test files and 2782 / 10 skipped tests; no code, configuration, listener, process, or protected runtime state was changed for the collision |
| 2026-07-28 21:38 CST | `GOV-UX-01B24C` deterministic gate | fail-first and final authenticated exact-group summary-policy discovery route; exact B24B delegation; current-session/status-purpose scope issuance; auth/query/scope/cross-session/cross-purpose isolation; identifier-free stable zero-write response; B24A/mutation exclusion; governance adjacency, canonical P8, static/release gates, hygiene, migration hashes, and exact six-path reverse/forward proof | exit 0; fail-first collected 29 tests with 28 skipped and returned `404` instead of required `401`; focused/full-wiring/five-file-adjacency/canonical-P8 gates passed 1/29/103/308 tests; the corrected normal-umask release gate passed 137 / 1 skipped files with 2802 / 10 skipped tests after two restrictive-wrapper attempts changed an unrelated permission fixture; exact current-session handles resolve only for `governance.group_summary_policy.status.read`, the catalog remains identifier-free and write-free, B24A and mutation remain uncalled, and status/POST routes stay absent; patch `/tmp/lethebot-gov-ux-01b24c-roundtrip-K9gu3b/gov-ux-01b24c.patch` has SHA-256 `21d498b124544d2ec3a7b1a6f8c2da235e6bc8fea99cc86a5a10562eca21c357`, mode `0600`, size `20994`, and both complete patch cycles passed with mode-`0700` trees and candidate/workspace equality; inventory remains 72 modified tracked plus 59 untracked with zero staged, patch encoding and filename hygiene are clean, and Pi packages/migration hashes remain unchanged |
| 2026-07-28 22:09 CST | `GOV-UX-01B24D` deterministic gate | fail-first and final exact-scope read-only group-summary-policy status route; current-session/status-purpose isolation; exact B24A delegation; stored/default-off identifier-free responses; mutation exclusion; governance adjacency, canonical P8, static/release gates, hygiene, migration hashes, and exact six-path reverse/forward proof | exit 0; fail-first collected 30 tests with 29 skipped and returned `404` instead of required `401`; focused/full-wiring/five-file-adjacency/canonical-P8 gates passed 1/30/104/308 tests; fresh normal-umask release passed 137 / 1 skipped files with 2803 / 10 skipped tests; exact current-session handles resolve only for `governance.group_summary_policy.status.read`, only B24A receives the exact group scope, stored-enabled/stored-disabled/default-off reads are stable and write-free, and POST/mutation stay absent; patch `/tmp/lethebot-gov-ux-01b24d-roundtrip-SVf66k/gov-ux-01b24d.patch` has SHA-256 `9534bc01e901741818ef2b07b271c8f3386463f0bca02a08dd0b99a9c188a9c7`, mode `0600`, size `22377`, and both complete cycles passed with mode-`0700` trees and candidate/workspace equality; inventory remains 72/59/0 and dependency/migration evidence is unchanged |
| 2026-07-28 22:43 CST | `GOV-UX-01B24E` deterministic gate | fail-first and final HTTP-disabled exact-group summary-policy change preview; default-off/stored version binding; generation advancement; enable no-backfill and disable enforcement/cancellation consequences; semantic rollback and domain-separated digest; query/service/repository adjacency, canonical P8, static/release gates, hygiene, migration hashes, and exact three-path reverse/forward proof | exit 0; fail-first collected 46 tests with 45 skipped and failed only on the absent method; focused/full-query/three-file-adjacency/canonical-P8 gates passed 1/46/68/309 tests; fresh normal-umask release passed 137 / 1 skipped files with 2804 / 10 skipped tests; invalid, no-op, and exhausted-generation evidence returns no preview, valid projections are identifier-free, stable, digest-bound, and write-free, and every handle/route/owner/audit effect stays absent; patch `/tmp/lethebot-gov-ux-01b24e-roundtrip-u2EOJG/gov-ux-01b24e.patch` has SHA-256 `4ed674cf08068f1c90f0559a5b49a009cdb913b2309a2ea6487e92a8789c2219`, mode `0600`, size `15948`, and both complete cycles passed with mode-`0700` trees and candidate/workspace equality; inventory remains 72/59/0 and dependency/migration evidence is unchanged |
| 2026-07-28 23:44 CST | `GOV-UX-01B24F` deterministic gate | legacy-owner fail-first and atomic expected-snapshot repository/service overload; implicit-versus-stored version fencing; two-connection winner/loser; generation/logical-time/ingress/cancellation/audit transaction; reopen/integrity/FKs; owner/CLI adjacency, canonical P8, static/release gates, hygiene, dependency/migration checks, and exact five-path reverse/forward proof | exit 0; fail-first produced the two expected legacy-shape failures; refreshed named/full-owner/five-file-adjacency/canonical-P8 gates passed 2/24/251/310 tests; fresh normal-umask release passed 137 / 1 skipped files with 2806 / 10 skipped tests; exact implicit or stored evidence updates once while stale/disappeared/no-op/exhausted/unadvanceable evidence is write-free, a second connection loses the original snapshot, and audit failure rolls back policy plus cancellation; patch `/tmp/lethebot-gov-ux-01b24f-roundtrip-h8PL67/gov-ux-01b24f.patch` has SHA-256 `e1965a2c36fef86cc5d22950813810aee80f77edd768bf07f91e405252ac66fb`, mode `0600`, size `38664`, and both complete cycles passed with mode-`0700` trees, exact manifests, and candidate/workspace equality; corrected inventory is 74/59/0 because the repository and its test became tracked modifications in this slice, while Pi packages and migration hashes remain unchanged |
| 2026-07-29 00:03 CST | `GOV-UX-01B24G` deterministic gate | fail-first and final exact-group summary-policy change-preview route; auth/query/Origin/CSRF/JSON/body/current-scope boundary; exact B24E delegation; current-session action/resource/state/generation/digest authority; zero confirmation/effect; full wiring/adjacency/P8/static/release gates, hygiene, invariants, and exact six-path rollback proof | exit 0; fail-first returned absent-route `404` instead of required `401`; named/full-wiring/six-file-adjacency/canonical-P8 gates passed 1/31/111/310 tests; fresh normal-umask release passed 137 / 1 skipped files with 2807 / 10 skipped tests; stored and implicit-default transitions return the unchanged identifier-free B24E projection plus exact fresh authority while invalid/no-op paths issue nothing and confirmation/B24F remain absent; patch `/tmp/lethebot-gov-ux-01b24g-roundtrip-E1MAh3/gov-ux-01b24g.patch` has SHA-256 `1ee2504a40e7f70a9f0020a6348786c793f1756ed034c965d5c354112eda8fe5`, mode `0600`, size `28063`, and both complete cycles passed with mode-`0700` trees and candidate/workspace equality; inventory/dependencies/migrations remain 74/59/0, Pi `0.80.2`, and recorded hashes |
| 2026-07-29 00:28 CST | `GOV-UX-01B24H` deterministic gate | fail-first and final exact-group summary-policy confirmation route; exact auth/query/Origin/CSRF/JSON/body/current-scope boundary; action/resource/group-bound single-use authority; B24E snapshot recomputation; B24F-only mutation; full wiring/adjacency/P8/static/release gates, hygiene, invariants, and exact six-path rollback proof | exit 0; fail-first returned absent-route `404` instead of required `401`; named/full-wiring/seven-file-adjacency/canonical-P8 gates passed 1/32/124/310 tests; fresh release passed 137 / 1 skipped files with 2808 / 10 skipped tests; exact confirmation consumes once, matches state/generation/digest, maps unknown authority to `404` and reuse/drift/no-op/stale evidence to `409`, and invokes only B24F with fixed reason while returning bounded identifier-free persisted/effect/audit/cancellation/rollback evidence; patch `/tmp/lethebot-gov-ux-01b24h-roundtrip-1Dd9sZ/gov-ux-01b24h.patch` has SHA-256 `fee575e47eea170dc57201a485c6ecd9c859765a5aa4d09955673c7c3862dad8`, mode `0600`, size `37706`, and both complete cycles passed with exact manifests, mode-`0700` trees, and candidate/workspace equality; inventory/dependencies/migrations remain 74/59/0, Pi `0.80.2`, and recorded hashes |
| 2026-07-29 00:51 CST | `GOV-UX-01B25A` deterministic gate | behavior-preserving local-admin display-profile redaction extraction; exact group/global selection, profile/history SQL, timestamps, counts, zero-row audit, governance redaction, audit-failure rollback, reopen/integrity/FKs, CLI delegation/output parity, adjacency/P8/static/release gates, hygiene, and exact five-path rollback proof | exit 0; fail-first collected 13 service cases with 12 skipped and failed only on the absent shared method; focused/unit/CLI-subprocess/adjacency/canonical-P8 gates passed 2/66/10/143/312 tests; corrected normal-umask release passed 137 / 1 skipped files with 2810 / 10 skipped tests; the shared service preserves exact SQL/transaction/audit behavior while CLI delegates once and identity unlink remains separate; patch `/tmp/lethebot-gov-ux-01b25a-roundtrip-nyxeD1/gov-ux-01b25a.patch` has SHA-256 `7dc8caa6eac3796579a1c9e0052aff05f41ff602fec58de1797704db4ce31524`, mode `0600`, size `17142`, and both five-path cycles passed with exact manifests, mode-`0700` trees, ASCII/path checks, and candidate/workspace equality; inventory/dependencies/migrations remain 74/59/0, Pi `0.80.2`, and recorded hashes |
| 2026-07-29 01:05 CST | `GOV-UX-01B25B` deterministic gate | behavior-preserving local-admin platform-account unlink extraction; exact active lookup/guarded disable, inactive no-ops, audit omission/redaction, audit-failure rollback, reopen/integrity/FKs, CLI outcome/error parity, adjacency/P8/static/release gates, hygiene, and exact five-path rollback proof | exit 0; fail-first collected 14 service cases with 13 skipped and failed only on the absent shared method; focused/unit/CLI-subprocess/adjacency/canonical-P8 gates passed 2/68/2/145/314 tests; normal-umask release passed 137 / 1 skipped files with 2812 / 10 skipped tests; the shared service owns the exact transaction/audit while CLI delegates and its orphaned audit adapter is removed; patch `/tmp/lethebot-gov-ux-01b25b-roundtrip-e50Mit/gov-ux-01b25b.patch` has SHA-256 `75a991c2c7a5f3fa13791c3d147b82bd3b79a96b6441b875ace5a8237f9d2974`, mode `0600`, size `18311`, and both five-path cycles passed with exact manifests, mode-`0700` trees, ASCII/path checks, and candidate/workspace equality; inventory/dependencies/migrations remain 74/59/0, Pi `0.80.2`, and recorded hashes |
| 2026-07-29 01:45 CST | `GOV-UX-01C1` deterministic gate | fail-first and final dependency-free browser login/session shell plus read-only aggregate Overview; exact static GET/security boundary; in-memory CSRF and cleared token; fixed DOM rendering; zero/healthy/loading/attention/unavailable/expiry/logout states; desktop/mobile focus, layout, network, and visual QA; HTTP/P8/static/release gates; hygiene, dependency/migration checks, and exact seven-path rollback proof | exit 0; characterization/fail-first/focused/full-HTTP/adjacency/canonical-P8 gates recorded 15 passing, 1 expected failure, 1/16/55/315 passing tests; the final normal-umask release gate passed 137 / 1 skipped files with 2813 / 10 skipped tests; Chromium 150 at 1440x900 and 390x844 proved all required states, visible focus, 44px controls, no overflow/clipping/overlap, zero unexpected console/page/request failures, and zero external requests, with the discovered logged-out grid overlap fixed and regression-covered; patch `/tmp/lethebot-gov-ux-01c1-roundtrip-FeCIIv/gov-ux-01c1.patch` has SHA-256 `17b64029b2b0368334835d005e33b7783130a8660f52adbda6c13d5dfd459e93`, mode `0600`, size `38355`, and independent reverse-first/forward-first deletion/creation proof passed with exact manifests, mode-`0700` trees, ASCII/path checks, and candidate/workspace equality; inventory is 74/60/0, Pi remains `0.80.2`, migration hashes are unchanged, and no schema/dependency/CLI/QQ/Provider/private/live boundary changed |
| 2026-07-29 01:52 CST | `GOV-UX-01C2` fresh deterministic baseline | unchanged C1 browser-shell and B14A model-invocation route characterizations; exact seven-file baseline capture before C2 production/test/doc edits | exit 0; named C1 and B14A regressions each passed 1 / 1 with 15 and 31 skipped; mode-`0600` logs have SHA-256 `20493930ec578b462af4c2bcfce41fcbec4ef6ece6aa404f06cb8ad1e5868488` and `a098adc9e7541a1fa818d391837a3e9b8c7613126bb9445b85290e7f3911d6c0`; mode-`0700` baseline `/tmp/lethebot-gov-ux-01c2-roundtrip-O62kyk` records mode-`0644` files and exact hashes at `main@cfebb77`, inventory 74/60/0, and a clean `git diff --check`; no product, test, canonical contract, route, dependency, schema, private, or live behavior changed |
| 2026-07-29 02:12 CST | `GOV-UX-01C2` deterministic gate | fail-first and final read-only browser Model invocations Activity view; fixed navigation/DOM fields and payload validation; loading/populated/empty/unavailable/expiry/logout states; responsive keyboard/focus/layout/network/visual QA; HTTP/P8/static/release gates; hygiene, dependency/migration checks, and exact six-path rollback proof | exit 0; C1/B14A characterizations passed 1/1 each, fail-first produced the one intended missing-Activity-nav failure, and focused/full-HTTP/adjacency/canonical-P8 gates passed 1/17/56/316 tests; normal-umask release passed 137 / 1 skipped files with 2814 / 10 skipped tests; 36,088-byte dependency-free assets call only the exact B14A GET and render fixed aggregate fields; Chromium at 1440x900 and 390x844 proved all required states, keyboard navigation, visible focus, 44px controls, no overflow/clipping/overlap, zero unexpected console/page/request failures, and zero external requests; patch `/tmp/lethebot-gov-ux-01c2-roundtrip-O62kyk/gov-ux-01c2.patch` has SHA-256 `bdcc156bdf7424ff9be8896711723ccb22287336a04a851bda60231da46b80ba`, mode `0600`, size `23072`, and exact six-path ASCII reverse-first/forward-first byte/mode proof passed with mode-`0700` trees, mode-`0644` product files, and candidate/workspace equality; inventory remains 74/60/0, Pi remains `0.80.2`, migration hashes are unchanged, and no backend/domain/dependency/schema/CLI/QQ/Provider/private/live boundary changed |
| 2026-07-29 02:56 CST | `GOV-UX-01C3` deterministic gate | fail-first and final read-only browser Tool calls Activity view; strict bounded payload validation; responsive semantic list; loading/populated/empty/malformed/oversized/unavailable/stale/expiry/logout states; browser/HTTP/P8/static/release/hygiene gates; and exact rollback proof | exit 0; C2/B14D characterizations passed 1/1 each, fail-first produced the intended missing-Tool-calls-tab failure, and focused/full-HTTP/adjacency/canonical-P8 gates passed 1/18/57/317 tests; normal-umask release passed 137 / 1 skipped files with 2815 / 10 skipped tests; 54,420-byte dependency-free assets call only the exact B14D GET, render fixed redacted operational fields, and omit payload and call/turn/user identifiers; Chromium at 1440x900 and 390x844 proved every required state, four-key tab navigation, focus, 44px controls, labelled mobile rows, layout/network/error hygiene, and visual review; exact patch `/tmp/lethebot-gov-ux-01c3-roundtrip-PGs843/gov-ux-01c3.patch` has SHA-256 `a66fede8e6045b0dec8e6a4b972b74dbbfb43f0d7f591523c1723d89f9736c6f`, mode `0600`, size `37754`, and independent six-path reverse-first/forward-first byte/mode proof passed; inventory remains 74/60/0, Pi and migration hashes are unchanged, and no backend/domain/dependency/schema/CLI/QQ/Provider/private/live boundary changed |
| 2026-07-29 13:57 CST | `GOV-UX-01B26A` deterministic gate | named missing-method fail-first; bounded profile/history-backed exact-user scope catalog; validation-before-deduplication, stable raw-ID order, 100/101 probe, purpose/session isolation, identifier omission, zero writes, canonical P8/static/release gates, hygiene, migration hashes, and exact three-path rollback proof | exit 0; fail-first failed solely because the catalog method was absent; focused, query/scope-registry adjacency, and canonical P8 passed 1/50/321 tests; after one environment-only `EADDRINUSE` collision and an unchanged focused recovery pass, corrected release passed 137 / 1 skipped files and 2819 / 10 skipped tests; profile-only/history-only users are included, duplicates and malformed legacy IDs are excluded, fixed DTOs expose no raw identifiers or display evidence, and integrity/FKs stay clean; patch `/tmp/lethebot-gov-ux-01b26a-roundtrip-UpCQdY/gov-ux-01b26a.patch` has SHA-256 `76531a80532df1499ecc528323323fee13d28aad9852cb877e12c101bb23a031`, mode `0600`, size `14177`, and both byte/mode cycles passed; inventory/dependencies/migrations remain 74/60/0, Pi `0.80.2`, and the recorded hashes |
| 2026-07-29 15:04 CST | `GOV-UX-01B26B` deterministic gate | named missing-method fail-first; exact-user private/global and group target union; strict target/metadata validation; independent target/history 100/101 probes; identifier/text omission; zero writes; canonical P8/static/release gates; hygiene, migration hashes, and exact three-path rollback proof | exit 0; focused, full query-service, query/scope-registry adjacency, and canonical P8 passed 1/48/51/322 tests; an unrelated permission fixture failed under restrictive process umask, its unchanged case passed alone, and corrected normal-umask release passed 137 / 1 skipped files with 2820 / 10 skipped tests; fixed target DTOs contain only fingerprint, kind/label, current-profile trust/date, and bounded history metadata while raw identifiers/text/end evidence stay absent; patch `/tmp/lethebot-gov-ux-01b26b-roundtrip-70mZ8w/gov-ux-01b26b.patch` has SHA-256 `f060889602d2ea7dd314d90c748aabcbe2e57e50ea6755a19f2783f876e3c634`, mode `0600`, size `25912`, and both byte/mode cycles plus candidate/workspace equality passed; inventory/dependencies/migrations remain 74/60/0, Pi `0.80.2`, and the recorded hashes |
| 2026-07-29 15:29 CST | `GOV-UX-01B26C` deterministic gate | named missing-method fail-first; shared B26B selector/projector; full domain-separated target resource IDs; exact trusted issuer input; opaque same-session handles; cross-session/purpose/kind/scope isolation; identifier omission; zero writes; canonical P8/static/release gates; hygiene, migration hashes, and exact three-path rollback proof | exit 0; focused, query/resource/scope-registry adjacency, and canonical P8 passed 1/55/323 tests; one unrelated transient application-release rehearsal failed, its exact unchanged case passed alone, and corrected complete normal-umask release passed 137 / 1 skipped files with 2821 / 10 skipped tests; B26B parity is exact while each selected target gains only an opaque handle/expiry bound to `governance.display_profile.targets.read` and `display_profile_target`; patch `/tmp/lethebot-gov-ux-01b26c-roundtrip-ilpMRV/gov-ux-01b26c.patch` has SHA-256 `fa36c82d7f1f97cebba097c9856f27c8f09a399baeebf87f6cf19680be7bc2c8`, mode `0600`, size `20822`, and both byte/mode cycles plus candidate/workspace equality passed; inventory/dependencies/migrations remain 74/60/0, Pi `0.80.2`, and the recorded hashes |
| 2026-07-29 15:55 CST | `GOV-UX-01B26D` deterministic gate | named absent-route fail-first; authenticated-unscoped exact-user display-profile discovery; exact B26A delegation; current-session target-read scope issuance; auth/query/scope/cross-session/cross-purpose isolation; identifier omission; zero writes; canonical P8/static/release gates; hygiene, migration hashes, and exact six-path rollback proof | exit 0; focused/full-wiring/five-file-adjacency/canonical-P8 gates passed 1/33/120/323 tests; complete normal-umask release passed 137 / 1 skipped files with 2822 / 10 skipped tests; `GET /governance/api/v1/display-profile/scopes` returns only the unchanged B26A catalog and binds exact users to `governance.display_profile.targets.read`, while B26B/B26C, resource issuance, mutations, and every target/preview route remain unused or absent; patch `/tmp/lethebot-gov-ux-01b26d-roundtrip-I16e0a/gov-ux-01b26d.patch` has SHA-256 `0c0098028eee9dcab6bf50f14e6f4ff645198f18488c7addf79f48ad06831470`, mode `0600`, size `20459`, and both byte/mode cycles plus candidate/workspace equality passed; inventory/dependencies/migrations remain 74/60/0, Pi `0.80.2`, and the recorded hashes |
| 2026-07-29 16:17 CST | `GOV-UX-01B26E` deterministic gate | named absent-route fail-first; exact-user scoped display-profile target list; B26C-only delegation; current-session resource issuance; purpose/kind/scope/target isolation; identifier omission; zero writes; canonical P8/static/release gates; hygiene, migration hashes, and exact six-path rollback proof | exit 0; fail-first returned absent-route `404` instead of required `401`; focused/full-wiring/five-file-adjacency/canonical-P8 gates passed 1/34/110/323 tests; complete normal-umask release passed 137 / 1 skipped files with 2823 / 10 skipped tests; `GET /governance/api/v1/display-profile/targets` resolves only current-session exact-user scope authority, returns the unchanged B26C page, and binds full target IDs to `governance.display_profile.targets.read` plus `display_profile_target`, while dynamic detail, resource resolution, preview, confirmation, and mutation routes remain absent; patch `/tmp/lethebot-gov-ux-01b26e-roundtrip-myoDj5/gov-ux-01b26e.patch` has SHA-256 `99dd113208596df6f8ca5f97007808f1f9efdd502b4a3375e408c65e9622e394`, mode `0600`, size `28037`, and both byte/mode cycles plus candidate/workspace equality passed; inventory/dependencies/migrations remain 74/60/0, Pi `0.80.2`, and the recorded hashes |
| 2026-07-29 16:58 CST | `GOV-UX-01B26F` deterministic gate | named missing-method fail-first; exact B26B/B26C scope-and-target selection; private/group/history-only detail; current-value and stable history reads; 160-code-point redaction; 32/33 bound; identifier omission; zero writes; canonical P8/static/release gates; hygiene, migration hashes, and exact three-path rollback proof | exit 0; fail-first failed only because the method was absent; focused/full-query/four-file-adjacency/canonical-P8 gates passed 1/50/90/324 tests; complete normal-umask release passed 137 / 1 skipped files with 2824 / 10 skipped tests; only full target authority for a currently selected exact-user target returns the unchanged B26B projection plus bounded redacted current/history values and opaque history fingerprints, while malformed, cross-user, missing, and unselected targets fail before secondary reads and every route/preview/mutation remains absent; patch `/tmp/lethebot-gov-ux-01b26f-roundtrip-3qLr2Y/gov-ux-01b26f.patch` has SHA-256 `5c80660426b31848295de9e622458732b8bc98162ff3318f5b7a24bf519522ec`, mode `0600`, size `23506`, and both byte/mode cycles plus candidate/workspace equality passed; inventory/dependencies/migrations remain 74/60/0, Pi `0.80.2`, and the recorded hashes |
| 2026-07-29 17:29 CST | `GOV-UX-01B26G` deterministic gate | named absent-route fail-first; exact current-session scope/resource authority; B26F-only detail delegation; private/group/missing detail; identifier omission; zero writes; canonical P8/static/release gates; hygiene, migration hashes, and exact six-path rollback proof | exit 0; fail-first returned absent-route `404` instead of required `401`; focused/full-wiring/five-file-adjacency/canonical-P8 gates passed 1/35/112/324 tests; complete normal-umask release passed 137 / 1 skipped files with 2825 / 10 skipped tests; exact read-only target detail now resolves session/purpose/kind/scope-bound authority before passing only resolved scope and internal target ID to B26F, returns its DTO unchanged or fixed `404`, and leaves preview/confirmation/mutation absent; patch `/tmp/lethebot-gov-ux-01b26g-roundtrip-Bsfoa0/gov-ux-01b26g.patch` has SHA-256 `8c2233d132c6c685888156da328df497b25353b5e1f539ce94916e6cb1e1b397`, mode `0600`, size `31848`, and both byte/mode cycles plus candidate/workspace equality passed; inventory/dependencies/migrations remain 74/60/0, Pi `0.80.2`, and the recorded hashes |
| 2026-07-29 17:54 CST | `GOV-UX-01B26H` deterministic gate | named missing-method fail-first; exact-target write-free redaction preview; B26B/B26C authority; full matching profile/history row snapshot; typed storage/hex drift binding; exact counts/effects; irreversible rollback boundary; identifier omission; zero writes; canonical P8/static/release gates; hygiene, migration hashes, and exact three-path rollback proof | exit 0; focused/full-query/four-file-adjacency/canonical-P8 gates passed 1/51/92/325 tests; complete normal-umask release passed 137 / 1 skipped files with 2826 / 10 skipped tests; exact private/global, group, history-only, already-redacted, malformed-match, and overflow cases prove stable constant-memory snapshots whose fingerprints and preview digests change on value/type/time/trust/lifecycle/add/remove drift, while outputs stay identifier-free and all reads preserve integrity/FKs; patch `/tmp/lethebot-gov-ux-01b26h-roundtrip-Mz5vWT/gov-ux-01b26h.patch` has SHA-256 `3bdbfaf18a6f3d220d32e5cff6a0690afcea6bb0960c370c4fdd4e0a0a7fc622`, mode `0600`, size `28226`, and both byte/mode cycles plus candidate/workspace equality passed; inventory/dependencies/migrations remain 74/60/0, Pi `0.80.2`, and the recorded hashes |
| 2026-07-29 18:23 CST | `GOV-UX-01B26I` deterministic gate | named legacy-path fail-first; shared target-ID/snapshot helpers; strict atomic expected-snapshot display-profile redaction; private/group/history-only/already-redacted/malformed-row coverage; full drift and count fencing; audit rollback; reopen/integrity/FKs; legacy service/CLI and B26H parity; canonical P8/static/release gates; hygiene, migration hashes, and exact four-path rollback proof | exit 0; focused/B26H-parity/legacy-CLI/full-query-service/seven-file-adjacency/canonical-P8 gates passed 2/1/2/66/165/326 tests; complete normal-umask release passed 137 / 1 skipped files with 2827 / 10 skipped tests; exact snapshots mutate once under an immediate transaction while malformed, missing, drifted, count-mismatched, and audit-failure paths are write-free or roll back, committed evidence survives reopen, and legacy scalar service/CLI plus B26H projection/digest remain unchanged; patch `/tmp/lethebot-gov-ux-01b26i-roundtrip-UC75tu/gov-ux-01b26i.patch` has SHA-256 `499e4f3982f515445d0865ffc11674f63538c3f1d739f569f4dbc7cba81aae3d`, mode `0600`, size `52137`, and both byte/mode cycles plus candidate/workspace equality passed; inventory/dependencies/migrations remain 74/60/0, Pi `0.80.2`, and the recorded hashes |
| 2026-07-29 18:48 CST | `GOV-UX-01B26J` deterministic gate | named absent-route fail-first; mutation-marked exact-authority target POST; strict auth/Origin/CSRF/query/body/scope/resource boundary; B26H-only private/group preview; snapshot/count/digest-bound current-session authority; identifier omission; zero consumption/service/writes; confirmation absence; canonical P8/static/release gates; hygiene, migration hashes, and exact six-path rollback proof | exit 0; focused/full-wiring/seven-file-adjacency/canonical-P8 gates passed 1/36/133/326 tests; complete normal-umask release passed 137 / 1 skipped files with 2828 / 10 skipped tests; valid POST returns B26H unchanged plus fresh `display_profile.redact` authority while invalid/missing/cross-authority paths fail before preview or issuance and B26I/B25A/B25B stay uncalled; patch `/tmp/lethebot-gov-ux-01b26j-roundtrip-C5PQjb/gov-ux-01b26j.patch` has SHA-256 `62280aaaa2771e35b3d42b6c1546cd2cb1924cd8bb80b1a33c188c97a5e4ffe2`, mode `0600`, size `33577`, and both zero-fuzz byte/mode cycles plus candidate/workspace equality passed; inventory/dependencies/migrations remain 74/60/0, Pi `0.80.2`, and the recorded hashes |
| 2026-07-29 19:12 CST | `GOV-UX-01B26K` deterministic gate | named missing-method fail-first; exact private/group/history-only target-to-mutation selector; B26B/B26C bounded authority reuse; validation-before-SQL; cross-user/missing/malformed/unselected rejection; public B26B/B26H parity; zero writes; canonical P8/static/release gates; hygiene, migration hashes, and exact three-path rollback proof | exit 0; focused/full-query/seven-file-adjacency/canonical-P8 gates passed 1/52/134/327 tests; complete normal-umask release passed 137 / 1 skipped files with 2829 / 10 skipped tests; only selected exact-user target authority yields the raw internal canonical-user/group selector while all public projections remain identifier-free and byte-identical; patch `/tmp/lethebot-gov-ux-01b26k-roundtrip-D9rPK8/gov-ux-01b26k.patch` has SHA-256 `929a28bd298da38e0d9b2b9dc10ba40815508680ebdaceb05032a1e53b5c236e`, mode `0600`, size `10288`, and both zero-fuzz byte/mode cycles plus candidate/workspace equality passed; inventory/dependencies/migrations remain 74/60/0, Pi `0.80.2`, and the recorded hashes |
| 2026-07-29 19:41 CST | `GOV-UX-01B26L` deterministic gate | named absent-route fail-first; mutation-marked exact target confirmation; strict auth/Origin/CSRF/query/body/scope/resource boundary; single-use exact action authority; B26H fingerprint/count/digest revalidation; B26K selectors; B26I-only fixed-reason mutation; private/group effects; identifier/audit redaction; replay/drift/owner-failure mapping; canonical P8/static/release gates; hygiene, migration hashes, and exact six-path rollback proof | exit 0; focused/full-wiring/seven-file-adjacency/canonical-P8 gates passed 1/37/135/327 tests; complete normal-umask release passed 137 / 1 skipped files with 2830 / 10 skipped tests; exact confirmations consume once, mutate only exact profile/history rows, close open history, append one redacted fixed audit, return only bounded identifier-free evidence, and preserve integrity/FKs; patch `/tmp/lethebot-gov-ux-01b26l-roundtrip-y3gotv/gov-ux-01b26l.patch` has SHA-256 `53706f1e332d04effbd089e9076339157cb2f6bbd0d65a758ecfba64c7445d6a`, mode `0600`, size `34171`, and both zero-fuzz byte/mode cycles plus candidate/workspace equality passed; inventory/dependencies/migrations remain 74/60/0, Pi `0.80.2`, and the recorded hashes |
| 2026-07-29 22:18 CST | `GOV-UX-01B27A` deterministic gate | named missing-method fail-first; exact active platform-account preview; strict input and stored-row/type/time validation; owner-bound public fingerprint; full-row typed snapshot; stable preview digest; disable/audit/privacy/no-relink consequences; identifier omission; zero writes; B25B and canonical P8/static/release gates; hygiene, migration hashes, and exact three-path rollback proof | exit 0; focused/full-query/B25B-adjacency/canonical-P8 gates passed 1/53/7/328 tests; complete normal-umask release passed 137 / 1 skipped files with 2831 / 10 skipped tests; malformed, missing, inactive, deleted, or malformed-row mappings return no preview, while one exact active mapping yields stable identifier-free expected effects and drift-bound evidence without mutation; patch `/tmp/lethebot-gov-ux-01b27a-roundtrip-rGwDc3/gov-ux-01b27a.patch` has SHA-256 `23dc0478c92478b52e3f8ad9a45ac4470091442e3985d6bd9daed86ca6fd398b`, mode `0600`, size `22533`, and both zero-fuzz byte/mode cycles plus candidate/workspace equality passed; inventory/dependencies/migrations remain 74/60/0, Pi `0.80.2`, and the recorded hashes |
| 2026-07-29 22:42 CST | `GOV-UX-01B27B` deterministic gate | named legacy-path fail-first; shared B27A exact snapshot reader; strict expected-input validation; immediate snapshot fence; guarded one-row disable; fixed reason-bound redacted audit; inactive/drift/count/audit/reopen coverage; B27A/B25B/HTTP/CLI/P8/static/release gates; hygiene, migration hashes, and exact four-path rollback proof | exit 0; focused/parity/full-query-service/HTTP-adjacency/CLI-adjacency/canonical-P8 gates passed 1/3/69/58/2/329 tests; after one environment-only fixed-port collision and an unchanged focused recovery pass, corrected normal-umask release passed 137 / 1 skipped files with 2832 / 10 skipped tests; exact snapshots disable once while malformed, drifted, inactive, count-mismatched, and audit-failure paths are write-free or roll back, committed state survives reopen, and B27A plus legacy B25B behavior stay exact; patch `/tmp/lethebot-gov-ux-01b27b-roundtrip-teJ6xm/gov-ux-01b27b.patch` has SHA-256 `741b9b0f4fef4935390d3f5e944cc207cbe975b1ce43a1c1e51bc32f17dd40c4`, mode `0600`, size `35671`, and both zero-fuzz byte/mode cycles plus candidate/workspace equality passed; inventory/dependencies/migrations remain 74/60/0, Pi `0.80.2`, and the recorded hashes |
| 2026-07-29 23:00 CST | `GOV-UX-01B27C` deterministic gate | named absent-route fail-first; mutation-marked authenticated-unscoped unlink preview; strict auth/Origin/CSRF/query/scope/body boundary; B27A-only delegation; private structured selector resource binding; snapshot/revision/digest preview authority; identifier omission; zero resolution/consumption/mutation/audit; canonical P8/static/release gates; hygiene, migration hashes, and exact six-path rollback proof | exit 0; fail-first returned absent-route `404` instead of required `401`; focused/full-wiring/six-file-adjacency/canonical-P8 gates passed 1/38/100/329 tests; complete normal-umask release passed 137 / 1 skipped files with 2833 / 10 skipped tests; valid previews return B27A unchanged plus current-session opaque resource and fresh action handles, repeated requests reuse only the resource handle, and every invalid/missing/inactive/malformed path remains authority- and write-free; patch `/tmp/lethebot-gov-ux-01b27c-roundtrip-ROItmY/gov-ux-01b27c.patch` has SHA-256 `de2a051b1cc42868900d34fd94d3b12f2ab139daf91a84808a77a0d204594e51`, mode `0600`, size `24916`, and both zero-fuzz byte/mode cycles plus candidate/workspace equality passed; inventory/dependencies/migrations remain 74/60/0, Pi `0.80.2`, and the recorded hashes |
| 2026-07-29 23:40 CST | `GOV-UX-01B27D` deterministic gate | named absent-route fail-first; static authenticated-unscoped exact confirmation; resource-before-consume session/purpose/kind/system isolation; canonical private-selector parsing; single-use action authority; B27A fingerprint/revision/digest revalidation; B27B-only fixed-reason mutation; identifier-free disable/effect/audit/rollback evidence; canonical P8/static/release gates; hygiene, migration hashes, and exact six-path rollback proof | exit 0; fail-first returned absent-route `404` instead of required `401`; focused/B27C-pair/full-wiring/six-file-adjacency/canonical-P8 gates passed 1/2/39/100/329 tests; complete normal-umask release passed 137 / 1 skipped files with 2834 / 10 skipped tests; exact confirmation disables one active mapping and appends one redacted audit while unknown authority is `404` and replay/drift/inactive/service races are `409` without retry; patch `/tmp/lethebot-gov-ux-01b27d-roundtrip-H2rZKX/gov-ux-01b27d.patch` has SHA-256 `ca1e39b1730135db74ca517ca43f3ada6cea8ef9ad1faa7927d79e7f415496e3`, mode `0600`, size `36135`, and both corrected zero-fuzz byte/mode cycles plus candidate/workspace equality passed; inventory/dependencies/migrations remain 74/60/0, Pi `0.80.2`, and the recorded hashes |
| 2026-07-30 00:05 CST | `GOV-UX-01C5C` deterministic gate | one fixed readable controller over only the three homogeneous Activity-list lifecycles; exact endpoint/renderer/DOM/message/sequence configurations; unchanged HTML/CSS, visible behavior, validation, accessibility, and backend authority; stricter shell headroom regression; complete desktop/mobile C5B matrix; canonical P8/static/release gates; hygiene and exact six-path rollback proof | exit 0; fail-first measured 65,314 bytes against the new less-than-61,500 assertion; final bodies are 14,047/14,794/32,543 bytes, 61,384 total with 3,930 reclaimed and 4,152 free; named/full-HTTP/six-file-adjacency/canonical-P8 gates passed 1/21/56/329 tests; Chromium made 34 requests per viewport with zero unexpected console/page/request/external failures and byte-identical screenshots; complete normal-umask release passed 137 / 1 skipped files with 2,834 / 10 skipped tests; patch `/tmp/lethebot-gov-ux-01c5c-roundtrip-Isniye/gov-ux-01c5c.patch` has SHA-256 `031c89eb4738967c86cff574f2656b014ada2512b2d472613ed1ec3cc04c245e`, mode `0600`, size `18767`, and both corrected `v2` zero-fuzz byte/mode cycles plus candidate/workspace equality passed; inventory/dependencies/migrations remain 74/60/0, Pi `0.80.2`, and the recorded hashes |
| 2026-07-30 00:42 CST | `GOV-UX-01C5E` deterministic gate | explicit fixed key/ID binding table; accepted-value domains and deterministic presentation; shared bounded text/date validation, record cell/status construction, and normalized-list rendering; unchanged named renderers, exact keys, visible DOM, HTML/CSS, requests, controller, and authority; stricter prerequisite regression; complete desktop/mobile C5B matrix; canonical P8/static/release gates; hygiene and exact six-path rollback proof | exit 0; fail-first measured 61,384 bytes against the new less-than-57,500 assertion; final bodies are 14,047/14,794/28,617 bytes, 57,458 total with 3,926 reclaimed, 4,042 bytes below the C5C feature budget, and 8,078 below the permanent boundary; named/full-HTTP/six-file-adjacency/canonical-P8 gates passed 1/21/56/329 tests; Chromium made 34 requests per viewport with zero unexpected console/page/request/external failures and byte-identical screenshots; complete normal-umask release passed 137 / 1 skipped files with 2,834 / 10 skipped tests; patch `/tmp/lethebot-gov-ux-01c5e-roundtrip-SDWf58/gov-ux-01c5e.patch` has SHA-256 `2272ca8c878399f1227614c1fbae299d547c018d8175639a01ed8e531ec5a1d0`, mode `0600`, size `28049`, and corrected `v4` reverse-first/forward-first zero-fuzz byte/mode cycles plus candidate/workspace equality passed; inventory/dependencies/migrations remain 74/60/0, Pi `0.80.2`, and the recorded hashes |
| 2026-07-30 01:16 CST | `GOV-UX-01C5D` deterministic gate | fifth read-only Job attempts Activity tab over only B14G; strict exact result-free validation; projected-away attempt ID; shared controller and C5E primitives; loading/populated/optional/empty/malformed/unknown/result/oversized/unavailable/stale/expiry/logout states; five-tab desktop/mobile browser QA; canonical HTTP/P8/static/release gates; hygiene and exact six-path rollback proof | exit 0; resumed fail-first found the missing fifth tab; final bodies are 15,802/14,794/30,876 bytes, 61,472 total with 28 bytes below the C5D feature threshold and 4,064 below the permanent boundary; named C5D/compact/full-HTTP/six-file-adjacency/canonical-P8 gates passed 1/1/22/56/330 tests; Chromium made 53 requests per viewport with zero unexpected console/page/request/external failures and clean visually reviewed screenshots; complete normal-umask release passed 137 / 1 skipped files with 2,835 / 10 skipped tests; patch `/tmp/lethebot-gov-ux-01c5d-resume-roundtrip-a1Fq16/gov-ux-01c5d.patch` has SHA-256 `ad944ef72461ef5573fa6114721f96e2d057f7b68bd0e8200dca28824fee94db`, mode `0600`, size `14478`, and accepted `v3` reverse-first/forward-first zero-fuzz byte/mode cycles plus candidate/workspace equality passed with all proof directories mode `0700`; inventory/dependencies/migrations remain 74/60/0, Pi `0.80.2`, and the recorded hashes |
| 2026-07-30 01:54 CST | `GOV-UX-01C6A` deterministic gate | sixth read-only Action decisions Activity tab over only B14E; strict exact actions-free validation and nested bounds; projected-away turn ID; retained C5D primitives; complete decision/evaluator/failure/stale/session/keyboard/responsive browser matrix; canonical HTTP/P8/static/release gates; hygiene and exact six-path rollback proof | exit 0; fail-first found only the missing sixth tab; final bodies are 17,249/14,794/33,454 bytes, 65,497 total with 39 bytes below the permanent boundary; named C6A/C5D/compact, full HTTP, four-file HTTP adjacency, and canonical P8 passed 3/23/69/331 tests; Chromium made 33 requests per viewport with zero unexpected console/page/request/external failures and clean visually reviewed screenshots; corrected normal-umask release passed 137 / 1 skipped files with 2,836 / 10 skipped tests; patch `/tmp/lethebot-gov-ux-01c6a-roundtrip-cZFg8L/gov-ux-01c6a.patch` has SHA-256 `c00fa482aebd261fbb7aabccc46fae506cd917a3d56d929c3ba615576cbddfd7`, mode `0600`, size `14348`, and reverse-first/forward-first zero-fuzz byte/mode cycles plus candidate/workspace equality passed with all proof directories mode `0700`; inventory/dependencies/migrations remain 74/60/0, Pi `0.80.2`, and the recorded hashes |
| 2026-07-30 02:26 CST | `GOV-UX-01C6B` deterministic gate | behavior-preserving fixed DOM construction for the five non-model Activity list shells; exact pre/post runtime-structure oracle; unchanged controllers/requests/validation/session/keyboard/responsive behavior; less-than-60,500 prerequisite; canonical HTTP/P8/static/release gates; hygiene and exact six-path rollback proof | exit 0; fail-first measured 65,497 bytes against the new prerequisite; final bodies are 8,641/14,794/37,000 bytes, 60,435 total with 5,062 reclaimed and 5,101 free under the permanent boundary; full HTTP, four-file HTTP adjacency, and canonical P8 passed 24/70/332 tests; pre/post desktop and mobile structures are byte-identical with SHA-256 `b18d22317c3205a77f9608e37b4a735c98f852fc8bd7a750c4889365e570b852`; Chromium repeated 33 requests per viewport with zero unexpected console/page/request/external failures and clean visual review; normal-umask release passed 137 / 1 skipped files with 2,837 / 10 skipped tests; patch `/tmp/lethebot-gov-ux-01c6b-roundtrip-zPNH3Z/gov-ux-01c6b.patch` has SHA-256 `bb2ff1fcb44250dd20e89993436ef3700ca6d9fb74ab882a27ba20edef286ea0`, mode `0600`, size `38937`, and independent reverse-first/forward-first byte/mode cycles plus candidate/workspace equality passed; inventory/dependencies/migrations remain 74/60/0, Pi `0.80.2`, and the recorded hashes |
| 2026-07-30 02:53 CST | `GOV-UX-01C6C` deterministic gate | seventh read-only Action executions Activity tab over only B14F; strict exact audit-entry-free validation across action/status/audit/effect/evidence domains; semantic status badges; complete invalid/no-replacement/stale/session/seven-tab/responsive browser matrix; canonical HTTP/P8/static/release gates; hygiene and exact six-path rollback proof | exit 0; fail-first found only the missing seventh tab; final bodies are 8,641/14,850/39,809 bytes, 63,300 total with 2,236 bytes below the permanent boundary; named/full HTTP, four-file HTTP adjacency, and canonical P8 passed 1/25/71/333 tests; Chromium made 64 exact execution requests across both viewports with 20 invalid/unavailable states each and zero unexpected console/page/request/external failures; normal-umask release passed 137 / 1 skipped files with 2,838 / 10 skipped tests; patch `/tmp/lethebot-gov-ux-01c6c-roundtrip-nx5FKY/gov-ux-01c6c.patch` has SHA-256 `3cdf665dc206758d293988c11f56958aa6f1f976665664add2e026c83e578769`, mode `0600`, size `19347`, and independent zero-fuzz reverse-first/forward-first byte/mode cycles plus candidate/workspace equality passed; inventory/dependencies/migrations remain 74/60/0, Pi `0.80.2`, and the recorded hashes |
| 2026-07-30 03:18 CST | `GOV-UX-01C7A` deterministic gate | eighth read-only Event processing failures Activity tab over only B14H; strict exact details-free validation across required/optional bounds, canonical dates, fixed conversation types, and lowercase hashes; complete invalid/no-replacement/stale/session/eight-tab/responsive browser matrix; canonical HTTP/P8/static/release gates; hygiene and exact six-path rollback proof | exit 0; fail-first found only the missing eighth tab; final bodies are 8,641/14,850/42,007 bytes, 65,498 total with 38 bytes below the permanent boundary; focused C7A/C6C/compact, B14H wiring, full HTTP, four-file HTTP adjacency, and canonical P8 passed 4/1/26/72/334 tests; Chromium made 86 exact failure requests across both viewports with 31 invalid/unavailable states each, byte-identical runtime structures, and zero unexpected console/page/request/external failures; normal-umask release passed 137 / 1 skipped files with 2,839 / 10 skipped tests; patch `/tmp/lethebot-gov-ux-01c7a-roundtrip-cof4cL/gov-ux-01c7a.patch` has SHA-256 `9c612fdb050a7d7318e8a99c7f9a819c0176883aa4d84e351d2bf11a8fce0bc3`, mode `0600`, size `16648`, and independent zero-fuzz reverse-first/forward-first byte/mode cycles plus candidate/workspace equality passed; inventory/dependencies/migrations remain 74/60/0, Pi `0.80.2`, and the recorded hashes |
| 2026-07-30 03:43 CST | `GOV-UX-01C7B` deterministic gate | behavior-preserving shared fixed bounds and row classes plus delimiter-adjacent JavaScript newline compaction; exact pre/post runtime-structure oracle; unchanged HTML/CSS, endpoints, validation, rendering, requests, session, keyboard, and responsive behavior; less-than-63,300 prerequisite; canonical HTTP/P8/static/release gates; hygiene and exact six-path rollback proof | exit 0; fail-first measured 65,498 bytes against the new prerequisite; final bodies are 8,641/14,850/39,788 bytes, 63,279 total with 2,219 reclaimed and 2,257 free under the permanent boundary; full HTTP, four-file HTTP adjacency, and canonical P8 passed 27/73/335 tests; pre/post desktop and mobile runtime structures are byte-identical with SHA-256 `f11169afe4b1057432a7eaea753b2edb26816b8f160c83b634396d5f9b69763c`; Chromium repeated 86 failure requests and 31 invalid states per viewport with zero unexpected console/page/request/external failures and clean visual review; normal-umask release passed 137 / 1 skipped files with 2,840 / 10 skipped tests; accepted patch `/tmp/lethebot-gov-ux-01c7b-roundtrip-v2-pO9rWR/gov-ux-01c7b.patch` has SHA-256 `d4763ac687a8dbd0200e6390bc8f45e7c5fa389a166094e07605796b9a1bf1d6`, mode `0600`, size `26796`, and independent zero-fuzz reverse-first/forward-first byte/mode cycles plus candidate/workspace equality passed; inventory/dependencies/migrations remain 74/60/0, Pi `0.80.2`, and the recorded hashes |
| 2026-07-30 04:08 CST | `GOV-UX-01C7C` deterministic gate | ninth read-only Audit Activity tab over only B14I; exact top-level/actor schemas, bounds, canonical timestamp, details exclusion, and explicit redaction signals; complete invalid/no-replacement/stale/session/nine-tab/responsive browser matrix; canonical HTTP/P8/static/release gates; hygiene and exact six-path rollback proof | exit 0; fail-first found only the missing Audit definition; final bodies are 8,641/14,850/41,778 bytes, 65,269 total with 267 bytes below the permanent boundary; focused/full-HTTP/four-file-adjacency/canonical-P8 gates passed 1/28/74/336 tests; Chromium made 114 exact Audit requests with 45 invalid states per viewport, byte-identical desktop/mobile structures, and zero unexpected console/page/request/external failures; the restrictive-umask aggregate run was excluded, its isolated unchanged fixture passed 24 / 24 under normal umask, and the corrected release passed 137 / 1 skipped files with 2,841 / 10 skipped tests; accepted patch `/tmp/lethebot-gov-ux-01c7c-roundtrip-JhULjJ/gov-ux-01c7c.patch` has SHA-256 `d4997d6baefa73849ae4d319a65a99a406a5225bb2334f5427d51095512433e1`, mode `0600`, size `14227`, and independent zero-fuzz reverse-first/forward-first byte/mode cycles plus candidate/workspace equality passed; inventory/dependencies/migrations remain 74/60/0, Pi `0.80.2`, and the recorded hashes |
| 2026-07-30 05:00 CST | `GOV-UX-01C8B` deterministic gate | first exact-scope read-only Memory Records browser destination; separate static Memory module; explicit scope selection; strict catalog/record schemas; current-session opaque-handle isolation; no detail or mutation; complete desktop/mobile invalid/stale/session matrix; Activity regression; canonical HTTP/P8/static/release gates; hygiene and exact six-path rollback proof | exit 0; fail-first returned `404` for `/governance/memory.js`; final HTML/CSS/`app.js`/`activity.js`/`memory.js` are 8,641/16,415/20,939/23,616/16,720 bytes with a 45,995-byte fixed shell and every asset below 65,536; focused/full-HTTP/four-file-adjacency/canonical-P8 gates passed 1/30/76/338 tests; all three JavaScript bodies parse; Chromium exercised 18 invalid catalog and 53 invalid record states per viewport with clean screenshots, while the separate Activity oracle retained SHA-256 `18fc451df066c3b27e333dcee2106ea762918cfc29b45075edab17a0465db4c1`; normal-umask release passed 137 / 1 skipped files with 2,843 / 10 skipped tests; accepted patch `/tmp/lethebot-gov-ux-01c8b-roundtrip-uZEkD2/gov-ux-01c8b.patch` has SHA-256 `3bf2bfaa2fc6da0a856ebac335eeb6ff8a7c77154bab64f3100a80f8e1294b80`, mode `0600`, size `36758`, and independent zero-fuzz reverse-first/forward-first byte/mode cycles plus candidate/workspace equality passed; inventory/dependencies/migrations remain 74/60/0, Pi `0.80.2`, and the recorded hashes |
| 2026-07-30 05:37 CST | `GOV-UX-01C8C` deterministic gate | secondary purpose-isolated read-only Memory Review queue; Records-default two-tab interaction; independent catalogs, selections, handles, and request sequences; strict review schemas; no detail or mutation; complete desktop/mobile invalid/stale/session matrix; exact Activity regression; canonical HTTP/P8/static/release gates; hygiene and exact six-path rollback proof | exit 0; fail-first found only the missing Review scope endpoint definition; final HTML/CSS/`app.js`/`activity.js`/`memory.js` are 8,641/16,918/24,328/23,616/31,215 bytes with a 49,887-byte fixed shell and every asset below 65,536; focused/full-HTTP/four-file-adjacency/canonical-P8 gates passed 1/31/77/339 tests; all three JavaScript bodies parse; Chromium exercised 15 invalid catalog and 29 invalid Review states with exact purpose headers, no handle leakage, no detail/mutation requests, clean responsive screenshots, zero console/page/request failures, and Activity structure SHA-256 `fa3ae562f41023eb0a44c5276e12ee7b3253aeae23d6448323d676c01aaad8a2`; normal-umask release passed 137 / 1 skipped files with 2,844 / 10 skipped tests; accepted patch `/tmp/lethebot-gov-ux-01c8c-roundtrip-doGpP4/gov-ux-01c8c.patch` has SHA-256 `44a1a9d10ebbd1bcf3bc628c632b75b6d5a6c6eef956484f9ca36583df901c66`, mode `0600`, size `38850`, and independent zero-fuzz, zero-offset reverse-first/forward-first byte/mode cycles plus candidate/workspace equality passed; inventory/dependencies/migrations remain 74/60/0, Pi `0.80.2`, and the recorded hashes |
| 2026-07-30 06:29 CST | `GOV-UX-01C8D` deterministic gate | read-only exact-scope Memory Review detail; current-row resource authority; independent detail sequencing and cancellation; strict list/detail coherence; 32-entry candidate/revision bounds; fresh-handle refresh; malformed/drift no-replacement; no preview or mutation; complete desktop/mobile invalid/stale/session matrix; canonical HTTP/P8/static/release gates; hygiene and exact six-path rollback proof | exit 0; fail-first found the missing detail endpoint definition and row control; final HTML/CSS/`app.js`/`activity.js`/`memory.js` are 8,641/16,918/24,227/23,616/50,900 bytes with a 49,786-byte fixed shell and every asset below 65,536; focused/full-HTTP/four-file-adjacency/canonical-P8 gates passed 1/32/78/340 tests; all three JavaScript bodies parse; Chromium completed 18 assertions, including malformed/drift no-replacement, optional-effect absence, 32/33 truncation, fresh-handle refresh, exact GET-only requests, navigation/logout clearing, and no horizontal overflow; normal-umask release passed 137 / 1 skipped files with 2,845 / 10 skipped tests; accepted patch `/tmp/lethebot-gov-ux-01c8d-roundtrip-YczfvE/gov-ux-01c8d.patch` has SHA-256 `58a296cf709fedf10486a2d1b351eefe1c7391567f902c73df77a53fb8360ec9`, mode `0600`, size `35423`, and independent zero-context reverse-first/forward-first byte/mode cycles plus candidate/workspace equality passed; inventory/dependencies/migrations remain 74/60/0, Pi `0.80.2`, and the recorded hashes |
| 2026-07-30 07:20 CST | `GOV-UX-01C8E` deterministic gate | read-only exact-scope Memory Record provenance detail; current-row resource authority; independent detail sequencing and cancellation; complete nested row coherence; 32-entry source/revision/audit bounds; fresh-handle refresh; malformed/drift no-replacement; no preview or mutation; complete desktop/mobile invalid/stale/session matrix; canonical HTTP/P8/static/release gates; hygiene and exact six-path rollback proof | exit 0; fail-first found the missing provenance endpoint definition and row control; final HTML/CSS/`app.js`/`activity.js`/`memory.js` are 8,641/16,918/24,227/23,616/64,978 bytes with a 49,786-byte fixed shell and every asset below 65,536; focused/full-HTTP/four-file-adjacency/canonical-P8 gates passed 1/33/79/341 tests; all three JavaScript bodies parse; Chromium completed 48 functional assertions plus 15 desktop and 15 mobile visual assertions with fresh-authority refresh, exact GET-only requests, navigation/logout clearing, no handle leakage, and no overflow; normal-umask release passed 137 / 1 skipped files with 2,846 / 10 skipped tests; accepted patch `/tmp/lethebot-gov-ux-01c8e-roundtrip-final-mWkeTW/gov-ux-01c8e.patch` has SHA-256 `11bc66374619fbcabf3736f5db77a3df500c66b27e8f7e91eaad8b347e9fc386`, mode `0600`, size `59793`, and independent reverse-first/forward-first byte/mode cycles plus candidate/workspace equality passed; inventory/dependencies/migrations remain 74/60/0, Pi `0.80.2`, and the recorded hashes |

| 2026-07-30 08:14 CST | `GOV-UX-01C8F` deterministic gate | behavior-preserving static Memory presentation-module extraction; one top-level static import; exact schema/normalization/coherence/DOM/formatting/renderer ownership split; byte-identical protected assets and desktop/mobile structures; unchanged requests, states, authority, focus, cancellation, and visible output; no feature or mutation; canonical HTTP/P8/static/release gates; hygiene and exact six-path rollback proof | exit 0; the passing pre-refactor characterization passed 1 / 1, then fail-first found only absent `/governance/memory-presentation.js` and `memory.js` at 64,978 bytes; final HTML/CSS/`app.js`/`activity.js`/`memory.js`/presentation bodies are 8,641/16,918/24,227/23,616/32,442/33,072 bytes, all four JavaScript modules parse, and the first four assets remain byte-identical; focused/full-HTTP/four-file-adjacency/canonical-P8 gates passed 1/35/81/343 tests; baseline/final desktop and mobile structures plus screenshots are byte-identical, final static/API requests are 15/9 versus 14/9 baseline, and both runs have zero errors, overflow, or handle disclosure; normal-umask release passed 137 / 1 skipped files with 2,848 / 10 skipped tests; accepted patch `/tmp/lethebot-gov-ux-01c8f-roundtrip-QtDimH/gov-ux-01c8f.patch` has SHA-256 `16b383c84a2e81820d3cc96123164a9869107783a5b9fb27281de38ccff3fa8b`, mode `0600`, size `18247`, and independent reverse-first/forward-first byte/mode cycles plus candidate/workspace equality passed; inventory/dependencies/migrations remain 74/60/0, Pi `0.80.2`, and the recorded hashes |
| 2026-07-30 09:06 CST | `GOV-UX-01C8G` deterministic gate | first write-free browser maintenance-approval preview; shell-owned same-origin CSRF transport; current Review scope/resource authority; exact B7C schema and row/detail/preview coherence; seven explicit states; independent sequencing, cancellation, expiry, and handle non-disclosure; confirmation absence; canonical HTTP/P8/static/release gates; hygiene and exact six-path rollback proof | exit 0; predecessor characterization passed 8 / 8 and fail-first found only the absent preview command; final HTML/CSS/`app.js`/`activity.js`/`memory.js`/presentation bodies are 8,641/16,918/24,428/23,616/39,220/38,754 bytes with a 49,987-byte fixed shell and all four JavaScript modules parsing; presentation oracle passed 16 cases; focused/full-HTTP/four-file-adjacency/canonical-P8 gates passed 1/36/82/344 tests; desktop/mobile each made 13 exact preview POSTs with zero unexpected errors, overflow, handle disclosure, storage, confirmation requests, or durable writes; normal-umask release passed 137 / 1 skipped files with 2,849 / 10 skipped tests; accepted patch `/tmp/lethebot-gov-ux-01c8g-roundtrip-ZGlen0/gov-ux-01c8g.patch` has SHA-256 `99c503bf3aca40968690b902b8a0a88d2eade1e1efa9608aa8d9ffffcfbb2ef1`, mode `0600`, size `28373`, and independent reverse-first/forward-first byte/mode cycles plus candidate/workspace equality passed; inventory/dependencies/migrations remain 74/60/0, Pi `0.80.2`, and the recorded hashes |
| 2026-07-30 12:31 CST | `GOV-UX-01C8H` deterministic gate | exact-scope browser maintenance-approval confirmation through existing B7D; private single-use authority; strict result coherence; six terminal states; success refresh; C8G regression; canonical HTTP/P8/static/release gates; hygiene and exact six-path rollback proof | exit 0; named/full-HTTP/four-file-adjacency/canonical-P8 gates passed 1/37/83/345 tests; final HTML/CSS/`app.js`/`activity.js`/`memory.js`/presentation bodies are 8,641/16,918/24,428/23,616/46,670/41,758 bytes with a 49,987-byte fixed shell; presentation oracle passed 16 preview and 15 confirmation cases; desktop/mobile each made 11 exact previews and 6 exact confirmations with duplicate suppression, complete state/invalidation coverage, success refresh, zero leakage/storage/unexpected errors, and no rejection/application request; normal-umask release passed 137 / 1 skipped files with 2,850 / 10 skipped tests; accepted patch `/tmp/lethebot-gov-ux-01c8h-roundtrip-qeFBGnnx/gov-ux-01c8h.patch` has SHA-256 `253e728ab96e3e75a9917563df89f543e62328c52561fe1b92e26acc4db2887f`, mode `0600`, size 29,013, and independent reverse-first/forward-first byte/mode cycles plus candidate/workspace equality passed; inventory/dependencies/migrations remain 74/60/0, Pi `0.80.2`, and the recorded hashes |

The fresh private B13C rollback proof is
`/tmp/lethebot-gov-ux-01b13c-roundtrip-DFOORM/gov-ux-01b13c.patch`
(`sha256:896c7ff39a157ee0ed043d6398f6414820eb2750496a637a2b7dce7805e52255`,
mode `0600`, size `17500`); its proof root and candidate/baseline trees are
mode `0700`. Reverse and forward application restored the five allowed paths
byte-for-byte and mode-for-mode, and the candidate snapshot matched the
workspace.

The fresh private B14A rollback proof is
`/tmp/lethebot-gov-ux-01b14a-roundtrip-9mi0ry/gov-ux-01b14a.patch`
(`sha256:0f3651455f0098f34f60e30613578039c5d99f3bf01e43d40be2adefc078dcce`,
mode `0600`, size `13228`); its proof root is mode `0700`. Reverse and forward
application restored the six allowed product/test paths byte-for-byte and
mode-for-mode, and the candidate snapshot matched the workspace.

The fresh private B14B rollback proof is
`/tmp/lethebot-gov-ux-01b14b-roundtrip-9xZz8Z/gov-ux-01b14b.patch`
(`sha256:1d6a35bfbfc3c59ddec9b05d941d78e89c4cfb4c8ea301f0f27b44b8dfc3e7f1`,
mode `0600`, size `13113`); its proof root is mode `0700`. Reverse and forward
application restored the six allowed product/test paths byte-for-byte and
mode-for-mode, and the candidate snapshot matched the workspace.

The fresh private rollback releases are
`/tmp/lethebot-schema-v8-releases-wgbI75/prior-source` and
`/tmp/lethebot-schema-v8-releases-wgbI75/candidate-source`, with managed digests
`796b23e6ee441ab44f38482be832f346feaeb9bc66220d41d052341a61996b17` and
`b8d335e2c058a7482d64b118e4403098ff71ca95484a35b1aedffd56a6a08dd9`.
The enclosing directory is mode `0700`; both releases passed their own build
and preflight before the aggregate cross-version rehearsal. They are synthetic
local evidence for this slice, not authorization to deploy or operate on a live
database.

The following `/tmp` artifacts are historical private evidence from the prior
runtime program. Their recorded existence is not proof that they still exist,
that they match `cfebb77`, or that they are a valid rollback baseline for a
future candidate. P4/P9 must build and verify fresh immutable prior/candidate
releases before any authorized deployment.

The v1 artifact was recorded as
`/tmp/lethebot-v1-baseline-20260712-1806/lethebot-v1-image.tar`.
The managed-release v1 baseline is
`/tmp/lethebot-v1-managed-20260712-232832/release`; its private archive is
`/tmp/lethebot-v1-managed-20260712-232832/lethebot-v1-managed.tar`. Unlike the
container export, this bundle includes the lockfile and release-local runtime
dependencies required by the managed release digest and startup gate.
The final schema-v2 candidate is
`/tmp/lethebot-v2-final-managed-20260713-002328/release`; its private archive is
`/tmp/lethebot-v2-final-managed-20260713-002328/lethebot-v2-final-managed.tar`.
The final schema-v3 candidate is
`/tmp/lethebot-v3-final-managed-20260713-020731/release`; its private archive is
`/tmp/lethebot-v3-final-managed-20260713-020731/lethebot-v3-final-managed.tar`
with mode `0600`.

## Completed Slice: `GOV-UX-01B12L`

- `requirement`: `GOV-UX-01B12L` shared bounded event-processing-failure inspection extraction.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: bounded event-processing-failure SQL, optional details
  projection, fixed DTO, and deterministic redaction ran inside the CLI and were
  unavailable to a shared Activity view.
- `selection_reason`: the B12J/B12K critical-path reassessment left P4 externally
  blocked. B12L was the only remaining CLI-owned bounded Activity evidence read
  and required neither a route nor a mutation.
- `implementation_contract`: `GovernanceQueryService.listEventProcessingFailures`
  owns the exact filters, bound, descending order, fixed DTO, parsing fallback,
  and redaction. The CLI list is a typed identity delegate; failure persistence,
  turn execution, and aggregate health remain unchanged.
- `allowed_paths`: `docs/governance-ui.md`,
  `src/governance/query-service.ts`, `src/cli/governance.ts`,
  `tests/unit/governance/query-service.test.ts`,
  `tests/unit/cli/governance.test.ts`, and this mutable checkpoint only.
- `protected_paths`: all B3-B12K HTTP/session/scope/resource/preview, mutation,
  shared memory/review/audit/invocation/tool/action/job/job-attempt/heartbeat
  reads; `GovernanceService`; event-failure schema/write behavior, ingress/turn
  admission and execution, aggregate health, operations metrics, context/action
  behavior, CLI grammar/output, browser/config/schema/migrations/dependencies/
  lockfile, Provider/private/live data, deployment/restart/container behavior,
  and every prior slice remain unchanged.
- `assertions`: conjunctive filters, exact descending occurrence-time/ID order,
  explicit/default limits, details opt-in, malformed-JSON fallback, recursive
  diagnostic/identifier redaction, nullable fields, exact dates, zero writes/FK
  drift, and CLI identity delegation all pass.
- `focused_commands`: recorded in the B12L evidence row above.
- `schema_dependency_live_boundary`: no HTTP/browser route, mutation, schema,
  migration, dependency, QQ, Provider, private/live data, deployment, restart,
  container, or persistence-format operation occurred.
- `rollback_boundary`: the exact B12K baseline, B12L candidate, roundtrip tree,
  and private patch are in `/tmp/lethebot-gov-ux-01b12l-roundtrip-hTFRpv`.
- `fail_first_evidence`: 2 files / 73 tests collected; 71 passed and exactly the
  missing shared list method and CLI delegation assertions failed.
- `final_evidence`: recorded in the B12L evidence row above; all required gates passed.
- `exact_next_action`: completed; the selected B12M follow-on is recorded below.

## Completed Slice: `GOV-UX-01B13A`

- `requirement`: `GOV-UX-01B13A` shared stored Explain/context-trace read extraction.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: the stored context-trace projection ran inside
  `GovernanceCLI.findStoredContextExplanation`, so a future Explain view would
  have had to depend on CLI code rather than the shared governance query boundary.
- `selection_reason`: B12M completed the bounded Activity and Privacy read
  inventory. The remaining read-only SQL in `GovernanceCLI` is either mutation
  precondition validation or Explain evidence; the stored trace branch is the
  smallest Explain slice and does not alter ContextBuilder rebuild behavior,
  action/tool ordering, or any mutation path.
- `implementation_contract`: `GovernanceQueryService.explainStoredContext` owns
  one typed read over the existing `ContextTraceRepository.findByTurnId` and one
  pure fixed projection. The CLI stored-trace helper delegates without changing
  the `ContextExplanation` result, stored-vs-rebuilt selection, or action/tool
  evidence composition.
- `allowed_paths`: `docs/governance-ui.md`,
  `src/governance/query-service.ts`, `src/cli/governance.ts`,
  `tests/unit/governance/query-service.test.ts`,
  `tests/unit/cli/governance.test.ts`, and this mutable checkpoint only.
- `protected_paths`: privacy preference implementation and B12A-B12M reads;
  all HTTP/session/scope/resource/preview and mutation paths;
  `ContextTraceRepository` persistence/serialization/redaction behavior,
  ContextBuilder rebuild semantics, action/tool selection and ordering, CLI
  grammar/output, browser/config/schema/migrations/dependencies/lockfile,
  Provider/private/live data, deployment/restart/container behavior, and every
  prior slice remain unchanged.
- `assertions`: stored traces map field-for-field to the existing fixed
  `ContextExplanation` stored branch, including optional memory selections,
  exact dates/IDs and redacted persisted values; missing traces return `null`;
  the query is read-only with clean integrity/FKs; the CLI delegates the exact
  turn ID and preserves stored-vs-rebuilt behavior and output.
- `focused_commands`: fail-first query/CLI unit regressions; existing stored
  `why` CLI process tests; context-trace repository and Explain adjacency;
  governance HTTP non-exposure; canonical P8 command; typecheck, scoped/full
  lint, release gate, diff hygiene, migration hashes, and exact five-path
  reverse/forward proof.
- `schema_dependency_live_boundary`: B13A may only relocate the bounded stored
  read and pure projection. No context-trace write, schema/migration,
  dependency/lockfile, HTTP/browser route, mutation, CLI grammar/output,
  QQ, Provider, private/live data, deployment, restart, container, or
  persistence-format operation is permitted.
- `rollback_boundary`: the exact B12M candidate was used as the B13A baseline;
  the fresh five-path B13A baseline/candidate, roundtrip tree, and private patch
  are in `/tmp/lethebot-gov-ux-01b13a-roundtrip-J7LA7j`.
- `fail_first_evidence`: 2 files / 77 tests collected; 75 passed and exactly the
  missing shared method and delegation assertions failed.
- `final_evidence`: recorded in the B13A evidence row above; all required gates
  passed.
- `exact_next_action`: completed; the selected B13B follow-on is recorded below.

## Completed Slice: `GOV-UX-01B13B`

- `requirement`: `GOV-UX-01B13B` shared Explain action/tool evidence read
  extraction.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: bounded tool-call, action-decision, and action-execution SQL
  and DTO composition ran inside the CLI Explain path, so a future Explain view
  would have had to duplicate that evidence boundary.
- `selection_reason`: B13A removed the stored-trace dependency from the CLI.
  These three remaining Explain reads were bounded, read-only, already had
  shared list projectors, and could be extracted without changing persistence,
  ContextBuilder rebuilds, or any HTTP route.
- `implementation_contract`: `GovernanceQueryService.explainToolCalls`,
  `explainActionDecision`, and `explainActionExecutions` own typed shared reads
  and pure projections for the selected action decision, its ascending action
  executions, and ascending tool calls. Linked-decision preference, existing
  redaction, nullable fields, reaction effect labels, exact ordering, and the
  `ContextExplanation` shape remain unchanged; CLI helpers are typed identity
  delegates.
- `allowed_paths`: `docs/governance-ui.md`,
  `src/governance/query-service.ts`, `src/cli/governance.ts`,
  `tests/unit/governance/query-service.test.ts`,
  `tests/unit/cli/governance.test.ts`, and this mutable checkpoint only.
- `protected_paths`: B13A stored-trace ownership and all B3-B12M reads;
  `ContextTraceRepository` persistence/serialization/redaction behavior,
  ContextBuilder rebuild semantics, action/tool persistence and execution,
  governance mutations, HTTP/session/scope/resource/preview routes, CLI
  grammar/output, browser/config/schema/migrations/dependencies/lockfile,
  Provider/private/live data, deployment/restart/container behavior, and every
  prior slice remain unchanged.
- `assertions`: exact linked action-decision selection, ascending execution and
  tool ordering, fixed redacted Explain DTOs, nullable/error/effect parity,
  zero-write behavior with clean integrity/FKs, no context rebuild on stored
  traces, and CLI identity delegation all pass.
- `focused_commands`: fail-first query/CLI unit regressions; existing `why` CLI
  process tests for action/tool evidence; action/tool repository and Explain
  adjacency; governance HTTP non-exposure; canonical P8 command; typecheck,
  scoped/full lint, release gate, diff hygiene, migration hashes, and exact
  five-path reverse/forward proof.
- `schema_dependency_live_boundary`: B13B only relocates the bounded Explain
  action/tool reads and pure projections. No write, schema/migration,
  dependency/lockfile, HTTP/browser route, mutation, CLI grammar/output, QQ,
  Provider, private/live data, deployment, restart, container, or
  persistence-format operation is permitted.
- `rollback_boundary`: the B13A candidate is the fresh five-path baseline;
  the final reverse/forward proof is in
  `/tmp/lethebot-gov-ux-01b13b-roundtrip-W976OF` at
  `gov-ux-01b13b.patch` (`sha256:d8ce367715d78c6459efc0349c53b08fa9c7b0c54c32d55420449ad5e6a3a3af`,
  mode `0600`, size `27321`, proof root mode `0700`).
- `fail_first_evidence`: 2 files / 79 tests collected; 77 passed and exactly
  the missing shared action/tool methods and CLI delegation assertions failed.
- `final_evidence`: the B13B evidence row above records all required gates,
  including the canonical six-file P8 set at 233 / 233 and the exact patch hash.
- `exact_next_action`: completed; reassess the remaining turn-resolution read
  before selecting B13C.

## Completed Slice: `GOV-UX-01B13C`

- `requirement`: `GOV-UX-01B13C` shared Explain turn-resolution read extraction.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: `GovernanceCLI.resolveExplainContextOptions` owned the direct
  `agent_turns`/`chat_messages` lookup that selected an explicit or latest turn
  and supplied conversation metadata for Explain rebuilds.
- `selection_reason`: B13B completed the shared action/tool Explain evidence.
  This was the final bounded Explain-owned read and could move without changing
  CLI validation, ContextBuilder behavior, stored-trace selection, or any route.
- `implementation_contract`: `GovernanceQueryService.resolveExplainTurn` owns
  the exact/latest `started_at DESC` read and
  `projectGovernanceExplainTurnResolution` owns the fixed raw-ID DTO with
  nullable conversation type, group, and sender fields. The CLI awaits the
  typed delegate and retains canonical-user inference and rebuild inputs.
- `allowed_paths`: `docs/governance-ui.md`,
  `src/governance/query-service.ts`, `src/cli/governance.ts`,
  `tests/unit/governance/query-service.test.ts`,
  `tests/unit/cli/governance.test.ts`, and this mutable checkpoint only.
- `protected_paths`: B13A/B13B Explain projections and all prior governance
  reads; ContextTraceRepository, ContextBuilder, action/tool persistence and
  ordering, governance mutations, HTTP/session/scope/resource/preview routes,
  CLI grammar/output, browser/config/schema/migrations/dependencies/lockfile,
  Provider/private/live data, deployment/restart/container behavior, and every
  prior slice remain unchanged.
- `assertions`: exact-ID and latest-turn selection, joined metadata including
  nulls, missing rows, clean zero-write integrity/FKs, private/group rebuild
  inputs, existing validation/errors, canonical-user inference, and stored-trace
  preference all pass.
- `focused_commands`: fail-first and final query/CLI unit regressions; selected
  `why` process tests; the canonical six-file P8 command; typecheck, scoped and
  full lint, release gate, diff hygiene, migration hashes, and the exact
  five-path reverse/forward proof.
- `schema_dependency_live_boundary`: B13C only relocates the bounded
  turn-resolution read and pure projection. No write, schema/migration,
  dependency/lockfile, HTTP/browser route, mutation, CLI grammar/output, QQ,
  Provider, private/live data, deployment, restart, container, or
  persistence-format operation changed.
- `rollback_boundary`: the B13B candidate is the fresh five-path baseline; the
  B13C candidate was privately reverse/forward applied and matched byte-for-byte
  and mode-for-mode.
- `fail_first_evidence`: 2 files / 81 tests collected; 79 passed and exactly
  the missing shared method and CLI delegation assertions failed.
- `final_evidence`: the B13C evidence row above records the canonical P8 and
  release gates; the private patch hash and proof directory are recorded there.
- `exact_next_action`: completed; reassess the remaining P8 governance workflow
  boundary before selecting another slice.

## Completed Slice: `GOV-UX-01B14A`

- `requirement`: `GOV-UX-01B14A` authenticated Activity model-invocation summary
  read.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: the shared model-invocation aggregate exists and is already
  consumed by CLI/overview-adjacent governance code, but no local HTTP Activity
  read workflow exposes its payload-free health projection.
- `selection_reason`: this is the smallest remaining operator read boundary. It
  is system-wide, contains no raw invocation IDs or payloads, requires no scope
  or resource handle, and can be independently disabled without changing CLI,
  QQ, mutation, or browser-shell behavior.
- `implementation_contract`: register only authenticated read-only
  `GET /governance/api/v1/activity/model-invocations` with purpose
  `governance.activity.model_invocations.read`. Reject query strings and the
  scope header through the existing HTTP boundary, call only
  `GovernanceQueryService.summarizeModelInvocations()` with default filters,
  and return its fixed aggregate DTO. No route body, SQL write, raw provider
  prompt/response, invocation identifier, or new filter semantics are allowed.
- `allowed_paths`: `docs/governance-ui.md`, `docs/architecture.md`,
  `docs/deployment.md`, `docs/security-privacy.md`, `src/index.ts`,
  `tests/integration/governance-http-wiring.test.ts`, and this checkpoint only.
- `protected_paths`: all query-service/CLI behavior and DTOs; existing HTTP
  session, CSRF, body, scope/resource/preview registries and routes; browser
  assets; governance mutations; config/schema/migrations/dependencies/lockfile;
  QQ, Provider, private/live data, deployment/restart/container behavior; and
  every prior slice.
- `assertions`: unauthenticated access is `401`; authenticated access is `200`
  with the exact aggregate projection and ISO `generatedAt`; a scope header or
  query string is rejected before the callback; repeated reads are zero-write;
  no raw invocation identifier, prompt, response, or provider payload appears;
  disabled governance remains unreachable; existing overview/review routes and
  session lifecycle remain unchanged; every canonical trust, architecture, and
  deployment document describes the same registered domain-route boundary.
- `focused_commands`: fail-first and final focused route coverage; four-file
  governance HTTP adjacency; the canonical seven-file P8 command;
  `pnpm typecheck`; scoped ESLint; `pnpm release:check`; `git diff --check`;
  and a private reverse/forward patch proof over the six allowed product/test
  paths.
- `schema_dependency_live_boundary`: read-only route wiring and synthetic
  tests/docs only. No schema, migration, dependency, lockfile, persistence,
  Provider, QQ, private DB, deployment, restart, container, or browser visual
  operation changed.
- `rollback_boundary`: reconstruct a six-path fresh B13C baseline by adding the
  pre-B14A security document to the existing five-path proof, then strictly
  reverse and forward apply only the B14A route, test, and canonical-document
  changes byte-for-byte and mode-for-mode.
- `fail_first_evidence`: the focused assertion returned `404` instead of the
  expected unauthenticated `401`, proving the route was absent before B14A.
- `correction_evidence`: the fresh required-doc audit found
  `docs/security-privacy.md` still claiming that production wiring supplies no
  domain route; the statement now records the authenticated-unscoped bounded
  Activity route and its payload/identifier exclusions.
- `final_evidence`: the deterministic gate row above records the focused,
  adjacency, canonical P8, static, release, hygiene, and migration evidence.
  The corrected six-path patch has SHA-256
  `0f3651455f0098f34f60e30613578039c5d99f3bf01e43d40be2adefc078dcce`,
  mode `0600`, and size `13228`; its mode-`0700` proof root independently
  round-tripped all six paths byte-for-byte and mode-for-mode, and every
  candidate snapshot matches the workspace.
- `exact_next_action`: assess `GovernanceQueryService.listWorkerHeartbeats` as
  `GOV-UX-01B14B` and record the complete pre-edit HTTP contract.

## Completed Slice: `GOV-UX-01B14B`

- `requirement`: `GOV-UX-01B14B` authenticated Activity worker-heartbeat read.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: `GovernanceQueryService.listWorkerHeartbeats()` already owns
  the bounded, ordered, default-details-hidden, recursively redacted read, but
  the production authenticated-unscoped route inventory exposes only the
  adjacent model-invocation Activity projection.
- `selection_reason`: worker health is a required Activity workflow, the shared
  DTO is already privacy-bounded, and the default read can be exposed as one
  independently reversible route without adding filters, handles, mutation,
  query-service behavior, or a browser shell.
- `implementation_contract`: register only authenticated read-only
  `GET /governance/api/v1/activity/worker-heartbeats` with purpose
  `governance.activity.worker_heartbeats.read`. Reject every query string and
  scope header through the existing authenticated-unscoped boundary, call only
  `GovernanceQueryService.listWorkerHeartbeats()` with no options, and return
  its existing default projection. The route accepts no body or filter, keeps
  `details` absent, and preserves the shared default 100-row limit,
  `heartbeat_at DESC, worker_id ASC` ordering, nullable current-job field, date
  conversion, and identifier redaction.
- `allowed_paths`: `docs/governance-ui.md`, `docs/architecture.md`,
  `docs/deployment.md`, `docs/security-privacy.md`, `src/index.ts`,
  `tests/integration/governance-http-wiring.test.ts`, and this checkpoint only.
- `protected_paths`: `GovernanceQueryService`, `GovernanceCLI`, their DTOs and
  tests, worker/job repositories and persistence, the generic HTTP server and
  session/CSRF/scope/resource/preview registries, existing routes, browser
  assets, governance mutations, config/schema/migrations/dependencies/lockfile,
  QQ, Provider, private/live data, deployment/restart/container behavior, and
  every prior slice.
- `assertions`: unauthenticated access is `401` with zero query-service calls;
  a query string or scope header is `400` before the callback; authenticated
  access is `200` with the exact default list and ISO heartbeat dates; the
  shared method is called with no arguments; raw worker, worker-type, current-
  job, and details values do not appear; repeated reads are deterministic and
  produce zero writes; the listener remains default-unreachable; and the
  model-invocation, overview, review, and session lifecycle stay unchanged.
- `focused_commands`: fail-first then final single-route integration coverage;
  governance HTTP/application-wiring adjacency; the canonical P8 command from
  `docs/long-term-development-constraints.md`; `pnpm typecheck`; scoped ESLint;
  `pnpm release:check`; `git diff --check`; migration hashes; inventory/privacy
  hygiene; and an exact six-path private reverse/forward patch proof.
- `schema_dependency_live_boundary`: read-only route wiring plus synthetic
  tests and canonical docs only. No schema, migration, dependency, lockfile,
  persistence, Provider, QQ, private DB, deployment, restart, container, or
  browser visual operation is permitted.
- `rollback_boundary`: the exact B14A candidate is captured across all six
  allowed product/test paths under the mode-`0700` private proof root
  `/tmp/lethebot-gov-ux-01b14b-roundtrip-9xZz8Z`; the final patch must strictly
  reverse to that baseline and forward to the candidate byte-for-byte and
  mode-for-mode without touching the workspace during proof.
- `fail_first_evidence`: the single focused integration test collected nine
  tests, skipped eight unrelated cases, and failed because unauthenticated
  `GET /governance/api/v1/activity/worker-heartbeats` returned `404` instead of
  the required `401`; no source or canonical domain document had been edited.
- `final_evidence`: the deterministic gate row above records the focused,
  full-route, application/HTTP adjacency, canonical P8, static, release,
  integrity/FK, privacy, inventory, migration, and hygiene evidence. The exact
  six-path patch has SHA-256
  `1d6a35bfbfc3c59ddec9b05d941d78e89c4cfb4c8ea301f0f27b44b8dfc3e7f1`,
  mode `0600`, and size `13113`; its mode-`0700` proof root strictly restored
  both boundaries and the candidate snapshot matches the workspace.
- `exact_next_action`: completed; the full P8 reassessment selected the bounded
  default job list as `GOV-UX-01B14C`.

## Completed Slice: `GOV-UX-01B14C`

- `requirement`: `GOV-UX-01B14C` authenticated Activity job-list read.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: `GovernanceQueryService.listJobs()` already owns the bounded,
  stably ordered, payload-hidden, recursively redacted default read, but the
  production Activity routes expose model-invocation and worker-heartbeat data
  without the job half of the required jobs/workers workflow.
- `selection_reason`: after the required two-slice critical-path reassessment,
  this is the smallest remaining operator workflow. It completes jobs/workers
  with an existing default-safe DTO; memory and Explain need lookup/handle
  design, while tool/action, audit/failure, and privacy reads expose broader
  evidence surfaces. No new filter, handle, mutation, or browser route is
  required.
- `implementation_contract`: register only authenticated read-only
  `GET /governance/api/v1/activity/jobs` with purpose
  `governance.activity.jobs.read`. Reject every query string and scope header
  through the existing authenticated-unscoped boundary, call only
  `GovernanceQueryService.listJobs()` with no options, and return its existing
  default projection. The route accepts no body or filter, keeps `payload` and
  `result` absent, and preserves the shared default 100-row limit,
  `scheduled_at ASC, created_at ASC` ordering, nullable operational fields,
  date conversion, diagnostic redaction, and identifier redaction.
- `allowed_paths`: `docs/governance-ui.md`, `docs/architecture.md`,
  `docs/deployment.md`, `docs/security-privacy.md`, `src/index.ts`,
  `tests/integration/governance-http-wiring.test.ts`, and this checkpoint only.
- `protected_paths`: `GovernanceQueryService`, `GovernanceCLI`, their DTOs and
  tests, job/attempt/worker repositories and persistence, the generic HTTP
  server and session/CSRF/scope/resource/preview registries, existing routes,
  browser assets, governance mutations, config/schema/migrations/dependencies/
  lockfile, QQ, Provider, private/live data, deployment/restart/container
  behavior, and every prior slice.
- `assertions`: unauthenticated access is `401` with zero query-service calls;
  a query string or scope header is `400` before the callback; authenticated
  access is `200` with the exact default list and ISO dates; the shared method
  is called with no arguments; raw job/type/idempotency/lease/diagnostic values,
  payload, and result do not appear; repeated reads are deterministic and
  produce zero writes; integrity/FKs remain clean; the listener remains
  default-unreachable; and all existing routes/session lifecycle stay unchanged.
- `focused_commands`: fail-first then final single-route integration coverage;
  governance HTTP/application-wiring adjacency; the canonical P8 command from
  `docs/long-term-development-constraints.md`; `pnpm typecheck`; scoped ESLint;
  `pnpm release:check`; `git diff --check`; migration hashes; inventory/privacy
  hygiene; and an exact six-path private reverse/forward patch proof.
- `schema_dependency_live_boundary`: read-only route wiring plus synthetic
  tests and canonical docs only. No schema, migration, dependency, lockfile,
  persistence behavior, Provider, QQ, private DB, deployment, restart,
  container, or browser visual operation is permitted.
- `rollback_boundary`: the exact B14B candidate is captured across all six
  allowed product/test paths under the mode-`0700` private proof root
  `/tmp/lethebot-gov-ux-01b14c-roundtrip-0Rwbl4`; the final patch must strictly
  reverse to that baseline and forward to the candidate byte-for-byte and
  mode-for-mode without touching the workspace during proof.
- `fail_first_evidence`: the single focused integration test collected ten
  tests, skipped nine unrelated cases, and failed because unauthenticated
  `GET /governance/api/v1/activity/jobs` returned `404` instead of the required
  `401`; no source or canonical domain document had been edited.
- `final_evidence`: the deterministic gate row above records the focused,
  full-route, application/HTTP adjacency, canonical P8, static, release,
  integrity/FK, privacy, inventory, migration, and hygiene evidence. The exact
  six-path patch has SHA-256
  `d41420e0eeaad4fe9715336c8a8c77cbc72b83e167a22b761cd547072701c74f`,
  mode `0600`, and size `12718`; its mode-`0700` proof root strictly restored
  both boundaries and the candidate snapshot matches the workspace.
- `exact_next_action`: completed; the P8 reassessment selected the bounded
  default tool-call list for `GOV-UX-01B14D` contract reproduction.

## Completed Slice: `GOV-UX-01B14D`

- `requirement`: `GOV-UX-01B14D` authenticated Activity tool-call list read.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: `GovernanceQueryService.listToolCalls()` already owns the
  bounded, newest-first, payload-hidden, recursively redacted default read, but
  the production Activity routes expose model invocation and jobs/workers data
  without the required tool-call workflow.
- `selection_reason`: after closing the jobs/workers pair, this is the smallest
  remaining required Activity view. Its existing default DTO is payload-free
  and needs one table/read path; action history needs paired decision/execution
  semantics, audit/failures expose broader evidence surfaces, privacy needs a
  scope decision, and memory/Explain require lookup or handle design.
- `implementation_contract`: register only authenticated read-only
  `GET /governance/api/v1/activity/tool-calls` with purpose
  `governance.activity.tool_calls.read`. Reject every query string and scope
  header through the existing authenticated-unscoped boundary, call only
  `GovernanceQueryService.listToolCalls()` with no options, and return its
  existing default projection. The route accepts no body or filter, keeps
  `input` and `output` absent, and preserves the shared default 100-row limit,
  `created_at DESC, id DESC` ordering, nullable diagnostic/timing fields, date
  conversion, recursive identifier/classification/diagnostic redaction, and
  persisted `secretsRedacted` signal.
- `allowed_paths`: `docs/governance-ui.md`, `docs/architecture.md`,
  `docs/deployment.md`, `docs/security-privacy.md`, `src/index.ts`,
  `tests/integration/governance-http-wiring.test.ts`, and this checkpoint only.
- `protected_paths`: `GovernanceQueryService`, `GovernanceCLI`, their DTOs and
  tests, tool/action/audit/failure repositories and persistence, the generic
  HTTP server and session/CSRF/scope/resource/preview registries, existing
  routes, browser assets, governance mutations, config/schema/migrations/
  dependencies/lockfile, QQ, Provider, private/live data, deployment/restart/
  container behavior, and every prior slice.
- `assertions`: unauthenticated access is `401` with zero query-service calls;
  a query string or scope header is `400` before the callback; authenticated
  access is `200` with the exact default list and ISO date; the shared method is
  called with no arguments; raw tool/turn/actor/classification/diagnostic values,
  input, and output do not appear; repeated reads are deterministic and produce
  zero writes; integrity/FKs remain clean; the listener remains default-
  unreachable; and all existing routes/session lifecycle stay unchanged.
- `focused_commands`: fail-first then final single-route integration coverage;
  governance HTTP/application-wiring adjacency; the canonical P8 command from
  `docs/long-term-development-constraints.md`; `pnpm typecheck`; scoped ESLint;
  `pnpm release:check`; `git diff --check`; migration hashes; inventory/privacy
  hygiene; and an exact six-path private reverse/forward patch proof.
- `schema_dependency_live_boundary`: read-only route wiring plus synthetic
  tests and canonical docs only. No schema, migration, dependency, lockfile,
  persistence behavior, Provider, QQ, private DB, deployment, restart,
  container, or browser visual operation is permitted.
- `rollback_boundary`: the exact B14C candidate is captured across all six
  allowed product/test paths under the mode-`0700` private proof root
  `/tmp/lethebot-gov-ux-01b14d-roundtrip-c86lm9`; the final patch must strictly
  reverse to that baseline and forward to the candidate byte-for-byte and
  mode-for-mode without touching the workspace during proof.
- `fail_first_evidence`: the single focused integration test collected eleven
  tests, skipped ten unrelated cases, and failed because unauthenticated
  `GET /governance/api/v1/activity/tool-calls` returned `404` instead of the
  required `401`; no production source or canonical domain document had been
  edited.
- `final_evidence`: the deterministic gate row above records the focused,
  full-route, application/HTTP adjacency, canonical P8, static, release,
  integrity/FK, privacy, inventory, migration, and hygiene evidence. The exact
  six-path patch has SHA-256
  `74f87a6f725bb713d22ca52f373bf433cb576889840ad814c8fa8e37be3235f2`,
  mode `0600`, and size `13159`; its mode-`0700` proof root strictly restored
  both boundaries and the candidate snapshot matches the workspace.
- `exact_next_action`: completed; the full P8 reassessment selected the bounded
  default action-decision list for `GOV-UX-01B14E` contract reproduction.

## Completed Slice: `GOV-UX-01B14E`

- `requirement`: `GOV-UX-01B14E` authenticated Activity action-decision list
  read.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: `GovernanceQueryService.listActionDecisions()` already owns
  the bounded, newest-first, action-hidden, recursively redacted default read,
  but the production Activity routes expose tool calls without the action half
  of the tools/actions workflow.
- `selection_reason`: the required two-slice P8 reassessment found this to be
  the smallest next operator workflow. It completes the first half of action
  history using one existing default-safe table projection; action executions
  can follow independently, while audit/failures, privacy, memory, and Explain
  require broader evidence, scope, or handle decisions.
- `implementation_contract`: register only authenticated read-only
  `GET /governance/api/v1/activity/action-decisions` with purpose
  `governance.activity.action_decisions.read`. Reject every query string and
  scope header through the existing authenticated-unscoped boundary, call only
  `GovernanceQueryService.listActionDecisions()` with no options, and return its
  existing default projection. The route accepts no body or filter, keeps
  `actions` absent, and preserves the shared default 100-row limit,
  `created_at DESC, id DESC` ordering, nullable evaluator outcome, exact action
  count, ISO date conversion, and recursive identifier/classification/reason/
  suppressor redaction.
- `allowed_paths`: `docs/governance-ui.md`, `docs/architecture.md`,
  `docs/deployment.md`, `docs/security-privacy.md`, `src/index.ts`,
  `tests/integration/governance-http-wiring.test.ts`, and this checkpoint only.
- `protected_paths`: `GovernanceQueryService`, `GovernanceCLI`, their DTOs and
  tests, action/tool/audit/failure repositories and persistence, the generic
  HTTP server and session/CSRF/scope/resource/preview registries, existing
  routes, browser assets, governance mutations, config/schema/migrations/
  dependencies/lockfile, QQ, Provider, private/live data, deployment/restart/
  container behavior, and every prior slice.
- `assertions`: unauthenticated access is `401` with zero query-service calls;
  a query string or scope header is `400` before the callback; authenticated
  access is `200` with the exact default list and ISO date; the shared method is
  called with no arguments; raw decision/turn/classification/reason/suppressor
  values and action plans do not appear; repeated reads are deterministic and
  produce zero writes; integrity/FKs remain clean; the listener remains default-
  unreachable; and all existing routes/session lifecycle stay unchanged.
- `focused_commands`: fail-first then final single-route integration coverage;
  governance HTTP/application-wiring adjacency; the canonical P8 command from
  `docs/long-term-development-constraints.md`; `pnpm typecheck`; scoped ESLint;
  `pnpm release:check`; `git diff --check`; migration hashes; inventory/privacy
  hygiene; and an exact six-path private reverse/forward patch proof.
- `schema_dependency_live_boundary`: read-only route wiring plus synthetic
  tests and canonical docs only. No schema, migration, dependency, lockfile,
  persistence behavior, Provider, QQ, private DB, deployment, restart,
  container, or browser visual operation is permitted.
- `rollback_boundary`: the exact B14D candidate is captured across all six
  allowed product/test paths under the mode-`0700` private proof root
  `/tmp/lethebot-gov-ux-01b14e-roundtrip-7g1uku`; the final patch must strictly
  reverse to that baseline and forward to the candidate byte-for-byte and
  mode-for-mode without touching the workspace during proof.
- `fail_first_evidence`: the single focused integration test collected twelve
  tests, skipped eleven unrelated cases, and failed because unauthenticated
  `GET /governance/api/v1/activity/action-decisions` returned `404` instead of
  the required `401`; no production source or canonical domain document had
  been edited.
- `final_evidence`: the deterministic gate row above records the focused,
  full-route, application/HTTP adjacency, canonical P8, static, release,
  integrity/FK, privacy, inventory, migration, and hygiene evidence. The exact
  six-path patch has SHA-256
  `5887abc6675fe6b6f729ddafdd77b0fd64618d0d8c18e655e04f9ef123db1286`,
  mode `0600`, and size `13533`; its mode-`0700` proof root passed independent
  reverse-first and forward-first cycles and the candidate snapshot matches the
  workspace.
- `exact_next_action`: completed; the P8 reassessment selected the paired bounded
  default action-execution list for `GOV-UX-01B14F` contract reproduction.

## Completed Slice: `GOV-UX-01B14F`

- `requirement`: `GOV-UX-01B14F` authenticated Activity action-execution list
  read.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: `GovernanceQueryService.listActionExecutions()` already owns
  the bounded, newest-first, audit-entry-hidden, recursively redacted default
  read, but the production Activity routes now expose action decisions without
  the execution half of the action-history workflow.
- `selection_reason`: this is the smallest paired continuation after B14E and
  uses one existing default-safe table projection. It completes bounded action
  history without adding filters or linkage APIs; audit/failure, privacy,
  memory, and Explain workflows still require broader evidence, scope, or
  handle decisions.
- `implementation_contract`: register only authenticated read-only
  `GET /governance/api/v1/activity/action-executions` with purpose
  `governance.activity.action_executions.read`. Reject every query string and
  scope header through the existing authenticated-unscoped boundary, call only
  `GovernanceQueryService.listActionExecutions()` with no options, and return
  its existing default projection. The route accepts no body or filter, keeps
  `auditEntry` absent, and preserves the shared default 100-row limit,
  `executed_at DESC, id DESC` ordering, nullable executed/downgrade/error fields,
  audit level, ISO date conversion, and recursive identifier/classification/
  diagnostic redaction.
- `allowed_paths`: `docs/governance-ui.md`, `docs/architecture.md`,
  `docs/deployment.md`, `docs/security-privacy.md`, `src/index.ts`,
  `tests/integration/governance-http-wiring.test.ts`, and this checkpoint only.
- `protected_paths`: `GovernanceQueryService`, `GovernanceCLI`, their DTOs and
  tests, action/tool/audit/failure repositories and persistence, the generic
  HTTP server and session/CSRF/scope/resource/preview registries, existing
  routes, browser assets, governance mutations, config/schema/migrations/
  dependencies/lockfile, QQ, Provider, private/live data, deployment/restart/
  container behavior, and every prior slice.
- `assertions`: unauthenticated access is `401` with zero query-service calls;
  a query string or scope header is `400` before the callback; authenticated
  access is `200` with the exact default list and ISO date; the shared method is
  called with no arguments; raw execution/decision/effect/classification/
  diagnostic values and audit entries do not appear; repeated reads are
  deterministic and produce zero writes; integrity/FKs remain clean; the
  listener remains default-unreachable; and all existing routes/session
  lifecycle stay unchanged.
- `focused_commands`: fail-first then final single-route integration coverage;
  governance HTTP/application-wiring adjacency; the canonical P8 command from
  `docs/long-term-development-constraints.md`; `pnpm typecheck`; scoped ESLint;
  `pnpm release:check`; `git diff --check`; migration hashes; inventory/privacy
  hygiene; and an exact six-path private reverse/forward patch proof.
- `schema_dependency_live_boundary`: read-only route wiring plus synthetic
  tests and canonical docs only. No schema, migration, dependency, lockfile,
  persistence behavior, Provider, QQ, private DB, deployment, restart,
  container, or browser visual operation is permitted.
- `rollback_boundary`: the exact B14E candidate is captured across all six
  allowed product/test paths under the mode-`0700` private proof root
  `/tmp/lethebot-gov-ux-01b14f-roundtrip-2fIsrP`; the final patch must strictly
  reverse to that baseline and forward to the candidate byte-for-byte and
  mode-for-mode without touching the workspace during proof.
- `fail_first_evidence`: the single focused integration test collected thirteen
  tests, skipped twelve unrelated cases, and failed because unauthenticated
  `GET /governance/api/v1/activity/action-executions` returned `404` instead of
  the required `401`; no production source or canonical route document had been
  edited.
- `final_evidence`: the deterministic gate row above records the focused,
  full-route, application/HTTP adjacency, canonical P8, static, release,
  integrity/FK, privacy, inventory, migration, and hygiene evidence. The exact
  six-path patch has SHA-256
  `8950856cd22c89c18bfb4f4e61459eac9937c6569debaaf9b44625e97529f78d`,
  mode `0600`, and size `13631`; its mode-`0700` proof root passed independent
  reverse-first and forward-first cycles and the candidate snapshot matches the
  workspace.
- `exact_next_action`: completed; the required two-slice P8 reassessment selected
  the bounded default job-attempt list for `GOV-UX-01B14G` contract reproduction.

## Completed Slice: `GOV-UX-01B14G`

- `requirement`: `GOV-UX-01B14G` authenticated Activity job-attempt list read.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: `GovernanceQueryService.listJobAttempts()` already owns the
  bounded, newest-first, result-hidden, recursively redacted default read, but
  the production Activity routes expose jobs and worker heartbeats without the
  attempt history required to diagnose execution and retry behavior.
- `selection_reason`: the required two-slice reassessment found this to be the
  smallest remaining operator workflow. It completes bounded job execution
  history using one existing default-safe table projection; audit/failure reads
  can follow as a separate pair, while privacy, memory, and Explain require
  broader scope or handle decisions.
- `implementation_contract`: register only authenticated read-only
  `GET /governance/api/v1/activity/job-attempts` with purpose
  `governance.activity.job_attempts.read`. Reject every query string and scope
  header through the existing authenticated-unscoped boundary, call only
  `GovernanceQueryService.listJobAttempts()` with no options, and return its
  existing default projection. The route accepts no body or filter, keeps
  `result` absent, and preserves the shared default 100-row limit,
  `started_at DESC, id DESC` ordering, attempt number, nullable completion/
  heartbeat/error fields, ISO date conversion, and recursive identifier/
  classification/diagnostic redaction.
- `allowed_paths`: `docs/governance-ui.md`, `docs/architecture.md`,
  `docs/deployment.md`, `docs/security-privacy.md`, `src/index.ts`,
  `tests/integration/governance-http-wiring.test.ts`, and this checkpoint only.
- `protected_paths`: `GovernanceQueryService`, `GovernanceCLI`, their DTOs and
  tests, job/worker/action/tool/audit/failure repositories and persistence, the
  generic HTTP server and session/CSRF/scope/resource/preview registries,
  existing routes, browser assets, governance mutations, config/schema/
  migrations/dependencies/lockfile, QQ, Provider, private/live data,
  deployment/restart/container behavior, and every prior slice.
- `assertions`: unauthenticated access is `401` with zero query-service calls;
  a query string or scope header is `400` before the callback; authenticated
  access is `200` with the exact default list and ISO dates; the shared method
  is called with no arguments; raw attempt/job/worker/classification/diagnostic
  values and result payloads do not appear; repeated reads are deterministic and
  produce zero writes; integrity/FKs remain clean; the listener remains default-
  unreachable; and all existing routes/session lifecycle stay unchanged.
- `focused_commands`: fail-first then final single-route integration coverage;
  governance HTTP/application-wiring adjacency; the canonical P8 command from
  `docs/long-term-development-constraints.md`; `pnpm typecheck`; scoped ESLint;
  `pnpm release:check`; `git diff --check`; migration hashes; inventory/privacy
  hygiene; and an exact six-path private reverse/forward patch proof.
- `schema_dependency_live_boundary`: read-only route wiring plus synthetic
  tests and canonical docs only. No schema, migration, dependency, lockfile,
  persistence behavior, Provider, QQ, private DB, deployment, restart,
  container, or browser visual operation is permitted.
- `rollback_boundary`: the exact B14F candidate is captured across all six
  allowed product/test paths under the mode-`0700` private proof root
  `/tmp/lethebot-gov-ux-01b14g-roundtrip-XOMVJI`; the final patch must strictly
  reverse to that baseline and forward to the candidate byte-for-byte and
  mode-for-mode without touching the workspace during proof.
- `fail_first_evidence`: the single focused integration test collected fourteen
  tests, skipped thirteen unrelated cases, and failed because unauthenticated
  `GET /governance/api/v1/activity/job-attempts` returned `404` instead of the
  required `401`; no production source or canonical route document had been
  edited.
- `final_evidence`: the deterministic gate row above records the focused,
  full-route, application/HTTP adjacency, canonical P8, static, release,
  integrity/FK, privacy, inventory, migration, and hygiene evidence. The exact
  six-path patch has SHA-256
  `69851fcd551ab78e5c63c78af82199ff1913bd5ac83286144b959c3c5c44b2db`,
  mode `0600`, and size `12491`; its mode-`0700` proof root passed independent
  reverse-first and forward-first cycles and the candidate snapshot matches the
  workspace.
- `exact_next_action`: completed; the P8 reassessment selected the bounded
  default event-processing-failure list for `GOV-UX-01B14H` contract
  reproduction.

## Completed Slice: `GOV-UX-01B14H`

- `requirement`: `GOV-UX-01B14H` authenticated Activity event-processing-
  failure list read.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: `GovernanceQueryService.listEventProcessingFailures()` already
  owns the bounded, newest-first, details-hidden, recursively redacted default
  read over hash-only failure evidence, but the production Activity routes do
  not expose the required failure-triage workflow.
- `selection_reason`: after completing bounded job history, this is the smallest
  default-safe operational failure view. It uses one existing table projection
  without raw messages or errors; the bounded audit read can follow as its pair,
  while privacy, memory, and Explain require broader scope or handle decisions.
- `implementation_contract`: register only authenticated read-only
  `GET /governance/api/v1/activity/event-processing-failures` with purpose
  `governance.activity.event_processing_failures.read`. Reject every query
  string and scope header through the existing authenticated-unscoped boundary,
  call only `GovernanceQueryService.listEventProcessingFailures()` with no
  options, and return its existing default projection. The route accepts no body
  or filter, keeps `details` absent, and preserves the shared default 100-row
  limit, `occurred_at DESC, id DESC` ordering, nullable source/turn/conversation/
  hash fields, ISO date conversion, retained fixed hashes, and recursive
  identifier/classification/diagnostic redaction.
- `allowed_paths`: `docs/governance-ui.md`, `docs/architecture.md`,
  `docs/deployment.md`, `docs/security-privacy.md`, `src/index.ts`,
  `tests/integration/governance-http-wiring.test.ts`, and this checkpoint only.
- `protected_paths`: `GovernanceQueryService`, `GovernanceCLI`, their DTOs and
  tests, failure/audit/job/worker/action/tool repositories and persistence, the
  generic HTTP server and session/CSRF/scope/resource/preview registries,
  existing routes, browser assets, governance mutations, config/schema/
  migrations/dependencies/lockfile, QQ, Provider, private/live data,
  deployment/restart/container behavior, and every prior slice.
- `assertions`: unauthenticated access is `401` with zero query-service calls;
  a query string or scope header is `400` before the callback; authenticated
  access is `200` with the exact default list and ISO date; the shared method is
  called with no arguments; raw failure/source/turn/classification/diagnostic
  values and details do not appear while fixed hashes remain exact; repeated
  reads are deterministic and produce zero writes; integrity/FKs remain clean;
  the listener remains default-unreachable; and all existing routes/session
  lifecycle stay unchanged.
- `focused_commands`: fail-first then final single-route integration coverage;
  governance HTTP/application-wiring adjacency; the canonical P8 command from
  `docs/long-term-development-constraints.md`; `pnpm typecheck`; scoped ESLint;
  `pnpm release:check`; `git diff --check`; migration hashes; inventory/privacy
  hygiene; and an exact six-path private reverse/forward patch proof.
- `schema_dependency_live_boundary`: read-only route wiring plus synthetic
  tests and canonical docs only. No schema, migration, dependency, lockfile,
  persistence behavior, Provider, QQ, private DB, deployment, restart,
  container, or browser visual operation is permitted.
- `rollback_boundary`: the exact B14G candidate is captured across all six
  allowed product/test paths under the mode-`0700` private proof root
  `/tmp/lethebot-gov-ux-01b14h-roundtrip-J69BFH`; the final patch must strictly
  reverse to that baseline and forward to the candidate byte-for-byte and
  mode-for-mode without touching the workspace during proof.
- `fail_first_evidence`: the single focused integration test collected fifteen
  tests, skipped fourteen unrelated cases, and failed because unauthenticated
  `GET /governance/api/v1/activity/event-processing-failures` returned `404`
  instead of the required `401`; no production source or canonical route
  document had been edited.
- `final_evidence`: the deterministic gate row above records the focused,
  full-route, application/HTTP adjacency, canonical P8, static, release,
  integrity/FK, privacy, inventory, migration, and hygiene evidence. The exact
  six-path patch has SHA-256
  `f595b9d98099ad1561fce414c411755e61dd87a88fb4cc13c280604fd3259d5e`,
  mode `0600`, and size `13889`; its mode-`0700` proof root passed independent
  reverse-first and forward-first cycles and the candidate snapshot matches the
  workspace.
- `exact_next_action`: completed; the required two-slice P8 reassessment selected
  the bounded default audit list for `GOV-UX-01B14I` contract reproduction.

## Completed Slice: `GOV-UX-01B14I`

- `requirement`: `GOV-UX-01B14I` authenticated Activity audit-list read.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: `GovernanceQueryService.listAudit()` already owns the bounded,
  newest-first, details-hidden, recursively redacted default read, but the
  production Activity routes expose failure evidence without the paired audit
  workflow.
- `selection_reason`: the required two-slice reassessment found this to be the
  last default-safe Activity table projection in the audit/failure group. It
  completes bounded operational evidence without adding filters or actor scope;
  privacy, memory, and Explain workflows require broader scope or handle
  decisions.
- `implementation_contract`: register only authenticated read-only
  `GET /governance/api/v1/activity/audit` with purpose
  `governance.activity.audit.read`. Reject every query string and scope header
  through the existing authenticated-unscoped boundary, call only
  `GovernanceQueryService.listAudit()` with no options, and return its existing
  default projection. The route accepts no body or filter, keeps `details`
  absent, and preserves the shared default 100-row limit,
  `timestamp DESC, id DESC` ordering, ISO date conversion, nullable actor/risk/
  evaluator fields, default details-redacted/redacted signals, and recursive
  identifier/classification/summary redaction.
- `allowed_paths`: `docs/governance-ui.md`, `docs/architecture.md`,
  `docs/deployment.md`, `docs/security-privacy.md`, `src/index.ts`,
  `tests/integration/governance-http-wiring.test.ts`, and this checkpoint only.
- `protected_paths`: `GovernanceQueryService`, `GovernanceCLI`, their DTOs and
  tests, audit/failure/job/worker/action/tool repositories and persistence, the
  generic HTTP server and session/CSRF/scope/resource/preview registries,
  existing routes, browser assets, governance mutations, config/schema/
  migrations/dependencies/lockfile, QQ, Provider, private/live data,
  deployment/restart/container behavior, and every prior slice.
- `assertions`: unauthenticated access is `401` with zero query-service calls;
  a query string or scope header is `400` before the callback; authenticated
  access is `200` with the exact default list and ISO date; the shared method is
  called with no arguments; raw event/actor/classification/summary/detail values
  do not appear and details remain absent; repeated reads are deterministic and
  produce zero writes; integrity/FKs remain clean; the listener remains default-
  unreachable; and all existing routes/session lifecycle stay unchanged.
- `focused_commands`: fail-first then final single-route integration coverage;
  governance HTTP/application-wiring adjacency; the canonical P8 command from
  `docs/long-term-development-constraints.md`; `pnpm typecheck`; scoped ESLint;
  `pnpm release:check`; `git diff --check`; migration hashes; inventory/privacy
  hygiene; and an exact six-path private reverse/forward patch proof.
- `schema_dependency_live_boundary`: read-only route wiring plus synthetic
  tests and canonical docs only. No schema, migration, dependency, lockfile,
  persistence behavior, Provider, QQ, private DB, deployment, restart,
  container, or browser visual operation is permitted.
- `rollback_boundary`: the exact B14H baseline and B14I candidate are captured
  across all six allowed product/test paths under the mode-`0700` private proof
  root `/tmp/lethebot-gov-ux-01b14i-roundtrip-gnPVfv`; strict reverse-first and
  forward-first cycles restored both snapshots byte-for-byte and mode-for-mode
  without touching the workspace.
- `fail_first_evidence`: the single focused integration test collected sixteen
  tests, skipped fifteen unrelated cases, and failed because unauthenticated
  `GET /governance/api/v1/activity/audit` returned `404` instead of the required
  `401`; no production source or canonical route document had been edited.
- `final_evidence`: the deterministic gate row above records the focused 1 / 1,
  full-route 16 / 16, four-file adjacency 42 / 42, canonical P8 295 / 295,
  typecheck, scoped ESLint, and release 2692 / 10 skipped test results. The
  six-path patch has SHA-256
  `d0cb7c265a4bbabf80df2d9d449325688bb57ab9efe4a4e7029377bd5b9b621a`,
  mode `0600`, and size `12981`; its mode-`0700` proof root passed independent
  reverse-first and forward-first cycles and the candidate snapshot matches the
  workspace.
- `exact_next_action`: completed; the default-safe authenticated-unscoped
  Activity API inventory is complete. The post-B14I architecture reassessment
  selects a purpose-specific scope-catalog/handle characterization before any
  Privacy, Memory, or Explain route is exposed.

## Completed Slice: `GOV-UX-01B15A`

- `requirement`: `GOV-UX-01B15A` purpose-specific Privacy user-scope catalog
  and handle-issuance projection, HTTP-disabled.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: the only production scope catalog is proposal-backed and
  issues handles bound to `memory.maintenance.review`; the shared bounded
  privacy-preference read accepts raw user filters, so exposing it through the
  current catalog would either omit users without maintenance proposals or
  violate the cross-purpose handle boundary.
- `selection_reason`: Privacy is the smallest remaining scoped workflow because
  its legitimate scope is exactly one canonical user. A catalog over
  `canonical_users` can include users with no current preference row, remain
  bounded and identifier-free, and establish the handle authority needed for a
  later read and mutation workflow without changing either route now.
- `implementation_contract`: add only
  `GovernanceQueryService.listPrivacyPreferenceScopeHandles(issuer)`. Read at
  most 101 canonical users in stable `last_seen_at DESC, id ASC` order, return
  at most 100 entries with explicit truncation, and pass each exact
  `{ kind: 'user', canonicalUserId }` scope only to the trusted issuer. Return
  only a stable 16-character display fingerprint, `scopeKind: 'user'`, fixed
  `User privacy` label, the issuer's 43-character handle, and numeric expiry.
  Reuse the existing logical-scope fingerprint domain so a later persistent
  selector can correlate the same user scope across purpose-specific catalogs;
  the fingerprint grants no authority. The issued handle remains bound by the
  registry to the future `governance.privacy.preferences.read` purpose and must
  not resolve as `memory.maintenance.review`. Register no HTTP route.
- `allowed_paths`: `src/governance/query-service.ts`,
  `tests/unit/governance/query-service.test.ts`, `docs/governance-ui.md`, and
  this checkpoint only.
- `protected_paths`: `GovernanceScopeHandleRegistry`, generic HTTP/session/
  CSRF/scope/resource/preview dispatch, application wiring and existing routes,
  privacy repository/schema/writes/audits/enforcement, maintenance-review
  queries/handles/routes/mutations, CLI/QQ/browser code, config/schema/
  migrations/dependencies/lockfile, Provider, private/live data, deployment,
  restart, container, persistence format, and every prior slice.
- `assertions`: the catalog includes canonical users with and without privacy or
  maintenance rows; duplicate domain evidence cannot duplicate a scope; order
  and 100/101 truncation are stable; issuer calls receive only exact user
  scopes; the DTO contains no raw canonical/platform/session/secret value or
  issuer extras; repeated issuance through the stable registry is deterministic;
  privacy handles resolve only for the exact session and privacy purpose and
  fail for maintenance purpose; reads produce zero writes; integrity/FKs stay
  clean; and no HTTP route becomes available.
- `focused_commands`: fail-first then final single-test query-service coverage;
  full query-service and scope-registry adjacency; the canonical P8 command;
  `pnpm typecheck`; scoped ESLint; `pnpm release:check`; `git diff --check`;
  migration hashes; inventory/privacy hygiene; and exact three-path private
  reverse/forward patch proof.
- `schema_dependency_live_boundary`: shared read projection, synthetic tests,
  and canonical contract only. No schema, migration, dependency, lockfile,
  route, persistence write, Provider, QQ, private DB, deployment, restart,
  container, or browser operation is permitted.
- `rollback_boundary`: the exact B14I baseline and B15A candidate for all three
  product/test paths are privately captured under mode-`0700`
  `/tmp/lethebot-gov-ux-01b15a-roundtrip-IqRoSX`; strict reverse-first and
  forward-first cycles restored both snapshots byte-for-byte and mode-for-mode
  without touching the workspace.
- `fail_first_evidence`: the focused query-service run collected 30 tests,
  skipped 29 unrelated cases, and failed only because
  `service.listPrivacyPreferenceScopeHandles` was not a function; no production
  source or canonical behavior document had been edited.
- `final_evidence`: the deterministic gate row above records the focused 1 / 1,
  query-service/scope-registry 33 / 33, canonical P8 280 / 280, typecheck,
  scoped ESLint, and release 2693 / 10 skipped test results. The three-path
  patch has SHA-256
  `37757207d4ee7f227e08276ea466c207a534ac2041f4a5f8e87f9bb8a19f01e6`,
  mode `0600`, and size `11486`; its mode-`0700` proof root and trees passed
  independent reverse-first and forward-first cycles and the candidate snapshot
  matches the workspace.
- `exact_next_action`: completed; select an exact-user, HTTP-safe Privacy
  preference list projection that strips owner identifiers before the catalog
  or data route is exposed.

## Completed Slice: `GOV-UX-01B15B`

- `requirement`: `GOV-UX-01B15B` exact-user HTTP-safe Privacy preference list
  projection, HTTP-disabled.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: `GovernanceQueryService.listPrivacyPreferences` preserves the
  CLI inspection DTO, including canonical user and updater user fields, and
  accepts caller-provided filters. Passing that result directly to HTTP would
  expose owner identifiers and would not make the resolved scope authoritative.
- `selection_reason`: B15A now provides exact user scopes and privacy-purpose
  handles, but the data route must remain absent until its shared service method
  accepts only the resolved scope, applies it before the bound, and returns a
  fixed owner-identifier-free projection. This is the smallest independent
  prerequisite before either Privacy route is wired.
- `implementation_contract`: add only
  `GovernanceQueryService.listPrivacyPreferencesForScope(scope)`. A non-user
  scope returns an empty fixed page without calling the legacy read. An exact
  user scope calls the existing `listPrivacyPreferences` with only that raw
  canonical user ID and a 101-row truncation probe, preserves its stable order,
  slices to 100, and returns explicit `truncated`. Each entry contains only
  redacted preference type, state, optional reason, optional updater actor class
  and context, and created/updated dates. Canonical user and updater user IDs are
  absent. Register no HTTP route and change no CLI DTO or privacy behavior.
- `allowed_paths`: `src/governance/query-service.ts`,
  `tests/unit/governance/query-service.test.ts`, `docs/governance-ui.md`, and
  this checkpoint only.
- `protected_paths`: B15A catalog and fingerprint semantics,
  `GovernanceScopeHandleRegistry`, generic HTTP/session/CSRF/scope/resource/
  preview dispatch, application wiring and existing routes, privacy repository/
  schema/writes/audits/enforcement, maintenance-review queries/handles/routes/
  mutations, CLI/QQ/browser code, config/schema/migrations/dependencies/
  lockfile, Provider, private/live data, deployment, restart, container,
  persistence format, and every prior slice.
- `assertions`: exact user scope is passed to the existing read as
  `{ canonicalUserId, limit: 101 }`; other scopes make zero legacy-read calls;
  order and dates survive; 100/101 truncation is explicit; preference/reason/
  updater classification redaction survives; canonical user, updater user,
  platform, secret, and unrestricted fields are absent from the DTO; empty user
  scope is a fixed empty page; repeated reads make zero writes; integrity/FKs
  stay clean; the B15A catalog is unchanged; and no HTTP route becomes available.
- `focused_commands`: fail-first then final single-test query-service coverage;
  full query-service and privacy repository adjacency; the canonical P8 command;
  `pnpm typecheck`; scoped ESLint; `pnpm release:check`; `git diff --check`;
  migration hashes; inventory/privacy hygiene; and exact three-path private
  reverse/forward patch proof.
- `schema_dependency_live_boundary`: shared read projection, synthetic tests,
  and canonical contract only. No schema, migration, dependency, lockfile,
  route, persistence write, Provider, QQ, private DB, deployment, restart,
  container, or browser operation is permitted.
- `rollback_boundary`: the exact B15A baseline and B15B candidate for all three
  product/test paths are privately captured under mode-`0700`
  `/tmp/lethebot-gov-ux-01b15b-roundtrip-fJX3C8`; strict reverse-first and
  forward-first cycles restored both snapshots byte-for-byte and mode-for-mode
  without touching the workspace.
- `fail_first_evidence`: the focused query-service run collected 31 tests,
  skipped 30 unrelated cases, and failed only because
  `service.listPrivacyPreferencesForScope` was not a function; no production
  source or canonical behavior document had been edited.
- `final_evidence`: the deterministic gate row above records the focused 1 / 1,
  query-service/privacy-repository 38 / 38, canonical P8 281 / 281, typecheck,
  scoped ESLint, and release 2694 / 10 skipped test results. The three-path
  patch has SHA-256
  `366f985f57d0f454604d0b828fb8a367c8dd37a7998839038d87db01049f3ab9`,
  mode `0600`, and size `11350`; its mode-`0700` proof root and trees passed
  independent reverse-first and forward-first cycles and the candidate snapshot
  matches the workspace.
- `exact_next_action`: completed; the required two-slice Privacy reassessment
  selects a separate authenticated-unscoped `GET /privacy/scopes` discovery
  route so the existing maintenance-review `/scopes` contract remains exact.

## Completed Slice: `GOV-UX-01B15C`

- `requirement`: `GOV-UX-01B15C` authenticated purpose-specific Privacy scope
  discovery route.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: B15A provides the bounded exact-user catalog and privacy-
  purpose issuer projection, but no authenticated route can obtain those
  handles. The existing `/scopes` route is intentionally proposal-backed and
  bound only to maintenance review.
- `selection_reason`: the required two-slice Privacy reassessment found that a
  distinct static discovery route preserves the exact legacy `/scopes` DTO and
  avoids caller-selected purpose input. It is the smallest route slice before
  the B15B data page can be exposed separately.
- `implementation_contract`: register only authenticated read-only
  `GET /governance/api/v1/privacy/scopes` through the existing unscoped boundary
  with route purpose `governance.privacy.preferences.scopes`. Reject every query
  string and scope header before the callback. Call only B15A
  `listPrivacyPreferenceScopeHandles`, and bind its trusted issuer to the current
  session digest/expiry, exact user scope, and
  `governance.privacy.preferences.read`. Return the existing fixed catalog; do
  not register `/privacy/preferences`, change `/scopes`, or add any mutation.
- `allowed_paths`: `src/index.ts`,
  `tests/integration/governance-http-wiring.test.ts`, `docs/architecture.md`,
  `docs/deployment.md`, `docs/governance-ui.md`, `docs/security-privacy.md`, and
  this checkpoint only.
- `protected_paths`: `GovernanceQueryService` and B15A/B15B DTO semantics,
  `GovernanceScopeHandleRegistry`, generic HTTP/session/CSRF/scope/resource/
  preview dispatch, existing routes and maintenance catalog, privacy data route,
  repository/schema/write/audit/enforcement behavior, governance mutations,
  CLI/QQ/browser code, config/schema/migrations/dependencies/lockfile, Provider,
  private/live data, deployment, restart, container, persistence format, and
  every prior slice.
- `assertions`: unauthenticated access is `401` with zero query/issuer calls; a
  query or scope header is `400` before the callback; authenticated access is
  `200` with the exact B15A fixed catalog; issuer input is the current defensive
  session binding, exact user scope, and only the privacy-read purpose; repeated
  reads return the stable handle and make zero writes; the response omits raw
  canonical/platform/session/secret values and issuer extras; the privacy handle
  cannot authorize `/memory-reviews`; `/scopes` remains proposal-only;
  `/privacy/preferences` remains `404`; integrity/FKs remain clean; the listener
  remains default-unreachable; and existing routes/session lifecycle stay exact.
- `focused_commands`: fail-first then final single-route integration coverage;
  full governance wiring plus HTTP/application adjacency; the canonical P8
  command; `pnpm typecheck`; scoped ESLint; `pnpm release:check`;
  `git diff --check`; migration hashes; inventory/privacy hygiene; and exact
  six-path private reverse/forward patch proof.
- `schema_dependency_live_boundary`: read-only route wiring, synthetic tests,
  and canonical docs only. No schema, migration, dependency, lockfile,
  persistence write, privacy data route, Provider, QQ, private DB, deployment,
  restart, container, or browser operation is permitted.
- `rollback_boundary`: the exact B15B baseline and B15C candidate for all six
  product/test paths are privately captured under mode-`0700`
  `/tmp/lethebot-gov-ux-01b15c-roundtrip-kjwDIn`; strict reverse-first and
  forward-first cycles restored both snapshots byte-for-byte and mode-for-mode
  without touching the workspace.
- `fail_first_evidence`: the focused integration run collected 17 tests,
  skipped 16 unrelated cases, and failed because unauthenticated
  `GET /governance/api/v1/privacy/scopes` returned `404` instead of the required
  `401`; no production source or canonical route document had been edited.
- `final_evidence`: the focused final regression passed 1 / 1 with 16 skipped,
  the full governance wiring file passed 17 / 17, query/service/HTTP/scope-
  registry adjacency passed 4 files / 66 tests, and the canonical P8 gate passed
  281 / 281. Typecheck, scoped ESLint, and release passed; release recorded 132
  / 1 skipped files and 2695 / 10 skipped tests. The route calls only B15A,
  binds issued handles to the current session and privacy-read purpose, preserves
  maintenance discovery and cross-purpose isolation, keeps the data route
  absent, produces zero DB writes, and retains clean integrity/FKs. The six-path
  patch has SHA-256
  `f47126730bf06698635f1a12011fb7015e328089df946a2ae6f812348ae69902`,
  mode `0600`, and size `15215`; its mode-`0700` proof root and trees passed
  independent reverse-first and forward-first cycles and the candidate snapshot
  matches the workspace.
- `exact_next_action`: completed; expose the already-proved B15B page through a
  separate exact-scope read-only `/privacy/preferences` route using only the
  B15C privacy-purpose handle.

## Completed Slice: `GOV-UX-01B15D`

- `requirement`: `GOV-UX-01B15D` exact-scope read-only Privacy preference route.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: B15C issues session- and purpose-bound exact-user handles and
  B15B owns the fixed owner-identifier-free page, but
  `GET /governance/api/v1/privacy/preferences` is still absent, so the complete
  read workflow cannot reach the shared page.
- `selection_reason`: discovery and the scoped projection are now independently
  proved. Registering one existing static authorized route is the smallest next
  dependency-ordered slice and adds no query, mutation, repository, or generic
  HTTP behavior.
- `implementation_contract`: register only read-only
  `GET /governance/api/v1/privacy/preferences` in the existing exact-scope route
  class with purpose `governance.privacy.preferences.read`. Let the shared
  boundary authenticate, reject queries, require and resolve exactly one opaque
  handle against the current session and purpose, and pass only the resolved
  scope to the callback. Call only B15B
  `listPrivacyPreferencesForScope(scope)` and return its fixed page. Do not add
  a mutation, accept raw selectors, change discovery, or widen maintenance
  routes or handles.
- `allowed_paths`: `src/index.ts`,
  `tests/integration/governance-http-wiring.test.ts`, `docs/architecture.md`,
  `docs/deployment.md`, `docs/governance-ui.md`, `docs/security-privacy.md`, and
  this checkpoint only.
- `protected_paths`: B15A/B15B/B15C query-service, catalog, DTO, and issuer
  semantics; `GovernanceHttpServer`; `GovernanceScopeHandleRegistry`; generic
  session/CSRF/scope/resource/preview dispatch; maintenance scope/review routes
  and mutations; privacy repository/schema/write/audit/enforcement; CLI/QQ/
  browser code; config/schema/migrations/dependencies/lockfile; Provider,
  private/live data, deployment, restart, container, persistence format, and
  every prior slice.
- `assertions`: unauthenticated access is `401`; missing or malformed scope is
  `400`; a query is `400`; unknown, cross-session, or cross-purpose authority is
  `404`; all rejection paths make zero B15B calls. A B15C handle returns `200`
  and passes only its resolved exact user scope to B15B. The page preserves the
  fixed 100/101 bound, order, ISO dates, redacted reason/updater classification,
  and explicit truncation while omitting canonical owner/updater, platform,
  session, secret, and unrestricted fields. Repeated reads are deterministic
  and write-free; integrity/FKs stay clean; discovery and maintenance behavior
  remain exact; no Privacy mutation appears; and the listener remains default-
  unreachable.
- `focused_commands`: fail-first then final single-route integration coverage;
  full governance wiring plus query/service/HTTP/scope-registry adjacency; the
  canonical P8 command; `pnpm typecheck`; scoped ESLint; `pnpm release:check`;
  `git diff --check`; migration hashes; inventory/privacy hygiene; and exact
  six-path private reverse/forward patch proof.
- `schema_dependency_live_boundary`: read-only route wiring, synthetic tests,
  and canonical docs only. No schema, migration, dependency, lockfile,
  persistence write, privacy mutation, Provider, QQ, private DB, deployment,
  restart, container, or browser operation is permitted.
- `rollback_boundary`: the exact B15C baseline and B15D candidate for all six
  product/test paths are privately captured under mode-`0700`
  `/tmp/lethebot-gov-ux-01b15d-roundtrip-kcIF4D`; strict reverse-first and
  forward-first cycles restored both snapshots byte-for-byte and mode-for-mode
  without touching the workspace.
- `fail_first_evidence`: the focused integration run collected 18 tests,
  skipped 17 unrelated cases, and failed because unauthenticated
  `GET /governance/api/v1/privacy/preferences` returned `404` instead of the
  required `401`; no production source or canonical route document had been
  edited.
- `final_evidence`: the focused final regression passed 1 / 1 with 17 skipped,
  full query/service/HTTP/scope-registry adjacency passed 4 files / 67 tests,
  and the canonical P8 gate passed 281 / 281. Typecheck, scoped ESLint, and
  release passed; release recorded 132 / 1 skipped files and 2696 / 10 skipped
  tests. The shared boundary resolves only the current-session Privacy-purpose
  handle, the callback calls only B15B with the exact resolved user scope, and
  the fixed page preserves bounds/order/dates/redaction while removing owner and
  updater identifiers. Rejections precede B15B, repeated reads are byte-stable
  and write-free, integrity/FKs remain clean, and discovery, maintenance, and
  mutation boundaries remain exact. The six-path patch has SHA-256
  `0e80c7382419e2f2b8e640e5acf37bdadb2a64494bd4c32094394195d671ace6`,
  mode `0600`, and size `17155`; its mode-`0700` proof root and trees passed
  independent reverse-first and forward-first cycles and the candidate snapshot
  matches the workspace.
- `exact_next_action`: completed; reassess the full P8 critical path after the
  B15C/B15D discovery-and-page pair, inventory remaining HTTP-disabled shared
  Memory, Explain, Privacy mutation, and Operations workflows, and select the
  smallest dependency-ready slice without widening authority.

## Completed Slice: `GOV-UX-01B16A`

- `requirement`: `GOV-UX-01B16A` HTTP-disabled Memory-record exact-scope
  catalog.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: B12C owns the shared database-backed memory-record list, but
  its future exact-scope HTTP workflow has no record-backed selector. The
  existing maintenance catalog is proposal-backed, so a scope containing
  memory records but no maintenance proposal is undiscoverable and cannot be
  safely reused as Memory authority.
- `selection_reason`: the post-B15C/B15D critical-path reassessment found no
  remaining route that is directly dependency-ready. Memory and Explain need
  purpose-specific exact-scope discovery, Privacy mutations need shared typed
  preview/confirm ownership, and Operations needs a coordinator over existing
  maintenance modules. A read-only, HTTP-disabled Memory catalog is the
  smallest enabling slice and changes no route or mutation authority.
- `implementation_contract`: add a fixed maximum-100
  `listMemoryRecordScopeHandles(issueHandle)` projection to
  `GovernanceQueryService`. Read distinct exact scopes represented by
  `memory_records` across every lifecycle state; normalize, validate,
  deduplicate, and stably order them before a 101-row truncation probe. Support
  global, user, group, private/group conversation, and system scopes; omit tool
  records because the durable schema has no exact tool-name selector. Reject
  blank, non-text, whitespace-padded, or over-256-character legacy identifiers.
  Reuse the `memory-maintenance-scope` fingerprint domain so one logical scope
  has the same persistent display fingerprint across views. Pass raw exact
  scope only to the trusted issuer for the future
  `governance.memory.records.read` purpose, and return only fingerprint, scope
  kind, optional conversation type, fixed label, opaque handle, expiry, and an
  explicit truncation flag. Register no HTTP route.
- `allowed_paths`: `src/governance/query-service.ts`,
  `tests/unit/governance/query-service.test.ts`, `docs/governance-ui.md`, and
  this checkpoint only.
- `protected_paths`: B12C memory-list behavior and CLI delegation; maintenance
  and Privacy catalog/handle semantics; all HTTP/application/session/CSRF/
  scope/resource/preview code; repository/schema/migration/dependency/lockfile;
  governance mutations; CLI/QQ/browser code; Provider, private/live data,
  deployment, restart, container, and every prior slice.
- `assertions`: synthetic memory records with no maintenance proposals cover
  every supported exact scope, duplicates, multiple lifecycle states, tool
  rows, and malformed legacy identifiers. Normalization and deduplication occur
  before the stable scope-rank/private-before-group-conversation/raw-identifier
  order and 100/101 bound. The real scope registry resolves each handle only
  for its issuing session and `governance.memory.records.read`, while another
  session and maintenance or Privacy purposes fail. Repeated issuance is
  stable; DTOs contain only the fixed fields and no raw canonical, group,
  conversation, session, secret, or issuer-extra values. Reads produce zero
  writes and retain clean integrity/FKs; no HTTP route or mutation appears.
- `focused_commands`: fail-first then final focused query-service regression;
  full query-service and scope-registry adjacency; the canonical P8 command;
  `pnpm typecheck`; scoped ESLint; `pnpm release:check`; `git diff --check`;
  migration hashes; inventory/privacy hygiene; and exact three-path private
  reverse/forward patch proof.
- `schema_dependency_live_boundary`: read-only query-service projection,
  synthetic tests, canonical contract, and checkpoint only. No HTTP route,
  persistence write, repository behavior, schema, migration, dependency,
  lockfile, governance mutation, Provider, QQ, private DB, deployment, restart,
  container, or browser operation is permitted.
- `rollback_boundary`: the exact B15D baseline for all three product/test paths
  is privately captured under mode-`0700`
  `/tmp/lethebot-gov-ux-01b16a-roundtrip-Zc6yXp`; its strict reverse-first and
  forward-first cycles restored baseline and candidate byte-for-byte and
  mode-for-mode without touching the workspace.
- `fail_first_evidence`: the focused run collected 33 tests, skipped 31
  unrelated cases, and both new cases failed only because
  `GovernanceQueryService.listMemoryRecordScopeHandles` was absent; no
  production source or canonical contract had been edited.
- `final_evidence`: the two focused regressions passed with 31 unrelated tests
  skipped; query-service/scope-registry adjacency passed 36 / 36, and the
  canonical P8 gate passed 283 / 283. Typecheck, scoped ESLint, and the final
  release gate passed; release recorded 132 / 1 skipped files and 2698 / 10
  skipped tests. Synthetic records with no maintenance proposals proved every
  supported scope and lifecycle state, duplicate normalization, stable order,
  100/101 truncation, tool omission, malformed/non-text/control/full-trim-set/
  overlong identifier filtering, real registry purpose/session isolation,
  stable handles and fingerprints, fixed identifier-free DTOs, zero query
  writes, and clean integrity/FKs. The exact three-path patch is
  `/tmp/lethebot-gov-ux-01b16a-roundtrip-Zc6yXp/gov-ux-01b16a.patch`
  (`sha256:cbc3974cc9892d19c9e539cc33fe558c5619d7d9382790e591fd50119fbb74d2`,
  mode `0600`, size `23814`); its proof root and all proof trees are mode
  `0700`, both application orders passed byte/mode comparison, and the candidate
  snapshot matches the workspace. Inventory remains 68 tracked plus 49
  untracked paths with zero staged; `git diff --check` is clean, the patch is
  ASCII-only, sensitive untracked filenames are absent, and migrations 007/008
  retain SHA-256
  `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`/
  `95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`.
- `exact_next_action`: completed; bind the catalog to the existing
  authenticated-unscoped boundary through a separate purpose-specific
  `/memory/scopes` route while leaving Memory data routes absent.

## Completed Slice: `GOV-UX-01B16B`

- `requirement`: `GOV-UX-01B16B` purpose-specific Memory-record scope discovery
  route.
- `status`: `DETERMINISTIC_READY`.
- `observed_gap`: B16A proves a bounded record-backed exact-scope catalog and
  future Memory-read handles, but no authenticated route invokes it. The legacy
  `/scopes` route remains proposal-backed and maintenance-purpose only, so it
  cannot safely grant Memory-record read authority.
- `selection_reason`: B16A makes one static authenticated-unscoped route
  directly dependency-ready. Exposing only discovery is smaller than designing
  the fixed Memory page, preserves purpose separation, and adds no query,
  mutation, repository, or generic HTTP behavior.
- `implementation_contract`: register only read-only
  `GET /governance/api/v1/memory/scopes` in the existing authenticated-unscoped
  route class with discovery purpose `governance.memory.records.scopes`. Reject
  query strings and scope headers before invocation. Call only
  `GovernanceQueryService.listMemoryRecordScopeHandles`; its trusted issuer must
  bind each exact scope to the current session digest and absolute expiry with
  purpose `governance.memory.records.read`. Return the fixed B16A catalog
  without issuer extras or raw scope/session values. Do not register a Memory
  data/detail route, accept selectors, or widen maintenance or Privacy handles.
- `allowed_paths`: `src/index.ts`,
  `tests/integration/governance-http-wiring.test.ts`, `docs/architecture.md`,
  `docs/deployment.md`, `docs/governance-ui.md`, `docs/security-privacy.md`, and
  this checkpoint only.
- `protected_paths`: B16A/B12C query-service, catalog, list, DTO, and issuer
  semantics; `GovernanceHttpServer`; generic session/CSRF/scope/resource/
  preview dispatch; maintenance and Privacy discovery/data/mutation behavior;
  CLI/QQ/browser code; repository/schema/migrations/dependencies/lockfile;
  Provider, private/live data, deployment, restart, container, persistence
  format, and every prior slice.
- `assertions`: unauthenticated access is `401`; a query or scope header is
  `400`; all rejections make zero B16A calls. An authenticated request calls
  only B16A, returns its fixed order/bound/truncation/labels/fingerprints, and
  binds every handle to the current session, expiry, exact scope, and Memory-
  read purpose. Repeated discovery is stable and write-free. Issued handles fail
  for another session and maintenance or Privacy purposes; legacy `/scopes` and
  `/privacy/scopes` remain isolated; `/memory/records` stays absent; integrity/
  FKs remain clean; and the listener stays default-unreachable.
- `focused_commands`: fail-first then final single-route integration coverage;
  full governance wiring plus query/service/HTTP/scope-registry adjacency; the
  canonical P8 command; `pnpm typecheck`; scoped ESLint; `pnpm release:check`;
  `git diff --check`; migration hashes; inventory/privacy hygiene; and exact
  six-path private reverse/forward patch proof.
- `schema_dependency_live_boundary`: read-only route wiring, synthetic tests,
  and canonical docs only. No query-service or repository behavior,
  persistence write, Memory data route, schema, migration, dependency,
  lockfile, governance mutation, Provider, QQ, private DB, deployment, restart,
  container, or browser operation is permitted.
- `rollback_boundary`: the exact B16A baseline for all six product/test paths is
  privately captured under mode-`0700`
  `/tmp/lethebot-gov-ux-01b16b-roundtrip-QhD7J7`; after implementation its patch
  must independently prove reverse-first and forward-first restoration byte-
  for-byte and mode-for-mode without touching the workspace.
- `fail_first_evidence`: the focused integration run collected 19 tests,
  skipped 18 unrelated cases, and failed because unauthenticated
  `GET /governance/api/v1/memory/scopes` returned `404` instead of the required
  `401`; neither B16A nor the scope issuer ran, and no production source or
  canonical route document had been edited.
- `final_evidence`: the focused final regression passed 1 / 1 with 18 skipped,
  full governance HTTP wiring passed 19 / 19, query/service/HTTP/scope-registry
  adjacency passed 4 files / 70 tests, and the canonical P8 gate passed 283 /
  283. Typecheck, scoped ESLint, and the release gate passed; release recorded
  132 / 1 skipped files and 2699 / 10 skipped tests. The authenticated-unscoped
  boundary rejects unauthenticated access, queries, and scope headers before
  B16A runs. Successful reads call only B16A, issue exact-scope handles with the
  current session digest/expiry and `governance.memory.records.read`, return
  fixed identifier-free catalog entries, and are byte-stable and write-free.
  Maintenance and Privacy routes reject those handles, the legacy catalogs stay
  isolated, `/memory/records` remains absent, and integrity/FKs stay clean. The
  exact six-path patch is
  `/tmp/lethebot-gov-ux-01b16b-roundtrip-QhD7J7/gov-ux-01b16b.patch`
  (`sha256:52816192dd67ddf28ee3af46fe6cad9936da4bf11f252925fad80f2e1193fb3c`,
  mode `0600`, size `17336`); its proof root and all proof trees are mode
  `0700`, independent reverse-first and forward-first cycles restored baseline
  and candidate bytes/modes, and the candidate snapshot matches the workspace.
  Inventory remains 68 tracked plus 49 untracked paths with zero staged;
  `git diff --check` is clean, the patch is ASCII-only, sensitive untracked
  filenames are absent, and migrations 007/008 retain SHA-256
  `63cef549525b93e3c12603c2691fb7dc62cd614e777e0b92ee5b8fa2c06f3b4b`/
  `95853b66ef1e8af0976023ec61b78bdbc8cf081ee167d5dab52d92de9e1f500f`.
- `exact_next_action`: completed; reassessment after the B16A/B16B catalog-and-
  route pair selects `GOV-UX-01B16C`, an HTTP-disabled fixed exact-scope Memory-
  record page. Add a separate bounded identifier-free query-service projection;
  keep the resolved scope authoritative, keep raw selectors and HTTP routes
  absent, and do not expose B12C `listMemory`, which returns raw
  `MemoryRecord[]` fields.

## Next-Stage Gap Matrix

| ID | Status | Current evidence and planned phase |
|---|---|---|
| `SEC-INGRESS-01` | `DETERMINISTIC_READY` | Default bind is loopback; reverse HTTP is exposed only by HTTP transport or strict explicit enable; enabled non-loopback ingress requires a token; WS-default `404`, zero effects, and conditional endpoint logging are covered. |
| `SEC-BODY-01` | `DETERMINISTIC_READY` | Declared and streamed bytes are capped at 262144 by default, body receive time at 5000ms, missing auth candidates reject before buffering, HMAC remains bounded, and limit/timeout/abort paths prove one terminal response with zero effects. |
| `PRIV-LOG-01` | `DETERMINISTIC_READY` | Reverse HTTP logs contain bounded transport/disposition/status/byte metadata only; logger hooks cover standalone and nested 5-12 digit identifiers; listener coordinates preserve trusted numeric ports structurally. |
| `WORK-FAIR-01` | `DETERMINISTIC_READY` | A dedicated worker claims exactly `attention_recheck`; maintenance excludes it while retaining terminal handling for unknown legacy types. Distinct identities, concurrent lanes, filtered lease/retry behavior, and two-lane shutdown drain are deterministic; live behavior remains unproved. |
| `PI-CONC-01` | `IN_PROGRESS` | All P2 deterministic assertions pass: isolated active Agent sessions, full-workflow per-conversation FIFO, bounded global/queue admission, ingress-absolute deadlines, durable overload/queue-timeout outcomes, Provider suppression, and shutdown drain. Only reviewed live behavior remains unproved. |
| `PI-OBS-01` | `IN_PROGRESS` | `PI-OBS-01A/01B/01C/01D` are deterministic-ready: schema v7 and production runtime record each source/turn-owned Provider round with nullable typed usage, terminal recovery, reconciled turn totals, bounded usage/status consumers, and queue-wait/Provider/total-turn latency aggregates. Fresh live Provider evidence remains open. |
| `LIVE-REL-01` | `DETERMINISTIC_READY` | R1-R8 deterministic gates are green; recorded live runtime predates the fixes. P4 remains fresh-authority live work. |
| `MEM-MAINT-01` | `DETERMINISTIC_READY` | `MEM-MAINT-01A/01B1/01B2/01C1/01C2/01C3` prove stable normalized proposals, exact-scope review, exact approved apply, and exact applied rollback for conflict/consolidation/decay. The full chain is atomic, append-only, retry/reopen safe, stale/race safe, immediately retrieval-correct, and FK-clean; scanners remain proposal-only. P5 is `PHASE_COMPLETE`. |
| `TOOL-PROD-01` | `DETERMINISTIC_READY_LIVE_UNPROVED` | Six default production tools now include bounded private owner/admin `runtime.status` and `runtime.tools`; explicit workspace and exact-origin configuration produce exact eight/seven/nine catalogs with only reviewed tools. Permission, evaluator, path/network, timeout, output, redaction, audit, effect/retry, and rollback boundaries are deterministic; live tool proof remains for authorized P4/P9. |
| `ARCH-APP-01` | `PHASE_COMPLETE` | `ARCH-APP-01A1/01A2A/01A2B/01B1/01B2A/01B2B/01C` extracted HTTP, ingress recovery/claim, admission/failure lifecycle, the complete conversation pipeline, and the complete background runtime. The 01D audit classifies the remaining root as construction, lifecycle, boundary adaptation, diagnostics, and test access; there is no domain SQL, turn/worker pipeline, or duplicate production path. |
| `GOV-UX-01` | `DETERMINISTIC_READY_LIVE_UNPROVED` | Every currently registered browser-safe governance HTTP contract now has a local same-origin surface: Overview; nine Activity views; Memory Records/Review with approval, rejection, apply, rollback, expiration, forget, and restore; Explain; Privacy; Group-summary; Display-profile redaction; platform-account unlink; verified backup; and stopped-service restore-handoff preparation. All mutation flows use strict bounded projections and independent single-use in-memory authority. Chromium covers exact requests, duplicate suppression, authority secrecy, 1440/390/375 layout, keyboard focus, ARIA references, 44px controls, WCAG AA semantic color pairs, reduced motion, synthetic screenshot capture, same-origin resources, and no horizontal overflow/runtime errors. Retention remains an explicit backend prerequisite because no complete policy/snapshot/audit/preview-confirm contract exists. Live Provider/QQ, deployment/restart, restore execution, soak, production screenshots, and live accessibility sign-off remain unproved. |
| `OPS-SOAK-01` | `UNVERIFIED` | deterministic release/rollback tooling exists; the next candidate has no fresh 72-hour live soak. P9 owns current-release proof. |

## Current Usability Verdict

| Surface | Verdict | Evidence boundary |
|---|---|---|
| OneBot transport, raw/chat persistence, delivery | `OPERATIONAL` | Last-hour ingress was fully claimed/persisted; 58 successful replies had platform IDs and matching bot rows; no send failure. |
| Private direct chat and governed recall | `LIMITED_USABLE` | Earlier controlled private reply/proposal/approval/recall succeeded with correct private scope, and fresh-app restart recall is deterministic. The fresh audit window had no private messages; live restart/next-day recall remains unproved. |
| Group direct mention | `UNRELIABLE` | All 65 mentions created turns, but only 57 delivered; seven failed at evaluator parsing and one was policy-rejected silent. |
| Native reply to bot | `UNRELIABLE` | Gateway quote normalization worked, but only 8 of 12 reply-to-bot turns delivered; four failed evaluator parsing and quote relation was absent from Pi context. |
| Multi-person conversational attribution | `DETERMINISTIC_READY_LIVE_UNPROVED` | Opaque speakers, selected participants, explicit current input, and bounded same-conversation quotes pass deterministic gates; the last live runtime audit predates those fixes. |
| Group/cross-session memory | `NOT_AVAILABLE` live | The live runtime had no active group/conversation/global memory, summary jobs, or selected group memory. Exact-group opt-in, source-complete windows, fresh-app recall, and query-aware selection are deterministic only; live continuity remains open. |
| Worker/runtime health | `OPERATIONAL_LIMITED` | Extraction is live-proved; delayed Attention, reply-independent extraction, exact-group summary fencing, and frozen source windows are deterministic but not live-deployed. |
| Governance | `DETERMINISTIC_READY_LIVE_UNPROVED` | The local browser now covers every currently registered browser-safe contract, including maintenance rollback/expiration, record forget/restore, Privacy, Group-summary, Display-profile redaction, platform-account unlink, verified backup, and stopped-service restore-handoff preparation. Strict response normalization, action-bound closure-only authority, exact request bodies, duplicate suppression, and desktop/mobile accessibility/security QA are deterministic. Retention lacks a complete browser-facing backend contract; fresh QQ/Provider, deployment/restart, actual stopped-service restore, soak, and production accessibility evidence remain open. |
| Overall target product | `EXPERIMENTAL_NOT_NORMAL` | The stack can receive, reason, and send, but multi-person/quote correctness and direct-trigger reliability fail the basic usability gate. |

## Requirement Matrix

| ID | Status | Current evidence and exact gap |
|---|---|---|
| `GW` | `PROVED` live | Last-hour receipts were accepted; every successful reply had a platform message ID and matching same-conversation bot row; no delivery failure was observed. |
| `ING` | `IN_PROGRESS` | Raw/chat/admission persistence and deduplication are healthy; delayed candidates, extraction intents, frozen summary windows, and fresh-app recall are source-bound, durable, and FK-clean. Live deployment remains open. |
| `TURN` | `IN_PROGRESS` | Deterministic terminal evaluator failure now completes social processing with governed suppression evidence; the fixed path still needs the R4 live canary, and broader turn-runtime evidence remains open. |
| `ACT` | `IN_PROGRESS` | R2A-R3 and R5 deterministically prove addressing/risk separation, bounded evaluator failure, revision/source-bound memory wording, delayed source revalidation, suppressor attempts, and indeterminate-delivery failure. The corrected paths are not live-canary proved. |
| `MEM` | `IN_PROGRESS` | Private governed recall, privacy rejection, effect-bound outbound claims, exact creation-source binding, exact-group summary policy, reply-independent same-group proposals, source-complete summaries, two-process recall, query-aware selection, QQ governance, and the full conflict/consolidation/decay review/apply/rollback chain are deterministic. Live continuity/recall remains open. |
| `CTX` | `IN_PROGRESS` | R1 and R8 deterministically prove opaque distinct speakers, explicit current input, bounded same-conversation quotes, selected participants, budgeting, query-aware ranking, and trace evidence; the corrected pack still needs the R4 live canary. |
| `PI` | `IN_PROGRESS` | P2 scheduling/session isolation plus P3 schema, exact per-request runtime persistence, reconciled usage totals, bounded usage/status consumers, and latency aggregates are deterministic-ready; live Provider evidence remains open. |
| `TOOL` | `IN_PROGRESS` | Registry/policy/effect controls, private aggregate `runtime.status`, bounded `runtime.tools` inspection, default-off immutable-root workspace reads, and default-off exact-origin `web.fetch_text` are deterministic-ready; configured catalogs remain exact and all legacy handlers dormant. Only authorized live proof remains open. |
| `WORK` | `IN_PROGRESS` | Extraction jobs are live-healthy; delayed rechecks, summary policy fencing, reply-independent admission, frozen source-complete windows, and restart reopen behavior are durable and deterministic. Live deployment remains open. |
| `GOV` | `DETERMINISTIC_READY_LIVE_UNPROVED` | P8 now deterministically covers every currently registered browser-safe governance HTTP contract. The modular browser includes Overview, nine Activity views, Memory Records/Review and all maintenance/record transitions, Explain, Privacy, Group-summary, Display-profile redaction, platform-account unlink, verified backup, and stopped-service restore-handoff preparation. Synthetic Chromium proves exact requests, private authority, redacted output, duplicate suppression, desktop/mobile layout, focus/ARIA/control sizing/contrast/reduced-motion/no-overflow, same-origin loading, and a synthetic screenshot. Retention remains a prerequisite until its backend policy/snapshot/audit/confirmation contract exists; fresh live QQ/Provider, deployment/restart, restore execution, soak, and production accessibility proof remain open. |
| `OPS` | `IN_PROGRESS` | Current containers, schema, integrity/FKs, worker heartbeat, delivery, and latency are healthy; long soak, retention consistency, and off-host publication remain broader production work. |
| `LIVE` | `IN_PROGRESS` | Real private recall, group mentions, quotes, provider turns, delivery, and isolation have evidence, but recent direct-trigger failure rate and semantic attribution fail the usability gate; deterministic restart proof does not establish live restart/next-day recall. |
| `DOC` | `IN_PROGRESS` | Entry navigation selects the P0-P9 long-term program and companion goal; scoped R1-R8 contracts remain domain evidence. P1/P2, `PI-OBS-01A/01B/01C/01D`, `MEM-MAINT-01A/01B1/01B2/01C1/01C2/01C3`, `TOOL-PROD-01A/01B1/01B2/01C1/01D1`, and P7 are deterministic-ready with canonical contracts. P8-P9 and authorized live evidence stay open. |

## Product Decision References

The stable thresholds and behavior decisions previously stored in this mutable
checkpoint now live in `docs/design-decisions.md`:

- D9: group addressing and delayed Attention;
- D10: group context identity and quote semantics;
- D11: memory thresholds, per-group opt-in, QQ governance, and retention;
- D12: evaluator failure and memory-claim truthfulness.

## Prior Reliability Route And Remaining Live Gate

R0-R3 and R5-R8 below are deterministic-ready in the committed worktree. R4
remains live-unproved. This table preserves the scoped reliability contract; it
does not override the active P0-P9 program or the exact P3 next action above.

| Slice | Outcome | Verification and rollback boundary |
|---|---|---|
| `R0` | Add synthetic structural replay fixtures for multi-speaker history, duplicate display names, quote targets inside/outside the rolling window, narrative admin text, invalid evaluator output, and unsupported memory claims. Prove current failures before source edits. | Tests/docs only; no runtime or schema change. |
| `R1` | Add opaque speaker/message refs, selected-participant display metadata, explicit current message, and bounded same-conversation quote relations to ContextPack, Pi rendering, budgeting, and trace. | Limit edits to context/identity/types/Pi/main wiring and tests; prefer no migration; rollback image restores behavior. |
| `R2A` | Separate respond/defer/silent relevance from action risk; use deterministic admin commands and correct `isProactive`. | Attention/social-decision slice only; no evaluator transport or dependency change. |
| `R2B` | Use provider structured output when supported; otherwise one separately ledgered correction attempt; terminal true-risk failure becomes durable governed suppression, never fail-open. | Evaluator core and its tests; do not combine with R2A deployment or dependency upgrades. |
| `R3` | Bind each memory claim to the exact same proposition, subject, scope, source/effect, and lifecycle state in selected active memory or a fully committed same-turn proposal effect; correct unsupported text before send and persist the delivered version. Same-turn activation is not applicable because activation is a later governance transition. | Independent response/action guard; memory lifecycle remains unchanged; unrelated selected memory never authorizes a claim. |
| `R4` | Run deterministic gates and the authorized multi-participant/quote/admin/rapid-trigger QQ canary. Record `BASIC_USABLE` only with zero attribution, isolation, unsupported-claim, and ordinary-direct evaluator-loss failures. | Deploy the reviewed current R1-R8 candidate; canary failure restores the prior image. |
| `R5` | Add durable delayed Attention with the locked 15-second/120-second/human-answer/traffic/budget policy and auditable terminal suppressors. | New job type is isolated; stop enqueue and drain/cancel it before worker rollback. |
| `R6` | Reuse one governance service for deterministic QQ `/memory`/`/why` plus CLI delete/summary operations; CLI `/why` parity is not required. Add per-group summary opt-in defaulting off. Only the bot owner or a normalized owner/admin of that exact group may change it. Disable immediately stops enqueue/retrieval and cancels pending summary jobs; retained summaries stay governed and separately deletable. | Any preference schema is a separate migration with upgrade, FK, restore, and cross-version rollback evidence. Re-enable does not backfill missed windows. |
| `R7` | Run high-precision source-backed candidate detection after persistence, independent of replies; process summaries only for opted-in groups; prove approved recall after restart. | Scheduler paths remain opt-in and idempotent; stopping them does not rewrite existing memory state. |
| `R8` | Run `REL-RET-01`: at least 12 synthetic queries, each with one expected same-scope source, at least eight newer/higher-importance same-scope distractors, and incompatible-scope records. Skip ranking code only if the expected source is selected in 12/12 under production count/token limits, incompatible selections are zero, and trace reasons are complete; otherwise add current-query/FTS/quote/thread ranking. | Retrieval ranking is independently reversible to importance/recency; embeddings remain excluded without separate evidence. |

## Blockers And Exact Next Action

- No goal-wide external blocker currently applies because P1-P3 and P6-P8 have
  safe local deterministic work. P4 and the live portion of P9 still require
  fresh Provider, QQ, deployment/restart, and applicable runtime authority.
- Historical `/tmp` backups and evidence remain private, are not current source
  evidence, and must not be read, shared, staged, or treated as future rollback
  artifacts without fresh authority and validation.
- Exact next action: the current local P8 browser contract set and deterministic
  accessibility/security QA are complete. Do not invent retention mutation
  semantics: its next local slice must begin with an independently reviewed
  backend policy, snapshot, audit, and preview/confirmation contract. Otherwise
  the remaining work is authorization-bound live acceptance: Provider/QQ,
  deployment/restart, actual stopped-service restore, soak, and production
  accessibility/screenshots. No such live evidence is claimed here.
- Active execution instructions are in
  `docs/prompts/repair-and-long-term-development-goal.md`; the stable phase and
  rollback contract is in `docs/long-term-development-constraints.md`.
