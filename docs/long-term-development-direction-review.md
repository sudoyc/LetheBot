# Long-Term Development Direction Review

**Date:** 2026-07-10 03:45 local shell time (CST +0800)
**Scope:** concise current direction review for LetheBot production-readiness work. The pre-consolidation long-form review is preserved in `docs/archive/reviews/long-term-development-direction-review.md` and is historical context only, not completion proof.

## Executive Summary

LetheBot's target remains sound: a local-first QQ / SnowLuma / OneBot chatbot with governed memory, Pi reasoning, audited tools/actions/workers, operator governance, deployment/ops runbooks, and controlled live acceptance evidence.

Current deterministic foundations are strong, but production readiness remains unproven because real SnowLuma/QQ private and group exact-`@bot` acceptance evidence has not been produced and validated.

## Current Evidence Baseline

Fresh status after the latest full gate:

```bash
2026-07-10 03:45:24 CST +0800
tracked_dirty=58 untracked=19 total=77
```

Pi packages:

- `@earendil-works/pi-agent-core 0.80.2`
- `@earendil-works/pi-ai 0.80.2`

Latest verified full deterministic gate after the acceptance DB summary selected governed-memory group/conversation tool-source regression slice:

```bash
pnpm release:check
# exited 0
# 75 passed | 1 skipped test files
# 1245 passed | 8 skipped tests

git diff --check
# included in release:check and exited 0
```

Latest completed hardening:

- Acceptance DB summary selected governed-memory tool-source compatibility validation now rejects memory-governance DB hints when selected user-scoped memory is backed only by another user's successful `tool_output` source and is regression-locked for group-scoped and conversation-scoped memory backed by successful tool calls from other boundaries. Successful tool calls must have actor or source turn context compatible with the selected memory boundary; when actor metadata is absent, compatibility can be inferred from the source turn sender/context. The failing-first user-scoped regression initially showed `selectedGovernedMemoryContexts` stayed `1` after the source row was repointed to another user's successful tool call; after the fix plus group/conversation regression coverage, targeted user tool-source coverage passed with `2 passed | 48 skipped`, targeted group tool-source coverage passed with `2 passed | 50 skipped`, targeted conversation tool-source coverage passed with `2 passed | 52 skipped`, full local acceptance evidence coverage passed with `54 passed`, `pnpm typecheck` passed, `pnpm lint` passed, and final `pnpm release:check` exited `0` with `75 passed | 1 skipped` test files and `1245 passed | 8 skipped` tests.
- Acceptance DB summary selected governed-memory worker-source provenance validation now rejects memory-governance DB hints when selected governed memory is backed only by a completed `worker_extraction` job/attempt without explicit raw/chat provenance. Completed worker rows must carry raw/chat source identifiers in payload/result, and those identifiers must resolve to usable, owner/scope-compatible inbound QQ chat evidence. The failing-first targeted regression initially showed `selectedGovernedMemoryContexts` stayed `1` after the source row was repointed to a completed worker job without chat/raw provenance; after the fix, targeted worker-source coverage passed with `2 passed | 46 skipped`, full local acceptance evidence coverage passed with `48 passed`, and final `pnpm release:check` exited `0` with `75 passed | 1 skipped` test files and `1239 passed | 8 skipped` tests.
- Acceptance DB summary selected governed-memory owner/scope-compatible source validation now rejects memory-governance DB hints when a selected user-scoped memory is backed only by another user's inbound chat source. Inbound raw/chat source evidence must be usable and compatible with the selected memory boundary: user-scoped memory sources must resolve to the same canonical owner, `private_only` user sources must be private, `same_group_only` user sources must match group/conversation, group-scoped sources must match group/conversation, conversation-scoped sources must match conversation, and global/system memory still counts only when public. The failing-first targeted regression initially showed `selectedGovernedMemoryContexts` stayed `1` after the source row was repointed to another user's inbound chat row; after the fix, combined targeted owner/usable/source-link coverage passed with `3 passed | 43 skipped`, full local acceptance evidence coverage passed with `46 passed`, and final `pnpm release:check` exited `0` with `75 passed | 1 skipped` test files and `1237 passed | 8 skipped` tests.
- Acceptance DB summary selected governed-memory usable-source evidence validation now rejects memory-governance DB hints when selected governed memory only has unusable `memory_sources` rows. Selected memory source links must resolve to usable durable source evidence: inbound QQ `chat.message.received` raw events with linked inbound non-bot chat rows, inbound non-bot chat rows linked to those raw events, successful tool calls whose actor or source turn context is compatible with the selected memory boundary, or completed jobs/job attempts with explicit compatible raw/chat provenance in payload/result. Orphan/unresolvable sources, `user_command`-only links, bot-response chat rows, and rejected/error tool calls do not satisfy `selectedGovernedMemoryContextPresent`, in addition to the existing active/source/revision/non-secret visibility and scope gates. The failing-first targeted regression initially showed `selectedGovernedMemoryContexts` stayed `1` after the source row was repointed to a rejected tool call; after the fix, combined targeted usable/source-link coverage passed with `2 passed | 43 skipped`, full local acceptance evidence coverage passed with `45 passed`, and final `pnpm release:check` exited `0` with `75 passed | 1 skipped` test files and `1236 passed | 8 skipped` tests.
- Acceptance DB summary selected governed-memory source-link resolvability validation now rejects memory-governance DB hints when selected governed memory only has orphan/unresolvable `memory_sources` rows. Selected memory source links must resolve to durable source rows (`raw_events`, `chat_messages`, `tool_calls`, `jobs`, or `job_attempts`, with `chat_message` accepting either chat row ID or platform `message_id`), in addition to the existing active/source/revision/non-secret visibility and scope gates. The failing-first targeted regression initially showed `selectedGovernedMemoryContexts` stayed `1` after the source row was repointed to a missing chat-message source; after the fix, targeted source-link coverage passed with `1 passed | 43 skipped`, full local acceptance evidence coverage passed with `44 passed`, and final `pnpm release:check` exited `0` with `75 passed | 1 skipped` test files and `1235 passed | 8 skipped` tests.
- Acceptance DB summary selected governed-memory scope/actor validation now rejects memory-governance DB hints when selected governed memory is not scoped to the same flow actor/context. User-scoped memory must belong to the triggering sender via active QQ platform-account mapping; group-scoped memory must match the flow group or conversation; conversation-scoped memory must match the flow conversation; global/system memory counts only when public. The targeted regression verifies another user's user-scoped memory no longer satisfies `selectedGovernedMemoryContextPresent`; targeted coverage passed with `1 passed | 42 skipped`, full local acceptance evidence coverage passed with `43 passed`, and final `pnpm release:check` exited `0` with `75 passed | 1 skipped` test files and `1234 passed | 8 skipped` tests.
- Acceptance DB summary selected governed-memory visibility validation now rejects memory-governance DB hints when a complete flow selects a governed memory that is not visible in that same flow context. The DB summary now checks selected memory visibility against the flow context: `public` and `same_user_any_context` are visible, `private_only` counts only for private flows, and `same_group_only` counts only for matching group/conversation scope. The failing-first targeted regression initially showed `selectedGovernedMemoryContexts` stayed `1` after a group flow selected `private_only` memory; after the fix, targeted visibility coverage passed with `1 passed | 41 skipped`, full local acceptance evidence coverage passed with `42 passed`, and final `pnpm release:check` exited `0` with `75 passed | 1 skipped` test files and `1233 passed | 8 skipped` tests.
- Acceptance DB summary group-scope required-hints validation now rejects group completion evidence unless the same gateway-normalized `qq-group-<digits>` group ID is carried through the triggering group `chat_messages` row, the selected `context_traces` row, and the matching bot-response `chat_messages` row. Exact `mentions_bot=1` remains required for group targeting. The failing-first targeted regression initially showed group hints stayed true after the trigger row lost `group_id`; after the fix, targeted group-scope coverage passed with `2 passed | 39 skipped`, full local acceptance evidence coverage passed with `41 passed`, and final `pnpm release:check` exited `0` with `75 passed | 1 skipped` test files and `1232 passed | 8 skipped` tests.
- Governed-memory lifecycle state-machine validation now rejects invalid direct repository transitions before durable mutation. `MemoryRepository.updateState()` allows only the documented transitions for proposed, active, rejected, disabled, superseded, and deleted records, while preserving valid proposal decision, restore, delete, and supersede paths. The failing-first targeted regression initially showed `repo.supersede(disabledMemory)` resolved; after the fix, targeted lifecycle coverage passed with `3 passed | 36 skipped`, related memory/governance/CLI coverage passed with `80 passed | 110 skipped`, and final `pnpm release:check` exited `0` with `75 passed | 1 skipped` test files and `1230 passed | 8 skipped` tests.
- ContextBuilder memory retrieval-limit validation now prevents prompt-context starvation from inaccessible user memories. Group/private context is passed into prompt-eligible user/global memory retrieval before bounded repository limits apply, group/conversation-bound memories are fetched through separate scoped lookups, and bounded no-context scans still preserve rejection trace evidence for owner/admin explainability. The failing-first targeted regression initially showed selected group memory was empty when 50 private-only user memories consumed the default window; after the fix, targeted ContextBuilder coverage passed with `3 passed | 12 skipped`, related memory/context/tool/injection coverage passed with `73 passed`, and final `pnpm release:check` exited `0` with `75 passed | 1 skipped` test files and `1229 passed | 8 skipped` tests.
- Governed-memory retrieval/search visibility-limit validation now applies lifecycle, sensitivity, metadata, and context visibility predicates before bounded result limiting. `retrieve()` filters visibility before `ORDER BY importance ... LIMIT`, while `search()` joins FTS rows to `memory_records` so private/group visibility filters run before FTS rank limiting. The failing-first regression initially showed group retrieval and search returning `[]` when private-only rows consumed `limit=1`; after the fix, targeted visibility-limit coverage passed with `2 passed | 36 skipped`, broader retrieval/search lifecycle coverage passed with `5 passed | 33 skipped`, related memory/context/tool/injection coverage passed with `72 passed`, and final `pnpm release:check` exited `0` with `75 passed | 1 skipped` test files and `1228 passed | 8 skipped` tests.
- Governed-memory proposal-decision lifecycle validation now rejects non-proposed records in `MemoryRepository.approve()` and `MemoryRepository.reject()` before durable mutation. Proposal-only approval/rejection semantics are enforced at the repository boundary as well as in the governance CLI; rejected attempts leave record state, retrieval visibility, revision rows, memory audit rows, and FKs unchanged. The failing-first targeted regression initially showed `approve(active)` resolved, then targeted proposal-decision coverage passed with `3 passed | 33 skipped`; related memory/action/governance coverage passed with `99 passed`, and final `pnpm release:check` exited `0` with `75 passed | 1 skipped` test files and `1226 passed | 8 skipped` tests.
- Governed-memory lifecycle validation now rejects attempts to transition existing memory records back to `proposed`. Proposal state is now create-only in the repository lifecycle path; rejected transition attempts leave state, revisions, memory audit rows, and FKs unchanged. The failing-first targeted regression initially showed an active record could be changed back to proposed, then passed after the fix with `1 passed | 34 skipped`; related memory/action/tool coverage passed with `79 passed`, and final `pnpm release:check` exited `0` with `75 passed | 1 skipped` test files and `1225 passed | 8 skipped` tests.
- Governed-memory expiration lifecycle metadata validation now rejects non-finite optional `expiresAt` timestamps before durable writes. This prevents invalid expiration metadata from silently creating permanent or unreliable memory, and keeps rejected writes free of `memory_records`, `memory_sources`, `memory_revisions`, and memory audit rows. The failing-first targeted regression initially showed an invalid expiration resolved and created memory, then passed after the fix with `1 passed | 33 skipped`; combined provenance/lifecycle coverage passed with `4 passed | 30 skipped`, related memory/action/tool coverage passed with `78 passed`, and final `pnpm release:check` exited `0` with `75 passed | 1 skipped` test files and `1224 passed | 8 skipped` tests.
- Governed-memory duplicate explicit source provenance validation now rejects duplicate explicit `sourceId` values within one `sources[]` create request before durable writes. This prevents silent SQLite primary-key dedupe from making persisted source rows disagree with requested/audited source count, and keeps rejected writes free of `memory_records`, `memory_sources`, `memory_revisions`, and memory audit rows. The failing-first targeted regression initially showed duplicate source IDs resolved and created memory, then passed after the fix with `1 passed | 32 skipped`; combined source-provenance coverage passed with `3 passed | 30 skipped`, related memory/action/tool coverage passed with `77 passed`, and final `pnpm release:check` exited `0` with `75 passed | 1 skipped` test files and `1223 passed | 8 skipped` tests.
- Governed-memory implicit/default source provenance validation now rejects blank `sourceContext`-derived source IDs before durable writes. Explicit source links were already guarded; this slice extends the same non-empty `sourceId` rule to the implicit source path while preserving the deterministic `memory:<memoryId>` fallback when both `sources[]` and `sourceContext` are omitted. The failing-first targeted regression initially showed a blank implicit source resolved and created memory, then passed after the fix with `1 passed | 31 skipped`; combined source-provenance coverage passed with `2 passed | 30 skipped`, related memory/action/tool coverage passed with `76 passed`, and final `pnpm release:check` exited `0` with `75 passed | 1 skipped` test files and `1222 passed | 8 skipped` tests.
- Governed-memory source provenance validation now rejects malformed explicit source links before durable writes. Blank/whitespace-only `sourceId` values and non-finite `sourceTimestamp` values cannot create `memory_records`, `memory_sources`, `memory_revisions`, or memory `audit_log` rows; rejection leaves SQLite FKs clean. The failing-first targeted regression initially showed a blank source ID still created memory, then passed after the fix with `1 passed | 30 skipped`; related memory/action/tool coverage passed with `75 passed`, and final `pnpm release:check` exited `0` with `75 passed | 1 skipped` test files and `1221 passed | 8 skipped` tests.
- Fresh-schema SQLite coverage now proves `action_executions.executed_job_id` and `action_executions.executed_memory_id` are enforced FK links to durable `jobs` and governed `memory_records`. Attempts to insert action execution rows with missing job or memory linkage fail, leave no partial execution row, and keep `PRAGMA foreign_key_check` clean. Targeted database coverage passed with `1 passed | 23 skipped`, related schema/action coverage passed with `61 passed`, and final `pnpm release:check` exited `0` with `75 passed | 1 skipped` test files and `1220 passed | 8 skipped` tests.
- DB-backed action-executor coverage now proves the generated durable-job idempotency suffix rule for every current generated durable-job action group, not only `schedule_background_task`. Duplicate `admin_digest`, duplicate `silent_summarize_later`, and duplicate `schedule_background_task` actions in one decision all create distinct pending `jobs` rows, successful action executions, distinct `executed_job_id` values, expected `:actionN` idempotency keys, and clean SQLite FKs while single-action keys remain backward-compatible. Targeted duplicate-group coverage passed with `1 passed | 32 skipped`, related action/job/type coverage passed with `71 passed`, and final `pnpm release:check` exited `0` with `75 passed | 1 skipped` test files and `1219 passed | 8 skipped` tests.
- `ActionExecutor` now prevents same-decision durable-job idempotency collisions for multiple same-group local job actions. Single `admin_digest`, `silent_summarize_later`, and `schedule_background_task` actions keep their previous generated idempotency keys, while duplicate same-group durable jobs receive deterministic `:action1`, `:action2`, ... suffixes. A DB-backed regression proves two `schedule_background_task` / `summary` actions in one action decision create two pending `jobs` rows, two successful action executions, distinct `executed_job_id` values, expected generated idempotency keys, and clean SQLite FKs. The failing-first targeted test initially produced only one distinct job ID; after the fix, targeted coverage passed with `1 passed | 31 skipped`, full action-executor coverage passed with `32 passed`, related action/job/type coverage passed with `70 passed`, and final `pnpm release:check` exited `0` with `75 passed | 1 skipped` test files and `1218 passed | 8 skipped` tests.
- `SocialDecisionService` now re-anchors evaluator `modifiedAction` output to the local base action target and preserves locally derived hard constraints before persistence and execution. This prevents evaluator/model output from retargeting delivery, replacing canonical governance identity, dropping evaluator-required status, removing/shortening cooldowns, raising local response-token budgets, lowering redaction strictness, or removing locally required capabilities while still allowing controlled changes to action type, payload, and reason plus added/stronger constraints. Targeted service coverage passed with `4 passed`, related action/repository/type coverage passed with `54 passed`, the group app-path E2E passed with `91 passed` while proving weakened evaluator constraints are ignored for persisted actions and cooldown suppression, the follow-up downgrade regression proves evaluator `cooldownSeconds=0` cannot shorten the local group cooldown, and final `pnpm release:check` exited `0` with `75 passed | 1 skipped` test files and `1217 passed | 8 skipped` tests.
- `SocialDecisionService` now preserves canonical governance identity on base private reply action targets before evaluator modification and repository persistence. Private action decisions carry the normalized platform sender/delivery ID in `target.userId` and the resolved actor canonical user ID in `target.canonicalUserId`; group reply targets remain group-scoped and do not treat the group sender as a DM recipient. Failing-first unit coverage initially showed persisted private targets lacked `canonicalUserId`; after the fix, targeted service coverage passed, related action/repository/type coverage passed with `51 passed`, the app-path private-reply E2E passed with `90 passed` while asserting persisted `action_decisions.actions` matches `platform_accounts.canonical_user_id`, and final `pnpm release:check` exited `0` with `75 passed | 1 skipped` test files and `1213 passed | 8 skipped` tests.
- `dm_user` target identity semantics are now explicit and DB-tested: `ActionTarget.userId` is the gateway delivery user ID, while `ActionTarget.canonicalUserId` is the privacy/governance user ID. `ActionExecutor` checks proactive-DM opt-out against `target.canonicalUserId`, sends private messages with platform `target.userId`, and rejects proactive DMs missing a canonical target before privacy lookup or gateway send with `PROACTIVE_DM_CANONICAL_USER_REQUIRED` plus bounded `dm_opt_out=missing_canonical_user` audit evidence. Failing-first targeted coverage initially showed the platform ID was used for opt-out and missing canonical targets still attempted privacy lookup; after the fix, targeted `dm_user` coverage passed with `5 passed | 26 skipped`, full action-executor coverage passed with `31 passed`, related action/repository/schema/governance/type coverage passed with `103 passed`, and final `pnpm release:check` exited `0` with `74 passed | 1 skipped` test files and `1212 passed | 8 skipped` tests.
- `ActionExecutor` now persists bounded `dm_user` proactive-audit metadata on success, opt-out rejection, and send-failure paths. `action_executions.audit_entry` records `dm_proactive`, `dm_trigger`, `dm_opt_out`, `dm_redaction_level`, and `dm_cooldown_key`, while free-text action reasons and cooldown keys are redacted before durable persistence. The failing-first targeted regression initially showed `dm_user` audit entries lacked the required proactive/trigger/opt-out/redaction/cooldown metadata; after the fix, targeted `dm_user` coverage passed with `4 passed | 26 skipped`, full action-executor coverage passed with `30 passed`, related action/repository/schema/governance coverage passed with `87 passed`, and final `pnpm release:check` exited `0` with `74 passed | 1 skipped` test files and `1211 passed | 8 skipped` tests.
- DB-backed executor coverage now proves the existing `ActionExecutor` L0 final guard applies to durable local side-effect actions, not only outward sends. Prohibited decisions and evaluator-required-but-not-passed decisions reject `admin_digest`, `propose_memory`, and `schedule_background_task` before any gateway send, `jobs` write, or governed-memory row/source/revision write. Rejected `action_executions` carry `PROHIBITED_ACTION_DECISION` or `EVALUATOR_NOT_PASSED`, keep job/memory linkage columns null, and leave temp SQLite foreign keys clean. Targeted durable-action coverage passed with `2 passed | 27 skipped`, full action-executor coverage passed with `29 passed`, related action/repository/schema/governance coverage passed with `86 passed`, and final `pnpm release:check` exited `0` with `74 passed | 1 skipped` test files and `1210 passed | 8 skipped` tests.
- `ActionExecutor` now rejects user-scoped `propose_memory` actions when `memory_association=opted_out`. The check runs before turn-source lookup and before `MemoryRepository.create`, records a rejected action execution with `MEMORY_ASSOCIATION_OPT_OUT`, creates no memory/source/revision rows, omits candidate content from rejection evidence, and performs no gateway send. The failing-first targeted regression initially showed the action still succeeded after opt-out; after the fix, targeted `memory association|propose_memory` coverage passed with `4 passed | 23 skipped`, full action-executor coverage passed with `27 passed`, related action/repository/schema/governance coverage passed with `84 passed`, and final `pnpm release:check` exited `0` with `74 passed | 1 skipped` test files and `1208 passed | 8 skipped` tests.
- `ActionExecutor` now creates governed proposed memory for approved `propose_memory` actions instead of rejecting them as unwired. It requires a configured `MemoryRepository` and a traceable turn source, creates only `state='proposed'` records through `MemoryRepository.create`, links memory sources to the triggering `raw_event`, writes normal revision/audit evidence, stores `source_context='action_executor:propose_memory'` instead of raw action-provided source context, persists `action_executions.executed_memory_id`, returns `executed.memoryId`, and performs no gateway side effect. Deterministic policy rejects secret/prohibited proposal content before memory rows are written, and governance inspection plus owner/admin `/why` display redacted executed memory IDs. Targeted executor/repository/schema/governance/CLI coverage passed, related coverage passed with `83 passed`, and final `pnpm release:check` exited `0` with `74 passed | 1 skipped` test files and `1207 passed | 8 skipped` tests.
- `ActionExecutor` now schedules approved `silent_summarize_later` actions as durable local `summary` jobs rather than leaving them as pure no-ops. It requires a configured durable job repository and target conversation, generates `action:silent_summarize_later:<decisionId>:summary` idempotency keys, stores bounded action provenance, target conversation fields, and a redacted reason summary without copying raw prompt/action payload text, persists `action_executions.executed_job_id`, returns `executed.jobId`, and performs no gateway side effect. Because this is durable job scheduling rather than a pure no-op, prohibited and evaluator-unapproved decisions are now rejected before any `jobs` row is created. `schedule_background_task` payloads now also keep redacted worker-consumable task fields at top level while preserving redacted `taskPayload` provenance. Targeted executor and integration coverage passed, including an action-shaped summary job processed by the durable worker into source-linked memory with FK-clean evidence and DB-backed prohibited/evaluator guard regressions; final `pnpm release:check` exited `0` with `74 passed | 1 skipped` test files and `1203 passed | 8 skipped` tests.
- `ActionExecutor` now schedules approved `schedule_background_task` actions as durable local jobs rather than rejecting them as unwired. It requires a configured durable job repository, accepts only finite durable task types (`summary`, `extraction`, `consolidation`, `decay`, `conflict`, `admin_digest`, and `retention`), generates `action:schedule_background_task:<decisionId>:<taskType>` idempotency keys instead of copying raw model-supplied keys, stores redacted worker-consumable task fields plus bounded audit provenance and `taskPayload`, persists `action_executions.executed_job_id`, returns `executed.jobId`, and performs no gateway side effect. Targeted executor coverage first failed on `ACTION_NOT_IMPLEMENTED`, then passed; related action repository coverage passed; final `pnpm release:check` exited `0` with `74 passed | 1 skipped` test files and `1199 passed | 8 skipped` tests.
- `ActionExecutor` now schedules approved `admin_digest` actions as durable local `admin_digest` jobs rather than rejecting them as unwired. It stores coarse redacted job payload evidence, persists `action_executions.executed_job_id`, returns `executed.jobId`, and does not perform a gateway side effect. `LetheBotApp` wires the runtime `JobRepository`; governance inspection displays redacted `executedJobId`; and startup migration applies an additive compatibility patch for existing local v1 DBs. Targeted executor coverage first failed on `ACTION_NOT_IMPLEMENTED`, then passed; targeted schema compatibility and governance inspection coverage passed; combined related coverage passed with `75 passed`; final `pnpm release:check` exited `0` with `74 passed | 1 skipped` test files and `1198 passed | 8 skipped` tests.
- `AdminDigestWorker.generate()` now redacts dynamic sample identifiers and classifier fields before returning digest evidence or writing generated digest audit details. A temp-DB unit regression seeds failed job IDs/types, action-execution IDs/action types, tool-call IDs/tool names, and high-risk audit IDs/event types with assignment-shaped secret/platform fragments; the worker returns intact counts plus redacted samples only, omits raw dynamic values, payloads, tool input/output, error diagnostics, secrets, and platform-like IDs, and leaves SQLite FKs clean. Targeted worker coverage first failed on leaked sample IDs, then passed with `1 passed`; related `admin digest jobs` E2E coverage passed with `89 passed`; follow-up `pnpm typecheck`, `pnpm lint`, and `git diff --check` passed; final `pnpm release:check` exited `0` with `74 passed | 1 skipped` test files and `1196 passed | 8 skipped` tests.
- Owner/admin `why` without `--turn` now has deterministic latest-turn linked tool-call explainability coverage. The spawned temp-DB CLI regression seeds older and latest turns with linked `tool_calls`; default `why` resolves the latest stored turn, displays only that turn's redacted tool-call ID, tool name, status, `requested_by`, duration, error code, and bounded error message, and omits older-turn tool calls, tool input/output payloads, raw event/chat text, seeded secret-like values, and raw platform-like IDs. The command is read-only, leaves DB rows unchanged, and keeps FK-clean evidence. Targeted CLI coverage passed with `121 passed`; follow-up `pnpm typecheck`, `pnpm lint`, and `git diff --check` passed; final `pnpm release:check` exited `0` with `73 passed | 1 skipped` test files and `1195 passed | 8 skipped` tests.
- PiAdapter now preserves the `group.recent_summary` privacy boundary at exposure and execution time. Private turns do not expose the group-only tool, and group turns without a current group identifier return only a rejected no-data result instead of reading any other group's rows. The deterministic temp-DB regression seeds other-group text and secret/platform-like fragments, then proves prompt-facing and persisted rejection evidence omits the tool in private context, omits other-group text, raw chat text, raw IDs, and unredacted secret/platform-like fragments, and keeps foreign keys clean. Targeted PiAdapter coverage passed with `48 passed`; follow-up `pnpm typecheck`, `pnpm lint`, and `git diff --check` passed; final `pnpm release:check` exited `0` with `73 passed | 1 skipped` test files and `1195 passed | 8 skipped` tests.
- `acceptance:db-summary` now reports aggregate `tool_calls` counts by status (`success`, `error`, `timeout`, `rejected`, `other`) rather than only a total. The deterministic temp-DB regression seeds successful, rejected, and errored tool calls with secret/platform-bearing input/output/error payloads and proves the summary exposes only counts without printing tool payloads, tool diagnostics, raw message text, platform-like IDs, memory content, audit details, or DB row contents. Targeted local acceptance evidence coverage passed with `39 passed`; `pnpm typecheck`, `pnpm lint`, and `git diff --check` passed; final `pnpm release:check` exited `0` with `73 passed | 1 skipped` test files and `1194 passed | 8 skipped` tests.
- Owner/admin `why --turn <turn-id>` now has deterministic failed-turn linked tool-call explainability. A spawned temp-DB CLI regression seeds a failed turn with a stored context trace plus linked rejected/error `tool_calls`; `/why` displays redacted summaries with tool-call ID, tool name, status, `requested_by`, duration, error code, and redacted error message, while omitting tool input/output payloads, raw failed-turn runtime response text, raw event/chat text, seeded secret-like values, and raw platform-like IDs. The command is read-only, leaves DB rows unchanged, and keeps FK-clean evidence. Targeted CLI coverage passed with `121 passed`; `pnpm typecheck`, `pnpm lint`, and `git diff --check` passed; final `pnpm release:check` exited `0` with `73 passed | 1 skipped` test files and `1194 passed | 8 skipped` tests.
- `PiAdapterOutput.toolCallIds` now has deterministic turn-failure coverage for `beforeToolCall`-only rejected tool calls, not only wrapped-execute rejection, handler-error, or success paths. A temp-DB regression drives a Pi turn where the SDK hook blocks an evaluator-required tool before the handler runs; the turn returns `status=failed`, `toolCallIds=[...]`, the handler is not called, and the seeded secret-like hook args are redacted in durable `tool_calls` and `audit_log` rows with FK-clean evidence. Targeted PiAdapter coverage passed with `47 passed`; full PiAdapter coverage passed with `47 passed`; `pnpm typecheck` and `pnpm lint` passed; final `pnpm release:check` exited `0` with `73 passed | 1 skipped` test files and `1193 passed | 8 skipped` tests.
- `PiAdapterOutput.toolCallIds` now has deterministic turn-failure coverage for handler-error tool calls, not only rejected/successful executions. A temp-DB regression drives a Pi turn where an evaluator-bypass tool is proposed during `agent.prompt`, the handler throws, the turn returns `status=failed`, `toolCallIds=[...]`, and the seeded secret-like input/error is redacted in failed-turn output plus durable `tool_calls` and `audit_log` rows with FK-clean evidence. Targeted PiAdapter coverage passed with `46 passed`; full PiAdapter coverage passed with `46 passed`; `pnpm typecheck` and `pnpm lint` passed; final `pnpm release:check` exited `0` with `73 passed | 1 skipped` test files and `1192 passed | 8 skipped` tests.
- `PiAdapterOutput.toolCallIds` now includes rejected adapter-audited tool-call attempts, not only successful executions. A deterministic regression drives a Pi turn where an evaluator-required dangerous tool is proposed during `agent.prompt`; the handler is not called, the turn returns `status=failed`, `toolCallIds=[...]`, and the rejected tool input is durably redacted in both `tool_calls` and `audit_log` with FK-clean evidence. Targeted PiAdapter coverage passed with `45 passed`; final `pnpm release:check` exited `0` with `73 passed | 1 skipped` test files and `1191 passed | 8 skipped` tests.
- `acceptance:db-summary --require-acceptance-hints` now has deterministic coverage proving downgraded `react_only` face/text fallback does not satisfy complete delivered-reply acceptance hints, even when the app path persists the fallback as same-path `bot.response` traceability evidence. Targeted acceptance-summary coverage passed with `3 passed | 36 skipped`; final `pnpm release:check` exited `0` with `73 passed | 1 skipped` test files and `1191 passed | 8 skipped` tests.
- Downgraded `react_only` face/text fallback delivery now persists same-path `bot.response` / `bot-self` evidence when the executor returns an executed message ID, using the delivered `payload.reaction` text rather than the raw Pi draft. The targeted integration regression first failed because the fallback send had no persisted bot-response row, then passed with `1 passed | 88 skipped`; a follow-up type/lint/targeted gate passed with `3 passed | 86 skipped`, and the then-current full gate exited `0`.
- `bot.response` / `bot-self` persistence now records the actual delivered action `payload.text` after evaluator/tool/fallback modification, while `agent_turns.response_text` continues to preserve the raw Pi draft. The targeted integration regression first failed because the persisted bot-response row contained the Pi draft, then passed with `1 passed | 87 skipped`; the then-current full gate exited `0`.
- Owner/admin `why` output now labels `react_only` action-execution effects as `true_reaction`, `face_message_fallback`, or `silent_reaction_fallback` from durable execution status/message evidence. The targeted spawned-CLI regression first failed because no effect labels were present, then passed with `1 passed | 119 skipped`; the then-current full gate exited `0`.
- A full integration path now proves evaluator-approved true `react_only` delivery through `LetheBotApp`, `SocialDecisionService`, and `ActionExecutor` using `FakeOneBot` with `emojiLike=true`. The regression asserts a recorded reaction side effect, no fake sent message, persisted `action_executions.status="success"` without `executed_message_id`, no `bot.response` / `bot-self` chat row for the reaction-only turn, and FK-clean temp-DB state. Targeted coverage passed with `87 passed`; the then-current full gate exited `0`.
- `FakeOneBot.sendReaction()` now records true reaction side effects separately from fake sent messages. Deterministic tests can inspect/assert sent reactions and verify `reset()` clears reaction history, preserving the difference between true `react_only` delivery and face/text fallback messages without a real QQ/SnowLuma runtime. Targeted fake-gateway coverage passed with `31 passed`.
- Real `OneBotAdapter.getCapabilities()` now reports only implemented delivery capabilities: face/text reaction fallback available, true emoji-like reaction unavailable, and folded-forward group/private/custom-node delivery unavailable. Regression first proved folded-forward capabilities were being reported as `true`; targeted OneBotAdapter coverage then passed with `47 passed`. This keeps real runtime capability claims aligned with the conservative `send_folded_forward` fallback implementation.
- `acceptance:db-summary --require-acceptance-hints` now counts successful `reply_with_tool` executions as delivered reply evidence when they carry an `executed_message_id` and the same-path `bot.response` / `bot-self` row exists. This aligns final aggregate acceptance hints with the already-wired executor/main-turn persistence path for prepared tool-assisted replies. Regression first proved a group `reply_with_tool` flow with bot-response evidence did not count; targeted acceptance-summary coverage then passed. A negative regression also proves conservative `send_folded_forward` downgraded fallback remains excluded from complete acceptance delivered-reply success until real folded-forward node delivery is implemented; combined targeted coverage passed with `38 passed`.
- Main turn handling now persists `bot.response` / `bot-self` rows for newly wired outward action paths that actually deliver a message: successful `reply_with_tool` and downgraded `send_folded_forward` text fallback executions with `executed.messageId`. Regression first proved both sent paths had action execution rows but no persisted bot-response evidence; targeted integration coverage then passed with `86 passed`, and final `pnpm release:check` exited `0` with `73 passed | 1 skipped` test files and `1179 passed | 8 skipped` tests.
- `ActionExecutor` now handles `send_folded_forward` as a conservative fallback path rather than rejecting it as unwired. Real folded-forward node delivery remains unimplemented; when a target and safe prepared `payload.text` exist the executor sends one text fallback and persists a downgraded execution, otherwise it records downgraded silent evidence without a gateway side effect. The same L0 prohibited/evaluator-required guard runs before fallback delivery. Regression first proved `send_folded_forward` was still rejected as not implemented; targeted `send_folded_forward` coverage then passed with `18 passed`, full action-executor coverage passed with `18 passed`, `pnpm typecheck` / `pnpm lint` passed, and final `pnpm release:check` exited `0` with `73 passed | 1 skipped` test files and `1177 passed | 8 skipped` tests.
- `ActionExecutor` now handles `react_only` as a conservative capability-gated side-effect path. It requires `payload.reaction` and `payload.messageId`, prefers true gateway `sendReaction` when `emojiLike` is available, downgrades to a face/text message when `faceMessage` is available, or records downgraded silent evidence when no fallback exists; the same L0 prohibited/evaluator-required guard runs before reaction or fallback delivery. Regression first proved `react_only` was still rejected as not implemented; targeted `react_only` coverage then passed with `15 passed`, full action-executor coverage passed with `15 passed`, `pnpm typecheck` / `pnpm lint` passed, and final `pnpm release:check` exited `0` with `73 passed | 1 skipped` test files and `1174 passed | 8 skipped` tests.
- `ActionExecutor` now treats `reply_with_tool` as a governed outward reply delivery action after PiAdapter / ToolRegistry has already executed and audited the tool path. The executor sends only prepared `payload.text`, records normal `action_executions` evidence, and applies the same L0 prohibited/evaluator-required guards as ordinary replies before any gateway sender call. Targeted action-executor coverage passed with `11 passed`, `pnpm typecheck` / `pnpm lint` passed, and final `pnpm release:check` exited `0` with `73 passed | 1 skipped` test files and `1170 passed | 8 skipped` tests.
- Built-in `group.recent_summary` now provides a read-only current-group context helper. It is registered as `read_context`, group-chat-only, evaluator-bypass, no filesystem/network access, `redacted_full` audit, and `secret_possible` output. The handler reads only bounded current-group `chat_messages`, returns aggregate counts plus sanitized chronological excerpts, labels speakers as `participant_N` or `bot`, omits raw sender/group/message/raw-event IDs and other groups' text, and redacts secret/platform-like fragments before prompt-facing results plus persisted `tool_calls` / `audit_log` evidence. Targeted tool coverage passed with `11 passed`, targeted PiAdapter built-in coverage passed with `44 passed`, full PiAdapter coverage passed with `44 passed`, `pnpm typecheck` / `pnpm lint` passed, and final `pnpm release:check` exited `0` with `73 passed | 1 skipped` test files and `1168 passed | 8 skipped` tests.
- Built-in `memory.disable` now provides a conservative lifecycle mutation surface. It is registered with `modifies_memory` and `evaluatorPolicy=required`, so current PiAdapter execution is rejected until evaluator approval is wired; rejected calls persist redacted `tool_calls` / `audit_log` rows and leave target memory active. The direct handler disables only allowed non-deleted active memory through `MemoryRepository`, returns no durable IDs, allows ordinary actors only for their own eligible user memory, rejects disallowed/proposed/missing requests without mutation, removes disabled memory from retrieval immediately, and records FK-clean revision/audit evidence. Targeted tool coverage passed with `9 passed`, targeted PiAdapter `memory.disable|memory.propose|memory.search` coverage passed with `43 passed`, full PiAdapter coverage passed with `43 passed`, `pnpm typecheck` / `pnpm lint` passed, and final `pnpm release:check` exited `0` with `73 passed | 1 skipped` test files and `1165 passed | 8 skipped` tests.
- Built-in `memory.propose` now provides a conservative proposal-only memory write surface. It is registered with `modifies_memory` and `evaluatorPolicy=required`, so current PiAdapter execution is rejected until evaluator approval is wired; rejected calls persist redacted `tool_calls` / `audit_log` rows and create no memory records. The direct handler creates only `proposed` source-linked memory through `MemoryRepository`, returns no durable IDs, keeps group-chat user proposals same-group scoped, blocks ordinary-user global proposals, and rejects deterministic secret/prohibited content without durable rows. Targeted tool coverage passed with `7 passed`, targeted PiAdapter `memory.propose|memory.search` coverage passed with `42 passed`, full PiAdapter coverage passed with `42 passed`, `pnpm typecheck` / `pnpm lint` passed, and final `pnpm release:check` exited `0` with `73 passed | 1 skipped` test files and `1162 passed | 8 skipped` tests.
- Built-in `memory.search` now provides a product-like read-only tool path through ToolRegistry, PolicyGate, PiAdapter, `tool_calls`, and `audit_log`. It uses governed repository retrieval/search, requires canonical actor identity, respects current private/group/global visibility, omits durable memory/source IDs, coarsens source context, excludes other users' group-derived memory, and records group context in tool audit details. Targeted memory-search coverage passed with `4 passed`, targeted PiAdapter `memory.search` coverage passed with `41 passed`, full PiAdapter coverage passed with `41 passed`, `pnpm typecheck` / `pnpm lint` passed, and final `pnpm release:check` exited `0` with `73 passed | 1 skipped` test files and `1158 passed | 8 skipped` tests.
- `ActionRepository.createExecution()` now returns action-execution downgrade/error/audit diagnostics using the same redacted strings persisted to `action_executions`. Regression first proved the durable row was redacted while the returned `ActionExecutionResult` still exposed raw diagnostics; targeted action repository coverage passed with `3 passed`, action executor coverage passed with `9 passed`, and `pnpm typecheck` / `pnpm lint` passed; final `pnpm release:check` exited `0` with `72 passed | 1 skipped` test files and `1153 passed | 8 skipped` tests.
- The group-flow E2E now captures the Pi runtime input and directly proves the gateway-normalized group identifier reaches both `ContextPack.conversation.groupId` and `actor.groupId`. Targeted E2E coverage passed with `84 passed`, PiAdapter unit coverage passed with `40 passed`, and `pnpm typecheck` / `pnpm lint` passed; final `pnpm release:check` exited `0` with `72 passed | 1 skipped` test files and `1153 passed | 8 skipped` tests. This complements the existing durable context-trace and tool-policy evidence for group identity normalization.
- LetheBot now preserves gateway-normalized `qq-group-*` group identifiers through ContextBuilder, durable `context_traces.group_id`, Pi actor context, and group-scoped tool policy checks. Regression first proved context traces still stored bare numeric group IDs; targeted E2E group-flow coverage passed with `84 passed`, PiAdapter coverage passed with `40 passed`, `pnpm typecheck` / `pnpm lint` passed, and final `pnpm release:check` exited `0` with `72 passed | 1 skipped` test files and `1153 passed | 8 skipped` tests.
- PiAdapter tool audits now include the runtime `groupId` when a group context is present for tool execution or rejection. This preserves owner/admin review evidence for group-scoped tool policy outcomes without changing SQLite schema or exposing allowlist tables to prompts. Regression first proved group-scoped tool audit details omitted `groupId`; targeted PiAdapter coverage passed with `40 passed`, `pnpm typecheck` / `pnpm lint` passed, and final `pnpm release:check` exited `0` with `72 passed | 1 skipped` test files and `1153 passed | 8 skipped` tests.
- `PiAdapter` now threads group context into tool registry/policy boundaries. Group-scoped tools declared with `allowedGroupIds` are exposed and executable only when the current group identifier from `actor.groupId` or `ContextPack.conversation.groupId` matches, and the same actor context is used for `beforeToolCall` checks and wrapped handler execution. Regression first proved matching group-scoped tools were hidden/denied; targeted PiAdapter coverage passed with `39 passed`, registry/PiAdapter coverage passed with `49 passed`, and `pnpm typecheck` / `pnpm lint` passed. Final `pnpm release:check` exited `0` with `72 passed | 1 skipped` test files and `1152 passed | 8 skipped` tests.
- `ToolRegistry.checkPermission()` now enforces declared user/group allow and deny lists as L0 permission dimensions, not only actor class and invocation context. `deniedUserIds` / `deniedGroupIds` take precedence, and non-empty `allowedUserIds` / `allowedGroupIds` conservatively reject missing or non-matching identifiers. Regression first proved a denied user was still allowed; targeted registry coverage then passed with `1 passed`, registry/PiAdapter coverage passed with `46 passed`, related registry/file/Pi policy coverage passed with `17 passed`, `pnpm typecheck` / `pnpm lint` passed, and final `pnpm release:check` passed with `1149 passed | 8 skipped` tests.
- `ActionExecutor` now has an L0 final guard for direct action-decision callers. Outward reply/DM side effects are persisted as rejected and the gateway sender is not called when `riskLevel="prohibited"` or when an evaluator-required outward action lacks `evaluatorPassed=true`; current `silent_store` / `silent_summarize_later` audit/control paths remain available for rejected/downgraded flows. Regression first proved both bypass cases called the sender; targeted executor coverage then passed with `2 passed`, action repository/executor coverage passed with `12 passed`, evaluator downgrade/rejection compatibility coverage passed with `4 passed`, `pnpm typecheck` / `pnpm lint` passed, and final `pnpm release:check` passed with `1148 passed | 8 skipped` tests.
- `MemoryRepository.retrieve` and `MemoryRepository.search` now exclude expired active memories at the repository layer, preventing stale lifecycle-expired records from entering ordinary retrieval/search before context assembly. Regression first proved expired active records were returned; targeted expiration coverage then passed with `2 passed`, full memory repository/context/retrieval coverage passed with `51 passed`, `pnpm typecheck` / `pnpm lint` passed, and final `pnpm release:check` passed with `1146 passed | 8 skipped` tests.
- `MemoryRepository.create` now has an L0 final guard for direct group-chat-derived user-memory writes. Unsafe ordinary visibility requested for `source_context` beginning with `group_chat` and `scope="user"` is stored as `same_group_only`, with the policy adjustment recorded in revision and audit rows. This prevents repository bypasses from turning group-derived user evidence into private/public ordinary memory. Targeted memory repository coverage passed with `28 passed`; proposal/extraction/retrieval coverage passed with `42 passed`; final `pnpm release:check` passed with `1144 passed | 8 skipped` tests.
- `pnpm acceptance:db-summary -- --db=<path> --require-acceptance-hints` now requires complete private/group DB evidence to be selected-turn-linked, QQ raw-event-linked, delivered-reply-backed, persisted as same-path `bot.response` / `bot-self`, and targeted by the triggering inbound row.
- Private targeting is proven by `conversation_type="private"`.
- Group targeting is proven by `mentions_bot=1` on the triggering normalized chat row plus the same gateway-normalized `qq-group-<digits>` group ID carried through trigger chat, context trace, and bot-response evidence, preventing ordinary or group-scope-less group rows from satisfying group `@bot` acceptance DB hints.
- Memory governance evidence now requires at least one complete acceptance flow whose durable `context_pack_id` selected context chooses an active, source/revision-linked, non-secret/prohibited memory with a usable durable source evidence row, visible in that same flow context, and scoped to that same sender, group, conversation, or public/system boundary.
- Latest targeted selected-memory usable-source coverage passed with `2 passed | 43 skipped`, full local acceptance evidence coverage passed with `45 passed`, and final `pnpm release:check` passed with `1236 passed | 8 skipped` tests. Previous selected-memory resolvability, scope/actor, visibility, and group-scope coverage remain in the full local acceptance evidence file.

## Healthy Direction

Continue to optimize for:

- strict module boundaries: gateway, ingestion, memory, context, Pi, policy, executor, tools, workers, governance;
- auditable raw event -> chat message -> turn -> context -> decision -> execution traces;
- governed memory with source/revision/audit/lifecycle evidence;
- redaction-first operator and acceptance evidence;
- deterministic credential-free tests by default;
- opt-in live provider/platform evidence only when explicitly authorized.

## Current Risks / Gaps

1. Worktree remains dirty with 58 tracked dirty paths and 19 untracked paths.
2. Real SnowLuma/QQ acceptance is still missing.
3. Local evidence validators are ready, but `/tmp/lethebot-acceptance-evidence.md` has not been filled from live runtime and validated in complete mode.
4. Real provider/evaluator/tool-loop evidence remains opt-in and incomplete.
5. Multi-hour/day live soak and final packaging/installer proof remain incomplete.
6. Scratch/untracked cleanup requires explicit user authorization.

## Recommended Priority Order

1. Keep deterministic gates green while reducing recovery friction.
2. Preserve historical detail in archives and keep active status docs concise.
3. When authorized, run real SnowLuma/QQ private and group exact-`@bot` acceptance using the documented redaction-first evidence flow.
4. If live runtime is unavailable, continue only with high-value deterministic DB-backed gaps, not endless low-value parser/redaction variants.
5. Do not call the project production-ready until both acceptance validators pass against real controlled local evidence.
